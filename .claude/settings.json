{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qE 'git\\s+(commit|merge|rebase)' && [ \"$(git branch --show-current)\" = 'main' ]; then echo 'BLOCKED: main ブランチへの直接コミットは禁止です。feature ブランチを作成してください。' >&2; exit 2; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -q '\\.rs\"'; then cargo clippy -p snotra-core -p snotra -p snotra-settings --message-format short -- -D warnings 2>&1 | head -20; fi"
          },
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -q 'snotra-core.*\\.rs\"'; then cargo test -p snotra-core --lib 2>&1 | tail -5; fi"
          },
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qE '\\.(ts|tsx)\"'; then cd /c/workspace/Snotra && npm run typecheck 2>&1 | head -30; fi"
          },
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -qE '(tauri\\.conf\\.json|config\\.toml)'; then echo 'WARN: 設定ファイルの変更です。Windows 互換性を確認してください。' >&2; fi"
          }
        ]
      }
    ]
  }
}
