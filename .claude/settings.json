{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -q '\\.rs\"'; then cargo check -p snotra-core -p snotra --message-format short 2>&1 | head -20; fi"
          }
        ]
      }
    ]
  }
}
