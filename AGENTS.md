# AGENTS.md

このリポジトリで Claude Code が作業するときの開発プロセスガイド。`*.md` で意図を管理し、コードで実装事実を管理する。Claude Code 固有の運用（スキル・フック・コミュニケーション原則）は `CLAUDE.md`、実装前チェックは `.claude/rules/` を参照。

## 3層分担（責務分離）

- 第1層（意図管理）: `SPEC.md` と `AGENTS.md`
- 第2層（実装事実）: `snotra-core/src/*.rs`, `src-tauri/src/*.rs`, `ui/src/**`
- 第3層（整合運用）: 挙動変更を伴う変更では、意図（`SPEC.md`）と実装を同時に整合させる

## ドキュメント参照

- 意図（仕様）: `SPEC.md`
- プロダクト知識（アーキテクチャ・横断パターン）: `docs/architecture.md`
- 開発原則・デバッグ指針（汎用）: `docs/development-principles.md`
- ビルド・テストコマンド: `docs/build-commands.md`
- モジュール固有の不変条件: 各サブディレクトリの `CLAUDE.md`（`snotra-core/`, `src-tauri/`, `ui/`, `snotra-settings/`）
- 設定 UI デザインガイドライン: `snotra-settings/SETTINGS-DESIGN.md`（デザイントークン・Fluent タイポグラフィ・レイアウト規約）
- 実装前チェック: `.claude/rules/`（該当ファイルの読み取り時に自動配送）

## 開発ワークフロー

実装・修正に着手する際は、原則として以下のステップに沿って進行すること。

**サイクル開始前**: `RETROSPECTIVE.md` を読み、前回サイクルで生まれたバグのパターン・経路を確認する。同じ構造的ミスを繰り返していないか意識しながら以下のステップを進める。

0. 「バグ」か「仕様変更」かを判定する
   - バグ: `SPEC.md` の意図に合わせてコード修正
   - 仕様変更: `SPEC.md` → コード → ドキュメントの順に更新
   - **「fix」でも文書化された挙動を変えたら仕様変更**: バグ修正のつもりでも、`SPEC.md` 記載のフロー・IPC 契約・状態遷移を変えるなら仕様変更として `SPEC.md` を同期する。「fix」というコミット種別は SPEC 同期を免除しない
1. 要件（受け入れ条件）をテスト可能な形で確定する
2. 影響範囲（読む/触る/触らない）を列挙する
   - **対称コードパス確認**: 変更対象に対称ペア（`clicked`/`double-clicked`、`show`/`hide`、`enter`/`exit` 等）が存在する場合、それぞれへの適用要否を明示的に判断して記録する。検証手順は `/symmetric-check`
   - **git マージ状態の確認は `git diff` で行う**: `git log` のコミットメッセージはマージ済みかどうかの確認に使わない。実際のコード差分（`git diff main...HEAD` 等）で確認する
   - **関数使用箇所検索**: 新規定義・変更した関数が使える既存コードパスを grep で列挙し、適用の要否を判断する（関数を作ることとその関数を使うことは別の判断）
   - **同一パターン全コードパス検索**: バグを発見したとき、そのバグパターン（根本原因）をコードベース全体で検索し、同一パターンの別箇所を列挙する（修正完了時の完了ゲート版は `docs/development-principles.md` のデバッグ・バグ修正節）
   - **網羅性が要件のタスクは「枠組みの独立」な再導出で補完する**: 全文監査・全箇所改名など「漏れなく全部見つける」が要件のタスクでは、単一の分解枠組みは自身の盲点を構造的に継承する——実行の独立より枠組みの独立が見落としクラスに効く（#404）。機構は `/plan-review` Step 2b（独立再導出 + 差分。**常に実施**、経緯は #495 とスキル本文）
   - **列挙の真実源はツール自身に問う（glob の意味論はツールごとに違う）**: `git` pathspec の `**/` は 1 段以上を要求、TypeScript の `exclude` の `**/` は 0 段にもマッチ、`git ls-files` はブレース `{ts,tsx}` を展開しない（#471 で深さ 0 の実在ファイルを列挙から落とした）。型検査の対象なら `tsc --listFilesOnly`、追跡ファイルなら `git ls-files <dir>` の全件フィルタで、当のツール自身に列挙させる
   - **順序に依存する参照は、削除・挿入で静かに腐る**: 章番号・ルール番号のような**序数**で他所から参照されているものを削除・挿入するときは、参照側を全部数え上げる——概念ラベルで参照している箇所には当の語の grep では到達しない（#482 では番号参照する OPEN issue 5 件を独立再導出だけが検出した）。**直すときは番号ではなく名前で参照し直す**
   - **変更なしの根拠明示**: 「変更なし」と判断するとき、影響するケースを列挙して根拠を裏付ける
   - **キー/識別子形式の変更は「新規記録」「既存データ移行」「外部参照 API」の 3 者で同時に揃える**（具体手順: history キーは `snotra-core/CLAUDE.md` のキー正規化チェックリスト、config キーは `/plan-review` の 4 点セット）
   - **識別子の改名は「シンボル単位」で列挙し「概念」で分類する**: 名前↔概念は多対多——キー文字列だけの grep は間接命名（accessor 経由・派生 signal・概念ラベル）を取りこぼし、同名別概念を誤検出する。accessor/型を改名したら下流の **compile-fail を「改名検出器」として使う**（`cargo build -p <下流 crate>` で呼び出し元の漏れが必ず出る＝Phase 完了ごとの mid-verify）
   - **乖離の解消は「削除」も解候補に入れ、削除の安全性は compile/typecheck で裏取りする**: issue が提示する「片方に揃える」二択を鵜呑みにせず、まずコードで前提を裏取りする——一方が呼び出し元の無い死蔵コードなら、削除が乖離の根本を断つ最小解になりうる（#409。選択肢空間は前提の裏取りで広がる）。削除の安全性は静的 grep + 独立再導出 + **compile/typecheck を「死蔵検出器」として使う**（live import が残れば module-not-found で落ちる）の独立経路で裏付ける
   - **モジュール構成ドキュメントの同期**: 新規ファイル（`.rs` / `.ts` / `.tsx`）を追加・削除した場合は、対応するサブディレクトリの `CLAUDE.md` のモジュール構成セクションを「触る範囲」に含める
   - **件数・上限パラメータの変更は下流全段に追う**: バックエンドが返す件数 N や上限値を変更したとき、IPC シリアライズ・フロントエンドのレンダリング・アイコン取得など下流の各ステージで N が変わることの影響を列挙する
   - **`docs/` の更新要否を判断する**: 変更がアーキテクチャ・横断パターン・ビルドコマンド・開発原則に影響する場合、対応する `docs/` ファイルを「触る範囲」に含める
   - **機能削除・ルート変更時は E2E テストも影響範囲に含める**: フロントエンドの分岐削除や IPC ルート変更は E2E テストの前提を壊す可能性がある。`e2e/` ディレクトリで関連キーワードを grep し、テストシナリオの前提が維持されるか確認する
   - パス起点の事前調査（SPEC 番号整合・as-built / UI レイアウト崩れ / 設定の後方互換 / 比較関数・最適化 / Win32 テスト前提）は、対象ファイルを触ると `.claude/rules/` が自動配送する
3. 事前調査（レビュー未然防止）を実施する
   - 挙動変更なし前提は代表入力/出力をベースライン化して差分検証
   - 境界条件を列挙し最低1件ずつ検証ケースを用意
   - **リソース管理は生成/破棄ペアで計画する**: `listen()`・`ResizeObserver`・`URL.createObjectURL`・`Child` プロセスなど「戻り値がライフサイクルを持つもの」を生成する際は、破棄の「場所・構造・理由」まで同時に記述する。検証手順は `/symmetric-check`（リソースライフサイクル対称）
   - **状態フラグも真偽ペアで計画する**: フラグを `true` にする操作は「誰が・どの条件で `false` に戻すか」をセットで設計する——戻す経路が無いと、失敗時にシステムが回復不能な状態に固まる（Rust の具体則・`#[must_use]` の使い方は `.claude/rules/src-tauri.md`）
   - **これまで無意味だった状態に意味を与える変更は、その状態に到達する全経路を列挙する**: 「無出力 = 合格」「空 = 未設定」のような意味づけは、同じ状態を無害なノイズから**積極的な虚偽**に変える——意味が成り立たない経路をすべて塞ぐ（#471: hook の沈黙に「合格」を与えたとき、タイムアウト・起動失敗・出力溢れが沈黙経路として残っていた）。**分類を 1 つ足したときは、隣接分類への到達経路も再列挙する**——逃げ道は塞いだ扉の隣に現れる（#489）
   - **安全網が「効いている」ことは、故障注入で一度は実測する**: 「hook が自動実行する」「CI が担保する」は書かれた時点の期待であって測定結果とは限らない。依拠して手順を省く前に、故意に壊して検知されることを 1 度確かめる（#471 では hook の出力が一度も届いていないのに「届く」という記述の上に PR が積まれていた）
   - **故障注入では、稼働中のガードを弱めない——複製に変異を当てる**: 判定がソース述語でも実 git 操作でも規則は同じで、**変わるのは複製の作り方だけ**である（述語はメモリ上へコピーして変異、git 操作は hook を一時ディレクトリへコピーし使い捨て repo の `core.hooksPath` を向ける。手本は `.githooks/githooks.test.mjs`）。「判定が〜なら」のような条件節を免除と読んではならない（#504: 条件節を免除と読み、ライブの `pre-commit` を無害化して main 保護が数十秒消えた）。敵対操作を撃って拒否を観測する類はガードの**行使**であり、弱めていないので対象外（#482）
   - **安全網が「規範」（ドキュメント・スキル・チェックリスト）である場合、故障注入とは「それを回避しようとする読者」である**: 規範は機構ではないので実行して測れない——読者を演じるサブエージェントに文言だけを読ませ、抜け道を探させる。**1 巡では終わらない**（逃げ道は塞ぐたびに隣へ移る、#489）。**読者は「手を抜く読者」と「規則を全部守る読者」の 2 クラス要る**——忠実に従う読者が誤る経路は、手を抜く読者からは見えない（#488）
   - **故障注入は停止条件を先に決める**: 合格条件（通してはならないシナリオ）の列挙・上限巡数・上限で残ったものは「受容する穴」と明記する、まで計画に書く。決めなければ無限反復にも恣意的停止にもなる（#488）
   - **検査の入力集合を、具体対象で検算する**: 一覧・索引・サブエージェントの報告を根拠に「全部」「一致した」と言うときは、(1) 守りたい対象を 1 件名指しし、それが実際に読む入力に現れることを示し、(2) 判定対象外が同じ入力に混じらないことを示す（両方向）。**正常時に一覧へ出ない対象は、その一覧だけを見る検査からは永遠に見えず**（#488, #489）、余計なものが入れば誤爆する（#482）。母集団は**派生した参照集合ではなく、起きた事実**から取り（`closingIssuesReferences` ではなく `gh issue list --search "closed:>=<mergedAt>"`）、派生集合（`description` 等の要約）で関連性を判断しない
   - **全称表現は前提条件とセットで書く。書けないなら書かない**: 「唯一の」「すべての」「全経路」は、実装より強い主張になった瞬間に嘘になり、**規範を忠実に守る読者を誤りへ導く**（#488）。全称量化子を書いたら、その前提を同じ文へ書き出す。書き出せないなら、その主張は成り立っていない
   - **新設した規約は、既存の全事例に当てて検算してから書く**: 規則を新しく書いたら、その規則が支配する既存の事例をすべて列挙して当てる——1 件でも破っていれば、規則か事例のどちらかが誤っている（#476: 新規約が既存の clippy フラグ差で初日から違反していた）。「全称表現は前提条件とセットで書く」の実行手順版——前提を書けるかどうかは、既存事例に当ててみて初めて分かる
   - **照合は真実源に対して行う。派生コピー同士の一致は、完全性の証拠にならない**: N 箇所の写しが同時に古くなれば、相互照合は何も検出しない（#500, #476）。照合先を選ぶときは「これは事実か、事実の写しか」を問う（`Cargo.toml` の `members` は事実、`-p` のリストは写し）。**そして真実源を変えるときは、写しを grep で数え上げてから直す**——写しの数を知らずに直せば、必ず片方だけが更新される
   - **同じ表層形が複数の概念を担っていないか、消す前に分類する**: 重複排除の対象は**概念**であって文字列ではない——#500 の `-p` には「全メンバーの写し」（`--workspace` で機構的に消せる）と「編集した crate → そのテスト」の写像（消せない）が同居し、issue 本文自身が両者を混同していた。改名時の「名前↔概念は多対多」と同じ罠が、削除・共通化にも現れる
   - **カナリアで守るのは「沈黙する経路」だけでよい**: 検知器を足す前に、**その失敗は放っておくと loud に落ちるか**を確かめる——loud に落ちるものに見張りを付けるのは、コストを払って何も買っていない（#497, #500。hook 発火とカナリアの運用はルート `CLAUDE.md` のフック節）
   - **計画に書いた判定ロジックは、実装前に実行して測る**: 正規表現・位置計算・述語を計画へ書いたら、`node -e` 等で代表入力を通し、計画が主張する allow/block と一致するか確かめる（#482 では擬似コードが目的の入力を block していた）。**サブエージェントの実測も一次証拠として扱わない——判定の中核は自分で測る**（#482, #471）
   - **初回フローとガードの相互作用を検証する**: 正常運用時の早期リターンガード（`if indexing { return }` 等）が、初回起動・初期化フロー・エラーリカバリなどの特殊パスを意図せず阻害しないか確認する。ガードを追加/変更したときは「このガードが true になる全コンテキスト」を列挙する（UI 状態の検証手順は `/state-check`）
   - **サンプルコードに構造の理由を付記する**: リソース管理・非同期処理の配置が問われる箇所に「なぜこの構造か」をコメントする
   - **キャッシュ再利用は述語の単調性を証明してから実装する**: 前回結果を再利用するロジック（incremental search、メモ化等）を追加・変更するとき、全述語の単調性（今回の結果集合 ⊆ 前回の結果集合）を `/cache-check` で検証する。非単調な変換（例: ローマ字→かな）は bool フラグではなく実際の値を保持して `starts_with` 等で比較する
4. 失敗するテスト（または最小再現）を追加する
   - **既存テストを改名・転用するとき、元のテストが証明していた不変条件を孤立させない**: 改名は内容ではなく「どの命題を証明していたか」も追跡する。転用で失われる不変条件は別テストで補う
5. それが落ちることを確認する（Red）
6. 最小実装で通す（Green）
7. テストが通るまで 6 を反復する
8. 変更後の検証を実行する（スキップ不可）
   - 変更したファイルの種類に応じて、`docs/build-commands.md` の「変更後の検証チェックリスト」のカテゴリ A〜E から該当するものをすべて実行する
   - コマンド本体は SSOT である `docs/build-commands.md` を参照（このリストとの二重メンテを避けるため、コマンド文字列はこのファイルに書かない）
9. 報告は「追加/更新テスト名 + 検証した不変条件」を必ず含める

## 環境制約

- Agent `isolation: "worktree"` は使用可（worktree は `.claude/worktrees/agent-<id>/` に作られ、パス破壊は起きない／Git 2.54.0.windows.1 で検証済み）。変更なしの worktree は自動 cleanup されるが、**ファイルを変更した worktree は成果保全のため自動削除されず残る**ため、`git worktree remove --force <path>` + `git branch -D worktree-agent-<id>` で手動 cleanup する（`.claude/worktrees/` は `.gitignore` 済み＝誤コミット防止）
- **並列エージェント委譲はファイル境界で衝突を予測してから行う**: 同一ファイルに触りうるタスクは直列化するかマージ順を決めて委譲する。境界の見積もりは issue 記載の対象ファイルだけでなく「実装中に踏み込みうる隣接ファイル」まで含める（#439 と #435 の conflict 実績。リベース解決はコンテキストを保持した実装エージェント本人に依頼するのが最短）
- **委譲はコンテキストを継承しない**: メインエージェントの system prompt にしか無い事実（メモリ領域の絶対パス等）は、サブエージェントのプロンプトへ**明示的に渡す**——渡し忘れると、サブエージェントは見えないものを「無いもの」として報告する（#489）。あわせて、**`allowed-tools` はインライン実行のスキルを拘束しない**（実測）——frontmatter に `Agent` が無いことを根拠に「このスキルは委譲しない」と推論してはならない。また、**委譲した検査が対象を読む時刻は制御できない。検査対象を変更しながら検査を走らせない**（#489。スキルのステップ順序には理由がある）
- **長時間の委譲タスクは中断を前提に設計する**: セッションリミット・API エラーでエージェントは途中終了しうる（worktree が消え未コミット作業が失われたケースあり）。大きなタスクは Phase 分割し「各 Phase の検証 green 後にコミット」を指示に含める（#431）
- モジュール固有の不変条件・TDD ルールは各サブディレクトリの `CLAUDE.md` を参照（実装前チェックは `.claude/rules/` で自動配送。Win32 依存モジュールをユニットテスト前提にしない等も含む）

## RETROSPECTIVE.md の運用

- **更新タイミング**: サイクル終了後（実装・レビュー・追加修正まで完了したとき）
- **更新方法**: 上書き（追記しない）。**フォーマット**: 「よかったこと・伸びしろ」の2セクション構成
- **不変条件 — 持続タスクを置かない**: `RETROSPECTIVE.md` は上書きファイルであり、サイクルを越えて生き残る情報を入れてはならない。上書きの**前に**、教訓は `AGENTS.md` / 各 `CLAUDE.md` / スキルへ抽出し、実行タスクは PR 本文チェックリスト（PR 内で閉じるもの）か GitHub issue（サイクルを越えるもの。迷ったら issue）へ振り分ける
- 更新手順・振り分けの詳細・サイクル末 health-check の実行責任は `/retrospective`（`.claude/skills/retrospective/SKILL.md`）を SSOT とする
