# AGENTS.md

このリポジトリで Claude Code が作業するときの開発プロセスガイド。`*.md` で意図を管理し、コードで実装事実を管理する。Claude Code 固有の運用（スキル・フック・委譲・コミュニケーション原則）は `CLAUDE.md`、実装前チェックは `.claude/rules/` を参照。

**注意事項は「トリガー」に括り付けてある。** すべてを憶えておく必要はない——特定ファイルを触れば `.claude/rules/` が、特定の局面では下の「条件別チェック」表が、必要な検査をその場で示す。常に頭に置くのは、ワークフローの流れと「検証の作法」の数点だけでよい。

## 3層分担（責務分離）

- 意図は `SPEC.md` と本ファイル（第1層）、実装事実はコード（第2層）。**挙動を変える変更では、両者を同じ変更で整合させる**（第3層）

## ドキュメント参照

- 意図（仕様）: `SPEC.md`
- プロダクト知識（アーキテクチャ・横断パターン）: `docs/architecture.md`
- 開発原則・デバッグ指針（汎用）: `docs/development-principles.md`
- コメント規約（rustdoc / TSDoc の様式・粒度・定型ラベル）: `docs/comment-guidelines.md`
- ビルド・テストコマンド: `docs/build-commands.md`
- モジュール固有の不変条件: 各サブディレクトリの `CLAUDE.md`（`snotra-core/`, `snotra-egui-runtime/`, `src-tauri/`, `snotra-settings/`）
- 設定 UI デザインガイドライン: `snotra-settings/SETTINGS-DESIGN.md`（デザイントークン・Fluent タイポグラフィ・レイアウト規約）
- 実装前チェック: `.claude/rules/`（該当ファイルの読み取り時に自動配送）
- 意思決定記録（否定の知識＝なぜ B を却下したか）: `docs/adr/ADR-<slug>.md`（否定の知識が生じた決定のみ・#593。連番を振らない・#812）

## 開発ワークフロー

**サイクル開始前**: `RETROSPECTIVE.md` を読み、前回サイクルで生まれたバグのパターン・経路を確認する（同じ構造的ミスの再発を防ぐため）。

1. 「バグ」か「仕様変更」かを判定する
   - **判定はラベルの意味ではなく 2 つの参照で決まる**: `SPEC.md` に当該挙動の記述があるか、その記述に**合わせる**のか**変える**のか
   - バグ（記述に合わせる）: `SPEC.md` の意図に合わせてコード修正
   - 仕様変更（記述を変える）: `SPEC.md` → コード → ドキュメントの順に更新
   - **「fix」でも文書化された挙動を変えたら仕様変更**: バグ修正のつもりでも、`SPEC.md` 記載のフロー・状態遷移を変えるなら仕様変更として `SPEC.md` を同期する。「fix」というコミット種別は SPEC 同期を免除しない
2. 要件（受け入れ条件）をテスト可能な形で確定する
3. 影響範囲（読む/触る/触らない）を列挙する
   - **git マージ状態の確認は `git diff` で行う**: `git log` のコミットメッセージはマージ済みかの確認に使わない。実際のコード差分（`git diff main...HEAD` 等）で確認する
   - **バグ発見時は同一パターン全コードパス検索を行う**: そのバグパターン（根本原因）をコードベース全体で検索し、同一パターンの別箇所を列挙する（完了ゲート版は `docs/development-principles.md`「デバッグ・バグ修正」）
   - **「変更なし」と判断するときは、影響するケースを列挙して根拠を裏付ける**——列挙の完了は件数ではなく母集団が決める（`docs/development-principles.md`「列挙の完全性」）
   - 下の**条件別チェック**表で、変更の種類に応じた検査へ振り分ける
4. 事前調査（レビュー未然防止）を実施する
   - 挙動変更なし前提は代表入力/出力をベースライン化して差分検証する
   - 境界条件を列挙し最低1件ずつ検証ケースを用意する
   - **リソース管理・状態フラグは生成/破棄・真偽のペアで計画する**: `listen()`・`Child` プロセス・`AtomicBool` など「戻り値や状態がライフサイクルを持つもの」は、破棄・`false` 復帰の「場所・構造・理由」まで同時に記述する（戻す経路が無いと失敗時に回復不能な状態に固まる。Rust の具体則は `.claude/rules/src-tauri.md`、検証は `/symmetric-check`）
   - 変更の種類に応じた事前調査は下の**条件別チェック**表と `.claude/rules/`（対象ファイルを触ると自動配送）が示す
5. 失敗するテスト（または最小再現）から実装する — 落ちることを確認し（Red）、最小実装で通し（Green）、通るまで反復する
6. 変更後の検証を実行する（スキップ不可）
   - 変更したファイルの種類に応じて、`docs/build-commands.md`「変更後の検証チェックリスト」のカテゴリ A〜F から該当するものをすべて実行する
   - PostToolUse hook が自動実行する検査は**沈黙 = 合格**（失敗時のみ会話に届く。読み方は `CLAUDE.md`「フック」、発火の一覧は `docs/hooks.md`「PostToolUse（post-edit.mjs）の発火一覧」）。ただし沈黙が「合格」を意味するのは検査が割り当てられたファイルだけである
   - コマンド本体は SSOT である `docs/build-commands.md` を参照（このリストとの二重メンテを避けるため、コマンド文字列はこのファイルに書かない）

## 条件別チェック（トリガー → 参照先）

変更が下表のトリガーに当たるなら、その参照先の検査・手順を適用する。skill はその局面で起動し、`.claude/rules/` は対象ファイルを触れば自動で配送される。

| トリガー（この変更・局面に来たら） | 参照先 |
|---|---|
| 対称ペア（clicked/double-clicked・show/hide・enter/exit・生成/破棄・フラグ真偽）を変更 | `/symmetric-check` |
| UI モード・ガード条件を追加/変更（初回フローとガードの相互作用の確認を含む——初回起動・初期化パスを意図せず阻害しないか） | `/state-check` |
| 重複した読み・冗長に見える状態を束ねる/消す | 各箇所について「**後で**読まれる/立つことに依存していないか」を 1 行ずつ書き出してから着手（#671 PR A′: 到達しないはずの stale フラグが再表示を防いでいた・#673 PR B: 読みは前へ出せるが適用は動かせない箇所が 2 件） |
| 永続形式・識別子/キー形式を変更 | `/persistence-check`。history キーは `snotra-core/CLAUDE.md`、config キーは `/plan-review` の 4点セット（新規記録・既存移行・外部参照 API を同時に揃える） |
| 関数・型を新規定義／改名／導入 | 呼び出し元は LSP ツールの findReferences で列挙する（作ることと使うことは別判断。**grep は文字列一致ゆえ同名の別物を拾い、re-export 経由を落としうる**——LSP の無い環境でのみ grep へ落とす）＋ `/dry-check`。改名・**旧 API の削除**は下流の **compile-fail を「移行漏れ検出器」**に（`cargo build -p <下流 crate>`）。新 API の導入と呼び出し点の移行は **1 タスクに束ねる**——`-D warnings` 下で未使用の新 API は `dead_code` で落ち、旧 API を残せば導出が 2 箇所になる |
| worker spawn・channel・フレーム drain・Tauri listener・スレッド/窓をまたぐ共有状態・フレーム内 live-read・paint 後の遅延処理を追加/変更、または async 関数を追加/変更 | `/race-check` |
| 網羅性が要件（全文監査・全箇所改名・コンパイラを持たない機構の廃止） | **その一覧の母集団を誰が知っているか**から始める（`docs/development-principles.md`「列挙の完全性」）＋ `/plan-review`「Step 2b」（独立再導出） |
| 検査・検証手段を新設する、またはどの手段で保証するか決める | `docs/development-principles.md`「検証の層と、層と層の隙間」——手段ごとの死角の表。**穴は層の内側ではなく境界に空く**（層 A の出力が層 B へ届いているかは、どの層も見ない） |
| 調査・測定のための一時的な足場（script・workflow・env フック・**製品コード内の計装**）を新設**または撤去** | **新設時**: 撤去条件（どの issue が閉じたら消すか）と撤去対象の列挙を、**その成果物自身の doc へ書く**——PR 本文は merge 後に読まれないため、消す合図を自分で持たない足場は残り続ける（#872 の repro-pester-flake.ps1 の .NOTES が実際に撤去を駆動した。撤去済み）。**撤去時**: その計装が出した値で**引用されうるものが `PERFORMANCE.md` へ出所つきで着地しているか**を確かめてから消す——撤去は出所が永久に失われる瞬間であり、着地し損ねた値は写し同士が支え合うだけになる（#1032 の `40〜95 ms` が実際にそうなった・`docs/adr/ADR-measurement-canon-in-code-doc.md`）。書き方は `PERFORMANCE.md`「この文書へ記録するときの規約」 |
| ガバナンス機構**自身**の配置を変える（判定ファイルの移動・母集団の切り出しの変更・走査元の追加）、またはそれを守る機構を足そうとしている | `.claude/rules/safety-nets.md`「効いていることは、フォールトインジェクションで一度は実測する」で測り直す。**足す前に「壊れたとき緑が緑のまま推移するか」を問う**——推移しないなら置かない（気づく契機が残るなら、機構ではなく上の引き金で足りる）。段を増やす判断の正本は `docs/adr/ADR-governance-meta-demotion.md` |
| セーフティネット（hook・CI・`.githooks/`・`.claude/settings.json`・rules・skills・規範）を新設/変更 | `.claude/rules/safety-nets.md`（rules・skills までは対象を触ると自動配送。規範文書＝ルート `CLAUDE.md` / `AGENTS.md` 等は自動配送されないため手動参照） |
| 機構・層・ファイル群を**撤去する** | **削除したファイル名と、その層が持ち込んだ語彙を `git grep` で数え上げ、残った出現を「撤去を描写している / 撤去されたものが在る前提で書いている」へ振り分ける**（対象は `scripts/` を含む生きた層すべて。`grep -r` は `.claude/worktrees/` の追跡外を混ぜる）。**「撤去した識別子の残存 0 件」を根拠にしない**——それはコード識別子しか見ておらず、散文の語彙・**実体が別名で在る旧名（幽霊識別子）**・生きた層から手順として委譲された凍結文書は射程の外である（#1155 で 9 群が残っていた） |
| ガバナンス文書（`*.md`・スキル表・モジュール索引・rules・workflow）を変更、または `.rs` のコメントの見出し参照（正準形）とその参照先を変更 | `npm run governance:check`（`docs/build-commands.md` カテゴリ F・#587。PR では CI の governance-check job が常時実行）。**編集時にガバナンスの不整合の reminder が出ることがあるが、それが鳴らないことを「緑」と読まない**——見るのは編集した文書 1 枚に**帰属する分**だけであり、**どの判定がどこまで見るかは母集団ごとに違う**（#1139・射程は `docs/hooks.md`「検査ではない reminder」） |
| 文書に事実の写しを増やす変更 | 正本を 1 か所に定め他は参照へ（分担の記録は `docs/superpowers/specs/2026-07-19-doc-governance-design.md` §1）。**同じ事実を 2 か所以上へ書こうとしている時点で、それが写しである**——取りこぼすのは**写しを直す当のコミット**が典型（#977 で 5 枚へ全文を書き 6 枚目を落とした。しかもその 6 枚目は規範の**根拠**として使われており、前提が偽になって根拠ごと腐った）。**数え上げの母集団に PR 本文を含める**——squash で main の commit message になるのにリポジトリの grep には入らない（#1056 で 5 枚直して 6 枚目がそこだった） |
| ファイル（`.rs`）を追加/削除 | モジュールの責務は各ファイルの `//!` に書く。`CLAUDE.md` のモジュール構成節はファイル名の索引を保つため、追加時にファイル名行を足し削除時に消す（責務散文は `//!` を正本とする・#562）。**索引と `mod` 宣言は別々に機構が見る**——索引を書いても `mod` 宣言を忘れれば `governance:check` が赤にする。**この 2 つは編集時の見え方が非対称である**（#1139）: 索引漏れは編集直後に reminder が鳴るが、**`mod` 忘れは今も `governance:check` だけが赤にする**（cargo も rust-analyzer も報せない。機序と残余は `G-module-linkage` の注釈が正本・#1085）。**索引の reminder が鳴らなかったことを「`mod` も足りている」と読まない** |
| 機能削除・trace イベント名／hotkey 登録・表示経路の変更 | `scripts/smoke-egui.ps1` と `docs/build-commands.md` の smoke 前提（trace イベント名・hotkey）が壊れないか確認する（WebView2 e2e は #532 SU7 で撤去済み） |
| `Option` / フラグ / enum variant など**どの分岐が選ばれるかを決める値**の出所を変更 | **diff に現れない下流を 1 段辿り「この値で初めて走る行」を列挙する**——変更で初めて生きた組み合わせは、既存の検査からも**その変更のために新しく書いた検査からも**漏れる（#977: `extend_cached_masks` もその呼び出し行も 1 行変えていないのに、`None` → `Some` で初めて走った）。差分レビューは変わった行が無いので原理的に見つけにくい。検知器を置き、**呼び忘れを再現する変異で落ちることまで**確かめる |
| 件数 N・上限パラメータ・**導出の入力**を変更 | 下流全段に影響を追う。**局所的な正しさ（変化の向きが正しいこと）は判断材料にならない——変わった値を誰が消費するかを辿る**。とくに**その値を永続化する消費者**（#755/#801: クランプ入力を実高にした結果、hide がずれた位置を保存して恒久化した） |
| レビュー指摘へ修正（fix-forward）を当てた | 指摘を出した枠組み（check スキル・code-reviewer・レンズ）を**修正差分にも**再実行してから閉じる——修正は指摘箇所へ注意が集中し、周辺に新しい誤りを生む（2026-08-02 に同型 3 件。修正差分は再実行しない限り誰の検算も受けない）。**「解消した」の判定は再実行の結論を受け取るのではなく、指摘を見つけた道具で自分で測る**——#984 でラウンド 3 が「当該段落ごと消えた」と報告したのを検算せず解消表へ転記し、別枠組みの再実行が `grep -c` で現存を実測して訂正した |
| アーキ・横断パターン・ビルド・開発原則に影響 | 対応する `docs/` を「触る範囲」に含める |
| 各言語ファイルを編集 | `.claude/rules/`（読取で自動配送・パス起点の事前調査） |
| サブエージェントへ委譲する・worktree を使う | ルート `CLAUDE.md`「サブエージェント委譲と worktree」 |

## 検証の作法（全タスク共通）

特定のファイルにも局面にも紐付かず、**書く・判断するたびに**効く横断原則。抱える山ではなく、常に頭に置く数点。

- **全称表現は前提条件とセットで書く**（「唯一の」「すべての」「全経路」など）。**書けないなら書かない**——実装より強い主張になった瞬間に嘘になり、規範を忠実に守る読者を誤りへ導く。新設した規約は、既存の全事例に当てて検算してから書く（#488, #476）
  - **全称否定（「X は存在しない」「どこからも呼ばれない」）も同じ強さである**——不在の観測 1 つ（`which` が空・grep 0 件）で確定させず、探し方を変えて所在を確かめてから書く（PATH に無い ≠ 導入されていない・2026-08-03 jq で実害寸前）
  - **数え上げも同じ強さである**——版・経路・分岐を数えた散文は足すたびに腐る。数ではなく正本（分岐そのもの）を指す
  - **自分が引用した規範ほど破りやすい**（#977: この節を引きながら書いた doc が偽の全称で、次の反復で起動時 panic の根拠になりえた）
- **照合は SSOT に対して行う。派生コピー同士の一致を完全性の証拠にしない**——SSOT を変えるときは写しを grep で数え上げてから直す。列挙も SSOT のツール自身に問う（glob の意味論はツールごとに違う。詳細は `docs/development-principles.md`「列挙の完全性」）（#500, #471）
- **消す/共通化する前に「片方だけが変わる将来を 1 つ挙げられるか」を問う**——挙がれば別概念、挙がらないことが同概念の証拠になる。重複排除の対象は文字列ではなく概念である（#500）
- **計画に書いた判定ロジック（正規表現・位置計算・述語・検証コマンド）とテスト fixture（parse される入力データ）は、実装前に代表入力で実行して測る**——サブエージェントの実測も一次証拠にしない。判定の中核は自分で測る（#482, #471。fixture の例: 計画に書いた TOML が必須セクション欠落で parse 不能だった・SU3.5）。**検証コマンドは「観測形が対象を含むか」まで測る**——未コミットの実装に `git diff main...HEAD` を当てると commit 同士の比較ゆえ作業ツリーを見ず、対象ゼロのまま自明に green を返す（`..` と空白区切りの 2 点形も同じ。#922 で「挙動不変」の証拠がこの形だった）
- **主張は代理ではなく対象そのもので測ってから書く**——メタデータ・切り詰めた出力・類推・記憶から導いた断定が撤回の主因で、9 型中 8 型は一行のコマンド（過去コミットでの実測・切り詰め無しの grep・mtime・出力ファイルの読み直し・再実行）で防げた（撤回走査 2026-08-04・約 60 件）

## RETROSPECTIVE.md の運用

- **不変条件 — 持続タスクを置かない**: 上書き（追記ではない）の**前に**、教訓は `AGENTS.md` / 各 `CLAUDE.md` / スキルへ抽出し、実行タスクは PR 本文チェックリスト（PR 内で閉じるもの）か GitHub issue（サイクルを越えるもの。迷ったら issue）へ振り分ける
- 更新手順・振り分けの詳細・サイクル末 health-check の実行責任は `/retrospective`（`.claude/skills/retrospective/SKILL.md`）を SSOT とする
