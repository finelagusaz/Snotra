# AGENTS.md

このリポジトリで AI エージェントが作業するときの共通開発プロセスガイド。`*.md` で意図を管理し、コードで実装事実を管理する。エージェント固有のルール・ワークフローは各エージェントの設定ファイル（`CLAUDE.md` 等）を参照。

## 3層分担（責務分離）

- 第1層（意図管理）: `SPEC.md` と `AGENTS.md`
- 第2層（実装事実）: `snotra-core/src/*.rs`, `src-tauri/src/*.rs`, `ui/src/**`
- 第3層（整合運用）: 挙動変更を伴う変更では、意図（`SPEC.md`）と実装を同時に整合させる

## ドキュメント参照

- 意図（仕様）: `SPEC.md`
- プロダクト知識（アーキテクチャ・横断パターン）: `docs/architecture.md`
- 開発原則・デバッグ指針（汎用）: `docs/development-principles.md`
- ビルド・テストコマンド: `docs/build-commands.md`
- モジュール固有の不変条件: 各サブディレクトリの `CLAUDE.md`（`snotra-core/`, `src-tauri/`, `ui/`, `snotra-settings/`）
- 設定 UI デザインガイドライン: `snotra-settings/SETTINGS-DESIGN.md`（デザイントークン・Fluent タイポグラフィ・レイアウト規約）
- 実装前チェック: `.claude/rules/`（該当ファイルの読み取り時に自動配送）

## 開発ワークフロー

実装・修正に着手する際は、原則として以下のステップに沿って進行すること。

**サイクル開始前**: `RETROSPECTIVE.md` を読み、前回サイクルで生まれたバグのパターン・経路を確認する。同じ構造的ミスを繰り返していないか意識しながら以下のステップを進める。

0. 「バグ」か「仕様変更」かを判定する
   - バグ: `SPEC.md` の意図に合わせてコード修正
   - 仕様変更: `SPEC.md` → コード → ドキュメントの順に更新
   - **「fix」でも文書化された挙動を変えたら仕様変更**: バグ修正のつもりでも、`SPEC.md` 記載のフロー・IPC 契約・状態遷移を変えるなら仕様変更として `SPEC.md` を同期する。「fix」というコミット種別は SPEC 同期を免除しない
1. 要件（受け入れ条件）をテスト可能な形で確定する
2. 影響範囲（読む/触る/触らない）を列挙する
   - **対称コードパス確認**: 変更対象に対称ペア（`clicked`/`double-clicked`、`show`/`hide`、`enter`/`exit` 等）が存在する場合、それぞれへの適用要否を明示的に判断して記録する
   - **git マージ状態の確認は `git diff` で行う**: `git log` のコミットメッセージはマージ済みかどうかの確認に使わない。実際のコード差分（`git diff main...HEAD` 等）で確認する
   - **関数使用箇所検索**: 新規定義・変更した関数が使える既存コードパスを grep で列挙し、適用の要否を判断する（関数を作ることとその関数を使うことは別の判断）
   - **同一パターン全コードパス検索**: バグを発見したとき、そのバグパターン（根本原因）をコードベース全体で検索し、同一パターンの別箇所を列挙する
   - **網羅性が要件のタスクは「枠組みの独立」な再導出で補完する**: 全文監査・陳腐化スイープ・全箇所改名など「漏れなく全部見つける」が要件のタスクでは、単一の分解枠組み（section 分割・観点固定）は自身の盲点を構造的に継承する。`/plan-review` Step 2b の独立再導出（計画を見せず要件+コードから再列挙し差分）を必須の補完に組み込む——実行の独立より枠組みの独立が見落としクラスに効く（#404: section 分割した成果物監査が「陳腐化なし」と報告した範囲を、独立再導出が §4.7/§19.2/§19.8 の factual ドリフトで回収）
   - **列挙の真実源はツール自身に問う（glob の意味論はツールごとに違う）**: 「該当ファイルを全部挙げる」列挙で glob の見た目を信じてはならない。`git` の pathspec の `**/` は **1 段以上**のディレクトリを要求するが、TypeScript の `exclude` の `**/` は **0 段にもマッチ**する。`git ls-files` はブレース `{ts,tsx}` を展開しない。#471 では `git ls-files 'ui/src/**/*.test.tsx'` が実在する `ui/src/MainApp.test.tsx`（深さ 0）を落とし、その不完全な列挙のまま実装していれば「typecheck が発火するがそのファイルは検査されない」という当の issue の失敗様式を再現するところだった。**列挙は当のツール自身に問う**（型検査の対象なら `tsc --listFilesOnly`、追跡ファイルなら `git ls-files <dir>` を全件フィルタ）
   - **順序に依存する参照は、削除・挿入で静かに腐る**: 章番号・ルール番号・カテゴリ記号のような**序数**で他所から参照されているものを削除・挿入するときは、参照側を全部数え上げる。番号は「順序」という暗黙の状態を持つため、`grep` で当の語（`gh pr create` 等）を追っても**概念ラベルで参照している箇所には到達しない**。#482 で「最重要ルール 2」を削除したとき、`最重要ルール 4` を番号参照する OPEN issue が 5 件あり、独立再導出だけがこれを検出した。**直すときは番号ではなく名前で参照し直す**（`最重要ルール「エージェント設定の変更は合意してから」`）——順序に依存しない識別子は、次にルールを増減しても腐らない
   - **変更なしの根拠明示**: 「変更なし」と判断するとき、影響するケースを列挙して根拠を裏付ける
   - **キー/識別子形式の変更は全コードパスで同時に揃える**: 識別子・キー形式を変更する場合は「新規記録」「既存データ移行」「外部参照API」の3者が揃っているか確認する
   - **識別子の改名は「シンボル単位」で列挙し「概念」で分類する**: 改名対象の各シンボル（フィールド・accessor・関数・enum variant・i18n メソッド）ごとに全呼び出し元を grep で列挙する。キー文字列だけで grep すると、間接命名（accessor 経由の呼び出し・派生 signal・概念ラベル `label_max_history`）を取りこぼし、同名別概念（汎用 top-k パラメータ・bench 値）を誤検出する（名前↔概念は多対多）。accessor/型を改名したら下流の **compile-fail を「改名検出器」として使う**（`cargo build -p <下流 crate>` で呼び出し元の漏れが必ず出る＝Phase 完了ごとの mid-verify）
   - **乖離の解消は「削除」も解候補に入れ、削除の安全性は compile/typecheck で裏取りする**: 実装間・実装↔doc の不整合（例: Rust/TS の検証ロジック乖離）を解消するとき、issue が提示する「片方に揃える」二択を鵜呑みにせず、まずコードで前提を裏取りする——一方が呼び出し元の無い**死蔵コード**なら、揃えるより削除が乖離の根本を断つ最小解になりうる（#409: TS バリデータ `hotkeyValidation.ts` は旧フロント設定 UI 時代の孤児で、(a) Rust に禁止リスト追加でも (b) doc 明文化でもなく、削除が SPEC を as-built に戻した。選択肢空間は前提の裏取りで広がる＝[[feedback_verify_issue_premises]]）。ファイル削除の安全性は静的 grep（import 文を exhaustive に捕捉）+ 独立再導出 + **compile/typecheck を「死蔵検出器」として使う**（live import が残れば module-not-found で落ちる＝改名時の compile-fail＝改名検出器の削除版）の独立経路で裏付ける
   - **モジュール構成ドキュメントの同期**: 新規ファイル（`.rs` / `.ts` / `.tsx`）を追加・削除した場合は、対応するサブディレクトリの `CLAUDE.md` のモジュール構成セクションを「触る範囲」に含める
   - **SPEC.md のセクション番号整合**: セクションを追加・削除したとき、子セクション番号（`### N.x`）と以降のセクション（`## N+1.`〜）の番号もずれていないか確認する
   - **件数・上限パラメータの変更は下流全段に追う**: バックエンドが返す件数 N や上限値を変更したとき、IPC シリアライズ・フロントエンドのレンダリング・アイコン取得など下流の各ステージで N が変わることの影響を列挙する
   - **`docs/` の更新要否を判断する**: 変更がアーキテクチャ・横断パターン・ビルドコマンド・開発原則に影響する場合、対応する `docs/` ファイルを「触る範囲」に含める
   - **SPEC・docs は as-built を記述する**: 計画で「任意（流用可）」とした sub-feature は、実装がそれを採用したか確認してから SPEC/docs に書く。ドキュメントを実装と別タスク（特にサイクル末）で書くと、計画の楽観的な全体像をそのまま記述して実装と乖離しやすい（#394: SPEC が未実装の exe ピッカーを記述し最終レビューが検出）
   - **機能削除・ルート変更時は E2E テストも影響範囲に含める**: フロントエンドの分岐削除や IPC ルート変更は E2E テストの前提を壊す可能性がある。`e2e/` ディレクトリで関連キーワードを grep し、テストシナリオの前提が維持されるか確認する
   - **UI 変更はレイアウト崩れの観点で検証する**: スタイル・レイアウト・テキスト表示に影響する変更では、overflow／clipping／フォントレンダリング／コンテンツサイズが極端に大きい or 小さいケースを検証対象に含める。PR 作成前にビルドして目視確認する
   - **設定・コンフィグ変更は既存ユーザーへの後方互換性を確認する**: デフォルト値の変更・キー追加・フォーマット変更は、新規インストールだけでなく既存の設定ファイル（`settings.json`、ユーザープリセット等）でも正しく動作するか確認する。マイグレーションが必要な場合は明示的に計画する
3. 事前調査（レビュー未然防止）を実施する
   - 比較関数 + データ構造は「先頭要素が最良/最悪どちらか」を一文で明示
   - 最適化は「意味を変えない不変条件」を箇条書きで定義
   - 挙動変更なし前提は代表入力/出力をベースライン化して差分検証
   - 境界条件を列挙し最低1件ずつ検証ケースを用意
   - **リソース管理は生成/破棄ペアで計画する**: `listen()`・`ResizeObserver`・`URL.createObjectURL`・`Child` プロセスなど「戻り値がライフサイクルを持つもの」を生成する際は、破棄（`unlisten`・`disconnect`・`revokeObjectURL`・`kill`）の「場所・構造・理由」まで同時に記述する
   - **状態フラグも真偽ペアで計画する**: `AtomicBool` などのフラグを `true` にする操作は「誰が・どの条件で `false` に戻すか」をセットで設計する。`false` に戻す経路が存在しない場合、失敗時にシステムが回復不能な状態に固まるリスクがある。`false` に戻す責務を持つ関数（`start_index_build` 等）は `#[must_use]` を付けて `let _ =` による無視をコンパイル時に検出する
   - **これまで無意味だった状態に意味を与える変更は、その状態に到達する全経路を列挙する**: 「無出力 = 合格」「空 = 未設定」のように、それまで何も意味していなかった状態に解釈を与えると、**同じ状態が無害なノイズから積極的な虚偽に変わる**。意味づけの前に「この状態に到達しうる経路」を漏れなく挙げ、意味が成り立たない経路をすべて塞ぐ。#471 で PostToolUse hook の沈黙に「合格」の意味を与えたとき、タイムアウトによるプロセス kill・実行ファイルの起動失敗・出力バッファ溢れがいずれも沈黙を生む経路として残っており、安全網が false green を出す構造になっていた（検査ごとに自前でタイムアウトし報告する形へ修正）。**分類を 1 つ足したときは、隣接分類への到達経路も再列挙する**——逃げ道は塞いだ扉の隣に現れる（#489: 報告語彙に `Skipped` を足して逃げ道を塞いだら、既存分類の「正常（未初期化）」と「発見事項ゼロ」が新たな逃げ道になった）
   - **安全網が「効いている」ことは、故障注入で一度は実測する**: 「hook が自動実行する」「CI が担保する」という記述は、書かれた時点の期待であって測定結果とは限らない。安全網に依拠して手順を省く前に、**故意に壊して検知されることを 1 度確かめる**（型エラーを注入して hook の出力を見る等）。#471 では PostToolUse hook の出力が一度もエージェントに届いていなかったにもかかわらず、`CLAUDE.md` が「Edit/Write 後に会話へ流れる」と保証し続け、その誤った信念の上に別の PR が積まれていた。ただし**稼働中のガードを弱めて測ってはならない**——判定がソース述語なら、ライブのファイルではなく**複製に変異を当てる**（#482 で既定 `exitCode = 2` を 0 へ反転する編集は classifier が拒否した。ソースをメモリ上でコピーし 3 通りの変異を当てて、カナリアが全て捕まえることを実証した）。**安全網が「規範」（ドキュメント・スキル・チェックリスト）である場合、故障注入とは「それを回避しようとする読者」である**——規範は機構ではないので実行して測れない。代わりに「怠惰な読者」を演じるサブエージェントに文言だけを読ませ、抜け道を探させる。**1 巡では終わらない**（#489: `Skipped` を塞いだら「正常（未初期化）」の詐称へ、そこに証跡を課したら証跡なしの「発見事項ゼロ」へ逃げ道が移った。2 巡で収束）
   - **計画に書いた判定ロジックは、実装前に実行して測る**: 正規表現・位置計算・述語を計画へ擬似コードとして書いたら、`node -e` 等で代表入力を通し、**計画が主張する allow/block と一致するか**を確かめる。#482 では `hasSafeChain` の擬似コードが、その issue の目的である `git push … && gh pr create` を block し、`git push … && npm test; gh …` を allow していた（単一原因: 検出正規表現が先頭の区切り文字を消費し `index` がトークンを指さない）。**そして、サブエージェントの実測も一次証拠として扱わない**——同 issue で `/plan-review` の 1 体は「原案の正規表現でも `gh --repo o/r pr create` は一致する」と実測付きで報告したが、自分で測ると不一致（＝過小検出＝ fail-open）だった。#471 でも「exit 0 の stdout はエージェントに届く」というドキュメント引用が誤っていた。**判定の中核は自分で測る。**
   - **スレッド内 panic からの回復設計は release の `panic` 設定に依存する**: バックグラウンドスレッドの panic を `catch_unwind` で捕捉してフラグを戻す等の回復設計をするとき、release profile の `panic` 設定を確認する（このワークスペースは `Cargo.toml` で `panic = "abort"`）。`catch_unwind` は **unwind panic しか捕捉できない**ため、`panic = "abort"` の release では捕捉されずプロセスが abort する（回復は debug/test の `unwind` 限定。abort 自体は flag 固着 wedge にならない＝プロセス終了で回復）。「panic でも回復する」とコメント/設計で主張するなら、実際のビルド設定と一致させる
   - **バックグラウンドスレッドは「開始時キャプチャ」と「完了後の現在値」を比較する**: 長時間処理（インデックスビルド等）の開始時に設定をキャプチャし、完了後に現在の設定と比較して差分があれば再実行するパターンを使う。比較せずに完了通知だけ出すと、処理中の設定変更が失われ次回起動まで反映されない
   - **初回フローとガードの相互作用を検証する**: 正常運用時の早期リターンガード（`if indexing { return }` 等）が、初回起動・初期化フロー・エラーリカバリなどの特殊パスを意図せず阻害しないか確認する。ガードを追加/変更したときは「このガードが true になる全コンテキスト」を列挙する
   - **サンプルコードに構造の理由を付記する**: リソース管理・非同期処理の配置が問われる箇所に「なぜこの構造か」をコメントする
   - **キャッシュ再利用は述語の単調性を証明してから実装する**: 前回結果を再利用するロジック（incremental search、メモ化等）を追加・変更するとき、全述語の単調性（今回の結果集合 ⊆ 前回の結果集合）を検証する。変換関数が入力伸長に対して非単調な場合（例: ローマ字→かな）、bool フラグではなく実際の値を保持して `starts_with` 等で比較する
4. 失敗するテスト（または最小再現）を追加する
   - **既存テストを改名・転用するとき、元のテストが証明していた不変条件を孤立させない**: リファクタで既存テストの名前・前提・期待値を変えると、元テストが守っていたガード（例:「新キーは legacy で上書きされない」）が宙に浮く。転用で失われる不変条件は別テストで補う（改名は内容ではなく「どの命題を証明していたか」も追跡する）
5. それが落ちることを確認する（Red）
6. 最小実装で通す（Green）
7. テストが通るまで 6 を反復する
8. 変更後の検証を実行する（スキップ不可）
   - 変更したファイルの種類に応じて、`docs/build-commands.md` の「変更後の検証チェックリスト」のカテゴリ A〜E から該当するものをすべて実行する
   - コマンド本体は SSOT である `docs/build-commands.md` を参照（このリストとの二重メンテを避けるため、コマンド文字列はこのファイルに書かない）
9. 報告は「追加/更新テスト名 + 検証した不変条件」を必ず含める

## 環境制約

- Agent `isolation: "worktree"` は使用可（worktree は `.claude/worktrees/agent-<id>/` に作られ、パス破壊は起きない／Git 2.54.0.windows.1 で検証済み）。変更なしの worktree は自動 cleanup されるが、**ファイルを変更した worktree は成果保全のため自動削除されず残る**ため、`git worktree remove --force <path>` + `git branch -D worktree-agent-<id>` で手動 cleanup する（`.claude/worktrees/` は `.gitignore` 済み＝誤コミット防止）
- **並列エージェント委譲はファイル境界で衝突を予測してから行う**: 同一ファイルに触りうるタスクは並列にせず、直列化するかマージ順を決めて委譲する。境界の見積もりは issue 記載の対象ファイルだけでなく「実装中に踏み込みうる隣接ファイル」まで含める（settings 中心の #439 が snotra-core/config.rs に `normalized_default()` を追加し、config.rs 分割の #435 と conflict した実績。リベース解決はコンテキストを保持した実装エージェント本人に依頼するのが最短）
- **委譲はコンテキストを継承しない**: メインエージェントの system prompt にしか無い事実（メモリ領域の絶対パス等）は、サブエージェントのプロンプトへ**明示的に渡す**。渡し忘れると、サブエージェントは**見えないものを「無いもの」として報告する**（#489: health-check の Check 7 が、リポジトリ外にある `MEMORY.md` を「存在しない」と誤報した）。委譲先に「何を渡し、何をあえて渡さないか」を明文化する（`/plan-review` Step 2b の「`plan.md` を読ませない」が先例）。あわせて、**`allowed-tools` はインライン実行のスキルを拘束しない**（実測）——frontmatter に `Agent` が無いことを根拠に「このスキルは委譲しない」と推論してはならない。また、**委譲した検査が対象を読む時刻は制御できない。検査対象を変更しながら検査を走らせない**（#489 の振り返りで、メモリを更新する Step 6 と health-check を実施する Step 7 を待ち時間短縮のため並行させ、古い索引と新しい本文を同時に見た報告が返った。スキルのステップ順序には理由がある）
- **長時間の委譲タスクは中断を前提に設計する**: セッションリミット・API エラーでエージェントは途中終了しうる（resume は可能だが worktree が消え未コミット作業が失われたケースあり）。大きなタスクは Phase 分割し「各 Phase の検証 green 後にコミット」を指示に含める（#431 のフェーズ制が好例）
- Win32 依存モジュール（`src-tauri/src/ime.rs`, `src-tauri/src/platform/` 内の `hotkey.rs` 等）はユニットテスト前提にしない
- モジュール固有の不変条件・TDD ルールは各サブディレクトリの `CLAUDE.md` を参照（実装前チェックは `.claude/rules/` で自動配送）

## RETROSPECTIVE.md の運用

- **更新タイミング**: サイクル終了後（実装・レビュー・追加修正まで完了したとき）
- **更新方法**: 上書き（追記しない）。前回サイクルの内容を新サイクルの振り返りで置き換える
- **フォーマット**: 「よかったこと・伸びしろ」の2セクション構成（サイクルの教訓スナップショット）
- **不変条件 — 持続タスクを置かない**: `RETROSPECTIVE.md` は上書きファイルであり、サイクルを越えて生き残る情報を入れてはならない。実行が必要なタスクを残すと次サイクルの上書きで失われるため、必ず以下の追跡先へ振り分ける:
  - **ドキュメント改善（教訓の抽出）**: その場で `AGENTS.md` / 各サブディレクトリの `CLAUDE.md` / スキルへ直接反映する
  - **PR のライフサイクル内で閉じるタスク**（push・`e2e` 等のラベル付与・CI グリーン確認・手動マージ 等）: **対象 PR 本文のチェックリスト**に書く（PR が merge されれば自然に閉じる）
  - **サイクル/PR を越えて生き残るタスク**（follow-up・別リファクタ・繰越・要設計）: **GitHub Issues に起票**する。迷ったら永続側＝issue を選ぶ
- **更新手順**: ① 教訓を先に `AGENTS.md` / 各 `CLAUDE.md` / スキルへ抽出 → ② 実行タスクを上記の追跡先（PR / issue）へ振り分ける → ③ `RETROSPECTIVE.md` を 2 セクションで上書きする。抽出・振り分けの前に上書きすると教訓・タスクが失われる
- **サイクル末の衛生チェック**: サイクル末 health-check の実行責任は `/retrospective` が負う（末尾ステップで `.claude/skills/health-check/SKILL.md` のチェックを実施し、発見事項を上記の振り分けへ流す。`/health-check` は user 起動専用＝`disable-model-invocation` のため、retrospective はその定義に沿って直接実施する）
