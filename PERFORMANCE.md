# パフォーマンス最適化プレイブック

> **この節の具体例は WebView2 期のものである（#532 SU7 でフロント撤去済み）。** `clearTimeout`・
> `invoke<ArrayBuffer>`・`URL.createObjectURL`・`results-sync`・`Promise.allSettled` は現行構成に
> 対応物を持たない。**着手の順序（待ち時間 → 転送量 → 描画）は現行でも生きている**——
> 個々の手段は egui 経路の対応物へ読み替える。

検索/表示の体感遅延を改善するときは、次の順で着手すると最短で効果が出やすい。

1. 待ち時間を潰す（体感改善の即効枠）
   - 入力デバウンスは leading edge（初回即時発火）+ trailing 50ms。旧値は 150ms trailing のみだった
   - 古い非同期リクエスト結果の破棄（request id / generation）
   - `show` / `setSize` / `setPosition` などウィンドウ操作の不要呼び出し削減
   - OS 呼び出し待機を伴う処理（例: `launch_item`）は `timeout` を明示し、UI 側で `launching` と失敗通知を表示して「無反応」に見せない
   - 失敗通知の自動クリアは単一タイマーで管理し、再通知時は `clearTimeout` してから再設定する
   - **Win32 IPC の cold-call を避ける**: Tauri のウィンドウ API（`is_visible()` / `show()` 等）は内部で Win32 IPC を使い、WebView2 への初回アクセスで数十ms のオーバーヘッドが発生する。ホットパス上で状態確認目的に使う場合は `AtomicBool` 等で Rust 側にキャッシュし、Win32 IPC をスキップする。冪等な操作（`show()` 等）は pre-check なしで直接呼ぶ
2. 重複処理を消す（低リスク・高効率）
   - 同一データの二重取得（例: アイコン batch 取得）を責務分離して一本化
   - 同一状態を複数イベントで配信しない。結果表示は `results-sync`（`generation` + `shouldShow`）の単一契約で同期する
   - 画像・バイナリデータの IPC 転送は `tauri::ipc::Response` を使い base64 を排除する（転送量 ~25% 削減、Rust 側 encode コスト削減、フロント側 decode コスト削減）。`invoke<ArrayBuffer>` で受け取り `URL.createObjectURL(new Blob([buf]))` で表示する
3. 計算量を下げる（中〜大規模データ向け）
   - 毎回の全件ソートを top-k 抽出へ置換
   - ループ内の再計算（正規化や同一変換）を事前計算へ移動
   - `Mutex<T>` を保持したまま `SHGetFileInfoW` 等の OS IO を行うと、並列 IPC リクエストが Rust 側で直列化される。フロントの並列性（`Promise.allSettled`）を活かすには、IO 完了後にロックを取得する設計（キャッシュ済みバイト列の返却のみをクリティカルセクションに限定する等）を検討する
4. 描画のマイクロ最適化を行う（仕上げ）
   - 文字幅計測や省略文字列生成のキャッシュ
   - 無意味な再スクロール/再レイアウト抑制

## ビルドプロファイル最適化の知見

### Cargo ワークスペースのプロファイル上書き

クレート単位の `opt-level` を変更するには、ワークスペースルートの `Cargo.toml` に
`[profile.release.package.<name>]` セクションを追記する。**ワークスペースメンバーの
`Cargo.toml` に書いた `[profile.*]` は Cargo に無視される**（ワークスペースでは無効）。

```toml
# Cargo.toml (ルート) — 正しい書き方
[profile.release.package.snotra-settings]
opt-level = "s"
```

### opt-level = "s" 適用結果（issue #138）

| クレート | 変化 | 判断 |
|---------|------|------|
| `snotra-settings` | 4.0 MB → 3.8 MB (−5%) | 採用（低頻度起動） |
| `snotra` (src-tauri) | 6.7 MB → 5.5 MB (−18%) | 採用（ホットパスなし） |
| `snotra-core` | fuzzy +33%, new +11% | **不採用** |

`snotra-core` は `nucleo-matcher` + rayon の並列スコアリングがホットパスであり、
`opt-level = "s"` によるループアンローリング・SIMD 抑制が直撃する。
**`snotra-core` への `opt-level = "s"` / `"z"` 適用は行わない**。

## アイドルメモリ（working set）の削減（2026-06-01〜02 計測）

> **この節は WebView2 期の記録である（#532 SU7 でフロント撤去済み）。** 現行構成のプロセスツリーは
> **1 件**（webview2 子孫はゼロ）で、数値・内訳とも現行には当てはまらない。現行の実測は
> 「egui 期のメモリ実測」節を参照。`EmptyWorkingSet` の採用判断（下記）だけは現行でも生きている。

ランチャーは 99% 非表示常駐のため、アイドル時の物理メモリ（working set / private RSS）の最小化が重要。installed release ビルド（32GB/SSD 機）で WebView2 プロセスツリー（main + webview2 子孫 計 7 プロセス）を実機計測した。

### 実態（Private Working Set = プロセス固有の物理 RAM）

- 非表示アイドルの定常 baseline: **~110MB**。Working Set 表示値 ~390MB との差 ~280MB は Edge ランタイム共有 DLL（`msedge.dll` ~310MB 等）のページで、プロセス間共有・デマンドページゆえ Private には乗らない（「Snotra=390MB」は誤読）
- 内訳（`VirtualQueryEx` で committed をタイプ別分類）: WebView2 エンジン固有 ~64MB（browser 33 + renderer/V8 18 + utility/crashpad 13、アプリ制御不可）+ Rust 本体 ~30MB + GPU プロセス ~18MB
- Rust 本体 ~30MB の正体: index は極小（`index.bin` 数百 bytes）ゆえデータではなく、Tauri/WRY/tokio/serde/windows クレート + アロケータ保持の baseline（committed 42.8MB の内訳はヒープ 40.7MB / スレッドスタック 2.17MB）

### 採用: hide 時の `EmptyWorkingSet`（issue #355 / PR #360）

hide 経路で Win32 `EmptyWorkingSet` をプロセスツリー全体へ能動適用し、アイドル常駐を **110MB → 数MB** へ回収する。実装は `src-tauri/src/working_set.rs`、パターン詳細は `src-tauri/CLAUDE.md`「working set の能動回収」節。

| 状態 | Private WS |
|---|---|
| アイドル baseline | ~110 MB |
| hotkey hide 後（自動 trim） | ~9.4 MB |
| frontend(Escape) hide 後（自動 trim） | ~22.8 MB |

再表示レイテンシは劣化しない（trim 無/有とも ~41ms、メモリ圧迫下でも 44ms、ウィンドウ出現はブロックされない）。ページは standby/pagefile に退避され show 時に OS が透過 re-fault する（圧迫下のみハード読込 ~934/s ≈ 3.6MB/s で SSD では体感不能）。削減対象は物理 working set であって commit（~195MB 不変）ではない。frontend 経路が hotkey より浅い（22.8 vs 9.4MB）のは `notify_main_hidden` が tokio で `win.hide()` と並行するタイミング差（#361 で修正済み、下記）。

### follow-up: frontend hide の trim タイミング修正（issue #361 / 2026-06-02 計測）

frontend hide（`hideMainWindow()`）で `notifyMainHidden()`（trim）を `await win.hide()` の**後**に呼ぶよう順序を入れ替え、可視中の trim でレンダラがページを再 touch する取りこぼしを解消（hotkey 経路と同じ hide→trim 順）。MainApp.tsx のフォーカス喪失・クリック起動経路も `hideMainWindow()` に集約（DRY）。

クリーン再計測（検索なし、`%TEMP%/snotra-mem-measure.ps1`、**プロセスツリー総 WorkingSet64**= 共有ページ込みで上表の Private WS とは別指標）:

| 経路 | #361 前 | #361 後 |
|---|---|---|
| frontend(Escape) hide 後 | ~50 MB（3 回 50-61） | **~27 MB**（3 回 27-30） |
| hotkey hide 後（参照・無変更） | ~10 MB | ~10 MB |

frontend hide の総ツリー WS が**約半減**（frontend↔hotkey 差の ~57% を解消）。残差（~17MB）は frontend 経路が `suspend_webview`（TrySuspend）を行わない設計差に起因（tokio IPC スレッドの `with_webview` 非同期制約のため hotkey 限定。当時の正本だった src-tauri の「TrySuspend / Resume パターン」節は WebView2 層ごと #532 SU7 で消滅した——現行の回収機構は `src-tauri/CLAUDE.md`「working set の能動回収」）。gap は「trim タイミング」成分（#361 で解消）+「suspend 有無」成分（設計上の意図的な差）の和。**（2026-07-17 訂正: 下の follow-up のとおり、当時の TrySuspend は両経路とも同期失敗しており、この残差の「suspend 有無」解釈は成り立たない。実体は trim 実行タイミングの揺らぎだった）**

### follow-up: TrySuspend は一度も成立していなかった（2026-07-17 計測）

trace 計装（`suspend:call_returned` / `suspend:completed`）で、`TrySuspend` が hotkey 経路を含む**全 hide で同期 Err（0x8007139F ERROR_INVALID_STATE）を返し、導入以来一度も suspend が成立していなかった**ことを確認した。原因は `TrySuspend` の前提条件が `ICoreWebView2Controller.IsVisible=false`（HWND 非表示とは独立の controller プロパティ）であり、wry が `hide()` でこれを下げないこと。同期 Err では完了ハンドラが呼ばれないため、既存の握りつぶし（`let _ =`）では観測不能だった。

対処: `suspend_webview` で `SetIsVisible(false)` を自前実行してから `TrySuspend`、`resume_webview` で `SetIsVisible(true)` + `Resume` の対称復帰。同時に `notify_main_hidden` へ `run_on_main_thread` 経由の suspend を拡張（frontend hide も hotkey と同一の suspend → trim 順・同一実行文脈）。

計測（release ビルド・テスト index 数百件・プロセスツリー総 WorkingSet64・各 10 サイクル + アイドルプローブ）:

| 変種 | hide 直後(中央値) | idle 30s | idle 60s | idle 120s | show p50 / max |
|---|---:|---:|---:|---:|---|
| 修正前 Escape | ~30MB | 85.7 | 86.0 | — | 33.3 / 53.4ms |
| 修正前 hotkey | ~18MB | 69.9 | 70.5 | — | 33.9 / 52.3ms |
| 修正後 Escape | ~27MB | **14.0** | **18.9** | 30.7 | 34.6 / 52.3ms |
| 修正後 hotkey | ~37MB | **11.9** | **17.3** | 29.2 | 35.4 / 56.8ms |

- **本質的な効果は hide 直後ではなく定常アイドル**: suspend が成立しないとレンダラーが動き続け、trim の成果がアイドル 30 秒で ~70-86MB へ巻き戻る。成立後は 12-31MB で低空安定（約 55-70MB 削減）。99% 非表示のランチャーでは定常値が真の指標
- show レイテンシは劣化なし（suspend 成功率は両経路 10/10、クエリ入力 + アイコン取得を挟む 4 サイクルでも機能劣化なし）
- #355 の「hotkey hide 9.4MB vs frontend 22.8MB」の差は suspend ではなく trim タイミング差の産物だった

### 効かなかった/見送った手法

- **TrySuspend / MemoryUsageTargetLevel.Low 単独**: 当時「論理目標を下げるだけで物理 working set を回収しない（120 秒放置で 110MB 不変）」と結論したが、**2026-07-17 訂正: この時点の TrySuspend は `SetIsVisible(false)` を欠き同期失敗していた**——「効いているが物理を返さない」のではなく「そもそも動いていなかった」。成立後はアイドル再増殖の防止に有効（上の follow-up）。即時回収に `EmptyWorkingSet` が必要という結論自体は不変（両者は別レイヤーで補完的）
- **tokio / rayon の worker スレッド削減**: Rust 本体は Tauri 既定の multi-thread tokio（worker = CPU コア数）+ rayon プールで ~50 スレッドを抱えるが、スレッドスタックは計 2.17MB（committed 42.8MB の 5%）に過ぎず、worker を絞っても RSS 削減は <1MB で**無意味**。30MB の大半はヒープ/フレームワーク baseline
- **`--disable-gpu --disable-gpu-compositing`**: GPU プロセスは消えない（in-process ソフト合成に切替）が Private 18→6MB・合計 110→99MB。ただしブラウザフラグは Microsoft 非サポート（ランタイム更新で挙動変化）＋ CPU 合成化で描画レイテンシ未検証。限界効用は `EmptyWorkingSet`（~107MB）に対し桁違いに小さく見送り
- **アイコン PNG 圧縮強化（issue #335）**: 16×16 アイコンで実測 ~0.06MB と桁違いに小さく却下（詳細は #335）

## egui 期のメモリ実測（2026-07-25 計測）

> **この節の実運用点は 38,847 エントリである。2026-08-07 に同じ機で測り直したところ
> 312,377 エントリ（`index.bin` 107.0 MiB）に育っており、索引の常駐は 14.96 → 166.08 MiB
> になっていた。** 下の font A/B と「索引の常駐」の絶対値は現運用点に当てはまらない
> ——機構（`from_static` / cmap カバー判定 / Context 数への比例）だけが生きている。
> 現在の値と内訳は「索引の常駐の内訳（2026-08-07 計測・312,377 エントリ）」を見ること。

`#532 SU7` 後の構成（単一プロセス・WebView2 なし）を release ビルドで実機計測した。
計測ハーネスは `snotra-core/tests/memory_footprint.rs`（アロケータ計数・実行コマンドは
`docs/build-commands.md`）と `scripts/measure-memory.ps1`。

### 軸の取り違えに注意 — PrivWS は trim 後の値である

hide 経路の `EmptyWorkingSet`（`src-tauri/src/working_set.rs`）は物理ページをほぼ完全に返す。
**非表示アイドルの PrivWS は 2.3 MiB** で、ここに削る余地は無い。一方 PrivComm は trim で
**動かない**（97.1 → 97.1）。ゆえに**ヒープ調律の軸は PrivComm のみ**であり、PrivWS の
小ささを「メモリを使っていない」と読んではならない。

### font_family A/B（前景計測・`auto_hide_on_focus_lost = false`）

実運用 config（38,847 エントリ・migemo 有効）での PrivComm。B は `font_family` を
解決不能名にして user_font 経路だけを落とした変種。

| 段階 | A: `font_family = "HackGen Console"` | B: 解決不能（jp_font 単一） | 差 |
|---|---:|---:|---:|
| アイドル（一度も表示せず） | 61.9 MiB | 41.3 MiB | **20.6** |
| 表示（空クエリ） | 75.1 MiB | 43.8 MiB | **31.3** |
| 検索実行後 | 97.1 MiB | 56.2 MiB | **40.9** |

- **user_font 経路が最大の単一項目**。窓を一度も出していないアイドル時点で既に 20.6 MiB 効いており、
  フォント解決が表示より前に走ることを示す
- 差はファイルサイズ（`HackGenConsole-Regular.ttf` = 10.21 MiB）の約 2 倍。`resolve_font_family` の
  `data.to_vec()` による複製に加え、egui 側の解析・グリフ実体化が上乗せされる。**内訳は未分離**
- **差が段階とともに広がる（20.6 → 31.3 → 40.9）のが要点**。一度きりのバイト複製なら差は一定のはずで、
  広がるということはグリフ実体化の分が乗っているということ。同じクエリ 1 回の検索で A は +22.0 MiB、
  B は +12.4 MiB しか増えない——検索時の増分もアイコンではなく**主にフォント由来**である。
  ゆえに「user_font が CJK をカバーするなら冗長な jp_font を積まない」が具体的な削減候補になる
  （カバー判定の述語が要る）
- `JP_FONT_BYTES: OnceLock<Box<[u8]>>` は `YuGothM.ttc`（**13.26 MiB**）を丸ごと保持し**解放経路が無い**。
  user_font が CJK をカバーする場合（HackGen 等）でも fallback として常駐し続ける
- 実行間のばらつきは ~4 MiB（別実行のアイドルは 65.9 MiB）。背景再スキャンの進行差による。
  **1 MiB 単位の差を有意と読まない**

### 採用: user_font が CJK をカバーするときの jp_font 省略

user_font 自身が CJK をカバーするなら jp_font（`YuGothM.ttc` 13.26 MiB）は一度も glyph を
出さないまま常駐する。**user を先に解決 → cmap 実測でカバー判定 → 必要なときだけ jp を読む**
という順序へ入れ替えた（実装は `egui_shell/font_stack.rs` の `font_covers_cjk` /
`configure_japanese_font`）。判定はかな + JIS 第1水準 + **第2水準・互換漢字**を引き、
かなと常用漢字だけの中途半端な和文フォントを弾く。パース不能は「カバーしていない」＝ jp を積む安全側。

| 段階 | 変更前 | 変更後 | 差 |
|---|---:|---:|---:|
| アイドル | 61.9 MiB | 48.6-49.6 MiB | **-12.9** |
| 表示 | 75.2 MiB | 61.6-62.6 MiB | **-13.3** |
| 検索実行後 | 97.3 MiB | 84.6-84.8 MiB | **-12.6** |

- 削減量が**ファイルサイズとほぼ一致する**のは機構と整合する。user_font が全 glyph を
  出していたため jp_font はラスタライズされておらず、効いていたのは生バイト列と
  eager parse の分だけだった。**glyph 機構まで削れると見積もったのは誤りで、実測が正した**
- `JP_FONT_BYTES` の `OnceLock` は set-once・never-clear（`transmute` による `'static` 化の
  健全性の根拠）。CJK をカバーしないフォントで一度読むと、カバーするフォントへ切り替えても解放されない
  ——**削減は fresh start の値である**
- 残余リスク: プローブは標本であり、カバーしていると判定した後にプローブ外の文字が user_font に
  無ければ豆腐（□）になる。クラッシュしない字単位の静かな欠落ゆえ、判定は厳しい側へ倒してある

### 採用: user_font も `from_static` で積む（epaint の全体複製を止める）

`from_owned` で積んだフォントが epaint 側で丸ごと複製される。**機構の正本は
`egui_shell/font_stack.rs` の `font_definitions` の doc コメント**（epaint のどの関数がどう
複製するかはそこに書く。ここで再記述すると epaint の更新時に二重メンテになる）。
jp_font は元から `from_static` だったが user_font だけ `from_owned` のままで、
上の A/B はこの非対称をそのまま映していた（user 20.6 ≒ **2×** 10.21 / jp 12.9 ≒ **1×** 13.26）。

| 段階 | 変更前 | 変更後 | 差 |
|---|---:|---:|---:|
| アイドル | 48.8 MiB | **38.2 MiB** | -10.6 |
| 表示 | 61.0 MiB | **40.1 MiB** | -20.9 |
| 検索実行後 | 84.3 MiB | **54.3 MiB** | -30.0 |

- **差が 10.2 MiB 刻みで 3 段に増えるのが機構の証明**（ファイルサイズちょうど 3 つ分）。
  変更前の複製は 4 本: `FontData` が 2 Context 分（アイドルで顕在）＋ 各 Context が
  `FontsImpl` を組む時の Blob 深いコピー 2 本（main は表示時・results は検索時に顕在）。
  変更後は leak した 1 本を全員が借りる
- **窓が 2 つある構成では、フォント常駐は Context 数に比例する**。egui の Context は
  窓ごとに独立で `FontsImpl` も別々に持つ——「1 フォント = 1 常駐」ではない
- leak は family 名をキーにしたキャッシュで共有する（`Box::leak` を素で置くと 1 回の
  font_family 変更で 2 回漏れる）。漏れは distinct family 数で頭打ち・解放はされない

### 索引の常駐（アロケータ実測・38,847 エントリ）

| 指標 | 値 |
|---|---:|
| 定常 live | **14.96 MiB**（404 B/entry） |
| ロード時ピーク | 19.97 MiB（定常の 1.33 倍） |
| live ブロック数 | 233,430（6.0 blocks/entry） |

- `index.bin` は **6.80 MiB / 38,847 件**。「index は極小（数百 bytes）」という旧記述は
  WebView2 期の別環境の値で、実運用点とは 4 桁ずれる
- **`normalized_keys` が 3.05 MiB**（索引の 22%）。`normalize_entry_key` = 小文字化 + `/`→`\` の
  長さ保存変換ゆえ、on-disk バイト数は `target_path` と完全一致する——原文パスと二重に持っている
- migemo の限界費用は 10k/38.8k/100k で一貫して **+28.6%**（133 → 171 B/entry）
- アロケータ実測は `layout.size()` の集計であり、Windows ヒープのブロックヘッダ・
  サイズクラス丸めを**含まない**。233,430 ブロックゆえ実 RSS はこれより大きい

## 索引の常駐の内訳（2026-08-07 計測・312,377 エントリ）

実 config の `[[paths.scan]] path = 'C:\'` + `include_folders = true`（C ドライブ全体の
フォルダ索引）による現運用点。`index.bin` = **107.0 MiB**。ハーネスは
`snotra-core/tests/memory_footprint.rs`。**`--test-threads=1` が必須**——計数器が
`static AtomicUsize` のプロセス大域であり、並列実行では Phase A/B が奪い合って
エラーにならず**もっともらしい数値**を出す（実測: 単調性の破れと `live 0.00 MiB`）。

| 区間 | live | peak | blocks/entry |
|---|---:|---:|---:|
| `index.bin` ロード（+ 再スキャン複製） | 228.57 MiB | 273.08 MiB | 7.00 |
| **`SearchEngine` 常駐** | **166.08 MiB** | 166.08 MiB | **5.00** |

内訳は **100% 帰属済み（未帰属 0.00 MiB）**。557.4 B/entry の行き先:

| 項目 | 確保 | B/entry | 性質 |
|---|---:|---:|---|
| `entries[].target_path`（文字列） | 35.56 MiB | 119.3 | 原本 |
| `normalized_keys`（文字列） | 35.56 MiB | 119.3 | **`target_path` から 100% 再現可能** |
| `entries` Vec 本体 | 32.00 MiB | 107.4 | 実使用は 16.68 MiB |
| `lower_file_names`（文字列） | 10.47 MiB | 35.1 | **folder は 100% が `lower_names` と同一** |
| `entries[].name`（文字列） | 10.25 MiB | 34.4 | `target_path` の末尾成分 |
| `lower_names`（文字列） | 10.25 MiB | 34.4 | |
| `lower_names` / `lower_file_names` / `normalized_keys` の Vec 本体 | 各 8.00 MiB | 各 26.9 | |
| `char_masks` / `file_name_char_masks` | 各 4.00 MiB | 各 13.4 | 実使用は各 2.38 MiB |

### 重複の実測（機構からの導出ではなく実データの率）

- `is_folder` = 255,961 / 312,377（**81.9%**）
- `lower_file_names[i] == lower_names[i]`: folder **255,961/255,961（100.0%）** / file **0/56,416**。
  indexer が folder の `name` に `file_name()`、file に `file_stem()`（拡張子なし）を使う
  規則どおりで、例外は 1 件も無い
- `normalized_keys[i] == normalize_entry_key(target_path)`: **312,377/312,377（100.0%）**。
  `normalize_entry_key` は Unicode 小文字化ゆえ長さ保存ではないが、**この索引には
  例外が 1 件も無い**（他の索引で成り立つ保証ではない）

### 採用: `assemble` で全並列 Vec を `shrink_to_fit`（-28.25 MiB）

上表は反復前の値である。`assemble`（3 コンストラクタの唯一の合流点）で 8 本の Vec を
`shrink_to_fit` した結果:

| 指標 | 変更前 | 変更後 |
|---|---:|---:|
| `SearchEngine` 常駐 | 166.08 MiB | **137.83 MiB**（**-28.25**・-17.0%） |
| ロード時ピーク | 273.08 MiB | 273.08 MiB（不変・縮小は確保後） |
| live ブロック数 | 1,561,891 | 1,561,891（不変） |
| 構築の壁時計（実 `index.bin`） | 1 / 2 / 3 ms | 2 / 3 / 3 ms |

- **3 回の実行でバイト数・ブロック数とも完全に一致した**。アロケータ計数は決定的で、
  PrivComm の ~4 MiB のばらつきとは別の計器である
- **ブロック数は動かない**（`shrink_to_fit` はブロックの数ではなくサイズを変える・`allocs` は
  6 増える）。**ブロック数だけを見ていたら「何も起きていない」と読める**
- 対のレイテンシ実測（同日・同セッション A/B。設計書 §4.2・前例は #110）:
  `bench_new_scaling` 300k が 544 → 532 ms、`bench_fuzzy_search_scaling` 300k が
  3,829 → 3,469 µs。**どの規模でも向きが一貫せず、退行なし**（メモリレイアウトは不変で、
  変わるのは末尾の未使用容量だけゆえ機構とも整合する）
- 検知器は `snotra-core/src/search/tests/build.rs`。**余剰容量は検索結果を変えないため
  挙動テストでは捕まらない**——`shrink_to_fit` を 8 箇所とも外すと落ちることを実測した

### Vec 本体の確保が実使用の約 2 倍あった（反復 1 で解消）

Vec 本体の合計は **64.00 MiB**（確保）。`char_masks` は 524,288 = 2^19 要素分を確保して
312,377 しか使っていない。serde の `size_hint` は DoS 防止のため 4,096 要素で頭打ちにし、
以降は Vec の倍々成長に委ねるため、`index.bin` から読んだ全 Vec が成長の踊り場を抱える。

**この余剰は `SearchEngine` へそのまま持ち越される**（実測: 構築前に走査した内訳の合計が
構築後の常駐と一致し、未帰属 0.00 MiB）。機序は `new_with_cached_masks` の
`Vec<String>` → `Vec<Box<str>>` 変換が in-place collect（16 B ≤ 24 B・align 一致）で
確保ブロックを再利用し、要素サイズが縮んでも `layout.size()` が動かないためと**推定される**
——`allocs = 0` からの導出であって、in-place collect は std の特殊化ゆえ保証ではない。
**持ち越しの事実はバイト合計の一致が支えており、この機序の推定には依存しない。**

### パスクエリ全走査のコスト — `normalized_keys` を保持するか導出するか

計器は `snotra-core/tests/path_query_cost.rs`。パスクエリは Fuzzy ビットマスク pre-filter を
スキップする＝全走査であり、索引規模がそのまま毎打鍵に乗る唯一の経路である。

実 `index.bin`（312,377 件）・release・16 コア・**rayon 並列（製品と同じ形）**・各 3 回の最小値:

| needle | 保持（現行） | 導出: 素 | 導出: ASCII 高速路 | hits |
|---|---:|---:|---:|---:|
| `\workspace\` | 3.1 ms | 69.6 ms | **6.9 ms** | 8,370 |
| `\users\` | 2.2 ms | 56.5 ms | **4.0 ms** | 97,538 |
| `\node_modules\` | 2.2 ms | 54.0 ms | **4.7 ms** | 6,322 |
| `\zzz-no-such-path\` | 2.4 ms | 52.5 ms | **4.0 ms** | 0 |

- **計器の形を製品に合わせないと採否が反転する。** 同じ変更を単スレッドで測ると
  保持 28-43 ms / 導出:ASCII 50-75 ms（+20〜35 ms）で「見送り」に見えるが、
  製品の走査は rayon 並列であり、実際の追加コストは **+1.6〜3.8 ms**（60 Hz の 1 フレーム
  16.7 ms に対し十分小さい）
- **支配項は `char::to_lowercase()`（Unicode・テーブル参照つきイテレータ）である。**
  ASCII 高速路（`is_ascii()` なら bytewise `to_ascii_lowercase`）で 9.2-11.7× → 1.7-2.3× になる。
  **ASCII 範囲では Unicode 小文字化と ASCII 小文字化の結果が一致する**ためバイト一致は保たれ、
  実インデックスの全 312,377 パスで `normalize_entry_key` との一致を固定してある
- 実運用点で非 ASCII を含むパスは **5,432 件（1.7%）** だけで、そこだけ低速路へ落ちる
- **Unicode 小文字化は並列化の利きが悪い**（単スレッド 148 ms → 16 コア 55 ms で 2.7× 止まり）。
  ASCII 路は 75 → 4 ms（19×）と素直に効く。**倍率だけを見て絶対値を推定してはならない**

### 採用: `normalized_keys` の廃止（-40.32 MiB・IndexCache v5）

索引に持っていた `normalized_keys` を落とし、必要な候補についてだけ `target_path` から
導出する形へ移した（`search/scoring.rs` の `with_normalized_key`）。

| 指標 | 変更前 | 変更後 |
|---|---:|---:|
| `SearchEngine` 常駐 | 137.83 MiB | **97.51 MiB**（**-40.32**・-29.3%） |
| live ブロック数 | 5.00 / entry | **4.00 / entry**（1,561,891 → 1,249,513） |
| `recent_history`（窓を開くたび） | 65.4 ms | **9.8 ms**（-85%） |
| パスクエリ全走査 | 2.1-3.1 ms | 4.5-7.4 ms |

対のレイテンシ実測（同日・同セッション・**各 3 回**）:

| bench（300k・中央値） | 変更前 | 変更後 |
|---|---:|---:|
| `bench_fuzzy_search_scaling` | 3,652 µs | 3,993 µs（**+9.3%**） |
| `bench_new_scaling` | 530 ms | 529 ms（ノイズ内） |

- **1 標本ずつの比較は +29% を示した。3 回ずつ取り直すと +9.3% だった**——A 側の下振れと
  B 側の上振れを突き合わせていた。**退行の疑いこそ標本を増やす**（1 回の比較で
  「#110 の領域」と判断しかけた）
- fuzzy の +9.3% は 1 打鍵あたり +0.34 ms。マッチ**成立後**の履歴照合が導出を要求するため、
  マッチ件数に比例して乗る
- `recent_history` の改善は `normalized_keys` とは独立の欠陥の是正である。高々 8 件を探すのに
  312,377 件ぶんの照合表を毎回組んでいた——**探す側で表を組む**形に反転させ、走査 1 パスにした
- v4 キャッシュを持つ既存ユーザーは、`normalized_keys` を読んで捨てる経路を通る。復元する
  4 本は v5 と同じでどれも v4 に揃っているため **Wave 1 はスキップされたまま**であり、
  **常駐の削減は v5 への書き換えを待たずに効く**（ディスクとロード時ピークだけが据え置き）
  - **「据え置き」がどれだけ続くかを勘定していなかった。** 書き換えの契機は索引の中身が
    変わったときだけであり、**旧版は何日でも残る**——実運用点で実際にそうなり、毎起動
    35.98 MiB を読んで捨てる状態が続いていた（→「採用: 旧版 `index.bin` の自動昇格」）。
    削減を「常駐」と「ディスク・ロード」に分けて書いたところまでは正しく、**後者が
    いつ効くかを書かなかったことが穴だった**

### 採用: `target_path` のフォルダ木接頭辞共有（-42.59 MiB・実装後の実測）

| 指標 | 変更前 | 変更後 |
|---|---:|---:|
| `SearchEngine` 常駐 | 97.51 MiB | **54.92 MiB**（**-42.59**・-43.7%） |
| live ブロック数 | 1,249,513（4.00/entry） | **937,402（3.00/entry）** |
| `SearchEngine` 構築 | 2〜3 ms | **66〜78 ms**（唯一の対価） |
| ロード時ピーク | 122.52 MiB | 138.72 MiB（構築中の一時 Vec ぶん） |
| `recent_history`（実索引・製品） | 9.9 ms | **6.4 ms**（**-35%**） |
| パスクエリ p50（実索引・製品） | 13.1〜21.4 ms | **11.8〜19.3 ms**（**全件で速い**） |
| Fuzzy 検索 300k | 3,706 µs | 3,583 µs（不変） |

**検索経路はどれも遅くなっていない。** 下の「祖先の鎖を持ち回る」で書くとおり、素朴に
組み立てる形では全経路が 1.6〜1.9 倍遅くなっていた——**その中間状態を最終値と読まないこと。**

- **投影 55.02 MiB / 3.00 blocks に対し実測 54.92 MiB / 3.00 blocks**。バイトとブロックの
  両方が一致した——ブロック数は、うっかり per-entry の `Box<str>` が 1 本混ざるとバイトでは
  気づけずに崩れる側の指標である
- **3 回の実行でバイト数・ブロック数とも完全に一致した**（アロケータ計数は決定的）

#### 構築コストの見積もりが 4〜5 倍外れた（計器が払っていなかったもの）

実装前の計器は `build()` を 19.6 ms と測っていたが、実装後の `SearchEngine` 構築は
**55〜96 ms**（3 標本で 96 / 86 / 87、別セッションで 57）である。内訳を計時して測ると
**導出 + intern が約 30 ms、組み立てが約 25 ms**。

差の実体は**計器が `entries` を生かしたままだったこと**である。製品は組み替えの最後に
`Vec<AppEntry>` を消費し、**312,377 個の `String`（`target_path`）を解放する**。
`blocks` が -312,111 動くことがその実体で、`allocs` は +5,723 しか増えない
（`into_boxed_str` は確保し直さない）。**確保を測っていて解放を測っていなかった。**

計器が対象でないものを測っていた件は、この反復で通算 5 件目である（前 4 件は
「`target_path` のフォルダ木接頭辞共有」節の末尾）。**5 件のうち 4 件は「製品の形に
合わせる」で解け、この 1 件だけは「製品と同じ寿命で測る」でしか解けなかった**——
確保と解放は別の軸であり、片方だけを写した計器は嘘をつく。

#### 対のレイテンシ実測（同日・同セッション A/B・各 3 回・300k）

| bench | main | 本改修 |
|---|---|---|
| `bench_fuzzy_search_scaling` | 4082 / 3844 / 3706 µs（最小 3706） | 3583 / 3797 / 3697 µs（最小 3583） |
| `bench_new_scaling` | 510 / 502 / 515 ms（最小 502） | 553 / 556 / 567 ms（最小 **553**） |

- **検索はノイズの範囲で不変**（レンジが重なる）。`entries` が 56 → 32 B/要素になったぶん
  走査が触るキャッシュラインは減るが、それが測れるほどの差にはならない
- **1 標本目だけを見ると検索が 14〜20% 速く見えた**。3 回ずつ取るとレンジが重なる。
  #962 で「1 標本ずつの比較は +29%、3 回ずつだと +9.3%」を踏んだのと同じ形であり、
  **改善の疑いも退行の疑いと同じだけ標本を要する**
- 構築の +51 ms（502 → 553 ms）は実インデックスで測った構築コストと整合する

**`bench_*_scaling` は合成索引であり、全件走査 2 経路を 1 つも含まない**（パス区切りを
含むクエリが無く、`recent_history` も呼ばない）。そのうち `recent_history` だけは製品経路を
実 `index.bin` で測れる（`tests/path_query_cost.rs` の `measure_recent_history_cost` は
実 engine を呼ぶ）:

| `recent_history`（実 index.bin・製品経路・各 3 回） | 最小 |
|---|---:|
| main | 9.9 / 9.9 / 12.3 ms → **9.9** |
| 素朴な組み立て | 17.3 / 18.3 / 17.6 ms → 17.3（+7.4） |
| **祖先の鎖を持ち回る（最終）** | 6.4 / 6.6 / 8.5 ms → **6.4**（**-3.5・-35%**） |

**素朴に組み立てる形では計器の見積もり（+4.2 ms）より製品のほうが悪かった**（+7.4 ms）。
祖先の鎖を持ち回る形にしたところ main を 35% 下回った——**全件走査は 1 件ずつ独立に
組み立てる形ではなく、走査の順序を使う形で書く**。

#### パスクエリのフレームコスト — **フレーム予算を超える**（`measure_path_query_frame_cost`）

実 `index.bin` × `Engine::search`（config 実効 limit・履歴ブースト・top-k 組立込み）。
`has_path_sep` は incremental cache を無条件で無効化するため、**パスを打っている間は
毎打鍵が全件走査**になる。20 回の min/p50/max（µs）:

| query | main p50 | ①素朴 | ②鎖の持ち回り | ③+tie-break | 最終 Δ |
|---|---:|---:|---:|---:|---:|
| `users`（区切り無し） | 844 | 983 | 907 | 909 | +65 µs |
| `c:\` | 21,445 | 37,877 | 26,773 | **19,310** | **-2.1 ms** |
| `c:\users` | 15,384 | 27,116 | 17,994 | **14,090** | **-1.3 ms** |
| `c:\users\` | 15,057 | 28,334 | 18,564 | **13,938** | **-1.1 ms** |
| `\program files\` | 13,115 | 20,855 | 11,458 | **12,215** | **-0.9 ms** |
| `\zzz-no-such-path\` | 13,278 | 21,332 | 11,976 | **11,766** | **-1.5 ms** |

**main の時点で既にフレーム予算（60fps = 16,700 µs）に届いていた**（p50 13〜21 ms）。
①の素朴な組み立てはそれを 1.6〜1.9 倍にして 1 打鍵 1.3〜2.3 フレームにしたが、②③で
**全件が main を下回った**。

- **①→② 祖先の鎖を持ち回る**（`PathCursor`）。索引はソート順ゆえ隣り合うエントリは祖先を
  共有する。毎回根まで辿って全部書き直すのをやめ、**バッファを巻き戻して 1 段だけ書き足す**。
  親が鎖に載っているかの判定は `u32` の線形走査でメモリを追わない。外れたら作り直すので
  順序が乱れた入力でも結果は変わらない
- **②→③ tie-break を index 比較へ**。`c:\` は全件が同スコア（`PATH_BASE - 0`）になるため
  tie-break が総当たりで発火し、1 回ごとに両辺を組み立て直すと走査全体を支配する。
  整列済みかを**構築時に測って 1 bit で持てば**（`PathStore::sorted_by_path`）、index の順序が
  フルパスのバイト順と一致するので組み立てずに済む。**文書上の契約に寄りかからない**のが
  要点である——`SearchEngine::new` は任意順を受け取れるので、契約にすると破れたとき静かに
  順序が変わる。測って持てば、破れた入力は遅い経路を通るだけになる
- 区切りを含まないクエリの +65 µs は `heap_into_results` が `target_path.clone()` から
  `to_path()`（木を辿って組み立て）へ変わったぶんで、`result_limit`（既定 200）件に比例する。
  1 フレームの 0.4% であり体感には乗らないが、**`result_limit` を上げると比例して増える**

**①の値を最終値と読まないこと。** ①は「木から組み立てる」を素直に書いただけの状態であり、
走査の形（ソート順の連続・スコアの分布）を使い切っていない。計器の見積もり（+3 ms）が
製品の①（+8〜16 ms）と食い違ったのも、写しが `Engine::search` の他の処理を数えないためで、
**どちらも「測る対象を製品に合わせる」で同じ向きに解けた**。

#### 残余

- `drop` 後の残留が 0.00 → **0.11 MiB**。3 回とも同値で、索引規模に対して固定量である。
  v5 キャッシュヒット経路は Wave 1/2 をスキップするため従来 rayon を使っておらず、
  `PathStore::build` が**グローバルスレッドプールを初めて起動する**ためと**推定される**
  （プールの per-worker 状態は解放されない）。**推定であって実測ではない。**

### 採用: PATH スキャンの問いを反転（確保 314,395 → 2,066・反復 9）

**形式の軸ではない。** `index.bin` にも `SearchEngine` にも触れておらず、**索引の常駐は
35.44 MiB / 411,413 ブロックのままバイト単位で不変**である。削ったのは起動経路に挟まる
`indexer::scan_path_env` の区間だけで、`IndexCache` のバージョンは v6 のまま。

`main.rs` はロードと構築の**間**で PATH 上の実行ファイルを索引へ混ぜる。この関数は
`scan_path_dirs` へ既存エントリのスライスを渡し、**全件の正規化キーを `HashSet` へ積んでから**
候補を引いていた。実運用点では 312,691 件を積んで **101 件**を照合する比率である
（101 は `show_hidden_system = false` と PATH ディレクトリ間の重複排除を通した後の候補数。
実運用点では既存エントリと 1 件も重複しないので、返り値の件数も同じ 101 になる）。

#### 分割実測が 95% を 1 か所に名指しした

`scan_path_dirs` は `seen` を列挙ループより**前に無条件で**構築するため、写しを切り出さずに
引数だけで分割できる（`""` を渡すと `"".split(';')` は空文字列 1 個を返し、`trim` で
`continue` するのでループ本体は 0 回走る）。

| 呼び方（各 3 回の最小値） | 壁時計 |
|---|---:|
| `(&user_path, &[])` — ディレクトリ列挙のみ（PATH 17 個） | **9 ms** |
| `("", &entries)` — `seen` 構築のみ | **194 ms** |
| `(&user_path, &entries)` — 全体 | 181 ms |

**ファイルシステムの読みは 9 ms しかない。** 区間の実体は「312,691 回の
`normalize_entry_key`（1 件ごとに `String` を確保）＋ `HashSet` への挿入」であった。

#### A/B（同日・同セッション。before は 3 標本、after は 13 標本）

| 指標 | before | after |
|---|---:|---:|
| **PATH スキャン（壁時計・最小値）** | 186 ms（3 標本・186〜197） | **54 ms**（13 標本・54〜73・-132・-71%） |
| **PATH スキャン（確保回数）** | 314,395 | **2,066**（**-99.3%**） |
| 区間の peak | 48.10 MiB | **0.03 MiB**（-48.07） |
| 返り値 | +101 件 | +101 件（**不変**） |
| 索引の常駐 / ブロック | 35.44 MiB / 411,413 | 35.44 MiB / 411,413（**不変**） |

**3 区間の和を削減幅として書かない。** 消えるのは PATH 区間の 132 ms だけで、ロードと構築は
この変更の対象外である（実測はぶれの範囲に収まる——ロード 119〜167 ms・構築 54〜69 ms）。
和を取ると対象外のぶれが削減幅に混ざる。

**受け入れは壁時計ではなく確保回数で判定した。** 同じコードの壁時計が 54〜73 ms（+35%）と
ばらつくのに対し、確保回数は **13 回測って 13 回とも 2,066** である。**最小値どうしを比べる
のは規約**（外乱の下限を取る）だが、レンジがこれだけ広い以上、54 ms を「起動時にいつも
そうなる時間」と読んではならない。**節の見出しにも壁時計を載せていない**——測り直すたびに
見出しが動き、それを正準形で引用する `.rs` まで一緒に直すことになるからである。

#### 事前フィルタの健全性（偽陰性を出さないことの根拠）

候補側に小さな索引を作り、既存エントリは**ファイル名だけ**を篩に掛けて素通しする。
篩を抜けた少数だけがフルパスの正規化キーまで進む。両辺が `normalize_file_name_key_into`
という**同じ 1 つの手順**を通ることだけが一致の根拠であり、**論証の全文と、それが乗って
いる前提は同関数の doc が正本**（件数をここで数え上げない——前提を足したときに写しだけ
古い数のまま残る）。逆向き——別ディレクトリの同名ファイルが通り抜けること——は篩の仕様
である。

**検知器は 5 本とも「外して落ちるか」を実測した**（反復 8 で 3 本中 1 本が落ちなかった教訓）:

| 改変 | 落ちたテスト |
|---|---|
| フルパス比較を無条件 true に | `..._keeps_candidate_that_only_shares_a_file_name`（＋ clippy の `key is never read`） |
| 篩から正規化を外す | `..._skips_existing_paths_written_in_other_notations` |
| 返り値を逆順に | `..._preserves_enumeration_order` |
| `rejected` の添字を 1 つずらす | `..._rejects_only_the_matching_candidate_among_several` |
| 篩のバケットを `idxs.first()` へ縮める | `..._rejects_the_right_one_among_same_named_candidates` |

どの誤りも**結果が「それらしく」出る**——起動できるはずの exe が黙って減るか、重複エントリ
が索引へ入るかで、panic もテスト失敗も起きない。**既存の 6 本は 1 つも捕まえなかった。**

下 2 行は反転が**新しく作った故障モード**である。旧実装は判定と採用が `if seen.insert(key)
{ push }` の同一式にあり、どれを落とすかがずれること自体が起きなかった。3 段（篩の索引 →
`rejected` → `zip`）へ分解した以上、ずれを捕まえる検知器も同時に要る——**下から 2 行目は
最初の実装で書き落とし、最下行はその修正でも書き落とした**（`Vec<usize>` が存在する理由
そのものである「同名候補が複数」の経路を、相異なるキーのテストで埋めたつもりになっていた）。

#### 却下: `PathStore` に「このパスが在るか」を問う経路

計画段階では索引側へ問う形を想定していた。**順序の話に還元してはならない**——起動経路では
`scan_path_env` が `Engine::from_material` より前に走るのは
確かだが、それは**動かせる事実**であり、却下の根拠としては弱い。壊れない根拠は次の 3 つ。

1. **背景ビルド経路には engine が在るのに使えない。** PATH マージの時点で `state.engine` は
   生きているが、そこが映すのは**旧索引**である。照合すべき相手は直前の `rebuild_and_save` が
   返した新しい材料で、順序をどう動かしても engine は正しい答えを持たない。**「まだ
   `Vec<AppEntry>` のまま `PathStore` を持たない」と書いてはならない**（かつてそう書いていた）
   ——`rebuild_and_save` は `IndexMaterial` を返し、その `tree()` はまさにその地点で使える。
   **却下の根拠は「新しい索引がまだ無い」ではなく「engine が映すのが旧索引である」の側だけ**
   である（`main` でも同じ形が既に古かった——あちらも木を返していた）。**行番号を書かない**
   ——差分のたびにずれるので、位置は `scan_path_env` の呼び出しで指す
2. **`PathStore` は `search` の内部型**（`pub(super)`）であり、indexer から問うには
   search に新しい公開面を開くことになる——「持たない・導出する」設計への逆行
3. **101 件の問いのために全件の索引を建てるのは、反転前と同じ比率の誤りである**

反転は `scan_path_dirs` の内部で閉じるので、そちらを採った。

#### 残余: 約 45 ms（天井）

after の 54 ms（最小値）のうち列挙が 9 ms なので、残りは 312,691 件のファイル名を小文字化
してハッシュを引く**純粋な計算**である。確保は 2,066 まで落ちきっているので、**取れる額の
上限は約 45 ms**（最小値どうしの差ゆえ下限寄りの見積もりで、実行によってはもっと大きい）。

**「篩をもっと粗くするしかない」と書いてはならない**（この節の初稿がそう書いていた）。
粗い篩は残った選択肢どころか、**唯一成立しないもの**である——長さは正規化で保存されない
（`char::to_lowercase` は UTF-8 長を変える）ので非 ASCII 側で偽陰性が出る。健全化するには
ASCII 限定の分岐＝別実装が要り、`snotra-core/CLAUDE.md`「`normalize_entry_key` の冪等性契約」
が名指しで禁じている。**額ではなく健全性で落ちる。**

**「候補の表現が複数あるなら全部測る」を省いたのではない。** 反復 8 では 2 案を実装して
比べたが、ここでは**天井そのものを測った**——列挙 9 ms は表現を変えても消えない。残る道の
一覧は下の「次の反復の候補」節（実測と見積もりを分けてある）。

### 次の反復の候補（PATH 区間の残余から・実装前）

**実測と見積もりを分けてある。** 実測の記録（上の「採用」節）へ予定を混ぜると読み手が
両者を区別できないので節を分けた。反復 3 の「構造前提と 3 つのコスト（実装前）」と同じ
位置づけである。

**撤去条件**: 反復 10 以降でどれかを採ったらその行を「採用」節へ移し、測って成立しないと
分かったら「試みたが機能しない手法」へ移す。**どちらでもないまま残してはならない**——
実測の書に予測が住み着くと、次の読者が両者を取り違える。

| 候補 | 実測されているもの | 見積もり | 注意 |
|---|---|---:|---|
| アイコン剪定を篩へ通す | `drain_index` が `IconCacheState` の lock 内で索引 312,625 件のフルパスを組み直す。生き残るのは高々 `icon_cache_cap`（既定 1,000）件 | 組み直し **24.9 ms → 数百件ぶん** | `reject_existing` と同じ形（`IndexTree::file_key_into` で篩→通ったぶんだけ `raw_path_into`）。`IconCache::keys()` の新設が要る。**lock の外へ出す変種は採らない**——stale な集合で新規挿入を捨てうる＝挙動変更 |
| `IndexTree` のフィールドを private 化 | 現在 `pub(crate)` ゆえ、crate 内から構造体リテラルで `from_parts` の検証を迂回して組める | 額ではなく構造（不変条件を表現不能にする） | `columns()` / `into_columns()` の 2 口を出す。保存経路の `Cow::Borrowed` × 5・`PathStore::adopt` の分解・テストの `tree.names[0]` 系を全部書き換えることになるので、差分の外へ広がる |
| `reject_existing` の走査を rayon で並列化 | 走査は逐次で約 45 ms | **-32 ms** | 倍率 3.6 は `entries_digest` の 43 → 12 ms を**別の関数へ持ち込んだ外挿**。**「read-only だから digest と条件が揃う」は誤り**——ループは `rejected[i]` へ書き、`file_buf` / `full_buf` を毎反復書き換えるので、`map_init` で per-worker に割るか添字を収集して reduce する形が要る |
| `CachedLower::Collapsed.lower_names` を `Vec<Option<Box<str>>>` へ | 反復 11 で受容した重なり +20.74 MiB の内訳は 4 本の spine（2.38 + 2.38 + 7.16 + 7.16 = 19.08 MiB）でほぼ説明がつく | **-2.38 MiB**（spine の要素幅 24 → 16 B × 312,649）＋ cache-miss で `assemble` の `into_boxed_str` が move 1 つになり **4.77 MiB の一時確保が消える** | **`LowerFileName` に削り代は無い**（`Text(Box<str>)` にしても 24 B のまま——`String` の `Cap` が持つ niche が fieldless 2 変種を吸う。実測済み）。オンディスク型に触るので `/persistence-check` 対象・postcard の線上表現が `String` と `Box<str>` で同一かは**未実測**（ずれれば版バンプが要り額に見合わない）。cache-HIT 枝では純益がほぼ無い（今の `.collect()` が postcard の余剰 spine を詰め直す役目を兼ねている） |
| `by_file_key` を `FxHashMap` へ | 引く回数は既存側の 312,691 回 | -3〜5 ms | 依存は `Cargo.toml` に既にある（`history.rs` が先例）。単価差は一般値からの見積もりで未実測 |
| `path.is_dir()` をやめて `entry.metadata()` の属性語を読む | **242 回の syscall**（PATH 配下の全エントリ数） | 上限 9 ms | 列挙 9 ms が天井の根拠なので、**ここが削れると天井そのものが下がる**。⚠ `Path::is_dir()` はリンクを辿り `DirEntry::metadata()` は辿らない——reparse point / junction での挙動差は効率ではなく仕様の判断 |
| 拡張子照合の `to_ascii_lowercase()` を `eq_ignore_ascii_case` へ | **242 回の確保**（残る 2,066 の 12%） | µs 桁 | 壁時計では無視できる。挙げてあるのは受け入れ判定が確保回数だからで、額で採る話ではない |
| 列挙をロードと重ねる | `enumerate_path_candidates` は `existing_entries` を引数に取らない（**構造的に独立**） | ≤9 ms | 直列である必然が無い。ディスク I/O を競合させるので重なりは目減りする——測るなら総起動時間で |
| 篩の `rfind(['\\', '/'])` をバイトの `rposition` へ | 呼ばれる回数は 312,691 | **未実測** | 「char 配列パターンがバイトループより遅い」という前提自体が未検証。**先に単価を測ること** |

**触らないと判断したもの**（額を測ったうえで）: `enumerate_path_candidates` の `key.clone()`
（≤候補数の確保・約 7 µs）と `by_file_key` の `Vec<usize>`（同オーダー）。どちらも複雑さの
増分が額に見合わない。

### 採用: `PrebuiltIndex` を `CachedMasks` 込みで建てる（設定からの再構築・**未実測**）

`rebuild_and_save` が `save_cache_sorted` の返す派生データを捨てており、`drain_index` は木だけを
受け取って Wave 1/2 を建て直していた（反復 11 が起動経路だけを繋いだ残余）。**木と派生データを
`IndexMaterial` へ束ねて組のまま運ぶ形にし**、索引を建てる入口を `from_material` の 1 つへ
まとめて繋いだ。
**`index.bin` の形式には 1 バイトも触らない**（`INDEX_CACHE_VERSION` 据え置き・
`index_cache_on_disk_format_is_stable` の golden が差分ゼロで緑であることがその証拠）。

| 指標 | 値 |
|---|---|
| 構築段 allocs / peak / 壁時計 | **未実測** |

**額は下の反復 11 の実測と同一の導出である。** 通る関数列は `derive_columns` →
`into_cached_masks` → `new_with_cached_masks` で、cache-miss 起動で測った構築段
539 → 24 ms・allocs -2,716,070・peak -83.27 MiB がそのまま当てはまる。**この数字を上の表へ
書き写していないのは、それが別の経路で取った標本だからである。**

**独立に測っていない理由は 2 つ。** (1) 同じ関数列を通るので、測っても反復 11 の再確認にしか
ならない。(2) **この枝は起動レイテンシではない**——背景スレッドと設定ダイアログからの再構築で
あり、起動予算の判断材料にならない。計測ハーネスを新設する案は却下した: `IndexTree` /
`CachedMasks` はどちらも `Clone` を持たないのでフルスキャン 2 回か `Clone` の追加が要り、さらに
`derive_columns` を `pub` へ開ける必要が生じる——**その関数を `pub(crate)` に留めている理由
（検知器がファイルシステムと、型に無いロック契約を巻き込まないこと）が形骸化する**取引になる。

**呼び忘れを構造で消した。** マージ（マスクへ追記 ＋ 木へ根として追加）は
`IndexMaterial::extend_with_path_entries` の 1 メソッドで、フィールドが private ゆえ
**crate 外から片方だけ伸ばす形は書けない**（`IndexTree::extend_with_roots` も `pub(crate)` へ
下げた）。**「表現不能化ではない」という受容宣言は 4 か所から消した**——`&mut Option<_>` の
署名だけで閉じたと書いていた版は偽で、`&mut None` を渡す形が型を通っていた（同じ差分の検知器が
それを実演していた）。**crate 内ではまだ書ける**が、`snotra-core` の中に PATH マージの
呼び出し点は無い。

**検知器は 3 種の変異で落ちることを実測した**（`search/tests/build.rs`）: (1) 追記を欠く →
`path_merge_after_cache_miss_agrees_with_deriving_over_the_extended_tree` が
`SearchEngine: 派生文字列の長さが entries と一致しない`（`assemble` の `debug_assert`）で落ちる。
(2) 木への追加を `if let Some` の内側へ移す → `path_merge_extends_the_tree_even_without_derived_data`
が落ちる。(3) **マスクを取り落とす** → 同じ 1 本目が `has_masks()` の assert で落ちる——この 3 つ目は
A/B 一致では捕まらない（両側が木から導出するので**一致は成立したまま削減だけが消える**）。
`migemo` の両設定で落ちることも、ループ順序を反転させて確かめた。

**分岐の写しを 5 か所から 0 へ落とした。** 派生データの有無で建て方を選ぶ `match` は
`PrebuiltIndex` / `Engine` / `SearchEngine` の 3 層・5 呼び出し点に散っていた。`PrebuiltIndex`
にだけ集約用のコンストラクタを足す案は**そのうち 1 か所しか直せない**（根は最下層の 2
コンストラクタで、上の 2 層はその写し）ため採らなかった。

**型が不変条件を持つようにした。** `load_cache_in` は `IndexTree::from_parts` で**木の整合しか**
検証しておらず、切り詰められた `index.bin` は「木より短いマスク」として起動経路へ入りえた
（`assemble` の検証は `debug_assert` ゆえ release で消え、初回検索の添字外 panic になる）。
ディスクから来る全枝を `IndexMaterial::from_untrusted` へ通し、列長が揃わなければ全走査へ落とす。

### 採用: 保存が返した派生データを cache-miss がそのまま使う（構築 539 → 24 ms・反復 11）

`save_cache_sorted_in` が `char_masks` / `file_name_char_masks` / 潰し済み派生 2 本を計算して
`index.bin` へ書いた**直後に捨てて**おり、cache-miss の枝は `new_from_tree` で木を全件実体化
してから同じものを建て直していた。返り値を `(IndexTree, CachedMasks)` へ広げ、
`LoadOrScanResult` へ載せた（**当時は木と別のフィールドだった**——組へ束ねたのは上の反復）——**`index.bin` の形式には 1 バイトも触らない**
（`INDEX_CACHE_VERSION` は据え置き。`index_cache_on_disk_format_is_stable` の golden が緑の
ままであることがその証拠）。

**計器は cache-HIT 枝しか測らない。** `SNOTRA_CONFIG_DIR` を temp へ向け `config.toml` だけ
置いて（`index.bin` 不在）初回起動を再現した。scan 集合は実物のまま・312,648〜312,649 件・
`migemo_enabled = false`。

| 指標 | A（変更前） | B（変更後） | 差 |
|---|---:|---:|---|
| **構築段 allocs** | 2,801,461 | **85,391** | **-2,716,070・-97.0%** |
| **構築段 peak** | 92.81 MiB | **9.54 MiB** | **-83.27** |
| **構築段 壁時計** | 539 ms | **24 ms** | **-515・-96%** |
| 構築段 blocks | +98,452 | -3 | ロード段へ移動（下記） |
| ロード段 live | +20.22 MiB | +40.96 MiB | +20.74（寿命が延びた分） |
| ロード段 blocks | +312,918 | +411,374 | +98,456 |
| **合計 peak** | 113.03 MiB | **50.50 MiB** | **-62.53** |
| **索引の常駐** | 35.44 MiB | 35.44 MiB | **不変** |
| **`index.bin`** | 16.52 MiB | 16.52 MiB | **不変** |

構築段の peak 9.54 MiB は `PathStore` の `entries` Vec 本体ちょうど（312,649 × 32 B）である
——`adopt` の 1 本を残して、構築段の一時確保が消えた。

**壁時計のロード段は比較対象ではない。** 2 回の実行で `scan_ms` が 35,573 対 22,187 ms と
13 秒ずれており、これはファイルシステムのキャッシュの温度であってこの変更とは無関係である。
上の表で読めるのは**確保回数・peak・blocks（決定的）と、構築段の壁時計**だけである。

**cache-miss の直後に PATH エントリをマージする経路は、この変更で初めて生きた。** 変更前は
`cached_masks` が `None` ゆえ `extend_cached_masks` は呼ばれず、`new_from_tree` が拡張後の木
から導出していた。今は「マスクへ追記 → 木へ根として追加 → 派生データ込みで建てる」の順に変わる
——`assemble` の長さ検証は `debug_assert` ゆえ release では消えるので、追記の呼び忘れは
添字 panic か沈黙の食い違いになる。検知器は
`path_merge_after_cache_miss_agrees_with_deriving_over_the_extended_tree`（呼び忘れを再現する
変異で実際に落ちることを確認済み）。

**予測は 8 項目中 7 項目が当たり、1 項目が外れた**（実装前に `ab96dcd` へ記録）。外れたのは
「構築段 blocks は +98,452 のまま不変」で、実測は -3 だった。**潰し方がずれた合図ではなく、
不変量の名指しを取り違えていた**——索引が持つ 98,451 個の文字列ブロックは消えたのではなく、
構築段からロード段へ**移動した**（合計 blocks 411,370 → 411,371 は不変）。同じ現象を予測 6
（ロード段 live +22.7 MiB）では正しく当てており、**同一の寿命変化を 2 つの指標で逆に読んで
いた**のがこの外れの正体である。

**合計 peak は予測を大きく上回って減った**（予測は「-36 MiB には届かない」で、実測 -62.53）。
保存側 4 本が `assemble` まで生き延びる分の相殺を見込んでいたが、消えた `materialize` の
一時確保がそれを大幅に上回った。

**候補表の ⚠ は実装前に discharge した。** 「save 側で潰した `Collapsed` が `assemble` の
`Measured` 経路と同じ表現になるか」は、差が `lower_file_name(原文)` 対
`lower_file_name(組み直し)` の一点に縮む（`name` は木が逐語で持ち、判定は両経路とも
`query::measure_derived_sharing` の 1 か所を通る）。`path_store_raw_matches_target_path_over_real_index`
を実行して **312,648 件全件のバイト一致**を実測し、純関数ゆえ像も一致することを確かめた。
機構としては `search/tests/build.rs` の `save_side_collapse_*` 2 本が守る（実データ側は
**原文をファイルシステム走査から取る**——`index.bin` から取ると組み直し対組み直しの不動点に
なる。実測 312,649 件一致）。

**当時の残余（意図的）は解消済み**: `rebuild_and_save` → `drain_index` の枝は
木しか受け取らず、返るマスクを捨てていた。1 反復 1 候補の規約を割るため当時は
候補表へ回し、上の「採用: `PrebuiltIndex` を `CachedMasks` 込みで建てる」がそれを閉じて候補表
から外した。

### 採用: `target_path` の木表現をディスクへ（`index.bin` 51.34 → 16.52 MiB・IndexCache v7・反復 10）

下の「成立条件」節で立てた候補 A の実装後。**メモリ側で既に持っていなかった `target_path` を、
ディスクからも消した**——木の 5 列（`names` / `is_folder` / `parent` / `aux` / `table`）と
整列の旗をそのまま書き、ロードは実体を 1 つも作らずに `IndexTree` を受け取る。

| 指標 | v6 | v7 | 差 |
|---|---:|---:|---|
| **`index.bin`** | 51.34 MiB | **16.52 MiB** | **-34.82・-67.8%** |
| **ロード（壁時計・`total_ms`）** | 216 ms | **80 ms** | -136・-63% |
| `cache_load`（うち read） | 204 ms（16） | **62 ms**（4） | -142（-12） |
| `digest` | 11 ms | 17 ms | +6（組み直しのぶん・下の予測どおり） |
| **ロード段の確保回数** | 730,515 | **411,621** | **-318,894** |
| ロード段の peak | 170.90 MiB | **64.89 MiB** | **-106.01** |
| 構築の解放 | -3 blocks / 6 allocs | -3 blocks / 7 allocs | 不変 |
| 索引の常駐 | 35.44 MiB | 35.44 MiB | **不変** |

**v6 側は v7 のコードで v6 の `index.bin` を読ませた数字である**（フォールバック枝）。
反復 8 の表に載る 724,061 / -312,592 とは別物で、直接引き算してはならない——木の導入で
実体化と解放の**居場所**が既に動いており、v7 のコードでは v6 を読んでも構築の解放は 0 に
なっている（実体化と解放がロード区間の内側に収まったため）。エントリ数も 312,691 →
312,625（-66）と、v7 を書かせるための全走査の間に実データが動いている。

**事前登録した予測は当たった**（実装前に commit へ書いた: ロードの確保 → 約 411,370 /
構築の解放 → 約 0）。実測 411,621 は予測比 +251、エントリ数の差を勘案しても +317 で、
全確保の 0.08%。**外れていれば「まだどこかが `target_path` を実体化している」の合図**に
なるよう先に書いておいた数字である。

`index.bin` の内訳（312,625 件・payload 16.52 MiB。残余は 0 B）:

| 項目 | MiB | B/entry |
|---|---:|---:|
| 木（5 列 + 整列の旗） | 12.04 | 40.4 |
| — うち `names` | 10.56 | 35.4 |
| — うち `parent` | 0.88 | 2.9 |
| — うち `is_folder` / `aux` | 0.30 / 0.30 | 1.0 / 1.0 |
| — うち `table`（拡張子 + 根のフルパス・99 要素） | 0.01 | 0.0 |
| `file_name_char_masks` / `char_masks` | 1.31 / 1.28 | 4.4 / 4.3 |
| `lower_file_names`（3 状態） | 1.11 | 3.7 |
| `lower_names`（潰し済み） | 0.79 | 2.6 |

**残るディスクの最大項目は `names` の 10.56 MiB になった**（全体の 64%）。v6 まで
最大だった `target_path` 36.01 MiB は `parent` + `aux` + `table` の 1.19 MiB に置き換わっている。

計測時の注意（2 つとも実測で踏んだ）:

- **開発機の `index.bin` は放っておくと旧版のまま残る**。背景再スキャンが昇格させるまで
  ロードはフォールバック枝を通り、木は `target_path` を実体化してから建て直される
  ——**出力の見た目は成功時とまったく同じ**である。`tests/memory_footprint.rs` は
  ヘッダーを直接読んで版を報告し、旧版なら「この数字は現行形式の常駐ではない」と出す
  ようにした（ロードより**前**に読む。cache-miss の枝は測定区間の中で現行版を書くため）
- **`index.bin` の内訳計器は `sorted_by_path` を数え落としていた**（残余 +1 B で落ちた）。
  1 バイトでも残余が 0 でなければ帰属は誤りであり、この検算は「列を 1 本落とした誤り」と
  「旗を落とした誤り」を区別しない

### 候補 A（`target_path` の木表現をディスクへ）の成立条件 — 実装前の実測（反復 10）

ディスクの `target_path` 36.01 MiB は、メモリ側では既に持っていない（`PathStore` の
フォルダ木接頭辞共有で 0.01 MiB）。ディスクと構築の**両方**に効く唯一の候補である
——ロードの確保 724,061 のうち 312,691（43%）がこの `String` で、構築の解放
312,592 も同じものである。

**成立条件は「速いか」ではなく「原文をバイト一致で復元できるか」だった。** ロード側が
実体を持たなくなると、背景再スキャンの比較（`entries_digest`）はフルパスを組み直しながら
取ることになる。ここが 1 バイトでもずれれば digest が毎起動食い違い、**再スキャンが永久に
走り続ける**——結果は正しいまま静かに遅くなるので挙動テストでは捕まらない。

| 測ったもの | 値 | 計器 |
|---|---:|---|
| 原文の復元が `target_path` とバイト一致する件数 | **312,691 / 312,691** | `path_store_raw_matches_target_path_over_real_index` |
| 全件の組み直し（逐次・バッファ 1 本を使い回す） | 28.0 ms | `measure_raw_path_rebuild_cost_over_real_index` |
| 全件の組み直し（並列・刻みは digest と同じ 8192） | **7.5 ms** | 同上 |

**払い戻しは 7.5 ms である**（`digest_ms` は 8〜17 ms なので digest 区間は約 2 倍になる）。
削減の見込み——ディスク 36.01 MiB とロード確保の 43%——に対して小さく、**A は成立する**。

digest の値そのものは測っていない。混ぜるのは `name` / `target_path` / `is_folder` の 3 つ
だけなので、`target_path` がバイト一致し他の 2 つが不変なら**構成上**一致する。

#### 見送った: digest を「パス」ではなく「木」に対して取る（実装前・反復 10 では採らない）

組み直しの 7.5 ms は、digest がフルパスのバイトを混ぜることから来る。**木の列
（`parent` / `aux` / `table` / `name` / `is_folder`）を直接ハッシュすれば 0 になる**
——木からフルパスへの復元がバイト一致（上表）である以上、木が同じことと
エントリが同じことは同値なので、判定の鋭さは変わらない。

**それでも反復 10 では採らない。** `entries_digest` の意味そのものが変わるため既存の
digest テスト群が一斉に無効になり、背景再スキャン側は比較のたびに木を建てる（約 23 ms）
ことになる。**1 反復 = 1 候補**の規約に従い、7.5 ms は払って先へ進む。採るなら独立した
反復で、`sorted_comparison_ignores_enumeration_order` 系の検知器を作り直してから。

#### マージ順に守るべき整列の不変条件は無い（実測）

`PathStore.sorted_by_path` は構築時の実測値であり、これが真なら `cmp_paths` は組み立てずに
index を比べる。**実運用点では既に偽である**: 索引単体（312,691 件）は真だが、`main.rs` が
PATH の 101 件を**後ろへ足した**形では偽になる（PATH の実行ファイルはバイト順で途中に入る）。
ゆえに木表現へ PATH エントリを足す設計に、順序について守るものは無い。

**計測ハーネスと製品でここが食い違う**——`tests/memory_footprint.rs` は PATH スキャンの
返り値を捨てるので、その `PathStore` は真の側にいる。tie-break のコストを測るときは
この差を勘定に入れること。

### 採用: 派生文字列の共有をディスクへ（ロード 224 → 140 ms・IndexCache v6・反復 8）

メモリ側は反復 4・5 で `lower_names` / `lower_file_names` の 86.6% / 81.9% を共有で潰していたが、
**ディスクだけが全件を実体で持っていた**——読んで確保して `assemble` が即座に捨てる形である。

| 指標（A/B 各 3 回の最小値） | v5 | v6 |
|---|---:|---:|
| **ロード（壁時計）** | 224 ms | **140 ms**（-84・-37.5%） |
| `cache_load`（うち read） | 205 ms（29） | **127 ms**（14） |
| **構築（壁時計）** | 89 ms | **58 ms**（-31） |
| ロード段の確保回数 | 1,250,970 | **724,061**（**-526,909**） |
| 構築の解放 | -839,521 | **-312,592**（526,929 減） |
| ロード段の peak | 193.65 MiB | **154.38 MiB**（-39.27） |
| `index.bin` | 71.07 MiB | **51.34 MiB**（-19.73） |
| 索引の常駐 | 35.44 MiB | 35.44 MiB（**不変**） |

**起動経路の合計で -115 ms**（ロード -84 + 構築 -31）。常駐が動かないのは設計どおりである
——メモリ側の表現は反復 4・5 から変わっておらず、**変わったのは「そこへ至るまでに何個
確保するか」だけ**である。

#### 展開を `PathStore::build` の後へ下ろした（`/simplify` の裁定）

当初は `Collapsed` の展開を木の構築より**前**に置き、共有の旗を立てる位置を `Vec<usize>` へ
控えていた（旗は `CompactEntry` の空きパディングへ書くので、木が建つ前には立てられない）。
`/simplify` の 4 枠のうち 2 枠が「制約に足場で応えるより、制約が満たされる場所まで展開を
遅らせるほうが構造が 1 段浅くなる」と指摘し、1 枠が「10 MiB を構築ピーク中に生かすので
逆効果」と反対した。**実測が裁定した**:

| | 控える形 | 下ろした形 |
|---|---:|---:|
| 構築（壁時計） | 68 ms | **58 ms** |
| 構築区間の peak | 121.26 MiB | **119.26 MiB** |
| ロード段の peak / 確保回数 / 常駐 | — | すべて不変 |

ピークは**増えずに 2.00 MiB 減った**——`Vec<usize>`（256,092 件 × 8 B）が消えた額そのもの
である。反対側の見積もりが外れた機序は、`Vec<LowerFileName>` が**ロード段から既に生きて
いる**ことにある。変換を早めても遅らせても構築中の生存量は変わらず、動いたのは足場の分だけ
だった。同時に `UnpackedDerived` 型別名・早期 `return`・7 引数の補助関数
（「`assemble` の外へ出してはならない」という**コンパイラが強制しない約束**を伴っていた）が
すべて消えている。

#### 予測がバイト単位で当たった

着手前に計器で測った天井と、実装後の実測:

| | 天井（実装前） | 実測（実装後） |
|---|---:|---:|
| `lower_names` + `lower_file_names` | 1.90 MiB | **0.79 + 1.11 = 1.90 MiB** |
| `index.bin` | 51.34 MiB | **51.34 MiB** |
| ロード段の確保減 | -527,000 | **-526,909** |

**掛け算では出せなかった値である。** 重複率（86.6% / 81.9%）から算術すると可変長 varint と
enum のタグが落ちる。計器は 2 案（3 状態 enum / `Option` + 旗の `Vec<bool>`）を**両方**測り、
0.30 MiB の差で前者を採った——推論ではなく実測で選んだ。

#### 構築も速くなった（反復 4・5 の増分が戻った）

反復 4・5 は共有を測る比較と `Box<str>` の解放で構築を 1 反復あたり +17 ms 増やしていた
（57 → 75 ms）。v6 は**記録側が測った結果をそのまま表現として使う**ので、その比較も解放も
起きない。解放は 839,521 → 312,592 で、消えた 526,929 が反復 4・5 で作っていた分である。

#### 表現: `Option` では足りない

`lower_file_names` の `None` には「file name 成分が無い」という**先客**がいる。そこへ
「`lower_names[i]` と同一」を重ねると 2 つの意味が同じ表現に乗る——メモリ側は
`CompactEntry::file_name_is_lower_name`（構造体の空きパディング）で解いたが、**ディスクに
空きパディングは無い**。3 状態の enum（`Absent` / `SameAsLowerName` / `Text`）はタグ 1 バイトで
済み、旗を別 `Vec<bool>` で持つ案より 0.30 MiB 小さく、**意味も型に載る**。

#### 潰し済みかどうかを型で区別する（理由は「壊れるから」ではなく「無駄だから」）

`CachedLower::{Collapsed, Raw}` → `DerivedStrings::{Collapsed, Measured}` と型で分け、
潰し済みの列が測定ループへ流れないようにした。取り違えはコンパイルを通らない。

**当初この分離を「結果を壊しうる唯一の点」と書いたが、それは過剰だった。** 潰し済みの列を
測定ループへ流しても**結果は変わらない**——`measure_derived_sharing` が `lower_name: &str`
（`Option` ではない）を取るため、潰れた要素は `let Some(..) else { continue }` で素通りする。
「`None` どうしの一致で file name 成分の無いエントリに旗が立つ」という危険は、**判定を
一元化する前の `Option<&str>` どうしの比較に固有**のものであり、その一元化自体がこの反復の
中で先に済んでいた。`/simplify` の後に検知器を書き、測り直す実装へ戻す実験をして初めて
「落ちない」と分かった（2026-08-08）。

分離の実益は残る: **312,690 回の比較を丸ごと省ける**ことと、呼び出し点から「この版は
測り直さない」が読めることである。構築 89 → 58 ms のうち、比較ぶんがここに当たる。

**判定そのものは `query::measure_derived_sharing` の 1 か所**で、記録側・追記側
（`extend_cached_masks`）・適用側が同じ関数を通ることだけが、ディスクとメモリで潰れ方が
一致する根拠である。**そしてその関数のシグネチャ（`&str` を取る）が、上の危険を型として
表現不能にしている**——文書で禁じるより強い。

### 採用: 旧版 `index.bin` の自動昇格（ロード 337 → 212 ms・反復 7）

**削減の実装ではない。** 反復 2（`normalized_keys` の廃止・IndexCache v5）が**実運用点へ届いて
いなかった**のを届かせた反復である。実 `index.bin` は v5 導入から日が経っても **v4 のまま**で、
毎起動 35.98 MiB を読んで 312,377 個の `String` を確保しては捨てていた。

書く契機は cache-miss と背景再スキャンの `Changed` だけであり、索引の中身が変わらない限り
save は来ない。`IndexCacheV4` の doc が書いていた「次回の save で v5 へ昇格する」は、
**その「次回」が来ない経路を勘定していなかった**——実装より強い主張で、下の
「`normalized_keys` の廃止」節が「常駐の削減は v5 への書き換えを待たずに効く」と書いたときの
前提でもあった（常駐については正しいが、**ディスクとロード時ピークの据え置きが恒久だった**）。

| 指標（A/B を交互に 3 巡・各最小値） | v4 | v5 |
|---|---:|---:|
| **ロード（壁時計）** | 337 ms | **212 ms**（**-125**・-37%） |
| `cache_load` | 327 ms | **186 ms** |
| うち `read`（ファイル読み） | 49 ms | **35 ms** |
| ロード段の確保回数 | 1,562,117 | **1,250,986**（**-311,131**） |
| ロード段の peak | 273.08 MiB | **193.65 MiB**（-79.43） |
| `index.bin` | 107.00 MiB | **71.07 MiB**（-35.93） |
| 索引の常駐 | 35.41 MiB | 35.44 MiB |
| 構築（壁時計） | 100 ms | 93 ms（ぶれの範囲） |

#### 測定条件の差を明記する（同一索引の A/B ではない）

**2 標本のエントリ数が違う: 312,377（v4）対 312,690（v5）。** v5 標本は製品が再走査して
書いたものであり、その 1 日で 313 件増えていた。常駐の差（+0.03 MiB / +325 blocks）は
**この 313 件で説明がつき、v5 化による増加ではない**。ロード時間の差は 0.1% の件数差では
説明できない規模であり、**しかも件数が多い側が速い**ので、この条件差は効果を過小評価する
方向にしか働かない。

#### 支配項は read ではなく deserialize である

`cache_read_ms` を `cache_load_ms` から分離して初めて分かった: 読みは 49 → 35 ms
（-14）にすぎず、残り（278 → 151 ms）が deserialize である。**支配項は読むバイト数ではなく
確保の回数**で、これは今後のオンディスク形式の判断に直接効く——「読まない」より
「**materialize させない**」ほうが効く。

#### 製品経路で確かめられたのはどこまでか

製品（release ビルド）を起動して 50 秒後に `index.bin` が 107.00 → 71.08 MiB へ書き換わった
ことは実測した。**ただしそのとき索引の中身も変わっていた**（+313 件）ので、通ったのは
従来の `Changed` の枝であり、**昇格の枝そのものが実規模で働いた証拠にはならない**。
昇格の枝の一次証拠はユニットテスト側にある——`|| cached_version != INDEX_CACHE_VERSION` を
外すと `background_rescan_upgrades_stale_format_when_entries_are_unchanged` だけが FAILED になり、
対の `background_rescan_does_not_rewrite_when_format_is_current` は通ったままであることを実測した。

#### これ以前のロード時間の記録は、すべて v4 に対するものである

**この反復は新しい削減ではなく、反復 2 の削減がようやく届いたものである。** 337 → 212 ms を
反復 6 の 395 → 312 ms と足し合わせてはならない——**反復 6 も v4 のファイルに対して測られて
おり**、その絶対値は「二度と存在しない構成」を記述している（digest 化の差そのものは実在し、
削減幅は有効）。**この反復の時点で基準になった v5 のロードは 212 ms である**（その後
反復 8 が v6 で 140 ms へ移した——**最新の基準は常に一番下の「採用」節を見ること**）。

#### 落ち着いた運用点での v5 の内訳（312,690 エントリ・71.07 MiB・残余 0）

| 項目 | MiB | B/entry |
|---|---:|---:|
| `entries`（うち `target_path` **36.01** / `name` 10.55 / `is_folder` 0.30） | 46.86 | 157.1 |
| `lower_file_names` | 11.07 | 37.1 |
| `lower_names` | 10.56 | 35.4 |
| `file_name_char_masks` / `char_masks` | 1.31 / 1.28 | 4.4 / 4.3 |

**残る候補は 2 つに分かれ、難度が大きく違う。**

- **`lower_names` + `lower_file_names` = 21.63 MiB**。メモリ側は既に 86.6% / 81.9% を共有で
  潰しており（反復 4・5）、ディスクだけが全件持っている。**digest にもフルパス再構成にも
  触れない**ので、形式変更としては最も自己完結する
- **`target_path` 36.01 MiB**。木表現にはフォルダ木の再構成が要り、**digest の両辺を同じ形で
  取れるか**が成立条件になる（背景再スキャン側は `scan_all` の結果＝フルパスを持つ）

#### 標本の保全

`index.bin` は書き換わると旧版が失われ、**同じ状況を二度と作れない**（版が上がれば製品が
二度と v4 を書かないため）。昇格の前に v4 を `C:/tmp/snotra-index-v4-specimen-2026-08-07.postcard`
へ、昇格後の v5 を `C:/tmp/snotra-index-v5-2026-08-07.postcard` へ退避した。上の A/B は
この 2 標本を `%APPDATA%\Snotra\index.bin` へ交互に置いて測っている。**`.bin` 以外の名前で
`%APPDATA%\Snotra` の外に置くこと**——ローダーが触らないための条件である。

### 採用: 背景再スキャンの比較を digest へ（ロード 395 → 312 ms・反復 6）

**索引の常駐ではなく起動時間の反復。** 下の「`BackgroundRescanTask` の全エントリ複製」で
記録した 62.5 MiB の一時複製が、そのまま起動経路の時間でもあった。

| 指標 | 変更前 | 変更後 |
|---|---:|---:|
| ロード（壁時計・各 3 回の最小値） | 395 ms | **312 ms**（-83） |
| ロード段の確保ブロック | 1,874,279 | **1,249,524**（**-624,755**） |
| ロード段の一時メモリ | +185.01 MiB | **+122.52 MiB**（-62.49） |
| 索引の常駐 | 35.41 MiB | 35.41（不変） |

`cached_entries: Vec<AppEntry>` の消費点は `entries_equal` **1 か所だけ**で、必要なのは真偽値
1 つだった。列は保存 3 経路と比較経路のすべてが直前に `sort_entries_canonical` を通るので
canonical であり、digest は忠実な代替になる。

#### 計測の隙間がこの複製を隠していた

`cache_load_ms` は複製の**前**で止まり、複製は `total_ms` にしか効かない。**どのフェーズにも
現れない処理が、起動段の live ブロックの 1/3 を占めていた。** `LoadOrScanStats` に `digest_ms`
を足し、`tests/memory_footprint.rs` がフェーズ内訳と**残余**（total から全フェーズを引いた額）を
出すようにした。現在の残余は 0〜1 ms である——**名前の付いていない処理がそこに居ないことを、
読む側が毎回確かめられる形にした。**

```
フェーズ: total 318ms = hash 0ms + cache_load 275ms + digest 43ms + ...（残余 0ms）
```

#### digest は並列化が効く（解放と違って）

| 形 | digest（各 3 回の最小値） |
|---|---:|
| 直列 | 43 ms |
| **塊ごとに並列 + 塊順に畳む** | **12 ms** |

反復 4 では比較も解放も rayon へ出して最小値が動かなかった（`search/build.rs` の `assemble`）。
**あちらの支配項は解放でヒープが直列化するのに対し、こちらは純粋な計算である。**
「並列化は効かない」を前回の結果から一般化しなかったのが分かれ目だった。

#### 受容する残余

衝突（2⁻⁶⁴）は「変わったのに Unchanged と報告する」形で現れ、索引は次にファイルシステムが
変わるまで古いままになる。**一時的ではなく持続する**（同じ組み合わせなら毎回同じ衝突）。
`compute_config_hash` は同じ u64 でキャッシュの有効性そのものを判定しており（衝突すれば
**別の config の索引をそのまま使う**）、こちらの帰結はそれより軽い。

**digest は順序に依存する。** 列挙順を吸収しているのは `sort_entries_canonical` のほうであり、
ソートを外した経路ができると中身が同じでも「変わった」と判定して `index.bin` を毎回書き直す
（静かな性能劣化）。検知器は `sorted_comparison_ignores_enumeration_order` で、この反復で
「あれば望ましい」から**必須**へ格上げされた。

### `BackgroundRescanTask` の全エントリ複製

ロード区間 228.57 MiB と常駐 166.08 MiB の差 **62.49 MiB**（210 B/entry）は
`indexer.rs` の `BackgroundRescanTask.cached_entries`。キャッシュヒットする毎回の起動で
背景再スキャンの実行中ずっと常駐していた。

**解消済み**（反復 6・上の「背景再スキャンの比較を digest へ」）。複製を digest に替え、
一時 62.49 MiB と確保 624,755 ブロックがまるごと消えた。**この節が記録した額はそのまま
起動時間でもあった**——メモリの調査で見つけた項目が、軸を変えたときの最初の候補になった。

### `target_path` のフォルダ木接頭辞共有 — 構造前提と 3 つのコスト（反復 3・実装前）

`entries[].target_path`（35.56 MiB・常駐の 36.5%）を「親 index + 末尾成分」で持つ案。

**この節の計器は撤去済みである**（下の「内訳計器の修理」節）。実装前の見積もりを取るための
足場——`tests/memory_footprint.rs` の構造前提の走査と `tests/path_query_cost.rs` の `TreeIndex`
一式——であり、製品に `search/path_store.rs` が入って重複した。以下の数値は当時の実測の
記録であって、いま再現できるものではない。

**構造の前提は実データで成立する**（312,377 エントリ）。機構からは導けないので測った
——scan の順序・`show_hidden_system` フィルタ・ドライブ直下・UNC・区切りの揺れが
それぞれ例外を作りうる:

| 前提 | 実測 |
|---|---:|
| 親が索引に居る | 312,281 / 312,377（99.97%・孤児 96） |
| 親 + 区切り + 末尾 == `target_path`（バイト一致） | 不一致 **0** |
| 末尾成分 == `name`（folder） | 255,948 / 255,948（**100.0%**） |
| `name` が末尾成分の接頭辞（file） | 56,333 / 56,333（**100.0%**） |
| `parent_index < self_index` | 違反 **0**（循環を表現不能にできる） |
| 深さ | 平均 6.05 段 / 最大 17 段 |

`target_path` のバイトは folder が **87.4%**（件数比 81.9% とずれる——削減量の係数に
なるのは件数ではなくバイトのほうである）。

**2 案の比較はバイトだけでは決まらない。** 1 エントリあたりの小さな `Box<str>` の数が
アロケータ由来のオーバーヘッドを決め、`layout.size()` しか数えない計器には映らない:

| | 現在 | 末尾成分案 | 拡張子 intern 案 |
|---|---:|---:|---:|
| `target_path` 文字列 | 35.56 MiB | 0.76 | **0.22** |
| `entries` Vec 本体 | 16.68 MiB（56 B/要素） | 14.30（48 B） | **9.53（32 B）** |
| 確保ブロック | 312,377 | 56,429 | **98** |
| 常駐合計の投影 | 97.51 MiB | 60.33 | **55.02（-43.6%）** |

拡張子 id を `AppEntry` に埋める案が per-entry の確保を消す。ただし **distinct 拡張子 2 は
この config の産物**（scan が拡張子を絞っている）であり、intern 表を固定長で持つ根拠には
使えない。

払うコスト（各 3 回の最小値・同一セッション・rayon 並列＝製品と同じ形）:

| 経路 | 頻度 | 現在 | 木表現 |
|---|---|---:|---:|
| **木の構築**（`build()` 実測） | 起動・索引再構築のたび | 2〜3 ms | 19.6〜21.6 ms（**実装後は 55〜96 ms**・下の「採用」節） |
| `recent_history` の形 | 窓を開くたび | 11.3 ms | 15.5 ms（+4.2） |
| パスクエリ全走査 | `\` を含む打鍵ごと | 3.9〜5.0 ms | 6.9〜8.4 ms（+3） |

**構築の内訳は親解決の手段で 7 倍動く**（4 手段が同じ木を作ることをアサートした上での比較）:
HashMap 152.2 / 二分探索 112.4 / 二分探索 + rayon 並列 30.0 / **同上 + 直前の親の使い回し
23.0 ms**。使い回しが効くのはソート順で兄弟がほぼ連続するためで、段数も実測で決めた
（2 段 25.3 / 4 段 23.8 / 8 段 25.6 ms）。**スタックによる 1 パスは採れない**——区切り
`\`(0x5C) は `-`(0x2D) より大きく、`C:\a` / `C:\a-x` / `C:\a\b` の順に並ぶため子孫が
ソート順で連続しない。

**整列済みかの事前走査は要らない**（6.5 ms・全体の 4 分の 1 を占めていた）。`binary_search_by`
が `Ok` を返すのは `entries[pi].target_path == par` のときだけであり、未整列の配列でも
**別の親を返すことは起こりえない**——起こるのは取りこぼしだけで、取りこぼしたエントリは
側テーブル行き（フルパス保持）になり結果は正しいままである。残る唯一の危険（循環）は
`pi < i` の 1 比較で構造的に潰せる。**O(n) の走査が 1 比較のガードに置き換わった。**

**再構築は 2 種類要る。** `with_normalized_key` が作る小文字キー（履歴照合・パスマッチ）に
対して、tie-break（`ScoredEntry::cmp`）と `SearchResult.path` の clone は原文のバイトを
要求する。どちらも 312,377 件でバイト一致を固定した。

**`IndexCache` v6 では構築コストは消えない。** `entries_equal`・`try_background_rescan`・
アイコンキャッシュの剪定キーがロード結果のフルパスを要求するため、`index.bin` から
`target_path` を落としても結局どこかで再構築が要る。本当に消すには再スキャンの比較と
アイコンキーまで木へ移す必要があり、それは別の反復である。

#### 計器が対象でないものを測っていた 4 件（形が 4 件・寿命が 1 件）

判定は測り方で 2 度ひっくり返った。前回の反復 2（3 回ひっくり返った）と同じ機序である。
下の 4 件はいずれも**測る対象の形**を製品に合わせて解けたが、5 件目（「構築コストの
見積もりが 4〜5 倍外れた」）だけは形ではなく**寿命**——確保だけを測って解放を測って
いなかった——を合わせないと解けなかった:

1. **セグメントの追記を 1 文字ずつの `push` で書いていた**（走査 2.7〜3.4x → 1.48〜1.79x）。
   `normalize_entry_key_into` 自身が「`String: Extend<char>` が毎回 UTF-8 符号化の分岐を
   通り実測で 2.5-3 倍遅くなる」と明記している落とし穴を、計器が踏んでいた
2. **1 エントリごとに `String` を確保して連結していた**（構築 249.6 → 167 ms）
3. **`std::path::Path` を通していた**（構築 167 → 117 ms）。Windows では `OsStr`（WTF-8）
   → `str` の検証走査が全件に乗る
4. **製品が呼ぶ `build()` ではなく `build_with()` を測っていた**。両者の差である
   `choose_lookup`（6.5 ms）が計測区間の外に落ち、構築コストを 4 分の 3 に見せていた。
   **測り直したことが `pi < i` ガードへの置き換えを生んだ**——外にある額を計器へ入れると、
   その額を消す道が見える

### 内訳計器の修理 — 構築後の `SearchEngine` を走査する（反復 3 の副作用）

反復 3 のあと、内訳の走査が**構築前の `Vec<AppEntry>`** を見たまま残っていた。`target_path` の
`String` は構築時に `PathStore` へ組み替えられて解放されるので、その走査は**もう存在しない
物体を測っていた**——合計（アロケータ実測）は正しいまま、内訳だけが嘘になる形である。
出力は `構築前の内訳 122.52 MiB → 構築後の常駐 54.92 MiB（差 -67.60 MiB）` と正直に
差を出しており、**壊れていたのは計器ではなく読み方でもあった**（差を誰も読まなかった）。

走査を `src/search/footprint.rs` へ移し、構築**後**の `SearchEngine` 自身から数える形にした。
`SearchEngine::footprint_rows` は `self` を**網羅的に分解**する（`..` を書かない）ので、
並列 Vec を足したときの漏れは文書のチェックリストではなくコンパイラが捕まえる。

**帰属は完全である**（2026-08-07・312,377 エントリ・migemo 無効）:

| | 実測 | 帰属 | 差 |
|---|---:|---:|---:|
| バイト | 54.92 MiB | 54.81 MiB | +0.11 MiB |
| 確保ブロック | 937,402 | 937,235 | +167 |

差 0.11 MiB は `drop` 後の残留（0.11 MiB）と一致する——**索引ではないもの**（rayon の
グローバルスレッドプール等・「残余」節）であり、未帰属はこれを下回れない。

| 項目 | 確保 | blocks | B/entry |
|---|---:|---:|---:|
| `PathStore: entries[].name` | 10.25 MiB | 312,377 | 34.4 |
| `PathStore: entries` Vec 本体（`CompactEntry` 32 B） | 9.53 | 1 | 32.0 |
| `PathStore: table`（親不在のフルパス + 拡張子・99 本） | 0.01 | 98 | 0.0 |
| `lower_names`（**= `entries[].name`**） | **9.80** | **270,355** | 32.9 |
| `lower_names`（≠） | 0.45 | 42,022 | 1.5 |
| `lower_names` Vec 本体 | 4.77 | 1 | 16.0 |
| `lower_file_names`（**folder・= `lower_names`**） | **9.71** | **255,961** | 32.6 |
| `lower_file_names`（file・≠） | 0.76 | 56,416 | 2.5 |
| `lower_file_names` Vec 本体 | 4.77 | 1 | 16.0 |
| `char_masks` / `file_name_char_masks` | 各 2.38 | 各 1 | 各 8.0 |
| kana 2 本 / `incremental_cache` | 0.00 | 0 | 0.0 |

**条件を行の label に書いて割るのが要点である。** 1 行にまとめた `lower_file_names`
（10.47 MiB）を候補の見込みとして読むと**約 2 割甘い**——file の 0.76 MiB は共有できず、
Vec 本体 4.77 MiB はどの案でも残る。割った結果、上限はそれぞれ行の値そのものになった。

同時に、**割ったことで順位が入れ替わる新しい候補が出た**: `lower_names` の 86.6%
（270,355 件・9.80 MiB）が `entries[].name` と同一である。索引名が既に小文字のとき
ASCII 小文字化が恒等写像になるためで、候補としては `lower_file_names` の共有（9.71 MiB /
255,961 blocks）より大きい。**どちらもホットパスに分岐を 1 つ足す**ので、採否は測ってから決める。

### 採用: `lower_file_names` を `lower_names` と共有（-9.71 MiB・反復 4）

索引の 81.9% を占める folder は `lower_file_name` が `lower_name` と完全に同じ文字列で、
2 本の `Box<str>` に同じバイトを持っていた。構築時に**測って**一致するものを潰し、
`CompactEntry` の旗で読み替える。

| 指標 | 変更前 | 変更後 |
|---|---:|---:|
| 常駐 | 54.92 MiB | **45.21 MiB**（-9.71・**-17.7%**） |
| 確保ブロック | 937,402 | **681,441**（3.00 → **2.18** blocks/entry） |
| 帰属 | 54.81 + 未帰属 0.11 | 45.10 + 未帰属 0.11 |

**修理した内訳計器の予測（9.71 MiB / 255,961 blocks）とバイト単位で一致した。**
分割した行の値がそのまま実現値になったので、条件を label に書いて割る形は
候補の見積もりとして機能する。

**表現は測ってから選んだ。** 3 状態目（「共有している」）をどこに置くかで実質が変わる:

| 案 | 寸法 | 追加 | 実質削減 |
|---|---:|---:|---:|
| `lower_file_names` を 3 変種 `enum` へ | 16 → 24 B | +2.38 MiB | 7.33 MiB |
| 別 `Vec<bool>` | +1 B/entry | +0.30 MiB | 9.41 MiB |
| **`CompactEntry` の空きパディングへ `bool`** | **32 B（不変）** | **0** | **9.71 MiB** |

`Box<str>` の niche は 1 つしか無いため 3 変種 `enum` は 8 B 膨らみ、**粗利の 4 分の 1 を
食う**。測らずに `enum` を選んでいたら気づけなかった。

対のレイテンシ実測（同日・同セッション・各 3 回）:

| 経路 | main（p50 のレンジ） | 変更後 |
|---|---|---|
| `users`（区切り無し） | 888–1,020 µs | 878–897 |
| `c:\` | 19,223–19,861 | 18,090–20,495 |
| `c:\users` | 14,217–14,474 | 13,951–14,500 |
| `c:\users\` | 13,769–14,332 | 13,637–14,204 |
| `\program files\` | 11,616–11,849 | 11,772–12,152 |
| `\zzz-no-such-path\` | 11,825–12,312 | 11,490–12,294 |
| `recent_history`（最小） | 7.1–9.2 ms | 7.4–10.0 ms |

**全経路でレンジが重なる＝検索は変わっていない。** 読み替えは `entry_view` の 1 点で、
`entry` も `lower_name` も既に手元にあるため追加のメモリ読みが無い。

**払うのは構築の 57 → 75 ms（各 3 回の最小値）である。** 並列化では取り戻せない
——比較を rayon へ出しても、比較と解放の両方を出しても最小値は 75 ms のままだった
（3 変種を同一セッションで実測）。ゆえに最も単純な直列ループを残した。**増分の機序
（比較か 255,961 個の `Box<str>` の解放か）は切り分けていない。**

**`index.bin`（v5）は不変である。** engine の `lower_file_names` を読み戻して永続化する
経路は無く、キャッシュは `AppEntry` から書かれる（全参照を grep して確認）。組み替えは
反復 3 と同じく `assemble` の 1 点に閉じている。

**migemo 有効側も測った**（Phase B の合成ラダー）。実運用の計測環境は `migemo_enabled = false`
であり、測っていない側は「壊れても気づかない側」になる:

| N | migemo | 変更前 live / blocks | 変更後 |
|---|---|---|---|
| 38,847 | off | 2.22 MiB / 77,699 | 1.85 / 38,852 |
| 38,847 | on | 3.63 MiB / 116,548 | 3.26 / 77,701 |
| 100,000 | off | 5.72 MiB / 200,005 | 4.77 / 100,005 |
| 100,000 | on | 9.35 MiB / 300,007 | 8.39 / 200,007 |

kana 系 2 本は `assemble` の**外**で確定してから渡されるため、潰す位置の不変条件は
migemo の設定に依らない（`shared_file_name_flag_is_measured_not_inferred_from_is_folder`
が両設定を通す）。

**この A/B で計測の形の罠を 1 つ踏んだ。** Phase B だけを名指しで走らせると rayon プールの
立ち上げ（0.11 MiB）が最小規模の区間に計上され、**削減した側が増えて見える**
（`+0.48` が単独実行で `+0.59` になり、変更前の `+0.57` を上回った）。原因を推測で片付けず
両 Phase を同じ順で走らせて確かめた。注意書きはハーネスの `//!` にある。

### 採用: `lower_names` を `entries[].name` と共有（-9.80 MiB・反復 5）

`to_lower_folded` が恒等写像になる名前——既に小文字の ASCII と、変換対象を持たない CJK——は
`lower_name` が `name` と同じ文字列になる。実データで **86.6%**（270,355/312,377）。

| 指標 | 変更前 | 変更後 |
|---|---:|---:|
| 常駐 | 45.21 MiB | **35.41 MiB**（-9.80・**-21.7%**） |
| 確保ブロック | 681,441 | **411,086**（2.18 → **1.32** blocks/entry） |
| 帰属 | 45.10 + 未帰属 0.11 | 35.30 + 未帰属 0.11 |

**内訳計器の予測（9.80 MiB / 270,355 blocks）と 3 反復連続でバイト単位に一致した。**

**共有は鎖になった**: `lower_file_names[i]` → `lower_names[i]` → `entries[i].name`。
`assemble` は判定を全部済ませてから落とす——先に上流を潰すと下流の比較相手が消えて共有を
取りこぼすが、**結果は正しいまま削減だけが減る**ので挙動テストでは捕まらない。検知器
（`shared_lower_name_chain_collapses_both_links`）は索引が実際に文字列を落としたかまで見る。

**ここでは旗が要らない。** `lower_names` の `None` には先客がいないので `Option<Box<str>>`
（`Box<str>` と同じ 16 B）の 2 状態で足りる。`lower_file_names` の `None` は「file name 成分が
無い」を先に意味しており、そちらだけ `CompactEntry` の旗を要した——**同じ表層形が複数の概念を
担っていないかは、共有のたびに問い直す**。

対のレイテンシ実測（同日・同セッション・各 3 回・p50 のレンジ）:

| 経路 | main | 変更後 |
|---|---|---|
| `users`（区切り無し） | 854–893 µs | 789–859 |
| `c:\` | 18,221–18,745 | 17,916–19,320 |
| `c:\users` | 13,758–14,261 | 13,418–13,895 |
| `c:\users\` | 13,765–14,028 | 13,366–13,758 |
| `\program files\` | 11,663–11,985 | 11,469–11,852 |
| `\zzz-no-such-path\` | 11,255–11,849 | 11,308–11,455 |
| `recent_history`（最小） | 6.7–7.6 ms | 7.3–8.0 ms |

6 経路中 5 つでレンジが重なる。`c:\users\` だけ main の下限を **7 µs（0.05%）** 下回るが、
**改善の疑いも退行の疑いと同じだけ標本を要する**——他が全部重なる以上これは雑音であり、
高速化として記録しない。

**払うのは構築の 77 → 93 ms（各 3 回の最小値・ベースライン自身の振れ幅は 77–121）。**
反復 4 の +18 ms とほぼ同額であり、潰すブロック数もほぼ同じ（255,961 / 270,355）である。

#### 共有 1 反復あたり構築 +17 ms という傾向（次の候補を測るときの前提）

| 反復 | 構築（最小値） | 削減 | 潰したブロック |
|---|---:|---:|---:|
| 3（`target_path` の木） | 2〜3 → 66〜78 ms | -42.59 MiB | -312,377 |
| 4（`lower_file_names`） | 57 → 75（+18） | -9.71 | -255,961 |
| 5（`lower_names`） | 77 → 93（+16） | -9.80 | -270,355 |

**共有はブロックを落とす作業であり、落とす額に比例して構築が伸びる。** 起動全体は
ロード ~380 ms + 構築 93 ms なので現状は 20% ほどだが、同型の反復をあと 2 回積むと構築が
125 ms を超える。**次の候補は削減量だけでなくこの傾きも見て選ぶこと。** 反復 4 では並列化を
2 変種試して最小値が動かなかった（`assemble` のコメント）ので、傾きを寝かせたいなら
別の手（潰す作業自体を減らす・遅延させる）が要る。

#### issue #336 の判定が覆った理由（運用点と実装形の 2 つが変わった）

同じ案は 2026-06-17 に **NOT_PLANNED** で見送られている。覆したのは新しい着想ではなく、
**前提が 2 つとも変わった**ことである:

| | #336 の時点 | 現在 |
|---|---|---|
| 実運用点 | Start Menu + Desktop + PATH（**366 件**） | `[[paths.scan]] path = 'C:\'`（**312,377 件**） |
| `Same` 一致率 | 43.2% | **86.6%** |
| 1 件あたり削減 | ~7 B（PATH の実行ファイル stem は名前が短い） | **38 B** |
| 総削減 | ~0.35 MB / 50k 換算 | **9.80 MiB** |
| 実装の必須条件 | `enum` を **v5 IndexCache へシリアライズ**（さもないと起動時 `to_lower_folded` 再計算が復活）＝**9 経路同時更新**で effort=high | **不要**——`index.bin` は不変のまま `assemble` の 1 点で潰す（反復 3・4 で確立した形。ディスクからは従来どおり完全な `lower_names` を読むので再計算は起きない） |

#336 本文は「CJK 名主体の環境なら ROI は大きく化ける」と reopen 条件を明記していた。
**条件は満たされたが、それを確かめられたのは内訳計器を製品へ向け直したあと**である。

**#336 が残した計器は撤去した。** `measure_lower_name_footprint_report` は
`Config::default_scan_paths()` を「本番相当」と称しており、運用点が移った今は製品でないものを
測る。率の再評価はこの節が正本で、実測は `tests/memory_footprint.rs` の内訳が担う。

## 試みたが機能しない手法

- **Custom URI Scheme（`snotra-icon://` 等）による画像配信**: WebView2 では `register_uri_scheme_protocol`（WRY/Tauri）で登録したカスタムスキームへのリクエストが、WebView2 環境生成時の `SetCustomSchemeRegistrations` 事前宣言なしにはハンドラーに届かない。WRY 0.54.x では自動的に処理されず、`eprintln` 診断でハンドラーが一切呼ばれないことを確認済み。バイナリ配信の代替は `tauri::ipc::Response`（上記セクション2）を用いること。

- **`SearchEngine` の並列 Vec を `CachedEntry` 構造体に統合**（issue #110、branch `refactor/cached-entry`）:
  保守性改善を目的に、8本の並列 Vec をフィールドごとにまとめた `CachedEntry` 構造体への移行を試みた。
  実測で **Fuzzy full scan が 35〜120% 遅化** し却下。

  | エントリ数 | 並列Vec（現行） | CachedEntry | 増加率 |
  |----------:|---------------:|------------:|-------:|
  |     1,000 |         ~7 ms  |      ~14 ms |  +97%  |
  |    10,000 |        ~14 ms  |      ~21 ms |  +49%  |
  |    50,000 |        ~14 ms  |      ~30 ms | +120%  |
  |   100,000 |        ~22 ms  |      ~29 ms |  +35%  |

  原因: `char_masks`（`Vec<u64>`）は 8エントリ/キャッシュライン で bitmask プリフィルタを高速に走査できる。
  `CachedEntry`（~160 bytes/entry）に埋め込むと同じ走査で **~25倍のキャッシュライン**を消費する。
  並列 Vec のレイアウトはキャッシュ局所性のために意図的に維持している。
  詳細は `snotra-core/src/search.rs` の `SearchEngine` 構造体コメントを参照。

  **採用した別案（branch `refactor/entry-view-accessor`）**: AoS 統合の代わりに `EntryView<'a>` アクセサパターンを導入。
  `entry_view(i)` が並列 Vec の参照（`entry` / `lower_name` / `lower_file_name`。当時は `normalized_key` も含んでいたが、2026-08-07 に索引ごと廃して導出へ移した）を束ねて返すことで、スコアリングループの可読性を向上させた。`char_masks` / `file_name_char_masks` はプリフィルタのキャッシュ効率を保つため EntryView に含めず SearchEngine から直接アクセスする。
  `#[inline]` により性能への影響はゼロ（メモリレイアウト不変）。`new()` 末尾の `debug_assert!` で全 Vec 長の同期を検証する。

## 計測と受け入れ基準

- **計器が測る枝と、変更が触る枝が同じか先に確かめる**（反復 11）。`tests/memory_footprint.rs` の `measure_real_index_footprint` は開発機に `index.bin` が在る限り **cache-HIT 枝しか測らない**ので、cache-miss を触る変更は素直に走らせると差分ゼロになり、**出力の見た目は成功時とまったく同じ**である。cache-miss を測るには `SNOTRA_CONFIG_DIR` を temp へ向け `config.toml` だけ置く（`index.bin` は置かない）——scan 集合は実物のまま初回起動を再現でき、実運用の `index.bin` も無傷で済む。**A 側の標本は実装前にしか取れない**
- 変更ごとに「入力 → 検索結果反映」までの遅延を観測し、体感を先に確認する
- 体感改善後、必要なら p50/p95 を追加計測して次のボトルネックを特定する
- 原則として「待ち時間」「重複」「計算量」「描画」の順を崩さない
- ランタイムの計測は `SNOTRA_TRACE=1` の構造化トレース（`src-tauri/src/trace.rs`）で行う
- egui/softbuffer の計器は 5 つの env（いずれも未設定なら計器のコストは 0）。**このリストが計器の正本である**——`docs/build-commands.md` には置かない
  - **受理値: 空でなければ何でもよい**（`=1` でも `=0` でも点く）。**空文字は「未設定」として扱う**——判定は `snotra-egui-runtime/src/env.rs` の 1 箇所に集約してある（#872: PowerShell の env 復元が空文字を作り、測定ハーネスの全反復が黙って計器つきで走っていた）。**`SNOTRA_TRACE` だけは別の意味論である**（`1｜true｜yes｜on` のみ・`src-tauri/src/trace.rs` の `env_flag`）
  - `SNOTRA_EGUI_PAINT_TRACE`: paint フェーズ（`tess_ms` / `raster_ms` / `total_ms` / `meshes` / `px`）。#532 SU6.5 の flip ゲート G3(b) の主判定に使った
  - `SNOTRA_EGUI_REPAINT_TRACE`: フレームの到着（`window` / `focused` / `since_prev_ms` / egui の repaint 原因 `file:line`）。**「なぜ再描画が止まらないか」を推測せず原因に名乗らせる**ための計器（#628）
  - `SNOTRA_EGUI_WAKE_TRACE`: `RequestRedraw` の送信（repaint worker・`SEND`）と受信（イベントループの `RedrawRequested` arm・`RECV`・引き当て結果付き）。hidden 中にどの層が配送を抑止しているかの切り分けに使う（#697）
  - `SNOTRA_EGUI_INPUT_TRACE`: 打鍵の到達（注入 → tao の配送 → egui への push → フレーム）。**この計器は系を乱す**——runner では stderr 1 行が 17〜56ms かかり、最初のフレームの到来を押し下げる。**率を測る回と機序を測る回は別の回にすること**（#872/#936）
  - `SNOTRA_EGUI_IME_TRACE`: IMM32 の preedit 取得と候補窓位置（`windows_ime.rs`）
- **操作中の上限（2026-07-26・release・#737 実測）**: `RequestRedraw` の配送は窓が載るモニターのリフレッシュレートで頭打ち（取得失敗時 60Hz・contract-design spec 契約②）。144Hz 機の A/B でポインタ移動中の results 間隔 p50 が 3.5ms → **7.1ms（≈141fps）**、>200fps 相当の間隔（<5ms）が 4,829 → 170（−96%・残余は event queue のジッタとみられる——`SNOTRA_EGUI_WAKE_TRACE` の送信間隔での裏取りは未実施）、paint 占有 17.6% → 8.9%。**ポインタ移動中の p50 がリフレッシュレートの逆数を大きく下回ったら回帰を疑う**
- **可視アイドルの基準値（2026-07-26・release・#628 実測）**: main 2.0 fps / results 2.0 fps（＝ egui のキャレット点滅の下限）・paint 平均 0.96ms・**CPU 0.59%（1 コア比）**。修正前は 11.9 / 19.8 fps・5.1% だった。差は `RawInput::predicted_dt` の既定値（1/60 秒）が短い repaint 予約を「即時再描画」へ飽和させ、点滅の遷移ごとに約 26ms スピンしていたことによる（`snotra-egui-runtime/src/input.rs` の `take` を参照）。**アイドルで 2 fps を超えていたら回帰を疑う**
- コアロジックのマイクロベンチは `ignored` テストとして保持する。実行コマンドは [docs/build-commands.md](docs/build-commands.md) の「検索パフォーマンス計測」を SSOT として参照
- フォルダ列挙のホットパス確認には bench フィルタを絞る: `cargo test -p snotra-core bench_folder_ -- --ignored --nocapture`（SSOT の bench コマンドに対するフィルタ違いとして個別記載）
- `bench_folder_narrow_filter` は「大量エントリ + 狭いフィルタ」で、非一致エントリに不要な文字列化や属性判定をしていないかを確認する
- `bench_folder_hidden_filter_all` は `show_hidden_system = false` 相当で、`metadata()` を伴う属性判定コスト込みの回帰を確認する
- **warm frame は日をまたいで比較しない**——同一ホスト・同一バイナリでも日によって 3 倍変わる。構成 A / B の比較は必ず**同日・同条件で両方を測る**（#532 Phase 1 の検証バイナリで実測: 2026-07-17 に 26-30ms、7/14 は 8-10ms。変動の原因は未解明ゆえ、日をまたいだ数値の差を改善・退行と読んではならない）

## 計測ベースライン（2026-03-06）

環境: Windows 11 Home, release ビルド（`cargo test --release -p snotra-core bench_ -- --ignored --nocapture`）

### ファジー検索（`bench_fuzzy_search_scaling`）

| エントリ数 | 平均レイテンシ |
|----------:|-------------:|
|     1,000 |       54 µs  |
|    10,000 |      378 µs  |
|    50,000 |    1,314 µs  |
|   100,000 |    2,088 µs  |
|   300,000 |    6,923 µs  |

### Engine 初期化（`bench_new_scaling`）

| エントリ数 | 平均時間 |
|----------:|---------:|
|     1,000 |    < 1 ms |
|    10,000 |     9 ms  |
|    50,000 |    42 ms  |
|   100,000 |   124 ms  |
|   300,000 |   202 ms  |

### フォルダ列挙（`bench_folder_*`、max_results=50）

| ベンチ | エントリ数 | 平均時間 |
|--------|----------:|---------:|
| topk_sort | 1,000 | 2,677 µs |
| topk_sort | 5,000 | 7,491 µs |
| topk_sort | 10,000 | 14,199 µs |
| folder_narrow | 1,000 | 3,043 µs |
| folder_narrow | 5,000 | 6,279 µs |
| folder_narrow | 10,000 | 10,011 µs |
| folder_hidden_all | 1,000 | 3,251 µs |
| folder_hidden_all | 5,000 | 8,899 µs |
| folder_hidden_all | 10,000 | 14,829 µs |

### ホットキー表示レイテンシ（`show_main_and_emit`、SNOTRA_TRACE=1 計測、2026-03-07）

Win32 IPC cold-call 最適化（`is_visible()` → `AtomicBool`）適用後の値。

| 条件 | `show_main:total` | 備考 |
|------|------------------:|------|
| cold（初回） | 77ms | うち ~41ms はトレース I/O オーバーヘッド |
| warm（2回目以降） | ~38ms | |
| cold（トレースなし推定） | ~36ms | トレース I/O 分を差し引いた推定値 |

最適化前の cold は 191ms だった（`is_visible()` pre-check 61ms + ギャップ 71ms + `is_visible_after_show` 39ms）。
