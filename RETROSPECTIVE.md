# Retrospective — #1077: 画面に出ていない通常結果の起動を塞ぎ、`indexing` をフレーム内で 1 回だけ読む

## よかったこと

### 修正前の実機測定をフェーズ 0 として置いた

issue が「未検証」としていた 2 点（食い違う窓は開くか・症状は何か）に、実測で答えられた。**修正が入った後では原理的に測れない**ため、実装より前に置いたことが効いた。`SNOTRA_CONFIG_DIR` の使い捨てプロファイルを使ったので、承認された手順（実 `config.toml` を書き換える）より安全な側へ寄せられた——`docs/build-commands.md` に既にあった経路で、探して見つけたものである。

### 敵対的調査と独立導出が、別々の誤りを壊した

敵対的レビューは「読み点は 6 つでその全部」という**数え上げ**を壊し（実際は `poll_async` 経由が落ちていた）、独立導出レビューはガードの置き場所と `SPEC.md` の置き場所を動かした。同じ文書に 2 枠を当てて、重なりではなく別の面が出た。数え上げは「数を直す」のではなく**やめて grep を指す**方へ倒した。

### `/symmetric-check` Step 2c が、計画の緩和策の不足を名指しした

計画は隣接 `bool` の取り違えに newtype を当てる方針だったが、Step 2c の「**起点が同型なら型は守っていない**」が当たり、タプル構築子が公開されていれば任意の `bool` を包めることが分かった。構築子を読み点（`window_coordinator::read_indexing`）へ閉じる形へ転換し、コンパイルで塞いだ。計画レビューでは出ず、実装後の同じスキルで出た。

---

## 伸びしろ

### 母集団を持つ検査は、広すぎる方向で沈黙する

ソーステキスト検査の母集団カナリアを先例から写したが、**「空でないこと」しか見ていなかった**。終端を取り逃した母集団は EOF まで伸び、検査自身が `mod tests` に持つリテラルを飲み込んで `contains` を必ず真にする。検知器 2 本のうち落ちたのは 1 本で、**もう 1 本は同じ理由で「空虚に緑」だった**——落ちた側を追わなければ気づけなかった。教訓は `docs/development-principles.md`「検証の層と、層と層の隙間」へ、既存側の同型は #1108 へ。

### 手元と CI の改行コードの非対称を、書くときに参照しなかった

`find("\n    }\n")` は CRLF に一致しない。手元は `core.autocrlf=input`、CI は git-for-windows の system 既定 `true` である。**この非対称はリポジトリの `.gitattributes` の冒頭コメントが `.githooks/**` について既に記録していた**のに、新しい検査を書くときに参照しなかった。手元の全件実行 3 回はすべて緑で、CI が唯一の接地だった。

### 散文の是正が、新しい散文の誤りを 2 度生んだ

`SPEC.md` へ書いた 1 行を直すたびに新しい誤りが入った——「不可逆な起動だけ」（可逆なツール選択入場を止めるのと矛盾）、「新しい索引で作り直され」（`/r` 履歴行は索引由来でない）、「`/s` は構築中も打てる」（`ensure_not_indexing` が拒否する）、その是正が設定オープンまで巻き込んだ 1 件。実装差分は `FrameIndexing` の移設以降 1 行も動いておらず、**レビュー 3 巡の指摘はすべて散文側**だった。`AGENTS.md`「条件別チェック（トリガー → 参照先）」の fix-forward 行に従って再実行したことが、毎回の検出経路になった。

### 新設した ADR が、同じ PR の是正の母集団から漏れた

ADR を 2 つの是正コミットの**間**に書いたため、`SPEC.md` から消した誤った文言がそのまま ADR に残った。`AGENTS.md`「条件別チェック（トリガー → 参照先）」の写し関連の行が「取りこぼすのは写しを直す当のコミット」と既に書いており、既存トリガーで包含されているため文書追加はしていない。
