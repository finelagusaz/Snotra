# Retrospective — #1230 prompt-audit（Fable 5.1 較正）・#1231 全角括弧の intra-doc link 検査（#1172）

## よかったこと

### 同じ誤記へ、独立した 2 経路が同時に到達した

調査文書が「`linesOfComments` は trim 済みの行を返す」と誤記していた。敵対的調査（sonnet 1 体）が実装を読んでこれを壊し、主エージェントも並行して `out.push([i + 1, raw])` を読んで同じ結論に着いた。**片方だけなら「所見を受け取った」で終わるところが、両方が一次証拠で同じ行を指したので裁定に迷いが無かった。** その誤りは fixture 1 本（インデント済み `///`）で固定し、委譲先の変異注入で「`trimStart()` を外すとその fixture だけが赤になり、`governance:check` は 755 → 446 件へ減るだけで緑のまま」を実測した——検知器がどこに在り、どこに無いかが数字で残った。

### 独立導出が、3 枠すべてが見落とした fixture を拾った

`/plan-review` Step 2b の独立導出は `evidence.test.mjs` の `complete()` fixture を変更一覧へ足した。計画にも敵対的調査にも無く、足さなければ `npm test` が落ちていた。**計画と調査を読ませない枠が、読ませた枠と違う場所を見る**ことが今回も効いた。

### 文言を保つ組み直しを、集合差で検算した

`src-tauri/CLAUDE.md` の 4 段落（1.8 万字・太字 21〜24 個）を bullet 化したとき、旧版と新版から識別子・issue 番号・ADR 名を抽出して `comm` で比べた。落ちた 1 語（`main_visible`）は ADR へ移した当のもので、それ以外は 1 対 1 だった。目視では追えない規模の再組版を、1 行のコマンドで「何も失っていない」と言える形にできた（手順は `.claude/rules/governance-docs.md` へ置いた）。

---

## 伸びしろ

### 写しは、検査 ID ではなく判定関数名の綴りで居た

「検査 ID の列挙（写し）は `checks/` の外に無い」と、主・敵対的調査・独立導出の 3 枠が揃って結論した。だが `G-edit-findings-table.test.mjs` の `ALL` は判定**関数名**（`checkFoldedCodeSpans`）の手書き一覧で、`SCAN_SCOPED` へ足した瞬間 9 本が赤になった。3 枠とも `G-folded-code-spans` の綴りで grep しており、**同じ概念が別の綴りを持つ**ことを問わなかった。機構が捕まえたので害は無いが、写しを数えるときは「その概念を指す綴りは 1 つか」を先に問う（`SCAN_SCOPED` の doc に足し忘れの向きを書いた）。

### モデル較正の生き残りは、本文からは見分けられなかった

Opus 5 向けの較正 5 箇所のうち 4 箇所は既に別 PR で消えていて、残る 1 箇所が「3〜6 行」という数だけで居た。これが較正だと分かったのは `git blame` が #764 に着地したからで、条項自身は自分が何の栓かを名乗っていなかった。**次のモデルで反転する条項は、どのモデルのどの癖への栓かを添えて置く**（`.claude/rules/safety-nets.md`）。
