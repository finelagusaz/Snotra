# Retrospective — Snotra バグ修正サイクル全体の振り返り

対象フェーズ: 調査 (research.md) → 計画 (plan.md) → 実装 → レビュー (review.md) → 追加修正計画 (fix_plan.md) → 追加実装

---

## 1. 全体像と因果連鎖

本サイクルで扱った問題は、単純に「バグが 8 件あった」ではなく、**1 つのアーキテクチャ上の設計圧力が複数のバグを連鎖的に生み出した**という構造を持っている。

### 根源的な設計圧力：マルチウィンドウ分離

Snotra は `main`・`results` という別々の `WebviewWindow` を持ち、JavaScript コンテキストを共有しない。`search.ts` のシグナル（`results()`, `selected()`）は `ResultsWindow` から直接読めず、Tauri イベント（`results-updated`）経由でしか状態を伝達できない。

この制約が生み出した不変条件：
> **「`search.ts` の状態を変更したときは必ず `emitResults()` を呼んで `ResultsWindow` に通知する」**

コードベース全体でこの不変条件の守備範囲が暗黙のままだったことが、Bug 1・Bug 2・H1 という 3 件の独立した失敗の共通温床になっていた。

```
マルチウィンドウ分離設計（設計圧力）
  ├─→ Bug 1: exitFolderExpansion が emitResults を呼ばない
  ├─→ Bug 2: result-clicked 後に emitSelectionUpdate を呼ばない
  └─→ H1:   result-double-clicked 後に emitSelectionUpdate を呼ばない（Fix 2 の適用漏れ）
```

---

## 2. バグが生まれた 3 つの経路

### 経路 A：実装の抜け（研究で発見・計画で修正）

Bug 3（フォルダ展開履歴デッドコード）・Bug 4（parse_vk の Space フォールバック）・Bug 5（ホットキー失敗時の不整合保存）・Bug 6（first-run 条件誤り）はいずれも「実装段階の見落とし」に分類できる。設計意図と実装が乖離しており、research.md の段階で根本原因を特定すれば plan.md → 実装の流れで確実に修正できる種類のバグ。

### 経路 B：計画段階の判断ミスが実装に忠実に伝播（レビューで発見）

**H1（result-double-clicked への emitSelectionUpdate 漏れ）** は、plan.md の Fix 2 に起因する。

plan.md は `result-double-clicked` について次のように記述していた：
> 「`activateSelected` がアイテムを起動するためハイライト更新は不要。変更なし。」

この判断は **ファイル起動ケースしか考慮していない**誤りだった。フォルダ展開の場合は直後に `emitResults()` が走るため症状が見えにくく、ファイル起動のケースで `ResultsWindow` の選択表示が永続的にズレる問題が隠れていた。

実装フェーズはこの計画を正確に忠実に実装した。つまり **バグが計画に書き込まれ、実装によってバグが完成した**。

```
Fix 2 の仕様変更（ホバー不要・クリックのみ）
  ↓
result-double-clicked への影響分析が不完全
  ↓
plan.md に「変更なし」と明記
  ↓
実装フェーズが計画通りに実装（バグを忠実に再現）
  ↓
review で H1 として初発見 → fix_plan.md で修正
```

### 経路 C：計画のサンプルコードが不完全で実装が誤配置（レビューで発見）

**H2（listen/onCleanup の非対称配置）** は、plan.md の Fix 7 サンプルコードに起因する。

plan.md の Fix 7 は `onCleanup` を `if (listRef)` ブロック内に `ro.disconnect()` と `unlistenResults?.()` を束ねた単一クロージャとして示していた。実装フェーズはこのサンプルを踏襲し、`listen()` は `if (listRef)` ブロックの外に置いたまま `onCleanup` だけがブロック内に収まった。

結果として：
- `listRef` が `undefined` の場合 → `listen()` は実行されるが `onCleanup` が登録されない
- `.then()` がアンマウント後に解決した場合 → `unlistenResults` への代入が `onCleanup` 呼び出しより後になる

fix_plan.md では ResizeObserver と listen の **クリーンアップを独立させる**構造に整理し直すことで解決した。計画段階でこの分離を明示していれば、実装時の誤配置は起きなかった。

---

## 3. レビューフェーズが生み出した価値

review.md が発見した問題は 2 種類に分かれる。

### research.md にも plan.md にも存在しなかった真の新規発見

**M3（`rx.recv()` タイムアウトなし）と M4（`show_on_startup` の `window-shown` 漏れ）** はいずれも事前の調査・計画フェーズで一切言及されていない。

M4 の根本は DRY 違反だった。`show_main_and_emit()` という関数を作りながら、`show_on_startup` コードパスではその関数を使わずに `w.show() + w.set_focus()` を手書きしていた。関数化の恩恵（`window-shown` の emit・IME 制御）が適用されず、**関数を作ったにも関わらずその関数が使われていない箇所から機能不具合が生じた**。

M3 は `if let Ok(false) = rx.recv()` という単一パターンマッチが `Err(Disconnected)` を暗黙に成功扱いにする問題と、無制限ブロッキングという問題を同時に内包していた。`recv_timeout` への変更は機能改善と潜在バグ修正を同時に達成している。

### 計画の判断を覆した発見（H1・H2）

上記「経路 B・C」で述べた 2 件。レビューは「計画通りに実装されたか」の確認にとどまらず、**計画そのものの正しさを検証した**点で本質的な価値を持つ。特に H1 は plan.md が「変更なし」と明示していたにも関わらず問題として指摘された。これはコードレビューが「実装のチェック」ではなく「仕様判断の再検証」として機能した例。

---

## 4. 構造的パターンと教訓

### 「計画段階の判断ミスは実装で増幅される」

Bug を修正する計画が誤った前提を含む場合、実装フェーズはその誤りを完璧な精度で再現する。H1 がその典型例。計画段階で「変更対象外」と明示されたコードパスは、実装フェーズで検討の対象外になる。対称性（`result-clicked` ↔ `result-double-clicked`）・網羅性（全コードパスへの適用）の検証は計画段階で行う必要がある。

### 「サンプルコードの配置が実装の設計を決定する」

計画書のサンプルコードが `if (listRef)` ブロック内に `onCleanup` を置いていたことで、実装者もそのブロック内に配置した（H2）。サンプルコードの**構造**は、計画文章の**説明**より強い誘導力を持つ。plan.md のサンプルが「ResizeObserver のクリーンアップ」と「listen のクリーンアップ」を別々に示していれば、実装時の誤配置は防げた可能性が高い。

### 「関数を作ることとその関数を使うことは別の判断」

`show_main_and_emit()` という関数が存在しながら `show_on_startup` コードパスで使われていなかった（M4）。関数抽出の動機が「ホットキーイベントの処理を整理する」であり、`show_on_startup` コードパスが後から別手段で書かれたと推測される。関数を追加したとき、**その関数が使えるすべての既存コードパスを検索して置き換えること**が DRY の完結に必要。

### 「深刻度の高いバグほど複合的な失敗から生まれる」

Bug 1（Critical）は「`emitResults` を呼ばない」と「`setFolderState(null)` の順序誤りで effect が早期リターン」という **2 つの独立した失敗が重なって** 症状を引き起こしていた。どちらか一方だけを修正しても不完全だった。複合的バグを修正するとき、各失敗要因を独立して列挙し、修正が両方に対処していることを確認する必要がある。

### 「リソース管理は対症療法より構造的解決を優先する」

Bug 7（リスナーリーク）の修正では「`unlisten` 変数に保存する」という対症療法が plan.md に記載された。この結果、H2 の構造的問題（`listen()` と `onCleanup` の非対称）が残った。リソース管理の問題は「変数に保存する」の一歩先として「**どこで・どのような構造で後始末するか**」まで計画に含めるべきだった。fix_plan.md の H2 が示した「ResizeObserver と listen を独立したクリーンアップとして並べる」パターンが、本来 plan.md に記述されるべき内容だった。

---

## 5. バグ発見フェーズ別の分布

| バグ                                   | 発見フェーズ | 根因分類                 |
|----------------------------------------|--------------|--------------------------|
| Bug 1 (exitFolderExpansion)            | research     | 複合的失敗               |
| Bug 2 (result-clicked)                 | research     | emitResults 責務の抜け   |
| Bug 3 (フォルダ履歴デッドコード)       | research     | 実装の抜け               |
| Bug 4 (parse_vk フォールバック)        | research     | 実装の抜け               |
| Bug 5 (ホットキー不整合保存)           | research     | 処理順序ミス             |
| Bug 6 (first-run 条件誤り)             | research     | 条件式ミス               |
| Bug 7 (リスナーリーク)                 | research     | リソース管理抜け         |
| Bug 8 (IME タイミング競合)             | research     | Win32 非同期競合         |
| H1 (result-double-clicked 漏れ)        | **review**   | 計画の判断ミス           |
| H2 (listen/onCleanup 非対称)           | **review**   | 計画サンプルの構造的問題 |
| M3 (recv タイムアウトなし)             | **review**   | 事前未発見・新規         |
| M4 (show_on_startup window-shown 漏れ) | **review**   | DRY 違反・新規           |

全 12 件のうち、research.md で 8 件、review.md で 4 件（うち 2 件は新規、2 件は計画の誤りに起因）が発見された。

---

## 6. このサイクルで最も鋭い洞察

**「Fix 2 の仕様変更がバグを生んだ」という逆説がある。**

元の Bug 2 は「ホバーもクリックも選択ハイライトを更新しない」という単純な実装漏れだった。ユーザーからの仕様変更（「ホバーは更新しない・クリックのみ」）を受けて plan.md が Fix 2 を更新した際、`result-double-clicked` の扱いを「変更なし（ハイライト更新不要）」と判断した。

この判断自体は合理的に見えた。`activateSelected()` がアイテムを起動するのだから、ハイライトが残り続けても問題ない、という論理だ。しかし実際には **「起動後の ResultsWindow の見た目」** だけでなく **「ダブルクリックした瞬間のハイライト表示」** も考慮が必要だった。`result-clicked` には `emitSelectionUpdate()` があり `result-double-clicked` にはない、という非対称は、コードを読んだときに直感的に「欠落」に見える。

皮肉なことに、仕様変更（ホバー不要）に真剣に対応しようとした結果、その対応が不完全だったために新しい非対称バグを埋め込んだ。**修正の集中が、隣接するコードパスの見落としを引き起こした**という教訓がここにある。

---

## 7. 未解決の残存リスク

| 問題 | 場所 | 判断 |
|------|------|------|
| M5: App.tsx の listen unlisten 未登録 | App.tsx 複数箇所 | アンマウントなし設計・HMR のみ影響・保留 |
| IME タイミング競合 | platform.rs | HWND を直接渡すことで緩和済み・理論上残存 |
| WM_CONTEXTMENU と WM_RBUTTONUP の二重メニュー | platform.rs | 一部環境での二重表示リスク・未対応 |
| requestId の二重管理 | App.tsx / search.ts | 現状整合・設計上の注意点として記録 |

M5 は「SolidJS の async onMount におけるリアクティブコンテキスト喪失」という根本的な制約に起因する。cleanup 配列を同期部分に登録する構造変更が必要で、今後 `App.tsx` を大きく変更する際に同時対応するのが合理的。
