# Retrospective — blur 猶予の 2 件（#746 SPEC 逸脱の撤去 / #745 reset-on-show の backstop）

## よかったこと

### 争点は裁定せず「争点の形」で渡すと、誰も測らなかった material が出る

`auto_hide` の live-read が armed 限定から毎フレームへ変わる件を、結論を付けずレビューへ渡した。返ってきたのは「回帰ではない」だけでなく **「回避形（クロージャ遅延評価）はそもそも borrow checker を通らない」** という実測で、裁定済みで渡していたら誰も測らなかった。ルート `CLAUDE.md`「サブエージェント委譲と worktree」の既存規範が効いた実例。

### 多視点レビューでは枠組みだけでなく道具も分ける

#745 の計画レビューで `git log -S` / `blame` を逆向きの監査 1 枠にだけ渡した結果、**「根本原因は 2 変数分解ではなく実装漏れ」という主エージェントの誤認を訂正した唯一の経路**になった（他 3 枠はコードの現在形しか見ておらず原理的に到達できない）。同 `CLAUDE.md` へ追記済み。

### 実測で裁定する癖がついた

tao 0.35.3 の `apply_diff` が `SW_HIDE` を無条件で撃つこと、`keybd_event` がキーリピートを生まないこと、engine lock が既に毎フレーム 3 回取られていること——いずれも一次資料か実行で確かめてから書いた。#923 は tao を読んで初めて機序が確定し、#927 は測れないものを未確定のまま残せた。

---

## 伸びしろ

### 実機シナリオに、人間の反応時間より短い操作を要求していた

blur 猶予は 100ms、単純反応時間は〜200ms。#745 の計画は「猶予中に focus を戻す」「armed のまま別経路 hide」など 3 シナリオをこの形で書き、**計画レビュー 5 体を通してなお実機で初めて不能と分かった**。ユーザーに無駄な試行をさせた。教訓は `docs/development-principles.md`「デバッグ・バグ修正」へ配置（注入での代替も `SetForegroundWindow` の制限で届かない）。

### テストの doc に、実装より強い検出力を書いた

「対照テスト 2 本が reset の呼び出しを消す変更を落とす」は**偽**で、`code-reviewer` の実測が暴いた（削除しても全数が通る）。危険なのは向きで、同じ差分の 4 か所が正しく「検知手段は無い」と書くなか**テストの doc だけが「守られている」と誤信させた**。同上の文書へ配置。

### 全称「偽になる散文は全件反映済み」が破れた

`mod.rs` の re-export コメントを落としており、独立導出と実装者レンズの 2 体が同時に指した。`AGENTS.md`「検証の作法」の「全称表現は前提条件とセットで書く」を、**自分の作業記述にも**当てる。
