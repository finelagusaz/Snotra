# Retrospective — #1201 ソーステキスト検査の母集団をディレクトリにし、規範を機構へ降格する

## よかったこと

### 便益を「緑になったこと」ではなく対照の差で測った

`shift_activate` を兄弟モジュールへ移す変異は、この設計では 3 本とも緑になる。それだけでは何の証拠でもないので、**同じ移設を変更前の `include_str!` 版へ当てて赤 2 本を実測**し、さらに移した先でゲートを落として赤を確認した。規範が機構になったと言えるのはこの 3 点セットからであって、緑 1 つからではない。

### 意図的な分離を根拠の所在つきで先に渡すと、誤検出が消える

`/simplify` の 4 枠すべてへ「`method_body` と `owners_of` の並存は写しではない」等 4 件を ADR の所在つきで渡したところ、**4 枠とも 1 件も誤検出しなかった**（#872 と同じ結果）。渡さなければ確実に DRY 違反として挙がる形であり、採否の判断コストが毎回乗る。

### レビューの枠組みを変えたら振動が止まった

同じレビュー枠を自分の修正差分へ当て続けても収束しない（High 3 → ⚠️4 → ⚠️2 → ⚠️2）。3 巡目で「差分をレビューせよ」から**「この文を偽にする入力を探せ」**へ変え、判定を真偽の 2 値に接地させたところ、反例が再現手順つきで返るようになった。

---

## 伸びしろ

### 全称が実装より強い、が 4 巡連続で再生した——壊れたのは毎回「数」か「代理」

High を直す差分が 4 件、それを直す差分が 2 件、さらに 2 件を生んだ。正確に書き直しても別の形で偽が出る。止まったのは**数え上げと代理条件をやめて、述語の逐語と、母集団を返す関数を指したとき**だけ。`AGENTS.md`「検証の作法」へ代理条件の一文を足した。

### 撤去の語彙を「自分が想定した書き方」で grep した

規範 1 本を降格したあと `母集団がここに縛られている` で掃いたが、残っていた行は「**そこ**に縛られている」だった。返ったのは 0 件ではなく*少なすぎる件数*で、しかも落とした 1 行は降格対象そのものだった。概念語（`母集団` / `ソーステキスト`）で掃き直して発見。正本は `docs/development-principles.md`「列挙の完全性」。

### 1 つの理由を 3 つの対象へ一括で当てた

「母集団の取り方は実ディレクトリに対してしか意味を持たないので合成 fixture では測れない」は新設 3 関数のうち 1 つにしか当たらず、残る 2 つは引数を取る純関数だった。結果、検知器の無い不変条件が 1 つ残り、`/simplify` の altitude 枠に指摘されるまで気づかなかった。`docs/development-principles.md`「列挙の完全性」へ配置。

### 変異を 3 回続けて強く書きすぎた

綴りの差し替え・実定義の重複はいずれも rustc が先に落とし、**コンパイルエラーは検知ではない**。「呼び出しを丸ごと削る」形（＝実際に起きた回帰の姿）へ書き直して測り直した。`.claude/rules/safety-nets.md`「効いていることは、フォールトインジェクションで一度は実測する」へ、この形の見分け方を追記。
