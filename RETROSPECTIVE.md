# Retrospective — 単一 issue サイクル（#471 hooks の根治 / PR #478、follow-up #473〜#477・#479 起票）

## よかったこと

### 「測られていない前提」を故障注入で暴いた
issue #471 は PostToolUse hook の 5 課題を挙げていたが、調査中にサブエージェントが公式ドキュメントを引いて「exit 0 の stdout はエージェントに届かない」と主張した。もっともらしい引用に追従せず、`ui/src` に型エラーを注入して実 Edit を行い、(a) hook が確かに走った証拠（buildinfo の mtime 更新）、(b) 診断が確かに生成された証拠（`tsc` 直接実行）、(c) 会話には一行も現れないこと、を同時に押さえた。2 回再現。結果、**PostToolUse の安全網はエージェントに対して一度も機能していなかった**ことが確定し、`CLAUDE.md:51`「Edit/Write 後に会話へ流れる」がチームの誤った信念の震源であり、その上に #472 が積まれていたと判明した。issue §4「出力予算」は、誰も受け取っていない出力の予算配分を論じていた。ドキュメントの引用は強い根拠だが、この環境で何が起きるかは測るまで誰も知らない。

### 枠組みの独立な再導出（plan-review Step 2b）が、実行の独立では出ない盲点を回収した
`workspace/plan.md` を見せずに issue とコードだけから変更集合を再導出させた 1 体と、hook 層を担当した 1 体が、**独立に同じ 2 点へ収束**した——(1) 5 hook を 1 本化すると timeout 予算が 5 個から 1 個の直列共有に縮む、(2) §5 の再発は tsconfig と述語のドリフト検出テストでしか止まらない。どちらも issue の §1〜§5 という章立て（= 計画の分解枠組み）に沿って見ている限り視界に入らない。plan-review Step 2 の 3 体（計画を渡したレビュアー）は 1 件も挙げなかった。作者の分解に anchor したレビューは作者の盲点を継承する、という Step 2b の前提が実証された。

### 「症状の洗練」から「土俵の誤り」へ、実装途中でも設計を戻せた
`formatOutput` の切り捨て通知を head/tail で対称に置く設計を練り、`error` 件数を数える案まで並べた段階で、ユーザーの「どこでやっても取りこぼしがあるなら検出手段として間違えている」の一言で全面的に組み替えた。実測すると成功時も `cargo test | tail -5` は 5 行、`clippy | head -20` は 2 行を出し、しかもパイプ終端の `head`/`tail` が exit code を握り潰していた（`false | tail -5` の exit は 0）。**出力の有無は合否を意味しない。** 検出を exit code へ移し、テキストを切り捨て可能な証拠に降格した結果、切り捨て通知も件数カウントも正規表現も不要になった。issue の根本原因「判定に使う情報と検査対象のずれ」は、出力側にも及んでいた。

### 安全網に自己検査を持たせたら、導入直後に自分の欠陥を報告してきた
`code-reviewer` の指摘で `.claude/settings.json` と `.claude/hooks/**` の編集時に `hook-selftest` を発火させたところ、その変更を Write した瞬間に vitest が走り、**古くなった自分自身のテストを見つけて報告した**。落ちたテストの名前は「自己参照ループ防止」——レビュアーが「誤った理由づけ」と指摘した、まさにその一本だった（settings.json を parse するだけの検査にループは生じない）。誤った理由が、最も安価で最も効く検査を締め出していた。

---

## 伸びしろ

### 調査自身が、根治しようとしている病を発症していた
`git ls-files 'ui/src/**/*.test.tsx'` が実在する `ui/src/MainApp.test.tsx`（深さ 0）を取りこぼした。git の pathspec の `**/` は 1 段以上のディレクトリを要求し、TypeScript の `exclude` の `**/` は 0 段にもマッチする。**同じ見た目の glob が、ツールごとに違う意味を持つ。** その不完全な列挙のまま `selectChecks` を「1 段以上」の正規表現で書いていれば、`MainApp.test.tsx` の編集で typecheck が発火し、しかもそのファイルは検査されない——issue #471 の失敗様式そのものを、それを葬るための実装の中で再現していた。`review-tsconfig` が検出。しかも同じ罠を、このサイクルの health-check で二度目に踏んだ（`git ls-files` はブレース `{ts,tsx}` を展開せず、正常な `.claude/rules/ui.md` を「マッチ 0 件」と誤報した）。→ AGENTS.md「影響範囲」に「列挙の真実源はツール自身に問う」を追記済み。

### 回帰テストのために切り出した関数を、production が呼んでいなかった
`plan.md` は「`extractFilePath` と `selectChecks` を別々にテストしても『抽出は正しいが選択へ渡す配線を間違えた』失敗を捕まえられない」と正しく述べ、そのために `checksForPayload` を切り出した。ところが実装した `main()` は同じ配線を手書きで複製しており、**その関数を一度も呼んでいなかった**（`code-reviewer` が grep で検出）。テストは production が通らない経路を固定しており、計画が閉じたはずの穴が実装で開いたままだった。AGENTS.md の既存ルール「関数を作ることとその関数を使うことは別の判断」がそのまま該当する——新ルールは不要で、既存ルールの適用漏れ。`resolveTarget()` へ統合して解消。

### 「沈黙 = 合格」という新しい契約が、沈黙する経路を三つ取りこぼした
この PR より前、hook の沈黙は何も意味していなかった（そもそも出力が届いていなかった）。この PR は沈黙に「合格」という意味を与えた。**同じ沈黙が、無害なノイズから積極的な虚偽に変わった。** ところが計画は、タイムアウトによるプロセス kill・実行ファイルの起動失敗・出力バッファ溢れという沈黙経路を塞いでいなかった（`code-reviewer` High 3 件のうち 2 件がここに集中）。値や状態に新しい意味を与える変更は、その状態に到達する全経路の列挙を伴わねばならない。→ AGENTS.md「事前調査」に追記済み。

### 学習モードの委譲先を誤った
`formatOutput` を「トレードオフのある判断」としてユーザーに委ねたが、ユーザーが返したのは実装ではなく**設計そのものの否定**だった。委ねるべき判断を選ぶとき、自分がその設計を疑い終えたかを先に確かめる必要がある。土俵が誤っていれば、その上の選択肢はすべて誤っている。
