# Retrospective — リリース ZIP への snotra-settings.exe 欠落バグの調査・修正

## よかったこと

### 2層の根本原因を素早く特定
`release.yml` を一読して「ZIP に snotra-settings.exe がない」という表層の問題をすぐに発見し、さらに「そもそも CI でビルドされていない（Cargo 依存外クレートは npx tauri build でビルドされない）」という根本原因まで2層で掘り下げられた。推測でパッチを当てるのではなく、アーキテクチャを確認してから修正したのが手戻りなしにつながった。

### 早期検知の仕組みを修正とセットで提案・実装
バグを直すだけでなく、次回同じ問題が起きたときに CI で検知できる artifact 検証ステップを追加した。「直す」と「再発防止」を同じサイクルで完結させられた。

### 教訓の即時 CLAUDE.md 反映
「子プロセスバイナリが Cargo 依存外の場合はリリースフローで明示的にビルド・検証が必要」というパターンを、サイクル中に子プロセス管理セクションへ追記できた。

---

## 伸びしろ

### snotra-settings 追加時に release.yml の更新が漏れた
`snotra-settings` crate が workspace に追加されたタイミング（feat/egui-settings）で、`release.yml` のビルド対象とパッケージング対象の更新が行われなかった。「新 crate を追加したら release.yml を確認する」という意識が抜けていた。今回の教訓を CLAUDE.md に記録したことで次回は防ぎやすくなった。

---

## ネクストアクション

- [ ] PR #134 をマージする
- [ ] タグ `v*` を push して CI の Release ワークフローが正常完了することを確認
- [ ] 生成された ZIP に `snotra.exe` と `snotra-settings.exe` の両方が含まれることを手動確認
- [ ] 展開後に `/o` で設定ウィンドウが起動することを実機確認
