# Retrospective — issue #145 Phase 3（E2E を paths 自動起動へ・`e2e` ラベル廃止）

## よかったこと

### 記録が、書いた本人の推奨を正した
前セッションの未コミット `plan.md`（A-1 シンプル）が残っていた。今セッションでわたくしは A-2（detect ジョブ）を「推奨」として提示したが、それは GitHub の paths 判定を bash で再実装する「写し」で、リポジトリの「真実源はツール自身に問う」に反していた。ユーザーは前案 A-1（ネイティブ paths）へ差し戻した。**記録への信頼で動く**が効き、「Recommended」タグがユーザーの前回判断を上書きしかけたのを、残っていた計画書が救った。教訓は feedback メモリ（recommend-native-over-handrolled）へ抽出。

### 独立再導出だけが、機能破壊の false green を拾った
当初計画は「同期対象は docs 3 箇所のみ」と完全性を主張した。plan-review 第2ラウンド（3 体）が、`deps-update` スキルが `e2e` ラベルに依存して E2E を起動していること——依存 PR は lockfile のみ変更ゆえ paths 非該当で **ラベル空振り＝起動しない workflow を待つ false green**——を独立に検出した。#495 以来 4 度目の「過剰な完全性主張を独立再導出が反証」。今回の新しさは、漏れた消費者がコンパイラのない領域（スキル `.md`）にあり静かに壊れる点。AGENTS.md に規則化した。

### サブエージェントを一次証拠にしなかったら、前案の「幻の行」を捕まえた
前 `plan.md` は `pull_request_template.md:15` の「E2E 確認」行を編集対象に挙げていたが、現物は 6 行でその行は**存在しなかった**（派生コピーの陳腐化）。code-reviewer の H1（workflow が呼ぶ姉妹ファイル prepare-sidecar / playwright / vite の paths 漏れ）も、`package.json` で実在と使用を裏取りしてから反映した。「照合は真実源に対して行う」が機能した。

### ドッグフーディングが機構を実機で証明した
この PR 自身が `e2e.yml`（paths 該当）に触れるため、push で E2E が**ラベル無しに自動起動**し 10m42s で green。マージ後は `label-sync` がラベルを GitHub からも削除。機構の発火・健全性・波及が end-to-end で実測できた。

---

## 伸びしろ

### 「推奨」を、リポジトリの文化に照らして誤りかけた
A-2（detect ジョブ）を「推奨」に据えたのは、この repo の KISS +「真実源はツール自身に問う」に反していた。ユーザーの前回判断が正すまで、より複雑で「写し」の設計へ引っ張りかけた。選択肢提示では、ネイティブ/単純案を既定の推奨に据え、追加機構には「なぜその複雑さか」を添える。前提のない「Recommended」は、ユーザー自身の判断を上書きしうる。

### 機構の廃止で、定義は消したが消費者を数え損ねた（当初計画）
`e2e` ラベルを消すとき、当初計画は定義（`labels.yml`）+ `e2e.yml` + docs しか見ず、**依存者**の `deps-update` スキルを取りこぼした。コードの改名なら compile-fail が消費者の漏れを大声で捕らえるが、ラベル・CI トリガー・規約には検出器が無い——漏れは false green で静かに現れる。第2ラウンドの独立再導出が拾ったが、初回計画で「機構名で消費者を grep + 独立再導出」していれば防げた。AGENTS.md Step 2 に規則化。
