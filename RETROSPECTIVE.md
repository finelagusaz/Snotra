# Retrospective — #858 cargo fmt をゲートへ戻す（PR #860 / #861）＋ #859 clippy の component 明示（PR #862）

## よかったこと

### ユーザーの一言が、私の推論した前提を覆した

「追跡ファイルに `cargo fmt` の記録が 0 件」から「一度も整形していない」と結論していたが、「別マシンでやっていた時に動いていた」を受けて過去コミットを使い捨て worktree で検査したら、初期コミットが drift 0 だった。**習慣は人とマシンに載っていたのでリポジトリからは原理的に見えなかった**。A/B は対等な選択肢でなくなり（B は「在ったものを捨てる」宣言になる）、issue の主題も「整形するか」から「保つ主体を人から機構へ移す」へ変わった。

### フォールトインジェクションが、機構ごとに主張の真偽を切り分けた

「fmt を先に置けば clippy の完了を待たない」は **CI では真・hook では偽**だった（hook の検査ループは `break` しない）。CI 側の FI では 13 step が skipped になり 36s で終わり、それが「ビルドを要さないので最初に置く」の直接の証拠になった。**同じ文言でも、どちらの機構の話かで真偽が反転する**——測らなければ片方の説明をもう片方へ書き写して終わっていた。

### 独立レビューと code-reviewer が、別の層の見落としを捕まえた

計画レビューは `docs/hooks.md` の写し（**どの検査にも掛からない**）と `post-edit.test.mjs` の 6→8 箇所を、実装レビューは squash マージで `.git-blame-ignore-revs` が無効化される件を発見した。前者は使い捨て worktree に計画を実適用して `governance-check` を回しており、**計画の主張 3 件を実測で裏づけたうえでの指摘**だった。

---

## 伸びしろ

### 「機構が捕まえる」という自分の主張が、検査を書かない理由として機能していた

「`BUDGETS` 完全性カナリアが 3 点セットの欠落を捕まえる」と書いて新しい検査を省いたが、実測すると赤くなるのは 1 本だけで、残り 2 本は手書きの `it.each` が唯一の検知点だった。**範囲の過大評価がそのまま判断の入力になる**。母集団を `Object.keys(BUDGETS)` から導出して輪を閉じ、教訓は `.claude/rules/safety-nets.md`「検出器のカバー範囲は、欠落のパターンごとに検算する」へ置いた（既存 2 項と同じ族の 3 つ目）。

### ローカルで動くことの実測が、配送先では誤った安心だった

`git blame --ignore-revs-file` が効くことを feature ブランチ上で確かめたが、このリポジトリは squash 専用で、記録した SHA は main の blame グラフに現れない。**存在しない rev を無視しても git は静かに成功する**ため、腐っても気づけなかった。PR を 2 本に分けて解決し、否定の知識は `docs/adr/ADR-rustfmt-gate.md` へ。

### 既知の罠（パイプが exit code を置き換える）を自分で踏んだ

`gh pr checks --watch 2>&1 | tail` の `exit 0` を gh の値と読み、**故障注入が失敗した**と誤診しかけた。`docs/development-principles.md`「構造的設計原則と強制の階梯」が名指ししている形で、#812 で一度踏まれている。出力に矛盾（`no checks reported`）が出たから気づけたが、空なら誤った結論に着地していた。

### CI の検証項目を計画に置くと循環する

「CI が赤くなることを確認」を `plan.md` のチェック項目に書いたが、`ci.yml` は `pull_request` でのみ起動し、`gh pr create` は未チェック `- [ ]` で block される。計画では閉じられない項目だった。PR 本文チェックリストへ送って解決し、教訓は `.claude/rules/safety-nets.md`「検出器のカバー範囲は、欠落のパターンごとに検算する」へ置いた——**FI を要求する当のファイルが、その実測をどこで行えるかも持つべきだから**。

### 面積 ratchet の余裕が枯れて、正しい置き場を一度あきらめた

上の 2 件は最初 `/plan-review` へ置いた。`rules` の余裕が 96 字しかなく、意味のある追記が入らなかったためである。**配置の判断は済んでいたのに数字に押されて次善へ逃げた**——`docs/adr/ADR-area-metric-characters.md`「ゼロ余裕で据える」の却下理由（摩擦の日常化で赤の信用が落ちる）の裏返しで、余裕が枯れると今度は**規範を正しい場所に置かない**方向の回避が生まれる。`AREA_BUDGET.rules` を 8790→9200 へ引き上げ、理由コメントに置き場の検算を残した。
