# Retrospective — #999 調査サイクル（注入打鍵の沈黙・製品コード 0 行）

## よかったこと

### 計器を新設せず、既に在るものが何を出すかから問うた
`SNOTRA_EGUI_INPUT_TRACE` は `rx_key` / `push_key` / `take` を既に出しており、issue が
「区別できない」と書いた (A)/(B) は**コード変更なしで割れた**。調査 issue で最初に問うべきは
「何を足すか」ではなく「もう在るものが何を出すか」である。製品コードは 1 行も触れていない。

### 判定の読み方を測る前に固定した
`plan.md` D-1 が「ON 側が 0/N なら『直った』ではなく『タイミング依存』と読む」を**測る前に**
書いていたため、20 回 0 件という結果を後付けで解釈せずに済んだ。D-4 も数値ではなく
規則（Clopper–Pearson 下限から N を導く）を先に固定し、未確定欄を空にしたまま引き渡せた。

### 接地の 1 回が本番の 20 回を守った
ハーネスは接地で 3 件の欠陥を吐いた（注入側 env の消失・未 export 関数・`finally` の戻り値混入）。
**いずれも赤くならずに標本を汚す型**で、接地を計画に入れていなければ本番を回してから気づいた。

### 委譲レビューが公開直前の誤りを止めた
結論の中心（「Down は `ArrowDown` として届いていない」）は偽で、issue へ貼る 1 手前で
レビューが反証した。**投稿を検証の完了まで保留した**判断だけが、誤った issue の公開を防いでいる。

---

## 伸びしろ

### 自分が今書いた表を、直後に母集団として扱った
`research.md` に計器の一覧表を 4 行で書き、以後その表を母集団として扱った。実際の
`input_trace(` は 6 種で、落とした `push_key` こそが結論を決める層だった。
→ `AGENTS.md`「検証の作法」の照合の条へ「派生コピーには自分が今作った表も含まれる」を統合。

### 訂正差分が、訂正した当の型を 3 ラウンド再生した
数を直したその文の隣に矛盾を残し（差の 5 を 13 と書いた）、7 主張を個別に測って全部正しかったのに
**束ねた 1 文が偽**で、照合の母集団を「3 枚」と決めて**4 枚目が未検査**だった。
共通の機序は「検査の単位を決めた瞬間、その外側が検査されなくなる」。
→ `AGENTS.md`「条件別チェック（トリガー → 参照先）」の fix-forward 行へ統合。

### 「母集団はこれで導く」と書いた grep が 0 件を返していた
rustfmt が引数を次行へ折るため 1 行前提の式は当たらない。しかもそれを
「散文から写すな」という教訓の**直下**に置いており、従った読者は「計器は 1 つも無い」へ導かれる。
→ `AGENTS.md`「検証の作法」の「書いた判定ロジックは代表入力で実行して測る」条へ統合（文書側にも射程を広げた）。

### 生ログが利用者の実パスを載せることに、証拠を書き出してから気づいた
`[trace]` の `icon:extract_failed` は Dropbox 配下やユーザー名を逐語で出す。公開リポジトリの
`workspace/` へ置けば squash マージで main の履歴に残った。
→ `AGENTS.md` の足場トリガー行へ「実 config で測るなら生ログはリポジトリ外」を統合。
