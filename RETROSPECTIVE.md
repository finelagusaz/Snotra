# Retrospective — `view.rs`（1869 行）を 3 モジュールへ分割（#666 段 3 / PR #785）

## よかったこと

### codex の反証パスが、設計書の根拠を「事実として偽」と暴いた

設計 §2.2 は「入力の読みを前へ寄せてよい」根拠に「egui の入力はフレーム内で不変だから読む順序は消費に影響しない」を置いていた。同意ではなく**反証**を求めたところ、`InputState::key_pressed()` が `num_presses()` を経て `self.events` を走査する（`egui-0.35.0/src/input_state/mod.rs`）ため**一般命題として偽**だと返り、一次資料で自ら確認して確定した。

**この命題は既存の `view.rs` コメントから引き写したものだった**——リポジトリに在る記述を根拠に再利用するとき、それが「前提条件付きでのみ真」でないかを問う機会は、反証を求めて初めて生まれた。

### 分割前バイナリの A/B が、目視の FAIL 2 件を推論でなく実測で切り分けた

目視項目 7 と `smoke:startup` の失敗はどちらも回帰に見えた。`5d4b81e` を別 worktree でビルドして同じ操作を当てたところ**両方とも同症状**で、回帰でないと確定した（コード側の裏取り——クランプ経路が `5d4b81e` とバイト同一——とも一致）。副産物として、#738 の本文が「実機未観測」と書いていた現象の**初の実機観測**が取れた。

### 規則を先に 1 本置いたので、68 項目の分類が発散しなかった

`docs/adr/0008-window-coordinator-split-rule.md` が段 1 で「線が 5 つの異なる原理で引かれている」と落ちた先例に従い、着手前に規則 R(段 3) を置いた。**条項 4（ADR-0008 の継承）が無いとフォント内部 7 項目の行き先が決まらない**ことはレビューが検出——「例外ゼロ」と主張したら全項目へ当てて検算するまで信じない、が実地で効いた。却下 8 件は `docs/adr/0011-stage3-split-rule-and-module-naming.md` へ回収した。

---

## 伸びしろ

### 列挙をツールに問いながら、出力を `head` で切った

`research.md` の「文書側の参照」を作るとき `grep -rn "view\.rs" --include=*.md . | head -40` を使い、アルファベット順で後ろの `PERFORMANCE.md`（3 箇所）を落とした。`/plan-review` のスカウトが拾った。

`AGENTS.md`「検証の作法（全タスク共通）」の「列挙も SSOT のツール自身に問う」は守ったつもりだったが、**切った時点で母集団は自分の推測に戻る**。`/plan-review`「列挙の落とし穴（独立再導出が繰り返し拾った 3 クラス）」へ 1 文で括り付けた。

### 新設した `//!` が、同じ変更で自分から出ていった関数を「持っている」と書いた

`view.rs` の新しい `//!` が「本ファイルが個別に持つ反映境界 3 つ」に `ctx.set_fonts` を数えていたが、それは同じ PR のタスク 1 で `font_stack.rs` へ移設済みだった。最終レビューが検出。

`docs/development-principles.md`「ドキュメントとコードの分担」は #749 の同型失敗（**引き取った側**が誤って主張した）を既に記録していたが、今回は**手放した側**という鏡像だった。規範が在っても方向が違えば踏む。同節へ「引き取った項目だけでなく手放した項目も現在地で引き直す」を追記した。

### 連続する委譲タスクのブリーフを、前タスクの結果を見ずに書いた

タスク 3 のブリーフへ「触るのは `view.rs` だけ」と書いたが、対象の 4 件はタスク 2 で `launcher_controller.rs` へ移っていた。実装者が推測で進めず確認したため実害は出ていない——**`view.rs` 側だけで読みを作っていたら、`on_nav_keys` 内の再読が `retain` 済みで必ず `false` になり ↑↓ ナビが沈黙して壊れていた**。ルート `CLAUDE.md`「サブエージェント委譲と worktree」の「委譲はコンテキストを継承しない」の逆向き——**委譲する側が自分の直前の成果を忘れる**形である。
