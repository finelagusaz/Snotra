# Retrospective — 前サイクルが残した 2 件を閉じる（#495 トリガ廃止 / #500 真実源への接地）

## よかったこと

### 書いたばかりの規約が、書いた本人を捕まえた
前サイクルで `AGENTS.md` に「**新設した規約は、既存の全事例に当てて検算してから書く**」を追加した。その翌日、#495 の PR でこの手順を踏み、`Step 2b` への参照を全部 grep した。すると `AGENTS.md:37` が「網羅性が要件のタスクでは Step 2b を必須の補完に組み込む」と書いており、**読者に「条件付きだ」と推論させる**構造だった。嘘ではないが、トリガ廃止と噛み合わない。規約は文章として正しいだけでは足りず、**自分の変更に適用してみて初めて効く**。

### 独立再導出が、わたくしより正確に「何を守るか」を切った
#500 の当初案は「すべての `Cargo.toml` に `hook-selftest` を撃つ」だった。独立再導出は「ルートの `Cargo.toml` だけ」と結論した。考え直すと後者が正しい——crate 追加には**必ずルートの `members` 編集が要る**のでそこで捕まる。一方パッケージ名の改名は `cargo test -p snotra` が "package did not match" で**loud に落ちる**ので、カナリアは要らない。**守るべきは沈黙する経路だけ**であり、大声で失敗するものに見張りを付けるのはコストを払って何も買っていない。この非対称は `AGENTS.md` へ抽出した。

### 起票した当人が混同していた区別を、裏取りが解いた
#500 の issue は「`-p` の列挙が `members` の写しである」と一つの問題として書かれていた（起票者はわたくしである）。しかしコードを測ると、同じ `-p` に **2 種類**あった。`check` / `clippy` の列挙は「全メンバー」の**写し**で `--workspace` に置換して消せる。`cargo test -p snotra-core` は「編集した crate → そのテスト」の**写像**で消せない。表層形が同じというだけで、対処は正反対だった。**重複排除の対象は概念であって文字列ではない。**

### `/simplify` のもっともらしい提案を、6 行のコードが退けた
レビュアーは「手書き TOML パースを `expect(src).toContain(...)` に畳め。`toContain` は**本質的に fail-closed** だから安全だ」と論じた。行数は減り、姉妹カナリアとの統一も取れ、断る理由が見当たらない。`node` で 6 行走らせたら、偽の `[workspace.metadata]` に期待どおりのリテラルを置いた瞬間 **`toContain` は緑、現行パーサは赤**になった。`AGENTS.md`「サブエージェントの実測も一次証拠として扱わない。判定の中核は自分で測る」が、そのまま効いた。

### 故障注入が、カナリア自身の欠陥を暴いた
`code-reviewer` が Medium で「カナリアの正規表現が `[workspace]` セクションにアンカーされていない」と指摘した。修正したあと、その修正が本当に効くかを確かめるため、偽のセクションを前置して注入した。**修正前なら偽の `members` を読み、本物が 4 crate にドリフトしていても緑**になっていた。指摘を信じて直すのと、直った状態を測るのは別のことである。通算 9 件の故障注入は、すべて「壊したら本当に赤くなるか」を一度ずつ確かめた。

### 写しを消したので、検知器が要らなくなった
`cargo check --workspace` は cargo に `members` を読ませる。真実源へ接地した瞬間、hook・CI・SSOT・`package.json` の 4 箇所から crate リストの写しが**消えた**。カナリアで見張るより、見張る対象そのものを無くすほうが根治的である。残った `cargo test -p` の写像だけが、カナリアの守備範囲になった。

---

## 伸びしろ

### 写しを消す PR の計画書が、別の写しを片方だけ更新しようとしていた
`CLAUDE.md` には hook-selftest の発火セットを列挙する箇所が **3 つ**あり（L69・L84・L87）、#500 の計画は 2 つしか挙げていなかった。`/plan-review` のレイヤー検証が要対処として拾わなければ、**「写しの片方だけが更新されて静かにずれる」パターンを、それを根絶する PR の中で作っていた**。同じ根を踏むのはこれで 6 度目である。

原因は単純で、**写しの数を数えなかった**こと。真実源を変えるときは、その事実が何箇所に写されているかを grep で数えてから直す。`AGENTS.md` の「照合は真実源に対して行う」に、この実務手順を書き足した。

### 「入力集合を具体対象で検算する」を、自分のカナリアに適用していなかった
新設したカナリアは `Cargo.toml` **全体**を正規表現で舐めていた。判定したいのは `[workspace]` の `members` だけなのに、入力集合に他セクションが混じっていた。`AGENTS.md`「(2) 判定対象外の説明文・メタデータが同じ入力に混じらないことを示す」がドンピシャで当たる欠陥を、そのルールを引用しながら書いた PR で作った。**検査を書くたびに、その検査の入力集合を検算する。**

### 稼働中の安全網を、実際に切って測った
`AGENTS.md` は「**稼働中のガードを弱めて測ってはならない——複製に変異を当てる**」と書いている（#482 の教訓）。にもかかわらず #484 の故障注入で、ライブの `.githooks/pre-commit` を無力化して `githooks-selftest` が落ちることを確かめた。main 保護の Layer 1 が数十秒間、作業ツリーから消えていた。

言い訳はできる——feature ブランチにいて commit もしておらず、Layer 0（GitHub ruleset）は生きており、`git status` で復元も確認した。しかし規則は「危険だったか」ではなく「弱めるな」と言っている。`githooks.test.mjs` は使い捨て repo を作って**追跡ファイルの hook** を実行するため複製への変異が素直に効かない、という事情はあるが、それは**この故障注入だけ設計を考える必要があった**という意味であって、免除ではない。セッションが途中で落ちていれば、保護の無いツリーが残った。

### `/plan-review` のトリガは、3 サイクル「省略可」と言い続けた
的中率 0%。トリガ列（リネーム / config キー変更 / データ移行 / 横断スイープ / 多モジュール）は「既に踏んだ盲点」の記録であって、次の盲点クラスには当たらない。しかも「局所的か」を判定するのは盲点を持つ当人である。廃止して無条件実施にした。**列挙型の免除条件は、免除を必要とする当人が判定する構造になっていないか疑う。**
