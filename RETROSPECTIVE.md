# Retrospective — #1214 G-module-index の逆方向を節へ絞り、残る死角を宣言した

## よかったこと

### 敵対枠が「1 例からの一般化」を破り、却下済みの案が最良だと分かった

issue は案 A を「実測したが効きません」と却下していたが、その実測は 1 例だけだった。敵対枠が母集団を索引行の全数へ広げ、**掃除の費用 0 件で 12 件多く捕まえる**ことを示した。所見は逐語追認せず自分で再測して一致を確認し、方針そのものを案 C から案 A＋宣言へ変えた。**却下の根拠にした実測も機序の説明も正しく、足りなかったのは母集団だけである**——教訓は `docs/development-principles.md`「規範を書くときの作法」へ配置した。

### 「発火しない」側を測ったことが、計器の欠陥を 3 度暴いた

フォールトインジェクションで沈黙を見るたびに原因を切り分け、いずれも変異ではなく**ハーネスの欠陥**だと判明した（自前の母集団・finding を絞る鍵・対照の独立性）。`.claude/rules/safety-nets.md`「効いていることは、フォールトインジェクションで一度は実測する」が発火しない側にも及ぶと書いていたのが実際に効いた形で、教訓を同ファイルへ戻した。

### 規範の是正を、それを暴いた変更と同じ PR に載せた

`AGENTS.md` / `docs/hooks.md` / implement スキルの「索引漏れは編集直後に reminder が鳴る」は、案 A の有無に依らず今も偽だった。別 issue へ切ると、直るまでエージェントは偽の規範を信じて作業し続ける。**偽を暴いた変更が、その偽を直す責任も引き受ける。**

---

## 伸びしろ

### 測定ハーネスは、対象と同じ頻度で壊れる

3 度の欠陥はどれも「沈黙」か「偽の赤」として現れ、**検知の有無と見分けが付かなかった**。とくに (b) は `G-module-linkage` の finding がファイル名を `file` に入れて `message` に入れないという構造の話で、**対象を 1 行も読まずに計器を読めば分かった**。実物の構築関数を使い、finding の構造を読み、対照は独立性を測ってから使う——`.claude/rules/safety-nets.md` へ配置済み。

### 検証チェックリストに、自分自身を守る経路が無かった

`scripts/` の `.mjs` だけを変えたとき、`docs/build-commands.md` のカテゴリ A〜F はどれも `npm test` を要求しない。ガバナンス検査の**回帰テスト**を走らせる経路がチェックリストから消えている。主エージェントと委譲レビューが独立に同じ穴へ到達したので、読み違いではない。#1220 として起票した。

### ニアミス: 足場を母集団のディレクトリへ置きかけた

調査用スクリプトを `scripts/governance/checks/` へ作り、即座に撤去した。ただし `registry.mjs` は不正な形のファイルを**ファイル名を名指しして throw する**ので、コミットしていれば `governance:check` が明示的に落ちていた。**放っておいても明示的に失敗するものに見張りは要らない**——階梯の判定どおり、機構も規範も足していない。

### 前サイクルの `workspace/` 残骸を、気づかないまま巻き込んだ

`git rm -r workspace/` が `adversarial-1211.txt` も消した。撤去自体は運用どおりだが、**前サイクルが畳み損ねたことに気づく契機が無い**——次のサイクルが黙って吸収するので、誰も検算しない。害は小さいため機構は置かなかった。
