# Retrospective — CSP connect-src 不足によるリリースビルドのアイコン表示不具合 (#149)

## よかったこと

### CI バイナリの strings 解析で根本原因を特定できた
Tauri ランタイムの IPC フォールバック機構（custom protocol → postMessage）はソースコードが手元にないが、CI バイナリに `strings` をかけて埋め込み JS を読み解くことで、CSP ブロック → postMessage フォールバック → バイナリ応答の破損という因果関係を特定できた。

### コードレビューで `'self'` 欠落を未然に防いだ
`connect-src` を明示すると `default-src 'self'` からのフォールバックが効かなくなる CSP の仕様を、レビューエージェントが指摘。将来の同一オリジン fetch 問題を防げた。

### 設定テストの Red/Green 確認を実施した
CSP バリデーションテスト追加後、わざと `connect-src` を削除して失敗することを確認した。設定系テストは「壊れたときに本当に落ちるか」が見えにくいため、この検証が有効だった。

---

## 伸びしろ

### 初期仮説（COM STA）に時間を費やした
「アイコンが出ない」→「アイコン抽出の失敗」と仮定し COM STA 仮説を立てたが、`.exe` ファイル（COM 不要）も失敗するスクリーンショットで覆された。「全アイコンが失敗している」という症状の網羅性を最初に確認すれば、抽出側ではなく配信側（IPC）の問題だと早く気づけた。

### dev で再現しない問題の検出が遅れた
`tauri dev` では CSP が適用されないため、開発中にこの問題は再現しない。リリースビルド固有の設定（CSP・バンドル・プロトコル）に対する検証の仕組みが不足していた。今回の静的バリデーションテスト追加で CSP については対処済み。

---

## ネクストアクション

- [ ] `fix/csp-connect-src` ブランチをプッシュし PR を作成
- [ ] リリースビルド後の手動確認（アイコンが正常に表示されること）
