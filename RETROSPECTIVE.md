# Retrospective — #872 キャレット検査の機序再設計（L2 を kittest へ・L3 を focus_state 1 点へ）

## よかったこと

### 「現れ方を塞ぐ」から「巻き込みをほどく」へ視点が変わった

7 か月・5 回の対処がすべて観測装置側だったのは、**L3（実プロセス層）のために L1・L2 を巻き込んでいた**からだった。断言を 3 層へ分け、L2 を kittest へ移して L3 を 1 点へ縮めた結果、CI runner での所要が 16〜24 秒 → **1.44s**（`rust-check` 全体も 9m58s → 4m58s）、打鍵注入 8→0、待ち 3→1 になった。新しい観測点は 1 つも作っていない——検出器は #938 が既に置いていた `egui_input:focus_state` である。

### 委譲先へ「意図的に分けた構造」を先に渡すと、誤検出が消えた

`/simplify` の 4 枠へ、重複に見えるが意図的な分離 3 件を**根拠の所在つき**で渡したところ、4 枠とも 1 件も DRY 違反として挙げなかった。教訓はルート `CLAUDE.md`「サブエージェント委譲と worktree」へ配置した。

### 出力先の指定で、レビュー成果物が 9/9 回収された

plan-review 4 枠・code-reviewer・simplify 4 枠のすべてが**報告なし idle** で終わったが、呼び出し側がパスを指定していたため所見は 1 件も落ちなかった。

---

## 伸びしろ

### 故障注入が本来の回帰より強く、縛れていない検査を「縛れている」と記録した

同じ型を **3 回**踏んだ。とくに危うかったのは、実機配管の注入を「5 フレームとも偽」で行って赤くしたが、**本来の回帰は「1 フレーム目だけ偽」**で、待ちが一致の最後を返すため検査は PASS していた形である（合成 trace で実測）。教訓は `docs/development-principles.md`「構造的設計原則と強制の階梯」へ配置した。

### レビュー指摘への修正が、新しい fail-open を作った

`code-reviewer` の指摘に応えて `AfterAll` の絞り込みを足したが、絞り込みキーが空なら母集団ゼロで自明に緑になる構造だった——**同じ PR が ADR まで書いて塞いだ「空文字 env」と同じ形**である。`AGENTS.md`「条件別チェック（トリガー → 参照先）」の「レビュー指摘へ修正（fix-forward）を当てた」が律する状況そのものであり、規範は在ったが踏んだ。

### 前提の撤回が 3 回あり、いずれも一次資料を読む前だった

「8/30 は clean」（計器汚染を見落とし）・「この検出器は前面を必要としない」（`pre.focused` の条件を読む前）・「#786 へコメントする」（CLOSED で主題も違い、当の懸念は自分の修正が既に塞いでいた）。3 件とも、構造からの推論を測る前に述べたものだった。
