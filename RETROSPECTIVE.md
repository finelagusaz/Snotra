# Retrospective — 反復 10（`target_path` の木表現をディスクへ・IndexCache v7）

## よかったこと

### 実装前に予測を commit へ書いたことが、誤診を 1 回分先回りした

WIP コミットへ「ロードの確保 724,061 → 約 411,370・構築の解放 → 約 0」を実装前に書いた。実測は 411,621（予測比 +0.08%）。**当たったこと自体より、外れたときに何を意味するかを先に決めてあったこと**が効いた——外れれば「まだどこかが `target_path` を実体化している」の合図になる。後から測ると、その合図は読めない。

### 計器が自分の帰属漏れで落ちた

`index.bin` の内訳計器が残余 +1 B で assert に落ち、`sorted_by_path` を数え落としていたことが分かった。**1 バイトでも 0 でなければ帰属は誤り**という設計が、「列を 1 本落とした誤り」と同じ厳しさで旗の落としを捕まえた。落ちる仕掛けが無ければ静かに通っていた。

### 3 段のレビューが、別々の層の欠陥を拾った

`/persistence-check` → `/code-review high` → `/simplify` で計 30 件。**誤った出力を再現した指摘は 1 件も無く**、全部が「挙動が正しいことの証拠」の側だった。1 枠では届かない——永続の版遷移、証拠の不動点化、規則の二重出荷は互いに別の探し方でしか出ない。

---

## 伸びしろ

### 接地テストが不動点に化けていた（この反復で最も高くついた見落とし）

`target_path` をディスクから消したとき、`load_cached_entries` が「ディスクの原文」でなく「木から組み直した結果」を返すようになり、312,691 件の忠実性テストが「組み直し対組み直し」になった。**テスト本体は 1 行も変わっていない**ので差分を読んでも気づかず、壊れていても件数つきの成功メッセージが出る。教訓は `/persistence-check`「Step 3 — 後方互換テストの検証（最重要）」へ配置した。

### 版を 1 つ上げると、直前の版が「全ユーザーの実運用点」に変わる

v6 が現行から旧版へ落ちた瞬間に取りこぼしが 4 件同時に出た（内訳計器が v6 で黙る回帰・version 帰属テストの v6 枝欠落・`load_cache_in` 経由の後方互換テスト不在・`from_parts` の検証にテスト 0 件）。どれも「v6 が現行だった頃は不要だった」ものである。教訓は `snotra-core/CLAUDE.md`「IndexCache バージョン変更チェックリスト」へ 8 番目として配置した。

### doc が名指した検知器が存在せず、しかも機構が見ていなかった

`index_tree_file_key_matches_normalize_file_name_key_over_frozen_v6` はリポジトリ全体でその doc 行 1 件しかヒットしなかった。`G-stale-identifiers` はこの型を捕まえる検査だが、**述語が camelCase と SCREAMING_SNAKE だけで、Rust の snake_case を 1 件も見ていない**（3 形を並べて実測）。加えて母集団が `.md` のみで `.rs` の doc コメントを含まない。#975 へ起票。
