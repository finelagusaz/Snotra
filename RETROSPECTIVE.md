# Retrospective — #1106: 表示ゲートの連言④（`visible_rows = 0`）で隠れた行の起動を塞ぐ

## よかったこと

### 陰性が読めない測定に、対照列を置いた

連言④が偽の側では `egui_results:show` が**原理的に出ない**ため、その 1 列だけでは「行が空で空振りした」と「欠陥が無かった」を区別できない。`visible_rows = 8` を同じ打鍵列で並べ、修正前後の 4 点で挟み撃ちにした。#1059 が「勝敗の比を分解する対照列」を求めたのと**用途が違う**——あちらは機序の分解、こちらは陰性の解釈可能性である。教訓は `docs/development-principles.md`「デバッグ・バグ修正」へ括り付けた。

### 敵対的調査の所見を採り、添えられた機序は自分で測り直した

3b は「smoke の検証用プロファイルは `visible_rows` を書かないので明示注入が要る」と正しく指摘し、機序として `-AdditionalSections` を薦めた。読みに行くと同関数は `[appearance]` を自分で発行しており、その経路では TOML のテーブル再定義で parse が落ちる。**所見は真、機序は偽**——ルート `CLAUDE.md`「サブエージェント委譲と worktree」が #1056 で記録した形を、今回は採る前に捕まえられた。

### 計画の「1 コミットに束ねる」判断を、コンパイラが実証した

`-D warnings` 下で新 API を先に入れると `dead_code` で落ちる、という計画の根拠が実装でそのまま再現した（述語 → `E0063` → `E0061` の順に連鎖）。**コンパイラが名指した呼び出し点 4 か所は、計画段階に LSP `findReferences` で数えた 4 か所と一致した**——`/plan-review` Step 1 の 6 が要求する束ね方は、こういう形で事後に検算できる。

---

## 伸びしろ

### `///` の帰属が動いたのに、機械検査が 1 つも鳴らなかった

述語を既存 doc ブロックの**末尾**へ書き足す形で挿入したため、`results_window_height` の doc が丸ごと新しい関数のものになり、「`0.0` は hide の契約値である……ここが正本である」という正本の宣言が別の関数へ移った。参照 4 つが宙に浮いたまま fmt / clippy / test / `cargo doc` がすべて緑。doc の欠落を lint で deny 化する案も、workspace 全体で 305 件が該当するため採れない。挿入位置を「既存アイテムの後ろ」に取れば構造的に起きないので、`docs/comment-guidelines.md`「rustdoc の様式」へ置いた。

### その修正の隣で、次の誤りを作った

H1 を直した同じコミットで、`DriveResultsInputs` の「同名フィールド」を正本として指す doc を書いた——その名前のフィールドは存在しない（実際は `visible_rows`、型も違う）。識別子は両方実在するので壊れているのは**関係**であり、grep も `cargo doc` も見ない。`docs/comment-guidelines.md` が既に「doc の名指しは intra-doc link 形で書く」と定めており、**既存規約を守っていれば起きなかった**。捕まえたのは 2 巡目のレビュー（`SendMessage` で同じエージェントを継続）である。

### 検証カテゴリの該当判定を、広げる方向へ外した

描画を 1 ピクセルも変えないのに「表示経路を触った」を理由にカテゴリ D を該当と誤認し、PR 本文とマージ後の commit message へ「未実施」を書き残した。#558 は「拡張子で狭めるな」を戒めるが、**広げる側も安全ではない**——エージェントは `smoke:manual` を実行できないので、該当と読めばそのまま人間への申し送りになり、本当に要る項目を埋もれさせる。発火条件を読み直すまで気づかなかった。`docs/build-commands.md`「D. UI のスタイル・レイアウト・テキスト表示に影響する変更（A／B／C に追加）」へ置いた。
