# Retrospective — インデックス/アイコン処理の並行性競合①②③ + ドキュメントドリフトの根治

## よかったこと

### 監査ファースト — コードに触る前に競合の全容を掴んだ
「率直な感想を」という対話の入口から index/icon サブシステムの徹底監査に発展。`index.bin` 書き込み・index-build フラグ・アイコン無効化の3つの並行性競合と、その一根（「ガードが関数に付いていてリソースに付いていない」）を実装前に構造として把握できた。沈黙する競合（レース依存・コンパイルは通る）は、この事前監査なしには TDD の俎上にすら載らなかった。

### 並行性バグを決定論的にテストした
レース再現（flaky）を避け、「ロック／状態を保持した状態で関数がスキップ・ブロックするか」を決定論的に検証するパターンを3サイクル全てで適用。`INDEX_LOCK_TEST_GUARD` でロックに触れるテストを直列化し、3回連続実行で安定を確認。`panic = "abort"` の確認が Cycle B の設計（RAII guard 不採用）を決めたように、前提の確認がそのまま設計判断になった。

### 3サイクル分解 + 検証済みプリミティブの合成
競合群を A（`INDEX_WRITE_LOCK`）/ B（`try_begin_index_build`・`finish_index_build`）/ C（`RescanOutcome`・アイコン無効化の境界整理）に分け、各々 RED→GREEN→コミット。配線・統合ステップは「検証済みプリミティブの適用」として合成で正しさを立てた。巨大 PR を避け、各サイクルが独立に検証・レビュー可能な単位になった。

### 対症ではなく根治を選んだ
`/health-check` が拾った `architecture.md` のドリフト（存在しない `commands/updater.rs`）に対し、「行を直す＋同期チェックリストを伸ばす」ではなく「二重管理しているモジュール表そのものを撤去」を選択。さらに根治を守る回帰ガード（health-check Check 2 の置換）まで実施し、「ドリフトする面の除去」と「面が再び生えないことの検証」をペアにした。

---

## 伸びしろ

### ドキュメントドリフトを生んだ構造的欠陥（既存・本サイクルで根治）
PR #311 が `updater.rs` / `restart_app` IPC を削除した際、`src-tauri/CLAUDE.md` は同期したが `docs/architecture.md`（最終更新は #310）と `SPEC.md` §20.4 を取りこぼした。原因は3つ — (a) architecture.md がサブ `CLAUDE.md` と同じファイル単位モジュール表を二重管理していた、(b) AGENTS.md Step 2 の同期ルールが「サブディレクトリ CLAUDE.md」しか名指ししていなかった、(c) 「fix」とフレーミングされた変更が実は仕様（再起動機構）を変えていたのに SPEC 同期が発火しなかった。(a)(b) は PR #326 で根治、(c) は AGENTS.md Step 0 に教訓を抽出済み。

### RED テストを書く前に副作用面を確認すべきだった
Cycle C の `invalidate_icon_cache` テストを、当初は実 `icons.bin`（`Config::config_dir()` 経由）に触れる形で書き始め、GREEN 着手時に気づいて注入可能な seam（`invalidate_icon_cache_with(_, None)`）へ作り直す手戻りがあった。filesystem や OS リソースに触れうる関数の RED テストは、書く前に副作用面を確認する。

---

## ネクストアクション

- [ ] PR #325 / #326 / #327 のレビュー・マージ（#326 → #327 の順序、または同時にマージ）
- [ ] PR #325 の手動確認: 起動直後の `/s`・設定保存で `index.bin` が破損しないか / 設定変更を連打して `indexing` フラグが UI に正しく反映されるか / スキャン対象変更後に古いアイコンが再起動を跨いで残らないか
