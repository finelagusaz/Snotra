# Retrospective — #1056: `kana_lower_names` を密な文字列アリーナで持つ（索引の per-entry 確保が消えた）

## よかったこと

### 却下案を実装前に測った

「索引から外して検索時に導出する」案を、設計を確定する前に実データ 312,108 件で測って落とした（pre-filter の通過率は最悪 3.96% だが `to_kana` が 3.1 µs/件で、最悪クエリの純増が 38 ms）。**測ったのは設計の分岐点であって実装の後ではない**ので、後戻り費用がゼロで済んだ。判断の基準は `AGENTS.md`「条件別チェック（トリガー → 参照先）」の「読みが早期 return の前か後ろか」で、`normalized_keys` とは逆側だった。

### 変異注入を毎回自分で当てた

レビュアの報告を受け取るのではなく、主張を書く前に自分で測る規律が 3 回効いた。とくに fixture へ `sort_entries_canonical` を足した後は**塊への名前の割り当てが変わる**ため、前の測定が最終形を覆っていない。3 種とも取り直した。

### `/merge-pr` の 3 点検証

(3) の「知らないうちに閉じた issue」検索は、**(1) の件がその一覧に現れることを確かめて初めて接地する**。`--jq` で生文字列の `mergedAt` を取る運用（`powershell-json-datetime-breaks-gh-queries`）とあわせて機能した。

---

## 伸びしろ

### fixture が定数を跨がず、検知器が空だった

`KANA_CHUNK = 4096` に対して A/B 突き合わせの fixture は数件で、**塊が常に 1 つしか出ない**。順序保存も底上げも一度も走っていないのに「A/B が捕まえる」と 4 か所へ書いた。切り分けたのは対照の変異（塊が 1 つでも効くもの）が赤くなったことで、「到達していない」ではなく「到達したが分岐を見ていない」と確定した。教訓は `docs/development-principles.md`「検査・検出器を置くときの作法」へ括り付けた。

### 訂正そのものが未測定の主張になった

1 度目の訂正で「A/B が守るのは 2 つの導出元の一致」と書いたが、`IndexTree::materialize` も `name_at` も `self.names.get(i)` を読むので**導出元は 1 つ**だった。**指摘が正しいことと、その指摘に添えられた機序の説明が正しいことは別**である（同じ型をレビュア側も踏み、後に自分の誤りとして追認した）。運用は ルート `CLAUDE.md`「サブエージェント委譲と worktree」へ。

### 写しの母集団に PR 本文が入っていなかった

反証された主張の写しをリポジトリ内で 5 枚直し、6 枚目の PR 本文を落とした。**PR 本文は squash で main の commit message になる**のに、ファイルを grep する数え上げには入らない。`AGENTS.md`「条件別チェック（トリガー → 参照先）」の写し行へ 1 行足した。

### 検知器の移設は新設と同じ検算を要する

余剰容量の検査を `capacity == n` の組からアリーナ用の組へ移したが、移設先の経路は確保が寸法ぴったりで**構成上つねに 0** になり、`shrink_to_fit` を外しても緑のままだった。伸長に任せる別経路の索引を fixture へ足して初めて発火した。
