# Retrospective — #984（保存が返す派生データを再構築経路も使う・`IndexMaterial` へ束ねる）

## よかったこと

### 独立導出が、計画の設計判断を一次証拠で覆した

`/plan-review`「Step 2b — 独立導出による網羅性レビュー」へ WHAT だけを渡し、`plan.md` を読ませなかった。レビュアーは `save_cache_sorted` を素直に辿り、私が `save_cache_sorted_in`（`_in` 付き）だけを読んで包み側へ一般化した誤りへ**構造的に到達した**。計画を読ませていれば前提から出発して同じ誤りを追認していた。

### 「主張を弱める」ではなく「主張を真にする」を選べた場面が 2 度あった

`&mut Option<_>` の署名で閉じたと書いたが `&mut None` が型を通っていた。可視性を実測すると crate 外の呼び出し元がいずれもゼロで、**下げれば主張が真になる**と分かった。指摘に対し文言を弱めるのが既定手だが、閉じられるかを先に測る価値が実測で出た（`docs/adr/ADR-index-material-over-per-layer-constructors.md`）。

### レビューの枠組みを変えると別の層が出た

`code-reviewer` 3 ラウンドの後に `/code-review` が 7 件（うち偽の全称 3 件）を出した。**コード変更は全ラウンドで健全**で、退行は毎回散文参照の腐りだった。同じ枠組みを回す回数ではなく、枠組みの数が効いている。

---

## 伸びしろ

### レビュアーの「解消」判定を、検算せずに解消表へ転記した

ラウンド 2 の High をラウンド 3 が「当該段落ごと消えた」と報告し、それを `plan.md` の解消表へ写した。`grep -c` 一発で現存が分かる状態だった。**再実行の結論を受け取ることと、解消の主張を自分で測ることは別である**——教訓は `AGENTS.md`「条件別チェック（トリガー → 参照先）」の fix-forward 行へ配置した。

### 写しの掃除を目視でやり、修正編集自身が新しい違反を持ち込んだ

「1 段落 1 行」の違反が 26 件あり、**直す編集が 2 件を新たに持ち込んだ**。判定をスクリプトへ移した瞬間に見え、0 件まで詰められた。同型の取りこぼしはこのサイクルで 5 回起きており、毎回「思い出した箇所を直す」をしていた。機構化は #992 へ送った。

### 機構の実装の詳細を散文へ写し、並行マージで偽になった

`governance-check.mjs` を読んで「述語は camelCase と SCREAMING_SNAKE だけ」と正確に書いたが、並行して入った #989 が 3 つ目を足して偽になった。**書いた瞬間は真で、測っても grep しても防げない**第 3 の腐り方である。教訓は `.claude/rules/governance-docs.md`（連番が並行作業で衝突する項の一般化）へ配置した。残る射程は #993。
