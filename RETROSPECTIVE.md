# Retrospective — #1059（パスクエリの残り 6,944 µs）を spike で却下し、マッチ後コストを切り出した

## よかったこと

### 「勝たなければクローズ」を計画の形へ落とした

issue が課した判定条件をそのまま計画の骨格にし、**製品実装の計画を書かなかった**。作業項目は
勝敗のどちらでも実行するものだけで構成し、条件分岐を置かないことで、未採用の枝が未チェックの
`- [ ]` として残る事故（`gh pr create` ガードと引き渡し契約の両方に触れる）を避けられた。
最も単純な選択肢（何もしない）を実測で選ぶ形が、計画として表現できた。

### 判定の比を分解する対照列が、誤った機序の記録を防いだ

A 側を**単スレッドでも測る 1 列**を足したことで、予測（6 倍削減 対 16 倍損失）が実測
（1.29 倍 / 3.01 倍）と桁違いだったと分かった。判定は正しかったので、この列が無ければ
誤った機序のまま記録していた。教訓は `docs/development-principles.md`「検証の落とし穴」へ。

### spike を製品コードに 1 行も入れず、撤去条件を成果物自身に持たせた

`PathStore` が `pub(super)` である制約から置き場所は crate 内テストに限られたが、そこで完結した。
撤去の合図を issue 番号ではなく**当該 PR のマージ**にしたので、自己参照で発火しない形になった。

---

## 伸びしろ

### 差の分解を、機序を確かめる前に文書へ書きかけた

`c:\` と `zzz` の p50 差 7.7 ms を「マッチ後コスト」と断じて `workspace/research.md` へ書いた。
実際は `find` が一致で打ち切るため両行の `find` は別物で、差は**下限**にすぎない（推定 11.4 ms）。
`docs/development-principles.md`「検証の落とし穴」の新条項は却下の測定を対象にしているが、
**この誤りは測定ではなく引き算で作った数字**だった。引き算の各項が同じ条件で測られたかを
問う習慣が要る。

### 却下の記録に、測った実装への限定を書き落とした

ADR に「並列化しても天井は 1.29 倍」と書いたが、これは spike の KMP 実装で測った比であり、
内側の最適化余地までは否定していない。メモリの鮮度チェックで
`instrument-measures-alloc-not-free`（「素朴な実装の実測を候補の最終値と読まない」）と
突き合わせて初めて気づき、同サイクル内で限定を補った。**全称に見える数字は、測った対象の
限定とセットで書く**（`AGENTS.md`「全称表現は前提条件とセットで書く」の数値版）。

### 計器の実運用点との乖離を、2 サイクル続けて「引き継ぐ」で送った

`sorted_by_path` が実運用点でだけ反転する問題は `#1057` が発見し、`#1059` は比較可能性を
優先して計器を据え置いた。`#1067` へも「引き継ぐ前提」として送っている。判断としては妥当だが、
**3 サイクル目に入るなら、送るのではなく別計器を足す側へ倒す**のが筋である。
