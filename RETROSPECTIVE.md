# Retrospective — #1094（facade の再輸出を絞り、manifest 差分を load-bearing にした）

## よかったこと

### 切り替わりを「前後差」で測った

新しい検知器が発火したことだけでは、それが唯一の見張りになった証拠にならない。絞る**前**の
複製でも同じペア消失を注入し、そちらが `ERR_MODULE_NOT_FOUND` で落ちることを対照として測った。
「絞った後に緑 + manifest だけ赤」と「絞る前は import エラー」の差が、load-bearing の実体である。
`mutation-blocked-by-earlier-path` が記録した「緑になったこと自体は証拠ではない」の裏返し。

### 変異対象を「他所から逐語で名指されていないもの」に替えた

当初は既存テストに揃えて `G-ci-table` を消す計画だったが、`governance-manifest.test.mjs` がその
id を逐語で持つため、消すと `npm test` が母集団とは無関係に赤くなる。独立導出レビューがこれを
実測で発見し、対象を替えた。替えなければ「この層は黙っている」という当の観測ができなかった。
教訓は `.claude/rules/safety-nets.md`「フォールトインジェクションでは、稼働中のガードを弱めない——複製に変異を当てる」へ。

### 列挙を grep ではなく機構へ預けた

facade の公開面から名前を消したとき、消費者が残っていないことは ESM のリンク時検査が保証する
（named import は解決失敗で落ちる）。その機構が実際に効くことを変異注入で確かめたので、
「grep で数えた」より強い根拠になった。母集団を知っているのは grep ではなく Node のリンカである。

---

## 伸びしろ

### 数え上げを直した文が、また数え上げになる連鎖を 4 回踏んだ

(1) 未検証の前提（「テストの import は隣だけ」）で `grep -v` を付け母集団を狭めた →
(2) その訂正文で「すべて実測」と書いたが実測は半分だった → (3) 訂正した grep の除外句が
no-op のまま同じ形の穴が残った → (4) その穴を名指す文が「穴が 1 つある」という数え上げになった。
**4 回とも直した本人が、直した箇所の隣で踏んでいる**——`AGENTS.md`「検証の作法（全タスク共通）」の
「自分が引用した規範ほど破りやすい」が、1 サイクル内で 4 回観測できた形である。

### 除外句を付けた根拠を測らなかった

`grep -v` は「意図した対象」ではなく「当たったもの全部」を落とす。除外あり／なしの差を取れば
no-op だと 1 コマンドで分かったのに、3 巡目まで測らなかった。教訓は
`docs/development-principles.md`「列挙の完全性」へ（母集団を名乗るときは「入らないもの」を 1 つ名指す）。

### 事前の 2 枠が同じ前提を共有して素通りした

敵対的調査（`/start-issue`「3b. 敵対的調査（サブエージェント 1 体・sonnet）」）も独立導出
（`/plan-review`「Step 2b — 独立導出による網羅性レビュー」）も、同じ走査コマンドの前提を継承して
誤りを見逃した。捕まえたのは実装後に**全 19 本を実際に消して測った** `code-reviewer` である。
読み方を変える枠より、母集団を全数で当たる measurement の方が効いた局面だった。

### 測定環境の誤読を 2 度踏んだ

`node_modules` を張らない worktree の vitest は起動前に `ERR_MODULE_NOT_FOUND` で exit 1 を返し、
変異を検知した場合と終了コードが同じになる。出力を読んでいなければ、どちらも誤った実測として
記録に残っていた。手当ては `.claude/rules/safety-nets.md` の同節へ。
