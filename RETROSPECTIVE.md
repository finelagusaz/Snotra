# Retrospective — rules 薄化の横展開（#588 の結論を全 module rule へ・4 PR）

## よかったこと

### 段階的スコープ（1 本ずつ PR）が新パターンの発見を可能にした
「4 本一括」でなく「snotra-core.md 1 本から」を選んだことで、1 本目で薄化の型（正準実在確認 → 無ければ CLAUDE.md へ移設 → ポインタ化 → 実在確認 + governance）を落ち着いて固められた。そして 3 本目 src-tauri.md で「**約半分が正準の無い原本**」という質の違いに気づき、「この rule が正準」節という**原本温存パターン**をその場で足せた。一括なら 4 本分の裏取りを同時に抱えてこの区別を見落とし、原本を誤ってポインタ化（事実喪失）していた可能性がある。

### ポインタ健全性を作成時点で機械照合した
各薄化で「参照した CLAUDE.md 見出し名・シンボル名・primitive ファイルが実在するか」を `grep -c` / `test -f` で確認してからコミットした。#588 で発掘した「位置ドリフト」を**作らない**予防（作成時点で壊れたポインタを弾く）。governance G3 は散文中の見出し参照を検知しないため、この作成時 grep が防御線になる。

### #588 の 2 規範を 4 本すべてに一貫適用
「位置をファイル名・行で断定せず名前で grep」「改名/削除時は同 PR で更新」を全ルーターに適用。特に src-tauri.md では旧ルールにあった `main.rs:893` のような**行番号参照を排除**し、シンボル名（`child.kill()` / exit ハンドラ）へ置換した。横展開が規範の実地適用の場になった。

### honest な部分薄化（硬直的に全ポインタ化しない）
全ルールが「CLAUDE.md の写し」ではなかった。src-tauri は PlatformBridge 経由・子プロセス kill・`#[must_use]` 等の**原本**を持ち、これらはコード実在を裏取りした上で「この rule が正準」節に温存した。「薄化＝全部ポインタ化」と決めつけず、正準の所在で扱いを分けたことで事実を失わなかった。

---

## 伸びしろ

### 薄化の「型」に最初から「原本の扱い」を含めるべきだった
1 本目で固めた型は「正準実在確認 → 移設 → ポインタ化」で、「正準が**無い**事実（原本）をどうするか」が欠けていた。3 本目 src-tauri で初めて原本に直面し「この rule が正準」節を発明して補った。次に別種の横展開（別の文書群の薄化・統合）をするときは、型の初手に「**対象は全て正準の写しとは限らない。正準の無い原本は消さず温存する**」を含める。#588 のルーター群（特に src-tauri.md）に実例として残るため、AGENTS.md 等への新規追記はしない（検収条件＝軽く保つ）。

### byte 削減率に一瞬囚われた
snotra-settings（4%）・src-tauri（4%）で「薄化したのに小さくならない」と評価が揺れた。だが #588 の結論どおり**薄化の価値は byte でなく drift-resistance**（コピー除去で正準変更時にルーターが腐らない）。byte 減はコピー量に比例する副次指標にすぎず、優先度判断にも成否判断にも使うべきでなかった。行数がわずかに増えても、path 配送される rule は常時ロードでないため低コスト。「軽くする」検収条件は**事実コピーの除去**で満たされており、bytes/lines の増減で一喜一憂しない。
