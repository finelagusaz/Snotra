# Retrospective — 計量されていない面の膨張を機構と形で畳む（PR #827・#830）

## よかったこと

### 成長曲線を実測してから診断した

「肥大化している」という体感に対し、19 コミットぶんの字数推移（2,682 → 27,009 字・**減少コミットはゼロ**）を出してから原因を特定した。単調増加の理由は規律の緩みではなく `AREA_BUDGET` が skills 本文を免除していること——計量されない面だけが伸びていた。**同一リポジトリ内に対照があったこと**（`/race-check` が同じ週に −28%）が、原因と処方を同時に固定した。

### 判別式を先に決め、自分が書いた文にも当てた

「経験か蒸留か」ではなく「**読んだことで実行が変わるか**」で切ると、逸話に見える実測値（トリガ的中率 0%）が残り、手順に見える骨格が出ていった。#830 で 3 値廃止の経緯を書いた直後に同じ判別式を当てて削っており、規則が書き手自身に効くことを確認できた。

### 新設した検証経路を、報告の前に実行した

スクリプト単体のテストは緑でも、`SKILL.md` に書いた `npm run plan:ledger -- init` という**文字列そのもの**は一度も走っていなかった。報告前に実行して `--` の引数転送と `allowed-tools` の glob を確認した（#802 → #803 で 5 件の沈黙バグを生んだ形の回避）。

---

## 伸びしろ

### 削減幅の見積もりを、同一サイクルで 2 度外した

−30〜45% → −28% → 実測 −19.7%。原因は一貫して同じで、**移設先への追記と跡に残すポインタを引いていない**。散文をスクリプトへ出しても呼び出し 1 行と契約への参照は残り、正準形の参照は元の記述より長くなる。教訓は `docs/development-principles.md`「撤去（消す変更）の作法」へ配置した。

### `--delete-branch` が stacked PR の下流を沈黙で閉じた

#827 のマージで base ブランチが消え、#829 が GitHub により自動 `CLOSED` になった。**base を消された PR は reopen できない**ため、rebase して #830 で取り直す復旧が要った。手順は `/merge-pr` へ 1 段落で足した——`gh pr list --state open --base <branch>` を消す前に見る。

### 並走している別セッションの主題衝突に、マージ直前まで気づかなかった

#828 が同じサイクル中に「ナレッジ配置基準」を main へ入れており、**今回の判断（執筆規約をどこへ置くか）と正面から同じ主題**だった。事後の読み合わせでは整合していたが、着手前に open PR と main の動きを見ていれば基準に沿って書けた。ファイル境界が重ならなかったため無傷で済んだだけである。

### 規範の文言を測る手段が無いまま、規範を書いた

#830 の 3 変更はいずれも「`superpowers:writing-skills` が名指しする欠陥に当たる」ことまでが根拠で、**直した結果が良いことは測っていない**。`docs/adr/ADR-two-class-reader-discrimination.md` が既存の道具を「件数は文書ではなく道具に規定される」と結論しているため、代わりの計器も無い。受容した残余として PR 本文に明記した。
