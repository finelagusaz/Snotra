# Retrospective — 単一 issue サイクル（#488 (A2) の残りを hook で守らず Layer 0 で断つ / PR #494、follow-up #495 起票）

## よかったこと

### issue の前提を裏取りしたら、二択が三択になった
issue は「hook で守る / 守らずに根拠を記録する」の二択を提示していた。`AGENTS.md` の「issue が提示する二択を鵜呑みにせず、まずコードで前提を裏取りする」に従って測ると、**前提が二重に外れていた**。文書が制御しているつもりの経路（ブランチのコミット本文 → squash 本文への連結）は 30 PR で一度も誤 close しておらず、「実害 1 件」とされた #480 でさえ発火しえなかった。逆に**実際に issue を閉じている経路**（PR 本文のリンク）は、文書の手順では止まらない — PR #491 を `--body-file` 付きでマージしたのに #489 は 1 秒後に閉じた（squash commit に `Closes` は無く、close イベントの `commit_id` は `null`）。**手順書が握っていたのは、動いていないハンドルだった。** そして裏取りの副産物として、issue が想定しなかった第三の解（リポジトリ設定 `squash_merge_commit_message` を `PR_BODY` にして Channel 2 を Layer 0 から断つ）が現れた。**コード 0 行、API 1 コール、全マージ経路に効く。**

### 対称性への欲求が、根治した失敗様式を連れ戻すところだった
「`gh pr create` を守るなら `gh pr merge` も守るべきだ」は美しく、そして誤りだった。差は重要度でも難易度でもなく、**誤りの定義が観測可能かどうか**である。空 PR の誤りは*リポジトリの状態*として定義でき hook は git に問える。誤 close の誤りは*人の意図*で、真実源がどこにもない。ゆえに `deny` は原理的に書けず `ask` 止まりになり、その `ask` は stdout JSON + exit 0 を要求して `pre-bash.mjs` の「既定 exit 2」骨格を裏返す。**#482 が根治した fail-open を、対称性が呼び戻す。** この非対称は意図的であると `CLAUDE.md` に明記した——「対称にせよ」という将来の指摘への答えとして。

### 推測で先回りしなかったので、エラーが仕様を教えた
`squash_merge_commit_message=PR_BODY` を単独で PATCH したら **422** が返り、GitHub 自身が有効な組み合わせ 4 組を列挙してきた。`PR_BODY` は `PR_TITLE` としか組めない。サブエージェントは事前に「422 になる見込み（推測・確度高）」と報告していたが、計画は「推測で先回りせず、まず単独で送って応答を測る」と書いていた。おかげで**タイトル既定も動くという副作用**を、推測ではなく実測として文書化できた。

### 停止条件を先に決めたから、塞げないものを塞いだと書かずに済んだ
`/plan-review` が「V3 の『抜け道が無くなるまで』には合格条件も裁定者もない」と指摘した。実装前に、通してはならないシナリオ E1〜E5 と上限 3 巡、そして**上限で残ったものは「受容する穴」と明記する**という打ち切り規則を計画へ書いた。実際 3 巡で収束せず、確認とマージの間の本文書き換え（TOCTOU）は塞げないまま残った。規則があったので、無限反復にも「もう大丈夫でしょう」という恣意的停止にも倒れなかった。

### 抽出した教訓そのものを、第三者に反証させた
「4 事例は同じ根を持つ」という一般化を `AGENTS.md` へ書いた後、`codex` を非対話モードで呼び、**同意ではなく反証を求めた**。返ってきたのは「その定式化は狭い」だった——事例 1（hook が payload 全体を grep）は「守りたい対象が母集団に入っていない」ではなく「**余計なものまで母集団に入っている**」問題であり、false negative ではなく false positive 側の境界ミスである。正しい根は**両方向**——「判定したい対象だけが入力集合に入り、判定したい対象が漏れなく入っている」。これは #471 でこのリポジトリ自身が書いた「判定に使う情報と検査対象がずれている」そのものであり、**わたくしは自分たちの言葉を片方向に狭めていた**。教訓の抽出にも、独立した枠組みからの再導出が要る。

### 敵対的読者を 2 種類にしたら、別クラスの穴が出た
「怠惰な読者」と「規則を全部守る規則弁護士」に、同じ文面を読ませた。前者は過剰主張の自己矛盾を、後者は**手順を一つも破らずに誤 close へ至る TOCTOU** を突いた。**忠実に従う読者が誤る経路は、手を抜く読者からは見えない。** 3 巡すべてで、見つかった抜け道は事前に用意した E1〜E5 の**外**にあった。

---

## 伸びしろ

### 根治する当のパターンを、根治の道具の中で三度踏んだ
このサイクルが直した根は「**判定に使う情報と、検査対象がずれている**」である。その修正を書く過程で、同じずれを三度作った。

1. **検証設計 V2**: 「ブランチのコミット本文にしか無い一意文字列を探す」— `PR_BODY` の下では squash 本文＝PR 本文なので、説明のために PR 本文へ書いた識別子が混入し偽陽性を出す（`/plan-review` が検出）
2. **手順 1**: 「これが閉じる issue の**唯一の一覧**である」— 手順 3（`--body-file` に `Closes` を書けば一覧に載らないまま閉じる）と矛盾し、**規範を忠実に守る読者を誤 close へ導く**（故障注入 1 巡目）
3. **手順 4**: 「閉じた issue の全件を確認する」— **残すと決めた issue は、正しく動いていれば一覧に載らない**。守りたい当の対象を一度も見ていなかった（故障注入 3 巡目）

さらに **4 件目がサイクル末に出た**。`Check 7` はメモリ索引のリンク実在と `description`↔本文の一致を検査して全て合格したが、**メモリ本文の一文が、このサイクルで実測により覆した事実を述べたままだった**。関連性を索引の `description`（本文の派生要約）で判断したため、本文を読まなかったのである。

3・4 は #489 の Check 7 と同型である。あのときは「メモリが無い」と報告する検査が、メモリのある場所を見ていなかった。**検査を設計したら、判定したい対象と実際に読んでいる入力集合の対応を両方向で検算する。** 母集団は派生した参照集合ではなく、起きた事実から取る。

### 全称表現は、読者を安心させるために書き、その瞬間に嘘になる
「唯一の一覧」「全経路から消した」「手順 1 と 4 が唯一の防御」——このサイクルで 3 箇所書き、3 箇所とも前提条件が足りず、うち 1 つは忠実な読者を事故へ導く構造だった。`AGENTS.md` に「実装より強い主張を書かない」を持ちながら、**全称量化子がその違反の具体的な発現形である**ことに気づいていなかった。書いたら前提を同じ文へ書き出す。書き出せないなら、その主張は成り立っていない。

### 検出器を書くたび、また同じ罠を踏んだ（四度目）
書き換え対象の節を他所から参照している箇所を、`body-file` / `COMMIT_MESSAGES` / `squash.*本文` で grep して「1 件」と結論した。実際には `--subject` と「本文で `Closes` を制御する」で書かれた行が `plans/…layers.md:1465` にあり、**独立再導出だけが拾った**。前サイクルは `渡す|明記` で探して `含めるもの` を落とし、その前は `process.exit()` を禁じるカナリアがコメント文に反応した。**検索語は、すでに思いついている概念の像でしかない。** 独立再導出が効くのは実行が別だからではなく、枠組みが別だから。

### 「局所的だから省略可」を、盲点を持つ当人が判定していた
`/plan-review` Step 2b のトリガ列（リネーム / config キー変更 / データ移行 / 横断スイープ / 多モジュール）に、この計画は一つも該当しない。`.md` 2 ファイル、コード 0 行——文言どおりなら省略できた。省略せず実施したところ、独立再導出だけが 2 件を拾った（`.github/pull_request_template.md` が `Closes` 行を全 PR に既定供給していること、上記の名前参照）。**トリガが変更の規模で書かれているが、盲点は規模に比例しない。** しかも「局所的かどうか」は計画の作者が自己申告する。
