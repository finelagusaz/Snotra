# Retrospective — opt-level クレート単位最適化の計測・実装 (issue #138)

## よかったこと

### issue 本文の誤りを調査フェーズで事前発見
issue では「各クレートの `Cargo.toml` に `[profile.release]` を追記する」と記載されていたが、
これは Cargo ワークスペースでは無効（ワークスペースメンバーの `[profile.*]` は無視される）。
start-issue の調査段階でこの仕様を確認し、正しい方法（ルート `Cargo.toml` の
`[profile.release.package.*]`）に修正した計画を作れた。手戻りゼロ。

### 計測 → 判断 → 実装の流れが一発で完結
フェーズごとに計測・判断・適用/却下を分けたことで、最終構成の決定が明快だった。
snotra-core の +33% レグレッションは判断基準（10%）を大幅に超えており、
「採用しない」の根拠がデータで明確に示せた。

---

## 伸びしろ

### 特になし
今回は調査・計測・実装すべて一発で完了。手戻りなし。

---

## ネクストアクション

- [ ] PR を作成して main にマージする
- [ ] リリースビルドで実際のサイズが計画通りか確認する（snotra: 5.5 MB、snotra-settings: 3.8 MB）
