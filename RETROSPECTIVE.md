# Retrospective — 単一 issue サイクル（#489 health-check Check 7 の所在と委譲規約 / PR #491、follow-up #492 起票）

## よかったこと

### 敵対的読者に 2 巡させて、初めて収束した
`Skipped` という第三の値を報告語彙に足したとき、`code-reviewer` は Critical / High を返さなかった。そこで**「怠惰なエージェント」を演じるサブエージェントに文言だけを読ませ、抜け道を探させた**。1 巡目が見つけたのは、`Skipped` を塞いだ結果、逃げ道が**隣の扉**へ移ったこと——「メモリ未初期化のため正常」と詐称すれば、Check 7 は「実施・発見事項ゼロ」に計上され `All checks passed.` に到達できる。しかも「空を正常と断定しない」縛りを、委譲先にだけ課していた。「空」に証跡を課して塞いだら、2 巡目が次の扉を見つけた——**捏造すら要らない**「読みました、全部整合していました」。塞ぎ方はリポジトリが既に持っていた（`/plan-review` の「根拠のない『確認済み』は降格する」）。新しいパターンを作らず、既存の規律を適用した。**規範は機構ではないので実行して測れない。故障注入とは、それを回避しようとする読者である。**

### 「ドキュメントの修正」を故障注入で検証できた
`.claude/skills/**` には自動検査が一つも走らない（`selectChecks` の対象外）。それでも成果物は「サブエージェントの振る舞いを変える指示」だから、実際に委譲して測れる。V1（パス無し）は `[Skipped] 検証不能` と報告し、「不在」とは言わず、`All checks passed.` を書くことも拒否した。V2（パス有り）はグリーンを返した。**指示は、それが読まれる場所で測れる。** V1 のエージェントが自ら述べた一文——「わたしの system prompt にメモリ領域のパスは無く、あるのは scratchpad だけだ」——が、根本原因（コンテキストの継承境界）の直接証拠になった。

### サブエージェントの推論を、二度続けて実測で退けた
`/plan-review` の 1 体は「`retrospective` の `allowed-tools` に `Agent` が無い**から委譲できない**。Step 7 の委譲契約はデッドレターであり『インライン実行ゆえ安全』と書け」と主張した。frontmatter の確認は正しいが、**推論であって実測ではない**。前サイクルの `/retrospective` 実行中に実際に委譲でき、誤報はまさにその経路で起きている。従っていれば**実際に起きた経路を安全と偽る**ところだった。前サイクルでも同じ形（正規表現の一致判定）で退けており、`AGENTS.md` に書き足した「判定の中核は自分で測る」が、書いた直後に二度効いた。

### 塞げないものを「塞いだ」と書かなかった
「パスを持たない」「読んだが整合していた」という**自己申告は、文書では反証できない**。防御は根拠の要求（偽るコストを上げる）と `Skipped` の可視化（黙って消えられなくする）だけである。これを Check 7 に「規範であって機構ではない（受容する穴）」として明記した。`pre-bash.mjs` が `sh -c` 経由の迂回を受容する穴として書いたのと同じ規律。**実装より強い主張をドキュメントに書かない。**

---

## 伸びしろ

### 自分で起票した issue の「期待する解」を、症状のまま受け取りかけた
issue 本文（前サイクルの自分が書いた）は「Check 7 は所在を書いていない」と述べ、解として「所在を明記する」を挙げていた。しかし調査すると、それは症状だった。真の根は**コンテキストの継承境界**——メモリ領域の絶対パスはメインエージェントの system prompt にだけ存在し、委譲の瞬間に落ちる。`Glob` も `Read` も絶対パスならリポジトリ外へ届く（実測）。届かないのはツールではなく**届け先を知る者**である。ゆえに書くべきは定数ではなく手続き（「委譲するときはパスを渡せ」）だった。`AGENTS.md` の既存ルール「issue が提示する二択を鵜呑みにせず、まずコードで前提を裏取りする」がそのまま該当する——**自分が書いた issue にも適用される**。新ルールは不要で、適用漏れ。

### 新しい分類を足すとき、隣接分類への到達経路を列挙しなかった
`AGENTS.md` には既に「これまで無意味だった状態に意味を与える変更は、その状態に到達する全経路を列挙する」がある。`Skipped` を導入したとき、`Skipped` への経路は塞いだが、**その導入によって意味が変わった隣接分類**（「正常（未初期化）」「発見事項ゼロ」）への経路を見直さなかった。既存ルールの射程が「新しい状態」に限られていたための取りこぼしで、ルールを「分類を 1 つ足したときは隣接分類も再列挙する」へ拡張した。**逃げ道は塞いだ扉の隣に現れる。**

### 検出器を書くたび、また同じ罠を踏んだ
`/symmetric-check` で「委譲先に何を渡すか明記しているスキル」を数えようとして、`渡す|明記|読ませない` という語で grep した。`research-review` は「各サブエージェントへの指示に**含めるもの**」と書いており、実在する記述を取りこぼして「記述なし」と誤判定した。前サイクルでは `process.exit()` を禁じるカナリアがコメント文に反応した。`docs/development-principles.md` 原則 6 に「判定単位は構文的位置で定義する」を書いた当のサイクルの、次のサイクルである。**文字列と概念の距離は、書くたびに測り直すしかない。**

### 検査対象を変更しながら、検査を走らせた
サイクル末 health-check を、待ち時間を惜しんで Step 6（メモリの更新）**より先に**委譲起動した。スキルは Step 6 → Step 7 の順に並んでいる。結果、返ってきた報告は「`MEMORY.md` の索引が本文と矛盾している」という Warning を含んでいた——**古い索引と、更新後の本文を、同時に見ていた**。実際には索引も更新済みで、これは誤報である。**委譲した検査が対象を読む時刻は制御できない。** そしてこの誤報の構造は、このサイクルが根治した当のバグと寸分違わない——判定に使う情報と、検査対象がずれている。サブエージェントの報告を鵜呑みにせず自分で測ったから気づけたが、気づかなければ「実在しないドリフト」を修正していた。

### メモリが古びていることに、指摘されるまで気づかなかった
`subagent-must-sendmessage-to-main` は「`Agent` の成果物は届かない。`SendMessage` で明示せよ」と述べていたが、このセッションだけで 8 体のサブエージェントから成果物を tool result として受け取っている。harness は変わる。**メモリは「書かれた時点で真だったこと」を保存する時限付きの事実であり、「昔こうだったから今もこうだ」は推論であって実測ではない。** `/retrospective` Step 6 が無ければ、この誤った信念の上に次の委譲設計が積まれていた。
