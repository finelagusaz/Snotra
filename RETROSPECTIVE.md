# Retrospective — 呼び出し元ゼロの `productionOnly` 撤去と、外因で赤くなった CI への対処（#1095 / #1160 / #1161）

## よかったこと

### 委譲の 3 ラウンドが、毎回「前ラウンドの修正が生んだ誤り」を捕まえた

High（残余の実測が今日偽）→ Medium（その修正で書いた由来が誤り）→ Medium（さらにその隣の指示語が過大）と、**同一 hunk で 2 度**「修正が新しい誤りを生む」型を踏み、どちらも次のラウンドが捕捉した。`AGENTS.md`「条件別チェック（トリガー → 参照先）」の fix-forward 行が机上の用心ではないことの実証である。

### 道具を分けた枠が、主エージェントに原理的に届かない層を暴いた

独立導出だけが `git log -S` を使い、「この関数は G-stale-identifiers のために書かれたものではない」を確定させた。コードの現在形しか見ない枠は到達できない。ルート `CLAUDE.md`「サブエージェント委譲と worktree」の記録どおりの再現である。

### 撤去で消える測定を、生きた層へ回収した

委譲が副産物で出した「禁止された変換を入れても `governance:check` は緑のまま落ちる」は `workspace/` にしか無く、撤去で永久に失われるところだった。`lib.mjs` の禁止文の隣へ性質だけ残した——沈黙して壊れると知っている読者は、その警告を消さない。

### 照合対象を逐語で `plan.md` へ置いたことが、エディタのクラッシュを越えて効いた

ベースライン行は当初 scratchpad にしかなく、「別セッションの `/implement` が読めない」を理由に durable 化した。実際に効いたのは**クラッシュ後の再開**という想定外の事故に対してだった。

---

## 伸びしろ

### 「不在の観測ひとつで全称を確定させた」を 2 度踏んだ

clippy の指摘を「2 箇所」と読んだが、**判定器がエラーの出た crate で止まっていた**だけで実際は 4 箇所。banner の由来を「#891」と書いたが、測っていなかった（実際は #885）。どちらも `AGENTS.md`「検証の作法」の全称条項に当たる。**探し方を変える最も強い手は、判定器を自分の手元に持ってくること**だった。

### 判定器がローカルより新しい区間を想定していなかった

`.claude/rules/safety-nets.md`「CI の実測は PR が在って初めて行える」を workflow の話として読んでいたが、**lint のバージョン差でも同じ構造が現れる**。この穴は `rust-toolchain.toml` の固定（#1162）で機構が吸収した。

### 修正のついでに新しい断定を書いて、2 度とも誤った

「直す」作業中は注意が指摘箇所へ集中し、**その周辺へ新規の主張を足すときだけ検算が抜ける**。3 度目は自分で気づけたが、それは委譲へ「ここが今いちばん怪しい」と書こうとした瞬間だった——書けば怪しいと分かるものは、書く前に測るべきだった。
