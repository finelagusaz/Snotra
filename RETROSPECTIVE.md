# Retrospective — #646 egui UI 再設計（PR1: Metrics font 連動 + 2 行表示 / PR2: 結果窓の分離）

## よかったこと

### 触れるモックアップが設計判断を前倒しした

spec を書く前に、スライダーで `font_size` / 余白 / ギャップ / 件数を動かせる HTML モックアップを出した。結果、**コードを 1 行も書く前に**「ギャップは 8px でなく 4px」「結果行は 2 行表示にする」という 2 つの決定がユーザーから返ってきた。散文で「実件数フィットにしますか」と問うていたら得られなかった答えで、しかも後から変えれば PR2 の窓分離をやり直す規模だった。**視覚的な判断を仰ぐ局面では、言葉で問う前に触れるものを出す**方が速い。

### 独立再導出が今回も、偵察 3 体が見なかったものを拾った

`/plan-review` の Step 2b（計画を読ませない独立導出）が PR2 で 3 件を単独検出した——設定サイドカー起動時の `always_on_top` 解除が main だけで results に非対称（片方だけ下げると結果カードが設定画面の上に浮く）、`mod.rs` 再エクスポートの trim 漏れ（コンパイル不能になる）、そして「results で `frame.hide_window()` を呼ぶと `visible` フラグが固着し永久非描画になる」という禁止事項。**成果物監査（計画を読ませる偵察）は作者の分解を継承する**という前提どおりの結果で、4 サイクル連続で独立再導出だけが漏れを拾っている。

### 実装者の「Concern」欄が 2 件の断線を自己申告した

Task 4 では brief に無い reset フィールドの初期化漏れを自分で見つけて塞ぎ（放置すれば再 show 後に結果窓が永久にスキップされる）、Task 5 では移設で落ちた debounce ゲート（連打中に icon worker を積まない最適化）を「指示範囲外だが」と報告した。どちらも**レビューより前に実装者が見つけた**。報告フォーマットに「懸念」を必須欄として置いておく効果が出た。

### 根本原因を一次資料で接地し、連動する罠を修正前に予測できた

フォーカス奪取バグでは症状から直さず、tao 0.35.3 のソースを読んで `ShowWindow(SW_SHOW)` の活性化・`MARKER_DONT_FOCUS` が生成時 1 回限りであること・`apply_diff` の早期 return を確認した。その読解から「raw show にすると `hide()` が効かなくなる」「`set_always_on_top` が `SW_HIDE` を撃つ」という**まだ起きていない 2 つの故障を予測**し、同じコミットで塞いだ。症状だけ直していれば Escape で窓が消えなくなり、設定画面を開くと結果窓が消える別バグを 2 つ生んでいた。

---

## 伸びしろ

### コードレビューでは接地できない挙動クラスがある、と分かって workflow に入れていたのが唯一の救いだった

フォーカス奪取（1 文字入力で結果窓が現れた瞬間に入力欄からフォーカスが移り、2 文字目が打てない）は、**タスク単位レビュー 7 件・whole-branch レビュー（opus）・`/code-review` の 5 観点をすべて通過した**。diff には `focusable(false)` と `results.show()` が並んでいるだけで、その 2 行が OS レベルで衝突することは外部クレートのソースを読まない限り見えない。検出したのは実機スモークのみ。

救いは、最終レビューが自ら "Ready to merge? **With fixes**（コード修正は不要だが実機スモークがゲート）" と判定し、フォーカス非奪取を最優先確認項目に挙げていたこと。**「レビューで確からしさが上がった」と「実機で確かめた」を混同しない**運用が働いた。OS/ウィンドウ系（フォーカス・活性化・Z オーダー・DPI）は今後も計画段階で実機項目に落とす。

### 「この API で足りる」の裏取りが、同じファイルの隣の関数で覆った

事前調査で `WindowBuilder::focusable(false)` を推した根拠は、tao の `window_state.rs:297`（`!FOCUSABLE → WS_EX_NOACTIVATE`）を実際に読んだことだった。読んだ行は正しい。しかし**同じファイルの 325-336 行**——`ShowWindow` が `SW_SHOW` か `SW_SHOWNOACTIVATE` かを別条件で選ぶ分岐——は読んでいなかった。

「宣言的な属性で挙動を代替させる」判断は、**その属性が有効になる条件**だけでなく、**同じ状態を読む他の経路がそれをどう扱うか**まで見ないと片手落ちになる。教訓は `src-tauri/CLAUDE.md`「Win32 / Tauri 注意事項」へ配置した。

### テストの Red が「コンパイルエラー」で満たされ、検証したい経路を一度も通らなかった

PR1 で入れた config 新キーのテストは、`[visual]` セクションごと省いた TOML を fixture にしていた。この形は**親側の** `#[serde(default)]` で `VisualConfig` 丸ごと既定値に落ちるため、検証したかった field-level の `#[serde(default = "...")]` 属性が一度も実行されない——**属性を削除してもテストが通る**。TDD の Red は「フィールドが無い」というコンパイルエラーで満たされてしまい、そこで気づけなかった。

PR2 のレビューが指摘して初めて発覚し、修正では属性を一時的に外して**実際にテストが落ちること**を確認した。「落ちるのを見た」の中身が構文エラーなのか意味の検証なのかを区別する。教訓は `/persistence-check` Step 3 へ配置した。
