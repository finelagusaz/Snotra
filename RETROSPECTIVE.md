# Retrospective — 規範を機構へ移し、序数を引用の語彙から外す（#812 ほか・PR #806〜#817）

## よかったこと

### 着手前ゲートが、計画の前提を 4 回否定した

`/race-check` の母集団スクリプト・G-stale-identifiers・G-near-heading-refs・#789 の見直しで、**実装前に測った結果が計画の前提を覆した**。G-stale-identifiers は「#736 の 10 件を閉じる」前提が 2 件しか届かず自称スコープを狭め、G-near-heading-refs は #727 が提案した「窓幅を狭く取る」が分離軸として成立せず「引用が着地するか」へ替えた。

**実装後に気づいていれば、実装より強い主張を持つ機構が main に入っていた。** `AGENTS.md`「検証の作法（全タスク共通）」の「判定ロジックは実装前に代表入力で実行して測る」が 4/4 で効いた。

### `/norm-review` が、削る方向と残す方向の両方に証拠を出した

PR #806 では 4 条項中 2 条項に「対照も捕まえた（条項不要）」が出て削除し、`SKILL.md` は 140 行 7721 字 → 131 行 6978 字になった。PR #813 では 2 条項とも「捕まえた」で残した。**同じ計器が両方向へ振れたことで、初めて計器として信頼できる**（`docs/adr/ADR-norm-review-seeding.md`「帰結」が予告した機能の初発火）。

### 「検出できない」の正解が「検出器を足す」でないことが 2 度あった

#789（ADR 連番の重複を検出できない）の正解は連番の廃止だった——**同じ slug は同じパスになるので `git merge` が `CONFLICT (add/add)` で捕まえる**（実測）。#812 も却下案 1 で同じ判断をしている。`docs/development-principles.md`「構造的設計原則と強制の階梯」の (a)(b) より上に「**その状態を作れなくする**」段がある。

---

## 伸びしろ

### パイプが exit code を潰し、赤のままコミットした

`npm run governance:check && git commit` の鎖に `| tail -2` を挟んだため、G-heading-refs の赤を見落として commit まで進んだ。同じ形を本サイクルで 2 度踏んでいる（もう 1 度は `git push … | tail && gh pr create` で PreToolUse hook が鎖を認識できず拒否）。`docs/development-principles.md`「構造的設計原則と強制の階梯」項目 6 へ 1 文を括り付けた。

### 取り直す前の状態を根拠に断定した

#810 へ「この PR は現状のままマージできない」とコメントしたが、**その時点で既にマージ済みだった**（4 分前）。破壊的操作ではなかったため訂正コメント 1 件で済んだ。**#810 自身がまったく同じ機序で失敗している**——起票時に `0017` が空いていることを確認し、マージ時にはもう古かった。規範の裁定と機構の衝突が、同じ形で起きた。

### `/norm-review` の対照設計を誤り、効いている条項を削除候補にしかけた

種 1 の 1 度目で、射程の条項だけを削っても**命名の条項が同じ答えを残していた**ため、対照が正解を出した。これを「条項不要」と読めば、効いている条項を削っていた。不成立として、変更前の版そのものを対照に再走した。`/norm-review`「Step 2 — 対照（規範なし）」へ 1 文を括り付けた。

### 検出器を書くと、最初の被験者は自分になる

本サイクルで 3 回、自分が置いた検出器に自分の成果物が捕まった（G-heading-refs が ADR の見出し参照を、G-near-heading-refs が ADR に書いた「悪い形の実例」を、G-adr-citations がテストのフィクスチャを）。いずれも検出器が本物である証拠になったが、**フィクスチャは赤経路のため意図的に無効な値を持つ**という性質は、母集団の設計時に予見できたはずである。
