# Retrospective — #1211 アンインストール時のスタートアップ値（実装ゼロで閉じた）

## よかったこと

### 実装より先に、issue の前提そのものを対象で測った

`installerHooks` を書き始める前に `npx tauri build --bundles nsis` を打ち、生成された `installer.nsi` を読んだ。テンプレートは既に `DeleteRegValue` を持っており、**issue の前提が偽だった**。CLI バイナリから抽出したテンプレート（代理）で止めていたら `${PRODUCTNAME}` の展開は推測のままだった。**レンダリング済みの成果物を読む一手が、実装ゼロという結論を出した唯一の経路である。**

### 敵対的枠が、所見ではなく「断定の強さ」を正した

「`.nsh` を足すと更新で消える退行になる」は強すぎた——`$UpdateMode` は `un.onInit` で設定済みなので、フック内で同じガードを書けば回避できる。指摘を受けて機序を自分で一次証拠に当て、断定を「案文どおりに書けば退行する」へ弱め、却下の荷重を「そもそも不要」の側へ移した。**却下の結論が正しくても、その理由が過剰なら次の人を誤らせる。**

### 検査の赤を、検査自身が書いた処方で解いた

`G-stale-identifiers` が `productName` で鳴ったのは、`.json` が語彙源でないという**当該検査が残余として記録済みの形**だった。処方も同じ場所にあり（「文書側の記述を正確化して外す」・`docs/hooks.md` の `${CLAUDE_PROJECT_DIR}` が先例）、JSON のキーを実際の字面で書いて緑にした。**赤を見たら、まずその検査が自分の死角をどう書いているかを読む。**

---

## 伸びしろ

### 「受容する残余」は、直さない宣言ゆえ誰も後で検算しない

#1210 が書いた「アンインストールしても値が残る」は、Tauri の挙動を測らない類推だった。**残余は「これは直さない」という終端の宣言なので、後続のサイクルが検算しない。** しかも issue へ切り出されたことで「対処予定」の見た目が付き、訂正のためだけに調査サイクルが 1 本要った。教訓は `.claude/rules/spec.md` へ配置した（SPEC.md を読むと自動配送される）。

### 外部ツールの生成物は、依存更新の差分に現れない

削除行が効いているのは上流テンプレートがそう書いているからで、そのテンプレートはリポジトリの中に無い。将来 Tauri が行を外しても、`Cargo.lock` にも `package-lock.json` にも「消えた」とは書かれない。**気づく契機は `tauri build` の生成物を読むときだけ**であり、機構は置けなかった。`SPEC.md` §7.7 に残余として宣言してある。

### 写しの結合を宣言で済ませた——止める力は無い

`RUN_VALUE_NAME` と `"productName"` はずれても緑のまま削除だけが空振りする。検知器を置く案は差分を最小にする判断で見送り、`SPEC.md` の受容する残余へ落とした。`tauri.conf.json` は厳格な JSON でコメントを書けないため、注意書きは Rust 側にしか置けない。**宣言は受容であって解決ではない**——次に触る人がこの非対称に気づけるかは測っていない。
