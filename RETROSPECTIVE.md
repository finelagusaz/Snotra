# Retrospective — #738（ドラッグを終えた時点で main のバー矩形を作業領域内へ戻す・PR #909）

## よかったこと

### 敵対的レビューが計画を作り直させた

codex へ「同意でなく反証」を求めて 9 件、うち 5 件が設計を変える指摘だった。とくに「`MonitorFromWindow` は窓全体の矩形で選ぶので、行が出ただけでバーが隣モニターへ飛ぶ」と「毎フレームクランプはモニター間移動を封鎖する」は、主エージェントと advisor の両方が見落としていた。**後者は issue コメント 1 で人間が挙げていた懸念そのもの**で、計画は「ゴムバンドでも受け入れ条件を満たす」と誤判定していた。

### 識別的な検証手順を先に決めたことが、期せずして成立した

「画面中央で伸長させても何も証明できない（十分内側ならクランプ自体が起きない）」という指摘を受けて手順を具体化した。実測では起動直後に行が出た状態でクランプが走り、`top=956`（実高でクランプすれば 902）という直接の証拠が取れた。**手順を識別的にしておくと、狙っていなかった機会が証拠になる。**

### 却下の記録に「構造側の選択肢」が無いことを指摘で拾えた

`/simplify` の altitude レビューが「却下 1〜5 はすべて policy で、**誰が窓の幾何を所有するかを問うものが 1 つも無い**」「`git grep 878` が 0 件」と指摘した。`ADR-main-window-clamp-on-pointer-release` へ #878 継ぎ目 2 の検討記録を足し、#760 に着手するとき先に量るべきことを申し送った。

---

## 伸びしろ

### レビュー指摘への対処が、人間が裁定した不変条件を壊した

`code-reviewer` の Medium（`any_down()` の固着に備える backstop）は動機が一次資料で裏づいていたが、**実測するとドラッグ中も毎フレームクランプが走り、人間裁定「ドラッグを終えたら戻る」を壊していた**。ユニットテストも smoke も緑のままで、対照実験だけが差を示した。`docs/development-principles.md`「デバッグ・バグ修正」の「修正は新しい欠陥の生成過程」へ、**答えが実測でしか出ない場合がある**ことを括り付けた。

### 4b の修正で書いたコードに 4a の check が当たらない

`code-reviewer` の High を直す際に 4 段の導出を逐語で複写した。`/dry-check` は 4a で既に走り終えており 0 件だったため捕まらず、後から回した `/simplify` の 3 体が発見した。同じファイルの doc が「計算した値を捨てる関数は、次の利用者に写しを書かせる」とこの形を名指しで禁じていた。順序が固定である限り注意深さでは埋まらないので、#910 で構造側の対処を提案した。

### 却下した案の痕跡が、同一コミット内で文書に残った

backstop を実測で却下したのに、`src-tauri/CLAUDE.md` へ先に書いていた「ポインタ非押下か reset-on-show のフレームに限る」を直し忘れた。**同じコミットの中で実装と文書が食い違っていた**。発見は `/simplify` の 1 体で、しかも「範囲外ファイル（`*.md`）だが矛盾なので報告する」という判断に依っている。

### カテゴリ D の治具が `C:/tmp` へ戻った

前サイクル（#755/#801）ではリポジトリの `scripts/lib/SnotraSmoke.psm1` 経由で書けたのに、今回はマウス注入（`SetCursorPos` + `mouse_event`）が要るため自前 P/Invoke の使い捨て治具に戻った。**モジュールを通らないので #866 の画面ロック検出に守られない**。加えて OS の move loop を跨ぐドラッグ注入は再現性が低く（同一条件で 956 と 1050 が揺れた）、対照実験でしか結論を出せなかった。
