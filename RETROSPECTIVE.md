# Retrospective — results 窓の可視性を 4 連言の導出へ解いたサイクル（#750 / #752）

`SPEC.md` §8.6 の述語を文書側で整理し（#750）、実装側で融合された bool を解いた（#752）。3 段再編の段 2 にあたる。

## よかったこと

### 枠組みの独立が、実行の独立より効いた

計画レビューで 4 体を並列に走らせたが、**`plan.md` を読ませず同じ問題を一から解かせた 1 体だけ**が「問いの対象は 2 つの窓であって `view.rs` の内部ではない」に到達し、`SPEC.md` §8.6 の図の誤り——`IndexingMode` を排他ノードとして描く一方、同じ節の注記がそれを「どのモードにも重なる直交 boolean」と呼ぶ自己矛盾——を見つけた。計画を渡された 3 体は、渡された時点で同じ場所を見ていない。

一方で技術的な急所（`ctx.set_visuals` が現在の pass の `Ui` に届かない）は、枠組みを共有した 2 体が**別々の道具**（egui のソース追跡と Cargo registry の実体照合）で独立に当てた。**独立性は「枠組み」と「道具」の 2 軸で効く。**

### 敵対的レビューが、恒久化寸前の誤った禁止を壊した

計画は一度「`show_results` の構成を導出へ畳むのは恒久的に禁止」と結論し、それを実装の doc コメントへ書く直前まで進んだ。外部モデルによる敵対レンズが二分法の非網羅性を突き、第三案（pre-click の値を保持し post-click では件数だけ再評価）が実在することを示した。**その案は手元の独立再導出の成果物に既に書かれていた。**

誤った禁止を恒久文書へ固定していれば、実在する選択肢を将来の実装者から隠していた。経緯は `docs/adr/0007-results-presentation-two-stage.md`「検討した代替案と却下理由」の却下 3 に残した。

### 目視だけが捕まえた欠陥がある

カテゴリ D の実機目視で #755 を発見した。hide 時に status / toast が出ていると、再表示後に窓が伸びず案内が切り取られる。`show_egui_main` が窓を直接畳む一方、reset-on-show が main のサイズガードを戻さない（results 側は戻している——対称性の破れ）。

**clippy もユニットテストも trace-presence のスモークも一度も指摘していない。** 窓が伸びなかったことは、どの自動検出器の視界にも入らない。目視の費用に見合う成果が出た実例である。

### trace を「起きてはならないこと」の側で書けた

目視の支援ハーネスで、presence 検査ではない判定を 3 つ立てられた（main が hidden の間の `results:show` が 0 件／`rows = 0` の show が 0 件／hide を挟まない連続 show が 0 件）。`src-tauri/CLAUDE.md`「Win32 / Tauri 注意事項」が求める形が、実際に書ける不変条件として存在することを確認した。判定式は #757 に残した。

---

## 伸びしろ

### 同じ失敗を 4 度踏んだ——SSOT に問わず、手元の派生物で判断する

1. テスト本数を自作の正規表現で数え、`#[test]` に問わなかった（2 本取りこぼし）
2. 独立再導出の成果物 381 行を手元に持ちながら、通知の要約 20 行で統合した。落としたのは受け入れ条件の可否を決める発見と、未決事項 3 件
3. 削除するシンボルの名前依存を誰も grep で数えず、計画・独立再導出・レビュアの 3 者が揃って 3〜4 箇所と見積もった（実測 16 箇所）
4. `workspace/` の運用を `/plan-review` の記述だけで判断した。削除の規約は `/implement` にあった

4 度とも気づかせたのは外部の目である。`AGENTS.md`「検証の作法（全タスク共通）」は既にこれを禁じており、**規範を知らなかったのではなく、自分の判断がその規範の適用対象だと思っていなかった。** 前サイクルの「自分が引用した規範を、自分の書いたものに当てていなかった」が、そのまま再演された。

3 の教訓（compile-fail が名指しするのはコード参照だけである）は `docs/development-principles.md`「撤去（消す変更）の作法」へ配置した。

### 疑いながら、書き直さずに検査へ送った

計画の先送りに受け皿が無いことを「疑いがある」と**自分で書いた上で**、そのままレビューへ出した。指摘は当然のように返ってきた。**検査に依存して自分の判断を省いた**形である。疑った時点で直すほうが、レビューの往復より安い。

### 禁止は全称であり、前提条件とセットで書く必要がある

`AGENTS.md`「検証の作法（全タスク共通）」の「全称表現は前提条件とセットで書く」を肯定の側では意識していたが、**禁止の側では意識していなかった**。「〜してはならない」も同じ強さの主張であり、前提が書けないなら書いてはならない。今回はレビューで捕まったが、恒久文書へ入れば長く残る種類の誤りだった。

### 計測器の区間の切り方を誤り、自分のデータを読み違えた

目視ハーネスでマーカーを操作の「後」に打った結果、直前の操作が前の区間へ紛れ込み、「フレームが 1 枚も走っていない」と誤読した。実際にはイベントは存在していた。**計器を疑ったのは正しかったが、疑う先を間違えた**——マーカーの時刻とイベントの時刻を突き合わせて初めて分かった。**マーカーは操作の前に打つ。**
