# Retrospective — 単一 issue サイクル（#482 PreToolUse を pre-bash.mjs へ / PR #487、#473 クローズ、follow-up #488 起票）

## よかったこと

### バグが、バグを説明する行為そのものを妨げた——そして三度それに気づけた
調査中、`gh pr create` を**検索パターン文字列として含むだけ**の `grep` が hook にブロックされた。コミットメッセージ本文に書いたときも、その本文をファイルへ逃がすまでブロックされた。合計 3 回。同じ文字列を含む PowerShell コマンドは素通りした。狙って作った再現ではなく、**issue を調査する行為が issue を発症させた**。この一次証拠が `research.md` の骨格になり、「判定単位は文字列の出現ではなく構文的位置」という設計に直結した。実測は探しにいくものだが、向こうから来ることもある——来たときに「邪魔だ」ではなく「証拠だ」と読めるかが分かれ目だった。

### 未確定の一点を「実装を終えてはならない条件」として計画に書き、実測で閉じた
Windows の PowerShell tool の `tool_name` は公式ドキュメントに記載が無い。`matcher` が取りこぼせば D1 は直っていないのに、テストは全て緑になる（matcher と `TARGET_TOOLS` は構成上つねに一致するため、カナリアも検出できない）。計画 §6 に「この 1 点が未確定のまま実装を終えてはならない」と明記し、使い捨てブランチ（upstream 未設定）で PowerShell から `gh pr create --help` を叩き、`tool_name=PowerShell` を stderr で観測した。プローブに `--help` を選んだのは、**hook が発火しなくても PR が作られない**から——安全網を試すのに、失敗したら本番が壊れる実験をしてはいけない。`code-reviewer` は後にこの点を独立に High として挙げ、テストでは原理的に検出不能だと指摘した。計画が正しく警戒していた。

### 「既定値を安全側に置く」が、異常経路の個別ハンドリングを消した
PreToolUse は `exit 2` だけがブロックし、`exit 0` は許可、**それ以外の非ゼロ（Node が未捕捉例外で返す 1 を含む）はコマンドをそのまま実行する**。しかも Node は未捕捉例外時に `process.exitCode` を無視して 1 を返す。この 2 つを合わせると「fail-closed に倒す」の実体は、経路ごとの `if` ではなく**既定を 2 に置き、許可が確定した経路だけが 0 を書く**という構造になる。payload 破損・`command` 非文字列・git 不在・timeout・内部例外が、書かなくても全て安全側へ倒れた。強制の階梯を 2 段上げた形であり、`docs/development-principles.md` に原則 7 として抽出した。

### 独立再導出が、grep では原理的に到達できない参照を回収した
最重要ルール 2 を削除すると番号が繰り上がり、`最重要ルール 4` を**番号で**参照する OPEN issue 5 件が静かに腐る。`gh pr create` をいくら grep してもこれには辿り着かない——参照が概念ラベルだからである。計画を渡された 2 体のレビュアーは 1 件も挙げず、計画を見せなかった独立再導出の 1 体だけが挙げた。前サイクルで実証された「作者の分解に anchor したレビューは作者の盲点を継承する」が、別のクラスの盲点でも成立した。処置は番号→名前参照への書き換えで、**今回直すだけでなく再発を止めた**。

### 安全網のミューテーションテストに、稼働中の安全網を切る必要は無かった
既定 `exitCode = 2` を `0` へ反転する編集を permission classifier が拒否した（自分の hook を fail-open に弱める変更として正しい判断）。迂回はせず、しかし目的も諦めなかった——カナリアはソース文字列に対する述語なので、**ソースをメモリ上でコピーして 3 通りの変異（既定反転・順序入替・`process.exit` 導入）を当て**、全て捕まえることを実証した。「テストのために本番の安全網を切る」は最初から必要無い。

---

## 伸びしろ

### 計画の中核が、一度も実行されないまま計画書に載っていた
`hasSafeChain` の擬似コードは、この issue の目的である `git push -u origin HEAD && gh pr create` を **block** し、逆に `git push … && npm test; gh pr create` を **allow**（fail-open）していた。単一原因——検出正規表現が先頭の区切り文字を消費し、`index` が `gh` トークンではなく `&` を指す。`node -e` で 1 分測れば分かることを、計画段階では一度も測らなかった。`/plan-review` が拾ったから助かったが、拾われなければ「D3 を直す PR が D3 を直していない」という失敗様式で出荷していた。**計画に判定ロジックを書いたら、実装前にそれを実行する。** → AGENTS.md「事前調査」に追記済み。

### サブエージェントの実測を、一次証拠として受け取りかけた
`/plan-review` の 1 体が「原案の正規表現でも `gh --repo o/r pr create` は一致する」と、生の出力を添えて報告した。自分で測ると**不一致**——つまり原案は過小検出（fail-open）だった。もし報告を信じていれば「軽微な懸念」として流し、穴を残したまま出荷している。前サイクルでも「exit 0 の stdout はエージェントに届く」というドキュメント引用が誤っていた。**2 サイクル連続で同じ失敗様式**であり、偶然ではない。委譲は探索を広げるが、判定の中核を測る責務までは委譲できない。→ AGENTS.md に追記済み。

### プローブに選んだコマンドが、そのまま安全網の穴だった
故障注入 V4 に `git push --dry-run && gh pr create --help` を使った。副作用が無いから選んだのに、`--dry-run` は何も送信しないのだから「push が先行するから安全」という判定は誤りである。`code-reviewer` が Low として指摘するまで気づかなかった。**動詞の名前を見て意味を見ていなかった。** 実装は `hasSafeChain` から `--dry-run` / `-n` を除外して修正したが、より深い教訓は「安全網を試すために作った入力は、安全網の入力空間の一部でもある」ということ。プローブの設計時に、それが仕様上どう判定されるべきかを先に書いておけば、その場で気づけた。

### 検出器を守るために書いたカナリアが、検出器と同じバグを踏んだ
`process.exit()` の使用を禁じるカナリアが、**コメント文中の `process.exit()`** に反応して赤くなった。「文字列として現れること」と「呼び出されていること」の混同——今まさに hook で根治していたバグの、完全な相似形である。同型の誤爆は前サイクルの `config-warn`（ファイル内容中の `config.toml` に反応）にもあった。検出器を書くたびに同じ罠を踏んでいる。→ `docs/development-principles.md` 原則 6 に「判定単位は構文的位置で定義する」として明文化した。
