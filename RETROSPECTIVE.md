# Retrospective — #1210 スタートアップ登録を設定アプリのチェックボックスで行う

## よかったこと

### 状態の所有を OS へ置いたら、守るべき面が足されるのではなく消えた

`Config` にフィールドを足す素直な案を却下し、レジストリを正本にした。結果、`/persistence-check` の 4 点セット・`migrate.rs`・serde 既定値・バックアップ汚染の 4 面が**丸ごと消えた**。即時適用が既存機構と衝突しないのも偶然ではなく、`SECTION_TABLE` の型が `Config` の外を表現できないという構造の帰結である。**上位集合になる案は、一貫性を得て面を 4 つ背負う。**

### 変異注入が「全部緑」の内側で 2 つの穴を暴いた

`command_line_for` から引用符を外しても clippy は exit 0（`useless_format` は `Path::display()` に発火しない）。`REG_SZ` の `cbData` を要素数にする変異は cargo test / clippy / doc / governance の**すべてを通り抜けた**。後者には純粋関数のテストを新設し、変異で落ちることまで測った。**緑は「壊れていない」の証拠ではない。**

### 独立した枠組みが同じ結論へ別々に到達した

計画の独立導出（`workspace/` を読ませない枠）は中核 3 判断へ独立到達し、`/simplify` の Reuse と Simplification は同じ重複を別々に指した。**枠組みが違って結論が同じなら、その結論は枠組みの盲点ではない。** 意図的分離 3 件を ADR の所在つきで先に渡したところ、4 枠とも誤検出ゼロだった。

### 却下理由の失効を実測で捕まえた

`StartupApproved` へ追随しない理由は「形式を実測できない」だったが、目視 6 でその形式が測れた。**結論は変わらないのに根拠が半分死ぬ**形で、ユーザーの「そこは利用者の領分」という理由へ差し替えた。ADR に「却下理由が実測で覆った実例」として節ごと残してある。

---

## 伸びしろ

### 偽の全称が 5 回再生した——止まったのは毎回、述語を狭めたときだけ

`SettingsApp::new` の doc に「実 OS を読む地点は `run` 1 箇所」と書いたが、同じ関数内で `detect_opener_presets()` が PATH を走査していた。数え直しでは止まらず、主張を「スタートアップ登録を読むのは」へ狭めて初めて止まった。`AGENTS.md`「検証の作法（全タスク共通）」の代理条件の条項を**自分で引きながら**踏んでいる。

### `git grep` が追跡ファイルしか見ず、新規 3 枚が母集団から落ちた

`/symmetric-check` の母集団を作るとき、未コミットの新規 `.rs` が丸ごと飛ばされた。返ったのは 0 件ではなく「既存ファイルだけの一覧」で、成功した検索と見分けが付かない。`docs/development-principles.md`「列挙の完全性」へ配置した。

### 目視を共有モジュールで組まず、画面ロック検出の外へ出た

`scripts/lib/SnotraSmoke.psm1` の `Start-SnotraProcess` / `Send-SnotraKey` / `Set-SnotraForegroundWindow` はいずれも汎用の引数を取り、設定アプリにも使えた。にもかかわらず自前の `SendKeys` + P/Invoke で組み、`docs/build-commands.md`「エージェントが目視項目を自分で実施するとき」が名指しで禁じている経路を通った。**規範は在り、読んだうえで踏んだ。**

### 計画に「PR 本文に残す」を置いて循環を作った

`/start-issue` は「作業項目に置くのは計画が所有する作業だけ——コミット以降を置かない」と明示している。それを破ったため、目視は完了しているのに `gh pr create` が塞がれる状態になった。**境界はコミットより手前かどうか**であり、PR の存在を要する項目は計画が所有できない。
