# Retrospective — #838 / #921（SPEC の打鍵と選択を §4.9 へ一本化・PR #920 / #922 / #924）

## よかったこと

### fix-forward の再実行が 2 回とも新しい誤りを捕まえた

`AGENTS.md`「条件別チェック」の「レビュー指摘へ修正を当てたら、指摘を出した枠組みを修正差分にも再実行する」を #920 と #922 の両方で実行した。#922 では**新規 High** が出た——「コミット → push」を実施前に `- [x]` にしており、`.claude/hooks/pre-bash.mjs` の #749 ガード（未チェック項目があると `gh pr create` を拒む）を自分で解除していた。1 巡で閉じていれば、実装を 1 行も含まない PR を立てられる状態のまま進んでいた。

### 人間裁定に触れる指摘を、実装で覆さずに差し戻した

`code-reviewer` が「§6.3 に §4.9 への参照を残せ」と指摘したが、着手前に人間が同じ案を却下していた。「行を消す」の意か「§6.3 に選択の話を置かない」の意かが裁定文から決まらないため保留し、差し戻した。再検査でレビュー側も「決まらないものを決まったことにするより差し戻す方が正しい」と同意した。

### flake の切り分けを状況証拠で済ませなかった

`smoke-egui` が 1 度 fail したとき、差分 0 行・12 run で初の失敗・事象自体は発生済み（trace は 38.5 秒後に出た）という 3 点が揃っていた。それでも再実行して緑を確認した——**揃っているのは全部間接証拠**で、同じ入力で緑が出ることだけが直接の証拠になる。

---

## 伸びしろ

### 「挙動不変」の証拠式が、対象を 1 つも含まないまま自明に green だった

計画に `git diff --name-only main...HEAD` と書いた。`/implement` は**未コミットで検証する**ため、3 点形も 2 点形（`..`・空白区切り）も commit 同士の比較で作業ツリーを見ず、`src-tauri` の差分を **0 行**と報告する。実際に回したのは引数 1 個の形だったので測定自体は正しかったが、**計画の文面は何も測らずに合格する式**だった。`AGENTS.md`「検証の作法」の「計画に書いた判定ロジック…は実行して測る」へ検証コマンドを含める形で括り付けた。

### 修正の呼称がずれ、同じ誤りの再発経路を作った

上を直したあと「2 点形へ直した」と書いたが、`git diff main` は 2 点形ではなく**引数 1 個の形**である。次に誰かが「2 点形なら `main..HEAD` が正確だ」と善意で揃えれば元の誤りが丸ごと戻る。**修正の説明で使う語が実体とずれていないかは、修正そのものとは別に検算が要る。**

### grep の出力を切り詰めて、参照 1 件を取りこぼした

「移動で偽になる散文」を洗い出す grep に `head -20` を付け、`view.rs` が移設対象の行を `SPEC §4.8` で引いている箇所を枠外へ落とした。`/plan-review` の独立導出が拾った。`AGENTS.md`「検証の作法」が既に「切り詰めた出力」を撤回の主因として名指ししている——**規範が在っても、網羅を測る場面で自分から切り詰める形で再発する。**

### 腐り検知の錨を、機械が読めない形で書いた

`.rs` に置いた `SPEC §4.9「入力と選択」` は `governance-check.mjs` の `HEADING_REF` にマッチしない（正準形はバッククォート必須）。`/simplify` の altitude レンズだけがこれを見つけ、reuse と simplification は同じ行を「冗長」としか見ていなかった。**書式が機能を持つ**ことは、冗長さのレンズでは見えない。検出器の母集団に `.rs` を足す件は #925 へ送った。
