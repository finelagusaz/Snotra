# Retrospective — fix: LRU アイコンキャッシュサイズを top_n_history に統一

## よかったこと

### 3回の計画レビューで根本的な設計欠陥を事前に排除
plan v1 → v2 → v3 と外部レビュー・`/plan-review` を組み合わせて3回の反復を経た。v1 では「get() 保護ループが Blob URL revoke 後には機能しない」、v2 では「setMaxSize だけでは壊れた img src が残る」という根本的欠陥が実装前に発見された。実装フェーズでの手戻りがゼロだったのはこの事前検証の成果。

### 問題の根本原因を「パラメータの乖離」に正しく帰着
当初はキャッシュアルゴリズムの改善（優先度付き eviction、visible 保護ループ等）を検討したが、「バックエンドが返す件数とフロントエンドのキャッシュ容量が独立パラメータになっていること」が根本原因だと特定し、`top_n_history` に統一する最もシンプルな解決策を採用した。16×16 PNG のメモリコスト分析（200件でも 40〜400 KB）が判断の裏付けになった。

### 既存パターンの再利用で一貫性を維持
`max-results-changed` イベントの検出・発火パターン、`iconsEnabled=false` 時の `revokeAll + iconCacheVersion bump` パターンをそのまま踏襲した。新規パターンを導入せず、コードベースの一貫性を保てた。

---

## 伸びしろ

### 計画の反復に時間がかかった
3回のレビュー反復は正しい判断だったが、v1 の「cache < results で visible が evict される」問題は、CLAUDE.md の既存ルール「件数・上限パラメータの変更は下流全段に追う」を最初の計画段階で適用していれば v1 で気づけた可能性がある。ルールの適用タイミングを「実装時」だけでなく「計画策定時」にも意識する必要がある。

### LruIconCache の元の設計意図が不明瞭だった
固定値 100 が「なぜ 100 なのか」の根拠がコードにもコミットメッセージにも残っていなかった。今回の修正で `top_n_history` との関連を明確にしたが、パラメータの根拠を設計時に記録する習慣があれば、乖離の発生自体を防げた。

---

## ネクストアクション

- [ ] issue #245 対応: インデックス再構築中に `indexing()` signal が正しく true になっているか検証し、なっていなければ修正する
- [ ] issue #281 対応: Rust 側の `get_search_placement` コマンドハンドラの削除（フロントエンドから未使用）
