# Retrospective — 窓の幾何の継ぎ目 2 を塞ぎ、継ぎ目 4 の決着を記録した（#878 / PR #1171）

## よかったこと

### 「あるべき姿」を裁定則 1 文に落としたら、母集団の分類が機械的になった

OS 幾何の読み戻し 7 箇所へ「**コードが持っていない量だけ読んでよい**」を当てると、違反は 1 件だけだと確定した。issue が「継ぎ目 2 と 4 が残る」と書いていた前提のうち、4 が既に閉じていることも同じ則で示せた。**症状ではなく形を扱う issue ほど、則を先に決めると射程が縮む。**

### 変異の形を先に書いたら、検出器の射程のずれが実装前に見つかった

計画の検出器は「クランプが動いたか」だったが、注入する変異を書き下ろす段で**窓が作業領域の内側にいると沈黙する**と分かった。守りたい退行は導出の誤りで、クランプは境界越えでしか座標を変えない。位置に依存しない矩形の突き合わせへ替えた（`.claude/rules/safety-nets.md`「検出器のカバー範囲は、欠落のパターンごとに検算する」へ追記）。

### A/B の一致を、対照 2 種で較正してから受け取った

位置が変わらないことを実測したが、**一致だけでは計器が何も測っていない場合と区別が付かない**。非クライアント分を落とす／二重に足す 2 変異で ±5px・−10px が出ることを先に示し、その感度の下での一致として記録した（`docs/adr/ADR-show-path-derives-bar-rect.md`「挙動不変の実測（A/B・2026-08-24）」）。

---

## 伸びしろ

### 規範が write 時に効かず、review 時に回収された（4 件・うち同型は 2 件）

検出器の死角の数え上げ（2 つ）と doc の「消費者は 3 つ」が同型で、`docs/comment-guidelines.md`「第一原則: コメントは「なぜ」を書く」が**逐語で挙げている実例**そのものだった（規範自身が「誰も検査しない」と自己診断している）。残り 2 件は別型——全角括弧による intra-doc link の沈黙（→ #1172）と、テストコメントの誤述（→ 同 doc「テストコードのコメント」へ条項を追加）。**4 件ともマージ前にレビューが捕まえており、層としては機能した。** 数え上げの機構化は実測で却下した（`///` の該当 48 件のうち少なくとも 14 件は規範が明示的に許容する形で、例外が意味的ゆえ文字列述語で分離できない）。

### 委譲の中断で、成果物ファイルが 0 バイトだった

API エラーで落ちた枠は検証とレビューを終えていたのに、書き出しを最後に置いていたため会話にしか残っていなかった。**「測る前に、いま持っている分を書け」を指示に入れる**（ルート `CLAUDE.md`「サブエージェント委譲と worktree」へ追記）。名前付きで `SendMessage` できたことが回収の唯一の経路だった。

### 指摘 0 件の報告を、対象の照合なしに受け取りかけた

`/code-review` が別の作業コピーを見て「差分なし・指摘 0」を返した。**成功したレビューと出力が同じ**であり、気づけたのは結論ではなく報告が名乗ったブランチ名を自分の状態と突き合わせたからだった。対象を引数で明示すれば構造的に消える曖昧さである。
