# Retrospective — 検索結果スクロール対応 (#191)

## よかったこと

### ユーザーとの対話で計画の誤りを早期修正できた
当初 `SCROLL_FETCH_LIMIT = 100` というハードコード定数を計画したが、ユーザーの指摘（`top_n_history` が本来の検索結果上限として設計された値）を受けてすぐに計画を更新できた。YAGNI に沿った実装になり、新規フィールド追加を避けられた。

### 調査 → 設計対話 → 実装のフローが機能した
アイコンもたつきの多角的調査後、パイプライン全体の整理・2 段階化の効果・ユーザー設定による調整の運用をユーザーと対話して合意を形成してから実装に進んだ。過剰な自動最適化を避け、シンプルな実装で知覚的改善を達成した。

### コードレビューが High 発見事項を検出できた
`cachedMaxResults` の await 間ローカルキャプチャ漏れ（Stage 1/2 のスライス境界ずれリスク）をレビューエージェントが発見し、コミット前に 1 行の修正で対処できた。

---

## 伸びしろ

### 設定値の設計意図を調査段階で掴めなかった
`top_n_history` が検索結果上限として設計された値だという情報を調査で見落とした。「既存設定値に同等の役割があるか」を調査チェックに含めていれば、`SCROLL_FETCH_LIMIT` 定数を計画する迂回を避けられた。

### 件数変更の下流影響（アイコン取得）を plan.md に含めなかった
`engine.rs` の返却件数を 8 → 200 に変更する計画で、「その 200 件のアイコンが全件リクエストされる」という下流への連鎖を影響範囲に含めていなかった。アイコン 2 段階化は事後の追加実装になった。件数・上限系のパラメータ変更は下流全段を計画時に追うべきだった。

---

## ネクストアクション

- [ ] PR を作成してマージする
- [ ] issue #192（`extract_png` rayon 並列化）の実装を検討する
