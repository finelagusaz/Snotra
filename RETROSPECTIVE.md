# Retrospective — #755 / #801（show が「これから描く高さ」を導出する・PR #904）

## よかったこと

### 実測が設計の分岐を 2 か所で決めた

「両症状は 1 回の show では排他」を実測できたことが、そのまま検出器の設計条件になった——1 回目だけ見る検査は #801 の分岐しか踏まず #755 を永久に見逃す。「memo は #801 の原因ではなく抑制器」と分かったことで、#755 本文の修正案が #801 を製造すると判定でき、両方を解く道へ進めた。**issue 本文の記述を疑って測ったことが、両方の起点である。**

### 検出器を修正より前に置き、赤を受け入れ条件にした

Task 3 の完了時点で `smoke:egui` が赤になることを受け入れ条件に据えた。人間の裁定で「計画が優先」と決めてからレビュアーへ渡したので、赤いテストのコミットが欠陥として差し戻される往復が起きなかった。

### 表示仕様を先に確定させたら、修正が仕様の帰結になった

「通知は show の後から届く」という指摘から、守るべき不変条件が「show と最初のフレームの高さが一致すること」ではなく**「高さの変化は行の出没と 1 対 1」**だと分かった。3 つの選択（上端固定・下端はみ出し容認・即座）が互いを支え、位置クランプの修正が**バグ潰しではなく仕様の帰結**になった。

---

## 伸びしろ

### 「変化の向きが正しい」で裁定を止めた

最終レビューの警告項目を「実高でクランプするほうが正しい」で通した。誤りは**変わった値の消費者を辿らなかった**こと——hide が現在位置を永続化するため、ユーザーの選んだバー位置が恒久的にずれていた。`/code-review` が捕らえ、実測で裏づいた。`AGENTS.md`「条件別チェック（トリガー → 参照先）」の該当行へ括り付けた。

### 概念ラベルで書かれた根拠が 4 回偽になった

いずれも結論は真のまま**根拠だけ**が腐り、シンボル名の grep に掛からなかった。しかも 3 例目を潰した修正が 4 例目を生んだ——1 つのラベルを消すと同じ段落の別のラベルが diff の context 行に沈み、差分レビューが「訂正済み」と判定する。探し方（概念語の grep）と、訂正した段落は現物を読み直すことを `docs/development-principles.md`「ドキュメントとコードの分担」へ足した。

### 安全機構を入れた本人には、それが隠す退行が見えなかった

fail-safe（memo リセット）は show 側の導出が退行しても 1 フレームで補正するため、**外から測る検査を無力化する**。独立レビューの指摘で初めて測り、故障注入で「外部の矩形測定は正しい値を返すのに in-process の信号だけが鳴る」ことを確認した。`.claude/rules/safety-nets.md`「検出器のカバー範囲は、欠落のパターンごとに検算する」へ足した。

### 同じ枠組みのレビューを重ねても、枠組みの外は見えなかった

サブエージェント駆動の 5 タスクすべてがレビュー clean で、全ブランチの最終レビューも修正の波を経て clean になった。その状態で `/code-review` が実在の欠陥を 10 件出し、うち 2 件は挙動の後退だった。**レビューの回数ではなく、レビューの枠組みの数が効く。**
