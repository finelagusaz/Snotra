# Retrospective — #1246 Opus 5.5 向け prompt-audit・#1026 起動計器の終端を 1 か所から導く

## よかったこと

### 敵対的調査の「測定環境を疑え」が、計画の前提を 1 つ入れ替えた

`/start-issue`「3b. 敵対的調査（サブエージェント 1 体・sonnet）」の枠が、ハーネスの整合検査は `continue-on-error` の段でしか実バイナリに当たらず、**PR を止めていない**ことを読みに行って見つけた。これで「写しの一致を外から監視していた」は「PR 時点では誰も監視していなかった」に変わり、新しい単体テストの位置づけが「重複の置き換え」から「最初の検知器」へ変わった。計画とコードだけを読む枠では届かない場所であった。

### 検知器の到達範囲を、推論ではなく注入で決めた

「どの退行をどの検知器が捕まえるか」という主張は、委譲先が変異を注入して実測した。その結果、`finish` 側の退行は形によって捕まえる層が違うと判明した。名前を捨て忘れる形は clippy が止め、`_` で捨てる形は `bench-startup` のハーネスだけが捕まえる。**推論で書いた版は 2 度とも偽だった。**

### 二度監査した面での最大の所見は、モデル世代ではなく取り残された契約だった

prompt-audit で最も重かった所見は、Opus 5.5 向けの癖ではなかった。`code-reviewer` が今も `git diff HEAD` で差分を取っていたことである。起動経路は #1137 でコミット済み sha に変わっており、この取り方では差分が空になる。仕組みの側が動いたのに、プロンプトの側が追随していなかった。

---

## 伸びしろ

### コメントの全称を、修正のたびに 1 つずつ再生した

ハーネスの説明文は 4 巡の検証でそのたびに言い過ぎを指摘され、そのたびに直した。「その match の誤りだけ」「ここにしか届かない」「ここへ現れる」「成功経路だけ」の 4 つである。どれも**検知器の到達範囲を測る前に文へ書いた**のが原因で、規範（`AGENTS.md`「検証の作法（全タスク共通）」の全称表現、`.claude/rules/safety-nets.md`「検出器のカバー範囲は、欠落のパターンごとに検算する」）は既にこれを言っていた。到達範囲を書くときは、先に注入して測る。

### スキル間の写しが 2 か所でずれていた

1 つ目は、計画が変異注入を主エージェントに割り当てたことである。`/implement` は主エージェントが同じ木へ注入することを禁じている。2 つ目は、`/retrospective` が health-check の実行項目を写しており、Check 11 が落ちていたことである。観測点の表には、観測の無いサイクル末が 2 つ挟まっていた。前者は `/start-issue` に注記を足し、後者は写しをやめて正本を指す形へ直した。
