# Retrospective — シェル環境・git 操作の規範 5 件を PreToolUse へ機構化（#768 / PR #774・#772 の検証を同時に取得）

## よかったこと

### 計画へ書く前に判定を測ったので、計画に嘘が入らなかった

`AGENTS.md`「検証の作法（全タスク共通）」の「判定の中核は自分で測る」を、plan.md を書く**前**に実行した（代表入力 49 件 + 病的入力 130 呼び出し）。結果として計画に載った正規表現は最初から実測値であり、レビューが覆したのは**設計判断**（走査戦略・注入面）だけで、書いた式そのものは覆らなかった。測る順番が「計画の後」なら、覆るのは式でも設計でもなく計画全体になっていた。

### `/plan-review`「Step 2b — 独立導出 + 差分（常に実施・盲点クラスの漏れ検出）」が 4 サイクル連続で当てた

今回の漏れは `post-edit.mjs` が会話へ出す再現コマンドが、この PR が新設する `\` 判定に当たるというもの（実測 3/3 block）。**片方の hook が指示するコマンドをもう片方が拒む**状態で、私の「触る範囲」には最後まで現れなかった。#495 が「局所的か」を判定するのは盲点を持つ当人だとして常時実施へ変えた判断が、また正しかった。

### 規範を機構へ 2 段階で降ろせた

5 件を `judgeCommandShape` の判定へ移して常時ロードを −762 字にしたうえで、`/norm-review` が出した指摘 1 件をさらに**テストへ**落とした（拒否文言に `$env:X` が `/` 以外を伴って現れたら赤）。`docs/development-principles.md`「構造的設計原則と強制の階梯」を 2 段上げた形。**そのカナリア自身が最初バグっていた**（`\w+` の貪欲さがバックトラックして正しい文言でも赤）ので、複製への変異注入で 6 ケース測ってから採った。

---

## 伸びしろ

### 「測って出なかった」を不在の証拠にしかけた

レビューが報告した正規表現の指数爆発を自分で測り直したとき、`git log --f0=v0 …` という入力を使って 0.0ms を得て「問題なし」と判断しかけた。`log` が非フラグなので量指定子が 0 回で即失敗し、**バックトラックが起きない入力**だった。フラグを `git` の直後に並べて初めて 747ms が出た。義務（自分で測る）は果たしたのに結論を誤る形であり、`docs/development-principles.md`「デバッグ・バグ修正」へ裏側の則として括り付けた。

### 塞ぎに機構への言及を入れて、読者を「何が検出されるか」へ誘導した

1 巡目の修正で「`\` はこの hook の**別判定**に当たります」と書いたところ、2 巡目に両クラスの読者が独立に「別判定に当たらない `\` を選べばよい」と読んだ。**要件ではなく検出範囲を教えると、読者は検出範囲を最適化する。** `/norm-review`「Step 3 — 成立した指摘だけを塞ぐ」の「列挙ではなく原理で書けないかを先に問う」の同族であり、条項化の是非は #775 で合意を取る。

### 起動した指摘の全件評定が担保されていなかった

1 巡目の「手を抜く読者」6 件のうち 3 件に評定を付けず、報告にも残らなかった。**うち 1 件は成立していた**（#772 の検証で発見・`9feb6c2` で修正）。巡数と分量は様式が守らせるのに、全件評定だけが実行者の誠実さに委ねられている——降格した指摘は「指摘なし」と見分けがつかない、という #489 と同型の沈黙経路である。突き合わせを様式へ置く案は #775。
