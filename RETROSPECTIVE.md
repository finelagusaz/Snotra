# Retrospective — UI パフォーマンス最適化調査・実装

## よかったこと

### 3並列エージェント探索で網羅的な調査ができた
レンダリング・ビルド/アセット・IPC の3軸でコードベースを並列探索し、13件の課題を特定した。各エージェントの専門視点が重なることで、単一視点では見落としやすい「IPC ループバック」や「CSS contain 未使用」を検出できた。

### 実測で改善効果を定量化できた
インスタントコマンドのデバウンス効果を `__ipcStats` カウンタで Before/After 比較（8回→6回、25%削減）。仮説→計測→検証のサイクルを回せた。

### 調査で不要と判断した項目を明示的に切り捨てた
`fetchedNone` の上限（実は results 変化ごとにクリア）、`iconCacheSize` の分離（メモリ影響が小さい）、ブートストラップ並列化（効果が小さく可読性低下）を根拠付きで見送り、スコープを適切に絞れた。

---

## 伸びしろ

### resizable: false を見落とし、存在しない問題を最適化した
`win.setSize()` の「ドラッグリサイズ中の冗長な IPC」を課題として挙げたが、`tauri.conf.json` で `resizable: false` が設定されておりユーザーによるリサイズは不可能だった。`untrack` + 高さ変化ガードの実装自体は Rust 側 `set_size` からのループバック防止として有効だが、当初の課題設定が不正確だった。Tauri のウィンドウ設定（`tauri.conf.json`）を調査初期に確認すべきだった。

### 計測基盤が一時的だった
`__ipcStats` カウンタは計測後に除去した。開発中に繰り返し使える IPC 計測基盤が `perf.ts` のように常設されていれば、今後の最適化サイクルでも即座に Before/After 比較ができる。

---

## ネクストアクション

- [ ] PR #307 の手動テスト項目を完了する（通常検索・フォルダ展開・ツール選択・IC モード・ウィンドウ高さ変更）
- [ ] DEV 限定の IPC カウンタ基盤を `perf.ts` または `invoke.ts` に常設するか検討する（Issue 起票候補）
