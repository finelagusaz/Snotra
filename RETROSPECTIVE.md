# Retrospective — workspace lints の opt-in 漏れを機構で塞ぐ（#713・PR #820）

## よかったこと

### 述語を実装前に実データへ当てて、false green を 1 件潰した

`AGENTS.md`「検証の作法（全タスク共通）」の「判定の中核は自分で測る」がそのまま効いた。ルート側の述語は 2 案を書いて実 `Cargo.toml` を含む 9 ケースで実行し、当初案が「必須 lint の行だけ消える」形を**緑で通す**ことを実装前に発見した。

**文章としては最後まで筋が通っていた案であり、読み返しでは捕まらなかった。**

### 独立導出が、issue が定義した穴の外側を見つけた

`/plan-review`「Step 2b — 独立導出 + 差分（常に実施・盲点クラスの漏れ検出）」が、#713 の本文が「member 側の opt-in 漏れ」と定義した問題に対し、**ルート側にも同じ結果をもたらす沈黙経路がある**ことを独立に示した。主エージェントが cargo で再実測して 2 本を確認し（1 本は反証）、裁定を経て射程に入れた。

### 沈黙しない経路を意図的に射程外へ置いた

cargo が manifest エラーにする 3 形（root テーブルの消失・`workspace = false`・lint 併記）を実測で切り分け、検査を置かないと決めてコメントに残した。「カナリアで守るのは沈黙する経路だけでよい」が、費用ではなく**射程の設計**として働いた。

---

## 伸びしろ

### レビュー後に育った計画は、どのレビューも受けていない

`/plan-review` は 4 体全件が成立して着地したが、その後の裁定で findings クラスを 1 つ丸ごと追加した。**不成立ゼロゆえ `/start-issue`「5b. セルフレビュー（plan-review 固有の補完）」の免除条項は発火せず**、追加分だけが無検証で `/implement` へ渡りかけた（実際にその部分に欠陥があった）。手順の提案は #821。

### 全称条件だけの検査を書きかけた

「非空かつ全エントリが deny」は ∀ 条件であり、**要素が消える方向に空振りする**。守りたい識別子の名指し（∃）を組にして初めて閉じる。教訓は `docs/development-principles.md`「構造的設計原則と強制の階梯」の項 8 へ括り付けた。

### 実装レビューの Low 指摘が「散文の主張が実測より強い」に集中した

`code-reviewer` の Low 6 件のうち 4 件が、コメントの数え違い（5 経路 ↔ 6 形）・事実誤り（「警告も出さず無視する」が実際は警告が出る）・前提を欠いた全称表現だった。**実測した本人が書いた散文でも、測り直すと外れる。**
