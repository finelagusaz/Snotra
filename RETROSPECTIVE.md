# Retrospective — 束 A: アイコン欠落から見た目の規範化と適用まで（PR #716 / #717 / #719 / #721）

## よかったこと

### 規範を先に置き、後続を「判断」から「適用」へ落とした

サイクルの順序を #670（規範の仕様化）→ #672 / #654（その適用）としたのは、後続が**揃える先を参照できる状態を先に作る**ためだった。効果は PR の性格に現れている——#719 は「これは判断ではなく規範の適用である」と書いて始められ、固定値 3 つを消す作業に「何 px が正しいか」の議論が要らなかった。#654 も同様に、`SPEC.md`「見た目の規範」の「色は config `[visual]` から取る」が実装判断の根拠になった。

規範を先に書くコストは、適用側で**決めなくてよくなる判断の数**として回収される。

### 「原因を決める前に測る」が、2 つの独立した局面で前提を覆した

#628 で確立した刻みが、このサイクルで 2 回とも当たった。

- **#692**: 「アイコンが取れない」の仮説を 3 つ実測で潰し、`SHGetFileInfoW` が**成功を返しながら HICON を返さない**（プロセスごとに冷えたシェルキャッシュの初回要求）に到達した。待ち時間の効果が 0ms と 50ms で同じという実測が、リトライに sleep を入れない設計を決めた
- **#654**: headless egui で galley 幅を実測したところ、**issue 本文の前提と、レビューが述べた懸念の両方が覆った**。issue は入力欄の色差を「カスタムテーマで乖離しうる」としていたが実際は**既定設定で不整合**（egui 既定 `gray(180)` vs config 既定 `#E0E0E0`）。失敗詳細は「1 行トーストに収まらないのでは」と見られていたが可用幅は 532px あり、短い理由は全文入る

**issue を書いたのが過去の自分でも、前提は測るまで前提のままである。**

### 検知手段が無いと分かった時点で、記録ではなく設計を変えた

#654 で 2 回この選択をした。区切り文字の生成責務を呼び出し側（`view.rs` のインライン処理・テスト不能）から文言テーブル関数の中へ移し、「理由が空のときコロンだけ残る」をユニットテストが固定する不変条件に変えた。実 install 失敗は再現手段が無く失敗トーストの描画が**一度も観測できなかった**ため、env ハッチ（`SNOTRA_EGUI_FAKE_UPDATE_FAILED`）を新設して視覚スモークの対象にした。

#719 も同じ形で、**規範そのものを固定するテスト**（`status_size(24) > status_size(15)`）を置いている——固定値へ差し戻すと落ちる。`docs/development-principles.md`「構造的設計原則と強制の階梯」へ配置した。

### 判別可能な観測設計が、巻き添えの不在を証明した

#654 の視覚スモークで `text_color` をマゼンタ・`hint_text_color` を緑と**別々の色**にした。`weak_text_color` の差し替えがトーストへ漏れていれば緑になるはずのところ、マゼンタのままだったことが「漏れていない」の証拠になった。同じ色で試していれば、2 つの経路を区別できないまま「変わっていないように見える」で終わっていた。

`docs/development-principles.md`「デバッグ・バグ修正」が説く A/B ベースラインの、**同一 run 内で 2 経路を分離する**変種である。

---

## 伸びしろ

### 「渡していない」だけを探し、「渡したが無視されている」を見落とした

#654 で `SPEC.md`「見た目の規範」の適用漏れを探したとき、`view.rs` / `results_view.rs` の描画点を全件洗って「色未指定は入力欄の TextEdit 1 箇所のみ」と結論した。この検索は**引数が書かれている箇所を全部「対処済み」に見せる**。実際には hint が `RichText::color()` で色を渡しており、egui 0.35 の TextEdit がそれを `weak_text_color` で**無条件に上書き**していた（egui 自身が "users won't be able to override it" と注記）。

拾ったのは `/plan-review`「Step 2b」の独立再導出だった。**枠組みが違えば見えるものが違う**という Step 2b の前提そのものの実例で、#628 の `wake_results`（同概念・別名）に続いて 2 サイクル連続で独立再導出だけが検出者になっている。`docs/development-principles.md`「デバッグ・バグ修正」へ配置した。

### 受け皿を確認せずに「別の束が拾う」と書いた

#654 の計画に、`SPEC.md` §20.3 の腐りを「束 C（#674 / #698）の対象として残す」と書いた。両 issue の本文を読むと**どちらも §20.3 を名指ししていない**——#674 は §4.8 の CSS `:hover` など 3 項目、#698 は `code-reviewer.md` の SolidJS 記述である。そのまま出せば、**#654 自身が「コードコメントだけの deferred は消えやすいため issue 化する」という理由で起票された issue でありながら、その失敗様態を再生産していた。**

**送り先が名指しされていない deferred は、deferred ではなく脱落である。** `/plan-review`「Step 2 — 並列サブエージェントで検証」のスコープ観点へ配置した（#674 へはコメントで項目を追加済み）。

### 規範に条項を足すとき、検算の母集団を自分が見ていた範囲で取った

#654 で §11 に「色の指定を省いてライブラリ既定に委ねない」を足そうとしたとき、検算を `view.rs` / `results_view.rs` にしか当てていなかった。§11 にはスコープ宣言が無く、`snotra-settings`（色リテラルのデザイントークンを持ち `ui.label` 等を 60 件以上色未指定で描く）まで射程に入る。つまり**既存条項「描画コードに色リテラルを書かない」の時点で既に偽**であり、そこへ条項を足せば偽を 1 つ増やすだけだった。

`AGENTS.md`「検証の作法」は「新設した規約は、既存の全事例に当てて検算してから書く」と既に述べている——**規範の不足ではなく、母集団の取り方の誤りである**。母集団は自分が見ていた範囲ではなく、規範が名乗る範囲で取る。`.claude/rules/safety-nets.md` へ配置した。

### 索引の要約を信じかけ、本文の腐りに気づかなかった

サイクル末の鮮度チェックで、視覚検証に関するメモリが**全体として #532 SU7 で撤去された WebView2 フロントの話**だったことが判明した（`ui/` 不在・`typedoc.json` 不在・`npm run tauri dev` の vite 依存も消滅）。索引の一行要約は「Tauri dev は vite 依存」と読めており、**そこだけ見れば通過していた**。捕まえたのは、SU7 で消えた語（`vite` / `ui/src` / `SolidJS`）で**本文を grep** したからである。

`.claude/skills/health-check/SKILL.md`「Check 7 — MEMORY.md 参照の実在性」の 7d が禁じている「`description` で関連性を判断する」の実例が、また 1 件出た。**要約は本文の派生であり、本文が腐っても要約は正しいまま残りうる。**
