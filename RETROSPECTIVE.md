# Retrospective — #660 `snotra-egui-mvp`（#532 Phase 1 検証 crate）の撤去

## よかったこと

### 「どの機構も見ない範囲」を先に測ったことが、計画の骨になった

撤去対象は**被依存ゼロの leaf bin crate** で、通常最強の検知器＝コンパイラがほぼ全面的に盲になる。着手前に参照を全数列挙し（glob 制限なしの単一 grep を母集団とする）、そのうち `governance:check` が拾うのは 5 箇所だけ（G3 が 2 件・G5 が 3 件）と見積もった。残りは**件数の写し 3 件・散文の表記ゆれ・見出し・doc コメント内の疑似パス**で、機構は一切見ない。

この見積もりがあったから「手の列挙が主担保である」と自覚でき、最終 grep を完了条件に据え、`pub fn` の死が `dead_code` を発火させないこと（＝今撤去しなければ二度と浮上しない）を根拠に `close_window` を同一 PR に含める判断ができた。**盲域の大きさを測ることは、計画の精度ではなく計画の構造を決める。**

### フォールトインジェクションが「自然な中間状態」で無料になった

`.claude/rules/safety-nets.md` は「効いていることを一度は実測する」を要求するが、稼働中のガードを弱めてはならない。撤去タスクではこれが両立する——**crate を消して文書を直す前の中間状態**で `governance:check` を走らせると、予測どおり 5 件が `file:line` 付きで挙がる。検査そのものは無変更ゆえ、これは弱化ではなく**ガードの行使**である。撤去の順序が、そのままフォールトインジェクションの手順になった。

### 既存機構が「何を直すべきか」を名指しした

`Cargo.toml` の `members` を編集した瞬間、`post-edit.test.mjs` の members カナリアが発火し、失敗メッセージが更新先 4 箇所（`selectChecks` / `buildCommand` の case / `ci.yml` / `docs/build-commands.md`）を名指しした。それを 1:1 で消化したことが、そのまま完備性の論証になった。**注意書きではなく機構に書かれた教訓は、忘れても機能する。**

### 削除の前に不変条件を棚卸ししたので、回復不能な喪失が起きなかった

消える `CLAUDE.md` の不変条件 9 件を 1 行ずつ「crate と共に消える／既に他所でカバー／移設が必要」に分類し、孤立していた 1 件（warm frame の日次比較禁止）だけを `PERFORMANCE.md` へ移した。7 件は製品コード・`SPEC.md`・`tauri.conf.json` に実在することを `file:line` で確認した。**ファイルが消えた後では、この棚卸しはできない。**

---

## 伸びしろ

### 消す変更の中で「新しく書いた 1 文」を測らなかった

crate 撤去に合わせて既存の注意書きを補い、「`cargo run`（`-p` 欠落）は別 bin を起動しうる」と書いた。実測はルートで exit 101 のエラー（何も起動しない）で、主張は誤りだった。code-reviewer が一時領域に 2 bin の仮想ワークスペースを作って反証し、こちらでも再実測した。

原因は**削除の diff では「消した」ことに注意が向き、書き足した主張が検算対象だと認識されない**こと。残す記述に手を入れた瞬間、それは削除ではなく新規記述である。`docs/development-principles.md`「撤去（消す変更）の作法」へ昇格した。

### 計画に自分で書いた注意を、実装で自分が破った

教訓の移設先について、計画レビューの文書層偵察が「実装時に #399 の原型を地の文で語り直すと重複になる」と予告し、`plan.md` にも「参照に留める」と書いた。にもかかわらず新設節は前提と受容残余を語り直し、**同一事実の 4 箇所目**を作った（code-reviewer が検出）。

移設は貼り付けではなく**差分の抽出**である——移す前に既存の正本を grep し、その場所にしか無い部分だけを移す。同じ節へ昇格した。

### 「範囲外」の判断を 2 度覆した

`docs/build-commands.md` の「Surface/Device復旧方針」（実装に存在しない挙動）と、runtime 自身を「MVP runtime」と自称するログ文言を、当初は既存事象・スコープ外として置いた。前者は `docs/architecture.md` を正した結果として**自分が作った矛盾**であり、後者は撤去によって**現在形の主張が偽になる**記述だった。どちらも判定基準（「存在しなくなるものへ読者を送るか／現在形の主張が偽になるか」）は持っていたのに適用が漏れ、指摘を受けてから覆した。

スコープ規律は「広げない」だけでなく「**自分の変更が生んだ不整合は範囲内**」の側も持つ。同じ節へ昇格した。

### 引用位置を行番号で書き、書いた時点で既にずれていた

`research.md` / `plan.md` が引用した `view.rs:1651-1694` は実際には `:1697-1756` で、テスト本数も 3 本ではなく 4 本だった（plan-review の 2 体が独立に検出）。`.claude/rules/*` は #588 で「位置は行番号で断定せずシンボル名で grep」と既に記しているが、それはコードを**辿る**ときの規律で、計画文書を**書く**ときには適用されていなかった。撤去の作法の第 1 項に括り付けた。
