# Retrospective — #1222〜#1225 撤去条件の是正・検証カテゴリの穴・依存更新

## よかったこと

### 「案を却下した理由」を実測で覆す形が 2 度続けて効いた

前サイクルは issue の案 A 却下を母集団の拡大で覆し、今回は `fontdb` 0.24 の changelog が `Cargo.toml` の注記の根拠を消していることを見つけた。どちらも**結論ではなく根拠の側が腐る**形で、`cargo tree` や変異注入のような**一次の観測**でしか見えない。依存更新を「lock を上げるだけ」と読まず、上流の changelog が**こちらの文書の前提**を壊していないかを見る一手が効いた。

### レビューの 1 行が 4 行を削った

`ttf-parser` の注記から「なぜ根拠が消えたか」の説明を残そうとしたが、「必要だから L33 に書いてある、とするならこのコメントもいらないのでは？」で不要と判明した。**削除の理由はコミットメッセージが持つので、消した跡に説明を残す必要はない**。`.claude/rules/governance-docs.md`「ガバナンス文書の参照と命名のルール」の「その場限りの事情は書かない」が既に言っていたことで、規範の不足ではなく適用の失敗だった。

---

## 伸びしろ

### 精密に数えることが、何を数えているかを問わない言い訳になった

メタ層の撤去条件を適用したとき、行数ではなくサイクル境界まで突き合わせて「連続 2 回成立」と結論した。**数え方は正しかったが、「0 件」が何を意味するかは一度も問わなかった**——その 0 件は情報を持たない側だった。見つけたのは利用者で、私は規則を丁寧に適用するほど疑わなくなっていた。教訓は `docs/development-principles.md`「規範を書くときの作法」へ配置した。

### 既知の罠を、記録がありながら踏んだ

`gh api -f body="…"` の二重引用符内でバッククォートがコマンド置換として実行され、レビュー返信から sha が 1 語だけ消えた。メモリに「Write ＋ ファイル経由一本」と書いてあるのに、**短い文だからと直書きした**のが入口である。長さではなくバッククォートを含むかで判定する——日本語の技術文書では実質すべて該当する。気づけたのは投稿後に読み直したからで、同じメモリの後半がそのまま効いた。

### 一括更新と Dependabot PR の突き合わせが手順に無かった

`cargo update` は semver 互換の範囲しか上げないので、Dependabot PR は「吸収される」「されない」の 2 群に割れる。今回はどちらも手で `gh pr list` を打って気づいたもので、報告項目には無かった。#1226 として起票し、**同じセッション中に #1228 で `/deps-update` の Step 6 へ足した**（吸収された PR をスキルが閉じないことも明記した——Dependabot が自分で閉じることがあり、主体を増やすと記録から読めなくなるため）。
