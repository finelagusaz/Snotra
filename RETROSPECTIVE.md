# Retrospective — スキルの責務境界（PR #718 / #720 / #725）

`/start-issue` と `/implement` が「計画」を二重に所有していた食い違いを解いたサイクル。あわせて、文書の構造を凍らせていた序数参照を機械照合可能な正準形へ移し、恒久規範の面積指標を行数から文字数へ替えた。

## よかったこと

### 一次資料が、記事より強かった

外部記事の助言（`/implement` のマージ手順を skill 化して常時視界から外す・`/race-check` の防御文言を圧縮する）を 2 件却下できた。どちらも `docs/adr/0002-squash-merge-issue-autoclose.md` と `docs/adr/0003-race-check-predicate-and-norm-hardening.md` に**却下済みとして記録されていた**からである。否定の知識が、着手前の実行者を実際に止めた——ADR 規約が期待どおり働いた実例である。

### 「赤を先に出す」が、規範文書の変更でも成立した

`/implement` の Step 番号を繰り上げると、ルート `CLAUDE.md` からの正準参照が G11 で落ちる。これを計画に「**先に赤を出し、出なければ止まる**」と書いた。赤が出ないなら機構が効いていないことになり、計画の前提が崩れたと分かる——単体テストを持たないドメインでも Red→Green の形が取れた。

### 機構が、導入直後に作者を捕まえた

G11 は導入した当日に、自分を導入した `docs/adr/0004-canonical-heading-references.md` の初稿を 2 度落とした（死んだ節名を正準形で書いた／`/implement` だけ見出しの形が違い着地しなかった）。さらに `PERFORMANCE.md` が指す消滅済みの節という**実在のドリフト 1 件**を初回実行で検出した。人手の列挙規範の下では、これは腐ったまま残っていた。

### 指標の誤りを、指標自身が誘発した誤りで示せた

`docs/adr/0001-doc-minimization-cap-enforcement.md` は「行数が粗い代理指標であることは承知の上で受容した」と書いていた。本サイクルで実際に、`.claude/rules/` を 1 行も減らしていないのに基準を下げるという誤りを行数指標が誘発した。想定上の欠点が実測になったので、`docs/adr/0005-area-metric-characters.md` で文字数へ替えられた。

---

## 伸びしろ

### 規範文書の修正は、それ自体が新しい欠陥の生成過程である

同じ形の失敗を 4 度踏んだ。狭すぎる全称を広げれば広げすぎになり、2 軸を 1 事実へ畳めば前提が落ち、落ちた前提を段落で補えば同じ事実が 2 か所になる。**片側だけを見て直すと、反対側の崖から落ちる。**

ゆえに修正のレビューでは「指摘は解消されたか」だけを問うては足りず、**「この修正が何を持ち込んだか」を毎回独立に問う**必要がある。実際、最終レビュー後の修正が持ち込んだ矛盾（散文が撤回されたはずの主張を保持していた）は、その設問を明示的に入れたレビューだけが見つけた。

### 委譲の非対称に、設計で対応するのが遅れた

実装の成果は git に残るので、報告が届かなくても実在を確認して先へ進める（実際 2 度そうした）。一方レビュー・判定は**会話にしか存在しない**ため、届かなければ実施の有無すら区別できない——6 回中 5 回これを踏んだ。3 度目に構造を変える（ファイルへ書かせてパスを返させる）べきだったが、督促で回ってしまうため 5 度目まで運用で凌いだ。教訓はルート `CLAUDE.md`「サブエージェント委譲と worktree」へ配置した。

### 自分が引用した規範を、自分の書いたものに当てていなかった

`CLAUDE.md`「委譲はコンテキストを継承しない」が定める「検査対象を変更しながら検査を走らせない」をこのサイクルで 3 度引用しながら、レビュー起動後に実装者へ「以降触るな」と伝えず、実際に対象が動いた。**規則を知っていることと、それが要求する指示を出すことは別**である。同型で、`AGENTS.md`「全称表現は前提条件とセットで書く」を援用する ADR の文自身が前提条件を欠き、`governance:check` の保証範囲を過小に述べていた。

規範を引用する文ほど、その規範に照らして検算する必要がある——書いている最中は、自分の文が規範の適用対象だとは思っていない。

### 追加は常に重複の候補である

「前提が抜けているので段落を足せ」という指示が、そのまま「同じ事実が 2 か所」を作った。足す前に「その事実はすでにどこかに書かれていないか」を問えば防げた。**書く側からは補強に見える**ところが厄介で、重複だと分かるのは片方だけが更新された後——つまり事故が起きた後である。だから注意ではなく、構造で 1 か所へ寄せるほかない。

### 「正準形に見える」と「検査される」は違った

バッククォートと `「` の間に助詞が 1 つ挟まるだけで G11 の視界から外れる。計画に書いた 3 件がこの形で、人の目にもレビュー 1 巡目にも通り、実装者が判定関数を直接呼んで測って初めて分かった。しかもその 3 件が支えていたのは入口判定の中核推論だった。機構で吸収できる形なので #727 として起票した。
