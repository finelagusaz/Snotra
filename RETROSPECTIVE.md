# Retrospective — config の読みの規範から条件を無くし、担い手を lint からコンパイラへ移す（#1123 / PR #1127）

## よかったこと

### 「例外を置かない」を評価する枠が、前サイクルの ADR に用意されていた

`ADR-config-read-exception-discriminator` は「却下したのではなく評価していない。#1123 で評価する」と自分で書き残していた。おかげでこのサイクルは代替案の再導出から始めずに済み、**却下理由 3 つのうち 2 つが本決定で偽になった**ことも突き合わせで分かった。凍結 ADR は直さず、事実は新 ADR へ書いた。

### 意図的分離を根拠の所在つきで渡すと、レビュー枠は誤検出しない

`/simplify` の 4 枠へ「重複に見えるが意図的に分けた構造」6 件を根拠つきで先に渡したところ、**4 枠とも 1 件も誤検出しなかった**。ルート `CLAUDE.md`「サブエージェント委譲と worktree」が #872 で記録した手が再現した。

### 機構の乗り換えを両向きで測った

測定済みの機構（lint）を未測定の機構（コンパイラ）へ替えたので、`.claude/rules/safety-nets.md`「効いていることは、フォールトインジェクションで一度は実測する」に従って注入した。**効くこと（`E0599`）だけでなく、迂回（`config_handle().read()`）が今も通ることも同じ場で測った**——後者が「engine 錠越しの読みは書けなくなった」という偽の全称を条項へ書くのを止めた。

---

## 伸びしろ

### 規律の宛先を移すとき、その規律の「なぜ」を据え置いた

read guard 越しの `engine.lock()` の害を「待ちが両方に乗る」＝加算的な遅延と書いていたが、実体は逆順の ABBA デッドロックだった。**遅延なら読者は天秤にかけ、デッドロックなら絶対に避ける**——文は真のままでも拘束力が違う。しかもこの文言は #1032 以来のもので、**契約を 1 か所へ集約した当の改稿で写した**。教訓は `docs/development-principles.md`「ドキュメントとコードの分担（陳腐化の予防）」へ。

### 修正が規則の閉包を壊した

上を直すとき、閉じた形（「読みへ渡すのは純 CPU だけにする」）を開いた禁止 2 件へ替え、名指ししなかった他の錠を規則が禁じなくなった。**露見したのは、触らなかった委譲側が正本より広い契約を宣言していたから**である。教訓は同ファイル「規範を書くときの作法」へ。

### 敵対枠へ渡した命題の語が、その枠の走査範囲を決めた

「弁別子の言い換えを持つ**生きた文書**は 0 件」を `--include=*.md` だけで測って書き、敵対枠も同じ範囲で「壊せなかった」と宣言した。捕まえたのは無関係な確認で打った `--include=*.rs` の grep である。**包含フィルタは除外句と違い「除外した」自覚が残らない。**

### 機構が書き残す撤去条件は、書いた時点の想定でしかない

`clippy.toml` 群 3 の撤去手順には欠陥が 4 つあった（`pub(crate)` では `dead_code` が立って実装が止まる・test fixture が未列挙・**散文の掃除が一切未列挙**・合図が駆動力を持たない）。教訓は `docs/development-principles.md`「撤去（消す変更）の作法」へ、記録は `ADR-config-read-without-exception` へ。
