# Retrospective — #628 可視アイドル再描画の実測決着（PR #709）

## よかったこと

### 計器を先に入れ、原因に名乗らせた

issue は源の候補を 3 つ挙げていたが、どれも推測だった。egui の `Context::repaint_causes()`（`file:line` を返す）を計器化してから測ったことで、**推測では届かなかった真因**（`predicted_dt` の既定値が短い repaint 予約を「即時再描画」へ飽和させ、キャレット点滅の遷移ごとに 26ms スピンする）に到達した。仮説（点滅）を先に実装していたら、点滅を止めて parity を捨てながら、増幅段はそのまま残っていた。

「設計を決める前に測る」（#634/SU4）「マージする前に測る」（SU6.5）に続く 3 つ目の形として、**「原因を決める前に測る」**が加わった。

### 過去サイクルの実測記録が、作業そのものを消した

issue 項目 1（`fill_mesh` 最適化）は、SU6.5 の G3(b) が「`raster_ms` p95 4.68ms・予算超過 0/3680」を記録していたため**一行も書かずに閉じられた**。SU6.5 設計の決定 6 が「落ちたときだけ実施する」と分岐を事前に固定していたことが効いている。記録が無ければ、issue の記述（`raster_ms=27〜44`）を信じて不要な最適化を実装していた。

### 独立導出が、grep で到達できない参照を拾った

`/plan-review`「Step 2b」（plan/research を読ませない独立再導出）が `wake_results`（`view.rs:850`）を検出した。これは「repaint を要求する」概念を `wake_*` という**別名**で書いた箇所であり、`grep request_repaint` では原理的に到達しない。research.md の「アイドルでは発火しない」という前提はこれで訂正され、測定条件が 2 つに分かれた。

### 観測条件が汚染された run から、プロトコル非依存の指標で結論を出した

再測 1 回目を自分の手順ミスで無効にした後、**「点滅待ちの直後に何フレーム続くか」**という局所構造の指標を作った。総フレーム数や平均 fps と違いセッション長・操作量に依存しないため、汚染された run からでもスピンの消失（0% → 93%）を判定でき、クリーン計測では 100% に達した。指標を作り直したことで、測り直しの回数を 1 回で止められた。

---

## 伸びしろ

### 理論値の見積りを、実測と同じ重みでユーザーの判断材料に載せた

源をキャレット点滅と同定した時点で「周期 0.5s ゆえアイドルは 2fps・CPU 1% 未満」と見積もり、**その数字を選択肢 A/B の比較表に載せてユーザーに決定を求めた**。実測は 11.5fps・5.1% で、5.8 倍のずれがあった。B（受容）を勧めた根拠「1% 未満なら実害が薄い」は、実測後には成り立たなかった。

機構を正しく同定していても、**周期と負荷の間に増幅段が挟まりうる**。実測か理論値かを明示していれば、ユーザーは「測ってから決める」を選べた。`docs/development-principles.md` へ配置した。

### 人手の観測窓に、観測条件を変える確認作業を混ぜた

再測 1 回目で、カーソル挙動の目視確認のためのマウスホバーを計測 run に含めるよう依頼した。`ScrollArea` のフェードアニメーションが混入し、アイドル計測として無効になった。**目視確認は計測を止めてから行う**。あわせて、`Tee-Object` がプロセス終了までフラッシュしないためログが 0 バイトに見えた件も、手順に「終了させるまで」を書いていなかった。

### 派生コピーから手順を引き写し、実在しないパスを渡した

release バイナリのパスを SU6.5 の計画書から引き写して `src-tauri/target/release/` と書いたが、正しくはワークスペース直下の `target/release/` だった（`docs/build-commands.md` が SSOT）。既存規範（「照合は SSOT に対して行う。派生コピー同士の一致を完全性の証拠にしない」）が既に覆っている失敗であり、**規範の不足ではなく適用漏れ**である。

### 「同じ形の経路」を、欠陥が 1 つ見つかった時点で列挙しなかった

カーソル点滅（毎フレーム無条件に窓外の Win32 API を撃つ）を直したとき、`handle_platform_output` 内の同型経路（IME 位置更新も毎フレーム無条件）を同時に確認していない。ユーザー報告が無ければ気づけなかった欠陥クラスであり、1 件見つかった時点で同じ形を数え上げるべきだった（`AGENTS.md`「バグ発見時は同一パターン全コードパス検索」）。`snotra-egui-runtime/CLAUDE.md` の不変条件として配置した。
