# Retrospective — 窓の駆動を 1 つの責務へ集めたサイクル（#749・段 1）

`egui_shell/window_coordinator.rs` を新設し、窓を駆動する 11 関数を集めた。3 段再編の段 1 で、issue は「ほぼ移設・意味変化ほぼゼロ」と規定していた。実際に難しかったのは移設ではなく、**線をどこに引くか**だった。

## よかったこと

### 「文書としての計画」を読むレンズが、標準のレビューが 1 件も挙げなかった穴を開けた

`/plan-review` の標準構成（レイヤー別スカウト 3 + 独立導出 1）は要対処 5 件を挙げ、「実装着手可」で終わっていた。その後に回した一貫性・MECE の 4 レンズが、**標準構成が 1 件も挙げなかった不整合を 15 件超**出した。

最も重かったのは「**分割の線が 5 つの異なる原理で引かれ、衝突時にどちらが勝つか書かれていない**」である。実例として `read_metrics` と `max_results` はどちらも「唯一の消費者が移設対象の中にある」形なのに、計画は片方だけを運んでいた。これが規則 R（例外ゼロ）へ繋がり、`docs/adr/0008-window-coordinator-split-rule.md` になった。

拾えなかった理由は構造的に見える。標準スカウトは**担当ファイル群**を軸に分割され、独立導出は plan.md を読まずコードから再導出する。どちらも **plan.md を成果物として読む視点を持たない**——数値の整合・節どうしの覆い・手順としての実行可能性は、コードを見ても分からない。常設するかは #761 で判断する。

### 落ちたエージェントの成果物が、ファイル出力にしていたおかげで回収できた

4 レンズのうち 1 体が API エラー（`Connection closed mid-response`）で異常終了した。だがディスクには **103 行の完走した成果物**が残っており、`cargo` / `clippy` / `cargo doc` / `governance:check` のベースライン実測まで含んでいた。報告だけに依存していれば「落ちた＝何も得られなかった」で終わっていた。

#754 の硬化（台帳＋ファイル出力＋パスは呼び出し側が指定）が、**想定していた「無報告 idle」ではなく「異常終了」で効いた**実例である。

### コンパイラを移行漏れ検出器として使い切れた

view-local なデルタガード 2 フィールドを削除したところ、`E0609` が参照 6 箇所を正確に列挙した。`AGENTS.md`「条件別チェック（トリガー → 参照先）」が言う「旧 API の削除は下流の compile-fail を移行漏れ検出器に」がそのまま働き、手で数える工程が消えた。

### 目視が、自動検出器の視界の外にある欠陥を 1 件見つけた

カテゴリ D の 10 項目で #760 を発見した。メイン窓が作業領域の外にあると、検索結果窓が丸ごと画面外へ置かれ 1 行も見えない。`clamp_results_height` の床（1 行 + padding は必ず返す）は**高さしか見ておらず、位置が外にあれば「1 行は見える」を満たさない**——床の存在理由そのものが果たされていない。

clippy もユニットテストも `smoke:egui` も指摘していない。実施と記録が会話にしか残らない問題は `scripts/manual-smoke.ps1` を足して解いた。

---

## 伸びしろ

### 計画に書いたのに、実行を落とした

Phase 3 に「doc コメント中の `mod.rs::` 参照 3 件を訂正する」と書きながら、実装で落とした。コードレビューの「git 履歴」観点が拾って初めて気づいた。

**計画はチェックリストとして機能しなかった。** Phase ごとの完了条件は書いてあったが、そこに「この 3 件を直したか」は入っていなかった——**本文に書いた作業が完了条件に写っていなければ、書いたことは実行の保証にならない。**

### 自分の書いた説明が、自分の主張を反証していた

新設した `//!` に「main のサイズ適用は `view.rs` にある」と書いた。しかし根拠に挙げた `show_egui_main`（bar_height collapse で `window.set_size` を呼ぶ）は、**同じ変更で自分のファイルへ引き取っていた**。

移設では**コードは全部通る**まま散文だけが腐る。しかも `mod.rs` の 2 箇所は「`view.rs`（drive）」「`view.rs` の drive」と略記しており、**シンボル名の grep では届かなかった**（4 箇所と数えたが実際は 6 箇所）。一般則は `docs/development-principles.md`「ドキュメントとコードの分担（陳腐化の予防）」へ、判断の経緯は ADR-0008 の却下 5 へ置いた。

### 新設した検査の期待が、SPEC より強かった

カテゴリ D の項目 7 に「results がタスクバーの下へ潜らない」と書いたが、`SPEC.md` §8 は 1 行 + padding のはみ出しを**受容残余として明記している**（#675）。メイン窓を下端へ置けば**設計どおり落ちる**項目になっていた。実機で踏むまで気づかなかった。

**同じ誤りが #757 の目視シナリオにも先に書かれていた**——期待の書き写しは、検算されないまま複製される。受容残余を持つ挙動では、**残余の境界そのものを判定条件にする**（`docs/development-principles.md`「構造的設計原則と強制の階梯」）。

### 数値の腐りが計画の全体に及んだ

「移設する 9 関数」（`main.rs` 由来を二重計上）、「目視 7 項目」（実列挙は 9）、「`CLAUDE.md` の 5 行」（実体は単一の箇条書き行）、「自己言及 4 か所」（実際は 6）——**宣言した数値が、編集を継ぎ足すたびに実体から離れた。**

内部矛盾レンズが全件拾ったが、**この照合は本来かなりの部分が機械化できる**（宣言された数と実列挙の突き合わせ）。人手のレビューに載せ続ける性質のものではない。

### 対話スクリプトを書くとき、非対話環境を設計に入れていなかった

`manual-smoke.ps1` の初版は、`Read-Host` が EOF で空文字を返し続けると**再入力ループが無限に回り、起動済みのアプリが残る**構造だった。エージェント・CI から走ることを想定していなかった。

`[Console]::IsInputRedirected` でアプリ起動**前**に落とす形へ直した。**判定を人間が入れられない環境で走らせる意味は無い**——これは注意書きではなく機構で塞ぐべきものだった。
