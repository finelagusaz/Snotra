# Retrospective — 検査集合の穴を 3 PR で塞ぐ（#474 / #475+#476 / #497+#484、follow-up #500 起票）

## よかったこと

### 抽象化を第三者に反証させたら、自分の物語が削られた
4 つの issue を「検査写像 M が 5 箇所に多重定義されドリフトしている」と抽象化し、`codex` を非対話モードで呼んで**同意ではなく反証**を求めた。返ってきたのは「その定式化は物語化が強く、同語反復に近い」。より単純な説明は「検査カバレッジの境界が、編集トリガー・CI・docs・各ツールの対象集合で別々に管理されている」であり、#474 は写像のドリフトではなく **tsc 自身の program 集合の欠落**だった。#497 も「M の自己適用」ではなく「**カナリアはあるが、そこへ到達する経路が無い**」——ガードの到達性の問題。抽象化の美しさは、説明力の証拠ではない。

### 「合意不要の quick win」は幻だった
当初は「型負債の返済だけ先行すれば合意なしで着手できる」と分割した。codex が突いたのは依存だった——`@types/selenium-webdriver` は `package.json` に触れ、tsconfig を広げないと 9 件の解消が確定せず、そして **tsconfig を広げるとカナリアが hook の変更を強制する**。どう切っても合意対象になる。#471 が仕込んだカナリアが設計どおりに発火した最初のケースであり、「原子的に変えよ」という設計者の意図が実際に効いた瞬間でもあった。

### 故障注入を 5 回やり、安全網が実際に効くことを一度ずつ見た
`src-tauri` のテストを壊す → `tauri-test: 失敗 (exit 101)`。CSP から `ipc:` を外す → `csp-test: 失敗`。`vitest.config.ts` の include を削る → 新カナリアが**編集時に**落ちる。`.githooks/pre-commit` を無力化する → `githooks-selftest` が 6 テスト失敗を報告。`Cargo.toml` に存在しない member を足す → `cargo-check: 失敗`。**どれも、この 3 PR の前は完全な沈黙だった。** 「hook が守っている」は、壊して確かめるまでは期待にすぎない。

### 発火とカナリアを対にする、という規律が生まれた
`package.json` に `hook-selftest` を撃つのは一見よい。だが**そのファイルについては何も検証しない**——vitest が起動することしか証明しない。カナリアの無いファイルに検査を割り当てるのは、沈黙を「何も検証していない緑」に置き換えるだけである。独立再導出のサブエージェントも、私と独立に同じ結論（"otherwise it is cargo-cult"）へ到達した。**一致は、盲点が無いことの能動的証拠として働く。**

### 実装中に、hook が自分のミスを捕まえた
`vitestSpec(root, target)` の呼び出しだけ書いて定義を忘れたとき、hook が `再現: node vitest.mjs run C:\workspace\Snotra` という壊れた再現コマンドを出した。`root` を `target` の位置に渡していたのである。**安全網を編集したら安全網を検査する**という規律が、その安全網の実装中に効いた。

### 訂正文にも全称主張を書かなかった
設計文書 §1 は 4 つの沈黙経路を挙げる。PR-3 で閉じたのは **1 つだけ**（matcher 外の編集・`config-warn` の envelope 分岐・`post-edit.mjs` 自身の構文エラーは残る）。当初の訂正案は「入力集合の拡張で置換した」と書こうとしており、`/plan-review` がこれを過剰主張として止めた。§10 には「**これは機構ではなく規範による解決であり、読者が前提条件を忘れれば false green は再発する**」と書いた。全称主張を弱める PR が、自分の成果についても全称主張をしない。

---

## 伸びしろ

### 規約を新設した瞬間に、既存コードがそれを破っていた
「フックの cargo コマンドは SSOT と**一字一句一致**させる」と `docs/build-commands.md` と health-check Check 5 に書いた。その時点で既存の clippy は `--message-format short` を持っており、**規約どおり照合すれば Check 5 が初日から誤報する**構造だった。しかも修正圧は誤った向きに働く——規約に従ってフラグを外せば、rustc の長形式診断が証拠予算を潰す。code-reviewer が High で捕まえるまで気づかなかった。

これは `AGENTS.md`「全称表現は前提条件とセットで書く」の違反であり、**この 3 PR が根絶しようとしている当のパターン**である。前提条件を書けるかどうかは、**既存事例に当ててみて初めて分かる**。規則を書いたら、その規則が支配する事例をすべて列挙して当てる——この実行手順を `AGENTS.md` へ抽出した。

### 「現在の実測に一致する」を最適化して、fail-closed 方向を捨てていた
`Cargo.toml` の発火条件に、私は `/^([^/]+\/)?Cargo\.toml$/`（ルート + 1 段）を選んだ。`git ls-files` の実測 4 件と完全に一致するからである。独立再導出が指摘したのは、**将来 2 段目にワークスペースメンバーが生えたとき静かに検査されない**こと——過小検出は沈黙であり false green になる。過剰検出は `cargo check` が走るだけで無害。検出器の述語では、**現在の実測への一致と、変化に対して fail-closed であることは別の要件**であり、後者が優先する。basename アンカーへ直した。

### 相互照合を「検知器」と呼んでいた
`--lib` 乖離（#476）を直し、health-check Check 5 に「hook と SSOT を照合する」を足した。だが cargo の `-p` リストは hook・CI・SSOT の 3 箇所にハードコードされ、**真実源（`Cargo.toml` の `members`）とは照合していない**。4 つ目の crate を足せば 3 箇所とも同じ誤りを共有し、相互照合は「一致している」と報告する。**N 箇所の相互一致は、真実源との一致を意味しない。** 同じ根の 5 例目として #500 を起票し、「照合先は事実か、事実の写しか」を `AGENTS.md` へ抽出した。

### 存在しない節番号を計画に書いた
設計文書の訂正対象を「§9 と §13/§14」と書いたが、当の文書は §1〜§10 しかない。grep のヒットが別文書（`code-reviewer.md`・`SPEC.md`）のものだった。`AGENTS.md`「判定対象外のメタデータが同じ入力に混じらないことを示す」——自分で書いたルールを、自分の grep に適用していない。

### `/plan-review` Step 2b のトリガは、3 サイクル連続で外れ続けた
3 PR とも「hook のファイル 1 つ」の局所変更で、トリガ列（リネーム・config キー変更・データ移行・横断スイープ・多モジュール）に該当しない。文言どおりなら 3 回とも省略できた。省略せず実施したところ、独立再導出**だけ**が毎回漏れを拾った（`CLAUDE.md` のフック表ドリフト、「沈黙は合格」が 4 箇所・2 クラスあること）。**盲点は変更の規模に比例せず、そして「局所的か」を判定するのは盲点を持つ当人である。** 証拠を #495 へ追記した。
