# Retrospective — 反復 11（保存が返した派生データを cache-miss が使う・PR #977）

## よかったこと

### ⚠ を実装前に構成的へ畳んで、測る対象を 1 点まで縮めた

候補表が「未実測」と印していた「save 側の `Collapsed` が `assemble` の `Measured` 経路と同じ表現になるか」は、`name` が木に逐語で在ることと判定が `measure_derived_sharing` の 1 か所を通ることから、**差が `lower_file_name(原文)` 対 `lower_file_name(組み直し)` の一点に縮んだ**。既存の接地テストを 1 本走らせて全件のバイト一致を実測すれば、純関数ゆえ像も一致する。**測る前に「何を測れば足りるか」を縮める**のが最も安い手だった。

### 予測を実装前に書く規律が、2 反復連続で「外れの読み方」を与えた

8 項目中 7 当たり。外れた 1 件（構築段 blocks）は**潰し方がずれた合図ではなく、不変量の名指しの取り違え**と即座に読めた——ブロックは消えず構築段からロード段へ移動しており、同じ寿命変化を別の予測項では正しく当てていた。後から測っていれば、この読み分け自体ができない。

### 意図的分離の申し送りが 2 サイクル連続で誤検出 0 件

`/code-review` と `/simplify` の計 26 件で、意図的に分けた構造への誤検出は 1 件も無かった。ただし**申し送りに誤りがあればそこは通る**——「ループの形は 2 か所」と渡したが実際は 3 か所目があり、枠がそれを訂正した。

---

## 伸びしろ

### 新しく生きた組み合わせが、どの検査からも見えていなかった

`cached_masks` が `None` → `Some` に変わったことで、`main.rs` の PATH 併合が `extend_cached_masks` を**初めて呼ぶ**ようになった——その関数も呼び出し行も 1 行も変えていない。**変更のために新しく書いた検知器 2 本もそこを迂回**しており、長さの整合を守るのは release で消える `debug_assert` だけだった。教訓は `AGENTS.md`「条件別チェック（トリガー → 参照先）」へ配置した。

### 自分が引用した規範を、その引用のすぐ隣で破った

`LoadOrScanResult.cached_masks` の doc に偽の全称を書き、しかもそれは**この差分自身が予定した次の反復で起動時 panic の根拠になりえた**。直した版では「数え上げてはならない」と書いた同じ段落で数え上げていた。`AGENTS.md`「検証の作法（全タスク共通）」へ「数え上げも同じ強さ」「自分が引用した規範ほど破りやすい」を追記した。

### 写しを直すコミット自身が、写しを 5 枚に増やして 6 枚目を落とした

「cache-miss はもう `new_from_tree` を通らない」を 5 か所へ全文で書き、6 枚目（`SearchEngine::new_from_tree`）を取りこぼした。しかもその文は「木専用の導出を書き起こすな」という**規範の根拠**として使われており、前提が偽になって根拠ごと腐った。**腐った 3 件はいずれも「コードが既に決めている事実」（経路の数・分岐の列挙・呼び出し元）の写しだった。**

### 可視性を広げる弁明を doc に書いた時点で、切り口が対象と合っていなかった

A/B 検知器のために `index.bin` を書く関数ごと `pub(crate)` へ広げ、テストがファイルシステムを巻き込み、**型に無いロック契約の観客が crate 全体へ広がった**（しかも新しい呼び出し自身がその規範の反例になっていた）。純粋な導出を切り出して private へ戻したら 3 つとも同時に消えた。教訓は `docs/development-principles.md`「構造的設計原則と強制の階梯」へ。

### 型の幅を導出で見積もって外した

`LowerFileName` を「`String` 24 B ＋タグ → 32 B」と導出したが実測 24 B（`String` の `Cap` が持つ niche が fieldless 2 変種を吸う）。在庫へ誤った額を書く手前で測って止まった。
