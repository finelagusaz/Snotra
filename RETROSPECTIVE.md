# Retrospective — #836 フォルダ展開中の現在地表示（PR #842・`0bb9cde`）

## よかったこと

### 一次証拠に序列を付けたことが、3 対 1 の判断を正しい側へ倒した

「フォルダ名かフルパスか」で issue 本文の散文とスクリーンショットが食い違っていた。画像を取得して直接読み、撤去済みフロントを `git show 15933af^:` で復元して突き合わせた結果、一次証拠 2 つが一致してフルパスに決まった。ラウンド 3 の独立再導出だけが逆を主張したが、**サブエージェントは添付画像を読めない**——見ていない証拠が 1 つあると分かれば、票数は判断材料にならない。

### 「カテゴリ D はエージェントが実行できない」という前提が誤りだった

実行できないのは `manual-smoke.ps1` が合否の記録に対話入力を要求するからで、操作と観測そのものは `keybd_event` + 窓矩形のキャプチャでできる。11 項目を実測し、`main` のビルドとの A/B で陽性対照（`main` では hint が「検索...」のまま）まで取れた。**部品は既に `scripts/` にあり、別々のスクリプトへ分かれていただけだった**（#843）。

### 却下した 6 案を ADR へ回収してから `workspace/` を畳んだ

status 行案・results ヘッダ案・自前の中間省略・純粋核への抽出・2 回読みの束ね・常設目視項目の追加。**なぜ採らなかったか**はコードにも `SPEC.md` にも残らないため、削除で失う前に `docs/adr/ADR-folder-location-display-surface.md` へ移した。

---

## 伸びしろ

### 検索パターンが「自分の想定した書き方」を列挙し、2 回とも逆の診断を出した

`trace\] `（BRE で `\]` が `]` になる）と `Get-Process -Name 'snotra'`（他 5 本は `Get-Process snotra`）。どちらも件数が少なすぎたのに 0 件ではなかったため、「重複ではなく穴だ」という**逆の設計判断**を一度出した。教訓は `docs/development-principles.md`「列挙の完全性」へ 4 つ目のクラスとして配置した。

### 目視項目の期待値を、実装を読まずに書いた

項目 8（ツール選択中に hint が出る）と項目 11（フォルダ展開中に indexing の status 行が出る）はどちらも `view.rs` を読めば偽と分かった。前者は buf が非空ゆえ hint が描かれず、後者は `overlay_kind` の第 1 引数が `view_kind()==Results` でゲートされている。**目視項目も判定ロジックである**——`AGENTS.md`「検証の作法（全タスク共通）」が求める「実装前に代表入力で測る」の対象に含まれる。

### 検証環境の不備を製品の不具合と誤読しかけた

fixture の scan パスを `/` で書いたために `←` が効かず、「`C:` まで辿れない」という製品バグに見えた。真因は #841（`compute_parent_dir` が `rfind('\\')` しか見ない）で製品側の欠陥でもあったが、**先に自分の入力を疑う**手順を踏んでいない。

### 計画レビューが 3 ラウンドで収束しなかった

未確定は全て潰せた（条件 i）が、最終ラウンドの訂正 4 件が誰のレビューも受けないまま引き渡した（条件 ii 不成立）。訂正はいずれも散文の誤り（`poll_async` の説明・`enter_tool` の呼び出し元・系統数 2→3・記録経路）で、変更ファイル集合は動いていない。打ち切りの設計は機能したが、**ラウンド 3 の指摘が全て「自分が書いた説明の誤り」だった**ことは記述密度への信号として残る。
