# Retrospective — #884 連作の完結（#888/PR #899・段 5 語彙降格/PR #902・装置全却下で close）

## よかったこと

### 目的関数を親に合わせた瞬間、装置が次々と消えた

「SPEC.md が綺麗になればまぁ、なんでもあり」の一言が #888 の成果物 3 件を 1 件へ縮め（G-smoke-traces と ADR 新設を却下）、最後は装置 4 種（mermaid 語彙化・極性反転・述語拡張・ペア検査）すべてが「降格が守備対象を食べ尽くした」実測で畳まれた。起票時は追い風だった「機構が吸収できるなら規範にしない」が、実績調査の裁定後は向かい風になる——**同じ設計の評価が、目的関数の座標系だけで反転する**。意図のある短い一言に手順の再導出を委ねる憲章の、そのままの実例だった。

### 独立導出（`/plan-review`「Step 2b — 独立導出による網羅性レビュー」）が両方向に効いた

漏れ 12 件の捕捉に加え、plan 側の裁定を 2 件覆し（`bar_height` の降格・`config_watcher` の keep）、逆に plan が根拠の再照合で 2 件を防衛した（`FOLDER_EXPANSION_WEIGHT` は機械照合射程・`tauri::Window` は導出自身の「外部 API の同一性が主語」裁定を採用）。**捕捉だけでなく裁定の質も枠組みの独立から出る**——一致した項目より、不一致 4 件の突き合わせが計画を最も強くした。

### 受け入れ条件に「転記元を知らない再計測」を置いていた

全数計測（712 スパン）を計画の前に書き、降格後の再計測を受け入れ条件に固定していたため、導出 69 件を E 表へ手写しした際に落ちた 1 件（@384 の裸の `config`）が Phase 2 で機械的に浮上した。**検証が実装の写しではなく元データから独立に走る**構図が、転記漏れという「転記先だけを見ると見えない」欠陥クラスを構造的に捕まえた。

### 並行セッションとの境界を計画の不変条件に予告した

`fix/visual-color-same-frame` の存在を調査段階で認知し、「§11 は触らない」を計画へ明記した。#901（テーマ 3 値の同フレーム適用）と #902 は同日に無衝突で積み重なった。境界は「実装中に踏み込みうる隣接ファイル」ではなく**隣接する節**まで見積もる、の SPEC 版。

---

## 伸びしろ

### 列挙から列挙への手写しで、69 件中 1 件が落ちた

独立導出の 69 件を計画の E 表へ目視で反映し、1 件（@384）を落とした。捕まえたのは上記の再計測であって転記の丁寧さではない——**転記の完了は「写し終えた」ではなく機械の差分ゼロで宣言する**。教訓は `docs/development-principles.md`「列挙の完全性」へ配置した（転記という様態は既存 4 項のどれにも無かった）。

### 計画が発明した参照が、指し先を読まずに書かれていた

E49 の「§19.3 と同じ」は §19.2 の内容を指したつもりの誤指しで、code-reviewer の High が捕捉した。実装は計画に忠実だったので、欠陥は計画文言そのものにあった——**計画に新設する参照（節番号・見出し）も「代表入力で実行して測る」の対象であり、指し先を読んで主張の実在を確かめてから書く**。検出器（code-reviewer）が現に捕まえたため恒久規範は足さない。

### 現文言を読まずに書いた置換文字列だけが失敗した

E 表 50 件のうち 49 件は現文言の逐語引用で書き一発で当たり、唯一 @197（`focusable(false)`）だけは文言を推測で書いて Edit が失敗した。失敗は音を立てたので実害ゼロだが、対照実験のような結果になった——**逐語引用の規律は 98% ではなく 100% にしてこそ、失敗の残り 1 件が「推測した箇所」だと即座に言える**。
