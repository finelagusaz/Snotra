# Retrospective — search.rs 責務分割ロードマップ（#596・6 PR）＋ rules 薄化評価（#588）

## よかったこと

### スクリプト抽出でバイト等価移設 + 機械照合
テスト分割（#597・約 1,615 行）とコード移設（#598〜#600）を、手打ち転記でなく Node スクリプトで「関数を名前でスライスして再配置」した。転記ミスがゼロになり、`cargo test -- --list` の**葉テスト名 diff 一致**で欠落/追加を機械照合できた。挙動保存 refactor では「バイト等価であること」を接地して示せるのが強い。

### PostToolUse hook の即時フィードバックループ
モジュール分割は可視性（`pub(super)`）・import の配線ミスが出やすいが、各 Edit 直後に clippy/test が即座に「どのシンボルが未解決か」を返し、小刻みに直せた。特に #601（IncrementalCache）・#602（TopK）の設計変更で、コンパイラを対話的オラクルとして使えた。needle_u32 の可視性漏れ・heredoc のバックスラッシュ破損も hook が即検出した。

### worktree による同一マシン before/after ベンチ
perf 回帰の裏取りを、main の worktree でビルドして before を取り after と比較した。#602 では**連続する after 実行間で 300k が 1624→2048µs（26%）変動**することを示し、before/after 差がノイズ内であることを実証。「速くなった/遅くなった」の過剰主張を避け、byte 等価ロジックゆえ系統的回帰なし、と接地して結論できた。

### #596 を #588 の評価装置として二重活用
search.rs 分割を「rules 薄化（#588）の反例監査」を回す評価装置に使い、6 サンプル（#597〜#602）を完走。薄化ルーターに事実コピー欠陥ゼロを確認し、中核トリガ（`/cache-check`・Ord/top-k）の実発火まで観測。単に「欠陥なし」で終わらず**運用則 2 つを発掘**（位置断定しない・改名時同 PR 更新）してルーターに恒久化できた。監査がドリフトを捕捉→同 PR で是正、のループも機能した。

---

## 伸びしろ

### bash HEREDOC 規約違反（バックスラッシュ破損）
TopK テストを追加する際、うっかり `cat >> file <<'RUST'` の heredoc を使い、`"C:\\fake\\..."` の `\\` が `\` に潰れて `unknown character escape` のコンパイルエラーになった。これは CLAUDE.md「シェル環境」表が**明示的に禁じている事故そのもの**（終端マーカー漏れ・引用境界破壊）。複数行テキストの追加は最初から Write/Edit を使うべきだった。今回はたまたま Rust のエスケープエラーで loud に落ちたが、CLAUDE.md が挙げる「コミットメッセージ本文への終端漏れ」は静かに壊れる経路であり、常に loud とは限らない。

### スクリプト内正規表現の識別子取りこぼし
`pub(super)` 注入の正規表現 `^(    )([a-z_]+): ` が、数字を含む識別子 `needle_u32` をマッチせず 8/9 フィールドしか処理しなかった（#599 でも同型を踏んでいた）。AGENTS.md「検証の作法」の既存原則『計画に書いた判定ロジック（正規表現）は実装前に代表入力で実行して測る』を守り、`needle_u32` のような**数字入り識別子を代表入力に含めて**測っていれば事前に検出できた。既存原則の適用漏れであり、新規ルールは不要。
