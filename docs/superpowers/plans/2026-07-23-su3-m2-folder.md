# SU3 M2 folder Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** egui/softbuffer メインウィンドウの検索 UI に、WebView2(SolidJS) と parity なフォルダ展開（→/←/親ナビ・Escape 復帰・async 列挙 + staleness・同期フィルタ・Enter で explorer・列挙失敗行）を直 Engine で足す。

**Architecture:** フィルタモデル B（一度読み + 同期フィルタ）。ナビゲーション（→/←/親）のみ `std::thread` で全列挙・全ソート（上限なし）し driver がキャッシュ、フォルダ内の打鍵フィルタは新規同期 core fn `folder::filter_sorted` で毎フレーム同期に絞る。状態機械は src-tauri の純粋 `SearchState`（`Option<FolderFrame>` + `folder_filter` + `folder_gen`）に閉じ、driver（`view.rs`）は薄い imperative shell。設計 SSOT は `docs/superpowers/specs/2026-07-22-su3-search-experience-design.md`（特に「M2 実装確定」節）。

**Tech Stack:** Rust / egui 0.35 / winit / softbuffer / Tauri v2 / nucleo_matcher / snotra-core。

## Global Constraints

- **flag OFF（`SNOTRA_EGUI_MAIN` 未設定）で WebView2 経路・IPC コマンド・E2E 注入は完全不変（G1）。** 新 core fn は additive、IPC コマンドは削除しない。**例外**: `folder::score_entries` の path tie-breaker は共有ゆえ WebView2 の `list_folder` 経路にも及ぶが、影響は「同名を `to_lower_folded` で畳み込むと同キーになるエントリ（例 `Café`/`Cafe`）の tie 順が deterministic 化する」だけ。tie 順は SPEC 未規定で、既存の `score_entries_top_k_order_independent_of_input_order` 不変条件を真に成立させる方向の強化ゆえ許容する（B の「全ソート→filter→take-k ＝ filter→score_entries→take-k」一致の前提でもある）。
- **純粋核はユニットテスト**（`folder.rs` / engine wrappers / `search_state.rs`）。**src-tauri の view/Win32 はユニットテスト前提にしない**（`view.rs` は clippy + trace スモークで検証・`.claude/rules/src-tauri.md`）。
- **フィルタ文字列は専用 `folder_filter` field**。`SearchState.query` は展開前検索語を保持する（folder 中も不変）。`activate` は `self.state.query()` を無改変で記録＝履歴 query は展開前語（SolidJS parity）。
- **`folder_gen` は全離脱経路（Escape/hide/reset/親子遷移/enter）で失効**、`apply`（`accept_folder_result`）は token 一致 ∧ folder モードのときだけ受理。
- **各 green でコミット。** `cargo test -p snotra-core` / `cargo test -p snotra` / `cargo clippy -p <crate> --all-targets -- -D warnings` を検証に使う（`docs/build-commands.md`）。実機スモークは flag ON（`SNOTRA_EGUI_MAIN=1`）で `msedgewebview2.exe` 子孫 0 を確認。

---

## File Structure

- `snotra-core/src/folder.rs`（Modify）: 共有全順序比較器 `folder_entry_cmp` の抽出 + `score_entries` に path tie-breaker、新規 `sort_entries_unlimited`（全ソート・上限なし）、新規 `filter_sorted`（同期フィルタ）。責務は既存の `//!`（フォルダ列挙とフィルタ/ソート）に収まる。
- `snotra-core/src/engine.rs`（Modify）: `Engine::finalize_folder_list_unlimited`（history でソート・上限なし）、`FolderListContext::filter_sorted`（`SearchMode` を呼び出し側へ露出させないラッパー）。
- `src-tauri/src/egui_shell/search_state.rs`（Modify）: `FolderFrame` 型、`SearchState` に `folder` / `folder_filter` / `folder_gen` を追加、`view_kind()` を実装、`enter_folder` / `navigate_folder` / `parent_dir` / `accept_folder_result` / `set_folder_filter` / `on_escape` / `reset` 拡張、`compute_parent_dir` 純関数、`EscapeOutcome` enum。ユニットテスト対象。
- `src-tauri/src/egui_shell/view.rs`（Modify）: `FolderListContext` キャッシュ + `mpsc` チャネル + ナビゲーションスレッド、→/← キー処理、`run_search` の `view_kind` 先 dispatch、入力欄バインドの mode 分岐、Escape ラダー実行、列挙失敗行、チャネル drain。clippy + trace スモーク検証。
- `src-tauri/src/egui_shell/mod.rs`（参照のみ確認）: `EguiShellState.reset_pending` は既存。追加不要。

---

## Task 1: core B primitives（`folder.rs` + `engine.rs`）

**Files:**
- Modify: `snotra-core/src/folder.rs`（`score_entries` cmp 抽出 + path tie-breaker・`sort_entries_unlimited`・`filter_sorted`）
- Modify: `snotra-core/src/engine.rs`（`finalize_folder_list_unlimited`・`FolderListContext::filter_sorted`）
- Test: `snotra-core/src/folder.rs`（`#[cfg(test)] mod tests`）

**Interfaces:**
- Produces:
  - `folder::sort_entries_unlimited(entries: Vec<DirEntryData>, history: &HistoryStore) -> Vec<SearchResult>`（全順序で全件ソート・上限なし）
  - `folder::filter_sorted(cached: &[SearchResult], filter: &str, mode: SearchMode, max: usize) -> Vec<SearchResult>`（同期フィルタ・空 filter は先頭 max 件・I/O 無し）
  - `Engine::finalize_folder_list_unlimited(&self, entries: Vec<folder::DirEntryData>) -> Vec<SearchResult>`
  - `FolderListContext::filter_sorted(&self, cached: &[SearchResult], filter: &str) -> Vec<SearchResult>`
- Consumes: 既存 `folder::read_dir_entries` / `matches_filter`（private・同一モジュール）/ `to_lower_folded` / `HistoryStore::folder_expansion_count`。

- [ ] **Step 1: 共有比較器を抽出し path tie-breaker を足す failing test を書く**

`snotra-core/src/folder.rs` の `#[cfg(test)] mod tests` に追加（DirEntryData は同一モジュールゆえ private フィールドを構築できる）:

```rust
#[test]
fn folder_cmp_is_total_order_by_path() {
    // "Café" と "Cafe" は to_lower_folded で同キー化する（アクセント畳込み）。
    // path tie-breaker が無いと select_nth_unstable の境界順が read_dir 順へ漏れる。
    // empty_history() は既存 folder テストのヘルパー（HistoryStore::load()）。
    let hist = empty_history();
    let entries = vec![
        DirEntryData { path: "C:\\d\\Cafe".into(), name: "Cafe".into(), is_folder: false },
        DirEntryData { path: "C:\\d\\Café".into(), name: "Café".into(), is_folder: false },
    ];
    let a = sort_entries_unlimited(entries.clone(), &hist);
    // 入力順を反転しても同一順序（path 昇順で確定）
    let mut rev = entries;
    rev.reverse();
    let b = sort_entries_unlimited(rev, &hist);
    assert_eq!(a.iter().map(|r| r.path.clone()).collect::<Vec<_>>(),
               b.iter().map(|r| r.path.clone()).collect::<Vec<_>>());
    // path 昇順: "Cafe" < "Café"（バイト比較）
    assert_eq!(a[0].path, "C:\\d\\Cafe");
}
```

- [ ] **Step 2: test が落ちることを確認**

Run: `cargo test -p snotra-core folder_cmp_is_total_order_by_path`
Expected: FAIL（`sort_entries_unlimited` 未定義でコンパイルエラー）

- [ ] **Step 3: 比較器抽出 + tie-breaker + `sort_entries_unlimited` を実装**

`snotra-core/src/folder.rs` の `score_entries` 内のインライン `cmp` クロージャ（`let cmp = |a, b| { ... }`）を削除し、モジュール private の free fn に置換する:

```rust
/// フォルダ列挙の全順序比較器。is_folder 降順 → 展開回数降順 → 小文字名昇順 → path 昇順。
/// path を最終 tie-breaker にして全順序化する（同名畳込みの tie が入力順へ漏れるのを防ぐ・#532 SU3 M2）。
fn folder_entry_cmp(
    entries: &[SearchResult],
    a: &(String, u32, usize),
    b: &(String, u32, usize),
) -> std::cmp::Ordering {
    let a_entry = &entries[a.2];
    let b_entry = &entries[b.2];
    b_entry
        .is_folder
        .cmp(&a_entry.is_folder)
        .then_with(|| b.1.cmp(&a.1))
        .then_with(|| a.0.cmp(&b.0))
        .then_with(|| a_entry.path.cmp(&b_entry.path))
}

/// (lower_name, exp_count, index) の並び替えキーを構築する（score_entries / sort_entries_unlimited 共有）。
fn build_sort_keys(entries: &[SearchResult], history: &HistoryStore) -> Vec<(String, u32, usize)> {
    entries
        .iter()
        .enumerate()
        .map(|(i, e)| {
            let lower = to_lower_folded(&e.name);
            let exp_count = if e.is_folder {
                history.folder_expansion_count(&e.path)
            } else {
                0
            };
            (lower, exp_count, i)
        })
        .collect()
}

/// keyed の順序で entries を並べ替えて所有権を取り出す（clone 回避の mem::replace・共有ヘルパー）。
fn collect_by_keyed(mut entries: Vec<SearchResult>, keyed: Vec<(String, u32, usize)>) -> Vec<SearchResult> {
    keyed
        .into_iter()
        .map(|(_, _, i)| {
            std::mem::replace(
                &mut entries[i],
                SearchResult { name: String::new(), path: String::new(), is_folder: false, is_error: false },
            )
        })
        .collect()
}

/// フィルタ非依存の確定順序で全件をソートする（上限なし）。egui folder 経路がナビゲーション時に
/// 1 度だけ呼び、driver がキャッシュして打鍵ごとに `filter_sorted` で同期に絞る（#532 SU3 M2 B）。
pub fn sort_entries_unlimited(
    entries: Vec<DirEntryData>,
    history: &HistoryStore,
) -> Vec<SearchResult> {
    let entries: Vec<SearchResult> = entries
        .into_iter()
        .map(|e| SearchResult { name: e.name, path: e.path, is_folder: e.is_folder, is_error: false })
        .collect();
    let mut keyed = build_sort_keys(&entries, history);
    keyed.sort_by(|a, b| folder_entry_cmp(&entries, a, b));
    collect_by_keyed(entries, keyed)
}
```

`score_entries` を共有ヘルパーで書き直す（select_nth の O(N) top-k 最適化は維持）:

```rust
pub(crate) fn score_entries(
    entries: Vec<DirEntryData>,
    history: &HistoryStore,
    max_results: usize,
) -> Vec<SearchResult> {
    let entries: Vec<SearchResult> = entries
        .into_iter()
        .map(|e| SearchResult { name: e.name, path: e.path, is_folder: e.is_folder, is_error: false })
        .collect();
    let k = max_results.min(entries.len());
    if k == 0 {
        return vec![];
    }
    let mut keyed = build_sort_keys(&entries, history);
    if k < keyed.len() {
        keyed.select_nth_unstable_by(k - 1, |a, b| folder_entry_cmp(&entries, a, b));
        keyed.truncate(k);
    }
    keyed.sort_by(|a, b| folder_entry_cmp(&entries, a, b));
    collect_by_keyed(entries, keyed)
}
```

- [ ] **Step 4: test が通ることを確認**

Run: `cargo test -p snotra-core folder_cmp_is_total_order_by_path`
Expected: PASS

- [ ] **Step 5: 等価性（B の前提）テストを書いて落ちるのを確認**

```rust
#[test]
fn unlimited_filter_take_equals_score_entries() {
    // B の中核: 「全ソート→filter→take-k」＝「filter→score_entries→take-k」。
    // 全順序化した今、両者は同一（境界の tie も path で確定）。
    use crate::search::SearchMode;
    let hist = empty_history();
    let entries = vec![
        DirEntryData { path: "C:\\d\\Café".into(), name: "Café".into(), is_folder: false },
        DirEntryData { path: "C:\\d\\Cafe".into(), name: "Cafe".into(), is_folder: false },
        DirEntryData { path: "C:\\d\\dog".into(), name: "dog".into(), is_folder: false },
    ];
    // 経路 A: 全ソート → filter("cafe") → take(1)
    let sorted = sort_entries_unlimited(entries.clone(), &hist);
    let a = filter_sorted(&sorted, "cafe", SearchMode::Substring, 1);
    // 経路 B: read_dir 相当の filter を read_dir_entries で（filter="cafe"）→ score_entries(max=1)
    let filtered = read_dir_entries_for_test(entries, "cafe", SearchMode::Substring);
    let b = score_entries(filtered, &hist, 1);
    assert_eq!(a.iter().map(|r| r.path.clone()).collect::<Vec<_>>(),
               b.iter().map(|r| r.path.clone()).collect::<Vec<_>>());
}

/// read_dir_entries のフィルタ段だけをテストで再現する（実 I/O を避ける）。matches_filter は
/// private だが同一モジュールから呼べる。
#[cfg(test)]
fn read_dir_entries_for_test(entries: Vec<DirEntryData>, filter: &str, mode: crate::search::SearchMode) -> Vec<DirEntryData> {
    let filter_lower = to_lower_folded(filter);
    let mut matcher = Matcher::new(MatcherConfig::DEFAULT);
    entries
        .into_iter()
        .filter(|e| filter.is_empty() || matches_filter(&e.name, &filter_lower, mode, &mut matcher))
        .collect()
}
```

Run: `cargo test -p snotra-core unlimited_filter_take_equals_score_entries`
Expected: FAIL（`filter_sorted` 未定義）

- [ ] **Step 6: `filter_sorted` を実装**

`snotra-core/src/folder.rs` に追加:

```rust
/// キャッシュ済みソート結果を表示名でフィルタし先頭 max 件を返す（#532 SU3 M2・同期・I/O 無し）。
/// egui folder 経路が打鍵ごとに呼ぶ。`read_dir_entries` のフィルタ段（`matches_filter`）を
/// キャッシュへ再適用する。ソート順はキャッシュ時点で確定済みゆえ再スコア不要（表示名のみ・§6.3）。
pub fn filter_sorted(
    cached: &[SearchResult],
    filter: &str,
    mode: SearchMode,
    max: usize,
) -> Vec<SearchResult> {
    if filter.is_empty() {
        return cached.iter().take(max).cloned().collect();
    }
    let filter_lower = to_lower_folded(filter);
    let mut matcher = Matcher::new(MatcherConfig::DEFAULT);
    cached
        .iter()
        .filter(|r| matches_filter(&r.name, &filter_lower, mode, &mut matcher))
        .take(max)
        .cloned()
        .collect()
}
```

- [ ] **Step 7: 等価性テストが通ることを確認 + filter_sorted 単体テストを追加**

```rust
#[test]
fn filter_sorted_empty_returns_prefix_up_to_max() {
    use crate::search::SearchMode;
    let cached = vec![sr("a"), sr("b"), sr("c")];
    let out = filter_sorted(&cached, "", SearchMode::Substring, 2);
    assert_eq!(out.iter().map(|r| r.name.clone()).collect::<Vec<_>>(), vec!["a", "b"]);
}

#[test]
fn filter_sorted_matches_display_name_not_path() {
    use crate::search::SearchMode;
    // path に "zzz" を含むが name には含まない → filter "zzz" は 0 件（表示名のみ・§6.3）
    let cached = vec![SearchResult { name: "report".into(), path: "C:\\zzz\\report".into(), is_folder: false, is_error: false }];
    let out = filter_sorted(&cached, "zzz", SearchMode::Substring, 8);
    assert!(out.is_empty());
}

// テスト用ヘルパー（既存 tests mod にあれば再利用）
#[cfg(test)]
fn sr(name: &str) -> SearchResult {
    SearchResult { name: name.into(), path: format!("C:\\d\\{name}"), is_folder: false, is_error: false }
}
```

Run: `cargo test -p snotra-core folder::`
Expected: PASS（既存 `score_entries_top_k_order_independent_of_input_order` 含め全緑）

- [ ] **Step 8: engine ラッパーを追加**

`snotra-core/src/engine.rs` の `impl FolderListContext` に追加:

```rust
    /// キャッシュ済みソート結果を現フィルタで同期に絞る（#532 SU3 M2）。SearchMode を
    /// 呼び出し側（src-tauri driver）へ露出させないためのラッパー。
    pub fn filter_sorted(&self, cached: &[SearchResult], filter: &str) -> Vec<SearchResult> {
        folder::filter_sorted(cached, filter, self.mode, self.max_results)
    }
```

`impl Engine` に追加:

```rust
    /// folder ナビゲーション時に全件を全順序でソート（上限なし）して返す。driver がキャッシュし、
    /// 打鍵フィルタは `FolderListContext::filter_sorted` で同期に行う（#532 SU3 M2 B）。
    /// history は最新状態を使う（`finalize_folder_list` と同様・スコアリングのみへの影響）。
    pub fn finalize_folder_list_unlimited(
        &self,
        entries: Vec<folder::DirEntryData>,
    ) -> Vec<SearchResult> {
        folder::sort_entries_unlimited(entries, &self.history)
    }
```

- [ ] **Step 9: clippy + 全テスト**

Run: `cargo clippy -p snotra-core --all-targets -- -D warnings && cargo test -p snotra-core`
Expected: clean + PASS

- [ ] **Step 10: Commit**

```bash
git add snotra-core/src/folder.rs snotra-core/src/engine.rs
git commit -m "feat(core): folder B primitives（全順序化 score_entries・sort_entries_unlimited・filter_sorted）#532 SU3 M2"
```

---

## Task 2: 純粋状態核（`search_state.rs`）

**Files:**
- Modify: `src-tauri/src/egui_shell/search_state.rs`
- Test: 同ファイル `#[cfg(test)] mod tests`

**Interfaces:**
- Consumes: `snotra_core::engine::FolderListContext`（Task 1）は driver 側キャッシュに置くため **本タスクでは import しない**（状態核は folder 型に依存しない・Option Y）。`SearchResult`（既存）。
- Produces:
  - `FolderFrame { restore_query: String, restore_results: Vec<SearchResult>, restore_selected: usize, current_dir: String }`
  - `SearchState::view_kind() -> ViewKind`（`folder.is_some()` で Folder）
  - `SearchState::enter_folder(&mut self, dir: String) -> u64`（saveView + frame push + gen++ + filter クリア・token を返す）
  - `SearchState::navigate_folder(&mut self, dir: String) -> u64`（current_dir 書き換え + gen++ + filter クリア）
  - `SearchState::parent_dir(&self) -> Option<String>`（folder 中のみ・`compute_parent_dir`）
  - `SearchState::folder_current_dir(&self) -> Option<&str>`
  - `SearchState::accept_folder_result(&self, token: u64) -> bool`（token==folder_gen ∧ folder.is_some()）
  - `SearchState::folder_filter(&self) -> &str` / `set_folder_filter(&mut self, f: String)`
  - `SearchState::folder_gen(&self) -> u64`
  - `SearchState::on_escape(&mut self) -> EscapeOutcome`（`EscapeOutcome { RestoredSearch, Hide }`）
  - `compute_parent_dir(dir: &str) -> Option<String>`（純関数・ドライブルート/UNC 終端）

- [ ] **Step 1: `compute_parent_dir` の failing test を書く**

`search_state.rs` の tests に追加（`folderNav.ts:computeParentDir` の parity）:

```rust
#[test]
fn parent_dir_drive_and_unc_roots() {
    assert_eq!(compute_parent_dir("C:\\a\\b"), Some("C:\\a".to_string()));
    assert_eq!(compute_parent_dir("C:\\a"), Some("C:\\".to_string()));
    assert_eq!(compute_parent_dir("C:\\"), None);            // ドライブルート終端
    assert_eq!(compute_parent_dir("\\\\srv\\share\\x"), Some("\\\\srv\\share".to_string()));
    assert_eq!(compute_parent_dir("\\\\srv\\share"), None);  // UNC 共有ルート終端
    assert_eq!(compute_parent_dir("\\\\srv"), None);          // UNC 不完全
}
```

- [ ] **Step 2: 落ちるのを確認**

Run: `cargo test -p snotra parent_dir_drive_and_unc_roots`
Expected: FAIL（`compute_parent_dir` 未定義）

- [ ] **Step 3: `compute_parent_dir` を実装**

`search_state.rs` に追加（`folderNav.ts` の Rust ポート・末尾 `\` 正規化・UNC ガード）:

```rust
/// 親ディレクトリを返す。ルート（`C:\` / `\\server\share\`）で None。folderNav.computeParentDir 相当。
pub(crate) fn compute_parent_dir(current_dir: &str) -> Option<String> {
    // 末尾 `\` を剥がす（ただしドライブルート "X:\" は保持しない — 後段で判定）。
    let normalized = if current_dir.len() > 3 && current_dir.ends_with('\\') {
        &current_dir[..current_dir.len() - 1]
    } else {
        current_dir
    };
    // UNC ルート判定: \\server\share（2 セグメント以下）は終端。
    if let Some(rest) = normalized.strip_prefix("\\\\") {
        let parts: Vec<&str> = rest.trim_end_matches('\\').split('\\').filter(|p| !p.is_empty()).collect();
        if parts.len() <= 2 {
            return None;
        }
    }
    // 最後の `\segment` を削る。
    let cut = normalized.rfind('\\')?;
    let mut parent = normalized[..cut].to_string();
    // "X:" → "X:\"（ドライブルートは末尾 `\` 必須）。
    if parent.len() == 2 && parent.as_bytes()[1] == b':' && parent.as_bytes()[0].is_ascii_alphabetic() {
        parent.push('\\');
    }
    if parent.is_empty() || parent == normalized {
        return None;
    }
    // \\server（share 未満）は終端。
    if let Some(rest) = parent.strip_prefix("\\\\") {
        let parts: Vec<&str> = rest.trim_end_matches('\\').split('\\').filter(|p| !p.is_empty()).collect();
        if parts.len() < 2 {
            return None;
        }
    }
    Some(parent)
}
```

- [ ] **Step 4: 通るのを確認**

Run: `cargo test -p snotra parent_dir_drive_and_unc_roots`
Expected: PASS

- [ ] **Step 5: FolderFrame + state fields + view_kind + enter/navigate の failing test を書く**

```rust
#[test]
fn enter_folder_saves_view_and_switches_kind() {
    let mut s = SearchState::new();
    s.set_query("fire".into());
    s.set_results(vec![res("a"), res("b")]);
    s.move_selection(1); // selected=1
    assert_eq!(s.view_kind(), ViewKind::Results);
    let tok = s.enter_folder("C:\\proj".into());
    assert_eq!(s.view_kind(), ViewKind::Folder);
    assert_eq!(s.folder_current_dir(), Some("C:\\proj"));
    assert_eq!(s.folder_filter(), "");
    assert_eq!(s.folder_gen(), tok);
    // query は展開前語を保持（相乗りしない）
    assert_eq!(s.query(), "fire");
}

#[test]
fn navigate_folder_bumps_gen_and_clears_filter() {
    let mut s = SearchState::new();
    let t1 = s.enter_folder("C:\\a".into());
    s.set_folder_filter("x".into());
    let t2 = s.navigate_folder("C:\\a\\b".into());
    assert!(t2 > t1);
    assert_eq!(s.folder_current_dir(), Some("C:\\a\\b"));
    assert_eq!(s.folder_filter(), "");
}
```

- [ ] **Step 6: 落ちるのを確認**

Run: `cargo test -p snotra enter_folder_saves_view_and_switches_kind`
Expected: FAIL（未定義）

- [ ] **Step 7: FolderFrame と state 拡張を実装**

`search_state.rs` の `SearchState` を拡張（既存 `query`/`results`/`selected` に追加）:

```rust
/// フォルダ展開モードの退避/復元単位（`Option<FolderFrame>`・#532 SU3 M2）。深掘りは push でなく
/// current_dir 書き換え。フォルダ内フィルタは frame でなく SearchState.folder_filter が持つ。
#[derive(Debug, Clone)]
pub struct FolderFrame {
    pub restore_query: String,
    pub restore_results: Vec<SearchResult>,
    pub restore_selected: usize,
    pub current_dir: String,
}

/// Escape ラダーの分岐（driver が side-effect を実行）。M2 は folder 段と top-level のみ
/// （instant/command 解除段は M3）。
#[derive(Debug, PartialEq, Eq)]
pub enum EscapeOutcome {
    /// folder → 展開前検索状態へ復帰済み（driver は追加操作なし）
    RestoredSearch,
    /// top-level → hide 要求（driver が emit）
    Hide,
}
```

`SearchState` の struct 定義に追加:

```rust
pub struct SearchState {
    query: String,
    results: Vec<SearchResult>,
    selected: usize,
    folder: Option<FolderFrame>,
    folder_filter: String,
    folder_gen: u64,
}
```

`new()` を更新: `folder: None, folder_filter: String::new(), folder_gen: 0` を追加。

`view_kind()` の M1 固定実装を置換:

```rust
    /// 軸1。folder モードなら Folder、それ以外 Results（tool は SU3.5）。
    pub fn view_kind(&self) -> ViewKind {
        if self.folder.is_some() { ViewKind::Folder } else { ViewKind::Results }
    }
```

メソッド群を追加:

```rust
    /// 通常検索 → folder 突入。展開前状態を frame に退避し gen を進める。token を返す。
    pub fn enter_folder(&mut self, dir: String) -> u64 {
        self.folder = Some(FolderFrame {
            restore_query: self.query.clone(),
            restore_results: self.results.clone(),
            restore_selected: self.selected,
            current_dir: dir,
        });
        self.folder_filter.clear();
        self.selected = 0;
        self.folder_gen += 1;
        self.folder_gen
    }

    /// folder 内で親/子へ遷移（frame の current_dir を書き換え・push しない）。token を返す。
    pub fn navigate_folder(&mut self, dir: String) -> u64 {
        if let Some(f) = self.folder.as_mut() {
            f.current_dir = dir;
        }
        self.folder_filter.clear();
        self.selected = 0;
        self.folder_gen += 1;
        self.folder_gen
    }

    pub fn folder_current_dir(&self) -> Option<&str> {
        self.folder.as_ref().map(|f| f.current_dir.as_str())
    }

    /// folder 中の親ディレクトリ（ルート終端で None）。
    pub fn parent_dir(&self) -> Option<String> {
        self.folder.as_ref().and_then(|f| compute_parent_dir(&f.current_dir))
    }

    pub fn folder_gen(&self) -> u64 {
        self.folder_gen
    }

    /// 遅延到着したナビ結果を受理してよいか（token 一致 ∧ folder 中）。driver が false なら破棄。
    pub fn accept_folder_result(&self, token: u64) -> bool {
        token == self.folder_gen && self.folder.is_some()
    }

    pub fn folder_filter(&self) -> &str {
        &self.folder_filter
    }

    pub fn set_folder_filter(&mut self, f: String) {
        self.folder_filter = f;
        self.selected = 0;
    }
```

- [ ] **Step 8: 通るのを確認**

Run: `cargo test -p snotra enter_folder_saves_view_and_switches_kind navigate_folder_bumps_gen_and_clears_filter`
Expected: PASS

- [ ] **Step 9: Escape ラダーと staleness の failing test を書く**

```rust
#[test]
fn escape_folder_restores_then_hides() {
    let mut s = SearchState::new();
    s.set_query("fire".into());
    s.set_results(vec![res("a"), res("b"), res("c")]);
    s.move_selection(2); // selected=2
    s.enter_folder("C:\\proj".into());
    s.set_folder_filter("x".into());
    // 1 回目の Escape → 展開前状態へ復帰
    assert_eq!(s.on_escape(), EscapeOutcome::RestoredSearch);
    assert_eq!(s.view_kind(), ViewKind::Results);
    assert_eq!(s.query(), "fire");
    assert_eq!(s.results().len(), 3);
    assert_eq!(s.selected(), 2);
    // 2 回目の Escape（results + plain）→ hide
    assert_eq!(s.on_escape(), EscapeOutcome::Hide);
}

#[test]
fn stale_folder_result_is_rejected() {
    let mut s = SearchState::new();
    let t1 = s.enter_folder("C:\\a".into());
    let t2 = s.navigate_folder("C:\\a\\b".into());
    assert!(!s.accept_folder_result(t1));  // 旧 token は破棄
    assert!(s.accept_folder_result(t2));   // 最新 token は受理
}

#[test]
fn escape_invalidates_gen_so_late_nav_result_is_dropped() {
    let mut s = SearchState::new();
    s.set_results(vec![res("a")]);
    let tok = s.enter_folder("C:\\a".into());
    s.on_escape(); // folder 離脱 → gen 失効 + folder None
    assert!(!s.accept_folder_result(tok)); // 離脱後は旧ナビ結果を受理しない
    assert_eq!(s.view_kind(), ViewKind::Results);
}

#[test]
fn reset_invalidates_folder_gen_and_clears_mode() {
    let mut s = SearchState::new();
    let tok = s.enter_folder("C:\\a".into());
    s.reset(); // show 時 reset
    assert!(!s.accept_folder_result(tok));
    assert_eq!(s.view_kind(), ViewKind::Results);
    assert_eq!(s.folder_filter(), "");
}
```

- [ ] **Step 10: 落ちるのを確認**

Run: `cargo test -p snotra escape_folder_restores_then_hides`
Expected: FAIL（`on_escape` 未定義）

- [ ] **Step 11: on_escape と reset 拡張を実装**

```rust
    /// Escape ラダー（M2）: folder 中は展開前状態へ復帰、top-level は Hide。
    /// folder 離脱時は folder_gen を進めて遅延到着した旧ナビ結果を無効化する。
    pub fn on_escape(&mut self) -> EscapeOutcome {
        if let Some(f) = self.folder.take() {
            self.query = f.restore_query;
            self.results = f.restore_results;
            self.selected = clamp_selected(self.results.len(), f.restore_selected);
            self.folder_filter.clear();
            self.folder_gen += 1; // 離脱経路でも失効
            EscapeOutcome::RestoredSearch
        } else {
            EscapeOutcome::Hide
        }
    }
```

`reset()` を拡張（既存の query/results/selected クリアに folder 失効を追加）:

```rust
    /// resetForShow 相当。show のたびに driver が呼ぶ。folder モードも解除し gen を進める
    /// （hide 前の in-flight ナビ結果を再表示後に轢かせない）。
    pub fn reset(&mut self) {
        self.query.clear();
        self.results.clear();
        self.selected = 0;
        self.folder = None;
        self.folder_filter.clear();
        self.folder_gen += 1;
    }
```

- [ ] **Step 12: 通るのを確認 + folder 中 interp が Plain のままか確認するテスト**

```rust
#[test]
fn folder_mode_interp_is_plain_even_with_prefix() {
    let mut s = SearchState::new();
    s.enter_folder("C:\\a".into());
    s.set_folder_filter("@x".into()); // folder_filter に @ が入っても
    // interp は view_kind()==Folder ゆえ Plain（query 相乗りしないので query は空のまま）
    assert_eq!(s.interp("@"), QueryIntent::Plain);
}
```

Run: `cargo test -p snotra egui_shell::search_state`
Expected: PASS（全 folder テスト + 既存 M1 テスト緑）

- [ ] **Step 13: clippy + テスト**

Run: `cargo clippy -p snotra --all-targets -- -D warnings && cargo test -p snotra egui_shell::search_state`
Expected: clean + PASS

- [ ] **Step 14: Commit**

```bash
git add src-tauri/src/egui_shell/search_state.rs
git commit -m "feat(egui): SearchState に folder 状態機械（Option<FolderFrame>・folder_gen・Escape ラダー）#532 SU3 M2"
```

---

## Task 3: driver 配線（`view.rs`）

**注**: `view.rs` は egui/Win32 依存ゆえユニットテスト前提にしない（`.claude/rules/src-tauri.md`）。検証は `cargo clippy` + flag ON の trace スモーク。各ステップは concrete edit。

**Files:**
- Modify: `src-tauri/src/egui_shell/view.rs`

**Interfaces:**
- Consumes: Task 1（`FolderListContext::filter_sorted` / `Engine::finalize_folder_list_unlimited` / `capture_folder_list_context` / `read_dir_entries` / `folder::error_result`）、Task 2（`SearchState` folder API・`EscapeOutcome`）。

- [ ] **Step 1: import と driver フィールド・チャネルを追加**

`view.rs` 冒頭の use に追加:

```rust
use std::sync::mpsc::{Receiver, Sender, channel};
use snotra_core::engine::FolderListContext;
use crate::egui_shell::{EscapeOutcome, ViewKind};
```

ナビ結果メッセージ型を `view.rs` に定義:

```rust
/// ナビゲーションスレッド → driver のメッセージ（#532 SU3 M2）。token で staleness 判定。
enum FolderMsg {
    /// 列挙成功: (ctx, 全ソート済み full 集合)。driver がキャッシュし filter_sorted で絞る。
    Loaded(u64, FolderListContext, Vec<SearchResult>),
    /// 列挙失敗: 単一エラー行（§6.6・filter 非適用で常時表示）。
    Failed(u64, Vec<SearchResult>),
}
```

`SearchWindowView` に追加フィールド:

```rust
    folder_tx: Sender<FolderMsg>,
    folder_rx: Receiver<FolderMsg>,
    /// ナビゲーションでロードした (ctx, 全ソート済み) キャッシュ。打鍵フィルタの源。
    folder_cache: Option<(FolderListContext, Vec<SearchResult>)>,
    /// 列挙失敗時の単一エラー行（filter を無視して表示）。
    folder_error: Option<Vec<SearchResult>>,
```

`new()` に追加:

```rust
        let (folder_tx, folder_rx) = channel();
        // ... Self { ..., folder_tx, folder_rx, folder_cache: None, folder_error: None }
```

- [ ] **Step 2: ナビゲーションスレッド spawn ヘルパーを追加**

```rust
    /// dir を別スレッドで全列挙・全ソートし FolderMsg を channel へ送る（token 付き）。
    /// capture（ロック内）→ read_dir_entries（ロック外 I/O・dead UNC でも event-loop を止めない）
    /// → finalize_folder_list_unlimited（ロック内・history でソート）の 3 段（engine.rs のロック最小化パターン）。
    ///
    /// **per-nav `std::thread::spawn` は意図的な選択**（advisor 2026-07-23）: 単一 worker だと 1 つの
    /// hung `read_dir`（dead UNC）が後続の全フォルダロードを塞ぐ。per-nav spawn は hang を 1 スレッドに
    /// 隔離し、正常な dir へのナビは動き続ける。dead UNC でのスレッドリークは best-effort として受容する
    /// （M1 の event-loop 同期 `ShellExecuteW`-on-dead-UNC と同類のトレードオフ）。正常な dir の read_dir は
    /// ミリ秒オーダーで完了するため高速 →/← でも実質的な pileup は起きない。共有 tokio blocking pool
    /// （`spawn_blocking`）は採らない——dead UNC が pool を飽和させ icon/index 等の他利用者を巻き込むため。
    fn spawn_folder_load(&self, token: u64, dir: String) {
        let app = self.app_handle.clone();
        let tx = self.folder_tx.clone();
        std::thread::spawn(move || {
            let Some(state) = app.try_state::<crate::AppState>() else { return };
            let ctx = { state.engine.lock().unwrap().capture_folder_list_context() };
            let entries = match ctx.read_dir_entries(std::path::Path::new(&dir), "") {
                Ok(e) => e,
                Err(_) => {
                    let err = snotra_core::folder::error_result(std::path::Path::new(&dir));
                    let _ = tx.send(FolderMsg::Failed(token, err));
                    return;
                }
            };
            let sorted = { state.engine.lock().unwrap().finalize_folder_list_unlimited(entries) };
            let _ = tx.send(FolderMsg::Loaded(token, ctx, sorted));
        });
    }
```

- [ ] **Step 3: `run_search` を view_kind 先 dispatch へ組み替える**

現 `run_search`（`interp` 先）を置換。folder は同期フィルタ、results のみ interp 分岐:

```rust
    /// view_kind 先の同期 dispatch（#532 SU3 M2）。folder は cache/error を同期フィルタ、
    /// results は M1 の interp 分岐（plain 検索）。folder 打鍵が engine.search へ漏れない。
    fn run_search(&mut self) {
        match self.state.view_kind() {
            ViewKind::Folder => {
                if let Some(err) = &self.folder_error {
                    self.state.set_results(err.clone()); // 列挙失敗行（filter 非適用）
                } else if let Some((ctx, sorted)) = &self.folder_cache {
                    let filtered = ctx.filter_sorted(sorted, self.state.folder_filter());
                    self.state.set_results(filtered);
                }
                // cache 未着（ロード中）は前フレーム結果を保持（フリット無し・set しない）
            }
            ViewKind::Results => {
                let prefix = self.instant_prefix();
                match self.state.interp(&prefix) {
                    QueryIntent::Plain => {
                        if self.state.query().trim().is_empty() || self.indexing() {
                            self.state.set_results(Vec::new());
                            return;
                        }
                        let query = self.state.query().to_string();
                        let results = {
                            let state = match self.app_handle.try_state::<crate::AppState>() {
                                Some(s) => s,
                                None => return,
                            };
                            let mut engine = state.engine.lock().unwrap();
                            engine.search(&query)
                        };
                        self.state.set_results(results);
                    }
                    _ => {
                        self.state.set_results(Vec::new()); // command/instant は M3
                    }
                }
            }
        }
    }
```

- [ ] **Step 4: channel drain（毎フレーム・最新 token のみ適用）を `update()` 冒頭付近に追加**

まず既存の `reset_pending` 消費ブロック（`sh.reset_pending.swap(false)` → `self.state.reset()` の箇所）に driver 側キャッシュのクリアを 1 行足す（`state.reset()` は state の folder のみ失効するため・advisor 2026-07-23）:

```rust
        // reset_pending 消費ブロック内、self.state.reset() の直後に追加:
        self.folder_cache = None;
        self.folder_error = None;
```

続いて `update()` の reset 処理直後、input 処理より前に drain を挿入:

```rust
        // ナビ結果を drain し、現行 folder_gen と一致する最新のものだけ適用する（stale 破棄・滞留 drain）。
        let mut latest: Option<FolderMsg> = None;
        while let Ok(msg) = self.folder_rx.try_recv() {
            let tok = match &msg { FolderMsg::Loaded(t, ..) | FolderMsg::Failed(t, ..) => *t };
            if self.state.accept_folder_result(tok) {
                latest = Some(msg); // 後着で上書き＝最新を採る
            }
        }
        if let Some(msg) = latest {
            match msg {
                FolderMsg::Loaded(_, ctx, sorted) => {
                    self.folder_error = None;
                    self.folder_cache = Some((ctx, sorted));
                }
                FolderMsg::Failed(_, err) => {
                    self.folder_cache = None;
                    self.folder_error = Some(err);
                }
            }
            self.run_search(); // 現 folder_filter で即再フィルタ（ロード中打鍵の消失防止）
            ctx_repaint = true; // 下で request_repaint（到着フレームを描く）
        }
```

（`ctx_repaint` は既存の repaint 要求と統合。無ければ末尾で `if latest_applied { ui.ctx().request_repaint(); }` に置換してよい。）

- [ ] **Step 5: →/← キー処理を追加（↑↓ の直後）**

`update()` の ArrowUp/Down 処理の後に挿入:

```rust
        // → : 選択中がフォルダなら展開（results 中は enter、folder 中は深掘り）。ファイル/エラー行は無反応。
        if ctx.input(|i| i.key_pressed(egui::Key::ArrowRight)) {
            if let Some(sel) = self.state.results().get(self.state.selected())
                && sel.is_folder && !sel.is_error
            {
                let dir = sel.path.clone();
                let tok = match self.state.view_kind() {
                    ViewKind::Folder => self.state.navigate_folder(dir.clone()),
                    ViewKind::Results => self.state.enter_folder(dir.clone()),
                };
                self.folder_cache = None;
                self.folder_error = None;
                self.spawn_folder_load(tok, dir);
            }
        }
        // ← : folder 中は親へ、通常検索中は選択項目の親を展開して folder 突入。
        if ctx.input(|i| i.key_pressed(egui::Key::ArrowLeft)) {
            match self.state.view_kind() {
                ViewKind::Folder => {
                    if let Some(parent) = self.state.parent_dir() {
                        let tok = self.state.navigate_folder(parent.clone());
                        self.folder_cache = None;
                        self.folder_error = None;
                        self.spawn_folder_load(tok, parent);
                    }
                }
                ViewKind::Results => {
                    if let Some(sel) = self.state.results().get(self.state.selected())
                        && !sel.is_error
                        && let Some(parent) = crate::egui_shell::compute_parent_dir(&sel.path)
                    {
                        let tok = self.state.enter_folder(parent.clone());
                        self.folder_cache = None;
                        self.folder_error = None;
                        self.spawn_folder_load(tok, parent);
                    }
                }
            }
        }
```

（`compute_parent_dir` は Task 2 で `pub(crate)`。`egui_shell/mod.rs` が `pub(crate) use search_state::compute_parent_dir;` を re-export しているか確認し、無ければ追加する。）

- [ ] **Step 6: Escape を無条件 hide からラダーへ差し替える**

現 `if escape { self.emit_hide(); }` を置換:

```rust
        if escape {
            match self.state.on_escape() {
                EscapeOutcome::RestoredSearch => {
                    // folder 離脱 → cache/error 破棄、復帰済み results を描く
                    self.folder_cache = None;
                    self.folder_error = None;
                    ui.ctx().request_repaint();
                }
                EscapeOutcome::Hide => self.emit_hide(),
            }
        }
```

- [ ] **Step 7: 入力欄バインドを mode 分岐にする**

現 TextEdit の `let mut buf = self.state.query().to_string();` と `if response.changed() { self.state.set_query(buf); ... }` を mode 分岐へ:

```rust
        let in_folder = self.state.view_kind() == ViewKind::Folder;
        let mut buf = if in_folder {
            self.state.folder_filter().to_string()
        } else {
            self.state.query().to_string()
        };
        // hint も folder 中は現在地を出す（任意・§6 の文脈提示）
        let response = ui.add(
            egui::TextEdit::singleline(&mut buf).hint_text(hint).desired_width(f32::INFINITY),
        );
        if response.changed() {
            if in_folder {
                self.state.set_folder_filter(buf);
                self.run_search(); // folder は同期フィルタ（debounce 不要・I/O 無し）
            } else {
                self.state.set_query(buf);
                self.last_input_at = Instant::now();
                if self.search_debounce.on_input() {
                    self.run_search(); // leading
                }
                ctx.request_repaint_after(self.search_debounce.interval());
            }
        }
```

（`hint` の indexing 分岐は既存のまま。folder 中は query が空でも indexing hint を出さないよう、`hint` 算出を `!in_folder && self.indexing() && ...` に狭める。）

- [ ] **Step 8: trailing debounce と結果表示の folder 対応を確認**

trailing debounce（`self.search_debounce.poll(...)` → `run_search`）は folder 中も呼ばれるが、`run_search` の Folder 分岐は同期フィルタゆえ無害（冪等）。`show_results` 判定はそのまま（folder 結果も `state.results()` に入る）。`activate` は **無改変**（`self.state.query()` を記録＝folder 中も展開前語・parity 保全）を確認する。

- [ ] **Step 9: clippy**

Run: `cargo clippy -p snotra --all-targets -- -D warnings`
Expected: clean

- [ ] **Step 10: trace スモーク（flag ON・実機）**

Run: `$env:SNOTRA_EGUI_MAIN=1; $env:SNOTRA_TRACE=1; cargo run -p snotra --release`（`docs/build-commands.md` の起動手順に従う）
確認（目視 + trace）:
- 検索 → 結果でフォルダ選択 → `→` で展開（folder モード・中身表示）
- folder 中に文字入力 → 同期フィルタ（表示名絞り込み・遅延なし）
- `←` で親へ / ルート（`C:\`）で無反応
- `Escape` 1 回で展開前の検索語・結果・選択位置へ復帰、2 回目で hide
- フォルダで `Enter` → explorer で開く
- アクセス拒否ディレクトリ → 単一エラー行・`Enter` 無効・`←`/`Escape` は有効
- 高速 `→`/`←` 連打で stale 結果が現ビューを轢かない（trace で `accept_folder_result` false の破棄を確認）
- **`→`/`←` 押下時に入力欄（TextEdit）のカーソルが飛ばない/二重処理されない**（egui は `preventDefault` 相当が無いため press を ctx で拾いつつ focused TextEdit もカーソル移動しうる。ナビ時は `folder_filter` クリア + 欄切替で不可視のはずだが、DOM parity 仮定が egui のイベントモデルに触れる唯一点ゆえ実機で確認・advisor 2026-07-23。破綻していなければ消費機構は足さない）
- `msedgewebview2.exe` 子孫 0

- [ ] **Step 11: flag OFF 完全不変（G1）確認**

Run: `cargo test -p snotra && cargo test -p snotra-core`（flag 未設定）
Expected: 既存テスト全緑（WebView2 経路・IPC 不変。score_entries の tie-breaker 影響は既存テストが緑を保つことで確認）

- [ ] **Step 12: Commit**

```bash
git add src-tauri/src/egui_shell/view.rs src-tauri/src/egui_shell/mod.rs
git commit -m "feat(egui): folder driver 配線（→←/Escape ラダー・async ナビ + drain・同期フィルタ）#532 SU3 M2"
```

---

## Self-Review

**Spec coverage（SU3 spec §6 / M2 実装確定節 → タスク）:**
- §6.1 →/← 基本動作 → Task 3 Step 5。§6.2 ルート定義 → Task 2 `compute_parent_dir`（Step 1-4）。§6.3 folder 内検索（表示名のみ）→ Task 1 `filter_sorted`（Step 6-7）+ Task 3 Step 7。§6.4 Escape 復帰 → Task 2 `on_escape`（Step 9-11）+ Task 3 Step 6。§6.5 Enter で explorer → Task 3 Step 8（`activate` 無改変・folder path を ShellExecuteW）。§6.6 列挙失敗行 → Task 3 Step 1/2/4（`FolderMsg::Failed`）。
- フィルタモデル B / core split → Task 1。folder_filter 専用 field → Task 2 + Task 3 Step 7。Option<FolderFrame> → Task 2。folder_gen 全離脱失効 + apply 限定 → Task 2 Step 9-11 + Task 3 Step 4。drain 最新 token → Task 3 Step 4。view_kind 先 dispatch → Task 3 Step 3。path tie-breaker → Task 1 Step 1-4。
- **cache 上限 / live 更新 parity 対象外**: 本 plan は「上限なし full cache・folder 滞在中の fs/config 変化非反映」を明示採用（spec の受容判断どおり）。巨大 dir の実測は Task 3 Step 10 のスモークで体感確認に留め、数値上限は導入しない（YAGNI・稀ゆえ）。
- **スレッドモデル（codex #3 の正確な帰結・advisor 2026-07-23 で訂正）**: drain（Task 3 Step 4）が担うのは**結果の staleness 破棄のみ**。スレッド/IO の pileup 側は per-nav `std::thread::spawn` で扱い、**dead UNC でのスレッドリークは意図的な best-effort 受容**（Task 3 Step 2 の doc 参照。単一 worker/共有 pool は dead UNC が全ロード or 他 subsystem を巻き込むため却下）。「worker で bound する」とは**しない**——per-nav 隔離の方が dead UNC 耐性が高い。

**Placeholder scan:** なし（全ステップに concrete code / command）。

**Type consistency:** `filter_sorted`(core free fn: `(cached, filter, mode, max)`) と `FolderListContext::filter_sorted`(`(cached, filter)`) は名前同一・引数違いだが呼び出し文脈が異なる（driver は後者）。`FolderMsg` の token は `u64`（`folder_gen` と一致）。`EscapeOutcome` は `RestoredSearch`/`Hide`（M3 で `ClearMode` 追加予定・本 plan では未使用）。`compute_parent_dir` は `search_state.rs` 定義・`mod.rs` re-export で driver から `crate::egui_shell::compute_parent_dir`。

**未解決の実装時確認点（着手時に潰す）:**
- `mod.rs` の re-export（`compute_parent_dir` / `EscapeOutcome` / `ViewKind`）が driver から見えるか（Task 3 Step 1/5 で確認・無ければ追加）。
- `SearchResult` の `is_error` 行が `filter_sorted` を通ると名前非一致で消える → 列挙失敗行は cache でなく `folder_error` 経路で filter を迂回する設計（Task 3 Step 3-4）で担保。
- `list_folder` IPC（WebView2）は本 plan で不変（`score_entries` の tie-breaker のみ共有・G1 例外として global constraints に明記済み）。
