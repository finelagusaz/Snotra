# SU3.5 tool-selection 実装計画（#532 Phase 2 + #638）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** egui 経路にカスタムオープナーのツール選択メニュー（SPEC §18.3/18.4/18.5）を parity 実装し、#638（instant 重複名）を config ロード時の先勝ち正規化で構造的に排除する。

**Architecture:** spec `docs/superpowers/specs/2026-07-23-su3.5-tool-selection-design.md` のとおり、`SearchState` に `tool: Option<ToolFrame>` を追加する **2 スロット + 優先度射影**（`view_kind()` = tool > folder > results・SolidJS 実装と同型・tool-on-tool 表現不能）。解決/起動は core `find_matching_tools` / `launch_with_tool_core` を IPC・トレイと共有する。**副捕獲**: egui `activate()` が opener 解決を通らない既存 parity gap（§18.3 全起動経路統一からの乖離）も本 SU で塞ぐ。

**Tech Stack:** Rust（egui 0.35 / tauri v2）。純粋核は TDD ユニット、driver は build+clippy+GUI smoke（「view はテスト前提にしない」規律）。

## Global Constraints

- **G1 不侵**: フラグ OFF（`SNOTRA_EGUI_MAIN` 未設定）の WebView2 経路・SolidJS・IPC の挙動を変えない（`launch.rs` の変更は `launch_with_tool_core` の可視性 `pub(crate)` 化のみ＝挙動中立）
- **書き戻し禁止（#638 決定 2）**: dedup は in-memory のみ。`apply_migrations()` の戻り値 `changed` に**寄与させない**（`load_from_dir_reporting` は `changed=true` で `save_to_dir` する実装・config.rs:826-829。寄与させるとユーザーの手編集行がファイルから消える）。**適用範囲は load 経路の自発的書き戻しの禁止のみ**——settings の明示保存（`snotra-settings/src/app.rs` `save()`・backup インポート）は `normalize_scan_paths`/`normalize_openers` と同様に正規化済み内容を保存する既存設計であり、そこで dedup 済み Vec が保存されるのは**許容**（ユーザー操作起点の保存。plan-review scout-core 指摘への意思決定・spec 決定 2 注記）
- **engine ロックは同期・短時間のみ**（`snotra-core/CLAUDE.md` ロック最小化。解決はロック内純 CPU → clone して即解放）
- 各タスク完了ごとにコミット（ブランチ `feat/532-su3.5-tool-selection`）。PostToolUse hook（clippy + crate テスト）は沈黙 = 合格
- コメント規約: 「なぜ」のみ（`docs/comment-guidelines.md`）。SPEC §・issue 番号への参照を残す

---

### Task 1: #638 — instant 重複名の先勝ち正規化（snotra-core）

**Files:**
- Modify: `snotra-core/src/config.rs`（`apply_migrations()` 末尾 + private fn 追加 + テスト）

**Interfaces:**
- Produces: `Config::apply_migrations()` 通過後の `config.instant_commands` に重複 name が存在しない不変条件（IPC `get_instant_commands`/`execute_instant_command` と egui `run_search_with`/`execute_instant_selected` の first-match `find` が表示と常に一致する）

- [ ] **Step 1: 失敗するテストを書く**

`snotra-core/src/config.rs` のテストモジュール（既存 `cfg_with_instant` ヘルパー・`InstantAction` serde gate テスト群の近く）へ追加。`InstantAction` のバリアント構築は同モジュールの既存テスト（`instant_commands` を組み立てているもの）の書式をそのまま流用する:

```rust
#[test]
fn dedup_instant_commands_first_wins_and_keeps_order() {
    // #638: 重複名は先勝ち（実行時 first-match と同じ行が残る＝挙動変化なし）。
    let mut config = Config::normalized_default();
    config.instant_commands = vec![
        InstantCommand {
            name: "gh".into(),
            description: String::new(),
            action: InstantAction::Url { url: "https://github.com/{q}".into() },
        },
        InstantCommand {
            name: "gh".into(),
            description: String::new(),
            action: InstantAction::Url { url: "https://example.com/{q}".into() },
        },
        InstantCommand {
            name: "g".into(),
            description: String::new(),
            action: InstantAction::Url { url: "https://google.com/{q}".into() },
        },
    ];
    config.apply_migrations();
    assert_eq!(config.instant_commands.len(), 2);
    assert_eq!(config.instant_commands[0].name, "gh");
    assert!(
        matches!(&config.instant_commands[0].action, InstantAction::Url { url } if url.contains("github")),
        "先頭定義（github）が残る"
    );
    assert_eq!(config.instant_commands[1].name, "g");
}

#[test]
fn dedup_instant_commands_does_not_flag_changed() {
    // 決定 2: dedup は changed に寄与しない（true だと load が config.toml へ書き戻し、
    // ユーザーの手編集行を消す・config.rs load_from_dir_reporting）。
    let mut config = Config::normalized_default(); // migration 済み＝以後の changed は新規要因のみ
    config.instant_commands = vec![
        InstantCommand {
            name: "gh".into(),
            description: String::new(),
            action: InstantAction::Url { url: "https://github.com/{q}".into() },
        },
        InstantCommand {
            name: "gh".into(),
            description: String::new(),
            action: InstantAction::Url { url: "https://example.com/{q}".into() },
        },
    ];
    assert!(!config.apply_migrations());
    assert_eq!(config.instant_commands.len(), 1);
}
```

（`InstantAction::Url { url: String }` は config.rs:76-84 の実定義と一致することを確認済み）

- [ ] **Step 2: 落ちることを確認する（RED）**

Run: `cargo test -p snotra-core dedup_instant`
Expected: FAIL（`len()` が 3 のまま／`apply_migrations` に dedup が無い）

- [ ] **Step 3: 最小実装**

`config.rs` の `apply_migrations()` 実装（674 行付近）の `fallback_hotkey_if_system_shortcut()` 行の直後・`changed` return の前に呼び出しを追加し、private fn を migration 系列の隣に置く:

```rust
        changed |= self.fallback_hotkey_if_system_shortcut(); // system shortcut 検出時のデフォルト復帰
        self.dedup_instant_command_names(); // #638: 意図的に changed へ寄与しない（下記 doc）
```

```rust
    /// #638: 重複名 instant コマンドの先勝ち正規化（in-memory のみ）。実行時解決は
    /// first-match（`commands/instant.rs` / egui `execute_instant_selected` の `find`）のため、
    /// 先頭を残せば実行される action は従来と同一で、候補リストの表示だけが実行と一致する。
    /// 設定 UI は保存時に重複を拒否する（`validate`）ため、ここに来るのは手編集 config のみ。
    /// **意図的に `changed` へ寄与しない**——`load_from_dir_reporting` は changed=true で
    /// `save_to_dir` するため、寄与させるとユーザーの手編集行（重複定義）をファイルから
    /// 消してしまう（spec 2026-07-23 決定 2: 書き戻し禁止）。
    fn dedup_instant_command_names(&mut self) {
        let mut seen = std::collections::HashSet::new();
        self.instant_commands.retain(|c| {
            if c.name.is_empty() {
                return true; // 空名は dedup 対象外（検出=validate / 補正=migration の責務分離。
                             // validate の !name.is_empty() エラーを migration が隠さない）
            }
            let keep = seen.insert(c.name.clone());
            if !keep {
                eprintln!(
                    "[config] duplicate instant command name '{}' ignored (first definition wins)",
                    c.name
                );
            }
            keep
        });
    }
```

- [ ] **Step 4: 統合テストを追加する（load がファイルを書き戻さないことの直接検証）**

同テストモジュールへ追加（`load_from_dir_reporting` は同モジュール内から呼べる private・doc に「config_dir を注入可能にし統合テストする」）:

```rust
#[test]
fn dedup_load_does_not_rewrite_config_file() {
    // 決定 2 の直接検証: 重複入り TOML を load してもメモリ上のみ dedup され、
    // config.toml のバイト列は不変（ユーザーの手編集行を消さない）。
    let dir = std::env::temp_dir().join(format!("snotra-dedup-{}", std::process::id()));
    std::fs::create_dir_all(&dir).unwrap();
    let path = dir.join("config.toml");
    let toml_str = r#"
[hotkey]
modifier = "Alt"
key = "Q"

[appearance]
window_width = 600
visible_rows = 10

[[instant_commands]]
name = "gh"
url = "https://github.com/{q}"

[[instant_commands]]
name = "gh"
url = "https://example.com/{q}"
"#;
    std::fs::write(&path, toml_str).unwrap();
    let (config, _) = Config::load_from_dir_reporting(&dir);
    assert_eq!(config.instant_commands.len(), 1);
    assert_eq!(
        std::fs::read_to_string(&path).unwrap(),
        toml_str,
        "load が config.toml を書き戻していない"
    );
    let _ = std::fs::remove_dir_all(&dir);
}
```

（TOML の最小形は既存テスト `apply_migrations_leaves_valid_fuzzy_history_cap_ratio_unchanged`（config.rs:1762 付近）の「他の移行項目が無い最小 TOML では changed=false」パターンを踏襲。instant の TOML キーは untagged `InstantAction::Url` ゆえ `url =`）

- [ ] **Step 5: 通ることを確認する（GREEN）**

Run: `cargo test -p snotra-core dedup_`
Expected: PASS（3 tests）。続けて `cargo test -p snotra-core` で既存全テストの無回帰を確認（特に `validate` / instant serde gate / `normalized_default` 系）

- [ ] **Step 6: SPEC §19.2 へ一行追記（仕様変更の文書同期・AGENTS.md Step 0）**

`SPEC.md` §19.2（設定構造）の末尾へ追加:

```markdown
- 同名エントリが複数ある場合、ロード時に先勝ちで正規化する（後方の重複は無視・stderr 警告。`config.toml` 自体は書き換えない）。設定画面は保存時に重複名を拒否する
```

Run: `npm run governance:check` → G1..G10 PASS

- [ ] **Step 7: コミット**

```bash
git add snotra-core/src/config.rs SPEC.md
git commit -m "fix(#638): instant 重複名を config ロード時に先勝ち正規化（in-memory・書き戻しなし）"
```

---

### Task 2: 状態核 — ToolFrame + 2 スロット射影（search_state.rs）

**Files:**
- Modify: `src-tauri/src/egui_shell/search_state.rs`（`ViewKind::Tool`・`ToolFrame`・`SearchState` 拡張 + テスト）
- Modify: `src-tauri/src/egui_shell/view.rs`（`ViewKind::Tool` 追加に伴う compile-green スタブ 3 箇所のみ。実挙動は Task 3）

**Interfaces:**
- Consumes: `snotra_core::config::OpenerTool { name, exe, args }`・`snotra_core::ui_types::SearchResult { name, path, is_folder, is_error }`
- Produces（Task 3 が依存する正確な署名）:
  - `pub enum ViewKind { Results, Folder, Tool }`
  - `pub struct ToolFrame { pub restore_results: Vec<SearchResult>, pub restore_selected: usize, pub target_path: String, pub target_is_folder: bool, pub tools: Vec<OpenerTool>, pub launch_query: String, pub saved_folder_filter: String }`（restore_query は持たない——SolidJS `popView` の tool 段は query を復元せず、tool 中は入力無効ゆえ query 不変。plan-review 独立導出で確定）
  - `SearchState::enter_tool(&mut self, target_path: String, target_is_folder: bool, tools: Vec<OpenerTool>)`
  - `SearchState::tool_frame(&self) -> Option<&ToolFrame>`
  - `EscapeOutcome::RestoredFromTool`（新バリアント）
  - `SearchState::reset()` が tool もクリア／`accept_folder_result` が tool 中 false

- [ ] **Step 1: 失敗するテストを書く**

`search_state.rs` のテストモジュールへ追加（既存の folder 系テストの隣。`SearchResult` 生成は既存テストのヘルパー/リテラル書式を流用）:

```rust
fn make_tools() -> Vec<OpenerTool> {
    vec![
        OpenerTool { name: "VSCode".into(), exe: "Code.exe".into(), args: String::new() },
        OpenerTool { name: "Terminal".into(), exe: "wt.exe".into(), args: "-d {path}".into() },
    ]
}

#[test]
fn enter_tool_from_results_saves_and_replaces_rows() {
    let mut s = SearchState::new();
    s.set_query("code".into());
    s.set_results(vec![SearchResult {
        name: "proj".into(), path: "C:\\proj".into(), is_folder: true, is_error: false,
    }]);
    s.enter_tool("C:\\proj".into(), true, make_tools());
    assert_eq!(s.view_kind(), ViewKind::Tool);
    // 行はツール一覧（name=表示名・path=exe・§18.4）
    assert_eq!(s.results().len(), 2);
    assert_eq!(s.results()[0].name, "VSCode");
    assert_eq!(s.results()[0].path, "Code.exe");
    assert!(!s.results()[0].is_folder);
    assert_eq!(s.selected(), 0);
    let f = s.tool_frame().expect("frame");
    assert_eq!(f.target_path, "C:\\proj");
    assert!(f.target_is_folder);
    assert_eq!(f.launch_query, "code");
}

#[test]
fn escape_from_tool_restores_results_view() {
    let mut s = SearchState::new();
    s.set_query("code".into());
    s.set_results(vec![SearchResult {
        name: "proj".into(), path: "C:\\proj".into(), is_folder: true, is_error: false,
    }]);
    s.enter_tool("C:\\proj".into(), true, make_tools());
    assert_eq!(s.on_escape(), EscapeOutcome::RestoredFromTool);
    assert_eq!(s.view_kind(), ViewKind::Results);
    assert_eq!(s.results()[0].name, "proj");
    assert_eq!(s.query(), "code");
}

#[test]
fn escape_ladder_tool_then_folder_then_hide() {
    // §18.4: tool → folder 復帰（filter 込み）→ results → hide の全段
    let mut s = SearchState::new();
    s.set_query("pre".into());
    s.set_results(vec![SearchResult {
        name: "dir".into(), path: "C:\\dir".into(), is_folder: true, is_error: false,
    }]);
    s.enter_folder("C:\\dir".into());
    s.set_folder_filter("fil".into());
    s.set_results(vec![SearchResult {
        name: "child".into(), path: "C:\\dir\\child".into(), is_folder: false, is_error: false,
    }]);
    s.enter_tool("C:\\dir\\child".into(), false, make_tools());
    assert_eq!(s.view_kind(), ViewKind::Tool);

    assert_eq!(s.on_escape(), EscapeOutcome::RestoredFromTool);
    assert_eq!(s.view_kind(), ViewKind::Folder); // folder が下に残っている（§18.5 直交）
    assert_eq!(s.folder_filter(), "fil"); // saved_folder_filter 復元
    assert_eq!(s.results()[0].name, "child"); // folder のフィルタ済みビュー復元

    assert_eq!(s.on_escape(), EscapeOutcome::RestoredSearch);
    assert_eq!(s.view_kind(), ViewKind::Results);
    assert_eq!(s.query(), "pre");

    assert_eq!(s.on_escape(), EscapeOutcome::Hide);
}

#[test]
fn reset_clears_tool_slot() {
    let mut s = SearchState::new();
    s.set_results(vec![SearchResult {
        name: "f".into(), path: "C:\\f".into(), is_folder: false, is_error: false,
    }]);
    s.enter_tool("C:\\f".into(), false, make_tools());
    s.reset(); // §18.5: ホットキー再表示（resetForShow）でツール選択はリセット
    assert_eq!(s.view_kind(), ViewKind::Results);
    assert!(s.tool_frame().is_none());
}

#[test]
fn interp_during_tool_is_plain() {
    // §18.5 入力無効の状態面: tool 中はどんな query でも Plain（instant/command 化しない）
    let mut s = SearchState::new();
    s.set_results(vec![SearchResult {
        name: "f".into(), path: "C:\\f".into(), is_folder: false, is_error: false,
    }]);
    s.enter_tool("C:\\f".into(), false, make_tools());
    s.set_query("@gh".into());
    assert_eq!(s.interp("@"), QueryIntent::Plain);
    s.set_query("/r".into());
    assert_eq!(s.interp("@"), QueryIntent::Plain);
}

#[test]
fn folder_results_rejected_while_tool_is_open() {
    // §18.5「ツール選択中の検索結果が上書きされない」: tool 中に遅延到着した folder ナビ
    // 結果（dead/slow UNC）を drain が受理してツール一覧を潰さない。
    let mut s = SearchState::new();
    let tok = s.enter_folder("C:\\slow".into());
    s.enter_tool("C:\\slow\\x".into(), false, make_tools());
    assert!(!s.accept_folder_result(tok));
    s.on_escape(); // tool 解除 → folder 復帰
    assert!(s.accept_folder_result(tok)); // folder に戻れば同 token は再び有効（gen は進めていない）
}
```

- [ ] **Step 2: 落ちることを確認する（RED）**

Run: `cargo test -p snotra egui_shell::search_state`
Expected: FAIL（`ViewKind::Tool` / `ToolFrame` / `enter_tool` 未定義のコンパイルエラー）

- [ ] **Step 3: 実装**

`search_state.rs`:

(a) `ViewKind` に `Tool` を追加（enum 定義・8 行付近）:

```rust
pub enum ViewKind {
    Results,
    Folder,
    Tool,
}
```

(b) import に `OpenerTool` を追加（既存 `use snotra_core::ui_types::SearchResult;` の隣）:

```rust
use snotra_core::config::OpenerTool;
```

(c) `FolderFrame` の下に `ToolFrame` を定義:

```rust
/// ツール選択モードの退避/復元単位（#532 SU3.5・SolidJS ToolSelectionFrame parity）。
/// tool は folder の上に積まれうる（§18.5 直交）が、tool-on-tool は Option ゆえ表現不能。
/// restore_query を持たない——tool 中は入力無効（§18.5）で query 不変ゆえ復元不要
/// （SolidJS popView の tool 段も query を復元しない）。launch_query は起動 API へ渡す
/// 元クエリで復元には使わない（SolidJS #538 の launchQuery / restoreQuery 型分離）。
#[derive(Debug, Clone)]
pub struct ToolFrame {
    pub restore_results: Vec<SearchResult>,
    pub restore_selected: usize,
    pub target_path: String,
    pub target_is_folder: bool,
    pub tools: Vec<OpenerTool>,
    pub launch_query: String,
    pub saved_folder_filter: String,
}
```

(d) `EscapeOutcome` に追加:

```rust
    /// tool → 直下ビュー（folder/results）へ復帰済み（driver は folder cache を保持したまま
    /// repaint のみ。RestoredSearch と違い cache/error を破棄しない——folder が下に生きている）
    RestoredFromTool,
```

(e) `SearchState` に field `tool: Option<ToolFrame>` を追加し `new()` で `None` 初期化。

(f) `view_kind()` を射影に変更:

```rust
    /// 軸1: モーダルビュー頂点の射影（§18.5 優先度 tool > folder > results と一対一）。
    pub fn view_kind(&self) -> ViewKind {
        if self.tool.is_some() {
            ViewKind::Tool
        } else if self.folder.is_some() {
            ViewKind::Folder
        } else {
            ViewKind::Results
        }
    }
```

(g) `enter_tool` / `tool_frame` を `enter_folder` 群の隣に追加:

```rust
    /// ツール選択へ突入（§18.4: 結果リストをツール一覧で置換）。現在ビュー（Results/Folder
    /// どちらでも）の表示状態を frame へ退避する。tools ≥ 2 の判定は driver 側
    /// （§18.3: ≤1 は通常 Enter と同一のため、そもそも呼ばれない）。
    pub fn enter_tool(&mut self, target_path: String, target_is_folder: bool, tools: Vec<OpenerTool>) {
        let rows: Vec<SearchResult> = tools
            .iter()
            .map(|t| SearchResult {
                name: t.name.clone(),
                path: t.exe.clone(),
                is_folder: false,
                is_error: false,
            })
            .collect();
        self.tool = Some(ToolFrame {
            restore_results: std::mem::take(&mut self.results),
            restore_selected: self.selected,
            target_path,
            target_is_folder,
            tools,
            launch_query: self.query.clone(),
            saved_folder_filter: self.folder_filter.clone(),
        });
        self.results = rows;
        self.selected = 0;
    }

    pub fn tool_frame(&self) -> Option<&ToolFrame> {
        self.tool.as_ref()
    }
```

(h) `on_escape()` の先頭に tool 段を追加（folder 段より前・§18.5 優先度）:

```rust
    pub fn on_escape(&mut self) -> EscapeOutcome {
        if let Some(t) = self.tool.take() {
            // query は復元しない（tool 中は入力無効で不変・ToolFrame doc 参照）
            self.results = t.restore_results;
            self.selected = clamp_selected(self.results.len(), t.restore_selected);
            self.folder_filter = t.saved_folder_filter;
            return EscapeOutcome::RestoredFromTool;
        }
        // …既存の folder 段・Hide 段は無変更…
    }
```

(i) `reset()` に `self.tool = None;` を追加（`self.folder = None;` の隣）。

(j) `accept_folder_result` を tool ガード付きに:

```rust
    /// 遅延到着したナビ結果を受理してよいか（tool 非表示 ∧ token 一致 ∧ folder 中）。
    /// tool 中は §18.5「検索結果が上書きされない」ため受理しない（driver は破棄でなく
    /// 保留せず捨てる——escape で folder へ戻れば次の打鍵/ナビが再ロードする）。
    pub fn accept_folder_result(&self, token: u64) -> bool {
        self.tool.is_none() && token == self.folder_gen && self.folder.is_some()
    }
```

(k) **view.rs の compile-green スタブ**（`ViewKind::Tool` 追加で壊れる網羅 match 3 箇所 + `EscapeOutcome::RestoredFromTool` 追加で壊れる網羅 match 1 箇所＝**計 4 箇所**。ここを塞がないと Task 2 のコミット時点で crate がビルドできない——plan-review scout-egui 指摘。ViewKind 3 箇所の実挙動は Task 3 で確定、Escape arm は最終形をここで先出しする）:

- `run_search_with` の `match self.state.view_kind()` に arm 追加:

```rust
            ViewKind::Tool => {
                // §18.5: ツール選択中は検索結果を上書きしない（trailing/changed とも no-op）
            }
```

- ArrowRight ブロック（629 行付近）: if 条件の先頭に Tool 除外を追加し、内側の `match` を if/else に変える:

```rust
        if ctx.input(|i| i.key_pressed(egui::Key::ArrowRight))
            && self.state.view_kind() != ViewKind::Tool // §18.5 ←→無効
            && let Some(sel) = self.state.results().get(self.state.selected())
            && sel.is_folder
            && !sel.is_error
        {
            let dir = sel.path.clone();
            let tok = if self.state.view_kind() == ViewKind::Folder {
                self.state.navigate_folder(dir.clone())
            } else {
                self.state.enter_folder(dir.clone())
            };
            // （以降の record_folder_expansion / spawn_folder_load は無変更）
```

- ArrowLeft ブロック（647 行付近）の `match self.state.view_kind()` に arm 追加:

```rust
                ViewKind::Tool => {} // §18.5 ←→無効
```

- Escape の `match self.state.on_escape()`（589 行付近）に arm 追加（**最終形・Task 3 での再変更なし**）:

```rust
                EscapeOutcome::RestoredFromTool => {
                    // tool 解除 → 直下ビュー（folder/results）を復元描画。folder が下に生きて
                    // いるため cache/error は破棄しない（RestoredSearch との差・純粋核 doc 参照）
                    ctx.request_repaint();
                }
```

(l) **doc comment の as-built 同期**（scout-egui 指摘・#562「//! が正本」）: `ViewKind` の doc（search_state.rs:6 付近「tool は SU3.5 予定」）と `EscapeOutcome` の doc（「M2 は folder 段と top-level のみ」）を 3 段ラダーの現実へ書き換える。`SearchState` struct doc（110 行付近）にも tool 軸を一言追記。

（mod.rs の re-export 追加は不要見込み——view.rs は `ToolFrame` を型名で名指しせず `tool_frame()` アクセサ越しに使う。コンパイルが最終判定）

- [ ] **Step 4: 通ることを確認する（GREEN）**

Run: `cargo test -p snotra egui_shell::search_state`
Expected: PASS（新 6 テスト + 既存 folder/escape/reset 系全部）。`cargo clippy -p snotra` も警告 0（hook 沈黙で確認可）

- [ ] **Step 5: コミット**

```bash
git add src-tauri/src/egui_shell/search_state.rs src-tauri/src/egui_shell/view.rs
git commit -m "feat(#532): SU3.5 状態核 — ToolFrame + 2 スロット射影・Escape 2 段復帰・drain ガード"
```

---

### Task 3: driver — Shift+Enter 入場・ツール起動・入力無効化・activate のオープナー適用

**Files:**
- Modify: `src-tauri/src/commands/launch.rs:171`（`launch_with_tool_core` を `pub(crate)` 化のみ）
- Modify: `src-tauri/src/egui_shell/view.rs`（入場/起動/ガード/Escape arm/TextEdit）

**Interfaces:**
- Consumes: Task 2 の `enter_tool` / `tool_frame` / `EscapeOutcome::RestoredFromTool`、`snotra_core::config::{find_matching_tools, OpenerTool}`、`crate::commands::launch::{launch_with_tool_core, launch_item_core, record_and_save, LaunchStatus}`
- Produces: egui 経路の §18.3（通常 Enter=先頭ツール・Shift+Enter=メニュー/同一動作）・§18.4（メニュー・Escape 復帰・行 index 起動）・§18.5（ガード群）の全実装

- [ ] **Step 1: `launch_with_tool_core` を `pub(crate)` 化**

`src-tauri/src/commands/launch.rs:171`:

```rust
pub(crate) fn launch_with_tool_core(path: &str, exe: &str, args: &str) -> LaunchResult {
```

（`launch_item_core` と同じ可視性。egui driver が IPC `launch_with_tool` と同じ core を共有する——drift 防止・M1 finding 5 の規律）

- [ ] **Step 2: view.rs — import と `resolve_tools` ヘルパー**

import へ追加（`use snotra_core::ui_types::SearchResult;` の隣）:

```rust
use snotra_core::config::{OpenerTool, find_matching_tools};
```

`record_folder_expansion` の隣にヘルパーを追加:

```rust
    /// 選択行のオープナー解決（§18.3 最具体ルール 1 件の tools）。IPC/トレイと同じ core
    /// `find_matching_tools` を共有（drift 防止）。is_folder は行（index の真実）から渡し、
    /// `resolve_all_openers` の `Path::is_dir` 再判定（fs touch・dead UNC で滞留しうる）を
    /// egui 経路では踏まない。lock は解決の間だけ保持（ロック内純 CPU → clone）。
    fn resolve_tools(&self, path: &str, is_folder: bool) -> Vec<OpenerTool> {
        let Some(state) = self.app_handle.try_state::<crate::AppState>() else {
            return Vec::new();
        };
        let engine = state.engine.lock().unwrap();
        find_matching_tools(path, is_folder, &engine.config().openers).to_vec()
    }
```

- [ ] **Step 3: `activate()` にオープナー適用（§18.3 通常 Enter＝先頭ツール・既存 parity gap 是正）**

`activate()`（139 行付近）の起動部を変更。`let path = result.path.clone();` の隣に `let is_folder = result.is_folder;` を追加し、`let outcome = launch_item_core(&path);` を以下へ置換:

```rust
        let tools = self.resolve_tools(&path, is_folder);
        let outcome = if let Some(first) = tools.first() {
            // §18.3 全起動経路統一: マッチしたルールの先頭ツールで起動（IPC launch_item の
            // resolve_opener 分岐と同型。M1 はここを素の ShellExecuteW にしていた＝parity gap）
            launch_with_tool_core(&path, &first.exe, &first.args)
        } else {
            launch_item_core(&path) // ルール無し: 従来どおり ShellExecuteW フォールバック
        };
```

import 行（`use crate::commands::launch::{...}`）に `launch_with_tool_core` を追加。既存 trace `egui_launch` の json に `"opener": !tools.is_empty()` を追加。

- [ ] **Step 4: Enter dispatch の Shift 分岐 + `shift_activate` / `execute_tool_selected`**

Enter 判定（760 行付近）を置換:

```rust
        // Enter: 選択項目を起動/実行（Shift は §18.3 のツール選択入場・後置 dispatch は M3 のまま）。
        let (enter_pressed, shift_held) =
            ctx.input(|i| (i.key_pressed(egui::Key::Enter), i.modifiers.shift));
        if enter_pressed && !self.state.results().is_empty() {
            if shift_held {
                self.shift_activate(self.state.selected());
            } else {
                self.activate_or_execute(self.state.selected());
            }
        }
```

`activate_or_execute`（275 行付近）の先頭に Tool 分岐を追加:

```rust
    fn activate_or_execute(&mut self, index: usize) {
        if self.state.view_kind() == ViewKind::Tool {
            self.execute_tool_selected(index); // §18.4 Enter/クリック＝選択ツールで起動
        } else if let Some(iq) = self.instant_rows_query.clone() {
            self.execute_instant_selected(index, &iq);
        } else {
            self.activate(index);
        }
    }
```

新規 2 関数を `activate_or_execute` の隣へ:

```rust
    /// Shift+Enter（§18.3）: 選択行の tools ≥ 2 ならツール選択メニューへ、それ以外
    /// （≤1・instant 行・tool ビュー中）は通常 Enter と同一（hide も同様）。folder ロード
    /// 未確定窓は activate と同じ理由で入場もしない（stale 行からの解決防止・#636 Finding A）。
    fn shift_activate(&mut self, index: usize) {
        if self.instant_rows_query.is_some() || self.state.view_kind() == ViewKind::Tool {
            // instant 行は §19.6「Shift+Enter=Enter」。tool ビュー中の Shift+Enter も Enter と同一。
            self.activate_or_execute(index);
            return;
        }
        if folder_load_pending(
            self.state.view_kind(),
            self.folder_cache.is_some(),
            self.folder_error.is_some(),
        ) {
            return;
        }
        let Some(row) = self.state.results().get(index) else { return };
        if row.is_error {
            return;
        }
        let (path, is_folder) = (row.path.clone(), row.is_folder);
        let tools = self.resolve_tools(&path, is_folder);
        if tools.len() >= 2 {
            // armed trailing がツール一覧を上書きしないよう掃除（finding 6 と同じ債務）
            self.search_debounce.cancel();
            self.state.enter_tool(path, is_folder, tools);
            if let Some(f) = self.state.tool_frame() {
                crate::trace_main(
                    "egui_tool_enter",
                    serde_json::json!({
                        "target_is_folder": f.target_is_folder,
                        "tools": f.tools.len(),
                    }),
                );
            }
        } else {
            self.activate_or_execute(index); // §18.3: 1 件以下は通常 Enter と同じ動作
        }
    }

    /// ツール選択中の起動（§18.4）。行 index で tools を照合（同一 exe でも引数違いを区別・
    /// パス文字列照合は禁止＝ui ルールと同根）。起動は launch_with_tool_core の同期直呼び
    /// （M1 activate と同型の意図的残余・#631 で async 化とセット解消）。成功時は IPC
    /// `launch_with_tool` と同じく launch_query で履歴記録 → 全クリア + hide（§19.6 instant
    /// の完了列と同型。reset は tool/folder/gen 込みで in-flight folder ロードも失効させる）。
    fn execute_tool_selected(&mut self, index: usize) {
        use crate::commands::launch::{LaunchStatus, launch_with_tool_core, record_and_save};
        let Some((target_path, launch_query, tool)) = self.state.tool_frame().and_then(|f| {
            f.tools
                .get(index)
                .map(|t| (f.target_path.clone(), f.launch_query.clone(), t.clone()))
        }) else {
            return;
        };
        let outcome = launch_with_tool_core(&target_path, &tool.exe, &tool.args);
        crate::trace_main(
            "egui_tool_launch",
            serde_json::json!({ "index": index, "status": format!("{:?}", outcome.status) }),
        );
        if matches!(outcome.status, LaunchStatus::Ok) {
            if let Some(state) = self.app_handle.try_state::<crate::AppState>() {
                record_and_save(&state, &target_path, &launch_query);
            }
            self.clear_search();
            self.state.reset();
            self.emit_hide();
        }
    }
```

- [ ] **Step 5: TextEdit の無効化（§18.5 入力無効・ファイル名表示・hint 切替）**

TextEdit ブロック（683 行付近）: `in_folder` の隣に `in_tool` を導出し、hint / buf / add を変更（Escape arm は Task 2 で最終形を追加済み・本 Step では触らない）:

```rust
        let in_tool = self.state.view_kind() == ViewKind::Tool;
        let in_folder = self.state.view_kind() == ViewKind::Folder;
        let hint: &str = if in_tool {
            // SolidJS placeholder.tool_select parity（egui の hint は buf が空のときだけ描かれる＝
            // HTML placeholder と同条件。表示されるのは対象パスが区切り終端等でファイル名が空のとき）
            "ツールを選択…"
        } else if !in_folder && self.indexing() && self.state.query().trim().is_empty() {
            "インデックス構築中..."
        } else {
            "検索…"
        };
```

```rust
        let mut buf = if in_tool {
            // §18.5: 対象の**ファイル名部分のみ**を表示——SolidJS inputValue は targetPath を
            // 区切りで split した末尾を返す（SearchWindow.tsx:255-267）。フルパスではない
            //（plan-review scout-parity 指摘で是正）。
            self.state
                .tool_frame()
                .map(|f| {
                    f.target_path
                        .rsplit(['\\', '/'])
                        .next()
                        .unwrap_or("")
                        .to_string()
                })
                .unwrap_or_default()
        } else if in_folder {
            self.state.folder_filter().to_string()
        } else {
            self.state.query().to_string()
        };
        let response = ui.add(
            egui::TextEdit::singleline(&mut buf)
                // §18.5 ツール選択中の入力は無効化。add_enabled（全体グレーアウト）でなく
                // interactive(false)（通常描画のまま読み取り専用・changed 不発火）——外観維持。
                .interactive(!in_tool)
                .hint_text(hint)
                .desired_width(f32::INFINITY),
        );
```

auto-focus 行（738 行付近）へ in_tool 除外を追加（非対話ウィジェットへ focus 要求しない）:

```rust
        if focused && !in_tool && !response.has_focus() {
            response.request_focus();
        }
```

- [ ] **Step 5b: doc comment の as-built 同期**

- `view.rs` の `//!`（1-4 行）の責務列挙に「tool 選択（§18）」を追記（#562: //! が責務の正本）
- `clear_search` の doc（171-174 行付近）「第 3 のクリアサイト（SU3.5 tool 等）」→ 実装済みの現実（execute_tool_selected が使用）へ更新
- `activate_or_execute` の doc（273-274 行付近）「tool-selection は SU3.5」→ 削除し Shift 分岐の現実を一行で
- `commands/launch.rs` の `record_and_save` doc（80-84 行付近）の呼び出し元列挙に egui tool 起動経路を追記

- [ ] **Step 6: ビルドとテストの GREEN 確認**

Run: `cargo clippy -p snotra --all-targets` → 警告 0、`cargo test -p snotra` → 全 PASS（hook 沈黙でも可）
Expected: PASS。既存テスト（search_state の folder/escape 系・lifecycle）が無回帰

- [ ] **Step 7: コミット**

```bash
git add src-tauri/src/commands/launch.rs src-tauri/src/egui_shell/view.rs
git commit -m "feat(#532): SU3.5 driver — Shift+Enter 入場・ツール起動・入力無効化・activate のオープナー適用"
```

---

### Task 4: 統合検証 — GUI smoke + governance

**Files:**
- Modify: `src-tauri/CLAUDE.md`（commands/ 節の `launch_with_tool_core` 追記のみ。他は検証のみ——乖離が出た場合のみ SPEC §18 に as-built 注記）

- [ ] **Step 1: ワークスペース全テスト**

Run: `cargo test --workspace` → 全 PASS

- [ ] **Step 2: GUI smoke（実機・`SNOTRA_EGUI_MAIN=1 SNOTRA_TRACE=1 cargo run -p snotra --release`）**

config.toml に一時的に 2 ツール以上の `[[openers]]` ルール（例: `target = "folder"` に VSCode + Explorer）がある状態で:

- [ ] Results で folder 行を選択 → Shift+Enter → ツール一覧表示（`egui_tool_enter` trace・行 = name + exe 淡色）
- [ ] ↑↓ でツール選択 → Enter → 選択ツールで起動 + hide（`egui_tool_launch` trace・履歴に記録）
- [ ] ツール行クリック → 同上（行 index 照合）
- [ ] folder 展開（→）→ フィルタ入力 → Shift+Enter → tool → Escape → **folder ビューにフィルタ・結果・選択が復元**（2 段復帰）→ Escape → results → Escape → hide
- [ ] ツール選択中: 入力欄が target_path 表示 + 入力不可・←→無反応・↑↓は動く
- [ ] Alt+Q hide → 再表示 → ツール選択が消えて素の検索（reset-on-show）
- [ ] 通常 Enter（Shift なし）で folder 行 → **先頭ツールで起動**（§18.3 適用の是正確認）。ルール無しファイルは従来どおり既定プログラム
- [ ] instant（`@`）候補行で Shift+Enter → 通常実行（§19.6）
- [ ] flag OFF（env 無し）で従来 WebView2 経路が不変（起動・Shift+Enter ツール選択の既存動作）

- [ ] **Step 3: governance + 索引**

新規ファイル無し（索引更新不要）。`src-tauri/CLAUDE.md` モジュール構成の egui_shell 行は責務散文が `//!` 正本のため変更不要。ただし **commands/ 節の一行は更新する**（scout-egui 指摘・commands/ は「薄いラッパー集約記述の例外」として CLAUDE.md 直書きの場所）——「`launch.rs` は `launch_item_core`（`pub(crate)`、`instant.rs` から再利用）に加え…」の記述へ `launch_with_tool_core`（`pub(crate)`・`egui_shell/view.rs` から再利用）を追記。

Run: `npm run governance:check` → G1..G10 PASS

- [ ] **Step 4: コミットと push**

```bash
git add src-tauri/CLAUDE.md
git commit -m "docs: launch_with_tool_core の共有サイト追記（#532 SU3.5）"
git push -u origin feat/532-su3.5-tool-selection
```

（PR 作成はレビュー（/code-review 系）後・controller が実施。PR 本文に closing keyword を書かない——#638 を閉じるかは merge 時に closingIssuesReferences で確認）
