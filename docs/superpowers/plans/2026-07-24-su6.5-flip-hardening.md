# SU6.5 flip 前ハードニング 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** flip（SU7）の基準 2・3・5 を数字と目視に接地させ、その前提として残っている parity gap 3 件（#643 / #648 / #652）を潰す。

> **実行中に追加された Task 7・8**（計測が発見した gap の修正・as-built）:
> - **Task 7**: G1 メモリ実測で egui-hidden が trim されず可視時同値（43.4 MiB）のままだった。`hide_egui_main` が WebView2 の `suspend_and_trim_after_hide` にある `EmptyWorkingSet` trim を呼んでいなかったのが原因 → `crate::working_set::trim_idle_working_set(std::process::id())` を配線（再測で 0.4/0.9 MiB へ落ち基準 3 合格）。
> - **Task 8**: G2 外観目視で結果リストが 8 行表示中に早期自動スクロールした。`scroll_to_me(Some(Align::Center))`（中央維持）を `scroll_to_me(None)`（WebView2 `scrollIntoView({block:"nearest"})` parity・最小可視化）へ。
> どちらも task-review + 最終 whole-branch review 延長（64e9348..ab77b41・opus）を通過・スモーク合格。

**Architecture:** 実装 3 件を先に通してから release を焼き、計測ゲート G1〜G4 を一項目ずつ回す。#652 は「pending 格納 → `show_egui_main` → `wake_view`」の 3 段順序で解く（SU6 の変更失敗経路が *wake しない* のとは逆——起動時失敗には `config-applied` が随伴しないため）。#643 は WebView2 の CSS が `--search-bar-height: 52px` 固定である事実に基づき「バー据え置き + 入力欄フォントのみ追従」で閉じる。

**Tech Stack:** Rust（Tauri v2 / egui 0.35 / softbuffer）・PowerShell 7（計測）・既存 `SNOTRA_TRACE` 構造化トレース。

## Global Constraints

- **spec**: `docs/superpowers/specs/2026-07-24-su6.5-flip-hardening-design.md`（決定 1〜7 が本計画の根拠）
- **ブランチ**: `feat/532-su6.5-flip-hardening`（作成済み・spec コミット 2 本が既に載っている）。**`main` へ直接コミットしない**
- **コミットメッセージに bash HEREDOC を使わない**。一時ファイルに書いて `git commit -F <tmpfile>`（ルート `CLAUDE.md`「シェル環境」）
- **egui 経路の起動は `SNOTRA_EGUI_MAIN=1; cargo run -p snotra`**。`cargo run`（`-p` なし）は Phase 1 スパイク `snotra-egui-mvp` が立ち上がり SU 実装を含まない（SU4 で実際に誤った）
- **i18n 文言は `ui/src/lib/i18n.ts` の実物を開いて codepoint 単位で転記する**。計画書・レビュー引用の文字列を正本にしない（`strings.rs` の `//!` 規範・SU5/SU6 で 2 度捕捉された）
- **`*.rs` を編集すると PostToolUse hook が clippy + 当該 crate のテストを自動実行する。沈黙 = 合格**（失敗時のみ会話に届く）
- **LSP diagnostics は編集途中の stale スナップショットを出す**。DONE 判断は `cargo build` / `cargo test` の実行結果で接地する（SU4 で毎タスク再現）
- 検証コマンドの SSOT は `docs/build-commands.md`

## File Structure

| ファイル | 変更 | 責務 |
|---|---|---|
| `src-tauri/src/egui_shell/view.rs` | 修正 | TextEdit のフォント/hint 色（#643）・`start_launch` の wake（#648 A）・pending 消費の kind 分岐（#652） |
| `src-tauri/src/egui_shell/notify.rs` | 修正 | `InstallFailed` / `ToastKind::Failed` の dead payload 削除（#648 C） |
| `src-tauri/src/egui_shell/mod.rs` | 修正 | `HotkeyFailureKind` 追加・pending 型変更・`register_platform_event_listener` 新設・mode fallback（#648 F） |
| `src-tauri/src/egui_shell/strings.rs` | 修正 | `hotkey_initial_failed` 追加（i18n parity テスト付き） |
| `src-tauri/src/main.rs` | 修正 | 新 listener の登録（egui ブロック内・`setup_hotkey_listener` より前） |
| `src-tauri/src/platform/mod.rs` | 修正 | `SNOTRA_FAKE_INITIAL_HOTKEY_FAILURE` スモークハッチ |
| `snotra-egui-runtime/src/renderer.rs` | 修正 | `SNOTRA_EGUI_PAINT_TRACE` の paint フェーズ計器（G3(b) 用・Task 6） |
| `scripts/measure-memory.ps1` | **全面書き換え** | PrivWS 軸のツリー合算計測（誤軸 `WorkingSet64` の孤児を置換） |
| `package.json` | 修正 | `measure:memory` スクリプト追加 |
| `SPEC.md` | 修正 | §7.5 の egui parity 行に起動時経路を追記・§11 に #643 残余 1 行 |
| `docs/build-commands.md` | 修正 | `npm run measure:memory` をコマンド一覧へ |
| `docs/superpowers/specs/2026-07-21-phase2-softbuffer-migration-roadmap.md` | 修正 | 進捗節に SU6.5 完了行 |

新規ファイルは作らない（`.rs`/`.ts` の追加削除が無いのでモジュール索引の更新は不要。ただし Task 5 で `package.json` を触るため hook-selftest が走る）。

---

### Task 1: #643 検索バーの font_size 追従 + hint 色

**Files:**
- Modify: `src-tauri/src/egui_shell/view.rs`（TextEdit 構築部・`.hint_text(hint)` を含む `ui.add(...)` ブロック）
- Modify: `SPEC.md`（§11 ビジュアル）

**Interfaces:**
- Consumes: 既存 `SearchWindowView::row_theme() -> RowTheme`（`name_size: f32` = config `visual.font_size`、`path_color: Color32` = config `visual.hint_text_color`）
- Produces: なし（後続タスクは本タスクの成果に依存しない）

**新規ユニットテストは無い。** egui の描画属性（フォント・hint 色）は純粋核へ切り出せず、値の出所である `row_theme()` は SU4 で導入済みだから。検証は既存テストの回帰（hook 自動実行）+ 実機スモーク（Step 4）で行う。**テストを書けない理由をここに明示するのは、後から「TDD を飛ばした」と読まれないためである。**

- [ ] **Step 1: TextEdit のフォントと hint 色を config から描く**

`view.rs` の検索入力欄構築を以下へ置き換える。置換対象は `let response = ui.add(` から `);` までの現在のブロック（`.interactive(...)` / `.hint_text(hint)` / `.desired_width(f32::INFINITY)` を持つもの）。直前の `let mut buf = ...;` は変更しない。

```rust
        // §11 Part C（#643）: 入力欄フォントを config `font_size` へ追従させ、hint を
        // `hint_text_color` で描く。**バー高さ 52px は据え置く**——WebView2 の
        // `--search-bar-height` も固定値で `--font-size` に連動せず（`global.css`）、
        // `.search-input` が `font-size: inherit` で body の `--font-size` を受けるだけ。
        // ゆえに「バー固定 + 入力欄フォントのみ追従」が parity である（SU6.5 決定 3）。
        // 極端な font_size でバーからはみ出すのは WebView2 も同じ＝同じ壊れ方＝parity。
        let bar_theme = self.row_theme();
        let bar_font = egui::FontId::proportional(bar_theme.name_size);
        let response = ui.add(
            egui::TextEdit::singleline(&mut buf)
                // §18.5 ツール選択中の入力は無効化。add_enabled（全体グレーアウト）でなく
                // interactive(false)（通常描画のまま読み取り専用・changed 不発火）——外観維持。
                // launching 中も同様に打鍵を止める（Escape/blur/Alt+Q・↑↓は従来どおり通す・
                // spec 決定 3・4。↑↓は空リストゆえ自然 no-op）。
                .interactive(!in_tool && self.launching.is_none())
                .font(bar_font.clone())
                .hint_text(
                    egui::RichText::new(hint).font(bar_font).color(bar_theme.path_color),
                )
                .desired_width(f32::INFINITY),
        );
```

`row_theme()` は engine ロックを 1 回取る。結果リスト描画の 2 箇所が既に毎フレーム同じ呼び方をしており、本追加はその as-built に揃えたもの（`update()` 冒頭の「lock 1 回/フレーム」注記は SU6 のエッジ検出ブロックに限った話）。

- [ ] **Step 2: ビルドと lint を通す**

Run: `cargo clippy -p snotra --all-targets -- -D warnings`
Expected: 警告なしで終了（exit 0）。`TextEdit::font` は `impl Into<FontSelection>` を取り `FontId` はこれを満たす。もし型不一致が出たら `.font(egui::FontSelection::FontId(bar_font.clone()))` へ落とす。

- [ ] **Step 3: 既存テストの回帰を確認**

Run: `cargo test -p snotra`
Expected: 全て pass（本タスクはテストを増やさない。落ちたら描画以外へ波及している）。

- [ ] **Step 4: 実機スモーク（人手）**

```powershell
$env:SNOTRA_EGUI_MAIN=1; cargo run -p snotra
```

確認項目:
1. `config.toml` の `[visual] font_size = 15`（既定）で入力欄の文字が結果行の名前と同じ大きさに見える
2. `font_size = 24` に変更 → `config-applied` wake で**入力欄の文字が拡大**し、バー高さは 52px のまま（窓が縦に伸びない）
3. 空クエリ時の hint（「検索...」）が `hint_text_color`（既定 `#808080`）で描かれる。egui 既定の弱色より明確に指定色へ寄る
4. `font_size = 15` へ戻して回帰なし

- [ ] **Step 5: SPEC §11 に残余を 1 行追記**

`SPEC.md` の §11 ビジュアル節の末尾へ以下を追加する:

```markdown
- egui 経路の検索入力欄は `font_size` に追従し hint は `hint_text_color` で描かれる。**検索バー高さは 52px 固定**（WebView2 の `--search-bar-height` も `--font-size` に連動しない as-built の parity）。極端な `font_size` では入力欄がバー内に収まらないが、これは WebView2 経路と同挙動である（#643・#532 SU6.5）
```

- [ ] **Step 6: コミット**

コミットメッセージを一時ファイルへ書いてから（HEREDOC 禁止）:

```powershell
git add src-tauri/src/egui_shell/view.rs SPEC.md
git commit -F $env:TEMP/su65-task1.txt
```

メッセージ本文:

```
fix(egui): 検索バーの font_size 追従 + hint 色（#643・#532 SU6.5）

WebView2 の --search-bar-height は 52px 固定で --font-size に連動しない
（global.css）。ゆえに parity は「バー据え置き + 入力欄フォントのみ追従」で
あり、issue が想定したバー高さ rework は不要（SU6.5 決定 3）。

Closes #643
```

---

### Task 2: #648 の残り 3 点（(A) wake / (C) dead payload / (F) mode fallback）

**Files:**
- Modify: `src-tauri/src/egui_shell/view.rs`（`start_launch` 末尾 / `spawn_install` の `InstallFailed` 構築 / toast 描画の `ToastKind::Failed` パターン）
- Modify: `src-tauri/src/egui_shell/notify.rs`（`UpdaterPhase::InstallFailed` / `ToastKind::Failed` / 既存テスト）
- Modify: `src-tauri/src/egui_shell/mod.rs`（`spawn_update_check` の fallback）

**Interfaces:**
- Consumes: なし
- Produces: `UpdaterPhase::InstallFailed`（フィールド無しの unit variant へ）・`ToastKind::Failed`（同）。Task 3 以降は触らない

**(B)（`strings.rs` module doc の矛盾）は SU6 で解消済み**——`//!` に「#648(B) で旧記述を是正」が既に入っている。本タスクでは触らない。

- [ ] **Step 1: (C) の失敗を先に作る — テストから dead payload を外す**

`notify.rs` の既存テスト `dismiss_is_refused_while_installing` の一行を書き換える:

```rust
        u.phase = UpdaterPhase::InstallFailed;
```

（変更前は `UpdaterPhase::InstallFailed { message: "e".into() };`）

- [ ] **Step 2: テストが落ちることを確認**

Run: `cargo test -p snotra egui_shell::notify`
Expected: コンパイルエラー。`expected struct variant ... found unit variant` 相当（`InstallFailed` はまだフィールドを持つ）。

**`--lib` を付けてはならない**——`snotra` は lib target を持たず `error: no library targets found in package 'snotra'` で落ちる（実測）。positional なテスト名フィルタは 1 つだけ指定できる。

- [ ] **Step 3: (C) dead payload を型から落とす**

`notify.rs` の 2 箇所を修正する。

```rust
pub enum UpdaterPhase<U> {
    Idle,
    Checking,
    UpToDate,
    Available { version: String, can_install: bool, update: Option<U> },
    // version は toast() が Installing 局面で表示しない（update_installing は汎用文言・
    // 消費経路なし）ため保持しない。Available→Installing 遷移時に破棄する。
    Installing,
    // egui 側の update_failed(l) は引数を取らず、message は描画に使われない dead payload
    // だったため型から落とす（#648 C）。失敗の詳細は現状 trace（egui_update_install_failed）
    // だけが持つ。**parity 注記: WebView2 は errorDetail を toast に描く（UpdateToast.tsx +
    // MainApp.tsx の setUpdaterError）——egui の generic 固定表示はそれに対する既知の縮小差で
    // あり、詳細表示が要れば strings 拡張になる**（Task 2 レビューが実測で検出・全称「parity
    // 影響なし」は誤りだった）。
    InstallFailed,
}
```

```rust
#[derive(Debug, PartialEq)]
pub enum ToastKind {
    Available { version: String },
    Installing,
    Failed,
}
```

`toast()` の該当アームも合わせる:

```rust
            UpdaterPhase::InstallFailed => Some(ToastRow {
                kind: ToastKind::Failed,
                show_install: false,
                buttons_enabled: true,
            }),
```

- [ ] **Step 4: 生産者と消費者を型に合わせる**

`view.rs` の `spawn_install` 内、失敗遷移を修正する（`trace_main("egui_update_install_failed", ...)` の直後のブロック）:

```rust
                    if let Some(st) = handle.try_state::<crate::egui_shell::UpdaterUiState>() {
                        st.0.lock().unwrap().phase = crate::egui_shell::UpdaterPhase::InstallFailed;
                    }
```

`view.rs` の toast 描画のパターンマッチを修正する（`crate::egui_shell::ToastKind::Failed { .. } =>` の行）:

```rust
                crate::egui_shell::ToastKind::Failed => {
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cargo test -p snotra`
Expected: 全て pass。

- [ ] **Step 6: (A) `start_launch` 末尾に明示 wake を置く**

`view.rs` の `start_launch` で、`std::thread::spawn(move || { ... });` の**閉じ括弧の直後**（関数の最終行）に以下を追加する:

```rust
        // 状態を変えたら起こす（#648 A）。現状は「results クリア → 高さ collapse →
        // set_size → Resized → repaint」の暗黙連鎖で初回 overlay と timeout 予約が
        // 成立している（実測済）。連鎖は今日必ず成立するが、collapse をやめる類の将来変更で
        // 切れると、可視のまま遅い起動をしたときに overlay も 4 秒通知も次の入力まで出ない。
        // toast dismiss の同型バグ（SU5・e746826）と同じ規範で自己完結させる 1 行。
        ctx.request_repaint();
```

- [ ] **Step 7: (F) mode fallback を保守的既定へ**

`mod.rs` の `spawn_update_check` 内、`AppState` 不在時の fallback を修正する:

```rust
    let mode = app
        .try_state::<crate::AppState>()
        .map(|s| s.engine.lock().unwrap().config().general.auto_update)
        // AppState は setup 前に managed されるため実運用では到達不能。到達したら
        // 「設定を読めていない」状態なので、勝手に更新を始めない Disabled へ倒す（#648 F）。
        .unwrap_or(AutoUpdateMode::Disabled);
```

- [ ] **Step 8: 検証**

Run: `cargo test -p snotra && cargo clippy -p snotra --all-targets -- -D warnings`
Expected: 両方 exit 0。

- [ ] **Step 9: 実機スモーク（人手・回帰確認）**

```powershell
$env:SNOTRA_EGUI_MAIN=1; $env:SNOTRA_EGUI_FAKE_UPDATE=1; cargo run -p snotra
```

確認: toast が出る／`[閉じる]` で消えて**次の入力を待たずに**消える（SU5 の wake 回帰なし）／Enter でアプリ起動ができ、「起動中...」overlay が出る。

- [ ] **Step 10: コミット**

```powershell
git add src-tauri/src/egui_shell/view.rs src-tauri/src/egui_shell/notify.rs src-tauri/src/egui_shell/mod.rs
git commit -F $env:TEMP/su65-task2.txt
```

メッセージ本文:

```
chore(egui): SU5 follow-up 3 点を消化（#648・#532 SU6.5）

- (A) start_launch 末尾に明示 request_repaint。overlay の初回描画が暗黙連鎖
  （results クリア→collapse→Resized→repaint）に依存していたのを自己完結化
- (C) InstallFailed / ToastKind::Failed の dead payload を型から削除。詳細は
  trace だけが持ち、toast 行 1 は generic 文言で message を描いていなかった
- (F) spawn_update_check の AppState 不在時 fallback を Full → Disabled

(B) は SU6 で解消済み。

Closes #648
```

---

### Task 3: #652 の純粋核（`HotkeyFailureKind` + i18n 文言 + 消費分岐）

**Files:**
- Modify: `src-tauri/src/egui_shell/mod.rs`（enum 追加・pending 型変更・既存 listener の格納）
- Modify: `src-tauri/src/egui_shell/strings.rs`（文言 + parity テスト）
- Modify: `src-tauri/src/egui_shell/view.rs`（pending 消費の分岐）

**Interfaces:**
- Produces:
  - `pub(crate) enum HotkeyFailureKind { Initial, Change }`（`Clone + Copy + Debug + PartialEq + Eq`）
  - `EguiShellState.pending_hotkey_failure: Mutex<Option<(HotkeyFailureKind, String)>>`
  - `pub fn hotkey_initial_failed(l: Language, hotkey: &str) -> String`（`strings.rs`）
- Consumes: 既存 `hotkey_change_failed(l, hotkey)`・`NOTICE_HOTKEY`（5000ms）

- [ ] **Step 1: 失敗するテストを書く**

`strings.rs` の `#[cfg(test)] mod tests` へ追加する。**文字列は `ui/src/lib/i18n.ts` の実物を開いて転記すること**（このコードブロックからコピーせず、必ず突合する）:

```rust
    #[test]
    fn hotkey_initial_failed_matches_i18n() {
        // i18n.ts notice.hotkey.initial_failed の値と一字一句一致（2026-07-24 実物確認）。
        // change_failed と違い「他のアプリが使用中の可能性があります」で終わる。
        assert_eq!(
            hotkey_initial_failed(Language::Ja, "Alt+Q"),
            "ホットキー (Alt+Q) の登録に失敗しました。他のアプリが使用中の可能性があります"
        );
        assert_eq!(
            hotkey_initial_failed(Language::En, "Alt+Q"),
            "Failed to register hotkey (Alt+Q). Another application may be using it"
        );
    }
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `cargo test -p snotra egui_shell::strings`
Expected: コンパイルエラー `cannot find function 'hotkey_initial_failed' in this scope`。

**`--lib` を付けてはならない**——`snotra` は lib target を持たず `error: no library targets found in package 'snotra'` で落ちる（実測）。positional なテスト名フィルタは 1 つだけ指定できる。

- [ ] **Step 3: 文言を追加する**

`strings.rs` の `hotkey_change_failed` の直後へ:

```rust
/// 起動時ホットキー登録失敗通知（i18n.ts `notice.hotkey.initial_failed` と一字一句一致・
/// {hotkey} は書式挿入。英語文言に句点は付かない＝i18n.ts 実物どおり）。
/// `change_failed` と別キーなのは意図（旧ホットキー維持ではなく「開く手段が無い」告知）。
pub fn hotkey_initial_failed(l: Language, hotkey: &str) -> String {
    match l {
        Language::Ja => format!("ホットキー ({hotkey}) の登録に失敗しました。他のアプリが使用中の可能性があります"),
        Language::En => format!("Failed to register hotkey ({hotkey}). Another application may be using it"),
    }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test -p snotra --lib egui_shell::strings`
Expected: `hotkey_initial_failed_matches_i18n` を含め pass。

- [ ] **Step 5: pending スロットへ種別を載せる**

`mod.rs` の `EguiShellState` 定義の直前へ enum を追加する:

```rust
/// hotkey 登録失敗の種別（#652）。文言キーが別（i18n.ts `notice.hotkey.*`）ゆえ
/// pending に載せて view 側で整形を分岐する。
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub(crate) enum HotkeyFailureKind {
    /// 起動時の初回登録失敗（`platform-event` / `initial-hotkey-failed`）。
    /// SPEC §10 のとおり窓を能動表示してから通知する。
    Initial,
    /// 設定変更による再登録失敗（`hotkey-registration-failed`）。旧ホットキーを維持。
    Change,
}
```

`EguiShellState` の該当フィールドを差し替える（doc コメントも更新する）:

```rust
    /// hotkey 登録失敗の pending payload（SU6 spec 追補 2 + #652）。種別ごとに文言が違う
    /// ため `(kind, hotkey)` を保持し、view が消費時に lang() live-read で整形する。
    /// **wake の有無は経路で異なる**——Change は wake しない（wake を config-applied に
    /// 委ね、言語同時変更で旧言語整形になる競合窓を閉じる）。Initial は wake する
    /// （config 変更が随伴せず config-applied が来ないため・#652・SU6.5 決定 2）。
    pub(crate) pending_hotkey_failure: Mutex<Option<(HotkeyFailureKind, String)>>,
```

既存の `register_hotkey_failure_listener` の格納も種別付きへ:

```rust
        if let Some(sh) = handle.try_state::<EguiShellState>() {
            *sh.pending_hotkey_failure.lock().unwrap() = Some((HotkeyFailureKind::Change, hotkey));
        }
```

- [ ] **Step 6: view 側の消費を分岐する**

`view.rs` の pending 消費ブロック（`// hotkey 登録失敗の pending 消費` のコメントが付いた `if let`）を置き換える:

```rust
        // hotkey 登録失敗の pending 消費（SU6 spec 追補 2 + #652）。reset_pending 消費より後
        //（順序不変条件——reset の notice.clear() がこの set を消さないため）。整形はここで
        // lang() live-read: config-applied wake のフレームは update_config 後なので言語同時
        // 変更でも新言語で整形される。hidden 中の失敗は次 show のこの消費で表示される
        //（WebView2 は hidden 中に期限切れ・改善方向の受容差異・SU6 spec 追補 2）。
        if let Some(sh) = self.app_handle.try_state::<crate::egui_shell::EguiShellState>()
            && let Some((kind, hk)) = sh.pending_hotkey_failure.lock().unwrap().take()
        {
            let msg = match kind {
                crate::egui_shell::HotkeyFailureKind::Initial => {
                    crate::egui_shell::ui_strings::hotkey_initial_failed(self.lang(), &hk)
                }
                crate::egui_shell::HotkeyFailureKind::Change => {
                    crate::egui_shell::ui_strings::hotkey_change_failed(self.lang(), &hk)
                }
            };
            self.notice.set(msg, self.notice_base.elapsed(), crate::egui_shell::NOTICE_HOTKEY);
            ctx.request_repaint();
        }
```

`HotkeyFailureKind` を `mod.rs` の再エクスポート行に足す（`pub(crate) use` の並びのうち `lifecycle` の隣ではなく、`EguiShellState` と同じくモジュール直下の型なので `use` 追加は不要——`crate::egui_shell::HotkeyFailureKind` で参照できる）。

- [ ] **Step 7: 全体ビルドと検証**

Run: `cargo test -p snotra && cargo clippy -p snotra --all-targets -- -D warnings`
Expected: 両方 exit 0。`pending_hotkey_failure` の型変更に伴う compile-fail が全て解消していること（**下流の compile-fail を「変更漏れ検出器」として使う**——手で grep するより確実）。

- [ ] **Step 8: コミット**

```powershell
git add src-tauri/src/egui_shell/mod.rs src-tauri/src/egui_shell/strings.rs src-tauri/src/egui_shell/view.rs
git commit -F $env:TEMP/su65-task3.txt
```

メッセージ本文:

```
feat(egui): hotkey 失敗 pending に種別を載せる（#652 準備・#532 SU6.5）

起動時失敗（initial_failed）と変更失敗（change_failed）は i18n キーが別ゆえ、
pending を (HotkeyFailureKind, String) へ拡張して view 側で整形を分岐する。
文言は i18n.ts 実物から codepoint 転記し parity テストを追加。

配線（listener + 窓能動表示）は次コミット。

Refs #652
```

---

### Task 4: #652 の配線（listener + 窓能動表示 + スモークハッチ + SPEC 同期）

**Files:**
- Modify: `src-tauri/src/egui_shell/mod.rs`（`register_platform_event_listener` 新設）
- Modify: `src-tauri/src/main.rs`（egui ブロック内で登録）
- Modify: `src-tauri/src/platform/mod.rs`（`SNOTRA_FAKE_INITIAL_HOTKEY_FAILURE`）
- Modify: `SPEC.md`（§7.5 の egui parity 行）

**Interfaces:**
- Consumes: Task 3 の `HotkeyFailureKind::Initial`・`EguiShellState.pending_hotkey_failure`。既存 `show_egui_main(app: &AppHandle, t0: Instant)`・`wake_view(app: &AppHandle)`
- Produces: `pub(crate) fn register_platform_event_listener(app: &tauri::AppHandle)`

**ユニットテストは無い。** listener は Tauri のイベント配送と `show_egui_main`（Win32）に密結合で、`.claude/rules/src-tauri.md` の「Win32 依存モジュールはユニットテスト前提にしない」に当たる。検証は Step 5 の実機スモーク（ハッチで失敗を注入）で行う。

- [ ] **Step 1: listener を追加する**

`mod.rs` の `register_hotkey_failure_listener` の直後へ:

```rust
/// 起動時 hotkey 登録失敗の受け口（#652・SU6.5 決定 2）。`platform-event` の
/// `initial-hotkey-failed` を判別し、**格納 → show → wake** の順で処理する。
///
/// - **格納が先**: show が起こすフレームは reset-on-show の `notice.clear()` を通ってから
///   pending を消費する（view の順序不変条件）。逆順にすると clear と store の間にフレームが
///   挟まりうるため、通知が消えたまま二度と出ない。
/// - **show する**: ホットキーが登録できていない＝ユーザーが窓を開く手段がトレイしか無い。
///   SPEC §10「初回ホットキー登録失敗時は操作不能回避のため検索 UI を表示し」の実装で、
///   WebView2 では `MainApp.tsx` の `win.show()` が担っている経路の egui 版。
/// - **wake する**: `show_on_startup=true` で既に可視なら `show()` は再描画を生まない。
///   `register_hotkey_failure_listener`（変更失敗）が wake しないのと**意図的に逆**——
///   あちらは必ず `config-applied` が随伴するが、起動時失敗には config 変更が無く
///   `config-applied` は来ない。ここで起こさないと「hidden 中は update() が走らない」
///   不変条件（SU5）により通知が永遠に描かれない。
pub(crate) fn register_platform_event_listener(app: &tauri::AppHandle) {
    let handle = app.clone();
    app.listen("platform-event", move |event| {
        let Ok(payload) = serde_json::from_str::<serde_json::Value>(event.payload()) else {
            return;
        };
        if payload.get("event").and_then(|v| v.as_str()) != Some("initial-hotkey-failed") {
            return;
        }
        let hotkey = payload
            .get("hotkey")
            .and_then(|v| v.as_str())
            .unwrap_or_default()
            .to_string();
        if let Some(sh) = handle.try_state::<EguiShellState>() {
            *sh.pending_hotkey_failure.lock().unwrap() = Some((HotkeyFailureKind::Initial, hotkey));
        }
        show_egui_main(&handle, Instant::now());
        wake_view(&handle);
    });
}
```

- [ ] **Step 2: `main.rs` の egui ブロックで登録する**

`egui_shell::register_hotkey_failure_listener(&app_handle);` の直後へ:

```rust
                // 起動時 hotkey 登録失敗の受け口（#652）。RegisterInitialHotkey を送る
                // setup_hotkey_listener より前に登録される位置なので emit を取りこぼさない
                //（この egui ブロック自体が setup_platform_thread の直後・hotkey listener の前）。
                egui_shell::register_platform_event_listener(&app_handle);
```

- [ ] **Step 3: スモークハッチを追加する**

`platform/mod.rs` の `PlatformCommand::RegisterInitialHotkey` アームを置き換える:

```rust
            PlatformCommand::RegisterInitialHotkey => {
                // 視覚スモーク専用のエスケープハッチ（`SNOTRA_EGUI_FAKE_UPDATE` と同じ流儀）。
                // 実機で RegisterHotKey を失敗させるには他アプリが同じキーを握っている必要が
                // あり再現性が無いため、**登録は成功させたまま失敗イベントだけを流す**。
                // ゆえにハッチ使用中もホットキーは動く（#652 の受け口検証用）。
                let registered = hotkey::register(current_hotkey)
                    && !crate::trace::env_flag("SNOTRA_FAKE_INITIAL_HOTKEY_FAILURE");
                if !registered {
                    let hotkey_str =
                        format!("{}+{}", current_hotkey.modifier, current_hotkey.key);
                    let _ = app_handle.emit(
                        "platform-event",
                        serde_json::json!({
                            "event": "initial-hotkey-failed",
                            "hotkey": hotkey_str,
                        }),
                    );
                }
            }
```

- [ ] **Step 4: ビルドと検証**

Run: `cargo test -p snotra && cargo clippy -p snotra --all-targets -- -D warnings`
Expected: 両方 exit 0。

- [ ] **Step 5: 実機スモーク（人手・#652 の受け入れ確認）**

**(a) hidden 起動から（`show_on_startup = false`）**

```powershell
$env:SNOTRA_EGUI_MAIN=1; $env:SNOTRA_FAKE_INITIAL_HOTKEY_FAILURE=1; $env:SNOTRA_TRACE=1; cargo run -p snotra
```

期待: 起動直後に**窓が自動で開き**、検索バーに「ホットキー (Alt+Q) の登録に失敗しました。他のアプリが使用中の可能性があります」が 5 秒間表示される。trace に `egui_show:done` が出る。

**(b) visible 起動から（`show_on_startup = true` に変更して再実行）**

期待: 既に可視の状態でも同じ通知が出る。**これが `wake_view` の効きを見る唯一のケース**——wake が無いと、既可視では `show()` が再描画を生まず通知が次の入力まで出ない。

**(c) 言語 En で**（`config.toml` の `[general] language = "En"`）

期待: "Failed to register hotkey (Alt+Q). Another application may be using it"（末尾ピリオド無し）。

**(d) 回帰: ハッチ無しで起動**

期待: 通知が出ず、窓も勝手に開かない（`show_on_startup` の設定どおり）。

- [ ] **Step 6: SPEC §7.5 の egui parity 行を同期する**

`SPEC.md` §7.5 の egui 経路の行のうち、末尾の「hotkey 登録失敗は …」の一文を以下へ置き換える:

```markdown
hotkey 登録失敗は 2 経路とも受ける——設定変更時（`hotkey-registration-failed`）は payload を保持し次の表示時に整形・通知し、起動時（`platform-event` の `initial-hotkey-failed`）は §10 のとおり検索 UI を能動表示してから通知する（#652）
```

§10 の「初回ホットキー登録失敗時は操作不能回避のため検索 UI を表示し、ウィンドウ内にエラー通知を表示する」は**経路非依存の intent 正本ゆえ改訂しない**（SU6.5 決定 2）。

- [ ] **Step 7: コミット**

```powershell
git add src-tauri/src/egui_shell/mod.rs src-tauri/src/main.rs src-tauri/src/platform/mod.rs SPEC.md
git commit -F $env:TEMP/su65-task4.txt
```

メッセージ本文:

```
fix(egui): 起動時 hotkey 登録失敗を egui 経路でも通知する（#652・#532 SU6.5）

SPEC §10 が経路非依存で要求している「初回登録失敗時は操作不能回避のため
検索 UI を表示し通知する」が egui 経路に無かった（仕様変更ではなく parity gap）。

listener は「格納 → show_egui_main → wake_view」の 3 段順序。格納が先なのは
reset-on-show の notice.clear() より後に消費させるため。wake が要るのは既可視
時に show() が再描画を生まないため——変更失敗の listener が wake しない
（config-applied に委ねる）のとは経路の前提が違う。

検証は SNOTRA_FAKE_INITIAL_HOTKEY_FAILURE=1 のスモークハッチで注入する。

Closes #652
```

---

### Task 5: 計測ハーネス（`measure-memory.ps1` を PrivWS 軸へ置換）

**Files:**
- Modify（全面書き換え）: `scripts/measure-memory.ps1`
- Modify: `package.json`（`measure:memory`）
- Modify: `docs/build-commands.md`

**Interfaces:**
- Produces: `npm run measure:memory`（既定でプロセス名 `snotra` のツリーを合算し PrivWS / PrivComm を MiB で出力）

**なぜ新設でなく置換か**: 現行スクリプトは `WorkingSet64` 軸で、採用ゲートが「共有 Edge ページをツリー合算で ×6 二重計上する誤軸」と結論した当のもの。しかも参照ゼロの孤児。残すと将来の読者が誤軸で測る（SU6.5 決定 5）。

- [ ] **Step 1: スクリプトを全面書き換える**

`scripts/measure-memory.ps1` の内容を以下で置き換える:

```powershell
<#
.SYNOPSIS
  Snotra のメモリ実測（#532 flip 基準 3）。PrivWS（物理私有 working set）を本命軸に
  プロセスツリー全体を合算する。

.DESCRIPTION
  採用ゲート（issue #532 comment 5011127487）と同じ手法を再現する:
  - 本命軸は Win32_PerfFormattedData_PerfProc_Process.WorkingSetPrivate（PrivWS）。
    WorkingSet64 は共有 Edge ページをツリー合算で二重計上するため**出力しない**。
  - 子孫プロセス（msedgewebview2 等）を BFS で辿って合算する。
  - **プロセス件数を必ず出力する**。BFS が沈黙して 0 件を返す罠（#532 SU2 実測: CIM の
    ParentProcessId は uint32 で返り、int の PID 集合との比較が一致しない）を出力から
    検知できるようにするため——「測れなかった」と「0 だった」を区別できない測定器は使えない。

.PARAMETER ProcessName
  ルートプロセス名（既定 snotra）。

.PARAMETER Label
  出力に付ける見出し（例 "egui-visible" / "webview2-hidden"）。

.PARAMETER DelaySeconds
  サンプリング前の待機秒数。可視状態を測るときに使う（このスクリプトを起動した端末が
  フォーカスを奪うため）。**hide-on-blur を止める方が確実**なので、可視測定では
  config.toml の [general] auto_hide_on_focus_lost = false を推奨する。

.EXAMPLE
  npm run measure:memory
  pwsh -NoProfile -File scripts/measure-memory.ps1 -Label egui-hidden
#>
param(
    [string]$ProcessName = 'snotra',
    [string]$Label = '',
    [int]$DelaySeconds = 0
)

$ErrorActionPreference = 'Stop'

if ($DelaySeconds -gt 0) {
    Write-Host "$DelaySeconds 秒後にサンプリングします..." -ForegroundColor Cyan
    Start-Sleep -Seconds $DelaySeconds
}

$roots = @(Get-Process -Name $ProcessName -ErrorAction SilentlyContinue |
    ForEach-Object { [uint32]$_.Id })
if ($roots.Count -eq 0) {
    Write-Host "プロセスが見つかりません: $ProcessName" -ForegroundColor Red
    exit 1
}

# (ppid -> 子 pid) の索引を一度だけ作る。**型は uint32 に揃える**（上記 SU2 の罠）。
$byPpid = @{}
foreach ($p in Get-CimInstance Win32_Process) {
    $parent = [uint32]$p.ParentProcessId
    if (-not $byPpid.ContainsKey($parent)) {
        $byPpid[$parent] = New-Object System.Collections.ArrayList
    }
    [void]$byPpid[$parent].Add([uint32]$p.ProcessId)
}

# ルートから子孫を BFS（$pid は PowerShell の自動変数なので別名を使う）。
$tree = @{}
$queue = New-Object System.Collections.Queue
foreach ($r in $roots) { [void]$queue.Enqueue($r) }
while ($queue.Count -gt 0) {
    $cur = [uint32]$queue.Dequeue()
    if ($tree.ContainsKey($cur)) { continue }
    $tree[$cur] = $true
    if ($byPpid.ContainsKey($cur)) {
        foreach ($c in $byPpid[$cur]) { [void]$queue.Enqueue($c) }
    }
}

$rows = @(Get-CimInstance Win32_PerfFormattedData_PerfProc_Process |
    Where-Object { $tree.ContainsKey([uint32]$_.IDProcess) } |
    ForEach-Object {
        [pscustomobject]@{
            Name     = $_.Name
            ProcId   = [uint32]$_.IDProcess
            PrivWS   = [math]::Round($_.WorkingSetPrivate / 1MB, 1)
            PrivComm = [math]::Round($_.PrivateBytes / 1MB, 1)
        }
    })

$title = if ($Label) { "=== Snotra メモリ実測 [$Label] ===" } else { '=== Snotra メモリ実測 ===' }
Write-Host ''
Write-Host $title -ForegroundColor Cyan
Write-Host ("  ルート {0} 件 / ツリー {1} 件 / perf 取得 {2} 件" -f $roots.Count, $tree.Count, $rows.Count)

if ($rows.Count -eq 0) {
    Write-Host '  perf カウンタが 0 件です。BFS 不成立か perf 未提供の疑いがあり、' -ForegroundColor Red
    Write-Host '  「0 MiB だった」と読んではならない。ツリー件数と併せて手法を疑うこと。' -ForegroundColor Red
    exit 2
}

$rows | Sort-Object -Property PrivWS -Descending | Format-Table -AutoSize
$sumWs = [math]::Round(($rows | Measure-Object -Property PrivWS -Sum).Sum, 1)
$sumPc = [math]::Round(($rows | Measure-Object -Property PrivComm -Sum).Sum, 1)
Write-Host ("  合計 PrivWS  : {0,7} MiB  ← 本命軸（物理私有・flip 基準 3）" -f $sumWs) -ForegroundColor Green
Write-Host ("  合計 PrivComm: {0,7} MiB  （参考・commit）" -f $sumPc) -ForegroundColor DarkGray
Write-Host ''
```

- [ ] **Step 2: npm script を追加する**

`package.json` の `"smoke:startup"` の次行へ:

```json
    "measure:memory": "pwsh -NoProfile -File scripts/measure-memory.ps1",
```

- [ ] **Step 3: 動作確認（プロセス不在時と存在時の両方）**

Run: `npm run measure:memory`
Expected（Snotra 未起動時）: `プロセスが見つかりません: snotra` と表示し exit 1。

次に Snotra を WebView2 経路で起動してから:

Run: `npm run measure:memory`
Expected: **ツリー件数が 7 前後**（host + msedgewebview2 ×6）で、`perf 取得` がそれと同数。0 件や 1 件なら BFS が壊れているので型を疑う。

- [ ] **Step 4: `docs/build-commands.md` へコマンドを追加する**

`npm run smoke:startup` の行の直後（コマンド一覧のコードブロック内）へ:

```
npm run measure:memory            # メモリ実測（PrivWS 軸・ツリー合算・#532 flip 基準 3）
```

- [ ] **Step 5: hook-selftest の結果を確認**

`package.json` を編集したため PostToolUse hook が hook-selftest を走らせる。**沈黙 = 合格**（失敗時のみ会話に届く）。手動再実行は不要。

- [ ] **Step 6: コミット**

```powershell
git add scripts/measure-memory.ps1 package.json docs/build-commands.md
git commit -F $env:TEMP/su65-task5.txt
```

メッセージ本文:

```
chore: メモリ計測を PrivWS 軸へ置換（#532 SU6.5 flip 基準 3）

旧 measure-memory.ps1 は WorkingSet64 軸で、採用ゲートが「共有 Edge ページを
ツリー合算で二重計上する誤軸」と結論したもの。参照ゼロの孤児でもあったため
新設ではなく置換する（誤軸を残すと将来の読者がそれで測る）。

- 本命軸を WorkingSetPrivate（PrivWS）に。WorkingSet64 は出力しない
- 子孫 BFS の型を uint32 に揃える（SU2 実測: CIM ParentProcessId の型不一致で
  BFS が沈黙して 0 件を返した罠）
- ルート/ツリー/perf 取得の件数を必ず出力し、0 件を「0 MiB」と読ませない

Refs #532
```

---

### Task 6: 計測ゲート G1〜G4 の実行と記録

**Files:**
- Modify: `docs/superpowers/specs/2026-07-21-phase2-softbuffer-migration-roadmap.md`（進捗節）
- 記録先: issue #532 コメント / PR 本文

**Interfaces:**
- Consumes: Task 5 の `npm run measure:memory`・Task 1〜4 の実装

**このタスクはコードを書かない。** 実機操作は人手が要る（起動・目視・打鍵）。**一項目ずつ「手順提示 → 実測 → 記録」で進め、バッチで回さない**（SU6.5 決定 1）。

**同日ペア測定が容易であることの確認**: WebView2 経路と egui 経路は**同一バイナリの env フラグ切り替え**で選ばれる（`SNOTRA_EGUI_MAIN`）。採用ゲート時は別 bin のスパイクだったため「同日・同機・同一 index」を人手で揃える必要があったが、今回は 1 回の release ビルドで両構成を測れる。これはゲート測定より条件が厳しく揃うことを意味する。

- [ ] **Step 1: release をビルドする**

Run: `npm run tauri build`
Expected: `src-tauri/target/release/snotra.exe` が生成される（`cargo build --release` 単体は UI が壊れるので使わない）。

- [ ] **Step 2: G1 メモリ再測（4 サンプル）**

`config.toml` に `[general] auto_hide_on_focus_lost = false` を一時設定する（端末がフォーカスを奪っても窓が消えないようにするため。メモリ量には影響しない）。

WebView2 経路（フラグ無し）で `snotra.exe` を起動し、Alt+Q で表示してから:

```powershell
npm run measure:memory -- -Label webview2-visible
```

Alt+Q で非表示にし 3 秒待ってから:

```powershell
npm run measure:memory -- -Label webview2-hidden
```

アプリを終了し、egui 経路で起動して同じ 2 サンプルを採る:

```powershell
$env:SNOTRA_EGUI_MAIN=1; .\src-tauri\target\release\snotra.exe
npm run measure:memory -- -Label egui-visible
npm run measure:memory -- -Label egui-hidden
```

**合否**: egui の PrivWS が visible / hidden の**両方で** WebView2 版以下。
**同時に記録**: egui 経路の**ツリー件数が 1**（flip 基準 5「WebView2 子孫 0」の確認。この測定の副産物）。
**注目**: egui-hidden が跳ねたら SU4 の clear-on-hide（アイコンテクスチャ解放）が効いていない疑い。

測り終えたら `auto_hide_on_focus_lost` を元に戻す。

- [ ] **Step 3: G2 外観目視 parity**

既定ダークテーマ・`font_size = 15` で両経路を起動し、同一クエリで並置比較する。比較項目:

1. 検索バー — 背景・入力欄背景・文字サイズ・hint の文言と色・カーソル
2. 結果行 — アイコン・名前・淡色パス・省略記号の位置・選択行の色
3. 寸法 — 行高・余白・窓幅
4. **日本語 + 長パスを画面に出し、注意をテキストのエッジと小さい字に絞る**（採用ゲートで確立した UI 比較の罠回避——ASCII と大きい字では差が見えない）

**合否**: 気づく差が無い。差が出たら、修正するか parity-gap として SPEC に明記して受容するかをその場で判断する。スクリーンショット並置を PR 本文へ。

- [ ] **Step 4: G3(a) hidden で再描画が止まることの裏取り**

```powershell
$env:SNOTRA_EGUI_MAIN=1; $env:SNOTRA_TRACE=1; .\src-tauri\target\release\snotra.exe 2>&1 | Tee-Object -FilePath $env:TEMP\su65-hidden.log
```

Alt+Q で表示 → Alt+Q で非表示 → 60 秒放置。その間タスクマネージャーで CPU を見る。

**合否**: 非表示中の paint trace が 0 行、CPU ≒ 0%。止まらなければ #628 を flip ブロッカーへ昇格する（ロードマップ条文）。SU5 が製品経路で実測済みゆえ、これは裏取りである。

- [ ] **Step 5a: paint 計器を実装する（`snotra-egui-runtime`）**

**#628 が引用する `raster_ms` トレースは現在のコードに存在しない**——SU1 スパイク時の計測用で、実装には残っていない（`grep raster_ms` が `raster.rs` の doc コメントしか当てない）。測る前に計器を入れる。

`snotra-egui-runtime/src/renderer.rs` の `paint()` を修正する。既存の `SNOTRA_EGUI_IME_TRACE`（`windows_ime.rs`）と同じ env + `eprintln!` の流儀に揃える。

冒頭の `if !is_renderable_extent(...)` ブロックの直後へ:

```rust
        // paint フェーズ計測（#628・#532 SU6.5 G3(b)）。env 未設定なら Instant も取らない
        // ——常時 2 回の時刻取得を入れないため（計測器が測定対象を汚さない）。
        let trace = std::env::var_os("SNOTRA_EGUI_PAINT_TRACE").is_some();
        let t_begin = trace.then(std::time::Instant::now);
```

`let clipped = context.tessellate(output.shapes, ppp);` の直後へ:

```rust
        let t_tess = trace.then(std::time::Instant::now);
```

`for primitive in &clipped { ... }` ループの閉じ括弧の直後へ:

```rust
        let t_raster = trace.then(std::time::Instant::now);
```

`present_result?;` の直後、`Ok(PaintOutcome::Presented)` の直前へ:

```rust
        if let (Some(b), Some(t), Some(r)) = (t_begin, t_tess, t_raster) {
            eprintln!(
                "SNOTRA_EGUI_PAINT tess_ms={:.2} raster_ms={:.2} total_ms={:.2} meshes={} px={}",
                (t - b).as_secs_f64() * 1000.0,
                (r - t).as_secs_f64() * 1000.0,
                b.elapsed().as_secs_f64() * 1000.0,
                clipped.len(),
                size.width as u64 * size.height as u64,
            );
        }
```

Run: `cargo test -p snotra-egui-runtime && cargo clippy -p snotra-egui-runtime --all-targets -- -D warnings`
Expected: 両方 exit 0。

commit（メッセージは一時ファイル経由）:

```
chore(egui-runtime): paint フェーズ計器を追加（#628 計測・#532 SU6.5）

SNOTRA_EGUI_PAINT_TRACE で tess/raster/total と px 数を出す。#628 が引用する
raster_ms は SU1 スパイク時の計測で実装には残っていなかったため、G3(b) を測る
前に計器を入れる。env 未設定なら Instant も取らない。

Refs #628
```

- [ ] **Step 5b: G3(b) 入力レスポンスを測る**

**見立て（先に固定する）**: `raster_ms=27〜44` は SU1 スパイクの 900×588（≈529k px）での値。製品ウィンドウは幅 600・結果展開時 ~300px 高＝**≈185k px（約 1/3）**なので 9〜15ms が予測。フレーム予算 16.7ms の内側に入りうる。**この予測を先に書き留めてから測る**——予測なしに測ると、出た数字をそのまま「そういうもの」として受け入れてしまう。

Step 1 の release を計器込みで焼き直してから:

```powershell
$env:SNOTRA_EGUI_MAIN=1; $env:SNOTRA_EGUI_PAINT_TRACE=1; $env:SNOTRA_TRACE=1
.\src-tauri\target\release\snotra.exe 2>&1 | Tee-Object -FilePath $env:TEMP\su65-paint.log
```

日本語を含むクエリを 20 回程度打鍵（結果が出る語を選ぶ——空結果では 52px のままで px 数が変わる）。ログから `raster_ms` と `px` を集計する:

```powershell
$ms = Select-String -Path $env:TEMP\su65-paint.log -Pattern 'raster_ms=([\d.]+)' |
    ForEach-Object { [double]$_.Matches[0].Groups[1].Value }
"n={0} p50={1} p95={2} max={3}" -f $ms.Count,
    ($ms | Sort-Object)[[int]($ms.Count * 0.5)],
    ($ms | Sort-Object)[[int]($ms.Count * 0.95)],
    ($ms | Measure-Object -Maximum).Maximum
```

**主判定**: `raster_ms` の p95 < 16.7ms。
**副判定**: WebView2 側（`localStorage.snotra_perf=1` の `perf.ts` P95）との桁の一致。位相定義が違うため厳密比較はしない。

**落ちた場合のみ**: `fill_mesh` 最適化（`raster.rs`——三角形ごとに `inv_area = 1.0/area` を 1 回計算して per-pixel の 3 除算を乗算へ / 単色・不透明・非テクスチャ mesh の fast path）を実施し、再測して閉じる。この分岐に入ったら実施内容を本 Step の下へ追記する。

- [ ] **Step 6: G4 `e2e:tauri`**

PR を出すと `src-tauri` の変更により `e2e.yml`（E2E & Smoke）が paths で自動起動する。

**合否**: green。赤なら SU2〜SU6 のいずれかの回帰として追跡する。これは「まだ壊していない」ことの確認であり、flip 後の回帰手段ではない（WebView2 撤去で基盤ごと失われる・後継方針は SU7）。

- [ ] **Step 7: 結果を #532 コメントへ記録する**

G1 の 4 サンプル（PrivWS / PrivComm / プロセス数）・G2 のスクリーンショットと判定・G3(a)(b) の数値・G4 の CI リンクを 1 コメントにまとめて投稿する。**PrivWS の生数値を必ず残す**（採用ゲートのコメントと直接比較できるようにするため）。

- [ ] **Step 8: ロードマップ進捗節を更新する**

`docs/superpowers/specs/2026-07-21-phase2-softbuffer-migration-roadmap.md` の進捗節、`- **SU6 完了** …` の次行へ SU6.5 の完了行を追加し、`- 残り: ...` の行を `- 残り: SU7（→「依存順・並行」）` へ書き換える。完了行には G1〜G4 の結果要旨と #643/#648/#652 の close を含める。

- [ ] **Step 9: #646 へ位置づけを追記する**

```powershell
gh issue comment 646 --body-file $env:TEMP/su65-646.txt
```

本文: SU6.5（flip 前ハードニング）の対象外とし、SU7 以降の独立サイクルとする判断と理由（parity を測る対象そのものを作り替える作業は目的が逆を向く・SU3 の「単一ウィンドウ維持・2 窓分割は flip 後の別 spec へ」と整合）。

- [ ] **Step 10: コミットして PR を作る**

```powershell
git add docs/superpowers/specs/2026-07-21-phase2-softbuffer-migration-roadmap.md
git commit -F $env:TEMP/su65-task6.txt
npm run governance:check
git push -u origin HEAD && gh pr create --title "SU6.5: flip 前ハードニング（計測ゲート + #652/#643/#648）（#532 Phase 2）" --body-file $env:TEMP/su65-pr.txt
```

**PR 本文の注意**: `Closes #643` / `Closes #648` / `Closes #652` は書いてよいが、**`#532` を閉じる語（close/fix/resolve の 9 形）を本文のどこにも書かない**。マージ直前に `gh pr view <PR> --json closingIssuesReferences` を見て一覧に #532 が無いことを確認する（ルート `CLAUDE.md`「Git/GitHub 運用」の手順 1〜4）。

---

## 実行順と依存

```
Task 1（#643）→ Task 2（#648）→ Task 3（#652 純粋核）→ Task 4（#652 配線）
   → Task 5（計測ハーネス）→ Task 6（ゲート実行・release ビルドを含む）
```

- Task 3 → Task 4 は**型の依存**（`HotkeyFailureKind` と pending の型）。分割理由は、Task 3 が単体でテスト可能な純粋核で、Task 4 が Win32 依存の配線だから——レビュアーが片方だけ棄却できる境界に切ってある
- Task 5 は Task 1〜4 と独立（別ファイル）だが、Task 6 が release ビルドを要求するため実装完了後に置く
- Task 1・2・3・4 は全て `view.rs` に触れる。**並行実行しない**（ファイル境界の衝突・ルート `CLAUDE.md`「サブエージェント委譲と worktree」）
