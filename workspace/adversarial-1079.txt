敵対的レビュー: workspace/research.md（issue #1079）
検証環境: C:\workspace\Snotra、`cargo test -p snotra -q search_state` で 60 tests green を実測（後述）。
config.toml 実ファイル（%APPDATA%\Snotra\config.toml）の instant_command_prefix = "@"（既定と一致・research の前提が実運用点でも成立することを確認）。

## 壊せた項目

- [争点 2 / 「並び」の因果関係] research.md（および issue 本文）が描く 5 段の並びは、実際には**3 段で同じ欠陥が再現する**。「S の結果が folder 中に届く」（並び表の #3・`accept_worker_rows`）は必要条件ではない。 / 一次証拠: `search_state.rs:226-231` の `put_rows` は `self.dispatch.invalidate()` を**無条件に**呼び、`search_dispatch.rs:66-68` の `invalidate` も `self.pending = None` を**無条件に**代入する（`pending` が既に `None` でもガード無しで上書きする）。ゆえに `issue_search` → `enter_folder`（in-flight 温存）→ `on_escape`（`put_rows(f.restore_results,..)` が pending を無条件クリア）だけで `pending_seq() == 0` に到達し、`accept_worker_rows` を一度も呼ばずに `is_unsettled(false)` は偽になる。worker の遅着結果が実際に届くかどうかは結果に影響しない。 / この所見を偽にする手順: `search_state.rs` の `put_rows`（:226）と `search_dispatch.rs` の `invalidate`（:66）を再読し、`self.dispatch.invalidate()` の呼び出しに条件分岐（`if pending_seq != 0`相当）が本当に無いか確認する。ある場合はこの所見は誤り。
  - **含意**: research.md の並び表・issue 本文はどちらも「worker の遅着結果と folder ナビゲーションの競合」という**レース**として欠陥を語るが、実体はもっと単純で、**`on_escape` の `put_rows` が『いま本当に in-flight があるか』を見ずに無条件で in-flight を潰す**ことが直接の原因である。案 C（フラグ合成）を設計するなら、この単純化された因果（folder 復帰＝無条件 invalidate）を土台にした方が正確——「復帰時に行が query と対応していないか」ではなく「復帰の put_rows が本当に有効な差し替えだったか（in-flight を捨てたことに意味があったか）」を問う設計になりうる。
  - ただし claim 2（「5段はユニットテスト1本で再現できる」）自体は**否定されない**——研究の書いた 5 段のテストコードはそのまま緑に落ちる（過剰なだけで誤りではない）。壊れているのは「並びの因果説明」の精度である。

## 壊せなかった項目

- [争点 1] `run_search()` / `run_search_with()` の全腕（Folder / Tool / Results×{Plain, Instant, Command}）を読了。同期 `engine.search` を呼ぶ経路は 0 本。 / 一次証拠: `launcher_controller.rs:754-854`（`run_search_with` 全文）。Plain 腕は `search_tx.send(SearchRequest{..})`（:788）のみ。Instant 腕は `commands::instant::get_instant_commands`（`instant.rs:6-17`）を呼ぶが中身は `engine.lock()` して `config().instant_commands` を読むだけで `engine.search` は無い。Command（`/r`）腕は `engine.recent_history()`（:844）で `engine.search` ではない。Folder 腕・Tool 腕は engine に触れない。`instant_prefix()`（:660-673）は `engine.lock()` を取るが `engine.search` は呼ばない。同期 `engine.search` を呼ぶのは `on_enter` の flush ブロック（:1337-1341）だけである。 / この判定を偽にする手順: `launcher_controller.rs:754-854` を `engine.search` で grep し、`run_search`/`run_search_with` の呼び出しグラフ内（`on_enter` を除く）にヒットが無いことを確認する。

- [争点 1 補足] issue の「案 A の費用」記述が現在のコードの `run_search()`（字面どおりの関数）には当たらないという research.md の指摘（A-async vs A-sync の書き分け）は妥当。 / 一次証拠: `on_escape` doc（`search_state.rs:423-424`）「Escapeのたびに同期engine.searchをフレームへ乗せる」と、実際の `run_search()` の実装（上記）の不一致。issue 本文の「直すなら」節も文字どおり `run_search()` を名指している。 / この判定を偽にする手順: issue 本文と `on_escape` の doc コメントを再読し、案 A の costing が具体的にどの関数を指しているかを再確認する。

- [争点 2] `search_state.rs` の `#[cfg(test)] mod tests` は `super::*` を使い、`SearchState::new()` はコンストラクタ引数無し、`issue_search(Instant, Instant) -> u64`・`enter_folder(String) -> u64`・`accept_worker_rows(u64, Vec<SearchResult>, Instant) -> Option<Settled>`（`#[must_use]`）・`on_escape() -> EscapeOutcome`・`is_unsettled(bool) -> bool` は全て `pub fn` で同一モジュール内から直接呼べる。既存テスト（例: `late_worker_rows_are_dropped_in_folder_view:920-932`、`pending_seq_separates_the_two_drop_reasons:1084-1100`）が全く同じ操作列（`issue_search` → `enter_folder` → `accept_worker_rows`）を既に使っており、型・可視性の面で research.md が書いた検知器コードはそのままコンパイル・実行できる。`accept_worker_rows` の `#[must_use]` も `assert!(...is_none())` で正しく consume される（既存パターンと同型）。 / 実測: `cargo test -p snotra -q search_state` で既存 60 tests 全て green（コンパイル・実行基盤が健全であることを確認）。 / 一次証拠: `search_state.rs:477`（`issue_search`）, `:288`（`enter_folder`）, `:498`（`accept_worker_rows`）, `:430`（`on_escape`）, `:568`（`is_unsettled`）。 / この判定を偽にする手順: 実際に research.md の検知器コードを `search_state.rs` の `#[cfg(test)]` へ追加して `cargo test -p snotra -q` を走らせ、コンパイルエラー・assert 失敗の有無を見る（本レビューでは repo 非改変の制約のためコード上の型検算に留め、直接実行はしていない——推論の確度は高いが実行による接地はしていない）。

- [争点 3] `enter_tool` の呼び出し点は `shift_activate`（`search_state.rs:391` 内の `self.put_rows` 呼び出しの前段、`launcher_controller.rs:570`）のみ。`shift_activate` の呼び出し点は `on_enter`（:1351）のみ（grep 実測・`enter_tool(`/`shift_activate` の全ヒットを列挙、テストコード内の直接 `enter_tool(...)` 呼び出しはユニットテストのみで production 経路は 1 本）。クリック経路（`view.rs:1146`）は `activate_or_execute` を直接呼び、`shift_activate`/`enter_tool` を経由しない——ダブルクリックも扱わない設計（`results_view.rs:273`「ダブルクリックは扱わない」・§4.8）。 / 一次証拠: `Grep` 結果（`shift_activate|enter_tool\(|activate_or_execute\(|execute_tool_selected\(` の全ヒット）、`launcher_controller.rs:1320-1356`（`on_enter` が flush ブロック :1325-1348 → 活性化ブロック :1349-1355 の順で並ぶ）。 / この判定を偽にする手順: 同じ grep パターンを再実行し、`shift_activate(` の呼び出し点が `on_enter` 以外に増えていないか確認する。

- [争点 4] `launcher_controller.rs` に `#[cfg(test)]` は 0 件（grep 実測）。`src-tauri/tests/` ディレクトリは存在しない。`LauncherController::new` は `tauri::AppHandle` を要求し（:140）、`src-tauri/Cargo.toml` の `[dev-dependencies]` は `egui_kittest` のみで、`tauri` の test/mock feature は宣言されていない——`tauri::test::mock_app()` 系のハーネスをこの crate は持たない。 / 一次証拠: `grep -n "cfg(test)" launcher_controller.rs`（0 hit）、`Cargo.toml:56-57`（`[dev-dependencies]` の中身）。 / この判定を偽にする手順: `src-tauri/Cargo.toml` の `[dev-dependencies]` に `tauri = { features = ["test"] }` 相当が無いか、`src-tauri/tests/` が本当に存在しないかを再確認する。

- [争点 5] `config_watcher.rs` の `apply_config_change`（別スレッド・100ms debounce のファイル監視から起動）は UI の現在の view_kind を一切参照せずに `index_changed` なら無条件で `indexing::start_index_build(app)` を kick する（:157-162「ビルド進行中でも常に kick する」）。folder ビューを開いている最中でも config.toml の変更（設定アプリからの保存を含む）でこの経路は独立に発火しうる。`run_search_with` の Plain 腕は `self.indexing()`（`window_coordinator::read_indexing`・`AtomicBool` の `Relaxed` load・:93-97）が真なら早期 return で `set_results(Vec::new())` する（`launcher_controller.rs:778-781`）。ゆえに「folder 表示中に index rebuild が kick され、indexing==true のまま Escape で 案A/A-async の run_search() が撃たれる」という経路は構造的に到達可能——到達不能ではない。 / 一次証拠: `config_watcher.rs:157-162`、`window_coordinator.rs:93-97`、`launcher_controller.rs:778-781`。 / この判定を偽にする手順: `config_watcher.rs` の `apply_config_change` が呼ばれる条件（ファイル監視のトリガー）と `AppState.indexing` の書き込み元を再確認し、folder ビュー中にこの経路が本当にブロックされていないか調べる。

## ⚠️ 確信の持てない所見

- [争点 1 関連] research.md は案 A の残余費用（`instant_prefix()` の `engine.lock()`）を「案 A が持ち込む費用ではなく、既に存在する #1032 残余である」と述べるが、この言い回しは**呼び出し頻度の増加**を過小評価しうる。`run_search()` は現状 trailing poll / folder drain / `on_enter` から呼ばれているが、**folder からの Escape という新しい呼び出し点**を追加すれば、その分だけ `engine.lock()` を取る頻度が増える（folder を使うユーザー全員が escape のたびに新たに payする、という issue 本来の懸念の一部は生き残る）。「既存の残余」という表現は「新しい費用ではない」という含意を持たせがちだが、正確には「同じ種類の既存コストを新しい呼び出し点へ拡張する」である。 / なぜ確信が持てないか: 頻度増加の実害（フレーム落ち等）が実測されておらず、程度問題（頻度が無視できるほど小さいか）の判断は主観に依る。 / この所見を偽にする手順: 実機で folder 往復を高速反復し、`instant_prefix()` 呼び出し頻度・フレーム時間への影響を trace/計測して、既存の呼び出し頻度と比較する。

- [争点 3 関連] クリック経路（`view.rs:1146` の `activate_or_execute` 直呼び）は `on_enter` の flush（:1325-1348）を一切経由しない。理論上、打鍵直後（debounce 未発火・in-flight あり）に表示中の古い行をクリックすると、`is_unsettled` を経由する保護は無く「flush されていない行」で起動しうる。ただし、これは issue が扱う `is_unsettled` 述語の意味論違反（「最終クエリの結果が反映されていない」）とは**別の契約**——クリックは「今見えている行を起動する」契約であり、`rows_generation` 一致検査（#699）で「表示と起動の対象が食い違わないこと」だけを保証する設計に見える（`docs/architecture.md:228`「例外は Enter である」という記述はこの非対称を追認しているようにも読める）。 / なぜ確信が持てないか: この非対称が意図的設計か、単に #631/#1038 の射程が Enter に限定されていた（クリックは未検討）だけなのかを断定する一次資料（ADR・issue）を見つけていない。 / この所見を偽にする手順: `docs/adr/` 配下と #631 / #1038 の issue 本文を通読し、クリックが flush 対象外である設計上の理由が明文化されているか確認する。

## research.md に無いが指摘したいこと

- 「並び」節と検知器コードは issue 本文をほぼ逐語で継承しているが、上記「壊せた項目」で示した通り、真の最小再現は 3 段（`issue_search → enter_folder → on_escape`）で足りる。案 C の設計（未解決の疑問 1〜4）を詰める際は、5 段の「レース」ではなく「`on_escape`（および `enter_tool` 以外で `put_rows` を呼ぶ全経路）が無条件に in-flight を潰す」という単純な事実から出発した方が、フラグを立てる条件（未解決の疑問 1）を素直に導けるはず——「folder から復帰した」こと自体ではなく「`put_rows` を呼ぶ直前に `pending_seq() != 0` だったか」を捕まえれば足りる可能性がある。
- `cargo test -p snotra` は `[lib]` を持たないため `--lib` では失敗する（`src-tauri/CLAUDE.md` 既知）。`-q` 付きで対象を絞った `cargo test -p snotra -q search_state` は正常に走り、60 tests green。全crate合計のテスト数・時間までは今回計測していない（対象を絞った検証で目的は足りると判断した）。
