# 壊せた項目

- **軽微な引用位置のずれ: `ConfigError::VisibleRowsZero`（`config.rs:1065`）**
  一次証拠: `snotra-core/src/config.rs` を実読み。
  ```
  1064	        // Appearance validation
  1065	        if self.appearance.effective_visible_rows() == 0 {
  1066	            errors.push(ConfigError::VisibleRowsZero);
  1067	        }
  ```
  `:1065` は `if` 条件式の行であり、`ConfigError::VisibleRowsZero` という識別子そのものが現れるのは `:1066` である。命題 P1 の内容（本体経由の呼び出しが 0 件・`validate()` が config.toml の手編集を止めない）自体は真だが、シンボル位置の引用としては 1 行ずれている。
  この所見を偽にする手順: `Read C:\workspace\Snotra\snotra-core\src\config.rs offset=1061 limit=8` を実行し、`ConfigError::VisibleRowsZero` という字面が 1065 行目に現れることを示せば偽になる（実際には現れない——上記の通り 1066 行目）。

# 壊せなかった項目

- **P1（`visible_rows = 0` は本体で到達可能）**: `snotra-core/src/config.rs` の `Config::load_from_dir_reporting`（:969-1019）・`apply_migrations`（:786 系列）・`migrate_legacy_count_params`（:843）・`resolve_count_param_defaults`（:879）を全文読み、`.validate()` の呼び出し箇所を `Grep '\.validate\(\)'` で全文検索した。結果: config.toml から読み込む経路（`load_from_dir_reporting`）にも、ホットリロード経路（`src-tauri/src/config_watcher.rs` の `apply_config_change` / `should_apply_config_change`）にも `.validate()` 呼び出しは一切無い。`migrate_legacy_count_params` / `resolve_count_param_defaults` はどちらも `Option::get_or_insert*` で「None のときだけ埋める」実装のため、`visible_rows = 0`（`Some(0)`）は上書きされず生き残る。`.validate()` の呼び出し元は grep で 32 件ヒットし、本体側（`src-tauri`）からの呼び出しは 0 件、生きた呼び出し元は `snotra-settings/src/app.rs:211` と `snotra-settings/src/tabs/backup.rs:274` の 2 件のみ、残り 30 件は `snotra-core/src/config.rs` の `mod tests`——研究文書の記述と完全に一致した。
  壊そうとした具体的な手: config_watcher 側にだけ隠れた validate 呼び出しがないか、Config を deserialize する別経路（インポート等）が無いかを探したが見つからなかった。
  なぜ壊れなかったか: 主張どおり、コード上に阻止機構が存在しない。

- **P2（連言④が偽になるのは `max_results == 0` のときだけ）**: `layout::Metrics::from_config`（`layout.rs:48-59`）を読み、`row_height = (f + path_size(font_size) + row_padding as f64 + 4.0).max(24.0)` を確認。`font_size: u32` ゆえ `f ≥ 0`、`path_size` は `.max(9.0)` を持つため `≥ 9`、合計は最小でも `13`、その上に `.max(24.0)` で下限 24 を敷く。production での `row_height` の出所を `Grep 'row_height:'` で全文検索し、`ResultsInputs` を構築する生きた呼び出しが `window_coordinator.rs:826`（`row_height: i.row_height`）の 1 か所だけであること、その `i.row_height`（`DriveResultsInputs.row_height`）は `view.rs:1285` の `row_height: metrics.row_height` から来ており、`metrics` は `visual::visual_snapshot` 経由で同じ `Metrics::from_config` に到達することを確認した。`present_results` の呼び出しも production で `window_coordinator.rs:821` の 1 か所のみ（他は全て `layout.rs` の `mod tests`）。ゆえに production で `results_window_height` に渡る `row_height` は必ず `≥ 24.0` であり、`0.0` を返しうるのは `max_results == 0` の分岐だけである。
  壊そうとした具体的な手: `Metrics::from_config` を経由しない `row_height` の別経路（例えば `results_view.rs` の描画用行高が別に窓高さ判定へ紛れ込んでいないか）を `Grep 'row_height'` で洗ったが、`results_view.rs` 側は描画専用でゲート判定には使われていない。
  なぜ壊れなかったか: production の row_height 生成点が 1 つに集約されており、床 24.0 を持つ式しか存在しない。

- **P3（起動の入口は 4 か所で尽きる）**: `Grep '\.activate_or_execute\('` を `src-tauri/src` 全体に対して実行し、ヒットが `launcher_controller.rs:579` / `:622` / `:1418` と `view.rs:1175` の 4 件だけであることを確認した（テストコードも含めた全文検索）。`shift_activate` の `tools >= 2` 枝（:608-620）は `self.state.enter_tool(...)` を直接呼び `activate_or_execute` を経由しないことも確認済み（ADR-activation-gate-placement 却下 1 の記述と一致）。
  壊そうとした具体的な手: トレイメニュー経由の起動（`commands/launch.rs` の `pub` 関数群）・スラッシュコマンド経由（`execute_slash`）が別途行を起動していないか確認したが、トレイメニューは egui の `results` 一覧とは別の独立した起動経路（`launch_item_with_state` 等）であり、`SPEC.md` §4.7 の表示ゲートが対象とする「results 窓に出ている行」の起動とは無関係（トレイの「最近使った項目」メニューは表示ゲートの対象外の別 UI 面）。スラッシュコマンドは `execute_slash` 経由で `activate_or_execute` を通らないが、これは行の起動ではなくコマンド実行であり、研究文書の母集団（行の起動）の対象外として扱うのが妥当。
  なぜ壊れなかったか: 母集団を「`activate_or_execute` の呼び出し点」に明示的に絞った定義であり、grep で網羅的に確認でき、実際に 4 件で尽きている。

- **P4（連言④は③と射程が違う・全ビューに効く）**: `search_state::plain_results_hidden`（`search_state.rs:727`）の実装を読み、引数が `(view_kind, instant_rows, indexing)` の 3 つのみで `max_results` も `row_height` も受け取らないことを確認。`present_results`（`layout.rs:401-408`）の実装は `desired_height = results_window_height(i.max_results, i.row_height)` を `view_kind` に関わらず一様に計算しており、連言④はビュー種別に依存しない。`SPEC.md` §4.5 の「この高さは `results` に描くすべてのビューへ一様に適用される——通常結果・フォルダ展開・ツール選択メニュー・インスタントコマンド行」という文言も実在確認した（`SPEC.md:188`）。
  壊そうとした具体的な手: `ViewKind::Tool` や `Folder` のときだけ別の高さ計算式を通る特別経路がないか `present_results` の呼び出し元を再確認したが、`window_coordinator.rs:821` の 1 か所だけで view_kind 分岐は存在しない。
  なぜ壊れなかったか: 構造上、高さ計算とビュー種別は独立している。

- **P5（`max_results` は起動側へ配られていない）**: `Grep 'max_results|effective_visible_rows'` を `launcher_controller.rs` に対して実行し 0 件を確認。`window_coordinator.rs:747`（`fn max_results`）の doc「読み点の制約を持たないため `DriveResultsInputs` へは載せず、driver の内側で読む」を確認し、実際に `drive_results_window`（:821）内で `max_results(app)` をインラインに呼んでいる（`DriveResultsInputs` 経由ではない）ことをコードで確認した。
  壊そうとした具体的な手: `LauncherController` 構造体のフィールド一覧（`launcher_controller.rs:102-137`）を読み、`max_results` や `visible_rows` に相当するフィールドが無いことを確認。
  なぜ壊れなかったか: 主張どおり、起動側のコードには当該値への参照が一切無い。

- **P6（下限 1 clamp 案は却下済みの族）**: `docs/adr/ADR-results-fixed-height.md` を全文読み、却下 5「最大表示件数が 0（`config.toml` の手編集で到達する）のとき 1 行を床にする」の文言を確認した。「最大表示件数」という語は `SPEC.md` §4.5（`appearance.visible_rows`／`effective_visible_rows()`）の語彙と一致しており、`search.result_limit`（検索結果リストの最大長）とは別概念であることを `snotra-core/CLAUDE.md`「件数パラメータ」節（`visible_rows` = 可視行数、`result_limit` = 検索・フォルダの結果リスト最大長）で確認した。すり替えは無い。
  壊そうとした具体的な手: ADR 却下 5 が「窓の外にある候補が多い場合の話（result_limit）」を指している可能性を疑い、ADR 文脈（#835 の高さ固定化・`results_window_height(max_results, row_height)` の議論）を読み直したが、`results_window_height` の第 1 引数はまさに `effective_visible_rows()`（`window_coordinator.rs:750`）であり、取り違えは無かった。
  なぜ壊れなかったか: 用語・関数の対応関係が一貫している。

- **P7（`plain_results_hidden` は④を含まない）**: `search_state.rs:727` の実装 `pub fn plain_results_hidden(view_kind: ViewKind, instant_rows: bool, indexing: bool) -> bool { indexing && matches!(view_kind, ViewKind::Results) && !instant_rows }` を確認。引数はちょうど 3 つで `max_results` も `row_height` も入らない。
  壊そうとした具体的な手: 呼び出し元（`launcher_controller.rs:557` / `:586`、`view.rs:1123`）で第 4 引数的な値が別途合成されていないか確認したが、いずれも 3 引数のみで呼ばれている。
  なぜ壊れなかったか: 関数シグネチャそのものが証拠であり、動かしようがない。

- **技術的制約 1（`max_results` を配ると読み点の制約が生まれる）**: `AppState.indexing`（`state.rs:25`）が `AtomicBool` であること、`visible_rows` は `read_config`（`mod.rs:413` 系）経由の `RwLock<Config>` live-read であることをコードで確認し、研究文書が両者を区別している記述と矛盾しないことを確認した。
  壊そうとした具体的な手: `visible_rows` も実は AtomicBool 的な即時反映機構を持たないか確認したが、`config_watcher.rs` の `apply_config_change` が `update_config`（`engine.rs`）越しに `RwLock<Config>` を書き換える設計であり、読み取り側は毎フレーム `read_config` を呼ぶ形——記述通り。
  なぜ壊れなかったか: 型・機構の区別は実装と一致する。

- **技術的制約 2（`row_height` も起動側は持たない）**: 上記 P2/P5 の検証と同じ grep で確認済み（`launcher_controller.rs` に `row_height` の参照 0 件）。
  なぜ壊れなかったか: 同上。

- **技術的制約 3（ゲートは `activate_or_execute` の冒頭・tool/instant の dispatch より前に置く必要がある）**: `activate_or_execute`（`launcher_controller.rs:535-571`）の実装で、`plain_results_hidden` チェック（:557-563）が `if self.state.view_kind() == ViewKind::Tool`（:564）より前に置かれていることを確認した。現行の①（#1077 のゲート）がまさにこの位置にあり、④を同じ述語へ足すなら同じ位置に置く必要があるという主張と整合する。
  なぜ壊れなかったか: 現行実装が同じパターンを踏襲している。

- **技術的制約 4（仕様変更である）・5（`Config::validate` を適用経路へ足す案は不採用の方向）・6（`on_enter` の空チェックは②側で④とは独立）**: 制約 6 は `on_enter`（`launcher_controller.rs:1414`）の `if !self.state.results().is_empty()` を実読みし、`results()` の非空判定であって `visible_rows`/`row_height` を一切参照しないことを確認した。制約 4・5 は AGENTS.md のワークフロー規範・ADR 却下 5 の論拠の妥当な適用であり、事実についての反証点を見つけられなかった。
  なぜ壊れなかったか: 制約 6 はコード事実、制約 4・5 は規範の妥当な適用であり反証可能な事実命題としての誤りが無い。

- **未解決の疑問 Q1（実機再現の前提: 計器と対象が同じ枝を通るか）**:
  (a) `visible_rows = 0` が `effective_visible_rows()` を経由して `window_coordinator::max_results` へ届く経路を追跡: `read_config`（`mod.rs`）→ `AppState`/`Engine` の `config_handle()`（`RwLock<Config>`）。起動時ロードは `Config::load()` → `apply_migrations()`（clamp なし）、ホットリロードは `config_watcher::apply_config_change` → `update_config`（同じく clamp/validate なし）。いずれの経路でも `visible_rows = 0` は素通りする。**計器が測る枝と issue が触る枝は一致する**。
  (b) `scripts/lib/SnotraSmoke.psm1` の `New-SnotraVerificationProfile`（:205-282）を全文読み、既定で書く TOML は `[hotkey]` / `[appearance] window_width, show_icons` / `[general] auto_update="disabled"` / `[paths]` のみで、**`visible_rows` は既定で一切書かれない**（既定値 8 に落ちる）。`visible_rows = 0` を仕込むには呼び出し側が `-AdditionalSections`（または将来 `-GeneralSection` 相当の `[appearance]` 追加行）で明示注入する必要がある——**research.md はこれを明記していないが、既存の `-AdditionalSections` パラメータで実現可能であり、計器の構造自体が測定を妨げる欠陥ではない**。
  (c) `Grep '"egui_launch"|egui_results:show'` で production の発火点を確認: `launcher_controller.rs:246`（`crate::trace_main("egui_launch", ...)`、`activate` 関数内）と `window_coordinator.rs:855`（`crate::trace_main("egui_results:show", ...)`）がいずれも実在する trace イベントであることを確認した。
  (d) ユーザーの実 `config.toml`（`%APPDATA%\Snotra\config.toml`、`C:\Users\Eoh\AppData\Roaming\Snotra\config.toml`）を読み取り専用で確認: `visible_rows = 8`（既定値のまま）。**本 issue の危険な値には現在到達していない**——読むだけで書き換えは行っていない。
  結論: Q1 の実測前提は概ね妥当。唯一の補足点は (b) で、smoke プロファイルは既定で `visible_rows` を注入しないため、実機再現には `AdditionalSections` に `visible_rows = 0` を明示追加する 1 手が要る（これは research.md の欠落というより、plan.md 側で埋めるべき実装詳細）。

- **Q2（設計選択肢）・Q3（→/← のフォルダ突入を止めるか）・Q4（規範の置き場所）・Q5（要求判断）**: いずれも「未決定の設計上の疑問」として提示されており、事実主張ではなく検討材料の列挙である。Q3 の前提（「④偽では『突入すれば可視へ戻る』という #1077 の正当化理由が成り立たない——窓高は 0 のまま」）は、`present_results` が `view_kind` に依らず一様に高さを計算する（P4 で確認済み）という事実と整合しており、論理的に妥当。
  なぜ壊れなかったか: これらは疑問として明示されており、断定的な事実主張ではないため反証の対象にならない。

- **「関連ファイル・シンボル」節の行番号引用**: `results_window_height`（:143）／`present_results`（:401）／`ResultsInputs`（:343）／`Metrics::from_config`（:49）／`activate_or_execute`（:535）／`shift_activate`（:576）／`on_enter`（:1380）／`activation_uses_the_frame_indexing_value_not_a_live_read`（:1498）／`activation_entry_points_consult_the_display_gate`（:1533）／`method_body_is_line_ending_agnostic`（:1472）／`max_results`（:747）／`FrameIndexing`（:111）／`read_indexing`（:125）／`DriveResultsInputs`（:776）／`plain_results_hidden`（:727）／`effective_visible_rows`（:364）／`validate`（:1061）を実読みで全数照合し、全て一致することを確認した。
  なぜ壊れなかったか: 実ファイルの行番号と完全一致。

# ⚠️ 確信の持てない所見

- **「関連ファイル・シンボル」の `view.rs` `indexing_is_read_exactly_once_per_frame`（`:1316` 付近）**: 実際の定義行は `:1325` で、9 行のずれがある。ただし研究文書は「付近」と明記して曖昧さを許容しているため、これを誤りと断定するのは厳しすぎる可能性がある。確信度: 低（誤りと言うには研究文書側の申告自体が近似を許容している）。確かめるならこの 1 点は既に確認済みで、争点は「『付近』の許容範囲をどこまでとみなすか」という評価軸の問題であり、一次証拠の追加取得では解決しない。

- **P3 の母集団定義（「起動の入口」をどこまで含めるか）**: research.md は母集団を `activate_or_execute` の呼び出し点に限定しているが、issue #1106 自体は「Enter・Shift+Enter・クリックが行を起動する」という現象を問題にしており、`shift_activate` の `tools >= 2` 枝（`enter_tool` 直呼び）は「起動」ではなく「ツール選択モードへの遷移」である一方、この遷移自体も「画面に出ていない行を対象に始まる」という性質を技術的制約 3 の文脈で指摘している。この遷移を「起動の入口」に数えるべきか否かは定義次第であり、research.md 内では両方の扱い（P3 では数えず、技術的制約 3 では別途論じる）が整合的に保たれているため、矛盾ではなく意図的な区別と判断したが、完全な確信は持てない。確かめるには plan.md 段階でこの区別を明示的に定義させる必要がある。
