## 壊せた項目

- [P3・§3「限界」] 主張（逐語）: 「`ci.yml`（191 行）と `.claude/hooks/post-edit.mjs`（325-326 行）はどちらも `-D warnings` を渡すので現状は両方で赤いが、`cargo check --workspace`（カテゴリ A・フラグなし）では warn 止まりである。」
  反証: `#[expect(clippy::disallowed_methods, ...)]` を 4 か所へ付け、`disallowed-methods` から `Engine::config` を外した状態（M3 相当）を実際に作り、`cargo check --workspace --all-targets` と `cargo check -p snotra --all-targets` を実行した。両方とも診断が **1 行も出ず**（`warning:` すら無し）、`Finished` のみで exit 0 だった（2 回連続で確認）。一方 `cargo clippy --workspace --all-targets`（`-D warnings` 無し）では同じ状態で `warning: this lint expectation is unfulfilled` が 4 件出た。
  「warn 止まり」という表現は「診断は出るが非致命」を意味するが、実際は `cargo check` では診断そのものが**出ない**（`unfulfilled_lint_expectations` は clippy の lint 実行を通じてのみ評価され、`cargo check` は clippy を起動しないため）。「赤くなる条件」の分析（`-D warnings` 依存）自体は正しいが、対比として挙げた「flagなしでは warn」は「flag なしで clippy を回せば warn、check だけなら無診断」の 2 段を 1 段に圧縮しており、後者の場合を誤って記述している。
  この所見を偽にする手順: `src-tauri/clippy.toml` の `disallowed-methods` 配列から任意の 1 エントリを外し、対応する呼び出し点に `#[expect(clippy::disallowed_methods, reason = "...")]` を付けた状態で `cargo check --workspace --all-targets` を実行し、`unfulfilled` を含む行が出力に含まれるかを確認する（出れば本反証は誤り）。

- [測定環境（プロンプト冒頭で名指しされた項目）] 主張（`src-tauri/clippy.toml` 46-48 行、`G-clippy-disallowed.mjs` の同旨コメント、research.md §3 冒頭「clippy.toml は cargo の fingerprint に入らないため、toml だけを触った回は毎回 touch src-tauri/src/main.rs を挟んだ」）: 「clippy.toml だけを変更して同じコマンドを打つと、キャッシュ replay で設定を適用せずに exit 0 を返す（`.rs` を触るか `cargo clean -p snotra` を挟む必要がある）」。
  反証: 現在の環境（rustc 1.97.1 / clippy 0.1.97）で次を実測した。(1) baseline を `cargo clippy --workspace --all-targets -- -D warnings` で exit 0 と確認 → (2) `clippy.toml` に `Engine::config` の禁止エントリを追加（`.rs` は一切触らない）→ 直後に同じコマンドを実行 → **4 件のエラーが正しく出て exit 101**。(3) 同じエントリを削除（`.rs` は触らない）→ 直後に再実行 → **0 件・exit 0** に正しく戻る。2 往復とも `.rs` touch や `cargo clean` なしで即座に反映された。
  つまり、少なくともこの環境・このトゥールチェインでは「clippy.toml は cargo の fingerprint に入らない」という前提そのものが成立していない（過去に実在した clippy の既知の挙動が、現在のバージョンでは修正されている可能性が高い）。research.md 自身の M1〜M5 の数値結果は「常に touch していた」ため無効ではないが、**この前提を「誰も見ていない残余」として将来の実装判断に持ち込むと、実際には不要な手順を必須と誤認させる**（実害は小さいが、後述の「見落ち」の方がより重要な誤りを引き起こしうる）。
  この所見を偽にする手順: `cargo clean -p snotra` を挟んだ完全なコールドキャッシュ状態から同じ実験を行い、toml のみの変更が exit 0 のまま反映されない（本当にキャッシュ replay が起きる）ことを再現する。今回はコールドキャッシュでの再測定はコスト（フルリビルド）を理由に行っていない——**この反証は温かいキャッシュ限定の観測であり、コールドキャッシュでの挙動までは確認していない**。

## 壊せなかった項目

- [P1] 主張: `Engine::config` の呼び出し点は src-tauri に正確に 4 件（`icon.rs:17` / `launch.rs:107` / `launch.rs:158` / `config_watcher.rs:87`）で、`--all-targets` を付けても増えない。他 crate（snotra-settings / snotra-egui-runtime / snotra-core 自身 / 各 `tests/`）に禁止の影響を受ける呼び出しは無い。
  確かめ方: `disallowed-methods` へ `Engine::config` を実際に追加し、`cargo clippy --workspace --all-targets -- -D warnings` を実行。出力は上記 4 件のみで、`snotra-core`（`Checking`・エラー無し）・`snotra-egui-runtime`（同）・`snotra-settings`（同）は無傷、`snotra`（src-tauri）だけが `bin "snotra"` と `bin "snotra" test`（`--all-targets` により追加された test ターゲット）の 2 ターゲットでコンパイル失敗した。`grep -rn "\.config()"` で全 workspace を走査し、`snotra-core/src/engine.rs:470,479,584` の 3 件は `#[cfg(test)] mod tests`（327 行開始）内の自己呼び出しであり、`src-tauri/clippy.toml` の適用範囲（自身のディレクトリを起点に上へ辿る clippy.toml 探索）に snotra-core は入らないため無関係であることも確認した。`src-tauri/tests/` ディレクトリは存在しない（`--all-targets` の増分が無いことと整合）。
  限界: clippy.toml のディレクトリスコープ探索アルゴリズムそのもの（sibling crate に波及しないこと）は clippy の公式ドキュメントで裏取りしておらず、今回は「実際に workspace 全体で clippy を回して他 crate が無傷だったこと」という**外延的な実測**のみに依拠している。将来 clippy のバージョンが上がりこの探索規則が変わる可能性は排除できない。

- [P2] 主張: `#[expect(clippy::disallowed_methods, reason = "…")]` を 4 件に付けても、`cargo check --workspace` / `cargo test -p snotra` / `cargo doc --workspace --no-deps --document-private-items` / `cargo fmt --all -- --check` のどれも壊れない。
  確かめ方: 4 か所すべてに `#[expect(...)]` を付与し、`cargo fmt --all -- --check`（exit 0）・`cargo clippy --workspace --all-targets -- -D warnings`（exit 0、unfulfilled 0 件を `grep -i unfulfilled` で確認）・`cargo check --workspace --all-targets`（exit 0）・`cargo test -p snotra -q`（`292 passed; 0 failed; 4 ignored` で研究文書の数値と一致）・`cargo doc --workspace --no-deps --document-private-items`（exit 0、`Generated ... snotra\index.html`）を実行し、すべて確認どおりだった。
  限界: `cargo doc` の出力を「intra-doc link 切れが 0 件」まで詳細に検査していない（exit 0 と生成成功のみ確認）。ただし `#[expect]` は関数内の `let` 文に付く属性でありドキュメント生成に影響する形跡が無いため、リスクは低いと判断する。

- [P4] 主張: 同じ `clippy.toml` に在る 2 つの前例（`style_ui` を禁止しなかった／`all_styles_mut` を禁止して `view.rs:516` の `#[allow]` で開けた）の差は「正当な使用の件数」ではなく「呼び出し点ごとの判定義務の有無」である。
  確かめ方: `clippy.toml` 16-20 行（`style_ui`/`settings_ui` を含めなかった理由）と `view.rs` 500-517 行（`apply_exact_hit_test_style` の doc と `#[allow]` の使用箇所）を読み、`src-tauri/CLAUDE.md`「config の live-read」節の「呼び出し元を辿って判定すること」という要求文言も確認した。両者の記述は research.md の要約と整合する。
  限界: これは**価値判断・解釈の妥当性の主張**であり、コマンドで真偽を確定できる性質の命題ではない。「義務の有無」という切り口が唯一の正しい説明であるかどうかは、コードを読んでも決定不能——research.md 自身も「⚠️ この整理を『正当な使用の集合が閉じているから』という形で書くのは弱い」と留保しており、この論点は「壊す/壊せない」の外側にある(裁定材料であって事実の主張ではない)。

- [P5] 主張: `Engine::config_handle` の製品呼び出し点は `main.rs:242` の 1 件のみで、`state.rs:79` と `commands/system.rs:45` は `#[cfg(test)] mod tests` 内のテストヘルパーである。
  確かめ方: `grep -rn "config_handle"` で全 workspace を走査し 3 件の呼び出しを確認した上で、`state.rs`（70 行 `#[cfg(test)] mod tests {` → 76 行 `fn test_state()` → 79 行 `config: engine.config_handle()`）と `commands/system.rs`（33 行 `#[cfg(test)] mod tests {` → 42 行 `fn test_state(indexing: bool)` → 45 行 `config: engine.config_handle()`）を実際に読み、両方とも `#[cfg(test)]` 内であることを確認。`main.rs:240-248` の `AppState` 構築（`app_state` 変数）が製品側の唯一の構築点であることも確認した。
  限界: 無し（このコード上の事実は Read で完全に裏取りできる性質のもので、疑う余地は残っていない）。

- [P6] 主張: 兄弟 issue #1123 が採択されても、`Engine::config` を禁じる限り注釈は 0 件にならない（`config_watcher.rs` が残る）。#1123 本文の「#1122 の機構化が `#[allow]` 無しで済む」は不正確である。
  確かめ方: `gh issue view 1123` で本文を直接取得し、「得るのは統治だけである（規則が 1 本になる・スレッドの推論が要らなくなる・#1122 の機構化が `#[allow]` 無しで済む）」という一節が逐語で存在することを確認した。同じ本文が「（`config_watcher` の旧 config 読みは別勘定である——条項が『読みではなく適用手続きの一部』という手続きの理由で射程外にしている。本 issue の対象ではない。）」と明記しており、#1123 のスコープが `config_watcher.rs:87` を対象外としていることも確認した。`config_watcher.rs:87` は M1 の実測でも `Engine::config` の disallowed-methods 違反として実際に検出されている（4 件中の 1 件）ため、#1123 が採択されて残り 3 件が `read_config` に移っても、lint（`Engine::config` を丸ごと禁止する機構）の観点では `config_watcher.rs:87` に注釈が必要なままである——lint はメソッド呼び出しの構文的一致でのみ判定し、「適用手続きの一部だから対象外」という手続き上の理由を理解しない。
  限界: 「#1123 の本文が不正確」という評価そのものは research.md の解釈であり、#1123 の著者（同一人物）が実際に「機構化＝lint 全体の話」ではなく「注釈が要る 4 件のうち 3 件の話」という限定的な意味で書いた可能性は文面からは排除できない（本人に確認していない）。

## ⚠️ 確信の持てない所見

- clippy.toml のスコープが「そのファイルが置かれたディレクトリを起点に、コンパイル対象クレートの `CARGO_MANIFEST_DIR` から上方向に探索して見つかった最初のもの」という仕組みで、`snotra-core` 等の sibling crate には**構造的に**波及しえない、という理解を前提に P1 を確認した。今回はこの仕組みを**外延的に**（workspace 全体で clippy を回して他 crate が無傷だったことで）確認したのみで、`clippy-driver` のソースやドキュメントでこの探索アルゴリズムそのものを裏取りしていない。
  なぜ確信が持てないか: 探索アルゴリズムの正確な仕様（`CARGO_MANIFEST_DIR` を起点にするのか `CARGO_WORKSPACE_DIR` を起点にするのか等）を一次資料で確認していないため。
  この所見を偽にする手順: `clippy.toml` を `snotra-core/clippy.toml` としてもコピーで置いてみて `Engine::config` 相当の禁止（例えば別のメソッド）を書き、`snotra-core` 内で違反するコードが実際に検出されるかを確認する。それでも `src-tauri/clippy.toml` が `snotra-core` の lint に影響しないことが確認できれば、探索がディレクトリスコープであることのより直接的な証拠になる。

- 「clippy.toml が cargo の fingerprint に入らない」という主張が**このバージョンでは成立しない**という私の反証は、温かいキャッシュ下でのみ確認した。コールドキャッシュ（`cargo clean -p snotra` 後）でも同じ結果になるかは未検証であり、もし「初回ビルド時だけキャッシュに登録され、2 回目以降は正しく tracked される」といった非対称な挙動があった場合、私の反証はコールドキャッシュの挙動までは否定できない。
  なぜ確信が持てないか: フルリビルドのコスト（数分〜数十分）を理由に実施しなかったため。
  この所見を偽にする手順: `cargo clean -p snotra` の直後に `cargo clippy --workspace --all-targets -- -D warnings` を実行して exit 0 を確認 → `.rs` を一切触らず `clippy.toml` へ違反を注入 → 同じコマンドを再実行し、4 件のエラーが出るかを見る。出なければ元の「誰も見ていない残余」の記述はコールドキャッシュ限定で正しいことになる。

## 見落ち（research.md が一切触れていないこと）

- **`.claude/hooks/post-edit.mjs` の `selectChecks("src-tauri/clippy.toml")` は空配列 `[]` を返す**（node で `selectChecks` を直接呼んで実測）。`clippy.toml` は `.rs` でも `Cargo.toml`（ルート）でも `config.toml` / `tauri.conf.json` でも `CHECK_DEFINITION` 集合の要素でもないため、どの分岐にも当たらない。つまり `clippy.toml` **単体**を編集した場合、PostToolUse hook は fmt も clippy も何も走らせない——「沈黙 = 合格」という規約が成立するのは `selectChecks` が検査を割り当てたファイルだけであり（`AGENTS.md`「条件別チェック」表・`docs/build-commands.md` の該当節が明記する通り）、`clippy.toml` はその母集団の外である。
  これは research.md が触れている「clippy.toml は cargo の fingerprint に入らない（.rs を touch しないと嘘の exit 0）」という論点とは**別の・より根の深い問題**である——fingerprint の話は「正しいコマンドを打っても exit 0 になりうる」という話だが、こちらは「そもそも正しいコマンドが自動では一度も打たれない」という話である。今回の私の実測では clippy.toml 単体の編集直後でも clippy が正しく走った（前掲の反証）が、それは**私が手動でコマンドを叩いたから**であり、実装者が `#[allow]`/`#[expect]` を伴わず `clippy.toml` だけを編集してエディタで保存した場合、PostToolUse hook は何も言わない。実装時にこの機構化を進めるなら、「clippy.toml と対応する 4 件の `.rs` を同時に編集する」（今回の研究の M2 の手順どおり）ことが実質的な安全策になるが、research.md はこの hook 側の空カバレッジそのものを明示的には指摘していない（§3 冒頭は計測手順としての touch にのみ触れ、実装フロー上の hook カバレッジには触れていない）。

- research.md は `disallowed-methods` に `Engine::config` を追加する際、`REQUIRED_DISALLOWED_METHODS`（`G-clippy-disallowed.mjs` 52-62 行）への登録を「必要な変更」として issue #1122 本文・research.md 双方が挙げているが、**登録を忘れた場合に何が起きるか**（`governance:check` は赤くなるが、`clippy` 自体は禁止が効いているので緑のまま——つまり実装は正しく動くのに governance の母集団追跡だけが欠落する、という非対称な失敗モード）を明示的には書いていない。実装時に「clippy が緑だから完了」と早合点するリスクがある。

- research.md は「`clippy.toml` の禁止行を書いた PR の CI」でこの機構が実際に赤くなること（`ci.yml` の `cargo clippy` ステップが本当に `src-tauri/clippy.toml` を認識すること）を CI 上で実測していない（ローカルの `cargo clippy --workspace --all-targets -- -D warnings` でのみ検証）。ローカルと CI の toolchain バージョンが一致している保証（`rust-toolchain.toml` 等の固定）は確認していない。

## git status --short

```
?? workspace/
```

（`workspace/` は本タスクの成果物ディレクトリのみ。`src-tauri/clippy.toml` / `src-tauri/src/commands/icon.rs` / `src-tauri/src/commands/launch.rs` / `src-tauri/src/config_watcher.rs` / `src-tauri/src/main.rs` はすべて `git checkout --` で復旧済みで、`git diff --stat` の出力は空。足場は残っていない。）
