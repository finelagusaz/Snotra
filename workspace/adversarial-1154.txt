敵対的レビュー — research.md（#1154）2026-08-20

すべて `C:/workspace/Snotra` を cwd として、lib.mjs の実装（makeSnapshot / allHeadingRefDocs /
refScanLines / HEADING_REF / collectAnchors / resolveRefTarget）を import した独立スクリプトで
再導出した（scratchpad 配下の使い捨てスクリプト、リポジトリへは書いていない）。

====================================================================
■ 壊せた項目（重大）
====================================================================

[1] 「折れた正準形参照は今日 20 件在る」は過小評価である。真の値は最低でも 33 件。
    確信度: 高（2 系統の独立実装で再現）

    再現手順（これを偽にする手順）:
      1. research.md が書く数え方（allHeadingRefDocs → refScanLines → 行末が対象綴りで終わり、
         物理的に次の scanned 行が「行頭のコメント記号/>/箇条書きマーカーを落として」「 で
         始まる）をそのまま実装すると 20 件になる（research.md の一覧と 1 件残らず一致——
         これ自体は正しく再現できた）。
      2. しかし research.md 自身が「候補1の死角」として名指ししている
         「「」の内側での折り」（対象へのバッククォート + `「` まで同一行に既に開いており、
         ラベル本文だけが次行へ折れて `」` が次行以降にある形）を、
         「隣接する 2 つの scanned 行を連結し、連結後にだけ現れる HEADING_REF の一致
         （かつ一致が境界を跨ぐ）」という機械的な方法で数えると、
         **さらに 13 件**が見つかる。合計 33 件（内訳は下記）。
      3. 個々を実地で裏取り済み。例:
         - `snotra-core/src/indexer.rs:60-61`:
           `` `PERFORMANCE.md`「パスクエリ全走査のコスト — `normalized_keys` を ``
           の次行が `` 保持するか導出するか」）。 `` — `PERFORMANCE.md:301` の
           `### パスクエリ全走査のコスト — normalized_keys を保持するか導出するか` に対応する
           **正規の・現存する**見出しを指しており、正準形の参照として完全に有効なはずが、
           現状ではラベルが 2 行に割れているため G-heading-refs は一致すら生成しない
           （`checked` にも `findings` にも現れない——20 件の「折れ」より深く沈黙している）。
         - 同型を `snotra-core/src/index_tree.rs:148,181`・`src/search/build.rs:77`・
           `src/search/footprint.rs:206`・`src/str_arena.rs:5`・`src/indexer.rs:479,1030`・
           `src-tauri/src/egui_shell/launcher_controller.rs:1828`・`layout.rs:403`・
           `results_window.rs:111`・`src/main.rs:614`・`src/state.rs:37` で確認。
           いずれも `` ` `` と `「` は同一行、`」` を含むラベル本体だけが次行へ流れている。
      4. 「うち 1 件が繋いでも着地しない」は拡張後の 33 件でも変わらず 1 件のまま
         （`snotra-core/tests/path_query_cost.rs:265` →
         `docs/development-principles.md`「判定を持たない道具を層に数えてよい」。
         `docs/development-principles.md:176` は箇条書きマーカーの無い段落先頭の太字であり、
         `ANCHOR_SPECS` の太字リード正規表現 `/^\s*(?:[-*]|\d+[.)])\s+\*\*(.+?)\*\*/` に
         当たらないため、`collectAnchors` は拾わない——独立に確認した）。この 1 件についての
         research.md の記述はそのまま正しい。

    含意:
      - 「20 件 / 305 / 6.6%」はいずれも過小。少なくとも「33 件 / 318 / 10.4%」であり、
        これも上限ではない（バッククォート自体の分断など、research.md が候補1の死角として
        名指ししつつ実測していない他の折れ方は今回も測っていない）。
      - **もっと重要**: research.md が候補1として提案している検知（「行末が対象綴り＋次行が
        「で始まる」）をそのまま実装しても、この 13 件は**検知されないまま残る**。
        候補1は「20 件赤にする」ではなく「20/33 件しか赤にしない」検知器であり、
        出荷直後から既知の見逃しを持つ。これは「初回実行で 20 件赤」という比較表の記述
        （候補1の行）の実効性を弱める——20 件を直しても、同じ夜のうちにまだ 13 件の
        `checked` にも上がらない参照が残る。

[2] 候補1の実装射程についての追加指摘: 20 件のうち .rs/テストの内訳の表（層別分布表）は
    自己矛盾している（軽微だが検算不能）。
    確信度: 中

      表は「.rs（doc/行コメント）: 9」「テスト（.rs/.mjs）: 2、例 path_query_cost.rs:3,:10・
      plan-review-ledger.test.mjs:2」としているが、「テスト」行の「例」欄には実質 3 箇所
      （path_query_cost.rs の 2 行 + plan-review-ledger.test.mjs の 1 行）が列挙されている
      のに件数は「2」。実際に合計 20 を再現できる唯一の内訳は、
      「テスト」バケツを path_query_cost.rs:3 と :10 の 2 件だけに限り、path_query_cost.rs:265
      を「.rs」バケツの 9 件目に、plan-review-ledger.test.mjs を「.mjs/.ps1/.psm1」バケツの
      7 件目に、それぞれ回す解釈だけである。この解釈は算術としては成立する（9+7+2+2=20 が
      検算できた）が、表の「例」列に書かれた並びだけを読むと読者は誤った内訳を組み立てる。
      「層」軸と「テスト」軸が直交していない分類を 1 つの表に混ぜたことが原因。

====================================================================
■ 部分的に揺らいだ項目（⚠️ 確信度：中〜低。全面否定ではない）
====================================================================

[3] ⚠️「死角は二重ではなく三重」——構造としては正しいが、3 者を対称に並べるのはやや過大。
    確信度: 中

    確認できたこと（真）:
      - `G-near-heading-refs.mjs` は `HEADING_REF` を import しておらず、独自の
        `ADJACENT_REF = /`[^`\n]+`\s*(?:§\s*[\d.]*\s*)?「/` を持つ（lib.mjs から見て
        2 本目の同じ前提の独立コピー）。
      - `scripts/governance/dependents.mjs:20` は `HEADING_REF` を lib.mjs から直接 import
        しており（コピーではない）、`refScanLines` で行単位に回すのも同じ。ゆえに折れた参照は
        `buildDependentIndex` の逆引き索引にも入らない——3 者とも行単位という記述は正しい。
    留保:
      - `dependents.mjs` は「合否を持たない」計器であり（自身のコメントに明記、exit code は
        常に 0）、CI ゲートではない。折れた参照が dependents.mjs で沈黙しても、それは
        「WARN が出るはずの回で出ない」という劣化であって、G-heading-refs/G-near-heading-refs
        の沈黙（CI が green のまま参照の実在が保証されない）とは重大性が異なる。
        「二重ではなく三重」という表現は構造的には正確だが、3 つを並列の「死角」として
        重み付けなしに並べると、後段の議論（候補比較・技術的制約1）でこの計器の重みを
        CI ゲート 2 本と同格に扱ってしまう危険がある。

[4] ⚠️「機体が無記名という主張は全称としては偽」——4 件の反例自体は正確だが、実務上の
    深刻さは過小評価されている。
    確信度: 高（反例・母数とも実測で確認）

    確認できたこと:
      - 「開発機」15 出現、独立に確認一致。
      - 機体名（GPD WIN MINI）を伴うのは `:1050`・`:1196`・`:1288`・`:1345` の 4 箇所のみ
        （行番号まで research.md と完全一致）。
      - 日付つきの記述 40 件・ユニーク 15 日、独立に確認一致（`grep -noE` で 40/15）。
    独自に測ったこと（research.md が測っていない部分）:
      - 日付つき 40 件のうち、実際に機体名を伴うのは 4 件のみ＝**10%**。
      - `:1050`（「開発機は 2 台あるので機体を書く」と自ら規範を書いた節）**より後**の日付
        （2026-08-09・2026-08-10、`PERFORMANCE.md:2708` 近辺の「起動の端から端まで」節）でも、
        「開発機」とだけ書いて機体名を書いていない実例を確認した（`:2708`
        「開発機では値が変わらなかった」——2 台のどちらかを明示しない）。
        つまり自己申告した規範は事後も一貫して守られていない。
      - 結論: 「全称としては偽」という訂正そのものは正しいが、A-1（出所欄の必須化）の
        必要性を弱める根拠としては使えない——現行 40 件の 90% は依然として機体不明であり、
        規範を書いた本人ですら 1 か月後に自分の規範を守っていない。research.md はこの
        「訂正はしたが実務的な穴は埋まっていない」という非対称を明示していない。

[5] ⚠️ 技術的制約4（母集団改修コスト 17/21 件）の引用がやや精度を欠く。
    確信度: 高（ADR 本文で確認）

      `docs/adr/ADR-governance-corpus-reduction-rejected.md:17` の実測は
      「原因の内訳は**母集団・メタ層の変更** 17 件、製品/スキル追随 4 件」であり、
      「母集団の定義を触ったこと」だけが 17 件の原因ではない（メタ層の変更も含む合算）。
      research.md の「17 件は母集団の定義を触ったことが原因だった」という要約は、
      合算値をそのまま母集団側だけの数値として引用しており、わずかに精度を欠く。
      結論（候補3が母集団定義を新設するコストの型に当たる、という主張自体）は揺るがない。

[6] ⚠️ 候補2（ratchet）却下の「先例」引用は類推であり同一先例ではない。
    確信度: 中

      `ADR-doc-promise-over-area-ratchet.md` は実在し、確かに「±100 字の許容差で毎追記に
      鳴らす ratchet 運用」を却下している。ただし対象は文書の**文字数**であって
      **見出し参照の照合件数**ではない。却下理由も「正当な追記にしか鳴らず、置き場の歪みを
      生む」であり、research.md が候補2の欠点として挙げる「他 PR の増減と混ざる」とは
      別の理由である。「同型が却下した先例」という言い方は、機構としての類似性
      （数値の増減を見るゲート）に基づく類推としては妥当だが、**却下理由が一致する先例**
      ではない点は明示すべきだった。

====================================================================
■ 壊せなかった項目（明示的に列挙）
====================================================================

- 前提の裏取り表の全行: `governanceDocs`（lib.mjs:461）・`STALE_EXTRA_DOCS`（lib.mjs:588）の
  定義、`PERFORMANCE.md` がどちらにも入らないこと——lib.mjs を直接読んで確認、真。
- 285 件という現在の照合件数——`npm run governance:check` を実際に走らせ、出力
  「見出し参照 285 件を md 48 件 + .rs 101 件 + スクリプトのコメント 109 件から照合」で
  直接確認。research.md の自己申告ではなく生の実行結果と一致。
- `allHeadingRefDocs` の走査対象文書数（258）——独立に `makeSnapshot` + `allHeadingRefDocs`
  を実行し確認。
- 「1 件が繋いでも着地しない」——20 件でも、拡張後の 33 件でも変わらず 1 件
  （`path_query_cost.rs:265` → `development-principles.md`「判定を持たない道具を層に数えてよい」)。
  `collectAnchors` を直接実行し、太字リードが箇条書きマーカーを欠くために拾われないことを確認。
- `HEADING_REF` を dependents.mjs が import している事実、`G-near-heading-refs` が独自の
  `ADJACENT_REF` を持つ事実——両ファイルを直接読んで確認。
- rustfmt はコメントを折り返さない——**直接の実証実験**を行った。392 文字の日本語 1 行
  doc コメント（`//! ...`）を含む `.rs` を用意し、`rustfmt --check` は exit 0、
  `rustfmt` を素で走らせても diff ゼロ（no-op）だった。リポジトリに `rustfmt.toml` は
  存在せず（`find` で確認）、CI は `.github/workflows/ci.yml:159` で
  `cargo fmt --all -- --check` を実行している（存在を確認）。この主張は最も強く裏取りできた。
- `.rs` へ `.claude/rules/governance-docs.md` が配送されない、という技術的制約2——
  `ADR-canonical-heading-references.md:40`「受容する残余」に該当記述あり、確認。
- 測定環境の汚染疑い——`.claude/worktrees/agent-ac5ec9f15ef4cb614` という実在する古い
  worktree の複製は、`WALK_EXCLUDE_PATHS` の `.claude/worktrees`（ルート錨止め・完全一致）
  により `makeSnapshot` の再帰対象から外れることをコードで確認済み。かつ独立に走らせた
  `npm run governance:check` の実出力が research.md の 285 と一致したことも、汚染が
  無かったことの実測的な裏付けになっている（汚染していれば数がずれたはずである）。

====================================================================
■ 結論（レビュアの裁定）
====================================================================

research.md の中心的な実測手法・コード読解・関連ファイル表・技術的制約・ADR引用は総じて
高品質で、独立検証の大半が一致した。ただし**中心的な数値主張「20 件」は明確に過小**であり、
research.md 自身が「候補1の死角」として名指ししている「「」の内側での折り」を実際に
数えると 13 件が追加され、合計 33 件になる（本レビューはこれを 2 通りの独立実装で確認した）。
これは「候補比較表」の候補1の「初回実行で 20 件赤」という実効性の記述、および「6.6% が
不可視」という比率の両方に影響する。B の裁定（検知器の要否・形）を進める前に、この数字は
研究文書の本体で訂正されるべきである。
