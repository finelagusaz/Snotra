敵対的レビュー: workspace/research.md（issue #1180「撤去済みの digest_ms が散文に残っている」）
検証日: 2026-08-26 / ブランチ chore/digest-ms-stale-prose

## 壊せた項目

### [最重要] 争点2・7 — F5「digest_ms の 3 件を除き、生きた層の出現はすべて注記済み」は偽
主張（research.md F5）: 「結果: `digest_ms` の 3 件を除き、生きた層の出現はすべて『#1001 / #1023 で
撤去済み』の注記を同じ段落か直近に持つ。」

反証: `PERFORMANCE.md:1951`（C＝1954 の digest_ms 記述と**同じ表・同じ段落の 3 行上**）に、
ae3335df で消えた関数 `measure_raw_path_rebuild_cost_over_real_index` への参照が**現在形の
「計器」欄**として残っている。しかも同じ表の次の行（1952）は「同上」でこの関数を再度指す
（＝実質 2 出現）。この段落には撤去注記が一切無い。

一次証拠:
```
$ git log --oneline -S "measure_raw_path_rebuild_cost_over_real_index" --all -- snotra-core/
ae3335df perf(core)!: 背景再スキャンを撤去し、索引の更新を明示操作だけにする (#1023)
4c96fef7 perf(core): target_path の木表現をディスクへ（IndexCache v7・index.bin -67.8%・ロード -63%）

$ git grep -n "measure_raw_path_rebuild_cost_over_real_index" -- '*.rs'
（0 件＝現存しない）

$ git grep -n "measure_raw_path_rebuild_cost_over_real_index" -- PERFORMANCE.md
PERFORMANCE.md:1951:| 全件の組み直し（逐次・バッファ 1 本を使い回す） | 28.0 ms | `measure_raw_path_rebuild_cost_over_real_index` |
```
現在の PERFORMANCE.md:1949-1954:
```
| 測ったもの | 値 | 計器 |
|---|---:|---|
| 原文の復元が `target_path` とバイト一致する件数 | 312,691 / 312,691 | `path_store_raw_matches_target_path_over_real_index` |
| 全件の組み直し（逐次・バッファ 1 本を使い回す） | 28.0 ms | `measure_raw_path_rebuild_cost_over_real_index` |
| 全件の組み直し（並列・刻みは digest と同じ 8192） | 7.5 ms | 同上 |

払い戻しは 7.5 ms である（`digest_ms` は 8〜17 ms なので digest 区間は約 2 倍になる）。
```
（隣の `path_store_raw_matches_target_path_over_real_index` は現存する。混同注意——両者は
似た名前だが片方だけ撤去済み。）

これは issue #1180 自身が checklist 項目 3 で明示的に要求した「#1023 周辺で撤去された語彙全体を
git grep で走査する」の直接の産物になるはずの発見であり、research.md の F5 の scanning
methodology（走査語リストを事前に決め打ちして当てる方式）がまさにこの型の見落としを生む
ことを実例で示している——`measure_raw_path_rebuild_cost_over_real_index` は research.md の
走査語リスト（`BackgroundRescanTask` 等 27 語）に含まれていなかった。

この所見を偽にする手順: 上の 3 コマンドを再実行し、`measure_raw_path_rebuild_cost_over_real_index`
が `*.rs` に現存する、または PERFORMANCE.md の当該箇所に撤去注記が既にある、のいずれかを示せば
この所見は崩れる（現時点ではどちらも成立しない）。

**結論**: F2 の「3 箇所」・F5 の「3 件を除きすべて注記済み」は、母集団を `digest_ms` という
文字列に限定した時点で不完全である。同じ表・同じ段落に**未報告の第 4 の腐りが実在する**。
issue の受け入れ条件（同じクラスの残存を数え上げる）を満たすには、この 4 件目も振り分け対象に
加える必要がある。

### 争点3 — 「PERFORMANCE.md:2178（B）は撤去より前に書かれた記録である」は文字どおりには正しいが、
実質的に誤解を招く。段落は撤去後に**2 回**触られている。
主張（research.md F2 の表・本文）: 「B | PERFORMANCE.md:2178 | ... | 9acd129f（2026-08-07・撤去前）」
「3 箇所とも撤去より前に書かれた記録である。」

検算: `git log --oneline -L 2170,2185:PERFORMANCE.md`
```
46386da6 refactor(core)!: LoadOrScanStats の残る 6 フィールドも Duration で運ぶ (#1182)  2026-08-25
1a405180 refactor(core)!: LoadOrScanStats の total を Duration で運び、丸めを表示境界へ寄せる (#1181)  2026-08-25
9acd129f perf(core): 背景再スキャンの比較を digest へ（... 反復 6） (#970)  2026-08-07
```
`1a405180` は同じ段落内で `total_ms` → `total` を、`46386da6` は同じ段落内で `cache_load_ms` →
`cache_load` を書き換え、**さらに`46386da6` はこの段落へ新しい注記文（#1178 の丸め方式変更に
関する「この値を現在の基準として引かないこと」）を追記している**——つまり編集者は 2026-08-25 に
この段落を 2 回、しかも「注記を追記する」という研究文書 F3 が言う作法をまさに実演する形で
編集していながら、3 行上の `digest_ms` は素通りした。

「書かれた時点」を文字どおり「その部分文字列が最後に書き換えられた時点」と読めば真だが（
`digest_ms` という語自体は 9acd129f 由来のまま一字も変わっていない）、この段落全体が
「誰も見ていない古い記録」ではなく「直近（1 か月弱前）に 2 度、しかも撤去語彙のすぐ隣で
編集された段落」である、という事実は research.md の「3 箇所とも撤去より前に書かれた記録である」
という総括文からは読み取れない。team-lead の懸念（「触っている可能性がある」）は的中している。

この所見を偽にする手順: `git log -L 2170,2185:PERFORMANCE.md` を再実行し、2026-08-25 の 2 commit
が実在しない、またはそれらが digest_ms を含む文を書き換えていることを示せば崩れる（現状は
「隣接行を書き換えたが digest_ms は素通りした」が正確な記述であり、これは崩れていない）。

### 争点5 — F4「#1027 の裁定（改名は反映する）」は issue #1180 本文からの伝聞であり、
#1027 自体にはそのような文書化ポリシーの記述が無い。さらに issue 本文は「改名と撤去は別クラス」と
明示しており、research.md の F4 はこの区別を検討していない。

一次証拠（`gh issue view 1027`）: #1027 は `LoadOrScanStats.total_ms` を `Duration` へ変える**技術的
リファクタ issue**で、本文のどこにも「散文の記述へ改名を遡及反映する」という文書ポリシーの宣言は
無い（受け入れ条件はコード変更 3 点＋doc 反映のみで、PERFORMANCE.md の扱いには触れていない）。

一次証拠（`gh issue view 1180`、「判断を要する点（これが本題）」節、逐語引用）:
> `PERFORMANCE.md` は測定記録であり、同文書の規約は「この文書を『今も支えている値』と『歴史』に
> 分けない」「既存の記述へ遡及して補完しない」と言う。**#1027 では改名（概念が生きたまま名前が
> 変わる）について「反映する」と裁定したが、撤去（概念そのものが消えた）は別クラスである**——
> `digest_ms` に「現在の正しい名前」は存在しない

つまり:
1. 「#1027 の裁定」という言い回しの直接の出所は issue #1180 の本文であり、research.md はこれを
   一次資料（#1027 それ自体）で検算していない——team-lead の懸念が的中している。#1027 自体は
   単なる実装 issue で、文書運用ポリシーの明文裁定を持たない。「裁定」の実体は #1181/#1182 が
   実際に PERFORMANCE.md の見出しを書き換えたという**観測された挙動**であり、issue #1180 の
   著者（おそらく先行調査者）がそれを「裁定」と呼んでいるにすぎない。
2. **より重大な欠落**: issue #1180 自身が「改名と撤去は別クラスである」と明示的に論点として
   立てているのに、research.md の F4 はこの区別を一度も検討せず、「改名は遡及して反映されている」
   という事実（1a405180 の rename）を、あたかも digest_ms（撤去）の扱いを考える上での傍証である
   かのように提示している。issue が自ら「この類推は成立しない可能性がある」と釘を刺している点を
   research.md は素通りしている。F4 のセクション見出し「改名は遡及して反映されている——ただし
   PERFORMANCE.md 自身の規約との関係は非自明」はこの欠落を暗に認めているとも読めるが、
   「別クラス」という issue 本文の明示的な論点そのものへの言及が無い。

この所見を偽にする手順: `gh issue view 1027 --json body` を再実行し、そこに散文への改名反映
ポリシーの明文記述があれば崩れる（現状は無い）。または research.md 本文中に「改名と撤去は
別クラス」への言及箇所があれば崩れる（本文・未解決の疑問 4 件を確認したが該当箇所は無い）。

## 壊せなかった項目

### 争点1 — F1「#1001 と #1023 は同じ撤去である／digest_ms を消したコミットは 1 つだけ」
検算: `git log --oneline -S "digest_ms" --all -- snotra-core/ src-tauri/` は 4 commit を返すが、
`ae3335df` 以外（`e830ce8e`・`1133fa6e`・`9acd129f`）は診断済みで**すべて追加側**（`+` 行のみ）
であり、削除は `ae3335df` の 1 コミットのみ（`pub digest_ms: u128,` と生成 3 行）。`gh issue view 1001`
で issue #1001 の本文を確認し、#1023（PR、コミット ae3335df）がそれを閉じたことも整合。
壊せなかった。

### 争点2 — F2「digest_ms の現存は 3 箇所」（`digest_ms` という文字列そのものに限定した場合）
検算: 除外なしの `git grep -n digest_ms`（全追跡ファイル）、行またぎ分割の `git grep -Pzo`、
`git ls-files --others --exclude-standard`（未追跡は workspace/ の 2 ファイルのみ）、
`.claude/settings.local.json` の直接確認、`digest_over`・`digest.{0,3}区間` の別表記探索、
すべて 3 箇所（＋ docs/superpowers/ の凍結計画・設計書内）以外の新規ヒット無し。
**ただし「digest_ms」という文字列に限定した数え上げそのものが不完全な母集団選択である**
（上の「壊せた項目」参照——`measure_raw_path_rebuild_cost_over_real_index` は文字列としては
別語彙だが同じクラスの残存）。文字列 `digest_ms` の出現回数としては 3 箇所で壊せなかった。

### 争点3（A・C） — startup.rs:380（A）と PERFORMANCE.md:1954（C）は撤去より前に書かれ、
その後一度も触られていない
検算: `git log --oneline -L` をそれぞれの行範囲に当てた。A は 1133fa6e（2026-08-09）のみ、
C は 4c96fef7（2026-08-08）のみがヒットし、いずれも ae3335df（2026-08-10）より前かつ以後の
touch が無い。壊せなかった（B のみ争点3で壊れた。上記参照）。

### 争点4 — F3「PERFORMANCE.md は撤去済み語彙に注記を追記する作法を既に持つ」（6 例）
検算: 6 例（L70-72, L176, L1943, L1972, L2219, L2291-2294）を実際に開いて逐語確認。すべて
行番号・文面が research.md の記述と一致し、いずれも「原文を残したまま注記を追記する」形
だった。原文自体を書き換えた反例（作法が一枚岩でないことを示す反例）は探したが見つからな
かった——ただし争点3 で見つけた「同じ段落を後から編集しつつ隣の識別子だけ書き換え、
digest_ms 自体は素通りする」という第 3 のパターン（annotate でも rewrite でもなく「無視」）は
F3 の 2 分類（描写する/在る前提）のどちらにも当てはまらない実例として記録に値する。

### 争点5（後半） — G-stale-identifiers の母集団に識別子の遡及裁定は無い
ADR-measurement-record-provenance.md・ADR-measurement-canon-in-code-doc.md を全文確認。
両 ADR とも**数値の出所・正本**についての裁定であり、識別子（関数名・フィールド名）の
遡及編集について明示的に裁定した箇所は無かった。research.md 自身「関係は非自明」と留保
しており、この留保は壊せなかった（＝识別子の扱いは依然として未裁定のまま）。

### 争点6 — G-stale-identifiers の母集団は `.claude/{skills,rules,agents}/*.md` ＋
`docs/**`（docs/adr・docs/superpowers 除く）＋ STALE_EXTRA_DOCS 4 本（SPEC.md/CLAUDE.md/
AGENTS.md/snotra-settings/SETTINGS-DESIGN.md）
検算: `scripts/governance/lib.mjs:686-720` を実読。`staleIdentifierDocs`（正規表現
`^\.claude\/(skills\/.*|rules\/[^/]+|agents\/[^/]+)\.md$`）・`staleIdentifierGuideDocs`
（`docs/**` から superpowers・adr を除外）・`STALE_EXTRA_DOCS`（4 項目、逐語一致）を確認。
`scripts/governance/checks/` 全 23 ファイルを一覧し、`.rs` や `PERFORMANCE.md` の識別子腐りを
別途見る検査が無いか確認した——`G-heading-refs`（`headingRefSourceDocs` で `.rs` を走査）は
存在するが、これは**見出し参照（正準形構文）の実在検査**であり、`digest_ms` のような
バッククォート付きインラインコードの識別子の実在は見ない。両者は別の検査対象・別の構文を
見るので、research.md の「*.rs も PERFORMANCE.md も母集団外」という G-stale-identifiers 限定の
claim は壊せなかった。

さらに実測で `npm run governance:check` を実行し、現状（digest_ms 未修正）で
「全検査 passed（検査 23 件...）」を確認——research.md の「直しても直さなくても検査は沈黙する」
という帰結も実測で裏付けられた。`.claude/hooks/post-edit.mjs` の `selectChecks` を実読し、
`.md` ファイル編集には `checks` 配列に何も積まれない（reminder のみ、gate ではない）ことを確認、
`.rs` の `src-tauri/` 配下は `fmt`/`clippy`/`tauri-test` が積まれるが、これらはコメント文面の
正確さを検査しないため、この issue の修正を機構が保護しないという結論には影響しない。

### 争点7（本体） — 走査語リストで #1155 型の再発（注記無き残存）は digest_ms 以外に無い
「壊せた項目」の measure_raw_path_rebuild_cost_over_real_index を除けば、独自再導出（下記）でも
新規の未注記残存は見つからなかった。

## ⚠️ 確信の持てない所見

- **F6（`PERFORMANCE.md:2291-2294` の「後日（#996）」注記が腐っている）は research.md 自身の
  発見であり、`entries_equal`・`try_background_rescan` が共に `*.rs` に現存しないことは確認した
  （`git grep -n "fn entries_equal\|entries_equal("` 及び `"fn try_background_rescan\b"` が 0 件）。
  ただしこれが「同じクラス」に入るかは判断が分かれうる——注記自体が腐っているのであって
  裸の識別子が散文に残っているのとは形が違う。research.md も「issue の射程外」と明記しており、
  この整理自体に異論は無い。
- **「改名と撤去は別クラス」という issue #1180 の論点を research.md がどこまで意図的に避けたか
  は確信が持てない。** F4 のセクション見出し自体が「非自明」と留保しているため、無視したという
  より「未解決の疑問」へ格上げすべきところを F4 の事実節に留めた、という体裁上の問題である
  可能性がある。いずれにせよ、この論点への直接の言及が本文に無いことは事実である。
- **`measure_raw_path_rebuild_cost_over_real_index` の発見が issue #1180 のスコープ内か**は
  判断が分かれる。issue 本文の checklist 項目 3（「#1023 周辺で撤去された語彙全体を git grep で
  走査する」）の文言上は明確にスコープ内だが、issue 本文の「症状」節は `digest_ms` の 2 箇所
  だけを名指ししている。スコープの解釈次第で「この issue の受け入れ条件を満たすには必須」
  「範囲外として別 issue に切り出す」のどちらもありうる——これは判断であって事実の争いでは
  ないため、両論を team-lead へ返す。

## 走査語の独自再導出（争点7）

`ae3335df --stat` から変更ファイルを洗い出し、以下を独自に追加調査した（research.md の
27 語リストとの差分）:

1. `snotra-core/src/rescan_log.rs`（493 行丸ごと削除）から `pub` な関数・型を機械抽出:
   `LoggedOutcome`・`RescanRecord`・`unattributed`・`to_ms`・`ms_or_null`・`start_line`・
   `end_line`・`MAX_LINES`・`log_path_in`・`append_in`・`prune_in`・`new_sid`・`now_rfc3339`。
   → `LoggedOutcome`/`RescanRecord` は research.md のリストに在ったが、残り 11 語は無かった。
   これらすべてを個別に `git grep`（`*.rs`・`PERFORMANCE.md`・`RETROSPECTIVE.md`・`*.md`、
   docs/adr・docs/superpowers 除外）したが**全語 0 件**——research.md の走査語リストが
   不完全であってもこの 11 語では実害（見落とし）は生じていない。

2. `docs/build-commands.md` から削除された 6 行（`rescan-log.jsonl` の読み方コマンド）
   → `rescan_log`/`rescan-log` は research.md のリストに含まれておりカバー済み。

3. `snotra-core/src/search/tests/performance.rs` から削除された 87 行
   （`measure_raw_path_rebuild_cost_over_real_index` 関数本体とその doc）
   → **この関数名は research.md のリストに無く、実際に PERFORMANCE.md:1951-1952 に
   未注記のまま現存していた。「壊せた項目」参照。** 同 doc コメント中に在った
   `digest_over_tree_matches_digest_over_scanned_entries`（`indexer` 側の対応検査関数）も
   個別に `git grep` したが 0 件（研究対象外だが安全側の確認として実施）。

4. `snotra-core/src/indexer.rs` の 2117 行差分（今回は全文差分の機械抽出は行わず、research.md
   が既に挙げた語彙 `entries_digest`・`digest_over`・`DigestSource`・`try_background_rescan` 等の
   0 件確認のみ実施。追加の見落としは検出しなかった）。

**結論**: 独自再導出は research.md の走査語リストの**不完全性そのもの**を実証した
（13 語が抜けていた）。うち 12 語は実害が無かったが、1 語
（`measure_raw_path_rebuild_cost_over_real_index`）は実際に未注記の残存を隠していた。
これは「機械抽出した語彙リストに当てる」という F5 の方法論が、**同じ手法を独立に適用しても
再現的に見落としを生みうる**ことを示す一次証拠であり、issue #1180 の checklist 項目 3
（同じクラスの残存の数え上げ）を完了するには、少なくともこの 1 件を追加で振り分ける必要がある。
