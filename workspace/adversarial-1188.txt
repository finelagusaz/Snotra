# 敵対的調査 — workspace/research.md（issue #1188）

対象: C:/workspace/Snotra/workspace/research.md（読むだけ、編集していない）
一次資料: gh issue view 1188 / scripts/governance/lib.mjs / scripts/governance/checks/*.mjs /
scripts/governance/dependents.mjs / docs/adr/*.md / .claude/rules/governance-docs.md /
workspace/measure-heading-ref-nesting.mjs / workspace/probe-heading-ref-blind-spots.mjs
すべての grep から .claude/worktrees/ を除外して実施。

====================================================================
## サマリ: 壊せなかった項目（母集団の大半）
====================================================================

research.md の主張の大半は、独立した方法（研究のスクリプトを再実行するだけでなく、
別の grep 手法・node での正規表現の直接検証・実 governance:check の実行・ADR/検査コードの
直接読み・変異注入テスト）で裏取りでき、**壊せなかった**。

--------------------------------------------------------------------
### 争点 1「HEADING_REF の消費者は2つだけ・REF_HEAD 不変」→ 壊せなかった
--------------------------------------------------------------------
- 偽にする手順: `grep -rn "HEADING_REF" --include=*.mjs scripts/ | grep -v test.mjs`
  → 実行結果: `checks/G-heading-refs.mjs` と `dependents.mjs` の 2 本のみが import（実測済み）。
- `dependents.mjs`（scripts/governance/dependents.mjs:20,41）を実際に読んだ。`HEADING_REF` は
  「(target,label) の抽出」にのみ使われ、**節境界（`sectionsOf`）は `ANCHOR_SPECS` から独立に
  導出**されており `HEADING_REF`/`REF_HEAD` に一切依存しない（dependents.mjs:57 以降）。
  ゆえに研究の「節境界は動かない」は dependents.mjs 自身のコードからも確認できる。
- `G-near-heading-refs.mjs`・`G-folded-heading-refs.mjs` は `REF_HEAD`（頭）だけを import し、
  ラベル文字クラスを再定義していない（ソース確認済み・lib.mjs:161-177 の doc の記述と一致）。

--------------------------------------------------------------------
### 争点 2「提案正規表現は OLD の一致を壊す/過剰に伸びる」→ 主張の骨子は壊せなかったが、
###        ⚠️ 理論的な反例を1つ発見（ただし現在の走査対象には実在しない）
--------------------------------------------------------------------
node で OLD/NEW を直接実行し 15 パターンの敵対的入力を当てた
（スクリプト: `C:/Users/Eoh/AppData/Local/.../scratchpad/regex-adversarial.mjs`）。

- 「1 行に複数の正準形参照」「」が「より先に来る」「隣接参照」はすべて OLD/NEW で
  同一の一致・同一境界（壊れない）。偽にする手順: 上記スクリプトを再実行し diff を見る。
- ⚠️ **理論的な反例**: ラベルの中に「別の正準形参照に見える文字列」が丸ごと埋め込まれた行
  （例: `` `a.md`「見出し `code`「入れ子」だ」 ``）では、
  - OLD は内側の `` `code`「入れ子」 `` **だけ**を独立参照として拾う（外側の `a.md` への
    参照は形が壊れているため無視）。
  - NEW は外側 1 個の参照（label="見出し `code`「入れ子」だ"、target=a.md）として丸呑みし、
    内側の疑似参照は**独立した一致として現れない**。
  - これは構造的に「NEW は OLD の一致の**超集合**（一致は増えるだけで減らない）」という
    暗黙の枠組みへの反例であり、`OLD にだけ在る = 0 件` という不変条件は**普遍的には成り立たない**。
  - **ただし実在チェック**: `grep -rnP '「[^」]*`[^`]*`「' --include=*.md --include=*.rs --include=*.mjs .`
    （worktrees 除外）で該当パターンを探すと、ヒットは `.superpowers/sdd/plan/audit-references.md` /
    `.superpowers/sdd/plan/task-1-report.md` の 2 ファイルだけであり、どちらも
    `lib.mjs` の `WALK_EXCLUDE_PATHS`（`.superpowers`）で snapshot からもともと除外されている
    （fs-walk・git ls-tree 双方の除外リストに `.superpowers/` が入っている）。
    → **この反例は今日の走査対象（生きた母集団）には実在しない**。research.md の
    「OLD にだけ在る = 0 件（全 rev で）」という**実測結果自体**は
    `measure-heading-ref-nesting.mjs` を独立に再実行して確認済みで正しい
    （下記「一次証拠の再現」参照）。**主張されている実測値は壊せなかったが、
    その実測が依拠する「NEW は理論上も OLD の超集合」という一段強い暗黙の性質は偽である**、
    という区別が要る。将来この形の行が生きた層（`docs/adr/` を除く `.md`/`.rs`/スクリプトコメント）
    に書かれれば、OLD が拾っていた内側の疑似参照が NEW では消える（が、それは「壊れた偽陽性が
    消える」という意味では望ましい副作用でもある——判断は分かれる）。
  - 偽にする手順: 上記の grep を worktrees 除外つきで再実行し、ヒットが `.superpowers/` の
    外に出るかを見る。今日は出ない。

- 深さ 2 の入れ子（`` A「B「C」D」E」ではなく A「B「C」D」 `` のような閉じない中間形、および
  閉じる形 `` A「B「C」D」E」 ``）は OLD/NEW ともに**一致ゼロ**——research.md の
  「宣言する死角: 深さ2以上は今日も不可視のまま」という設計どおりであり、退行ではない
  （regex-adversarial.mjs の「深さ2の入れ子」2 ケースで確認）。

- 破滅的バックトラック: node で 20000〜5000 回反復の 3 種の病的入力を作り実測
  （`perf-check.mjs`）。全ケース 1ms 未満（0.07〜0.39ms）。線形という主張どおりで、
  壊せなかった。

--------------------------------------------------------------------
### 争点 3「深さ2以上の入れ子は今日0件・入れ子かつ折れは0件」→ 壊せなかった
--------------------------------------------------------------------
research.md の数え方（`REF_HEAD「` が当たる位置だけを見る `probe-heading-ref-blind-spots.mjs`）
とは**別の**方法で独立に検算した:

- ATX 見出し・番号付きリスト・太字リードの 3 種のアンカー行そのものを直接 grep し、
  ラベルテキストに深さ2以上の入れ子（`「[^「」]*「[^「」]*」[^「」]*」`）が含まれるものを数えた
  （REF_HEAD の有無・参照されているか否かに関係なく、**アンカー母集団全体**を対象にした、
  research.md より広い方法）。
  結果: **0 件**（全追跡ファイル、`.claude/worktrees/`・`.superpowers/`・`docs/superpowers/`・
  `workspace/` を除外）。
  偽にする手順: `grep -rnP '^(#{1,6}\s|\s*\d+[.)]\s|\s*(?:[-*]|\d+[.)])\s+\*\*).*「[^「」]*「[^「」]*」[^「」]*」' --include=*.md --include=*.rs --include=*.mjs .`（worktrees/.superpowers/docs/superpowers/workspace 除外）。
- 参考までに depth≥1（入れ子を含むアンカー）の全追跡ファイル数を同じ手法で数えると **570 件**。
  issue 本文が挙げる「591 件」とは一致しない（差 21）。ただしこの 591 という数字は**research.md
  自身の主張ではなく issue 本文の背景数値**であり、research.md はこれを引用・依拠していない
  （research.md の「宣言する死角」節は深さ2件数だけを主張しており、591 には触れていない）ため、
  research.md への反証としては扱わない。issue 側との差分は日付ドリフト（issue 執筆後に
  ファイルが増減した）が濃厚——調査の射程外として報告するに留める。

--------------------------------------------------------------------
### 争点 4「論点Aの裁定（新ADR + 凍結ADRへの短縮引用つき追記）は誤り」→ 壊せなかった
--------------------------------------------------------------------
- **凍結規約への違反**という主張: `docs/adr/ADR-adr-frozen-history.md` を全文読んだ。
  決定文は「そこから外への参照（パス・見出し・SPEC 節）は生きた層の改名・移動に**追随させない**」
  であり、**ADR 自身への日付つき追記を禁じる文言は無い**。
  むしろ研究が引く先例そのもの（`ADR-governance-meta-demotion.md`）は、**同一 ADR の中で
  複数回**（57行目「追記（2026-08-20・#1155）」の前に「訂正（2026-08-20・同日。凍結規約により
  書き換えず追記する）」も存在）この「本文は書き換えず追記で読み替える」形を使っている
  （grep で確認: `grep -n "追記（2026-08-20・#1155）\|凍結規約により本文は書き換えず" docs/adr/ADR-governance-meta-demotion.md` → 57行目・59行目、research.md の引用と逐語一致）。
  偽にする手順: 同ファイルを開き 40〜75 行目を読む。追記が凍結規約違反だと明言する記述はない。
- **「G-adr-citations は ADR→ADR の辺を実際には見張っていない」**という主張:
  `scripts/governance/checks/G-adr-citations.mjs` を読み、`adrCitationDocs` が
  `adrDocs`（= `docs/adr/` 配下）を明示的に足し戻していることを確認（同ファイル 27-38 行目、
  コメント「凍結された歴史も実在の辺だけは守る——ADR → ADR の短縮引用は…」）。
  **さらに変異注入で実測**: `ADR-folded-canonical-reference-detector.md` の複製（メモリ上の
  snapshot、実ファイルは1バイトも変更していない）へ実在しない `ADR-this-slug-does-not-exist-xyz`
  という引用を追記し `scanAdrCitations` を実行 → **findings 1 件で検出**（実行結果:
  `file: 'docs/adr/ADR-folded-canonical-reference-detector.md', line: 51, message: 'ADR の短縮引用が実在しない: ...'`）。
  偽にする手順: `C:/Users/Eoh/AppData/Local/.../scratchpad/test-adr-citations.mjs` を再実行する。
  → **「ADR→ADR の辺を見張っていない」は明確に偽**。
- **「issue の『新しい ADR で』が正しい」**という主張: issue 本文を実際に読むと
  「新しい ADR で …読み替えを与える（`ADR-governance-meta-demotion` が #1155 でやった先例）」と
  書いてあるが、**その先例（#1155 の追記）自体が凍結 ADR の中に置かれている**（上で確認済み）。
  issue の指示文と、issue が挙げる先例が字面のレベルで食い違っているのは research.md の指摘
  どおりで、これは私自身が issue 本文と ADR 本文を直接読み比べて確認した一次事実である。

→ 論点 A の裁定は 3 方向（凍結規約の文言・機構の実装・issue 内部の整合性）いずれからも
  壊せなかった。

--------------------------------------------------------------------
### 争点 5「論点B/Cの『変えない』は誤り」→ 壊せなかった（射程を広げても）
--------------------------------------------------------------------
research.md が名指す文書に加え、`docs/hooks.md`・`AGENTS.md`・root `CLAUDE.md`・各サブ
`CLAUDE.md`・`docs/development-principles.md`・`.claude/skills/**` を「入れ子」「ネスト」
「鉤括弧」「かぎ括弧」「ラベル」「正準形」で網羅的に grep した。

- 「入れ子」「ネスト」「鉤括弧」「かぎ括弧」がヒットしたのは `snotra-core/CLAUDE.md`
  （`scan_all` の走査根の入れ子——無関係な話題）と `snotra-settings/CLAUDE.md`
  （`(rule, tool)` インデックスのネスト——無関係）のみ。HEADING_REF/見出し参照のラベルの
  中身について述べた文は 0 件。
- さらに research.md が明示的に挙げていない `docs/build-commands.md`・`docs/comment-guidelines.md`
  も heading-refs 機構に言及しているため追加で確認したが、内容はそれぞれ「`.rs` 変更時に
  governance:check を打つ」「正準形参照を物理改行で折るな」であり、いずれもラベルの文字クラス
  （鉤括弧の入れ子可否）については何も主張していない——本件の変更で偽になる文は無い。

→ 論点 B/C の「変えない」は、research.md が名指した文書に加えて母集団を広げても壊せなかった。

====================================================================
## 測定環境そのものへの疑い（最重要指定項目）
====================================================================

### 実 governance:check との突き合わせ → 一致（壊せなかった）
`npm run governance:check` を実際に実行:
```
見出し参照 365 件を md 48 件 + .rs 102 件 + スクリプトのコメント 116 件から照合
近傍の見出し参照 19 件 / 折れうる位置 20 件
```
research.md の HEAD 行（checked=365）・issue 本文の内訳（heading 365 / near 19 / folded 20）
と**完全一致**。測定スクリプトが「何も測っていなかった」可能性は否定された。

### measure-heading-ref-nesting.mjs の歴史データ再現 → 完全一致（壊せなかった）
5 つの historical rev（6ccacd01 / f1827b08 / ac39cb26 / b93b0fb8^ / b93b0fb8）すべてを
自分で再実行し、research.md の表の docs/OLD checked/NEW checked/findings/差分件数を
**1 桁も違わず**再現した（実行ログは本ファイル生成過程のツール呼び出しに残る）。

### 測定スクリプトの構造検算
- `scanHeadingRefs`（本物・G-heading-refs.mjs）と `scan()`（測定スクリプト内）を並べて読んだ。
  `isRefTargetSpelling` の適用位置（resolveRefTarget の**前**）・`checked` の加算タイミング・
  `landed` の判定式（`anchors.some(a => a.startsWith(normAnchor(label)))`）は**完全に同一**。
- ⚠️ **1 点だけ、本物と異なる**: 測定スクリプトも probe スクリプトも
  `refScanLines(text, doc, [])` のように**使い捨ての空配列**を findings シンクとして渡している
  （measure-heading-ref-nesting.mjs:45, dependents.mjs 相当の設計と同じパターン）。
  本物の `scanHeadingRefs` は同じ `findings` 配列にコードフェンス不整合の finding を積み、
  それが最終的な `findings.length` に反映される。測定スクリプトの「findings」は
  **`landed=false` の件数のみ**を意味し、フェンス不整合由来の finding を一切拾わない。
  今日はこの母集団に不整合フェンスが無い（実測: 全 rev で「対象文書が読めない」系の
  finding も 0）ため今回の 0/0 という結論には影響しないが、**「測定スクリプトが本物と
  同じものを測っている」という主張のうち、findings の意味論だけは本物と非同値**である。
  偽にする手順: `scripts/governance/checks/G-heading-refs.mjs:44` の
  `refScanLines(text, doc, findings)` と `workspace/measure-heading-ref-nesting.mjs:45` の
  `refScanLines(text, doc, [])` を見比べる。

### 誤警報（自己訂正）: probe スクリプトの `text.includes(...)` は NUL バイトチェックだった
調査の初期段階で `probe-heading-ref-blind-spots.mjs:19` の
`if (text.includes(" ")) continue;` を「空白1文字を含むファイルを全部スキップする致命的バグ
（496 本中 495 本が空振り）」と誤認しかけた。原因は Read ツールが当該行の **NUL バイト
（`\0`）を空白のように表示していた**ことによる誤読——`xxd` でバイト列を直接見て
`text.includes("\0")`（バイナリファイル除外のための正当なチェック）であることを確認して
訂正した。**probe スクリプトを実際に実行した結果も「files=496」「深さ2以上=0件」
「行内で閉じない=13件」を出力しており**、この誤警報は実害なし。念のため申告する
（⚠️ではなく、壊せなかった項目として記録——ただし他の敵対的調査エージェントが同じ誤読をする
可能性があるため明記する）。

### 実ファイルとの照合
- 実 `package.json` の `governance:check` スクリプト定義（`node scripts/governance-check.mjs`）
  を確認——research.md の前提と一致。
- `lib.mjs` の `HEADING_REF` 定義（177行目）・`WALK_EXCLUDE_PATHS`（39行目）を実読——
  research.md が引用する現在の実装と一致。
- `.claude/settings.json` の hook 設定は本タスクの争点に直接関わらないため深追いしていない
  （`.claude/rules/governance-docs.md` 自身が「これらの編集に PostToolUse hook 検査は走らない」
  と明記しており、research.md もこの前提を崩していない）。

====================================================================
## 壊せなかった項目の一覧（再掲・チェックした手順つき）
====================================================================
1. HEADING_REF の消費者=2つ、REF_HEAD不変 — grep + dependents.mjs 直読
2. 提案regexがOLDの一致を非破壊で追加するのみ（1件の理論的反例を除き） — node直接実行 15ケース
3. 深さ2以上=0件 — 別方法（アンカー行への直接grep）で独立再検算
4. 入れ子かつ折れ=0件 — probe script実行で13件確認、うち入れ子含みは目視で0件
5. 破滅的バックトラックなし — node実測（3種病的入力、全て<1ms）
6. 歴史データ5revの表 — 全件独立再実行で完全一致
7. 現在のgovernance:check出力（365/19/20） — 実行して一致確認
8. G-adr-citations がADR→ADR辺を見張る — 変異注入で実測(findings 1件検出)
9. ADR-governance-meta-demotion.md:57,59の引用 — 逐語一致確認
10. 論点B/Cで偽になる文が無い — 対象文書+2文書追加でgrep、0件
11. 既存テストのベースライン — npx vitest run scripts/governance で479/479 pass（HEAD）

====================================================================
## ⚠️ 確信の持てない所見（返り値仕様に必ず含める）
====================================================================
- ⚠️ 争点2: ラベル内に別の正準形参照もどきが埋め込まれた行では、NEW は OLD が拾っていた
  「内側だけの疑似参照」を独立の一致として出さなくなる（外側1つに統合される）。今日の
  生きた母集団には実在しない（`.superpowers/` 配下の2件のみ、除外済み）が、
  「OLD にだけ在る = 0 件」を**将来にわたる普遍的性質**として書くと過大主張になる。
  研究の実測値そのもの（今日・全rev）は正しい。
- ⚠️ 測定/probeスクリプトの `findings` はフェンス不整合由来のfindingを含まない
  （空配列シンクを使っているため）。今日は無害（対象母集団に不整合フェンスが無い）だが、
  「本物と同じ判定」という主張のうち finding の意味論は本物と完全同値ではない。
- ⚠️ issue本文の「591件」（全追跡ファイルの入れ子アンカー数）と、私が独立に数えた570件が
  一致しない（差21）。research.md自身はこの数字を主張していないため直接の反証ではないが、
  母集団の実測値がドリフトしている可能性は記録しておく。
