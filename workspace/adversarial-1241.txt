敵対的レビュー — research.md（issue #1241）

対象: C:\workspace\Snotra\workspace\research.md 全文（ブランチ fix/rules-search-heading-refs、main e1531e0 と同一内容）
方法: lib.mjs / 各 G-*.mjs を実際に import し、node で HEADING_REF・NEAR_REF・ANCHOR_SPECS・headingRefDocs を当該ファイルへ実行して測定。npm run governance:check も実行してベースラインを再確認。

============================================================
壊せた項目（research.md の記述が事実と食い違う）
============================================================

[1] 命題: 「同ファイル L27（`Ord` / `BinaryHeap` の行）も `snotra-core/CLAUDE.md` 実装前チェック を散文形で指す」（research.md L13）
判定: 壊せた（事実誤り）
根拠: `.claude/rules/snotra-core-search.md` は全 25 行しかない（node で `text.split('\n').length` = 25）。`Ord` / `BinaryHeap` を含む行は L24 であり、L27 は存在しない。
```
node -e "...lines.forEach((l,i)=>{ if(l.includes('Ord')||l.includes('BinaryHeap')) console.log(i+1, JSON.stringify(l)); });"
24 "- **`Ord` / `BinaryHeap` / top-k に触れたら**: `snotra-core/CLAUDE.md` 実装前チェックの規律（…）に従う"
```
偽にする手順: `.claude/rules/snotra-core-search.md` を開き行番号を数える。research.md は「すべて実在確認済み」（L9見出し）と自称しているため、この誤記は同文書の自己主張と直接矛盾する。plan.md へ転記する前に L27→L24 へ訂正が必要。

[2] 命題（機序の帰属）: 「G-near-heading-refs — 窓幅 8・着地必須。L17 の「文字ビットマスク」は着地しないため沈黙、「incremental cache …」は窓幅 8 を超えるため沈黙」（research.md L24）
判定: 壊せた（機序の記述が誤り。結論＝沈黙は正しいが、理由の割り当てが事実と逆）
根拠: `NEAR_REF`（`G-near-heading-refs.mjs` L45、gap 1–8 文字）を実際の L17 文字列に当てたところ、**gap=8 では 2 つの対象とも一致が 0 件**だった。一致が生成されるのは gap を 16 まで緩めた場合のみ（`` `snotra-core/CLAUDE.md` `` から `「文字ビットマスク」` までの実際の隔たりは「の search.rs 節・」＝14 文字で、8 を超える）。
```
gap 8  -> matches: []
gap 10 -> matches: []
gap 12 -> matches: []
gap 14 -> matches: []
gap 16 -> matches: ['文字ビットマスク']
```
つまり「文字ビットマスク」側も**着地判定（`anchors.some(startsWith)`）に到達する前に、正規表現が候補としてすら拾えていない**。research.md は「着地しないため沈黙」（＝候補になったが anchors と一致しない、の意）と書いているが、実際にはそもそも `NEAR_REF` に一致しておらず、着地判定は実行されていない。両方の目標が同一の理由（gap 超過）で沈黙しており、「着地しない」と「窓幅を超える」を別の理由として書き分けたのは誤り。
偽にする手順: 上記の node スクリプトを再実行し、gap=8 で `matches` が空であることを確認する。着地判定に「到達すらしない」ことは `NEAR_REF.exec()` の呼び出し結果そのものが示す（マッチオブジェクトが無ければ後続の `anchors.some` は評価されない）。

============================================================
壊せなかった項目（実測で支持された）
============================================================

[3] 命題: 「`.claude/rules/*.md` は走査元に含まれる」（research.md L25）
判定: 壊せなかった
根拠: `lib.mjs` の `headingRefDocs` は `docs/superpowers/` `workspace/` `docs/adr/` の 3 接頭辞だけを除外し、`.claude/rules/` は除外しない。実測で `headingRefDocs(snapshot)` が返す md 48 件のうち、`ruleDocs(snapshot)` が返す rules 8 件全部が部分集合として含まれることを確認（`rules subset of md? true`）。`npm run governance:check` の実出力も「対象文書 36 件 / rules 8 件」「見出し参照 371 件を md 48 件…」と一致した。
偽にする手順: `ruleDocs(snapshot).every(r => headingRefDocs(snapshot).includes(r))` を評価し false が返るか確認する（今回は true）。

[4] 命題: 「normAnchor がバッククォートを剥ぐので、`` `「`char_bitmask` は `query.rs` に一元化済み」` `` は照合可能」（research.md L29）
判定: 壊せなかった
根拠: 提案形の行 `` `snotra-core/CLAUDE.md`「`char_bitmask` は `query.rs` に一元化済み」節 `` を `HEADING_REF` に当てたところ target/label が正しく分離してマッチし、`normAnchor(label)` が実見出し（L111 `` `char_bitmask` は `query.rs` に一元化済み ``）の正規化形と前方一致した（`lands? true`）。
偽にする手順: 上記の node スクリプトを再実行し `lands?` が false になるか確認する（今回は true）。

[5] 命題: 「`- `search.rs` — …` の箇条書きは着地先にならない」（research.md L23, L35）
判定: 壊せなかった
根拠: `ANCHOR_SPECS` の太字リード仕様は `^\s*(?:[-*]|\d+[.)])\s+\*\*(.+?)\*\*` で `**` 必須。`snotra-core/CLAUDE.md` L27 の実際の行 `` - `search.rs` — 検索順位計算… `` はバッククォート始まりで `**` を持たず、ATX・番号付き・describe/it のいずれにも当たらない。`collectAnchors` の出力に `search.rs` 由来のラベルは現れない。
偽にする手順: `collectAnchors(claudeMd)` の結果一覧に `search.rs` を含む要素があるか確認する（今回は無し）。

[6] 命題: ベースライン「`npm run governance:check` は main e1531e0 で全 24 検査 passed・見出し参照 371 件照合」（research.md L36）
判定: 壊せなかった
根拠: 現在のブランチ（main と同一内容）で実行し、出力は完全一致:
```
governance:check — 全検査 passed（検査 24 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 / … /
見出し参照 371 件を md 48 件 + .rs 137 件 + スクリプトのコメント 116 件から照合 / … / 近傍の見出し参照 20 件 / 折れうる位置 20 件 / …）
```
偽にする手順: 同コマンドを再実行し数値が異なるか確認する（今回は完全一致）。

[7] 命題: 「`snotra-core.md` L12〜L18・L24 の「」だけの節名は全 7 件が現存見出しに着地する」（research.md L44）
判定: 壊せなかった
根拠: `snotra-core/CLAUDE.md` の見出し一覧（`grep -n "^#{1,6}"`）と 7 ラベルを 1 件ずつ突き合わせ、全て前方一致で存在を確認した（実装前チェック（必須）L79・`normalize_entry_key` の冪等性契約 L97・history.rs のキー正規化に関するチェックリスト L152・`char_bitmask` は `query.rs` に一元化済み L111・index.bin 書き込みの排他（INDEX_WRITE_LOCK）L201・indexer.rs の索引更新の契機 L214・`scan_all` の重複排除 L225・開発ルール L69）。ただし `snotra-core.md` 側のラベルにはバッククォートの対象接頭辞が無く、機構（G-heading-refs/G-near-heading-refs）の走査対象にすらならない形である点は research.md も明記済み（Q2）。
偽にする手順: 7 ラベルのいずれかについて `snotra-core/CLAUDE.md` の見出し一覧に前方一致する要素が無いか確認する（今回は全件あり）。

[8] 命題: 「同ファイルで「」を含む行は L9 と L17 の 2 行だけで、L9 は節名ではなく散文の引用」（research.md L11）
判定: 壊せなかった
根拠: 全 25 行を走査し「」を含む行を抽出したところ L9・L17 の 2 行のみ。L9 の文言「本 rule は「どこを読むか・何を実行するか」だけを示す」は文法的に節名の指しではなく引用句。
偽にする手順: `text.split('\n').forEach((l,i)=>{ if(l.includes('「')) console.log(i+1); })` を実行し 2 件以外が出るか確認する（今回は出ない）。

============================================================
⚠️ 確信の持てない所見
============================================================

[9] 命題: 「件数が 371 → 371+N に増えることを『機構が見るようになった』の証拠にする」（research.md L36）
判定: ⚠️ 確信なし（過大主張ではないが精度に注意）
根拠: `G-heading-refs.mjs` L50 の `checked += 1` は `isRefTargetSpelling(target)` を通った時点でインクリメントされ、**着地判定の成否とは無関係**（着地失敗時は別途 `findings` へ積む）。したがって `checked` の増分は「正規表現が対象綴りを検出した」ことの証拠にしかならず、単独では「正しく着地した」ことの証拠にならない。research.md の文言は「機構が見るようになった」という狭い主張に留めており過大ではないが、着地の証拠としては別途「全検査 passed（findings 0 件のまま）」も同時に確認する必要がある——research.md 本文はこの両方を並べて書いているため実務上の欠落は無いと考えるが、この一文だけを抜き出すと誤読の余地がある。
偽にする手順: 着地しない正準形（存在しない節を指す `` `foo.md`「無い節」 `` 等）を仮に追加して `checked` が増えるが `findings` も増えることを確認すれば、`checked` 単独では不十分という指摘が裏付けられる（今回は未実施、コード読解のみ）。

[10] 命題: Q3「正準形の行が長くなり、物理改行を跨ぐと G-folded-heading-refs の対象になる」
判定: ⚠️ 確信なし（コード読解では支持されるが実測未実施）
根拠: `G-folded-heading-refs.mjs` のコメントは「ラベルの閉じを同一行に要求する」「形A/形Bの2種を見る」ことを明記しており、長い正準形が改行を跨げばこの検査の対象になるという主張はコードの設計意図と整合する。しかし実際に提案行を意図的に折り返して `scanFoldedHeadingRefs` へ通す実測は行っていない。
偽にする手順: 提案する正準形の行を物理改行させたテキストを作り `scanFoldedHeadingRefs` に通して finding が出るか確認する。

============================================================
測定環境について
============================================================

`npm run governance:check` は git ls-files 相当の追跡ファイル一覧（`makeSnapshot`、未見だが `git ls-files` で代替検証し governance:check 実出力と完全一致したため、走査母集団は作業ツリーの追跡ファイルで一致していると判断）を読んでおり、今回の未編集状態（main と同一内容、workspace/ のみ未追跡）で数値がすべて一致した。`.claude/rules/` が母集団に入っているかどうかは「rules 8 件」という別集計ではなく、`headingRefDocs` の返り値に実際に `.claude/rules/*.md` が含まれるかを直接コードで確認して裏取りした（[3]参照）。「別の枝を測っていた」という懸念は今回は該当しない（ブランチが main と同一内容であることは git status で確認、diff は取っていないが research.md 自身がそう明記している）。
