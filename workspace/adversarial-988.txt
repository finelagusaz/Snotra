敵対的レビュー: workspace/research.md（issue #988 調査）
実施日: 2026-08-28 / 対象: C:/workspace/Snotra/workspace/research.md
出力: このファイルへ逐次追記済み（中断耐性）

=====================================================================
## 壊せた項目
=====================================================================

### [壊せた-1] 「(a) 素のバッククォートへ落とす」を「規範違反を作る作業になる」と一律に言うのは overclaim（第一原則の射程が (a) 全体を覆っていない）

**research.md の逐語**:
> issue が求める「(a) 素のバッククォートへ落とす」と「8 件を 0 件にする」は、現在の正本に照らすと**規範違反を作る作業**になる。

**反証の一次証拠**（docs/comment-guidelines.md、e440c64f 時点の差分と現在の全文を読了）:

`第一原則`「書かないもの」の該当箇条は次の構造を持つ:
```
- **コードが決めている構造の事実**——経路の数、分岐の列挙、呼び出し元、到達可能性。
  これは「なぜ」型に見えるが写しである(...)
  - 数は書かない(...)
  - 名指しは書いてよい。ただし `[`Type::method`]` の形で書く(...)
```

「名指しは link 形で書け」という指示は、**「コードが決めている構造の事実（数・列挙・呼び出し元・到達可能性）を書く代わりに名指しで済ませるとき」という限定文脈の中の箇条**である。一方、`配置基準（3層）`の面ごとの表は `//`（inline）について次のように書く:

```
| `//`（inline） | その行・ブロックだけに効く理由 | 保証の要る参照（→「名指しと正本の指名」の表） |
```

ここでの「書かないもの」は**「保証の要る参照」だけ**であり、「link 形でない名指し」を禁じていない。さらに「名指しと正本の指名」節は次のように、`//` での記法使用を**禁止ではなく随意の選択**として扱っている:

> 保証が要る参照は production の doc コメント側へ置く。**それ以外の面でも記法は無駄にならない**——失うのは保証だけで、指名は残る。

「無駄にならない」は「使ってもよい・使う価値が残る」の意であって「使わなければ違反」ではない。

**この所見を偽にする手順**: 20 件の `//` 参照のうち、issue が定義する「構造的事実（経路数・呼び出し元列挙・到達可能性）の代わりの名指し」に該当**しない**もの（純粋な局所 why の中でたまたま名前へ触れているだけのもの）を 1 件でも特定し、それを素のバッククォートへ落としても第一原則違反にならないことを示せば、この所見は生き残る。逆に、20 件全部が「構造的事実の代替」だと示せれば research.md の主張が回復する。

**評価**: research.md の「結論」の一文は 20 件全体（母集団）に対する一律の断定であり、第一原則の射程（構造的事実の代替という狭い文脈）を超えて一般化している。個別の 20 件がすべてその狭い文脈に当たるかは検証されていない（research.md 自身も「1 件ずつ判断」という issue の要求には触れているが、この一般化の射程については論じていない）。

---

### [壊せた-2] 「#1176 以降も `//` の `[`X`]` を違反とする規範が正本のどこかに残っている」——見つかった

**一次証拠**: `docs/adr/ADR-measurement-canon-in-code-doc.md`「決定」節（2026-08-20 制定・13f8a2e7・#1176 の e440c64f 2026-08-25 より**前**）:

> 参照の形は在り処で分ける——`///` / `//!` からは intra-doc link（`cargo doc` の `broken_intra_doc_links` deny が綴りを検算する）、**`//`（非 doc）と `.md` からはバッククォートの散文**。

これは「`//` からは link 形ではなくバッククォートの散文で参照せよ」という明示的な指示であり、#1176 が反転させたはずの旧規範と同じ内容である。しかも**この ADR は歴史記録として孤立していない**——`docs/comment-guidelines.md`「測定値の置き場」が現在この ADR を正本として名指しで委譲している:

> この判断はここで新しく決めない。`docs/adr/ADR-measurement-canon-in-code-doc.md` と `PERFORMANCE.md`「この文書へ記録するときの規約」が既に決めている。

つまり「測定値の正本を指す参照」というカテゴリに限れば、**現在の正本（comment-guidelines.md 経由）が今なお `//` での link 形使用を推奨していない**。ADR は「凍結された歴史」であり黙って書き換えないのが規範（AGENTS.md）だが、この ADR は単なる歴史記録ではなく、生きた文書から能動的に委譲された**現在も参照される決定**である点が却下1系の ADR（単なる却下理由の記録）と違う。

**この所見を偽にする手順**: この ADR の「決定」節が測定値の正本指名という狭い文脈（`40〜95 ms` の 8 か所の写し問題）にのみ適用され、issue #988 が扱う「一般の名指し」には及ばないと示せれば、この所見の実害は縮む。ただし文言自体（「`//`（非 doc）...からはバッククォートの散文」）は一般形で書かれており、狭いスコープへの限定は明記されていない。

**評価**: research.md は 4 つの正本候補（comment-guidelines.md 本体・.claude/rules/comments.md・AGENTS.md・各 CLAUDE.md・development-principles.md・ADR 群）を検討したはずだが、comment-guidelines.md が委譲する ADR まで遡って読んでいない。「測定値の置き場」節を読んだなら、その先の委譲先を辿るべきだった。

---

### [壊せた-3] 「腐った参照は 0 件」は緩い述語（最終セグメント名の存在チェックのみ）であり、厳密な rustdoc 解決規則（スコープ）で測ると 1 件以上実際に壊れる

**research.md の逐語**:
> 述語は最終セグメント（`::` の後ろ）に対する `(fn|struct|enum|const|static|type|trait|impl) +<name>\b` である。(...) 18 / 18 が現存。腐りは 1 件も無い。

**反証の一次証拠**（実測・revert 済み）: `snotra-core/src/indexer/columns.rs:137` の `//` コメントは `[`IndexMaterial`]` を参照するが、`IndexMaterial` は `snotra-core/src/indexer.rs` で定義され、`columns.rs` は `use` でこれを import していない（`grep -n "IndexMaterial" snotra-core/src/indexer/columns.rs` は当該行 1 件のみ）。この行を一時的に `///`（doc コメント）へ昇格させ `cargo doc -p snotra-core --no-deps --document-private-items` を実行したところ:

```
error: unresolved link to `IndexMaterial`
   --> snotra-core\src\indexer\columns.rs:137:24
    |
137 | /// EXPERIMENT: 運ぶ器は [`IndexMaterial`] である(...)
    |                            ^^^^^^^^^^^^^ no item named `IndexMaterial` in scope
    = note: requested on the command line with `-D rustdoc::broken-intra-doc-links`
error: could not document `snotra-core`
```

（実験後、`git checkout -- snotra-core/src/indexer/columns.rs` で即座に revert 済み。作業ツリーはクリーンに復元。）

`IndexMaterial` という名前の定義は crate 内に実在する（`indexer.rs:55`）ので research.md の「名前が存在するか」述語は 18/18 を通すが、**rustdoc の実際の解決規則はスコープ（`use` されているか、同一モジュール内か、`crate::`/`Self::` などで正しく修飾されているか）を要求する**。この 1 件は「最終セグメントの名前は実在するが、そのファイルのスコープからは解決不能」という、research.md 自身が「射程」として明記した死角（パス修飾を見ていない）に正確に該当する。

**この所見を偽にする手順**: `columns.rs` が `IndexMaterial` を暗黙にスコープへ持ち込む経路（glob import・prelude 相当）がある、または rustdoc がスコープ外の型名にもフォールバック解決を行うと示せれば、この実験結果は覆る。しかし実測（上記エラー）は明確に「no item named `IndexMaterial` in scope」であり、フォールバックは起きていない。

**注記**: これは issue が恐れた「改名・削除で沈黙して腐る」ケースとは異なる（`IndexMaterial` は削除されていない、最初からスコープ外）。しかし research.md の主張は「18/18 が現存」＝「壊れていない」であり、この 1 件は**その face（doc コメント）へ実際に昇格すれば即座にコンパイルエラーになる**という意味で「現存するが着地しない」参照である。research.md の「射程」の自己申告（パス修飾を見ていない）は正しかったが、その射程の外側で実際に反例が出ることまでは検証していなかった。

**波及**: この発見は「生きて残る作業」節の (b) 候補選定にも関わる——research.md が (b) の候補として名指しした `columns.rs:139`（`derive_entry_collapsed`）・`:236-237`（`derive_entry_lowers`/`derive_entry_collapsed`）は同一ファイル内定義なので影響を受けないが、**同じファイルの数行上にある `:137`（`IndexMaterial`）は (b) へ格上げする際に `use crate::indexer::IndexMaterial;` の追加が必須**になる。この作業コストは research.md に書かれていない。

---

=====================================================================
## 壊せなかった項目
=====================================================================

1. **「#1176 が `//` インラインでのリンク形の禁止を明示的に解いた」**——PR #1176 のコミット本文・diff（`git show e440c64f -- docs/comment-guidelines.md`）で確認。旧文言「**リンク形が効くのは `///` と `//!` だけである**——`//` のインラインコメントは(...)インラインでは素のバッククォートで名を書き」が削除され、面を限定しない新文言「名指しは書いてよい。ただし `[`Type::method`]` の形で書く」に置換されている。PR 本文にも「`//` の禁止は解き、既存の 18 件はそのまま規約適合になる」と明記。**壊せなかった**（comment-guidelines.md 本体においては事実）。

2. **「issue の 8 件実測（8c606f2a 時点）は正確」**——`git grep -nE "^\s*//[^/!].*\[\`" 8c606f2a -- snotra-core src-tauri snotra-settings snotra-egui-runtime` で 8 行を再現し、issue が列挙した 8 箇所（ファイル・行番号・参照名）と完全一致することを確認。**壊せなかった**。

3. **「現在 20 件・2 crate（snotra-core 14・src-tauri 6）」**——`grep -rnE "^\s*//[^/!].*\[\`" --include=*.rs snotra-core/ src-tauri/ snotra-settings/ snotra-egui-runtime/` を実行し、20 行を確認。crate 別内訳も一致。ブロックコメント `/* */` での同形は 0 件（別途 grep で確認）。TOML セクション記法 `[appearance]` 等を「リンクなしの `[X]` 形」として誤検出していないかも確認したが、対象母集団（バッククォート付き `[`X`]`）とは異なる形であり述語は正しく除外していた。**壊せなかった**。

4. **「.claude/worktrees/ の汚染は無い」**——`grep -r .` でこの述語を撃つと `.claude/worktrees/agent-*/` 配下の複製ファイルが実際に混入することを実測で確認したが、research.md 自身のコマンドは `snotra-core/ src-tauri/ snotra-settings/ snotra-egui-runtime/` と crate ディレクトリを明示しており、`.claude/worktrees/` は含まれない。**汚染は起きていない**——research.md の自己申告どおり。

5. **「`broken_intra_doc_links` は workspace で deny・4 crate 全部が opt-in 済み」**——`Cargo.toml` の `[workspace.lints.rustdoc] broken_intra_doc_links = "deny"` を確認し、4 crate すべての `Cargo.toml` が `[lints] workspace = true` を持つことを確認（`snotra-egui-runtime/Cargo.toml` にはコメントで「ルート [workspace.lints.rustdoc] の deny がここで初めて効く」との記載あり＝#701 で意図的に配線）。実測（上記 [壊せた-3] の cargo doc 実行）でも `-D rustdoc::broken-intra-doc-links` がコマンドラインから指定されたのと同じ形で自動的に効いていることを確認。**表の「○」は本物**。

6. **「日本語の否定文字クラス `[^/!]` が意図どおり働いているか」**——`//` の直後に日本語（マルチバイト UTF-8）が来るケースを含めて 20 件全件を確認したが、`[^/!]` は「`/` でも `!` でもない 1 バイト」を要求するだけで、日本語の先頭バイトはこの条件を通常満たす（ASCII の `/` `!` と衝突しない）ため、意図どおり `///`・`//!` だけを除外できていた。バイト単位マッチが原因の false negative は見つからなかった。**壊せなかった**（この特定の述語形では罠が発火しない）。

7. **「`git grep <commit>` と作業ツリーの `grep` が同じ母集団定義で取られているか」**——8c606f2a・e440c64f（#1176 merge 直後・18 件）・現在（main・20 件）の 3 点で同一の正規表現・同一のディレクトリ引数を使って測り直し、8 → 18 → 20 の推移を確認した。述語の不整合は見つからなかった。**壊せなかった**。

---

=====================================================================
## ⚠️ 確信の持てない所見
=====================================================================

### [⚠️-1] ADR-comment-guideline-delivery-by-pointer「却下 3」と G-folded-code-spans の対象は同一であり、research.md の「覆っている」という表現はおおむね正確——ただし issue #988 への適用可能性への含意はやや強すぎる可能性

`G-folded-code-spans` の実装ヘッダを実測した:
```
//! G-folded-code-spans — コードスパンが物理改行を跨いだ形（#992）。
```
これは却下 3 の対象「折返し条項が禁じる『コードスパンを行またぎさせない』(...)コメント行のバッククォートの偶奇 + コードフェンス状態」と字面・述語（バッククォートの偶奇判定＋コードフェンス除外）ともに一致する。**対象は本当に同一**であり、research.md の「その却下は #1176 が覆している」という核の事実認定は正しい（コミットは #1166・17d953ca であり、research.md 側の「#1176」という帰属コミット番号がここで**誤記**になっている可能性がある——要確認。research.md の該当箇所を再読すると `#1166（17d953ca）` と正しく書かれており、誤記ではなかった。撤回）。

ただし、却下 3 が扱っていたのは**行またぎコードスパン**（`` `foo` `` が改行で分断される問題）であり、issue #988 が扱う**intra-doc link 形が `//` で検査されない**問題とは技術的に別物である。issue 自身も「同種の検知器」という表現で両者を**類推**として引用しているに過ぎず、「対象が同一だから却下 3 の裁定がそのまま #988 にも及ぶ」と主張しているわけではない。research.md の「先例は覆っている」という一節は、**却下 3 という個別の decision が覆った事実は正しい**が、これによって「issue #988 の主張全体の説得力が失われる」とまで一般化できるかは、二つの issue の類推の強さ次第であり、断定しにくい。

### [⚠️-2] ADR の「行またぎスパンの再発は誰も検知しない」は全体としては陳腐化しているが、`docs/adr/` と `docs/superpowers/` については今も真でありうる

comment-guidelines.md「日本語の折返し」節:
> 母集団の正本は各検査が導く `allHeadingRefDocs` であり(...)`.md` は大半が母集団の外にある（`docs/adr/` と `docs/superpowers/` が落ちるため。`.rs` とスクリプトは今日すべて入る）。

これが事実なら、`docs/adr/*.md` や `docs/superpowers/*.md` 内で新たに行またぎコードスパンが発生しても `G-folded-code-spans` の走査対象母集団（`allHeadingRefDocs`）に入らず検知されない可能性がある。この場合、ADR の「受容する残余」の一行（「行またぎスパンの再発は誰も検知しない」）は**`.rs` と大半のスクリプトについては偽になった**が、**`docs/adr/` 自身と `docs/superpowers/` については今なお真でありうる**。research.md はこの残余行を単純に「陳腐化した」と扱っており（「同 ADR の『受容する残余』の一項が現在は偽である」）、母集団の穴（`docs/adr/` 自身が除外されている）までは検算していない。**未検証**——`allHeadingRefDocs` の実際の生成コード（`scripts/governance/lib.mjs` 等）を読んで `docs/adr/` が本当に除外されているかまでは確認していない（comment-guidelines.md の記述を根拠にしているのみ）。この点は research.md の「未解決の疑問 2」（この一行の陳腐化をどう扱うか）に対する回答をより慎重にする材料になる。

### [⚠️-3] [壊せた-1] の指摘は「(a) の全部が違反」という一律の言い方への反証であって、「(a) は issue の要求として全く不要」ということまでは示していない

[壊せた-1] で示したのは、第一原則の link-form 要求が 20 件全部に一律に及ぶとは限らないという読みの余地であり、逆に「20 件のうち構造的事実の代替に当たる箇所は依然 (a) 化（バッククォートのみ化）が規範違反になる」可能性も残る。research.md の「生きて残るのは (b) だけ」という結論も、この意味では（(a) が一部で禁止されたままなら）過度に単純化されている可能性がある——ただし逆方向（(a) が広く許容される）に倒れれば research.md の実務上の結論はほぼ無傷で残る。**どちらの読みが優勢かは、20 件それぞれの文脈（構造的事実の代替か、純粋な why か）を 1 件ずつ判定しないと確定しない**——これはまさに issue #988 が要求していた「1 件ずつの判断」であり、research.md も issue もこの粒度までは踏み込んでいない。

=====================================================================
## まとめ（返り値用）
=====================================================================

壊せた項目 3 件（うち 1 件は実行時実験による直接反証・「[`IndexMaterial`]」が実際に cargo doc で unresolved link エラーを出した）。壊せなかった項目 7 件（#1176 の反転自体、8/18/20 件のカウント、汚染なし、deny 設定の実在、バイト述語の健全性は全て確認どおり）。⚠️ 3 件（却下3の対象同一性はほぼ確定だが issue #988 への波及の強さは未確定／ADR残余行の陳腐化は`.rs`面では真だが`docs/adr/`自身は母集団除外で今も検知されない可能性／(a)の禁止範囲の一律性は読みが割れる）。
