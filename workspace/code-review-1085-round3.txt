# code-reviewer — #1085 ラウンド 3（019d218 の検算）

所見: High 0 件 / Medium 1 件 / Low 2 件（ほかに確認のみ 1 件）。

H1（1 パス字句スキャナへの置換）と H2（偽の全称の書き直し）は解消を実測で確認した。
ラウンド 2 のプローブ 8 件は 8 件とも解消している。新しい欠陥の探索では 32 入力を当て、
沈黙側 2 件・誤検出側 1 件を見つけた——いずれも露出は現在 0 件で、High には当たらない。
ドッグフード・カナリアは縛っていることを決定的な形で確認した（下の Q3）。

一次証拠の区別: 「実測」はプローブの出力そのもの。Rust / cargo / rustfmt の意味論に触れる
箇所は reference 知識として明示する。

================================================================
## Q1. ラウンド 2 のプローブ一式（誤検出 4・沈黙 4）— 8 件とも解消
================================================================

| # | 入力（lib.rs） | 期待 | 実測 |
|---|---|---|---|
| 誤検出1 | pub const U: &str = "http://example.com"; ＋ mod real; | 緑 | 緑 OK |
| 誤検出2 | pub const Q: char = (char リテラルの二重引用符); ＋ mod real; | 緑 | 緑 OK |
| 誤検出3 | pub const S: &str = "/*"; ＋ mod real; | 緑 | 緑 OK |
| 誤検出4 | 先頭 BOM ＋ mod real; | 緑 | 緑 OK |
| 沈黙1 | r#"x"y / mod ghost; / "#; | 赤 | 赤 OK |
| 沈黙2 | r#" / #[path = "ghost.rs"] / mod g; / "#; | 赤 | 赤 OK |
| 沈黙3 | "http://x"; ＋ 複数行文字列の中の mod ghost; | 赤 | 赤 OK |
| 沈黙4 | char リテラル ＋ 複数行文字列の中の mod ghost; | 赤 | 赤 OK |

ラウンド 1 由来の 5 件（ブロックコメント / インライン mod ＋ 同名の兄弟 / 複数行文字列 /
src/bin / #[path] の .. 解決）も期待どおりを維持している。13 件すべて一致。

そちらの計測値も追試して一致した:
  -t "G-module-linkage" -> 22 passed
  npm test -> 677 passed
  npm run governance:check -> 全 20 検査 passed（実リポジトリで findings 0）
  checkModuleLinkage の実行時間は 1 回あたり約 98 ms（2 パス版は約 70 ms。増分は許容範囲）

================================================================
## Q2. 新しい欠陥の探索（32 入力・すべて実測）
================================================================

指定された 4 つの疑い方に沿って、誤検出側 15 本・沈黙側 17 本を当てた。

### 壊れなかったもの（＝スキャナが正しく処理した。列挙して残す）

誤検出側（本物の mod real; が緑のままであること。15/15 緑）:
  let r = 1; / let br = 1; / r#type（raw identifier）/ pub fn f<(ライフタイム a)>(x: &(a) str)
  / &(static) str / W<(_)> / 隣接する 2 つのライフタイム / エスケープされた char リテラル
  / byte char / byte string / 末尾がバックスラッシュのエスケープを含む文字列 / 除算の a / b
  / char の (スラッシュ) / doc コメント中のアポストロフィ / ブロックコメント中のアポストロフィ
  / raw string r#"a"#
  → 指定された「識別子末尾の r・br」「ライフタイム境界」の誤発火は 1 件も作れなかった。
    raw string 判定が ^b?r(#*)" で二重引用符を要求するため、r の直後が空白・=・# のいずれでも
    発火しない。char 判定も 1 文字ちょうど（またはエスケープ 1 個）と閉じ引用符を要求するため、
    ライフタイムは綴りで確実に外れる。

沈黙側（孤児 ghost.rs が赤になること）:
  ハッシュ 1〜22 個の raw string / br#" / U+FEFF 以外の char エスケープ('unicode escape') /
  ラベル(loop のライフタイム) / 入れ子ブロックコメント / 末尾バックスラッシュ / byte string /
  複数行文字列の中の #[path] / raw string の中の // / CRLF / 未終端の文字列・raw string・
  ブロックコメント（3 種とも末尾まで潰れて赤＝安全側）
  → 指定された「閉じない字句で沈黙側へ倒せるか」は作れなかった。潰しは位置と改行を保つので、
    潰しすぎても列 0 の綴りを新しく作ることが原理的にできない（＝赤にしか倒れない）。
  → #[path] の位置検算（潰した側で text[m.index] が # であること）も迂回できなかった。
    正規表現が連続一致を要求するので、# だけをコードに置いて残りをリテラルに置く綴りが作れない。

### Medium — 受容する残余の一覧に、沈黙側が 2 つ欠けている

場所: C:/workspace/Snotra/scripts/governance-check.mjs:185-198（受容する残余）
      ＋ C:/workspace/Snotra/docs/adr/ADR-ra-diagnostics-suppression.md:51-53

壊れた不変条件: 一覧は沈黙側として cfg の和だけを挙げているが、実測でもう 2 つある。
どちらも「列 0 に綴られているが本物の宣言ではないもの」を拾う形で、向きは沈黙である。

(1) macro_rules! の本体に列 0 で書かれた mod（実測・緑＝沈黙）

    macro_rules! m {
        () => {
    mod ghost;
        };
    }

    孤児 ghost.rs が緑になる。露出は現在 0 件（リポジトリに macro_rules! は 1 件も無い・実測）。
    rustfmt は macro_rules! の本体を整形しないことがある（reference 知識）ので、列 0 に来る
    余地は消えない。インライン mod x { ... } と同じ類型であり、そちらは一覧に載っている。

(2) ハッシュ 23 個以上の raw string の中の mod（実測・緑＝沈黙）

    場所: C:/workspace/Snotra/scripts/governance-check.mjs:249 の text.slice(i, i + 24)
    閾値をハッシュ 1〜30 個で走査して確定した——22 個までは赤、23 個から緑。
    24 文字の窓に r + ハッシュ + 二重引用符 が入らなくなると raw string と認識されず、
    中身がコードとして走査される。妥当な Rust で再現する（中身に二重引用符が 1 つ要る）:
      pub const S: &str = r(#が23個)"x"y
      mod ghost;
      "(#が23個);
    Rust はハッシュを 255 個まで許す（reference 知識）。実際の Rust コードは r### までで、
    露出は現在 0 件。

修正例: (2) はコード 1 行で閉じられる——窓を切らず、sticky な正規表現を元テキストへ当てるか、
上限を 256 にする。(1) は一覧に 1 行足すのが妥当（マクロ本体を追うのは費用に見合わない）。
いずれも「一覧へ足す」だけでも規範としては足りるが、**沈黙側の足は名指しで書く**こと——
現在の一覧は沈黙側を cfg の和 1 つしか挙げておらず、読者は「他は赤」と読む。

### Low — cfg で分けた同名モジュールのうち片方に #[path] があると、もう片方が赤くなる

場所: C:/workspace/Snotra/scripts/governance-check.mjs:309 の viaPath.has(m[1]) による読み飛ばし

viaPath はファイル単位・名前単位なので、同じ名前が cfg 違いで 2 回宣言され、片方だけが
#[path] を持つとき、もう片方の通常宣言が丸ごと読み飛ばされる。実測:

  #[cfg(windows)]
  #[path = "win.rs"]
  mod platform;
  #[cfg(unix)]
  mod platform;

  => demo/src/platform.rs が「到達できない」として赤（誤検出）。

向きは赤（大声）で、露出は現在 0 件。リポジトリの ime.rs は非 Windows 側をインライン
mod platform { ... } で書いているので当たらない——が、これは同じパターンのもう 1 つの
自然な綴りであり、次に platform 分岐を書く人が踏みうる。

修正例: viaPath を「名前」ではなく「その #[path] 宣言の位置」で持ち、通常ループでは
同じ位置の再解決だけを飛ばす。あるいは受容する残余へ 1 行足す（向きが赤なので後者でも可）。

### Low（確認のみ・所見ではない）— H2 の書き直しは実装と一致している

docs/adr/ADR-ra-diagnostics-suppression.md の残余 5 は、追わない綴り（インライン mod・
include!・属性の変種）に**限定して**「過検出の向き」と言い、そのうえで「誤りはすべて赤へ
倒れるとは言えない／両方向へ倒れる」と分けて書いている。私のプローブでも当該綴りは赤で、
限定は真である。scripts/governance-check.mjs:176-184 の設計注記も同じ形。
ラウンド 2 の H2 は解消と判定する。

================================================================
## Q3. ドッグフード・カナリアは縛っているか — 縛っている（決定的な形で確認）
================================================================

複製に変異を当てた（稼働中のガードは触っていない）。scripts/governance-check.mjs と
.claude/hooks/ をスクラッチパッドへ複写し、複写側だけを書き換えて実リポジトリを読ませた。
ドッグフードの判定（実リポジトリの全 .rs へ mod probe_zz; を注入し、declaredModuleFiles が
拾えるか）を同じ形で再現している。

| 版 | 拾えないファイル | ドッグフード |
|---|---|---|
| 対照（019d218・無変異） | 0 / 95 | 緑（落ちない） |
| **f26556f そのもの（歴史上の欠陥）** | **3 / 95** | **赤（落ちる）** |
| 変異: 文字列を末尾まで潰す（潰しすぎ） | 80 / 95 | 赤（落ちる） |
| 変異: 文字列を字句として扱わない | 0 / 95 | 緑（落ちない） |
| 変異: スキャナ素通し（潰し足りない） | 0 / 95 | 緑（落ちない） |

2 行目が本題である。**f26556f の実物**（declaredModuleFiles を export しただけで、他は 1 文字も
変えていない）にこの判定を当てると、ラウンド 2 で私が名指しした 3 枚——snotra-core/src/instant.rs・
src-tauri/src/egui_shell/mod.rs・src-tauri/src/startup.rs——がそのまま落ちる。
**このカナリアは、書かれる理由になった当の回帰で落ちる。** 飾りではない。

ただし縛る向きは限定されている。ドッグフードが見るのは「注入した宣言を拾えるか」だけなので、
**潰しすぎ（誤検出）の向きにしか効かない**。潰し足りない側（沈黙）では拾える宣言が増えるだけ
なので、素通し変異でも緑のままだった（上表 5 行目）。沈黙側を縛っているのはフィクスチャで、
素通し変異を当てると 5 本が落ちることを個別に確認した（ブロックコメント・raw string の奇数個の
二重引用符・raw string 中の #[path]・URL 位相反転・char 位相反転）。そちらの「素通しで 6/22」
とも整合する。

所見ではないが、記録として: 上の表の「文字列を字句として扱わない」変異は、歴史上の欠陥と
**同じではない**。当該変異ではファイル全体を巻き込む位相反転が起きず、行コメント誤認が
その行で止まるためである。**変異を「本来の回帰と同じ強さか」で測るという規範
（.claude/rules/safety-nets.md）が、ここで実際に効いた**——同じと見なしていれば
「カナリアが縛れていない」という誤った結論を出すところだった。

================================================================
## Q4（ラウンド 2 で決着済み・再掲のみ）
================================================================

M4（cfg の和）確定・真 / L2（#[cfg_attr]）確定・赤 / L4（WALK_EXCLUDE_NAMES）確定・沈黙 /
ADR の「現在 0 件」確定・真。今回あらためて崩れていないことを確認した（cfg の和は上の
Medium (1)(2) と同じ「沈黙側の残余」の仲間であり、こちらは既に一覧に載っている）。

================================================================
## Phase 2 の更新（差分ぶんのみ）
================================================================

2a: 計画の不変条件「全称表現を作らない」は、今回は**守られている**。ラウンド 1・2 で 2 度
    破られた条項だが、019d218 の書き直しは限定つきで、私の実測と一致した（上の「確認のみ」）。
    fail-closed・workspaceMembers・G-module-index の保証も維持（実リポジトリ緑・22 本緑）。
2b: 対称ペアの判定は変化なし。
2c: declaredModuleFiles を export したのはカナリアが実ファイルで検算するためで、
    doc コメントにその理由が書かれている。呼び出し元は governance-check.mjs 内部と
    テストの 2 か所だけで、同等ロジックの手書きは無い（実測）。
2e: SPEC 対象外（scripts/ と docs/ のみ）。
Phase 3: スキップ。計測値は Q1 のとおり（1 回あたり約 98 ms）。

================================================================
## 総評
================================================================

3 巡目で、はじめて**修正が新しい High を作らなかった**。構造で塞ぐ選択（2 パス → 1 パスの
字句スキャナ）が効いている——残った 3 件はいずれも「スキャナが扱う字句の外側」の話で、
2 パスのときのような「一方の数え違いがもう一方を反転させる」型の欠陥ではない。

残る 3 件はどれも露出 0 件・向きも明示できる形なので、**コード修正なしで一覧へ足すだけでも
閉じられる**（ハッシュ 23 個の件だけは 1 行で直せるので、直すほうが安いと思う）。

最後に 1 つ、AGENTS.md「レビュー指摘へ修正（fix-forward）を当てた」の行に従って言う——
この 3 件へ修正を当てたら、**その差分にもこのプローブ 32 本を当て直すこと**。ただし今回の
ように「一覧へ 1 行足す」だけの修正なら、機構は変わらないのでプローブの再実行は要らない。
再実行が要るのは text.slice(i, i + 24) を触る場合である。

制約の遵守: 本ファイル（指定された出力先）以外、リポジトリ内のファイルは変更・作成・削除して
いない（git status は本ファイルの untracked 1 件のみ）。変異はすべてスクラッチパッドへ複写した
複製に当てた。実行したのは git show/diff・grep・npm test・npm run governance:check・node -e・
スクラッチパッド内の cp / mkdir だけである。

