# code-review 2 巡目 — issue #732（fix-forward 差分の再検算）

母集団: 1 巡目と同じ `git diff HEAD`（13 ファイル）＋ 未追跡 `.claude/rules/comments.md`。**修正分だけを切り出していない。** 1 巡目→2 巡目の差分の同定には、1 巡目に取得した `git diff HEAD` の全文と現在の全文を突き合わせた（`diff` の結果で、コーディネータが申告した 8 箇所以外に変更が無いことを確認した）。

**結論: Critical 0 / High 0。** 1 巡目の H1・H2・M2・L1・L3・L4 と ⚠️1・⚠️2 はすべて閉じた（下の表に再測の証拠つき）。**新規は Medium 2 件・Low 4 件で、いずれも 1 行の編集で閉じる。** コーディネータの最大の関心（H1 の型の再発）への答えは **「字面では再発している／帰結としては再発していない」**——R2-L1 に分けて書いた。

再実行した検査: `npm run governance:check`（**全検査 passed**・見出し参照 **175 件**＝1 巡目 174 から +1・近傍の見出し参照 12 → 14）。行またぎスパン検知器（自作・フェンス除外）で全 `.rs` を再走査して **0 件**。他は指示に従い再実行していない。

---

## 1 巡目の所見のクローズ状況

| 所見 | 当てた修正 | 私の再測（一次証拠） | 判定 |
|---|---|---|---|
| H1（条項が「次の 3 段」と書いて 4 段） | 「次のとおり区別する」へ | `rg -n "次の [0-9] 段" docs/comment-guidelines.md` が **0 件**。段は `:22`〜`:25` の 4 本で変わらず | **閉** |
| H2（射程規則が解を返さない） | 1c へ tie-breaker、`plan.md` の偽の全称否定を機序つきで訂正、残余を記録 | `docs/comment-guidelines.md:100` に tie-breaker が在り、`plan.md:64` に訂正が在る。**tie-breaker が「カバー」を選ぶことは条項から導出できる**（「訳さない側へ倒す」＝カタカナ側） | **閉（ただし残余の根拠に R2-M1）** |
| M2（`path_query_cost.rs:231` の腐った頻度主張） | 頻度の主張を外し正本へ寄せた | `git log -S` で当該行の出自は `da73bcb`（2026-08-07・#962）。**その時点で既に空クエリの早期 return（`launcher_controller.rs:765`）と 2 経路だけの呼び出しが在った**（`git grep -n "recent_history()" da73bcb -- src-tauri` で確認）——**この主張は生まれた時から偽であり、腐ったのではない** | **閉（R2-M2 の指摘つき）** |
| L1（`同「日本語の折返し」`） | 正準形へ展開 | `HEADING_REF` を実データへ当てて **`code-reviewer.md:28` の 2 本とも CHECKED**（`docs/comment-guidelines.md`「第一原則」/「日本語の折返し」）。`同「` の残りは **0 件** | **閉** |
| L3（列挙の理由が無い） | 「index 0 の不変条件が枝ごとに同じ」を追記 | `font_stack.rs:7` 行末に理由が在り、`//!` の「いずれも index 0 へ `insert` する」と整合 | **閉（表現に R2-L3）** |
| L4（`plan.md` の 3 段/4 段目 不整合） | H1 と同時に解消したと申告 | `plan.md:61` は「段数は本文にも計画にも書かない」へ直った。**が `2 段目`/`4 段目`/`3 段目` の序数参照が 5 件残っている** | **部分（R2-L4）** |
| ⚠️1（「1 枝を足した」の可否） | 「過去の変化量は対象外」を追記 | `docs/comment-guidelines.md:22` に在る。`font_stack.rs:7` の「#689 が 1 枝を足した」がこの例外に収まる | **閉** |
| ⚠️2（「窓」の実測値が再現しない） | 数え方（出現数 `grep -o`）と行数版を併記 | `plan.md:60` に両方が在り、私の `rg -c` の値（27 対 13 / 8 対 2）と一致 | **閉** |

そのほかの回帰確認（母集団全体に対して）:

- 分断されていた 5 つのコードスパンは**いま全件 `rg -F` で 1 件ずつ引ける**（`proof.rs` / `results_window.rs` / `strings.rs` / `icon.rs` / `style.rs`）
- 変更 3 枚の `.md`（`docs/comment-guidelines.md` / `.claude/rules/comments.md` / `.claude/agents/code-reviewer.md`）に**バッククォート奇数個の行は 0 件**（＝行をまたぐスパンなし。フェンス除外）
- `.rs` の doc へ足した intra-doc link は 1 巡目から変わらず着地する（`cargo doc` 再実行の申告と整合）

---

## Medium

### R2-M1. tie-breaker の残余の根拠（「as-built だから書き換えない」）が repo の規範から導けず、同じ bullet の適用範囲と将来衝突する

**これはコーディネータの問い 4「tie-breaker が `SPEC.md:647` の残余を実際に説明できているか」への直接の答えである。判定: 説明はしているが、根拠が導けないので不足がある。**

- **場所**: `docs/comment-guidelines.md:100`（射程 bullet 末尾）——「`SPEC.md` 側は as-built の記述なのでこの規約では書き換えず」
- **一次証拠**: `as-built` は repo に実在する概念だが、**用語規範からの免除を述べた箇所は無い**。

```
$ rg -n "as-built" SPEC.md AGENTS.md CLAUDE.md */CLAUDE.md docs/*.md .claude/rules/*.md
SPEC.md:208 / :245 / :258 / :628 / :637（### as-built ＝節名とマーカー）
.claude/rules/spec.md:9  - **as-built を記述する**: …（書く義務の規則。書き換え免除ではない）
```

- **壊れる不変条件（将来）**: 同じ bullet の 2 文前に「**適用は新規分と、その変更で触った箇所に限る**」が在る。誰かが別の理由で `SPEC.md:647` を編集した瞬間、この 2 文は逆を指す——適用範囲は「触ったから射程内」と言い、as-built の文は「書き換えない」と言う。**どちらが勝つかは条項に書かれていない。**
- **修正例（どちらか）**:
  1. 根拠を**既に在る文へ寄せる**: 「`SPEC.md` 側は今回の変更で触っていないので射程外である（→ 適用範囲）」。これなら repo の規範から導ける
  2. as-built の文を**この実例の記録だと明示する**: 「（この実例では `SPEC.md` を触らなかったので据え置いた）」。規範として読まれない形にする

`font_covers_cjk` を共通の錨として名指しした点は**良い**（`src-tauri/src/egui_shell/font_stack.rs:34` に実在。snake_case ゆえ `G-stale-identifiers` の述語外で安全）。

### R2-M2. `path_query_cost.rs:231` が正本の持つ呼び出し元リストを再作成し、**2 つの写しが初日から食い違っている**

- **場所**: `snotra-core/tests/path_query_cost.rs:231` vs 正本 `snotra-core/src/search.rs:331-334`
- **食い違いの実体**（どちらも真であることを実装で確認した。だから correctness ではなく DRY の所見である）:

```
search.rs:333-334  「[`Self::search_with_options`] は空クエリに対して `Vec::new()` を返すだけで、ここを呼ばない」
                    → 実装: search.rs:256 の prepare_query_plan が norm_query 空で None を返し早期 return（callee 側の門）
path_query_cost.rs:231  「空クエリは呼び出し側が早期 return し」
                    → 実装: launcher_controller.rs:765-767 が set_results(Vec::new()) して return（caller 側の門）
```

- **壊れた不変条件**: 「頻度の正本は 1 か所」。正本を指すポインタ（「頻度の正本は `SearchEngine::recent_history` の doc」）を書いた**うえで**、呼び出し元の列挙と機序の説明を書き直している。ポインタ＋再導出は 2 正本であり、**初日から説明が 2 種類ある**状態を作った。
- **腐り方（具体）**: 1 巡目の修正で `search.rs:331` は「呼び出し元は**いずれも**明示の操作である」＝**数に依存しない全称**へ寄せた。いま `path_query_cost.rs:231` は `/r` とトレイを**名指しで閉じた列挙**にしている。3 つ目の明示操作が増えたとき、**正本は真のまま `tests/` 側だけが偽になる**——1 巡目に「数を落として全称にする」で得た性質を、下流で打ち消している。
- **修正例**（否定の主張とポインタは残し、再導出だけ落とす）:

```diff
-/// `recent_history` の実コスト。**窓を開いた瞬間・クエリ消去時には走らない**——空クエリは呼び出し側が早期 return し、走るのは `/r` スラッシュコマンドとトレイの履歴メニューである（頻度の正本は `SearchEngine::recent_history` の doc。この計測は「呼ばれたときの 1 回」を測る）。
+/// `recent_history` の実コスト。**窓を開いた瞬間・クエリ消去時には走らない**（頻度と呼び出し元の正本は `SearchEngine::recent_history` の doc）。**この計測が測るのは「呼ばれたときの 1 回」である。**
```

これで「何を代表する計測か」（＝コーディネータの問い 2 の核心）は保たれ、呼び出し元の列挙は正本 1 か所に残る。

---

## Low

### R2-L1. H1 の型は**字面では再発している／帰結としては再発していない**（コーディネータの問い 1 への答え）

- **場所**: `docs/comment-guidelines.md:21` の新しい括弧——「**この列に数を書かない**——段を足したときに条項自身が腐る。実際に初稿は「3 段」と書いたまま 4 段になっていた」
- **字面**: 数を書くなと命じる文そのものが、その列の数を 2 つ（「3 段」「4 段」）含んでいる。同じ差分で足した例外（`:22` の「**過去の変化量は対象外である**」）は**変化量**を免除しており、**過去の絶対数**は文面上カバーしていない。ゆえに規則は自分の文を自分で許していない。
- **帰結**: それでも**腐らない**。「初稿は…4 段になっていた」は過去の一時点についての記述で、5 段目を足しても真のまま残る。**だから Low である**（実害の無い自己不整合）。
- **一方で 2 つの様式違反は実在する**——同じ文書の「歴史メモの様式」が事故の記録に要求している 2 点を欠く: **issue / PR 番号を `#NNN` で参照する**、**実測値には条件（日付）を添える**。この括弧は #732 も日付も持たない。
- **修正例**（数を落とし、様式を満たす。1 手で 3 つとも閉じる）:

```diff
-次のとおり区別する（**この列に数を書かない**——段を足したときに条項自身が腐る。実際に初稿は「3 段」と書いたまま 4 段になっていた）:
+次のとおり区別する（**この箇条の段数を書かない**——初稿は段数を書いてしまい、段を足した時点で条項自身が腐った・#732 / 2026-08-08）:
```

### R2-L2. 同じ対象を「段」と「列」の 2 語で呼んでいる（`:21` だけが「列」）

- **場所**: `docs/comment-guidelines.md:21`（および `plan.md:261` の同文）
- **根拠**: これは**語の正しさの問題ではない**（「列」は数列・系列・列挙のとおり並びを指す普通の日本語である）。**同一の対象に 2 つの呼び名が付いていること**が所見である——この文書と計画は 4 つの区別を一貫して「段」と呼び（`docs/comment-guidelines.md:22-25` に続く 4 段・`plan.md:101`「1a の 2 段目」・`:175`「4 段目」）、`:21` の「この列」だけが別の語である。訳語条項が定着語について強制する唯一の項目（**同一ファイル内で語を混在させない**・`:100`）と同型の混在である。
- **修正例**: 「この段」か「この箇条」へ揃える。R2-L1 の diff に含めてある（そちらは「この箇条の段数」としてある）。

### R2-L3. 「1 目で見せる」——「一目」は慣用句であり、算用数字にすると別の語になる

- **場所**: `src-tauri/src/egui_shell/font_stack.rs:7` 行末（および `plan.md:265`）
- **一次証拠**: repo 全体で `一目` は 1 件（`docs/superpowers/` の履歴資料）、`1 目` は **この差分が書いた 2 件（＋私の 1 巡目レビュー 1 件）だけ**である。

```
$ rg -n -o "1 目" --glob *.rs --glob *.md .
workspace/plan.md:265:1 目
workspace/code-review-732.md:130:1 目
src-tauri/src/egui_shell/font_stack.rs:7:1 目
```

- **これは私の落とし穴である**——1 巡目の L3 の修正例に私が「1 目で見せる」と書き、それが逐語で採用された。`1 目` は囲碁の「一目（いちもく）」等と衝突し、数量として読めてしまう。
- **修正例**: `**枝を列挙するのは、この index 0 の不変条件が枝ごとに同じであることを一目で見せるためである**`（`一目` へ）。

### R2-L4. `plan.md` は「段数は本文にも計画にも書かない」と宣言しながら、序数参照 5 件を残している

- **場所**: `plan.md:61`（宣言）vs `:101`「1a の 2 段目」・`:175`「1a の 4 段目」・`:265`「3 段目」・`:266`「4 段目」・`:295`「3 段目」
- **なぜ「閉」ではないか**: 宣言が禁じたのは**段数**（いくつあるか）であり、**序数**（何番目か）は文面上カバーされていない。しかし腐りやすさは序数の方が高い——**段を 1 本挿入すると 5 件すべてが黙って別の段を指す**。H1 は「数が合わなくなる」だけだったが、序数は「合わないことすら見えない」。
- **射程の限定（だから Low）**: 現在この序数は `workspace/plan.md` にしか無い（`docs/comment-guidelines.md` / `.claude/rules/comments.md` / `.claude/agents/code-reviewer.md` に `N 段目` は **0 件**）。`workspace/` は `/implement` が削除する作業バッファであり、`headingRefDocs` の母集団からも外れている。
- **修正例**: 序数ではなく**先頭句で指す**——「1a の「名指しは書いてよい」の段」。恒久文書へ同じ書き方を持ち込まないことが本質で、plan 側は放置してもよい（**その判断をするなら宣言の文言を「段数と序数は書かない」へ揃えるか、宣言を plan 限定と明示する**）。

---

## 観点 2 — 逆向きの監査（修正差分が消した行）

道具: 1 巡目の `git diff HEAD` 全文と現在の全文の突き合わせ（消えた行の同定）＋ `git log -S`（その行の出自）＋ 実装での接地。

| # | 修正が消した行 | その行が運んでいた情報 | 再確立地点 | 判定 |
|---|---|---|---|---|
| 1 | `path_query_cost.rs:231`「空クエリ（窓を開いた瞬間・クエリ消去時）に走る `recent_history` の実コスト。」 | **この計測が何を代表しているか**（どの契機の経路のコストか）。コーディネータが名指しした論点 | 新 `:231` の「**この計測は「呼ばれたときの 1 回」を測る**」＋ 直後の段落（`:233-235`「`normalized_keys` を廃止するなら…先に現状を測る」＝**動機は未変更で健在**） | **再確立済み・かつ訂正**。`git log -S` で当該行は `da73bcb`（2026-08-07・#962）で生まれ、**その時点で既に空クエリの早期 return と 2 経路だけの呼び出しが在った**——つまり「空クエリで走る」は**生まれた時から偽**で、腐ったのではない。ゆえに「消えた真の情報」は無く、代表性の記述はより正確な形で残った。**残る所見は列挙の重複のみ（R2-M2）** |
| 2 | `docs/comment-guidelines.md:21`「次の 3 段で区別する:」 | 「区別は 3 つある」という**網羅性の主張**（かつ偽だった） | 「次のとおり区別する（…）:」——数を持たない導入へ。網羅性は箇条書きの構造自身が持つ | **再確立済み**。数え上げ無しで閉じた列挙を示す形（`font_stack.rs:7` の「次のとおり」と同じ処方）。**新たな自己不整合は R2-L1** |
| 3 | `plan.md:61`「次の 3 段へ置き換える」 | 同上（計画側の写し） | 「次のとおり置き換える（**段数は本文にも計画にも書かない**…）」 | **再確立済み**。ただし序数参照は残存（R2-L4） |
| 4 | `code-reviewer.md:28`「同「日本語の折返し」」 | **参照そのもの**（節の改題に追随する経路） | 同行の `` `docs/comment-guidelines.md`「日本語の折返し」 `` | **再確立済み・機械検査下へ移った**。`HEADING_REF` を実データへ当てて CHECKED を確認し、`governance:check` の見出し参照が **174 → 175 件**（＋1 は正確にこの 1 本）。**`同「` の残りは repo 内 0 件** |
| 5 | `plan.md` の「「被覆」は規範文書に 1 件も無い」（偽の全称否定） | なし（偽の主張） | `plan.md:64` の訂正欄——**機序まで書いてある**（`grep -rln …` の出力を `head` で切り詰め、その一覧から全称否定を書いた） | **再確立不要・記録として上位互換**。`AGENTS.md`「検証の作法」が求める「撤回の型を残す」形になっている |
| — | 上記以外の削除 | — | — | **無い**。1 巡目の diff 全文と現在の diff 全文の突き合わせで、変更はコーディネータ申告の 8 箇所に限られることを確認した（`.rs` 4 ファイル分の折返し修正・`proof.rs`/`build.rs`/`search.rs`/`indexer.rs` の本文は**1 文字も動いていない**） |

---

## 観点 1 — 条項と実装の整合（修正差分へ当て直し）

| 検査 | 結果 |
|---|---|
| 新しく書いた文に「コードが決めている構造の事実」の数え上げが混入していないか | **`.rs` 側は混入なし**。`path_query_cost.rs:231` の「呼ばれたときの 1 回」は計測の意味づけ（構造の数ではない）、`font_stack.rs:7` の「1 枝を足した」は `:22` の新例外に収まる。**`.md` 側は R2-L1 の 1 件**（過去の絶対数・腐らない） |
| 呼び出し元の列挙が新しく増えていないか | **増えている → R2-M2**（`path_query_cost.rs:231` が `/r`・トレイを再列挙。正本 `search.rs:331` は「いずれも」で数に依存しない形へ寄せた直後である） |
| 新しい文がコードスパンを行またぎさせていないか | **させていない**。`.rs` 全体で 0 件（フェンス除外の自作検知器）、変更 3 枚の `.md` でバッククォート奇数行 0 件。`font_stack.rs:7` と `path_query_cost.rs:231` はどちらも 1 段落 1 行 |
| 追記した名指しが実在するか | **実在**。`font_covers_cjk` は `font_stack.rs:34`、`SearchEngine::recent_history` は `search.rs:342`、`snotra-settings/CLAUDE.md` の「カバー」は `:61`、`SPEC.md` の「被覆」は `:94`・`:647` |
| tie-breaker が条項として自足しているか（判定が導けるか） | **選択は導ける／残余の根拠は導けない → R2-M1**。「訳さない側へ倒す」からカバーが選ばれることは条項内で閉じる。「as-built だから書き換えない」は repo の規範に対応が無い |
| 訳語条項を自分の新しい文に当てたか | **当てて 1 件見つかった → R2-L2**（「この列」）。加えて **R2-L3**（「1 目」）は同型の語選びの誤りで、**1 巡目の私の修正例が持ち込んだもの**である |
| 参照の着地（母集団全体・再測） | **全件 1 候補で着地**。下の「自己訂正」のとおり、今回は見出しではなく `collectAnchors` そのもので測った |

---

## 自己訂正 — 1 巡目の観点 3 は代理で測っていた

1 巡目に私は「前方一致の候補が**すべて 1 件**だったので、緑は偶然ではない」と書いた。**その測定は見出し（`^#{1,6}`）だけを母集団にしていた。** 実際の `collectAnchors`（`scripts/governance-check.mjs:1177`）は 3 種を集める:

```
/^#{1,6}\s+(.+?)\s*$/gm            見出し
/^\s*\d+[.)]\s+(.+?)\s*$/gm        番号付きリスト項目
/^\s*(?:[-*]|\d+[.)])\s+\*\*(.+?)\*\*/gm   太字で始まるリスト項目
```

ゆえにアンカー集合は見出しより**ずっと大きい**。私の 1 巡目の「候補 1 件」は、母集団を過小に取った proxy 測定だった（`AGENTS.md`「検証の作法」の「主張は代理ではなく対象そのもので測ってから書く」に自分が違反した）。

**`collectAnchors` を import して測り直した結果、結論は変わらない**——`.claude/rules/comments.md` の 5 本・`.claude/agents/code-reviewer.md` の 8 本・`docs/comment-guidelines.md` の 3 本、**すべて候補 1 件で意図した相手に着地する**。あわせて、1 巡目に私の proxy 測定が `*** NO LANDING ***` と誤判定していた 3 本（`code-reviewer.md:85`・`:124`・`comment-guidelines.md:104`）は、実際には太字リスト項目アンカーへ正しく着地していることも確認した（`governance:check` が緑である理由がこれである）。

---

## 1 巡目 ⚠️ の決着

コーディネータの問い 3 に対して、決着できるものを決着させた。

| 1 巡目 ⚠️ | 決着 | 決め手（一次証拠） |
|---|---|---|
| ⚠️4「`indexer.rs:811` のポインタが 2 段」 | **決着・所見なし。条項への追記も不要** | 2 段は**冗長な中継ではない**。`indexer.rs:811` が問うのは「誰が受け取るか」で、`LoadOrScanResult::cached_masks` の doc（`indexer.rs:336-341`）がそれに直接答えている（「キャッシュヒットに限らない——cache-miss の枝も…載せる」）。その doc がさらに指す `load_cache_in` / `save_cache_sorted` は**別の問い**（`None` になる条件）の正本である。問いごとに 1 段で着地しているので、「正本は 1 段で指す」という条項を足す必要はない |
| ⚠️5「`search.rs:331` の呼び出し元が 1 段の間接を挟む」 | **決着・所見なし（語の曖昧さは残るが直す必要なし）** | 間に入る `Engine::recent_history`（`snotra-core/src/engine.rs:167`）は **doc コメントを 1 行も持たない**（`sed -n 160,172p` で確認）。ゆえに頻度の契約を書ける場所は `SearchEngine::recent_history` の doc しか無く、そこに終端の呼び出し元（`/r`・トレイ）を名指しするのが唯一の選択である。「呼び出し元」が直接の呼び出し元（`engine.rs:169`）とも読める曖昧さは残るが、**書き換えると誤読予防の目的を損なう**ので触らないのが正しい |
| ⚠️6「新 rule へ #588 の grep 規範を言い直すか」 | **異論なし（不要に同意）** | 却下理由は成立している——`.claude/rules/comments.md` は**行番号もファイル位置も 1 つも指さず**、指すのは見出し名だけである。#588 の注意（ファイル名・行で断定せず見出しで grep せよ）は、それを既にやっている文書には向ける先が無い |

決着させなかった 1 巡目 ⚠️: ⚠️3（自作検知器のフェンス偽陽性・記録として残す価値がある）・⚠️7（`snotra-settings` の配送実測が孫エージェント報告のまま・重複排除ゆえ現セッションで再測不能）。どちらも 1 巡目の記述のまま有効である。

---

## ⚠️ 確信の持てない所見（2 巡目・空にしない節）

1. **`docs/comment-guidelines.md:100` は `G-near-heading-refs` の照合対象を 2 件増やした**（近傍参照 12 → 14）。`` `SPEC.md` が「被覆」 `` と `` `snotra-settings/CLAUDE.md` が「カバー」 `` は、閉じバッククォートから `「` までの gap が 2 字なので **`NEAR_REF` に一致する**。findings が出ないのは「被覆」「カバー」が対象文書のアンカー（`collectAnchors` の 3 種）に前方一致しないからだけである。**将来 `SPEC.md` に「被覆…」で始まる見出しか太字リスト項目ができると、この条項の実例が「正準形でない参照」として赤くなる。** 起こりにくいが、機序は説明できるので記録する。
2. **`path_query_cost.rs` は同じファイル内で名指しの様式を混ぜている**——`:18-19` の `//!` は `` [`measure_path_query_frame_cost`] `` の link 形を使い、`:231` の `///` は素のバッククォートである。1a の 2 段目は「doc コメントでは link 形で書く…**リンクにできるものは**リンクにする」と書いているが、**`cargo doc` は `tests/` の doc をビルドしないので、ここでは link 形にしても着地は誰も検査しない**。つまり「リンクにできるもの」の判定基準が条項に無い。ファイルの側の欠陥ではなく**条項の文言の隙**だと考えるが、確信は無い（1 句足すべきか、`tests/` は射程外と明記すべきか、判断しなかった）。
3. **R2-L4 の射程判断に自信が持てない。** 序数参照が `workspace/plan.md` にしか無いことは測ったが、**`/retrospective` や次サイクルの誰かがこの plan から恒久文書へ文を移すとき、序数がそのまま運ばれる**可能性は否定できない。「plan は消えるから放置でよい」は、消えるまでに引用されないことを前提にしている。
4. **R2-M1 の 2 案のうちどちらが良いかは決めていない。** 案 1（適用範囲へ寄せる）は根拠が導けるようになるが、「触ったら SPEC の用語も直す」と読まれると `SPEC.md` の as-built に手が入る道を開く。案 2（実例の記録と明示）は安全だが、次に同じ状況へ来た人に指針を与えない。**どちらを選ぶかは `SPEC.md` の as-built をどう扱うかの方針判断で、レビュアーが決めるべきことではない。**
5. **「1 巡目→2 巡目で変更は 8 箇所だけ」の確認方法に限界がある。** 私は 1 巡目に取得した `git diff HEAD` の**保存済み全文**と現在の全文を `diff` した。これは作業ツリー同士の比較として正しいが、**1 巡目の取得時点より前に私が読んでいないファイルが在れば、その差分は両方の diff に等しく含まれて見えない**。母集団は `git status --short` で数え直したので（13 変更 + 1 未追跡）取りこぼしは無いと考えるが、方法の限界として書く。

---

## まとめ

**Critical 0 / High 0。** 1 巡目の 6 所見と ⚠️ 2 件はすべて閉じ、機械検査（`governance:check` 全検査 passed・見出し参照 175 件・行またぎスパン 0 件・5 スパン全件 grep 可）で裏を取った。

残るのは 1 行の編集で閉じるものだけ:

1. **R2-M2** — `snotra-core/tests/path_query_cost.rs:231` から呼び出し元の再列挙と機序の再説明を落とし、否定の主張＋正本ポインタ＋計測の意味づけだけ残す（**正本と初日から食い違っているのが実測された唯一の所見**）
2. **R2-M1** — `docs/comment-guidelines.md:100` の「as-built だから書き換えない」を、適用範囲へ寄せるか実例の記録と明示するかに直す（**コーディネータの問い 4 への答え: 残余は述べられているが根拠が導けない＝不足あり**）
3. **R2-L1 + R2-L2** — `docs/comment-guidelines.md:21` の括弧から数を落とし「この列」→「この段」/「この箇条」へ揃え、`#732` と日付を添える（1 手で 3 つ閉じる diff を上に置いた）
4. **R2-L3** — `font_stack.rs:7` の「1 目」→「一目」（**私の 1 巡目の修正例が持ち込んだ誤り**）
5. **R2-L4** — `plan.md` の序数参照は放置可。ただし宣言の文言（「段数は…書かない」）と実態を揃えるか、恒久文書へ持ち出さないことを明示する

**3 巡目は不要と考える**——上の 5 件はいずれも「文言の 1 行」であり、当てた結果を検算する枠組みは既に走っている（`governance:check` と、`.rs` を触るなら `cargo doc`）。ただし **R2-M2 を当てるときだけは `path_query_cost.rs` の段落全体を読み直すこと**——1 行の書き換えで「この計測が何を代表するか」が再び落ちるのが、この論点で 2 度起きた事故の型である。
