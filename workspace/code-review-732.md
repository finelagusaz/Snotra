# code-review — issue #732「コメントガイドラインを参照するようにする」

対象: `chore/comment-guidelines-delivery` の未コミット差分（`git diff HEAD` 12 ファイル）＋ 新規未追跡 `.claude/rules/comments.md`。

**総評**: 配送の穴は塞がっており（観点 3 は一次証拠で discharge 済み）、逆向きの監査で消えた不変条件はすべて再確立地点を持つ。挙動へのブロッカーは無いが、**H1・H2 はこの差分の中で閉じるべきである**——正本の条項本文が自分に違反した状態でマージされることになる。**新設した条項 2 つが、自分自身に適用したときに破れている**——1 つは条項本文の数え上げ（H1）、1 つは条項が「機械的に解く」と称した判定が実際には解かないこと（H2）。どちらも `docs/comment-guidelines.md` への小さな編集で閉じる（`.rs` と `SPEC.md` を触る必要は無い）。

再実行した検査: `npm run governance:check`（**全検査 passed**・rules 11134/12000 字・見出し参照 174 件）。他は指示に従い再実行していない。

（所見 ID が連続しないのは、レビュー中に候補 2 件を降格・統合したため。H1・H2・M2・L1・L3・L4 の 6 件が全所見である。）

---

## Critical

**なし。**

---

## High

### H1. 新設条項が自分の禁じた型を含む——「次の 3 段」と書いて 4 段ある

- **場所**: `docs/comment-guidelines.md:21`（行末）／段は `:22`・`:23`・`:24`・`:25` の 4 本
- **壊れた不変条件**: 「数え上げは誰も検査しないので書かない」という、**同じ行が導入した規範**。条項本文が自分の禁じた型を使い、しかもその数が既に誤っている（`AGENTS.md`「検証の作法」の「新設した規約は、既存の全事例に当てて検算してから書く」）。
- **一次証拠**:

```
$ rg -n "次の 3 段" docs/comment-guidelines.md
21:...**腐るのは数であって名指しではない**ので、次の 3 段で区別する:
$ sed -n 22,25p docs/comment-guidelines.md | cut -c1-24
  - **数は書かない**（
  - **名指しは書いてよい。
  - **列挙が「正本の代わり」
  - **一意性の宣言はこの項の
```

`plan.md:171` 自身が「**1a の 4 段目**が許す一意性の宣言の形」と書いており、実装中に 4 段目を足したことは計画側にも記録されている。正本だけが「3 段」のまま取り残された。

- **修正例**（**数を足すのではなく落とす**——この差分が `font_stack.rs:7` に適用したのと同じ処方を条項自身へ当てる）:

```diff
-**腐るのは数であって名指しではない**ので、次の 3 段で区別する:
+**腐るのは数であって名指しではない**ので、次のとおり区別する:
```

「4 段」へ直すだけでも今は正しくなるが、5 段目を足した瞬間に同じ事故が再発する。**この条項に限っては「誰も検査しない数を条項本文が持たない」形にできる**（`plan.md:61` の「次の 3 段へ置き換える」も同時に直すのが筋）。

### H2. 訳語条項の射程規則は、この差分が実際に解いた 1 件を「機械的に解けない」

- **場所**: `docs/comment-guidelines.md:100`（射程規則）／適用先は `src-tauri/src/egui_shell/font_stack.rs:7`（「被覆」2 件 → 「カバー」）
- **壊れた不変条件**: 条項が名乗った判定力。`plan.md:87` は「**射程規則は次の 2 件も機械的に解く**」と書き、その根拠を「『被覆』は規範文書に 1 件も無い」に置いている。**測ると規則は解を返さず、両方の語が「このリポジトリの語」になる。**
- **一次証拠**（射程規則が名指しする母集団＝`SPEC.md` と各 `CLAUDE.md` そのもの）:

```
$ rg -n -o "被覆[^、。]{0,12}" SPEC.md
94:被覆する構成では積まれない
647:被覆するなら Yu Goth
647:被覆判定は cmap 実測（
647:被覆漏れなら豆腐（□）になる
647:被覆する場合の描画は不変（e
$ rg -n -o "カバー[^、。]{0,10}" SPEC.md AGENTS.md CLAUDE.md */CLAUDE.md docs/*.md
snotra-settings/CLAUDE.md:61:カバーする**単一フォント
```

`SPEC.md:647` は font_family 解決の as-built 節（`## 11. ビジュアル` → `### as-built`）で、**font_stack.rs が実装しているまさにその判定**を「被覆判定は cmap 実測」と記述している（#689 も同じ）。`snotra-settings/CLAUDE.md:61` は「両スクリプトを**カバー**する単一フォント」——**同じ語義**（フォントが字を持つか）である。ゆえに射程規則の定着テストは「被覆も定着・カバーも定着」を返して止まる。

- **切り分け（重要）**: **コード側の変更（`font_stack.rs` を「カバー」へ寄せる）は妥当であり、戻す必要は無い。** 同ファイル 15 行目は変更前から「CJK をカバーするか」を使っており（差分外・既存）、条項が定着語について強制する唯一の項目（**同一ファイル内で語を混在させない**）はこの変更で満たされる方向へ動いている。問題は**条項がこの決着を導けないこと**と、**決着の記録が偽の実測に立っていること**の 2 点である。
- **修正例 1（条項へ tie-breaker を書く。1 文）**:

```diff
 適用は新規分と、その変更で触った箇所に限る。定着語について守るのは**同一ファイル内で語を混在させないこと**だけである
+**複数の訳語が定着している場合は「迷ったら訳さない側へ倒す」が決める**（例: フォントが字を持つかの語義は `SPEC.md` が「被覆」・`snotra-settings/CLAUDE.md` が「カバー」で、どちらも定着ゆえこの規則で「カバー」へ倒す）
```

これを置くと、この差分が実際に行った選択が**条項から導出可能**になる（今は plan の grep 結果からしか導けず、その grep が誤っている）。

- **修正例 2（記録の訂正・必須）**: `plan.md:87` の「**『被覆』は規範文書に 1 件も無い**（`.rs` の font_stack.rs 2 行だけ・`.md` のヒットは `.superpowers/` の履歴資料のみ）」と `plan.md:169` の「規範文書に 1 件も定着していない」は**反証済み**（`SPEC.md:94`・`SPEC.md:647`。`SPEC.md` は `governance-check.mjs:1342` の `governanceDocs` 母集団に入る規範文書であり、`.superpowers/` ではない）。全称否定を残すと、次に射程規則を使う者が同じ誤りへ導かれる（`AGENTS.md`「検証の作法」の全称否定の項）。
- **受容する残余として明記を薦める 1 行**: `SPEC.md:647`（as-built）は「被覆判定」のまま、実装側 `//!` は「カバー」になった。両者の共通の錨は `font_covers_cjk` である。**`SPEC.md` の語を書き換えることは薦めない**——`plan.md:46` が「SPEC.md の更新は不要（挙動を変えない）」と境界を引いており、語の入れ替えはこの issue が買った価値ではない。

---

## Medium

### M2. `recent_history` の頻度について、同一 crate の 2 つのコメントが正面から矛盾している（差分外・同一パターン）

- **場所**: `snotra-core/src/search.rs:333`（この差分が触った段落の直下）vs `snotra-core/tests/path_query_cost.rs:231`
- **壊れた不変条件**: 「頻度を書くなら呼び出し元を名指しする」——`search.rs:335-336` が「この一行が『全件走査が毎回の窓表示に乗る』という誤読を**2 度**招いた」と自ら記録している、その誤読を **3 度目に招く記述が同じ crate に生きている**。
- **一次証拠**:

```
search.rs:333          /// **窓を開くたび・クエリを消すたびには走らない。**
path_query_cost.rs:231 /// 空クエリ（窓を開いた瞬間・クエリ消去時）に走る `recent_history` の実コスト。
```

どちらが正しいかを実装で接地した: `src-tauri/src/egui_shell/launcher_controller.rs:765-767` が空クエリで `set_results(Vec::new())` して `return` する（`QueryIntent::Plain` 腕）。`recent_history` を呼ぶのは `launcher_controller.rs:833`（`QueryIntent::Command` かつ `SlashCmd::History`）と `platform/tray.rs:34` の 2 経路だけである（`rg -n "recent_history"` で全件確認）。**`search.rs:333` が正しく、`path_query_cost.rs:231` が腐っている。**

- **なぜ報告するか**: 差分の行範囲外だが、`AGENTS.md`「開発ワークフロー」の項 3 の「バグ発見時は同一パターン全コードパス検索を行う」に当たる。この差分は「到達可能性の写しが腐る」型を条項化した PR であり、その型の生きた実例が**同じ関数の測定コメント側に**残っている。
- **修正例**: `path_query_cost.rs:231` を頻度の主張から外す（例: 「`recent_history` の実コスト（呼ばれるのは `/r` とトレイの履歴メニュー。頻度の正本は `SearchEngine::recent_history` の doc）」）。`tests/` からの intra-doc link が着地しないなら素のバッククォートで名を書く（1a の 2 段目のとおり）。**採用するなら `plan.md:39-44` の変更ファイル表と「計 12 ファイル（新規 1・変更 11）」の件数も同じ差分で直す**——件数は記録の中の全称であり、H2 が指摘したのと同じ型で嘘になる。

---

## Low

### L1. `code-reviewer.md:28` の「同「日本語の折返し」」は、この差分が自分で禁じた略記であり、どの検査も見ない

- **場所**: `.claude/agents/code-reviewer.md:28`
- **壊れた不変条件**: `plan.md:132` が根拠つきで置いた規範——「**参照を「同「…」」と略さない**。`G-heading-refs` の `HEADING_REF` はバッククォート付きの `*.md` 対象が `「` の直前に在ることを要求する。「同」で略すと**検査の視界から消える**」。
- **一次証拠**（`governance-check.mjs:1174` の `HEADING_REF` と `:1284` の `NEAR_REF` を実データへ当てた結果）:

```
=== .claude/agents/code-reviewer.md
  CHECKED L28 target=docs/comment-guidelines.md label=「第一原則」 -> lands on: ["第一原則:コメントはなぜを書く"]
  UNCHECKED(同 abbrev) L28 label=「日本語の折返し」
```

`NEAR_REF`（gap ≤ 8 の近傍形）も発火しない（閉じバッククォートから `「日本語の折返し」` までの gap が 8 字を大きく超えるため）。ゆえに `G-heading-refs` も `G-near-heading-refs` も見ていない。

- **再確立地点はある（だから Low）**: `.claude/rules/comments.md:17` の同じ節への参照は**検査されている**（観点 3 の表）。節名を改題すれば `G-heading-refs` は rules 側で赤くなるので、改題が沈黙で通ることはない。残余は「rules 側を直した人が agents 側を直し忘れる」経路だけである。
- **修正例**（`.claude/agents/` は `AREA_BUDGET` の母集団外なので面積の代償が無い・`plan.md:141`）:

```diff
-**この型と、コードスパンを行またぎさせる折返し（同「日本語の折返し」）は、
+**この型と、コードスパンを行またぎさせる折返し（`docs/comment-guidelines.md`「日本語の折返し」）は、
```

### L3. `font_stack.rs:7` の列挙は「なぜ列挙するか」を持たないので、次の読者が 1a の 3 段目で分類できない

- **場所**: `src-tauri/src/egui_shell/font_stack.rs:7`
- **状況**: 数は落とした（`plan.md:168` の意図どおり）。列挙 3 本と issue 参照は残っている。行末に「判定は `font_covers_cjk`」という正本ポインタは在るが、**「この列挙が無いと何が誤読されるか」は書かれていない**。1a の 3 段目は「列挙が『正本の代わり』なら書かない／『それが無いと誤読される』ための証拠なら残す。**名指しの根拠が隣に無いものは前者**」と判定するので、将来の読者はこの列挙を毎回自分で分類し直すことになる（`search.rs:335` と `proof.rs:24` にはその根拠が隣に在り、だから分類できる）。
- **意図的な保持を戻せとは言っていない**——薦めるのは削除ではなく**1 句の追記**。例: 「（列挙を残すのは、`insert` の index 0 という不変条件が枝ごとに同じであることを 1 目で見せるためである）」。

### L4. `plan.md` 内部の不整合（H1 の写し）

`plan.md:61` は「次の **3 段**へ置き換える」、`plan.md:171` は「1a の **4 段目**が許す」。H1 を直すときに同時に揃える。

---

## 観点 2 — 逆向きの監査（消えた行 → 不変条件 → 再確立地点）

道具: `git diff HEAD`（消えた行の逐語）＋ `git log -S`（その行が入ったコミットと意図）＋ 現行コードの実測（構築点・呼び出し元の実数）。

| # | 消えた行（要点） | その行が運んでいた不変条件 | 再確立地点 | 判定 |
|---|---|---|---|---|
| 1 | `proof.rs:24` 「構築点は **2 つだけ**である: …」／「**3 つ目**を足すときは」 | **網羅性**（private フィールド + `PhantomData` による健全性は「crate 内の構築点を全部見た」に依存する）＋ 追加時の証明義務 | `proof.rs:24` 現行「構築点は**ここに挙げるものに限る**」＋「**足すときは**、その経路が本当にイベントループ上かを一次証拠で示すこと」 | **再確立済み・強度同等**。実測で構築点は今も 2 つだけ（`EventLoopProof::new()` は `proof.rs:54` = `on_event_loop` と `runtime.rs:443` = `RuntimeFrame` リテラルのみ・`rg -n "EventLoopProof"` 全件）。導入コミット `4c5892a`（#881）の原文と照合し、落ちた主張が無いことを確認 |
| 2 | `search.rs:331` 「呼び出し元は **2 つ**で、**どちらも**明示の操作である」 | **全称**（すべての呼び出し元がユーザーの明示操作＝全件走査がホットパスに乗らない根拠） | `search.rs:331` 現行「呼び出し元は**いずれも**明示の操作である」 | **再確立済み・強度は上がった**（数に依存しない全称になった）。実測で製品経路は `launcher_controller.rs:833`（`/r`）と `tray.rs:34`（トレイ）の 2 件のみ |
| 3 | `build.rs:83` 「通る経路は **2 つ**（`new_from_tree` と、`new_with_cached_masks` の v3 フォールバック腕）」 | **閉じた集合**（直後の「規則を 2 つに増やすな」の前提） | `build.rs:83` 現行「通るのは [`SearchEngine::new_from_tree`] と、[`SearchEngine::new_with_cached_masks`] の v3 フォールバック腕**である**」 | **再確立済み**。「通るのは A と B である」は閉じた集合の主張で強度同等。さらに **intra-doc link 化で機械検査下に入った**（`Engine::new_from_tree`（`engine.rs:117`）と同名衝突があるが `SearchEngine::` で曖昧性が解けている）。実測で呼び出し元は `build.rs:385` と `build.rs:482` の 2 件のみ |
| 4 | `font_stack.rs:7` 「フォント登録は **3 枝**（#532 SU4 の **2 枝**に #689 が 1 枝を足した）」 | **網羅性**（登録経路を全部挙げた）＋ 履歴 | 現行「フォント登録の枝は**次のとおり**（#532 SU4 の形に #689 が 1 枝を足した）」＋ 行末の「判定は `font_covers_cjk`」 | **再確立済み（網羅性）／L3 の指摘つき**。「次のとおり」は閉じた列挙。ただし**列挙する理由**は再確立されていない（→ L3） |
| 5 | `indexer.rs:811` 「起動時に `SearchEngine::new_with_cached_masks()` がマスク再計算をスキップできるようにする」 | 「ここで計算する**目的**は下流の再計算回避」＋ 消費者の名指し | 現行 `indexer.rs:811`「受け取った側が再計算せずに索引の表現へそのまま使うためである（受け取る経路の正本は `LoadOrScanResult::cached_masks` の doc）」→ 実体は `indexer.rs:336-341` の field doc | **再確立済み・内容は訂正された**。field doc は「**キャッシュヒットに限らない**——cache-miss の枝も…載せる（反復 11）」「**`None` になる場合を数え上げてはならない**」と、消えた行より**広く正しい**。ポインタが 2 段になるが着地する |
| 6 | `docs/comment-guidelines.md:21` 「**数えず列挙せず、正本をリンクで指す**——`[Type::method]` なら `cargo doc` の intra-doc link 検査が着地を確かめるが、**散文の数え上げは誰も検査しない**（腐りやすい形が、ちょうど機械化されていない形と一致する）」（**逐語の引用だが、表を壊さないため `[Type::method]` の入れ子バッククォートを平らにしてある**） | (a) 数えない (b) 列挙しない (c) リンクで指す (d) 数え上げは無検査＝腐りやすい形と機械化されていない形の一致 | (a)(d) → `:22`（1 段目、丸括弧の一文ごと移設）／(c) → `:23`（2 段目・`///` `//!` 限定の補足つきで**強化**）／(b) → `:24`（3 段目） | **(a)(c)(d) は再確立済み。(b) だけ意図的に弱められた**——「列挙しない」が無条件禁止から「正本の代わりなら禁止／誤読の予防なら可」の条件付きへ緩んだ。これは D3（`plan.md:25`・ユーザー裁定「最小の介入」）の帰結であり**意図どおり**。**この差分で唯一、消えた不変条件が満額で再確立されていない箇所**なので明示する。緩和の代償は「分類が読者の判断に依る」ことで、L3 がその代償の実例である |
| 7 | `results_window.rs:216-217` / `strings.rs:48-49` / `icon.rs:754-755` / `style.rs:102-103` / `proof.rs:43-44` の物理改行 | なし（改行位置は主張を運ばない） | — | **主張の消失なし**を機械で確認。空白とコメント記号を除いた本文が HEAD と**バイト一致**（`git show HEAD:<f>` と作業ツリーを比較）: `results_window.rs` / `strings.rs` / `icon.rs` / `style.rs` の 4 件は `IDENTICAL`。`proof.rs` と `font_stack.rs` は #1・#4 の文言変更ぶんだけ差分（当該箇所以外は一致）。`plan.md:155`「文言は変えない」を満たす |
| 8 | `plan.md:218` の連結崩れ（D4 の行末「**→ 承認待ち**」の直後に D3 本文が改行なしで混入していた） | なし（記録の可読性） | `plan.md:219-220` で D4 と D3 の 2 行へ分離 | **改善**。「→ 承認待ち」の残骸も除去されている（D4 は承認済み） |

**受け入れ条件 5 の再測**: 分断されていた 5 つのコードスパンは、いま全件 `grep` で引ける（各 1 件ヒット）。

```
proof.rs:1           current_thread().id() == context.main_thread_id
results_window.rs:1  expected ResultsScale, found MainScale
strings.rs:1         t("search.placeholder.folder", { dir: fs.currentDir })
icon.rs:1            cargo test -p snotra --release icon_pipeline_cost_probe -- --ignored --nocapture
style.rs:1           ScrollArea::vertical().auto_shrink([false,false]).scroll_source(drag:false)
```

---

## 観点 1 — 条項と実装の整合（前向き）

| 検査 | 結果 |
|---|---|
| 新節「日本語の折返し」自身と他の条項本文がコードスパンを行またぎさせていないか | **満たす**。`docs/comment-guidelines.md` / `.claude/rules/comments.md` / `.claude/agents/code-reviewer.md` の 3 枚を fence 外で走査し、**バッククォート奇数個の行は 0 件**（＝行をまたぐスパンなし） |
| `.rs` 側に行またぎスパンが残っていないか | **0 件**（自作の検知器で全 `.rs` を走査。唯一のヒット `icon.rs:414 -> 417` はコードフェンスの偽陽性・⚠️ 3 参照） |
| 訳語条項の射程規則が、実際に行った修正と整合しているか | **整合しない → H2**。「被覆」→「カバー」は条項の定着テストでは決められない（`SPEC.md` に 5 箇所ある）。なお「畳み込み」を直さなかった判断は**正しい**（`snotra-core/CLAUDE.md:32`・`:100` が規範として使用＝定着語） |
| 数え上げ条項が禁じる型が、新しく書いた文に混入していないか | **混入 1 件 → H1**（`docs/comment-guidelines.md:21` の「次の 3 段」）。`.rs` 側の新規文には混入なし（`indexer.rs:811` の新文は数を持たず、`font_stack.rs:7` の「1 枝を足した」は履歴節・⚠️ 1） |
| 「窓」を「ウィンドウ」へ寄せ直していないか（射程規則の主眼） | **寄せ直していない**。差分に `窓` → `ウィンドウ` の置換は 1 件も無い。射程規則の例示も実データと一致（`src-tauri/CLAUDE.md`・`snotra-egui-runtime/CLAUDE.md` はどちらも「窓」優勢） |
| doc コメントへ足した intra-doc link が意図した相手へ着地するか | **着地する**。`SearchEngine::new_from_tree` は `build.rs:383`（`engine.rs:117` の同名 `Engine::new_from_tree` と正しく区別）、`SearchEngine::new_with_cached_masks` は `build.rs:413`、`crate::RuntimeFrame` は `lib.rs:18` の `pub use`、`on_event_loop` は同モジュール `proof.rs` の `pub fn` |
| `//` インラインへリンク形を書いていないか（1a 2 段目の新条項） | **満たす**。`indexer.rs:811` は素のバッククォート。条項がこの理由を明記している点も整合 |

---

## 観点 3 — 参照の着地（自分で確かめた）

`governance-check.mjs:1174` の `HEADING_REF`・`:1284` の `NEAR_REF`・`:1185` の `normAnchor`・`:1237` の前方一致を実データへ当てた結果（見出しは対象ファイルから実収集）:

| 参照元 | ラベル | 検査対象か | 着地した見出し（前方一致の候補**全件**） |
|---|---|---|---|
| `.claude/rules/comments.md:15` | 「第一原則」 | CHECKED | `第一原則: コメントは「なぜ」を書く`（**候補 1 件のみ**） |
| `.claude/rules/comments.md:16` | 「rustdoc の様式」 | CHECKED | `rustdoc の様式`（1 件・`TSDoc の様式` とは前方一致しない） |
| `.claude/rules/comments.md:17` | 「日本語の折返し」 | CHECKED | `日本語の折返し`（1 件） |
| `.claude/rules/comments.md:18` | 「言語（日英）」 | CHECKED | `言語（日英）`（1 件） |
| `.claude/rules/comments.md:22` | 「変更後の検証チェックリスト」 | CHECKED | `変更後の検証チェックリスト（必須・スキップ不可）`（1 件） |
| `.claude/agents/code-reviewer.md:28` | 「第一原則」 | CHECKED | 同上（1 件） |
| `.claude/agents/code-reviewer.md:28` | 同「日本語の折返し」 | **どちらの検査も見ない** | — → **L1** |

**「意図と違う見出しに着地していないか」への答え**: 前方一致の候補が**すべて 1 件**だったので、緑は偶然ではない。`「第一原則」` が入れ子の `「」` を持つ見出しへ前方一致で届くという `plan.md:132` の設計も実データで確認した。

`.claude/rules/comments.md` の型が既存 router と同じであることも確認: frontmatter は `paths:` に crate 名始まりの 4 glob（既存 3 枚と同型・bare `**/*.rs` を使っていない）、本文に「要約を置かない」の宣言あり、規範本文の写しは 0。`G-area-budget` は 11134/12000 字（`plan.md:134` の予測と一致）。

---

## ⚠️ 確信の持てない所見（分類から外れたもの・空にしない節）

1. **`font_stack.rs:7` が残した「#689 が 1 枝を足した」は、1a の 1 段目の違反か履歴か**——判定できなかった。「歴史メモの様式」（issue 番号つきの経緯）としては正しい形だが、1 段目は「数は書かない」を無条件に書いており、「1 枝足した」は現在の枝数を差分で語る形でもある。**この差分の他の判断（数を落とす）と並べると、残した理由が読み取れない。** 条項側に「過去の変化量は歴史であり対象外」と 1 句あれば決まる。曖昧さの指摘に留め、修正は薦めない。
2. **`plan.md:87` の「窓」の実測値が再現しない**——`src-tauri/CLAUDE.md` は「43 対 15」と記録されているが、`rg -c`（行数）では **27 対 13**、`snotra-egui-runtime/CLAUDE.md` は「13 対 2」に対し **8 対 2**。出現数と行数の数え方の違いで説明が付き、**結論（「窓」が定着している）は変わらない**。実測値に数え方が添えられていないだけの問題（「歴史メモの様式」の「実測値には条件を添える」）。低優先。
3. **自作の行またぎ検知器はコードフェンスを偽陽性にする**——`src-tauri/src/icon.rs:414 -> 417` の 1 件がそれで、実体は doc コメント内の triple-backtick text フェンス（分断されたスパンではない）。`plan.md:157` の scratchpad 版はフェンスを除いていると書かれており、その 0 件と私の結果は矛盾しない。**別の検知器を書く人が同じ偽陽性を踏むので記録に残す。**
4. **`indexer.rs:811` のポインタが 2 段になっている**——`indexer.rs:811` → `LoadOrScanResult::cached_masks` の doc → そこがさらに「正本は `load_cache_in` の分岐と `save_cache_sorted` の返り値」と指す。着地はするが、1a の「正本を指す」が何段までを許すかは条項が言っていない。実害は見つけられなかった。
5. **`search.rs:331` の「呼び出し元」が 1 段の間接を挟む点**——名指しの `/r` とトレイは `Engine::recent_history`（`engine.rs:167`）経由で `SearchEngine::recent_history` を呼ぶ。doc は `SearchEngine` 側に在るので厳密には直接の呼び出し元は `engine.rs:169` の 1 箇所である。**誤読の予防という目的には終端の呼び出し元を書くのが正しい**と考えて所見に落としたが、字面の厳密さを重んじる読者は指摘するかもしれない。
6. **`.claude/rules/comments.md` に既存 router 3 枚が持つ「位置はファイル名で断定せず見出し名・シンボル名で grep して辿る（#588）」の 1 文が無い**——新 rule はファイル位置を指さないので不要と判断したが、**折返し条項の根拠 1 がまさにこの #588 の規範に依っている**ので、router 側で 1 度言い直す価値があるかもしれない（面積の余裕は 866 字）。確信なし。
7. **配送の実測のうち `snotra-settings` の 1 本は孫エージェントの報告**（`plan.md:135` が自ら明記）。私はこれを再測していない（新鮮な context を要し、重複排除で現セッションからは測れない）。**受け入れ条件 1 の 4 glob のうち 1 本だけ一次証拠が主エージェントの手に無い**という状態は残っている。

---

## Phase 2 の定型項目（該当のみ）

- **2a 不変条件照合**: `plan.md:185-190` の 6 件を照合。「既存 router の配送を消さない」＝`plan.md:135` の実測で満たす（⚠️ 7 の限定つき）。「面積上限を上げない」＝`AREA_BUDGET` 未変更・11134/12000 で満たす。「新 rule に要約を置かない」＝満たす。「既存節名を変えない」＝`git diff` に見出し行の変更なし・足したのは `## 日本語の折返し` 1 本のみ。「節名を `## 日本語の折返し` で固定」＝rules 側の参照が検査下に在り満たす（agents 側は L1）。「新 crate 追加時の取りこぼし」＝受容する残余として記載どおり。
- **2b 対称ペア**: 非該当（生成/破棄・show/hide・フラグ真偽のいずれも触っていない）。
- **2c DRY / 関数カバレッジ**: 非該当（関数・型の新規定義 0 件）。
- **2d リソースライフサイクル**: 非該当（プロセス・ハンドル・フラグを増やしていない）。
- **2e SPEC.md 同期**: **更新不要で正しい**。挙動・状態遷移・永続形式・スコア式・設定キーのいずれも変えていない（差分は全てコメントと規範文書）。ただし `SPEC.md:647` との**語の乖離**が生じた点は H2 の残余として記載した。
- **2f 「変更不要」判断の再評価**: `plan.md:206`「`AGENTS.md`「条件別チェック（トリガー → 参照先）」は不要（『各言語ファイルを編集 → `.claude/rules/`』が新 rule を覆う）」を再評価。ケース A: `.rs` を編集する者 → rules が自動配送されるので届く（実測済み）→ OK。ケース B: `AGENTS.md` だけを読む者（rules が配送されない文書作業）→ `AGENTS.md`「ドキュメント参照」の 16 行目（コメント規約の行）が既に正本を指す。**その行の括弧内の要約（rustdoc / TSDoc の様式・粒度・定型ラベル）に新節 2 つ（折返し・訳語）は含まれないが、括弧は例示であり全称ではない**（`plan.md:270` の降格判断を追認）→ OK。ケース C: `code-reviewer` 以外のレビュー系サブエージェント（例 `/simplify` の枠）→ **届かない**が、これは D4 の合意範囲外（本 issue の射程外）。**結論: 変更不要を確認。**
- **Phase 3 パフォーマンス**: 非該当（実行されるコードを 1 行も変えていない）。

---

## まとめ（対応の優先順）

1. **H1** — `docs/comment-guidelines.md:21` の「次の 3 段」→「次のとおり」（＋ `plan.md:61`）。1 語。
2. **H2** — 射程規則に tie-breaker 1 文（`docs/comment-guidelines.md:100`）＋ `plan.md:87`・`plan.md:169` の反証済み全称否定を訂正 ＋ 残余（`SPEC.md:647` は「被覆判定」のまま）を 1 行記録。**`.rs` と `SPEC.md` は触らない。**
3. **M2** — `snotra-core/tests/path_query_cost.rs:231` の頻度の主張を外す（差分外だが同一パターン）。
4. **L1 / L3 / L4** — 略記の展開、列挙の理由 1 句、plan の内部不整合。

**`AGENTS.md`「条件別チェック」の「レビュー指摘へ修正（fix-forward）を当てた」行に注意**: 上の修正は `docs/comment-guidelines.md` の条項本文そのものに当たるので、当てた後に**この差分自身へもう一度**「新しく書いた文に数え上げが混入していないか」を掛けること（H1 はまさにその型の再発である）。
