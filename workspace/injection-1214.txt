# #1214 Phase 2 — フォールトインジェクション（2026-09-02）
# 実物 makeSnapshot: files 541 件。追跡ファイルは 1 バイトも変更していない。

## 対照 — 変異なし
  snotra-core: 0 件 （緑・掃除の費用 0 件）
  snotra-egui-runtime: 0 件 （緑・掃除の費用 0 件）
  src-tauri: 0 件 （緑・掃除の費用 0 件）
  snotra-settings: 0 件 （緑・掃除の費用 0 件）
  G-module-linkage: 0 件

削った行: - `style.rs` — デザイントークンと共有スタイルヘルパー（責務は `//!`）

[変異 1 — 索引行だけを削る（強化された側）]
  crate=snotra-settings 監視対象=style.rs
  ベースライン 0 件 → 変異後 1 件
  判定: 赤
    snotra-settings/CLAUDE.md:1 実ファイル snotra-settings/src/style.rs が索引（「モジュール構成」節のバッククォート）に見当たらない

削った行: - `autostart.rs` — Windows のログオン時自動起動（`HKCU\...\Run`）の登録・解除・状態取得（責務は `//!`）。**状態

[変異 1′ — 索引行だけを削る（残る死角・#1210 の形）]
  crate=snotra-core 監視対象=autostart.rs
  ベースライン 0 件 → 変異後 0 件
  判定: ★沈黙

[変異 2 — 索引行は残し `mod` 宣言だけ削る]
  snotra-core/src/lib.rs から autostart の mod 宣言を削除: 実施
  G-module-linkage: ★沈黙（0 件）
  G-module-index:   0 件（担当外なので 0 件が正しい）

削った行: - `tabs/` — 7タブの UI 実装（`mod.rs` + `common.rs` / `general.rs` / `search.rs` / `index.rs` / 

[変異 3 — ディレクトリ集約行（`tabs/`）を削る]
  判定: 赤（8 件）
    実ファイル snotra-settings/src/tabs/backup.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-settings/src/tabs/common.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-settings/src/tabs/general.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-settings/src/tabs/index.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-settings/src/tabs/instant.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-settings/src/tabs/opener.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-settings/src/tabs/search.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-settings/src/tabs/visual.rs が索引（「モジュール構成」節のバッククォート）に見当たらない

削った行: - `raster.rs`: egui Meshを CPU 側でラスタライズする純関数群（`renderer.rs`が消費）

[変異 4 — 1 行複数の索引行に言及されるファイルの、自分の索引行を削る]
  crate=snotra-egui-runtime 監視対象=raster.rs
  ベースライン 0 件 → 変異後 0 件
  判定: ★沈黙

削った行: - `renderer.rs`: softbuffer Surface初期化・`raster.rs`によるCPUラスタ・present

[変異 4′ — 対照: 他行から言及されない索引行を削る]
  crate=snotra-egui-runtime 監視対象=renderer.rs
  ベースライン 0 件 → 変異後 0 件
  判定: ★沈黙

# #1214 Phase 2（続き）— 変異 2 / 4′ の測り直し

[変異 2 — 索引行は残し `mod autostart;` を削る]
  変異: 実施（33 → 32 行）
  G-module-linkage: ベースライン 0 件 → 変異後 2 件 / 判定: 赤
    snotra-core/src/autostart/tests.rs:1 crate ルートから mod 宣言で到達できない（mod 宣言の書き忘れ。cargo も rust-analyzer も報せない）
    snotra-core/src/autostart.rs:1 crate ルートから mod 宣言で到達できない（mod 宣言の書き忘れ。cargo も rust-analyzer も報せない）
  G-module-index:   0 件（担当外なので 0 件が正しい）
  → 赤の理由は検査の assert（到達性の探索結果）であって、実行時エラーではない

[変異 4′ — 対照: 節の中で 1 回しか現れない索引行を削る]
  候補 9 件: lib.rs, env.rs, input.rs, ime.rs, windows_ime.rs, monitor.rs, proof.rs, repaint.rs, surface.rs
  lib.rs: 0 → 1 件 / 判定: 赤
  env.rs: 0 → 1 件 / 判定: 赤
  input.rs: 0 → 1 件 / 判定: 赤

[変異 4″ — 相互に言及し合う 2 行を**両方**削る]
  削った行数: 2 / 判定: 赤（2 件）
    実ファイル snotra-egui-runtime/src/raster.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
    実ファイル snotra-egui-runtime/src/renderer.rs が索引（「モジュール構成」節のバッククォート）に見当たらない
  → 片方だけでは互いの言及に救われるが、両方消えれば赤になる（死角は「相互参照が残る間」に限る）
