# 敵対的読者レビュー（手を抜く読者）— round 1

- **抜け道**: item 3 の母集団は「4a が名指しする check スキル」だが、4a 自身が SSOT（AGENTS.md 表）を「二重管理を避けるためここに再掲しない」と言いながら 6 個の固有名を直後の括弧で再掲している。読者はこの括弧列挙を population の実体として固定でき、AGENTS.md 表に将来スキルが増えても（例: `/plan-review`「Step 2b」）item 3 の報告義務には現れない。
  引用: `.claude/skills/implement/SKILL.md:104`「変更の種類に応じた check スキル（`/symmetric-check`・`/dry-check`・`/race-check`・`/cache-check`・`/persistence-check`・`/state-check`）を...実行する（トリガー→検査の写像の SSOT はその表。二重管理を避けるためここに再掲しない）」／`SKILL.md:128`「母集団は**「4a. check スキルの実行」が名指しする check スキル**と、4b の `code-reviewer`」
  **合格条件との対応**: 6

- **抜け道**: `SKILL.md` 自身がカテゴリ F の発火条件を「ソースファイルを追加/削除した場合」と狭く再定義しており、これは真の SSOT（`docs/build-commands.md` カテゴリ F 見出し＝ガバナンス文書を変更したら常に発火）より狭い。`.claude/skills/*.md` を編集しただけ（追加/削除なし）のタスクでは、この狭い再定義に従って F を「該当なし」にできる。
  引用: `SKILL.md:91`「**ソースファイルを追加/削除した場合、カテゴリ F（`npm run governance:check`）も実行する**」／`docs/build-commands.md:89`「### F. ガバナンス文書（`*.md`・`.claude/rules/`・`.claude/skills/`・workflow）を変更した場合」
  **合格条件との対応**: 新規（1 とも関連——check 対象を狭い再定義で回避）

- **抜け道**: 「実行不能」は「カテゴリ D 等」としか書かれておらず、「実行できるカテゴリをここへ入れてはならない」の禁止理由は「対話入力を要する」ことだけである。非 Windows 環境で作業しているエージェントは、`docs/build-commands.md`「Windows のみ実行可能」節の cargo test 群やカテゴリ C の smoke スクリプトを「プラットフォーム上実行不能」として同じ枠に押し込め、D 以外への拡張を「等」の一語で正当化できる。
  引用: `SKILL.md:127`「**該当するが実行不能**（カテゴリ D 等。人間へ引き渡す目視項目を列挙する——実行できるカテゴリをここへ入れてはならない）」
  **合格条件との対応**: 3

- **抜け道**: 「途中で止まった場合、到達しなかった Step に対応する項目は書かない」という原則と、直後の「Step 2 以降まで進んだ場合、以下を報告」という規定が、Step 3 の 5 サイクル中止（Step 2 は完了済み＝文字どおり「Step 2 以降まで進んだ」）に対してどちらが優先するか書かれていない。読者はどちらの読みも選べる——診断サマリーのみで済ませる／逆に到達していない Step 4 の項目を「進んだ場合」の規定を盾に書く、の両方向に開いている。
  引用: `SKILL.md:123`「途中で止まった場合、到達しなかった Step に対応する項目は書かない...Step 3 の 5 サイクル中止なら診断サマリー」／`SKILL.md:125`「Step 2 以降まで進んだ場合、以下を報告」
  **合格条件との対応**: 5

- **抜け道**: item 2（検証結果）の「実行」区分だけが「その出力を添える」という証拠を要求する。item 3（check スキル・code-reviewer）の「実施」区分には証拠添付の要求が無く、「発見件数と修正の有無」を書けば足りる——実際に走らせず「0 件」とだけ書いても文言上は満たせる。`/health-check` は「結論には根拠を添える。『発見事項なし』も同じである」と明記しているが、この規律は item 3 へ輸入されていない。
  引用: `SKILL.md:127`「**実行**（その出力を添える）」対 `SKILL.md:128`「実施したものは 1 件 1 行（check スキルは発見件数と修正の有無...0 件も 0 件と書く）」／`.claude/skills/health-check/SKILL.md:51`「**結論には根拠を添える。『発見事項なし』も同じである**」
  **合格条件との対応**: 新規

- **抜け道**: item 2・item 3 のどちらでも「該当なし」判定に理由・根拠の提示を要求していない（「名前を列挙」「1 行にまとめて」とあるだけ）。実際には該当するトリガーを、AGENTS.md 表と照合せずに「該当なし」と分類しても、報告フォーマット上は何ら区別できない。`/health-check` の Check 7 は「`[Skipped]` が正当なのは、文書化された前提を満たせない場合に限る」と明言し正当性の歯止めを置くが、`/implement` の出力節にはこの歯止めが無い。
  引用: `SKILL.md:128`「該当なしは 1 行にまとめて名前を列挙」／`.claude/skills/health-check/SKILL.md:94`「**`[Skipped]` が正当なのは、文書化された前提を満たせない場合に限る。**」
  **合格条件との対応**: 1

- **抜け道**: 4b は Critical/High のみ修正を義務付け、item 3 も Critical/High の件数しか報告を求めない。code-reviewer が Medium/Low の指摘を多数出しても、それらは修正義務も報告義務も無く、読者は「Critical: 0, High: 0」とだけ書けば文言上完全に満たせる。
  引用: `SKILL.md:108`「Critical または High の発見事項は修正してから次に進む」／`SKILL.md:128`「`code-reviewer` は Critical / High の件数と修正の有無」
  **合格条件との対応**: 新規

- **抜け道**: item 3 は母集団を「4a の check スキル＋ code-reviewer」に閉じると宣言しつつ、それ以外を書いてはならないとは明言していない。読者は AGENTS.md 表の非スキル行（例: rules 参照・grep 指示）を「該当なし」の列に混ぜて水増しでき、固定 7 項目（6 check スキル＋ code-reviewer）の充足確認を紛れさせられる。
  引用: `SKILL.md:128`「母集団は...であり、**その全員が 1 度ずつ現れる**」（それ以外を排除する文言は無い）
  **合格条件との対応**: 4

- **抜け道**: AGENTS.md の「関数・型を新規定義／改名／導入」行は `/dry-check` の実行と「呼び出し元を grep」という手作業を同じセルで束ねているが、item 3 の母集団は名前付き check スキルだけを追跡する。`/dry-check` を「実施・0 件」と報告すれば、同じ行が要求する grep 作業自体は報告の型に存在しないため、やらなくても報告上は露見しない。
  引用: `AGENTS.md:59`「呼び出し元を grep（作ることと使うことは別判断）＋ `/dry-check`」／`SKILL.md:128`（母集団が check スキル名のみを列挙）
  **合格条件との対応**: 新規

- **抜け道**: item 2 の「追加/更新したテスト名と、それが検証した不変条件を必ず含める」に「無ければ何を書くか」の明示が無い。ガバナンス文書だけを編集するタスク（本変更自体がそう）では「テストなし」が自明に正当となり、これと「カテゴリ F 該当なしの読み替え」（上記 2 件目）を組み合わせると、AGENTS.md 自身が governance:check の実行対象と明記する変更（`AGENTS.md:63`「ガバナンス文書...を変更 → `npm run governance:check`」）について、`/implement` の出力だけを見ると一見「検証済みで問題なし」に見える報告を作れてしまう。
  引用: `SKILL.md:127`「**追加/更新したテスト名と、それが検証した不変条件を必ず含める**」／`AGENTS.md:63`「ガバナンス文書（`*.md`・スキル表・モジュール索引・rules・workflow）を変更 | `npm run governance:check`」
  **合格条件との対応**: 新規（2 件目の抜け道と合成すると影響が拡大する）
