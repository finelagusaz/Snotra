- **抜け道**: 現在の実行環境が Windows でなければ、カテゴリ A の必須コマンド（`cargo check --workspace`・`cargo clippy --workspace --all-targets -- -D warnings`・`cargo test -p snotra-core`/`snotra-egui-runtime`/`snotra`/`snotra-settings`）を丸ごと「実行不能」に分類できる。
  引用: `.claude/skills/implement/SKILL.md:127`「実行環境が Windows でないときの Windows 専用コマンド（`docs/build-commands.md`「Windows のみ実行可能（`windows` クレートや Win32 API・実行バイナリに依存）」）に限り、その理由と人間へ引き渡す項目を必ず併記する」。参照先 `docs/build-commands.md:105-116` の「Windows のみ実行可能」ブロックは、カテゴリ A（`docs/build-commands.md:14-19`、"必須"）と**一字一句同じコマンド文字列**を列挙している。ゆえに非 Windows 環境ではカテゴリ A 全体が名指しで「Windows 専用コマンド」に該当し、必須検証が正当に「実行不能」へ倒れる。
  由来: 修正2（実行不能の限定先として名指しした「Windows のみ実行可能」節が、必須節カテゴリ A と同一コマンドを含んでいたことに気づかず限定範囲を広げすぎた）。

- **抜け道**: 項目3の「未実施」は理由の中身を一切制約されていない。「時間・手間」で check スキルを未実施にできる——項目2の「実行不能」に課した禁止がここには及ばない。
  引用: `.claude/skills/implement/SKILL.md:128`「**未実施**は理由を付けて 1 件 1 行」。対する `.claude/skills/implement/SKILL.md:127`「（時間・手間を理由にここへ入れてはならない）」はカテゴリ A〜F の「実行不能」だけに掛かる文で、check スキル（`/symmetric-check` 等）や `code-reviewer` の「未実施」には掛からない。
  由来: 修正2が隣の項目2だけを塞いだことで、同じ形の逃げ道が項目3側に無傷で残った（「隣に移った抜け道」）。

- **抜け道**: 1c-A（計画が要ると判明）は Step 2〜4 の途中でも発動でき、発動すれば出力は「1c の渡し方」（issue 草案の提示のみ）で足り、項目2・3 の実行結果報告義務そのものが消える。検証が重くなった/不利な発見が出た局面で「計画が要るサインが今立った」と申告すれば、Step 3・4 の重い報告を回避できる。
  引用: `.claude/skills/implement/SKILL.md:123`「この規定は下の「Step 2 以降まで進んだ場合」より優先する——Step 2 を終えていても、止まった時点より後の Step の項目は生成物を持たない（1c の停止は Step 2〜4 の途中でも起こりうる」。判定基準の「計画が要るサイン」（`SKILL.md:59-65`）は徴候列挙であり網羅ではないため、恣意的な該当宣言を排除できない。
  由来: 修正1（1c 停止が Step 2〜4 の途中でも起こりうる旨と、出力節の優先句を明文化したことで、遅延申告による軽い停止経路が Step 内のどの地点からも開かれた）。

- **抜け道**: 項目3の母集団は「4a の列挙」（固定6スキル名）に固定され、`AGENTS.md` の表に新しい check スキルが追加されても、この SKILL.md の 4a を書き換えない限り母集団に入らない——書き換え義務は「表へ足すとき」に課されるが、実装者が表の更新を知らない/確認しないまま進めば「4a が名指すもの」だけを母集団として正しく全数報告したことになる。
  引用: `.claude/skills/implement/SKILL.md:128`「この母集団が `AGENTS.md`「条件別チェック（トリガー → 参照先）」表と一致するのは、4a の列挙が表と同期している間だけである」。4a 自体は `.claude/skills/implement/SKILL.md:104`「`AGENTS.md`「条件別チェック（トリガー → 参照先）」表に従って実行する（トリガー→検査の写像の SSOT はその表。二重管理を避けるためここに再掲しない）」と書き、動的参照のはずなのに、項目3は静的な「4a の列挙」を母集団の定義に固定しており、両者が矛盾する。
  由来: 修正3（前提条件を明記したことで、母集団が表ではなく「4a の列挙」という別の・ずれうる実体に固定されていることが可視化され、そのずれを免罪符として使える）。

- **抜け道**: 項目2・3の「1行で根拠」は行数の制約だけで内容の実証性を要求しない。「該当なし: 対象外のため」のような同語反復の1行でも文言上は満たせる——`/health-check` Check 7 が要求する「リンク先ごとの実在の可否を一覧で示す」水準の実証は求められていない。
  引用: `.claude/skills/implement/SKILL.md:131`「どの値を割り当てたときも 1 行で根拠を書く——「該当なし」も同じである」。比較対象 `.claude/skills/health-check/SKILL.md:51`「結論には根拠を添える...7a はリンク先ごとの実在の可否を...一覧で示す」は根拠の形を具体的に指定するが、implement 側にはその指定がない。
  由来: 修正4（「1行で根拠」という形式要件だけを追加し、根拠の実証性水準を定義しなかったため、同語反復での充足が残った）。

- **抜け道**: 項目2で「実行不能」を選ぶとき、修正2は「理由」と「引き渡し項目」の**両方**を併記せよと要求するが、修正4は同じ値に対して「1 行で根拠」としか求めない。字数を切り詰めたい読み手は「1 行」を根拠に、引き渡し項目を省いて理由だけの1行にできる。
  引用: `.claude/skills/implement/SKILL.md:127`「その理由と人間へ引き渡す項目を必ず併記する」／`.claude/skills/implement/SKILL.md:131`「どの値を割り当てたときも 1 行で根拠を書く」。両者は同じ「実行不能」という値に異なる分量規律（複数要素の併記 vs 1行）を課しており、後者を優先すれば前者の「必ず併記」が骨抜きになる。
  由来: 修正2と修正4の重ね書きが生んだ矛盾（どちらも単独では健全だが、同じ値に異なる分量規律を課した）。
