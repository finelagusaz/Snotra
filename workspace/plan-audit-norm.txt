plan-audit-norm — 規範側（A）の整合レビュー
対象: workspace/plan.md Phase 2（規範 4 面）と、それが触る 4 ファイル
枠: 「足そうとしている規範の文面が、既存の規範と衝突しないか・写しを増やさないか」のみ
実施: 2026-08-20 / ブランチ chore/measurement-canon-and-fold-detector / 編集は本ファイルのみ

=========================================================================
0. 実測した前提（以降の所見はすべてこの上に立つ）
=========================================================================

E1. `npm run governance:check` の evidence 行（本日実行・生出力）:
    「検査 20 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 /
      恒久規範 常時ロード 16377 字・rules 12754 字 / 見出し参照 285 件を
      md 48 件 + .rs 101 件 + スクリプトのコメント 109 件から照合 / …」
    内訳（自分で数えた）: CLAUDE.md 7157 字 + AGENTS.md 8494 字 + skill description 726 字 = 16377。

E2. 面積に合否は無い。`AREA_BUDGET` は存在しない（`grep -rn AREA_BUDGET scripts/` が 0 件）。
    docs/adr/ADR-retire-area-budget.md「決定」1 が上限比較の廃止を、2 が「実測値は成功行が
    報告し続ける」を定めている。判定は `ADR-doc-promise-over-area-ratchet` の「書く約束」だけが持つ。

E3. 面積の 2 面の母集団:
    - 常時ロード = `ALWAYS_LOADED_FILES = ["CLAUDE.md", "AGENTS.md"]`（scripts/governance/instrument.mjs:34）
      + `disable-model-invocation` でない skill の description
    - rules 面 = `RULE_FILE_RE = /^\.claude\/rules\/[^/]+\.md$/`（scripts/governance/lib.mjs:478）
    → Phase 2 の 4 面のうち **面積に算入されるのは 2-2（AGENTS.md）と 2-4（rules）だけ**。
      2-1（PERFORMANCE.md）と 2-3（docs/comment-guidelines.md）は 1 字も計上されない。

E4. PERFORMANCE.md の所属（3 経路とも実測）:
    - `governanceDocs`（lib.mjs:461-470）に **入らない**（ルート直下の列挙は CLAUDE/AGENTS/CONTRIBUTING/SPEC のみ）
    - `STALE_EXTRA_DOCS`（lib.mjs:588）に **入らない** → G-stale-identifiers の視界外
    - `adrCitationDocs`（G-adr-citations.mjs:35-45 = governanceDocs + ADR + skillTree + nonDocSources）に
      **入らない**（`nonDocSources` は `\.(rs|mjs)$` のみ・:61）→ ADR 短縮引用 `ADR-<slug>` は照合されない
    - `headingRefDocs`（lib.mjs:508-516）に **入る**。同関数の doc（lib.mjs:496-499）が
      「見出し参照はガバナンス文書の外（`PERFORMANCE.md`）にも書かれ、実際にそこで腐っていた」と名指ししている
    → **PERFORMANCE.md に機構が届く経路は正準形の見出し参照 1 本だけである。**

E5. どの `.claude/rules/*.md` の `paths` にも `PERFORMANCE.md` は無い（8 本の frontmatter を全部読んだ）。
    `governance-docs.md` の paths は AGENTS.md / CLAUDE.md / docs/adr/** / scripts/**、
    `comments.md` の paths は 4 crate の `**/*.rs` のみ。
    → **PERFORMANCE.md も docs/**（adr 以外）も、編集時に規範が配送される面ではない。**

E6. `HEADING_REF = /`([^`\n]+)`\s*(?:§\s*[\d.]*\s*)?「([^「」\n]+)」/g`（lib.mjs:168）。
    両群が `\n` を除外し、かつ `scanHeadingRefs` は `refScanLines` で**行ごと**に `matchAll` する
    （G-heading-refs.mjs:44-45）。`\s*` は改行を食いうるが行単位走査ゆえ到達しない。
    → 「1 物理行に収まったものだけ」は **2 経路で真**（正規表現と走査単位の両方）。

E7. `PERFORMANCE.md:1050` の先例は実在する:
    「計測機は **GPD WIN MINI / G1617-01 / 23.8 GB**（開発機は 2 台あるので機体を書く）。」
    → plan.md:47 の引用は正しい。

E8. 33 件の折れ地点のうち、箇条書き・表の内側にあるものは **0 件**
    （scratchpad/bullet-scan.mjs で 33 件全件を走査。継続行を 12 行遡り、コメント記号剥がし後に
      `[-*+] ` / `1. ` / `|` で始まる行が在るかを見た。出力 "total 33 bullet-or-table 0"）。

E9. 33 件のファイル種別: `.rs` 24 / `.mjs` 4 / `.ps1`+`.psm1` 3 / `.md` 2。
    → **9/33 が `docs/comment-guidelines.md` の射程外のファイルに在る**（本書の射程は冒頭
      「コードコメント（rustdoc / TSDoc / インラインコメント）」であり、配送は `.rs` のみ・E5）。

=========================================================================
1. 要対処（5 件）
=========================================================================

-------------------------------------------------------------------------
N1【要対処】A-4 の行き先が、トリガー列とも母集団の括弧書きとも噛み合っていない
-------------------------------------------------------------------------
根拠: AGENTS.md:63 の当該行は
  「調査・測定のための一時的な足場（script・workflow・env フック）を新設 | 撤去条件（どの issue が
    閉じたら消すか）と撤去対象の列挙を、**その成果物自身の doc へ書く**——…」
計画（plan.md:12, :28, :164）が足そうとしているのは「**計装を撤去する前に**、引用されうる値を
出所つきでログへ着地させる」。噛み合わない点が 2 つある。

(a) **引き金の時制が逆である。** トリガー列は「新設」と書かれており、表の使い方は
    AGENTS.md:53「変更が下表のトリガーに当たるなら」＝**局面から行を引く**である。撤去の局面に
    来た読者はこの行に辿り着かない。

(b) **母集団の括弧書きが motivating case を含まない。** A-4 の実例は
    `docs/adr/ADR-measurement-canon-in-code-doc.md`「文脈」が記録する #1032 の計器で、これは
    「`update()` を 8 区間へ切り、trace の書き込み時間を各区間から控除する」**製品コード内の計装**
    である。`script・workflow・env フック` のいずれでもない。同 ADR「文脈」が
    「計装は同 issue の完了時に撤去されている……**寄せ先の節そのものが存在しない**」と書いた事故が
    まさに A-4 の防ごうとしているものであり、その事故は今の括弧書きの外で起きた。

影響: 裁定（置き場を AGENTS.md のこの行にする）を保ったまま直せる。トリガー列を
「…一時的な足場（script・workflow・env フック・**製品コード内の計装**）を新設**または撤去**」へ広げ、
参照先セルへ A-4 の 1 文を足す形になる。**トリガー列に触れずに参照先セルへ書き足すと、
規範は在るのに読まれない状態が確定する。**

この所見を偽にする手順:
  `grep -rn "計装" AGENTS.md docs/development-principles.md` で「計装 ∈ 足場」と定義した箇所を示す、
  または AGENTS.md の他の行で「新設」トリガーから撤去時の義務を配っている先例を 1 件挙げる。

-------------------------------------------------------------------------
N2【要対処】2-4 の括弧書き「折れは G-folded-heading-refs が捕まえる」が新しい全称で、
            計画自身の死角宣言がそれを偽にする
-------------------------------------------------------------------------
根拠: 足そうとしている文面（plan.md:50）は
  「かつ 1 物理行に収まったものだけである（折れは G-folded-heading-refs が捕まえる）」。
前半は真（E6）。**後半が偽になりうる。** plan.md:131-135 が同じ計画の中で 3 つの死角を宣言している:
  - バッククォート自体が行を跨ぐ形
  - 助詞を挟んで折れた形（G-near-heading-refs の対象が折れたもの）
  - `refScanLines` が i+1 を供給しない形（形 A のみ・fail-open）
つまり「折れ」の全体を捕まえるのではなく、形 A と形 B だけを捕まえる。
この行が置かれる `.claude/rules/governance-docs.md:18` は**射程を宣言する行**であり、そこに
射程より広い主張を置くのは、この節が果たしている役割の逆である。

これは AGENTS.md「全称表現は前提条件とセットで書く」に真正面から当たり、
記憶 `universal-claim-fix-regenerates-itself`（偽の全称を直した文がまた全称で偽になる・#1091 で 3 連鎖）
が言う再生の形そのものでもある。

提案: 「（形 A・形 B の折れは `G-folded-heading-refs` が赤にする。射程と死角は同検査のヘッダが正本）」。
射程の数え上げをこの行に書かず、検査自身の doc を指す——`.claude/rules/governance-docs.md` の
「機構の実装の詳細（述語の種類・件数・分岐の列挙）を散文へ写さない」がまさにこれを要求している。

この所見を偽にする手順:
  plan.md:131-135 の 3 つの死角がすべて「折れ」でないことを示す（＝「折れ」の定義を形 A・形 B に
  限る定義文が既に規範のどこかに在ることを grep で示す）。

-------------------------------------------------------------------------
N3【要対処】2-3 の条項は写しではないが、規範の射程と検査の射程が**ファイル種別**で食い違う。
            9/33 は規範が一言も及ばない面に在る
-------------------------------------------------------------------------
まず写しかどうか: **写しではない。**
  既存 `docs/comment-guidelines.md:62` は「**とくにコードスパン（バッククォートで囲んだ識別子・
  コマンド）を行またぎさせない**」と書く。形 A（`` `<対象>` `` で行が終わり次行が `「`）も
  形 B（`「` が閉じない）も、**コードスパン自体は割れていない**。実物で確認した——
  `snotra-core/src/index_tree.rs:62` は「実測は `PERFORMANCE.md`」で行が終わり
  次行が「「採用: 表示名を文字列アリーナで持つ」」で始まる。バッククォートは閉じている。
  上位の「文途中で物理改行を入れない」は覆うが、それは条項ではなく段落規範であり、
  「正準形の指し」という**機構が読む単位**を名指ししていない。→ 真に新しい射程である。

食い違い（宣言すべき非対称）:
  - `docs/comment-guidelines.md` の射程は冒頭が定めるとおり「コードコメント（rustdoc / TSDoc /
    インラインコメント）」で、配送は `.claude/rules/comments.md` の paths ＝ 4 crate の `**/*.rs` のみ（E5）。
  - 新検査の母集団は `ctx.allRefDocs` ＝ `.md` 全域（superpowers/workspace/adr を除く）+ `.rs` 全域
    + コメント記法を持つ全ファイル（`.mjs` `.ps1` `.psm1` `.yml` `.toml` ほか。lib.mjs:565-566）。
  - 今日の 33 件のうち **9 件（`.mjs` 4 / `.ps1`+`.psm1` 3 / `.md` 2）は `.rs` ではない**（E9）。
    この 9 件は「新設される規範がどれも配送されない面」で赤になる——`comment-guidelines` は
    `.rs` にしか配送されず、`.claude/rules/governance-docs.md` の paths は AGENTS.md / CLAUDE.md /
    docs/adr/** / scripts/** で、**PERFORMANCE.md も docs/design/** も入らない**（E5）。

ブリーフの「新検査が全ファイルを常時赤にする」という懸念は、実際にはこの形ではない——
33 件を本 PR が解消するので定常状態は 0 件である。**残るのは「規範の届かない面で赤が出る」**である。

> **訂正（2026-08-20・team-lead の裏取り要求を受けて再測定）**: 初稿はここで
> 「`.ps1` には『折返しあり へ合わせる』という**逆向きの明示的先例**が在り、新検査はそこで
> 既存規範と衝突する」と書いていた。**その性格づけが誤りである。**
> 文言自体は実在する——`docs/superpowers/plans/2026-08-10-search-worker.md:17`:
> 「**適用は `.rs` である**——PowerShell（`.ps1` / `.psm1`）のコメントは既存ファイルの作法
> （折返しあり）へ合わせること」。**しかしこれは規範ではない。** `docs/superpowers/` は
> **#589 で非規範化された歴史資料**であり、`scripts/governance/lib.mjs` の 4 述語
> （:446「docs/superpowers/ は歴史資料（#589 で非規範化）ゆえ除外」・:465・:512・:607）が
> 接頭辞で母集団から落とす。加えて当該文書は #1004 の**実装計画**で、その実装者に宛てた
> 1 回限りの制約である。**次に同じ資料を引きかけたら、まずこの非規範化を思い出すこと。**

**訂正後の形——衝突ではなく空白である。**
`AGENTS.md` / ルート `CLAUDE.md` / `docs/comment-guidelines.md` / `.claude/rules/*.md` を
`ps1|psm1|PowerShell` × `コメント|折返|折り返|作法|様式` で交差検索して **0 行**——
**`.ps1` / `.psm1` にはコメント作法の生きた規範が一つも無い。新検査だけが赤にする面である。**
一方 3 件は確実に母集団へ入る（`commentFamilyOf` が `ps` を返す。先例を書いた当の `.md` は `null`）。
衝突が無いぶん実害は小さいが、**宣言の必要性はむしろ強まる**——規範が一言も無い面で機構だけが
赤を出すと、赤を受け取った者に拠り所が無い。下の提案（検査ヘッダへの 1 行）が**唯一の拠り所**になる。

提案: 新設する検査自身のヘッダ（`G-folded-heading-refs.mjs`）に
「射程はファイル種別に依らない——`comment-guidelines` が `.rs` 限定なのに対し、この検査は
`allRefDocs` 全域を見る。正準形の指しは**どの言語のコメントでも**機構の入力だからである」を置く。
新しい rules ファイルは作らない（`detector-scope-only-as-tight-as-needed`）。

この所見を偽にする手順:
  `.claude/rules/` のいずれかの paths に `PERFORMANCE.md` または `docs/**`（adr 以外）が
  含まれることを示す。または `docs/comment-guidelines.md` が `.rs` 以外へ適用されると
  明示している行を grep で示す。

-------------------------------------------------------------------------
N4【要対処】2-1 の理由文は `docs/comment-guidelines.md`「歴史メモの様式」の写しになる。
            欄の一覧は写しではない
-------------------------------------------------------------------------
根拠: 既存 `docs/comment-guidelines.md:96`
  「**実測値には条件を添える**（日付・エントリ数・試行回数など。例: 「実測 17/2000 回」
    「2026-07-17 実測」）。**条件のない数値は再検証できない**」
計画 A-1（plan.md:27）「出所欄（日付・機体・規模・版・標本数）を必須にする。
書けない測定は「記録」ではなく「主張」として扱う」。

AGENTS.md「消す/共通化する前に『片方だけが変わる将来を 1 つ挙げられるか』を問う」を当てる:
  - **欄の一覧は片方だけが変わる。** 「機体」が在るのは開発機が 2 台あるからで
    （PERFORMANCE.md:1050 が先例・E7）、3 台目が増えれば PERFORMANCE.md 側だけが動く。
    「標本数」「規模」も A/B ログ固有。→ **別概念。写しではない。**
  - 「記録」と「主張」の区別も新規（既存に対応物が無い）。
  - **「条件のない数値は再検証できない」という理由文だけが同概念である。** これを 2 か所へ
    書けば、AGENTS.md「文書に事実の写しを増やす変更 → 正本を 1 か所に定め他は参照へ」に当たる。

提案: 正本は既存の `docs/comment-guidelines.md`「歴史メモの様式」に置いたまま（先着・より一般）、
PERFORMANCE.md 側は **欄の一覧と記録/主張の区別だけ**を書き、理由は
`` `docs/comment-guidelines.md`「歴史メモの様式」 `` を正準形で指す。
**この参照は機構が検算する**——PERFORMANCE.md は `headingRefDocs` に入り（E4）、
`docs/comment-guidelines.md` の `## 歴史メモの様式` は ATX 見出しゆえアンカーとして解決される。
つまり「誰も検算しない面」に置く規範へ、唯一届く機構の綱を 1 本張ることになる（N7 の緩和にもなる）。

この所見を偽にする手順:
  A-1 の文面を実際に書き、その中に「条件のない数値は再検証できない」と同義の理由文が
  1 つも無いことを示す（＝理由を書かずに義務だけ書くなら、この所見は不発になる）。

-------------------------------------------------------------------------
N5【要対処】「新規のみ・遡及しない」が節の本文に入らないと、既存 40 件が違反として読まれ、
            次の読者が機体を捏造する
-------------------------------------------------------------------------
根拠: 裁定（plan.md:9）は「新規のみ・遡及しない（既存 40 件は触らない。3 月の測定は機体を知る
手段が無く、埋めれば捏造になる）」。しかし作業項目 2-1（plan.md:163）が節へ書くと指定しているのは
「A-1・A-2 の『分けない』理由も 1 文で」だけで、**遡及禁止の境界は ADR 側にしか行き先が無い**。
受け入れ条件（plan.md:27）も「出所欄を必須にしている」しか要求していない。

害の形は既知である。記憶 `two-dev-machines-unlabeled-in-perf-doc` が
「PERFORMANCE.md は『開発機』と単数で書き機体を記録しない」を記録しており、
出所欄を「必須」とだけ書いた節を読んだ次のエージェントは、既存の「開発機」表記を
規約違反と見なして埋めにかかる。埋められる値は 1 台ぶんの推測しかない。

提案: 節の中に**適用境界を 1 文**で書く（「本節の適用は 2026-08-20 以降に足す記録である。
既存の記録は当時の条件を復元できないため遡及しない——埋めれば捏造になる」）。
ADR 側は却下案の記録であって、PERFORMANCE.md を編集する者には配送されない（E5）。

この所見を偽にする手順:
  `.claude/rules/` か PostToolUse の `selectChecks` が PERFORMANCE.md 編集時に ADR を
  配送する経路を示す。または既存 40 件のうち機体を書いていないものが 0 件であることを実測で示す。

=========================================================================
2. 軽微（4 件）
=========================================================================

-------------------------------------------------------------------------
N6【軽微】A-1 の節と `ADR-measurement-canon-in-code-doc`「帰結」の緊張——「主張」の行き先が
          既に別 ADR で決まっているのに、節が単独で読まれるとその出口が見えない
-------------------------------------------------------------------------
根拠: `docs/adr/ADR-measurement-canon-in-code-doc.md`「帰結」:
  「**測定値の正本は `PERFORMANCE.md` に限らない。** `PERFORMANCE.md` は A/B と実測ログの置き場であり、
    そこに記録の無い値まで押し込む場所ではない。**寄せ先が無いときはコードの doc を正本にしてよい。**」
A-1 が「出所欄を書けない測定は『記録』ではなく『主張』」と定めると、読者は「では主張はどこへ書くのか」
を問う。**答えは既に在る**（コードの doc）が、その ADR は PERFORMANCE.md の編集時に配送されない。
矛盾ではないが、A-1 の節が禁止だけを持ち出口を持たない形になる。
提案: 節の末尾に出口を 1 行（正準形で ADR の見出しを指す。E4 のとおり短縮引用 `ADR-<slug>` は
PERFORMANCE.md からは照合されないので、指すなら `` `docs/adr/ADR-measurement-canon-in-code-doc.md`「帰結」 ``
の形にする。先例: `docs/build-commands.md:70` / `docs/hooks.md:81` / `.claude/skills/health-check/SKILL.md:94`）。

この所見を偽にする手順:
  A-1 の文面に「書けない値の行き先」が既に含まれることを示す。

-------------------------------------------------------------------------
N7【軽微】ADR-measurement-record-provenance に「誰も検算しない面へ規範を置いた」残余が
          作業項目として指定されていない
-------------------------------------------------------------------------
根拠: plan.md:41 が新 ADR に負わせているのは「A の裁定と却下案（`PERFORMANCE.md` 分割・遡及補完）」
だけで、**置き場そのものの残余**が入っていない。実測した残余は 3 つ（E3・E4・E5）:
  (a) 面積に算入されない（`ALWAYS_LOADED_FILES` にも `RULE_FILE_RE` にも入らない）——
      `ADR-area-metric-characters`「検討した代替案と却下理由」が module CLAUDE.md について
      受容したのと**同じ形の抜け道**である。ここでは規範そのものを非課税面へ置くので、
      抜け道の性質は当時より強い
  (b) `G-stale-identifiers` の視界外（`STALE_EXTRA_DOCS` に無い）——節が名指す識別子は腐っても沈黙する
  (c) 編集時にどの rules も配送されない——規範が働く唯一の機構は「編集する当のファイルの冒頭に在る」
      という**位置による配送**である
(c) は弱点であると同時に、この置き場を選んだ最大の理由でもある（配送機構を持たない文書に対して
取りうる最強の手段）。ADR に**その両面**が書かれていれば、次に「PERFORMANCE.md を governanceDocs へ
足そう」という提案が出たとき、既に測った上で受容したことが分かる。
提案: ADR の「受容する残余」へ (a)(b)(c) を実測値つきで置く。

この所見を偽にする手順:
  plan.md の ADR 記述か受け入れ条件に置き場の残余への言及を見つける。

-------------------------------------------------------------------------
N8【軽微・⚠️】2-3 の条項と「日本語の折返し」の対象外リスト（表・箇条書き・コードフェンス）の
              整合。今日は 0 件だが、文面が但し書きを持たないと矛盾が残る
-------------------------------------------------------------------------
根拠: `docs/comment-guidelines.md:58`「表・箇条書き・コードフェンスは**構造が改行を要求するため
対象外**」。新検査には例外が無い（コードフェンスは `.md` では `refScanLines` が走査から外すが、
**rustdoc のフェンス（`///` に続く ``` 行）は外れず照合される**——lib.mjs:539-540 の
「受容する残余」が明記している。今日の影響は 0 件。`docs/comment-guidelines.md` は
コードコメントの規範なので、フェンスの衝突が起きうるのは `.rs` 側である）。
実測: 33 件の折れのうち箇条書き・表の内側に在るものは **0 件**（E8。33 件全件を走査）。
また `.md` の表行は 1 物理行なので構造的に折れない。→ 衝突は今日 0 件。
ただし `.rs` のコメント内箇条書き（`/// - …` の継続行）は将来生じうる。
提案: 2-3 の条項を「対象外リストを上書きする」形で書く（「正準形の指しだけは、上の対象外の
構造の中でも折らない」）。1 文で済み、面積は数十字。

⚠️ の理由: 今日 0 件なので「規範の穴」ではなく「文面の曖昧さ」であり、優先度の判断は
`detector-scope-only-as-tight-as-needed`（必要な分だけ縛る）と逆向きに働きうる。
書かない選択も筋が通る——その場合は検査ヘッダに「対象外リストは適用しない」と書けば足りる。

この所見を偽にする手順:
  「箇条書きは対象外」が段落内の折返しではなく箇条間の改行のみを指す、と読める既存の
  補足を `docs/comment-guidelines.md` から引く。または `.rs` のコメント箇条書きの継続行が
  `refScanLines` に供給されないことを実測で示す。

-------------------------------------------------------------------------
N9【軽微】「版」が何を指すか未定義（出所欄 5 項目のうち 1 つだけ）
-------------------------------------------------------------------------
根拠: 出所欄は「日付・機体・規模・版・標本数」（plan.md:27）。うち 4 つは PERFORMANCE.md の
既存記録に対応物が在る（:1050-1051「計測機は GPD WIN MINI / G1617-01 / 23.8 GB」「2026-08-12・
同日・同セッション・release」、:229「312,377 エントリ」、:2611「7 標本」）。
「版」だけは複数の読みがある——ビルドプロファイル（release/debug）／`INDEX_CACHE_VERSION`／
git の sha。記憶 `stale-ondisk-format-persists`（v4 が残り 36 MiB を毎起動捨てていた）は
**on-disk 版が測定を左右した実例**であり、ここで意図されているのはおそらくそれだが、
語だけでは伝わらない。1 語の例示（「版（release/debug と、on-disk 形式に依るなら
`INDEX_CACHE_VERSION`）」）で解ける。

この所見を偽にする手順:
  PERFORMANCE.md の既存記録から「版」を単独の語で書いている先例を 1 件引く。

=========================================================================
3. 未検証（3 件）
=========================================================================

-------------------------------------------------------------------------
U1【未検証】面積の増分は許容だが、判定は機構ではなく「書く約束」の意味判断である
-------------------------------------------------------------------------
測った値:
  - 今日の常時ロード = **16377 字**（E1。CLAUDE.md 7157 + AGENTS.md 8494 + description 726）
  - 2-2 が触る AGENTS.md:63 の行は現在 **205 字**。A-4 の 1 文（トリガー列の拡張 + 参照先の 1 文）は
    **+60〜130 字**と見積もる → 常時ロード面で **+0.4〜0.8%**
  - 2-4 は rules 面（今日 12754 字）へ **+40〜80 字**
  - 2-1（PERFORMANCE.md）と 2-3（docs/comment-guidelines.md）は **どちらの面にも 0 字**（E3）

判定: **許容**。理由を「書く約束」の 3 条で:
  (1) かぶりなく — **条件付きで満たす。** N4 を直せば満たす（理由文を参照へ寄せる）。直さなければ違反。
  (2) 必要なことだけ — 満たす。A-4 は推論では辿り着かない（`ADR-measurement-canon-in-code-doc`
      「文脈」が「寄せ先の節そのものが存在しない」という**実際に起きた事故**を記録しており、
      同 ADR は却下案の中で「計装を復元して測り直す」を「1 段も 2 段も大きい変更」として退けている
      ——つまり撤去後に取り返す道が実質無い）。
  (3) 古い情報を残さない — 該当なし（4 面とも純追記で、隣接する主張を偽にしない）。

未検証と書く理由: 「必要なことだけ」は本質的に意味判断で、機構は無い（E2。
`ADR-retire-area-budget`「帰結」が「漸進的な肥大を捕まえる機構は無い」と明言）。
上の判定は自分の読みであって実測ではない。**面積そのものは論点ではない**——
0.5% の増分で議論になる水準ではなく、`ADR-retire-area-budget` が退けたのは
まさに「数字と交渉する」形である。

この所見を偽にする手順:
  実際に 4 面を書いた後で `npm run governance:check` を再実行し、常時ロードが 16600 字を
  超えていたら見積もりが外れている。

-------------------------------------------------------------------------
U2【未検証・⚠️】Phase 2 で書く 4 つの文面のうち 3 つが、書いた瞬間に新検査の対象になる
-------------------------------------------------------------------------
2-1（PERFORMANCE.md）・2-3（docs/comment-guidelines.md）・2-4（.claude/rules/governance-docs.md）は
いずれも `allRefDocs` に入る。N2・N4・N6 の提案はどれも正準形の参照を新設するので、
**それ自身が折れれば新検査が赤にする**。plan.md:83-85 が「機構自身が 2 件折れている」として
1-2 に置いた規律（新設する検査のヘッダで同じ折れをしない）は Phase 1 のものだが、
**Phase 2 の 4 面にも同じ規律が要る**。とくに 2-1 と 2-4 は既存段落が折返し済みの面である
（`.claude/rules/governance-docs.md` は 1 段落 1 行だが、PERFORMANCE.md は折返しあり——
:1530 が今回の解消対象そのもの）。

⚠️ の理由: 実装前なので「実際に折る」かは測れない。Phase 2-5 の `governance:check` が捕まえるので
沈黙する経路ではないが、**Phase 2-5 は「緑を確認しコミット」としか書かれておらず、
2-1〜2-4 で新設した参照が checked に載ったか（318 → 320 等）を見る指定が無い**。
plan.md:22 の受け入れ条件は Phase 1 時点の 318 件で固定されており、Phase 2 で参照を足すと
その数が動く。**受け入れ条件と Phase 2 の成果物が数で衝突しうる。**

この所見を偽にする手順:
  Phase 2 の 4 面へ正準形の参照を 1 本も足さない文面で書き切る（N2/N4/N6 の提案を採らない場合）。
  その場合 318 は動かず衝突しない。

-------------------------------------------------------------------------
U3【未検証】射程外だが記録: plan.md:254-257 が挙げた `domains.test.mjs` の腐りは、
            A 側の規範（N3 の宣言）が在れば拾えた形ではない
-------------------------------------------------------------------------
plan.md が「機構自身の doc が 2 コミット前から偽である」と記録した件は、`.mjs` を指す参照が
`isRefTargetSpelling`（lib.mjs:171 — `.md` 末尾か `/skill-name` のみ）に当たらないために起きている。
**規範側（A）の 4 面はどれもこれを覆わない**（覆う必要も無い）。ここに書くのは、
N3 で「射程はファイル種別に依らない」と宣言するとき、**それが指すのは走査元の種別であって
参照先の綴りではない**ことを混同しない、という注意のためである。混同すると
「`.mjs` も指せる」と読める文面になり、実際には照合されないまま緑になる。

この所見を偽にする手順:
  `isRefTargetSpelling` が `.mjs` を受理することを実測で示す。

=========================================================================
4. ブリーフの問い 1〜5 への直接回答
=========================================================================

Q1（A-1 は「歴史メモの様式」の写しか）
  → **部分的に写し。** 欄の一覧（機体・規模・版・標本数）と「記録/主張」の区別は新規で写しではない
    ——「片方だけが変わる将来」が挙がる（機体は開発機の台数に依存する・E7）。
    **理由文「条件のない数値は再検証できない」だけが写しになる。** 正本は先着かつより一般の
    `docs/comment-guidelines.md`「歴史メモの様式」に置き、PERFORMANCE.md 側は正準形で指す。→ N4

Q2（2-3 は写しか / 射程の非対称）
  → **写しではない。** 既存条項が禁じるのは「コードスパンの行またぎ」で、形 A・形 B は
    コードスパンを割らない（実物で確認）。非対称は 2 つ在り、両方とも宣言が要る:
    (i) ファイル種別 — 規範は `.rs` 限定、検査は `allRefDocs` 全域。今日の 33 件のうち 9 件が
        `.rs` 以外（E9）。**`.ps1` / `.psm1` にはコメント作法の生きた規範が一つも無く
        （交差検索で 0 行）、新検査だけが赤にする面である**——衝突ではなく**空白**。
        （初稿はここを「逆向きの明示的先例が在る」と書いたが、その先例
        `docs/superpowers/plans/2026-08-10-search-worker.md:17` は #589 で非規範化された
        歴史資料であり規範ではない。訂正の全文は N3 の訂正ブロック） → N3
    (ii) 対象外リスト（箇条書き・表）— 今日 0 件（E8）だが文面上は矛盾しうる → N8
    ブリーフの「全ファイルを常時赤」は正しくない——33 件解消後の定常は 0 件である。

Q3（「1 物理行に収まったものだけである」は正しいか）
  → **正しい。2 経路で実測**（E6: 正規表現が両群で `\n` を除外・走査は行単位）。
    ブリーフが挙げた `isRefTargetSpelling` の `.mjs` 非対応は別軸で、この命題を偽にしない
    （`.mjs` を**指す**参照は見られないが、`.mjs` の**中に書かれた**正準形は見られる）。
    **偽になるのは括弧書きの後半である** → N2。

Q4（面積）
  → 今日 **常時ロード 16377 字 / rules 12754 字**。A-4 の 1 行で常時ロード **+0.4〜0.8%**。
    **合否の機構は存在しない**（`ADR-retire-area-budget` が上限判定を廃止）。
    「書く約束」で判定して **許容**——(2)(3) は満たし、(1) は N4 を直せば満たす。→ U1

Q5（誰も検算しない面へ規範を置くこと）
  → 実測した残余は 3 つ（面積非課税・stale 検査の視界外・rules 未配送。E3/E4/E5）。
    唯一届く機構は正準形の見出し参照（PERFORMANCE.md ∈ `headingRefDocs`）で、
    **N4/N6 の提案（理由と出口を正準形で外へ指す）はその綱を実際に張る**。
    計画の受容の記述は **不十分**——plan.md:10 は「`governanceDocs` へも足さない」しか書いておらず、
    それが何を失うか（3 つの残余）が言語化されていない。ADR へ書くべき。→ N7

=========================================================================
5. まとめ
=========================================================================
要対処 5 / 軽微 4 / 未検証 3。**Phase 2 の裁定（置き場を面ごとに分ける）を覆す所見は 1 件も無い。**
5 件の要対処はすべて「裁定の下で文面をどう書くか」に閉じており、最大のものは N1
（A-4 が着地する行のトリガー列と母集団が、A-4 の motivating case を含んでいない）である。
次いで N2（射程宣言の行に射程より広い全称を置こうとしている）。
