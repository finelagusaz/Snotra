# governance-refs レビュー — issue #765 plan.md

## 問題なし

- **序数参照 0 件の主張は裏付けられる**: `grep -rn "implement.*出力\|出力.*implement"` および `` `/implement`「出力」 `` の正準形 grep を `.claude/` `AGENTS.md` `CLAUDE.md` `docs/`（`docs/superpowers/` 除く）に対して実施。ヒットは `workspace/plan.md` `workspace/research.md`（母集団外）のみで、`docs/superpowers/specs/2026-07-27-opus5-skill-tuning-design.md:81` の「項目 4」参照も母集団外（同ディレクトリは G3/G4/G11 とも除外対象）。外部からの序数参照は確認できた範囲で 0 件。
- **G10 の前提（skills 本文は面積に算入されない）は実装で確認済み**: `scripts/governance-check.mjs:631-652` の `skillDescriptionArea()` は `text.match(/^description:[ \t]*(.*)$/m)` で `description:` 1 行だけを抽出し、本文は一切読まない。`ALWAYS_LOADED_FILES = ["CLAUDE.md", "AGENTS.md"]`（:542）にも skill body は含まれない。計画が `description` を触らない前提（不変条件 6）は正しい。実測ベースライン: `npm run governance:check` → `常時ロード 13274/13374 字`（余裕 100 字。ただし plan は `description`/`CLAUDE.md`/`AGENTS.md` を触らないためこの余裕には無関係に安全）。
- **G11 が新しく書く予定の参照は着地する**: `AGENTS.md`「条件別チェック（トリガー → 参照先）」= `AGENTS.md:48` の見出しと完全一致。`/health-check`「Check 7 — MEMORY.md 参照の実在性」= `.claude/skills/health-check/SKILL.md:34` と完全一致（`:85` の `[Skipped] Check 7 —...` という別見出しと前方一致で誤って衝突しない——`:34` 自体が完全一致するため）。`docs/build-commands.md` のカテゴリ A〜F は見出しが `### A. Rust ファイル...` 等であり「カテゴリ A〜F」という文字列とは前方一致しないが、research.md/plan.md の実際の書きぶり（例: plan.md:21 `` `docs/build-commands.md` のカテゴリ A〜F を母集団とする三値 ``）は `` `<対象>`「<見出し>」 `` の正準形（バッククォート直後に「」）を取っていないため G11 の走査対象（`HEADING_REF` 正規表現）にそもそも該当しない。誤検知・見落としのリスクなし。
- **`## 出力` / `### 4a.` / `### 4b.` を変えないという制約は、外部からの G11 参照を保護するのに十分**: `CLAUDE.md:74` と `docs/adr/0004-canonical-heading-references.md:28-29` が `/implement`「4b. code-reviewer エージェント」を正準形で参照しており、見出し文字列（`### 4b. code-reviewer エージェント`）を維持すれば両方とも着地し続ける。
- **`/norm-review`「Step 1」の 4 項目は Phase 2 に全て書かれている**: 合格条件 4 件（plan.md:32-35）／上限巡数「上限 2 巡」（:37）／分量予算「塞ぎ 1 件あたり 1 文まで」（:37）／上限到達時の扱い「2 巡後に残る抜け道は受容し…記録する」（:37）。4 つとも欠落なし。
- **カテゴリ F（`governance:check`）の限定の正確性**: 計画の「これが覆うのは形式であって、三値の文言が実際に義務を課すかは覆わない」という記述は、G1〜G11 の実装（すべて構文的な実在照合・面積カウント・写像照合であり、報告義務の意味論は見ていない）と整合する。正確。

## 軽微な懸念

- **`docs/build-commands.md` のカテゴリ参照は非正準形のままで良いか**: G11 に検出されないこと自体は「問題なし」だが、裏を返せば `/norm-review` の機械的検算もこの参照の妥当性を見ない。実害は無い（対象がファイル全体で見出し単位の対象化を要しないため）が、今後 `docs/build-commands.md` のカテゴリ見出しが改名されても検出されない依存であることは記録に値する。

## 要対処

- **`.claude/rules/safety-nets.md` が明示する「規範に条項を足す前にスコープ宣言を確認する」義務（`docs/development-principles.md:85`「構造的設計原則と強制の階梯」）を計画・研究のどちらも参照していない。** 実際にこれを怠ったと見える具体的な穴がある: 現行 `:123` の免除文は「**Step 1 で止まった場合**（1c-A/1c-B/1c-C）」だけを「出力」義務の対象外と宣言しており、`:125`「**Step 2 以降まで進んだ場合**」の対象範囲には Step 3 の 5 サイクル超過による中止（`:95-98`「5サイクル後もエラーが残る場合、中止して診断サマリーを書く」）も文字どおり含まれてしまう。この中止パスは Step 4 に到達しないため、計画が新設する項目 3（Step 4 レビュー結果）は**生成不可能**——現状の免除列挙（1c-A/B/C のみ）を項目名だけ更新しても、この経路は塞がれないまま残る。計画の不変条件 4「停止経路（1c-A/1c-B/1c-C）で新項目が要求されない」は自ら Step 3 中止パスを母集団から外して宣言しており、スコープ宣言をせずに範囲を狭めた状態になっている。
- **同じ理由で `safety-nets.md`「これまで無意味だった状態に意味を与える変更は、その状態に到達する全経路を列挙する」も未充足**: 三値化（実行／該当なし／実行不能）は「分類を1つ足す」変更そのものだが、Phase 2 の合格条件 4 件（plan.md:32-35）はこの Step 3 中止パスをどの分類にも割り当てる検証を含んでいない。`/norm-review` の 2 クラスの読者には Step 1 で確定した合格条件だけが渡される設計（`/norm-review`「Step 2 — 2 クラスの読者を起動する」）のため、合格条件に無いこの穴は偶然を除き検出されない。
  - **対処案**: Phase 1 の変更対象に `:95-98`（Step 3 中止時の診断サマリー）と `:123`（免除列挙）の関係を明示する一文を追加するか、少なくとも Phase 2 の合格条件へ「Step 3 で中止した場合に項目 3 が要求されないこと」を 5 件目として加え、`/norm-review` に検証させる。

## 未検証（理由）

- **`/norm-review` を実際に起動した場合の 2 クラス読者の実地結果**: 本レビューは Phase 2 未実施の計画段階の文書のみを対象にしており、`/norm-review` 自体の実行結果（成立した指摘・降格した指摘）は生成されていないため評価対象がない。上記「要対処」は `/norm-review` 実行前に停止条件（合格条件）へ反映されるべき指摘であり、実行後の結果検証ではない。
- **Phase 1 の実際の編集文言**: 計画は変更方針のみを記述しており、確定した書き換え後の文言（`:19` の従属節、`:123` の更新後の免除列挙、項目 2/3 の具体文）はまだ存在しない。G11・G10 への実害は上の「問題なし」で確認した制約（見出し不変・description 不変）が守られる限り生じないが、確定文言そのものの正準形整合は編集後に再確認が要る。
