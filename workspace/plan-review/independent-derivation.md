# 独立導出 — issue #765: `/implement`「出力」に Step 4 の受け口を作る

issue と現行コード・規範だけから独立に導出した変更集合。既存の `workspace/plan.md` / `workspace/research.md` は読んでいない（§4 に混入の記録）。

---

## 0. issue の主張の検算（先に済ませる）

issue は 2 件を主張する。**片方は既に古い。**

| issue の主張 | 検算結果 |
|---|---|
| (1) 「出力」節に Step 4（4a check スキル・4b `code-reviewer`）の結果を書く枠が無い | **成立**。`SKILL.md:125-128` の報告項目は 3 つで、Step 4 に触れる項目は無い |
| (2) 項目 2 が「check, clippy, test」と 3 種だけを名指しし、カテゴリ A〜F（6 分類）を覆っていない | **文言としては既に解消済み**。`git blame -L 127 .claude/skills/implement/SKILL.md` → `069205e`（#770, 2026-07-27 14:54）が項目 2 を書き換え、現行は「実行したカテゴリとその出力、および**実行できなかったカテゴリ（D 等）と人間へ引き渡す目視項目**（Step 3）」。`check, clippy, test` の 3 種名指しは残っていない |

**ただし (2) には同型の残余が残っている。** 現行文言の母集団は「実行したカテゴリ」＝**書き手が選んだ集合**であり、SSOT（`docs/build-commands.md` のカテゴリ A〜F）から導出されていない。「該当しなかったから実行しなかった」と「該当したのに実行しなかった」が同じ沈黙になる——これは (1) が Step 4 について指摘している欠陥そのものである。**ゆえに利用者の方針（項目 2 の母集団も同じ三値へ揃える）は、issue の literal な文言修正ではなく、この残余の除去として正当化される。** issue は SSOT だが、この 1 点については issue の記述が現行ファイルより古い。

---

## 1. 必要な変更集合

### 1-1. `.claude/skills/implement/SKILL.md`（唯一の必須変更ファイル）

| 箇所 | 変更 | 根拠 |
|---|---|---|
| `:127`（項目 2） | 母集団を `docs/build-commands.md`「変更後の検証チェックリスト」のカテゴリ A〜F に固定し、**三値**（実施 / 該当なし / 未実施）で書かせる形に書き換える。既存の「追加/更新したテスト名と、それが検証した不変条件を必ず含める」要求は**残す**（削ると #764 で足した義務が消える） | §0 の残余。母集団を書き手の裁量から外す |
| `:128` の直前 | **新項目**を挿入する — Step 4 のレビュー結果（4a の check スキル群 + 4b の `code-reviewer`）を、同じ三値で書かせる | issue (1) |
| `:128`（コミット） | 項目 3 → 項目 4 へ繰り下げ。**本文の変更は不要**（後述のとおり外部からの序数参照は 0 件） | 項目追加の従属変更 |
| `:123`（停止経路の免除文） | (i) 免除列挙に**新項目を加える**。加えないと、途中で止まった回に「Step 4 のレビュー結果」を書く義務だけが残り、生成物が無いのに報告を要求する壊れた契約になる。(ii) **免除の条件節を列挙から原理へ書き換える** — 現行は「**Step 1 で止まった場合**（1c-A / 1c-B / 1c-C）」しか名指ししていないが、**到達しない経路は 2 本ある**（下の行を参照）。「その Step に到達しなかった場合はその項目を書かない」という形にすれば、次に停止経路が増えても腐らない | `/norm-review`「免除句は名指しした対象しか守らない」「列挙ではなく原理で書けないかを先に問う」。ここは**既に列挙形**であり、列挙の外へ新項目が落ちる |
| 同上（**既存の穴の同時修正**） | **Step 3 の中止経路が免除列挙から落ちている。** `:95`「5サイクル後もエラーが残る場合、中止して診断サマリーを書く」は Step 4 にも Step 5 にも到達しない。ゆえに新項目だけでなく**現行の「コミットハッシュとメッセージ」項目も、この経路では既に充足不能**である（誰も指摘していない先行欠陥）。上の (ii) の原理化が同時にこれを塞ぐ | ADR 0006 の F1（指示と契約の食い違い）と同型。列挙で書いた免除は、列挙されなかった経路をそのまま残す |
| `:18`（`**作業中の実況は、発見があったときと方針を変えたときに限る。**`） | **従属節 1 つ**を足し、これが**作業中の実況**に掛かる制限であって「出力」節の報告義務には掛からないことを明示する | issue が名指しした衝突（「`/symmetric-check` を実行、発見なし」が述べてはならない実況になる＝正直に書いた側が罰される）。**分量は 1 文以内**（`/norm-review` Step 1-4 の塞ぎ予算。#764 で 2 巡の塞ぎが `code-reviewer` Phase 1 を 3.4 倍にした実績があるため、超えるなら受容する残余へ回す）。**列挙ではなく原理で書く**——「実況面 / 報告面」の境界を書くのであって、免除対象を列挙しない |

**変更しないこと（積極的な判断）**:

- `## 出力` / `### 4a. check スキルの実行` / `### 4b. code-reviewer エージェント` / `## Step 4 — レビュー` の**見出し文字列は 1 文字も変えない**。外部から正準形で指されており G11 の照合対象（§2-A）。
- `4a`（`:104`）の本文は変えない。写像の SSOT を `AGENTS.md`「条件別チェック（トリガー → 参照先）」表に置く構造は正しく、新項目はそこから母集団を借りるだけである。
- frontmatter の `description` は変えない（変えると G10 の常時ロード面に載る。§3-C）。

### 1-2. 三値の設計（この変更の核。ここが崩れると issue の欠陥を再生産する）

**満たすべき不変条件は 3 つある。**

1. **母集団は SSOT から導出され、書き手が選べない**
   - 項目 2 の母集団 = `docs/build-commands.md`「変更後の検証チェックリスト」の**カテゴリ A〜F**（`docs/build-commands.md:11,31,35,47,78,89` が各見出し）。
   - 新項目の母集団 = **`/implement`「4a. check スキルの実行」が名指しする check スキル**（`/symmetric-check`・`/dry-check`・`/race-check`・`/cache-check`・`/persistence-check`・`/state-check`）**＋ 4b の `code-reviewer`**。
   - **母集団が「閉じていて、報告時点で読み手が数え上げられる」ことが三値の成立条件である。** 4a が名指しする集合はこれを満たす。→ 別候補（`AGENTS.md` 条件別チェック表の全行）を採らない理由は §1-3。
2. **「該当なし」と「未実施」を同じ記号で書かせない**
   - `/health-check`「出力」の確立した規律（`.claude/skills/health-check/SKILL.md`: 「**「異常なし」と「見ていない」を同じ記号で書かない。**」「`[Skipped]` は発見事項ではない」）と同じ形にする。
   - **未実施には理由を必須にする。** 理由が要らないなら「未実施」は「該当なし」の別名になり、三値が二値へ退化する。
3. **件数を本文に書かない**
   - 「6 スキル」「カテゴリ 6 分類」のような数を SKILL.md に書くと、スキル・カテゴリの増減で**写しが腐る**。`AGENTS.md`「照合は SSOT に対して行う。派生コピー同士の一致を完全性の証拠にしない」と、G10 コメントが繰り返し警告する「写しのドリフト」に該当する（実例: #764 の敵対的読者が「数え上げの記述と実際の項目数の食い違い」を毎巡指摘している）。

4. **「該当するが実行不能」（カテゴリ D）を無限定の『未実施』へ吸収しない** — 吸収すると現行が要求する「人間へ引き渡す目視項目」の列挙が消え、`docs/build-commands.md:58` が塞いでいる同じ欠陥を作り直す。詳細と決着は §4-3。

**書式の粒度（推奨）**: 実施は 1 件 1 行（何を実行し、発見事項が何件で、修正したか）。該当なしは 1 行にまとめて列挙してよい（読む量を増やさないため）。未実施は 1 件 1 行で理由付き。

### 1-3. 母集団の候補を 1 つに絞った根拠（否定の知識）

新項目の母集団には 2 候補があった。**採らなかった方を記録する。**

- **却下: `AGENTS.md`「条件別チェック（トリガー → 参照先）」表の全行を母集団にする。** 表は 16 行あり、参照先の多くは **skill ではない**（`npm run governance:check`・`.claude/rules/safety-nets.md`・「1 行ずつ書き出してから着手」のような散文手順）。これらに対して「実施 / 該当なし / 未実施」を書かせても、それは**レビューの集合ではない**。母集団が開いた（何を数えるか書き手が決める）状態に戻り、三値の成立条件を破る。
- **採用: 4a が名指しする check スキル + 4b の `code-reviewer`。** 閉じており、報告者が今読んでいる SKILL.md の中だけで数え上げられる。

### 1-4. 変更しないが影響を検算したファイル

| ファイル | 検算 |
|---|---|
| `CLAUDE.md:74` | `` `/implement`「4b. code-reviewer エージェント」 `` を正準形で参照。**見出しを変えないので不変**。スキル表（G8）は `disable-model-invocation: true` の 3 件だけが対象で `/implement` は含まれない → 変更不要 |
| `AGENTS.md` | 条件別チェック表・検証の作法とも、報告様式には触れていない → 変更不要。ただし新項目が表を母集団の**上流**として間接的に使うため、表の行が増減しても新項目の文言は腐らない形（件数を書かない）にすること |
| `docs/build-commands.md` | 項目 2 の母集団 SSOT。**この方向（skill → docs）の参照は既に `:88`（Step 3）に在り**、新しい依存を作らない → 変更不要 |
| `.claude/agents/code-reviewer.md` | 「出力フォーマット」は Critical/High/Medium/Low の 4 段階。新項目はこの出力を要約して受けるだけなので、**エージェント側の変更は不要**。ただし新項目の文言は「Critical/High は修正済みであること」を書かせる形が 4b:108 の義務（「Critical または High の発見事項は修正してから次に進む」）と噛む |
| `docs/superpowers/specs/2026-07-27-opus5-skill-tuning-design.md:131` | 「受容する残余」に本 issue が逐語で記録されている（「`/implement`「出力」に Step 4（4a check・4b code-reviewer）の居場所が無く…」および「`/implement`「出力」の「check, clippy, test」3 種がカテゴリ 6 分類を覆わない」）。**`docs/superpowers/` は履歴資料**（G11 母集団から除外・`scripts/governance-check.mjs:813`）であり、**過去の記録として正しいまま**なので**書き換えない**。解消は本 PR/issue のクローズが表す |
| `.github/pull_request_template.md` | 「対応Issue / 変更内容」の 2 節のみ。レビュー結果の枠は無い。**今回は触らない**——PR 本文への転記を義務づけると `/implement` の外（PR 作成手順）へ責務が漏れ、issue の要求範囲を超える |

---

## 2. 間接参照の洗い出し（概念で分類）

grep した語だけでなく、**同名・別概念** と **同概念・別名** に分けて列挙する。

### A. 同概念・別名 — 正準見出し参照（G11 の照合対象。最重要）

`scripts/governance-check.mjs:715` の正準形 `` `<対象>`「<見出し>」 `` で `/implement` を指す参照を全 `*.md` から機械列挙した（`grep -rnoE '`/implement`\s*(§\s*)?「[^」]+」'`）。`workspace/` と `docs/superpowers/` は G11 母集団外（同 `:813`）。

| 参照元 | 参照ラベル | 影響 |
|---|---|---|
| `CLAUDE.md:74` | 「4b. code-reviewer エージェント」 | 見出し不変 → 影響なし |
| `docs/adr/0004-canonical-heading-references.md:28` | 「4b. code-reviewer エージェント」 | 同上。**同 ADR は「`/implement` だけは `### 4b.` 形式で『Step 4b』では着地しない」と明記**しており、`4b` 見出しの形を変える誘惑を明示的に禁じている |
| `docs/adr/0006-plan-ownership-boundary.md:17,19,37,41` | 「Step 1 — 入口判定」「1b. 分岐」「1c. 止まったときの引き渡し」「1a. workspace/plan.md の由来を確かめる」 | Step 1 系のみ。今回触らない |

**「出力」節の番号付き項目そのものへの外部参照は 0 件。** ← ここが繰り下げ安全性の根拠。

### B. 同概念・別名 — 序数参照（番号で構造を指す形）

`docs/adr/0004` と `.claude/rules/governance-docs.md` が禁じている形。**「出力」の項目を序数で指す外部参照は存在しない**（A の全件が見出しラベル参照）。SKILL.md 内部にも「上の項目 2」「下の 2〜4」のような自己序数参照は現存しない（`069205e`/`880be34` が名前参照へ移行済み）。

→ **項目 3 → 4 の繰り下げは、外にも中にも波及しない。**

### C. 見落としやすい機構的間接参照 — 番号付きリスト項目は G11 の**アンカー**である

`scripts/governance-check.mjs` の `collectAnchors`（`:720` 付近）は 3 種を参照先として収集する:

```
^#{1,6}\s+(.+?)$          ← ATX 見出し
^\s*\d+[.)]\s+(.+?)$      ← 番号付きリスト項目  ★
^\s*(?:[-*]|\d+[.)])\s+\*\*(.+?)\*\*  ← 太字リード
```

**つまり「出力」節の `1.` `2.` `3.` は、それ自体が G11 の被参照アンカーである。** 照合は正規化後の**前方一致**なので、たとえば `` `/implement`「最終検証結果」 `` は現行の項目 2 に着地する。今回それを指す参照は 0 件なので改稿は安全だが、**双方向の帰結**として:

- 項目 2 の**行頭語**（「最終検証結果」）を変えると、将来書かれる参照が壊れうる → **行頭語は保存する**のが安い。
- **新項目の行頭語は、この瞬間から公開面になる**（他文書が正準形で指せる）。安定した語を選ぶこと（例: 行頭を「レビュー結果」で始める）。

### D. 同名・別概念 — 「出力」という語の多義

| 出現 | 概念 | 今回の関係 |
|---|---|---|
| `/implement`「出力」（`:121`） | **本件の対象**。スキル終了時の報告契約 | 変更対象 |
| `/start-issue`「出力」（`start-issue/SKILL.md:132`、`/implement:44` から参照） | **引き渡し契約**（`plan.md` をレビュー済みで渡す約束）。報告様式ではない | **混同禁止**。`:44` の参照文字列を触らない |
| `/health-check`・`/retrospective`・`/norm-review`・各 check スキルの「## 出力」 | 各スキルの報告テンプレート | 変更不要。ただし **`/health-check`「出力」は三値記法の先例**であり、新項目はこれに倣うべき正本 |
| `docs/build-commands.md` の「出力」（`:27,28,58`）、`CLAUDE.md:40`「検出は exit code、出力は証拠」 | **コマンドの標準出力**（機械の出力） | 同名・完全に別概念。grep ノイズ源 |
| `docs/adr/0006:9` の「破れた出力契約」 | `/implement` の出力契約の**歴史的記述**（旧 Step 2 の「変更計画」要求が実構成と食い違っていた F1） | **今回の変更が同型の欠陥を作らないかの検算基準**。ADR 0006:39 が「出力契約が実際の Step 構成と食い違っていないか、はどの検査も見ていない＝残余」と明記している。**項目を足したのに Step 4 が無い / Step 1 停止経路で免除されない、はまさに F1 と同型** → §1-1 の `:123` 修正が必須である理由 |

### E. 同概念・別名 — 「実施の有無が観測できない」という失敗様態のラベル群

issue が「同じ失敗様態」と呼ぶものの各所での呼び名。**新項目の文言はこれらと整合していなければならない**（＝これらが正本であり、新項目はその適用例である）。

| 場所 | ラベル |
|---|---|
| `CLAUDE.md`「サブエージェント委譲と worktree」 | 「レビュー・判定は会話にしか無く、届かなければ実施の有無すら区別できない」（#725） |
| `/plan-review`「Step 3 — 結果の統合と報告」/ 第 4 分類「未検証（理由）」 | 「予算切れ・アクセス不能でも『問題なし』と書かせない」 |
| `/health-check`「出力」 | 「異常なし」と「見ていない」を同じ記号で書かない |
| `docs/build-commands.md:58`（カテゴリ D） | 「実施の有無が会話にしか残らないと『検証されていない』と『問題が無かった』が区別できなくなる」 |
| `docs/development-principles.md:31` | 「移送先が**実施の有無を観測できる形**（記録テンプレート等）を持つかも見る——持たない場所へ移した責務は沈黙する」 |
| `2026-07-27-opus5-skill-tuning-design.md`「横断して見えたパターン」3 | 「記録テンプレートの有無が、実施の有無の唯一の観測点になっている」 |

### F. 概念で探して見つかった、grep では出ない依存

- **`4a` の「発見事項があれば修正してから 4b に進む」と `4b` の「Critical/High は修正してから次に進む」** — 新項目は「発見事項の件数」だけでなく**修正の有無**を書かせないと、この 2 つの義務の履行が観測できない。
- **`/norm-review` は 4a の 6 スキルに含まれない。** `AGENTS.md` 条件別チェック表 →「セーフティネット…を新設/変更」行 → `.claude/rules/safety-nets.md` → 「**パスに属する規範（`.claude/rules/` `.claude/skills/` および規範文書）を新設・変更したら起動する**」という**2 段の間接**で到達する。`/plan-review`「Step 2b」も同型（表から名指しされるが 4a の列挙外）。→ **§4 の受容する残余**に記録する。母集団を広げて飲み込もうとすると閉じた集合を失う（§1-3）。
- **`code-reviewer` 起動時手順 5**（`.claude/agents/code-reviewer.md:22`）が `AGENTS.md`「条件別チェック（トリガー → 参照先）」を読むよう指示している — 4a と 4b は**トリガー写像（何が該当するか）を上流で同じ表から得る**。**ただし新項目の母集団は表ではない**——母集団は §1-2 のとおり「4a が名指しする check スキル + `code-reviewer`」の閉じた集合であり、表を母集団に採る案は §1-3 で却下している。上流の共有は「該当判定の根拠が同じ」という意味に限る。

---

## 3. 発火すべき検証・ガバナンス機構

### A. PostToolUse hook — **沈黙は合格ではない**

`.claude/hooks/post-edit.test.mjs:147` が `selectChecks(".claude/skills/implement/SKILL.md")` → `[]` を実測アサートしている。**`.claude/skills/**` に検査は 1 つも割り当てられていない。** よって編集後の沈黙は「何も走らなかった」であり、`CLAUDE.md`「フック」・`docs/build-commands.md:28` の限定どおり合格を意味しない。

### B. `npm run governance:check`（カテゴリ F）— **手で実行が必須**

`AGENTS.md` 条件別チェック「ガバナンス文書（`*.md`・スキル表・モジュール索引・rules・workflow）を変更」行。走る G と本変更での期待:

| G | 内容 | 本変更での判定 |
|---|---|---|
| G8 | ルート `CLAUDE.md` スキル表 ↔ `disable-model-invocation: true` の skill の**双方向**一致 | 影響なし（skill の増減も frontmatter 変更も無い） |
| G10 | 恒久規範の面積 ratchet（二面独立） | **影響なし。母集団は `ALWAYS_LOADED_FILES = ["CLAUDE.md", "AGENTS.md"]` + skill の `description` だけであり、skill 本文は明示的に対象外**（`governance-check.mjs` の G10 コメント:「対象外は意図的である: skills 本文・モジュール CLAUDE.md・docs・ADR はいずれも『その作業に入った者だけが読む面』」）。→ **issue が懸念する「出力を軽くしたばかりなのに項目を足してよいか」は、面積予算の問題ではない**。常時ロード面の余裕は現在ごく僅か（`AREA_BUDGET.alwaysLoaded = 13374`）だが、本変更はそこに 1 字も載せない |
| G11 | 見出し参照の着地 | **要注視**。見出し・番号付きリスト項目・太字リードを触るため。期待は緑（§2-A で参照 0 件を確認済み）。落ちたら**参照を直す**——見出しを作りに行かない |
| G1〜G7, G9 | 参照実在・モジュール索引・SPEC 番号・rules glob・コマンド写像等 | 影響なし |

CI 側: `ci.yml` の `governance-check` job は `if` ガードを持たず **`skip-ci` を貼っても走る**（`docs/build-commands.md` CI/CD メモ）。ローカル実行を怠っても PR で捕まるが、**PR まで漏らさない**のが規約。

### C. `.claude/rules/safety-nets.md` の自動配送

`paths` に `.claude/skills/**` が含まれるため、`SKILL.md` を編集した時点で**自動配送される**。そこが要求するもの:

- 「セーフティネットが**規範**の場合、フォールトインジェクションとは**回避しようとする読者**である。…パスに属する規範を新設・変更したら**`/norm-review` を起動する**」
- 「**これまで無意味だった状態に意味を与える変更は、その状態に到達する全経路を列挙する**」 ← **本変更に直撃する。** 「報告に現れない」という状態に「未実施」という意味を与えるのだから、**同じ状態へ到達する経路を数え上げる義務がある**（該当なし / 未実施 / Step 1 停止で到達しない / 書き忘れ）。三値の設計（§1-2）はこの要求への回答である。
- 「**分類を 1 つ足したときは、隣接分類への到達経路も再列挙する**——逃げ道は塞いだ扉の隣に現れる（#489）」

### D. 起動すべき skill

| skill | 起動理由 | 必須性 |
|---|---|---|
| `/norm-review` | 規範（skill 本文）の変更。`safety-nets.md` が明示的に要求 | **必須**。起動前に Step 1 の停止条件 4 点（合格条件・上限巡数 2・残余の明記・**塞ぎ 1 件 1 文の分量予算**）を書き出す。予算は §1-1 の `:18` 修正にも掛かる |
| `/dry-check` | 「文書に事実の写しを増やす変更」（`AGENTS.md` 条件別チェック）に近い。母集団の件数・スキル名の写しを新設していないかの確認 | 推奨（軽量。§1-2 の不変条件 3 を満たしていれば自明に通る） |
| `/symmetric-check`・`/race-check`・`/cache-check`・`/persistence-check`・`/state-check` | コードパス・状態・永続形式・並行に触れない | **該当なし**（Markdown のみ） |
| `code-reviewer` | `/implement` 4b。本変更が `/implement` 経由で行われるなら適用される | 該当（自己適用） |
| `/plan-review`「Step 2b」独立導出 | 本ファイルがその成果物 | 実施中 |

### E. PR 前ゲート（PreToolUse hook）

`.claude/hooks/pre-bash.mjs` の `decide` が `gh pr create` をコマンド位置に検出したとき、(i) 未 push の空 PR、(ii) `workspace/plan.md` に未チェックの `- [ ]` が残る場合を拒否する（#749）。**本変更は Markdown のみで検証が軽いため「plan.md を消し込まないまま PR」の誘惑が最大の類型である。** `git push -u origin HEAD && gh pr create`（`cd` を鎖に入れない）。

### F. その他の機構

- `npm test`（カテゴリ C/E）: `.claude/hooks/**` を触らないので**不要**。
- カテゴリ A〜D: Rust / TS / ウィンドウ / 目視のいずれにも該当せず**該当なし**。
- **`main` 直コミット禁止**（ルート `CLAUDE.md` 最重要ルール 1）→ `chore/` または `fix/` ブランチ。
- **squash マージ時の `closingIssuesReferences` 確認手順**（ルート `CLAUDE.md`「Git/GitHub 運用」の 4 手順）— 本 PR は #765 を閉じるが、`2026-07-27-opus5-skill-tuning-design.md`「受容する残余」に列挙された**他の残余（`/start-issue` 5b の回復経路・`/plan-review` の `allowed-tools` に `Grep` が無い）を巻き込んで閉じないこと**。PR 本文に他 issue 番号を書くなら手順 1〜4 を守る。

---

## 4. 確認できなかった観点・受容する残余

1. **`/norm-review` と `/plan-review`「Step 2b」が新項目の母集団の外にある。** 両者は `AGENTS.md` 表または `.claude/rules/safety-nets.md` 経由の**間接到達**であり、4a が名指しする 6 スキルに含まれない。**本変更そのものがその実例である**（規範変更なので `/norm-review` が該当するが、新項目の三値には現れない）。母集団を広げて飲み込むと閉じた集合を失い、三値が成立しなくなる（§1-3）ため、**名前を挙げた受容する残余として記録することを推奨する**。issue の要求（Step 4 の受け口）は 4a/4b で満たされる。
2. **三値の実効性を測る機構は無い。** `governance:check` の母集団に「報告様式を守ったか」は入らず、G10 も skill 本文を見ない。`2026-07-27-opus5-skill-tuning-design.md`「受容する残余」の「長さ較正は規範であり機構ではない」と同クラス。**規範として置く以外に手が無い**ことを明記すべき。
3. ~~カテゴリ D の分類は設計判断が残る~~ → **決着済み（残余ではない）。§1-2 への追加要件として扱う。**
   **「カテゴリ D は該当するが、エージェントは実行できない」は『未実施』へ黙って吸収してはならない。** 現行の項目 2 は「実行できなかったカテゴリ（D 等）と**人間へ引き渡す目視項目**」を要求しており、これを「未実施（理由: エージェント実行不可）」の 1 行に潰すと**引き渡す目視項目の列挙が消える**。`docs/build-commands.md:58` がその義務を課す理由は、まさに本 issue が問題にしている失敗様態そのもの（「実施の有無が会話にしか残らないと『検証されていない』と『問題が無かった』が区別できなくなる」）であり、潰せば欠陥を別の場所へ移すだけである。
   決着: **D を独立の状態として名指しする**（`/health-check` が `[Skipped]` を Check 7 だけに限定し、それ以外の Skipped 自体を発見事項と定めた形の対応物）。**もし三値のまま吸収するなら、『未実施』の行に「引き渡した目視項目を列挙する」義務を同居させること**——どちらの形でも、引き渡し項目の列挙が消える文言は不合格である。逆に D を無限定の『未実施』に置けるようにすると、「面倒な検査は未実施と書けば楽」という抜け道を `/health-check` が塞いだ場所に作り直すことになる。
4. **`/norm-review` を実際には回していない。** 本導出は文言の設計までであり、2 クラスの敵対的読者による検証は未実施。**新項目の文言が確定した後に必須**（§3-D）。
5. **混入の記録**: `grep -rn` を `workspace/` 除外なしで 2 回実行したため、`workspace/plan.md` と `workspace/research.md` の該当行がツール出力に現れた（項目の三値化・母集団・序数参照 0 件などに触れる行）。**両ファイルを開いてはいない**。本導出のうち偶然一致する結論には、それぞれ独立した一次証拠がある——(a) issue 第 2 点が古いことは `git blame -L 127`（`069205e`）、(b) 序数参照 0 件は `grep -rnoE '`/implement`「…」'` の全件列挙、(c) G10 が skill 本文を課税しないことは `governance-check.mjs` の `ALWAYS_LOADED_FILES` と G10 コメント、(d) 番号付きリストが G11 アンカーであることは `collectAnchors` の正規表現。いずれも混入行には無い（または混入行より詳細な）観測である。
6. **`.github/pull_request_template.md` への転記義務は検討していない。** レビュー結果を PR 本文にも残す案（会話が失われても残る）は issue の「4b の結果をコミットメッセージ側へ寄せる」案と同族だが、利用者が採ると決めた方針は「出力」節への項目追加であり、本導出はその制約に従った。**PR / commit 側への二重記録が要るかは未検討**。
