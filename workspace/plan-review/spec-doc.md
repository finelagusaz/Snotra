## 問題なし

- **差分①の語選択は妥当**。SPEC.md 内で「選択位置」を grep すると全 1 件（`SPEC.md:243`、§6.4 の Escape 復元値）のみで、それ以外の箇所（§6.1 含む）は未使用（`Grep pattern:"選択位置|選択中|選択対象"` 実行結果）。§6.1 が新たに「選択位置」を導入すると同一節内で異なる 2 概念（現在選択 vs 退避値）を同語で指すことになるため、別語「先頭の候補」を選ぶ判断は理に適う。逆方向の衝突（「候補」の既存用法との衝突）も確認済み：SPEC 内の「候補」は「候補一覧・候補リスト・候補を置換」などすべて集合（リスト全体）を指す用法で、「先頭の候補」（単一要素）と文法的役割が異なり実害の衝突は見当たらない（`Grep pattern:"候補"` 全 11 件を確認）。§4.1「先頭部分一致」（マッチング方式名の複合語）とも節・文脈が離れており誤読の実害は小さい
- **§6.4 への相互参照を置かない判断は妥当**。コードで両者が別の値であることを確認した：`enter_folder`/`navigate_folder`（`search_state.rs:236-258`）は突入・遷移のたびに `selected = 0` で現在選択を更新するのに対し、`restore_selected`（同 240, 302）は突入前の値を `FolderFrame` に退避し、Escape 時（同 334, 342）に `clamp_selected(..., restore_selected)` で復元するだけの別変数。両者は実装上も完全に独立しており、相互参照を増やすと「腐る」というリスクの方が実害に見合う
- **差分②「0 件なら選択対象なし」はコードの実挙動と整合**。`clamp_selected(len, idx)`（`search_state.rs:374-380`）は `len == 0` で無条件に `0` を返すので、フォルダ空到着時に selected が異常値を取ることはない。加えて `present_results`（`layout.rs:204-211`）は `result_count == 0` のとき `desired_height == 0.0` となり `Hidden` を返す（`present_results_truth_table_distinguishes_all_four_conjuncts` テスト・`layout.rs:441-461` で確認）。つまり空フォルダ到着時は results 窓自体が非表示になり「選択対象なし」は UI 挙動として正確な記述である
- **「フォルダが空」と「列挙に失敗」の別経路の取り違えは無い**。§6.6 の列挙失敗は「候補リストに単一のエラー行を表示」（`SPEC.md:254`）であり件数は 1（0 ではない）。0 件経路（空フォルダ）とは result_count が異なり、`present_results` の可視性判定も分岐する（前者は非表示、後者はエラー行 1 件で可視）。計画が両者を同じ文に書かず区別している点は正確
- **前ラウンド指摘の反映漏れは無し**。左右共通（Phase 1 の 2 番目）・2 段構成（1 番目）・「保証しない」の明記（3 番目）・識別子規律（5 番目）の 4 点すべてが Phase 1 のチェックリストに現れている（`plan.md:30-39`）

## 軽微な懸念

- Phase 1 のチェックリスト本体（`plan.md:30`）は「2 段（打鍵時点で先頭行 / 到着時はクランプのみ）」とだけ書いており、「変更ファイル一覧」表（`plan.md:19`）にある「0 件なら選択対象なし」の括弧書きがチェックリスト側には明示的に繰り返されていない。表とチェックリストは同一ドキュメント内で矛盾はしないが、実装者がチェックリストの5行だけを見て書くと 0 件の一句を書き落とす余地がわずかに残る。実害は小さい（同じ plan.md 内に明記があるため見落としの可能性のみ）
- Phase 1 の1番目の項目文自体が「選択位置を…追記する」と書いており、直後の4番目の項目が「裸の『選択位置』という語は使わない」と指示する。前者はチェックリストのメタ記述（内容の要約）であって実際に SPEC へ書く文言ではないと読めるが、5項目を通読せず順に着手すると一瞬迷いうる表現の重複

## 要対処

- なし

## 未検証

- 実際に §6.1 へ追記された確定文言（このラウンドでは SPEC.md はまだ編集されていない設計段階のため、最終的な日本語文がこの判定基準をすべて満たすかは実装後に読み直す必要がある）
- `folder.rs` の列挙順（ソート順）が実際にどのような順になるか自体は本レビューの範囲外（計画が「正本は `folder.rs` の `//!`」として書き写さない方針を取っており、その方針の当否は確認したが列挙順そのものの中身は未確認）
