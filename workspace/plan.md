# plan.md — issue #404 SPEC.md の最新化

## 方針

研究で確定した **6 件**の陳腐化（F1–F6、いずれも「実装が真実・SPEC が追従漏れ」）を、`SPEC.md` の該当本文のみを書き換えて as-built に揃える。**doc-only**。コード・挙動・IPC・設定フォーマットは一切変えない。

AGENTS.md Step 0 判定: 6 件とも「コード変更/実装進化が先に入り SPEC が追従していない」ケース。コードが意図的に正しいことを git/コメント/構造で裏取り済み（F2: SC_KEYMENU / Win 8+ shell reservation コメント、F3: #198 コミット、F4: `search.ts` 2 軸モデル、F5: `config.rs` 構造体 + §19.5 整合、F6: `app.rs` TabId）。よって**バグ修正ではなく SPEC の as-built 同期**。

検出経緯: F1–F3 は 3 分割監査 + メイン裏取り。**F4–F6 は plan-review Step 2b の独立再導出が追加検出**（メインで再裏取り済み）。2 つの監査枠組みが収束したため completeness の確度は高い。

## 変更ファイル一覧

- `SPEC.md` のみ（6 件 / 7 箇所、+ 任意 1 箇所）
  - **F1**: §1 スコープ境界（line 26-28）— `やること` にインスタントコマンド／カスタムオープナーを追記し、`将来の拡張ポイント` 行を削除 [将来framing]
  - **F2**: §7.4 ホットキーバリデーション（line 317-333）— ブロック条件に Win+* 無条件拒否を追記、表に `Alt+Space` 行を追加、`完全一致` 記述を修正 [factual]
  - **F3**: §17.1 履歴フォーマット互換（line 608-609）— フォールバックを `V3 -> V2` に、`V1/V2` を `V2` に修正（#198 付記） [factual]
  - **F4**: §4.7 結果表示制御（line 181）— `shouldShowResults` の式を 2 軸モデル準拠の正確な記述に修正 [factual]
  - **F5**: §19.2 共通フィールド（line 739）— `display` を「派生値・description 優先」に修正 [factual]
  - **F6**: §18.6（line 663）/ §19.8（line 921）— 設定タブ列挙を実 7 タブに修正 [factual]
  - **F6b（任意・低優先度）**: §7.2（line 305 直後）— `[インスタントコマンド]`/`[バックアップ]` のポインタ小エントリを追加してタブ構成を 7 タブ整合（completeness）
- `workspace/research.md` / `workspace/plan.md`（成果物コミット）

> 行番号は現行 SPEC（main）基準。編集は**「変更前」テキストの文字列一致**で行うため、行番号がずれても誤爆しない。

## 実装順序

全編集は相互依存なし。SPEC 上から順に F1 → F4 → (F6b) → F2 → F3 → F6(§18.6) → F5 → F6(§19.8) で適用（diff レビューの追いやすさのため）。

### F1: §1 スコープ境界

- 変更前:
  ```
  - **やること**: アプリ・フォルダの高速検索と起動、履歴による学習、テーマ・ホットキー等の基本カスタマイズ
  - **やらないこと**: ファイル内容検索、ウィジェット/情報表示、プラグインエコシステム
  - **将来の拡張ポイント**: インスタントコマンド（@プレフィックス）によるユーザー定義コマンド
  ```
- 変更後:
  ```
  - **やること**: アプリ・フォルダの高速検索と起動、履歴による学習、インスタントコマンド（@プレフィックス）によるユーザー定義コマンド実行、カスタムオープナーによる起動先のカスタマイズ、テーマ・ホットキー等の基本カスタマイズ
  - **やらないこと**: ファイル内容検索、ウィジェット/情報表示、プラグインエコシステム
  ```
- `将来の拡張ポイント` 行は**削除**。インスタントコマンドは実装済みで、他に列挙すべき将来項目もない。将来計画の管理は issue #404 備考どおり別管理（GitHub Issue 等）に委ね、SPEC に投機的な将来リストを残さない。

### F4: §4.7 結果表示制御

- 変更前（line 181）:
  ```
  - 結果の表示/非表示は `shouldShowResults` メモシグナル（`results().length > 0 && !indexing()`）で制御する
  ```
- 変更後:
  ```
  - 結果の表示/非表示は `shouldShowResults` メモシグナルで制御する。`results` が空なら非表示。ツール選択・フォルダ展開ビューはインデックス構築中でも表示し、通常結果ビューはインスタントコマンドモードまたは非インデックス時に表示する（2 軸モデル `viewKind()`/`interpKind()` から導出）
  ```
- 裏取り元: `ui/src/stores/search.ts:82-95`（`shouldShowResults` の `viewKind()` switch）。`ui/CLAUDE.md`「単一ウィンドウの高さ管理」が同一記述。

### F6b（任意）: §7.2 タブ構成のポインタ補完

- `[オープナー]` タブのブロック直後（§7.3 の前）に以下を追加:
  ```
  `[インスタントコマンド]` タブ:

  - プレフィックス設定・コマンド定義の追加/編集/削除/複製（詳細は §19.8 参照）

  `[バックアップ]` タブ:

  - config.toml のエクスポート/インポート・設定フォルダを開く（詳細は §13.3 参照）
  ```
- フル複製はせずポインタに留める（YAGNI）。**ユーザー判断で省略可**。

### F2: §7.4 ホットキーバリデーションルール

1. 条件列挙（現 line 317-319）に Win 修飾キーの無条件ブロックを 1 項目追加:
   ```
   - Win（Super/Meta）修飾キーを含む全組み合わせ（Win 8+ がシェルレベルで `Win+*` を予約済みのため無条件ブロック。下表の完全一致とは別枠のワイルドカード判定）
   ```
2. 表（現 line 321-326）に行を追加（**「RegisterHotKey 成功」の断定は外す**＝コード非由来のため。検証エージェント指摘を反映）:
   ```
   | `Alt+Space` | Windows システムメニュー（SC_KEYMENU・OS 機能を奪う） |
   ```
3. `完全一致` の記述（現 line 333）を修正:
   - 変更前: `- \`Alt+Shift+F4\` など modifier セットが異なる場合はブロックしない（完全一致）`
   - 変更後: `- 下表のエントリは modifier セット完全一致でブロックする（\`Alt+Shift+F4\` など modifier セットが異なれば非ブロック）。ただし Win 修飾キーを含む組み合わせは上記のとおりワイルドカードで無条件ブロックする（完全一致の例外）`
- **line 334「同じリストでガード」は触らない**: TS 側のみ `FORBIDDEN_MAIN_KEYS`（capslock・IME キー等）を持つ Rust/TS 乖離は実装不整合でありスコープ外（research スコープ判断参照）。F2 では Win/Alt+Space のみ是正する。
- 裏取り元: `config.rs:1211-1217`（`("alt","space")` + コメント `// Alt+Space: Windows system menu (SC_KEYMENU)`）、`config.rs:1256-1259`（`parts.contains(&"win") → true` + コメント `// Win 8+ reserves all Win+* combinations at the shell level.`）。

### F3: §17.1 履歴フォーマット互換

- 変更前（line 608-609）:
  ```
  - 読み込みは `V3 -> V2 -> V1` の順でフォールバックする
  - V1/V2（秒単位）を読み込んだ場合は、正規化・統合処理より先に `ms` へ変換する
  ```
- 変更後:
  ```
  - 読み込みは `V3 -> V2` の順でフォールバックする（V1（bincode）フォールバックは #198 で廃止済み）
  - V2（秒単位）を読み込んだ場合は、正規化・統合処理より先に `ms` へ変換する
  ```
- 直後の「単位混在のまま統合してはならない」行（line 613）は V2(sec)/V3(ms) 混在で依然有効 → **変更なし**（検証エージェント確認済み）。
- 裏取り元: `history.rs:10-14`（`HISTORY_FALLBACKS = &[V2]`）、git `fc4dd4e`（#198）。

### F5: §19.2 共通フィールド `display`

- 変更前（line 739）:
  ```
  - `display`: 結果リスト副テキスト（url または `exe args` の表現）。省略時は説明を使い、説明も無い場合はコマンドテンプレートを自動生成
  ```
- 変更後:
  ```
  - `display`: 結果リスト副テキストの表現（URL 種別は url、exec 種別は `exe args`）。ユーザーが設定する config フィールドではなく、バックエンドが DTO 生成時に常に算出する派生値。UI では `description` があればそれを優先表示し、無ければ `display` を表示する（§19.5 参照）
  ```
- 裏取り元: `config.rs:49-55`（`InstantCommand` に display フィールド無し）、`src-tauri/src/commands/launch.rs`（display 派生）、`ui/src/stores/search.ts:301`（`cmd.description || cmd.display`）、SPEC §19.5 line 866-867（description 優先＝整合）。

### F6: §18.6 / §19.8 設定タブ列挙

- §18.6（line 663）変更前: `- 設定画面に「オープナー」タブを追加（全般/検索/インデックス/ビジュアル/オープナー）`
  - 変更後: `- 設定画面に「オープナー」タブを追加（全タブ構成: 全般/検索/インデックス/ビジュアル/オープナー/インスタントコマンド/バックアップ）`
- §19.8（line 921）変更前: `- 設定画面のタブ構成: 全般/検索/インデックス/ビジュアル/オープナー/インスタントコマンド`
  - 変更後: `- 設定画面のタブ構成: 全般/検索/インデックス/ビジュアル/オープナー/インスタントコマンド/バックアップ`
- 裏取り元: `app.rs:53-70`（`TabId` 順: General/Search/Index/Visual/Opener/InstantCommand/Backup）、`snotra-settings/src/tabs/` の 7 ファイル。

## 不変条件

- **doc-only**: コード・挙動・IPC 契約・設定フォーマットを変えない。逆方向（SPEC→コード）同期は不要。
- **as-built 一致**: 編集後の SPEC 記述が引用元コード（`config.rs` / `history.rs` / `search.ts` / `launch.rs` / `app.rs`）の現状と一致すること。実装より「多い」記述（F3 の V1、F5 の display 設定可フィールド扱い）も「少ない」記述（F2 の Win/Alt+Space、F6 のタブ）も残さない。
- **内部整合**: F5 で §19.2 と §19.5（description 優先）の内部矛盾を解消する。F4 で §4.7 を ui 2 軸モデルと一致させる。F6/F6b で §18.6・§19.8・§7.2 の 3 つのタブ列挙箇所を相互に揃える（タブ列挙の「対称ペア」を全て同時更新）。
- **セクション番号不変**: 全編集は既存セクション内の本文修正（F6b は §7.2 内の項目追加でサブセクション番号を持たない）。セクションの追加・削除がないため `### N.x`・`## N.` の番号ずれは発生しない。
- **将来項目の消失なし**: F1 で削除する `将来の拡張ポイント` 行の唯一の項目（インスタントコマンド）は実装済み。失われる将来タスクはない。
- **F2 の網羅性（弱めた主張）**: SPEC 追記する Win+*/Alt+Space ルールは Rust `validate` と front `hotkeyValidation.ts` の**両方に実在**し、両者整合。**ただし line 334「同じリスト」の厳密整合までは保証しない**（TS のみの `FORBIDDEN_MAIN_KEYS` はスコープ外）。

## テスト方針

doc-only のため**コードテストは追加しない**（`docs/build-commands.md` のカテゴリ A〜D はいずれも非該当 = `.md` 単独編集で clippy/typecheck/test 不要）。代わりに以下の検証を実施:

1. **残存 framing の再 grep**: 編集後 `SPEC.md` に `将来|予定|未実装|拡張ポイント|V3 -> V2 -> V1|results\(\).length > 0 && !indexing` を grep し、F1/F3/F4 由来の陳腐化表現が残っていないことを確認（line 773・805「将来の予約語」は実装済み `{{…}}` の設計趣旨＝正当・残してよい）。
2. **as-built 逐語照合**: 編集 6 箇所を引用元コード行（`config.rs` / `history.rs` / `search.ts` / `launch.rs` / `app.rs`）と再突合し、追記/修正が現状コードと一致することを確認。
3. **内部相互参照の整合**: §19.2↔§19.5（display）、§18.6↔§19.8↔§7.2（タブ列挙）が編集後に矛盾しないことを確認。
4. **セクション番号整合**: §1〜§20 の見出し番号・子番号が編集前後で不変であることを確認。
5. **Markdown 健全性**: §7.4 の表が 6 行（本体）で崩れず描画されること、F1/F6b の箇条書きが正しく描画されることを目視（PR 作成前にプレビュー確認）。

## SPEC.md 更新要否

本 issue **そのもの**が SPEC.md の as-built 同期。コード変更を伴わないため、SPEC→コードの逆同期や docs/ への波及更新はスコープ外（docs/ の並行陳腐化は未監査 = 発見次第 follow-up issue）。

## セルフレビュー（Step 5 結果）

### 5a. check スキル結果

- **`/plan-review` 実行済み**（Step 2 成果物監査 3 体 + Step 2b 独立再導出 1 体）。
  - Step 2: F1（妥当・instant/opener 実装裏取り）、F2（ブロック対象網羅・SC_KEYMENU/Win8+ コメント実在、ただし「RegisterHotKey 成功」はコード非由来→**表文言から削除**）、F3（V3→V2・#198 帰属・「単位混在」行温存いずれも妥当）を確認。要対処なし。
  - **Step 2b（独立再導出）が F4/F5/F6 を追加検出** → メインで再裏取りし計画へ反映。最大の収穫。
- `/symmetric-check` `/cache-check` `/state-check` `/race-check` は非該当（doc-only・コードパス/async/キャッシュ/UI モードに触れない）。ただし F6 の「タブ列挙の対称ペア」（§18.6/§19.8/§7.2）は手動で 3 箇所同時更新として網羅。

### 5b. セルフレビューチェックリスト

1. **対称コードパス**: doc 内の対称＝同一事実を述べる複数箇所。F6 のタブ列挙（§18.6/§19.8/§7.2 の 3 箇所）、F5 の display（§19.2/§19.5 の 2 箇所）を全て同時に揃える。コード側の対称ペアは無し（doc-only）。
2. **影響範囲の網羅性**: 2 独立枠組み（3 分割 section 監査 + 独立再導出）が収束。6 件すべて exact コード行で裏取り。残存 framing の全文再 grep でも新規ゼロ。→ completeness 高。
3. **境界条件**: section 番号整合・Markdown 表描画・内部相互参照を検証項目化（テスト方針 3-5）。
4. **リソース管理**: N/A（doc-only・生成/破棄リソースなし）。
5. **既存パターンとの整合**: 新パターン導入なし。すべて as-built への追従。F4 は ui/CLAUDE.md の既存正記述に合わせる。
6. **YAGNI 違反**: 抑制済み。F2 表文言をコード由来に絞る（「RegisterHotKey 成功」削除）。FORBIDDEN_MAIN_KEYS の Rust/TS 乖離はスコープ外（follow-up）。F6b は§7.2 にフル複製せずポインタのみ＋**任意扱い**。
7. **シンプル化の挑戦**: doc-only で新状態（AtomicBool/Mutex/子プロセス）導入なし。「操作が失敗したら」の懸念は doc 編集に無し。唯一のリスクは「誤った行を編集／新たな不正確さの混入」→ 文字列一致編集 + 編集後の逐語照合 + 再 grep で緩和。
8. **破壊不変条件**: doc-only のため Win32 フック・ホットキー・IPC 等の「戻ってこない」系リスクは無い。検知手段は上記テスト方針（再 grep + 逐語照合 + PR プレビュー目視）。

### スコープに関する申し送り

- issue 明示例は F1 のみ。F2–F6 は「セクションによって内容が古い」という issue 本文に沿って監査が追加検出した同型の陳腐化。**全 6 件を実装対象とするが、ユーザーが F1 のみ／factual のみ等にスコープを絞る判断も可能**（F6b は特に任意）。
- follow-up 候補（別 issue）: ① FORBIDDEN_MAIN_KEYS の Rust/TS 乖離（SPEC line 334）② docs/ の並行陳腐化監査。
