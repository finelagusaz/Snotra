# plan — issue #765: `/implement`「出力」に Step 4 の受け口を作る

## 方針（ユーザー確認済み）

「出力」節に Step 4 の項目を 1 つ足し、**母集団を SSOT から導出する三値**で書かせる。併せて項目 2（最終検証結果）の母集団も同じ形へ揃える。issue 後半が名指しした「`check, clippy, test` の 3 種」は #764 で既に解消済みであり（`research.md`「前提の是正」）、直す対象は**そこに残った同型の残余**——母集団が「実行したカテゴリ」＝書き手の裁量になっている点である。

採る形の根拠は `/health-check`「Check 7 — MEMORY.md 参照の実在性」の三値と `/plan-review`「Step 3 — 結果の統合と報告」の配送欄。どちらも**母集団を先に閉じ、各要素に必ず 1 つの値を割り当てる**。#764 の総括パターン 2（書き手が分類を決める限り義務は書き手が消せる）・パターン 3（記録テンプレートの有無が実施の有無の唯一の観測点）への既存解である。

## 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `.claude/skills/implement/SKILL.md` | (a) `:19` の実況制限に、報告義務へは掛からない旨の従属節を 1 つ足す / (b) `:123` の停止経路の免除を、経路の列挙から「到達しなかった Step」という**原理**へ書き換える（`/plan-review` の要対処を受けた方針転換） / (c) `:125-128` の項目 2 を三値化し、Step 4 の項目を新設して 4 項目にする / (d) **（計画外・実装中に判明）** `:91` のカテゴリ F 発火条件の狭小化を是正する |

**触らない**: frontmatter の `description`（G10 常時ロード面）・`## 出力` / `### 4a.` / `### 4b.` の見出し文字列（`CLAUDE.md:74` と `docs/adr/0004` が正準形で指し、G11 が照合する）・Step 4 本体の手順（issue は報告の穴であって手順の穴ではない）。

## 実装順序

### Phase 1 — 文言の変更

- [x] `:125-128` の項目 2 を、`docs/build-commands.md` のカテゴリ A〜F を母集団とする三値へ書き換える。三値目は**無限定の「未実施」ではなく「該当するが実行不能（D 等）」として独立に名指しし、引き渡す目視項目の列挙を必須のまま残す**（`docs/build-commands.md:57` が「実施の有無が会話にしか残らないと『検証されていない』と『問題が無かった』が区別できなくなる」と定める。D を「未実施」へ畳むと同じ欠陥を作り直す）。既存の「追加/更新したテスト名と、それが検証した不変条件」の要求も残す
- [x] 項目 3 として Step 4 のレビュー結果を新設する。母集団は**「4a. check スキルの実行」が名指しする check スキルと 4b の `code-reviewer`**（件数は書かない——数を書くとスキルの増減でドリフトする）。実施は 1 件 1 行（発見件数・修正の有無）、該当なしは 1 行にまとめて列挙、未実施は理由付きで 1 行ずつ。**「該当なし」と「未実施」を同じ記号で書かない**
- [x] 既存の「コミットハッシュとメッセージ」を項目 4 へ繰り下げる
- [x] `:123` の免除を**列挙から原理へ書き換える** — 現行は「Step 1 で止まった場合」だけを名指しし、Step 3 の中止経路（`:94`「5サイクル後もエラーが残る場合、中止して診断サマリーを書く」）を覆っていない。その経路は Step 4・5 に到達しないため、**既存の「コミット」項目が既に充足不能である**（先行欠陥・項目を足すと 2 件に広がる）。「到達しなかった Step に対応する項目は、どこで止まったかを書いて省く」形にすれば両方同時に塞がる。免除対象を名前で列挙し直すと逃げ道が列挙の隣へ移る（`/norm-review`「Step 3 — 成立した指摘だけを塞ぐ」）
- [x] `:19` の「作業中の実況は、発見があったときと方針を変えたときに限る。」へ、**これが作業中の実況に掛かる制限であって「出力」節の報告義務を免除しないこと**を 1 つの従属節で足す（issue が指摘する「`/symmetric-check` を実行、発見なし」が述べてはならない実況になる衝突を、報告面と実況面の境界を明示して解く）
- [x] **（計画外・実装中に判明）** `:91` の箇条書きがカテゴリ F の発火条件を「ソースファイルを追加/削除した場合」と SSOT より狭く再定義していたのを是正する——項目 2 が A〜F 全件の値割り当てを要求した瞬間に、この狭小化が「F は該当なし」の口実になる（`/norm-review` 1 巡目・手を抜く読者が発見）

### Phase 2 — `/norm-review`（規範のフォールトインジェクション・必須）

`.claude/rules/safety-nets.md` が `.claude/skills/**` の変更で要求する。**停止条件は着手前に決める**（`/norm-review`「Step 1 — 停止条件を先に決める（起動前・計画に書く）」）:

**合格条件（通してはならないシナリオ）**:
1. *手を抜く読者* が「該当なし」を口実に、実施すべき check スキルを 1 件も名指しせずに済ませる
2. *手を抜く読者* が新項目を「発見があったときだけ書けばよい」と読み、`:19` の実況制限を報告義務の免除として使う
3. *規則を全部守る読者* が三値の「実行不能」をカテゴリ D 以外へ広げ、実行できるカテゴリを人間へ投げる
4. *規則を全部守る読者* が母集団の閉じ方を読み違え、`AGENTS.md`「条件別チェック（トリガー → 参照先）」表の非スキル行（rules 参照・grep 指示）まで 1 行ずつ書こうとする
5. *規則を全部守る読者* が Step 3 の中止経路（`:94`）で、到達していない Step 4 のレビュー結果を書こうとする（＝**捏造か沈黙のどちらかを強いられる**）
6. *規則を全部守る読者* が 4a の括弧書き 6 スキルを写像の SSOT と読み、`AGENTS.md`「条件別チェック（トリガー → 参照先）」表に増えたスキルを報告母集団から落とす（不変条件 1 の後段）

**上限 2 巡** / **塞ぎ 1 件あたり 1 文まで**（超えるものは受容する残余へ回す）。2 巡後に残る抜け道は受容し、コミットメッセージまたは `plan.md` 末尾へ記録する。

- [x] `/norm-review` を起動する（対象: 「`/implement`「出力」への Step 4 項目の新設と項目 2 の三値化」）。**渡すのは変更後の文言と上の合格条件・成果物の絶対パス** → 2 巡 × 2 クラス = **4 体すべて配送成立**（`workspace/norm-review/*.md`）
- [x] 成立した指摘だけを、分量予算内で塞ぐ（降格した指摘は事実として残す） → **1 巡目 15 件中 5 件・2 巡目 14 件中 3 件**を修正
- [x] 2 巡で打ち切り、残る抜け道を「受容する残余」として記録する

**各巡の結果**:

- **1 巡目**: 手を抜く読者 10 件・規則を守る読者 5 件 → **成立 5 件を修正**（降格 10 件）。最大の発見は**わたくしが「実行不能」をカテゴリ D に暗黙で閉じていたこと**——`docs/build-commands.md:105-125` は **Windows 専用コマンド**を名指しで列挙しており、カテゴリ A の必須コマンドの大半（`cargo check` / `clippy` / 各 `cargo test`）がそこに現れる。`等` の一語がその扱いを書き手の裁量へ開いていた。塞ぎは「実行者・実行環境に由来するものだけ」という**原理**（列挙ではない）。降格の主なもの: `code-reviewer` の Medium/Low が報告義務外（#764 が「ガイド推奨形」と判定済みの設計）・非スキル行の水増し（充足確認は消えない）・`/dry-check` 行の副義務（母集団の設計上の境界）
- **2 巡目**: 手を抜く読者 6 件・規則を守る読者 8 件 → **成立 3 件を修正**（降格 11 件）。**3 件中 1 件は 1 巡目の塞ぎ自身が作った過剰な全称表現**で、修正は**縮める**方向だった（「1c の停止は Step 2〜4 の途中でも」と書いたが、典拠は「実装の途中」＝ Step 2 のみ）。残る 2 件は「1 行で根拠」を上限と読める形の是正と、「時間・手間を理由にしない」歯止めが項目 2 の「実行不能」にしか掛かっていなかった件（**逃げ道が列挙の隣へ移った実例**）

### Phase 3 — 検証

- [x] `npm run governance:check`（カテゴリ F）を手で実行する。**`.claude/skills/**` に PostToolUse 検査は無く、編集後の沈黙は「何も走らなかった」である** → **G1..G11 passed**（対象文書 43 件 / skills 13 件 / 見出し参照 123 件を 55 文書から照合）
- [x] G10（面積 ratchet）が赤にならないことを確認する。理屈上は skills 本文が対象外だが、確認するのは `description` を触っていないことの実測でもある → **常時ロード 13274/13374 字**（plan-review 時のベースラインから不変＝`description` 未変更を実測）

**実行順序を計画から変えた**: Phase 3 を Phase 2 の前に実行した。G11 の参照が着地しない文言のまま読者エージェントを起動すると 2 巡が無駄になるため（依存関係の逆転であって、作業の省略ではない）。**上の値は、Phase 2 の 2 巡と `code-reviewer` の指摘を反映し終えた最終文言に対して再実行したものである**——Phase 2 が正準見出し参照を 4 件増やしたため、順序を変えた回の記録が古い文言のものになる経路が実在した（`code-reviewer` H2）。

## 不変条件

1. **母集団は書き手が選べない集合として閉じている** — 項目 2 は `docs/build-commands.md` のカテゴリ A〜F（SSOT そのもの）、項目 3 は `/implement`「4a. check スキルの実行」が名指しするスキル集合。どちらも件数を本文に書かない（スキル・カテゴリの増減で写しが腐るため）。**この不変条件が破れた形**＝「実行したもの / 該当したものだけを書けばよい」と読める文言に戻ること

   **ただし項目 3 の母集団は SSOT ではなく写しである** — 4a 自身が「写像の SSOT は `AGENTS.md`「条件別チェック（トリガー → 参照先）」表。二重管理を避けるためここに再掲しない」と述べたうえで、6 スキルを括弧書きで列挙している。**本変更はこの括弧書きを「便宜的な列挙」から「報告義務の閉じた母集団」へ格上げする**（`.claude/rules/safety-nets.md`「これまで無意味だった状態に意味を与える変更は、その状態に到達する全経路を列挙する」に当たる操作である）。表へスキルを 1 件足して 4a の括弧書きを直し忘れると、そのスキルは報告母集団から**沈黙して落ちる**。**この乖離を検知する機構は存在しない**（`scripts/governance-check.mjs` を grep して確認済み——4a の列挙と表を結ぶ検査は無い）。表を直接指す形へ寄せると `AGENTS.md` 表の非スキル行（rules 参照・grep 指示）を読み違える経路が開くため寄せず、**写しであることを名指しした受容する残余として扱う**
2. **「該当なし」「未実施」「実行不能」が互いに区別可能である** — 3 つを同じ記号・同じ省略で書ける文言にしない。破れると「検証されていない」が「問題が無かった」に化ける（`/health-check`「Check 7 — MEMORY.md 参照の実在性」の三値と同じ理由）
3. **報告面と実況面の境界が明示されている** — `:19` の実況制限が「出力」節の義務を削れない。破れると issue が指摘する「正直に書いた側が罰される形」が残る
4. **到達しなかった Step の項目が要求されない** — 母集団は 1c 経路（1c-A / 1c-B / 1c-C）**だけではない**。Step 3 の中止経路（`:94`）も Step 4・5 に到達しない。破れると、到達していない実行で存在しないレビュー結果を書かせることになる（**書けないので捏造か沈黙のどちらかになる**）。ゆえに免除は経路の列挙ではなく「到達しなかった Step」という原理で書く
5. **外部からの参照が壊れない** — `## 出力` / `### 4a.` / `### 4b.` の見出し文字列を変えない。`CLAUDE.md:74`・`docs/adr/0004-canonical-heading-references.md` が正準形で指し、G11 が照合する。「出力」項目への**序数参照は外部に 0 件**（`research.md` で grep 済み・plan-review で 2 レイヤーが独立に再確認）のため、項目の繰り下げは外へ波及しない。**ただし番号付きリスト項目自体が G11 のアンカー面である**（`scripts/governance-check.mjs:722` の `collectAnchors` が `^\s*\d+[.)]\s+` を拾う）——新項目の行頭語はこの変更の瞬間から公開面になるため、後から気軽に変えられる文言のつもりで書かない
6. **常時ロード面を増やさない** — frontmatter の `description` を触らない（G10 の `alwaysLoaded` には skill description が算入される。skills 本文は対象外）

## テスト方針

このファイルには実行可能な検査が無いため、**検証は 2 系統**である。

- **機械**: `npm run governance:check`（カテゴリ F）— 参照実在（G11 の正準見出し照合）・スキル表・面積 ratchet（G10）。**これが覆うのは形式であって、三値の文言が実際に義務を課すかは覆わない**
- **規範**: `/norm-review` の 2 クラスの読者（Phase 2）— 上の合格条件 4 件が、機械検査の外側を受け持つ唯一の検証である

**このサイクルで新規のユニットテストは追加しない**（対象が `*.md` の規範文言であり、`post-edit.mjs` の `selectChecks` にも `vitest` の include にも母集団が無い）。

## SPEC.md 更新要否

**不要。** 変更対象はエージェントの運用規範であり、プロダクトの挙動・状態遷移ではない（`AGENTS.md`「3層分担（責務分離）」の第 1 層のうち `SPEC.md` が持つのは製品仕様であって、スキルの報告契約ではない）。

## セルフレビュー

### `/plan-review` の要対処（2 件・いずれも本計画へ反映済み）

- **免除のスコープ不足** → Phase 1 の該当項目と不変条件 4 を「列挙から原理へ」書き換え、`/norm-review` の合格条件 5 を追加した。**先行欠陥の是正を含む**（既存の「コミット」項目も Step 3 中止経路では充足不能だった）
- **カテゴリ D の吸収禁止** → Phase 1 の項目 2 に、D を無限定の「未実施」へ畳まず独立に名指しし目視項目の列挙を残す旨を明記した

配送は台帳 3 件中 3 件が実在し、**独立レビュー不成立のエントリは無い**。Step 2b の独立導出は変更ファイル・変更箇所・「issue 後半は既に古い」の判断で計画と一致し、加えて上記 2 件と G11 アンカー面の指摘を持ち込んだ（不変条件 5 へ反映）。

### 5b の 3 観点

1. **境界条件** — 報告の形が壊れうる端は 5 つあり、`/norm-review` の合格条件と Phase 1 の文言でそれぞれ 1 件ずつ当てる: (a) check スキルが 1 件も該当しない変更（全 6 名が「該当なし」の 1 行に並ぶ最小形） (b) 全 6 件が該当する変更（最大形） (c) `code-reviewer` が Critical/High とも 0 件（**実施したのに書くことが無い**——「実施 / Critical 0・High 0」と書けることを文言で保証する） (d) Step 3 中止経路（項目 3・4 が省かれる） (e) 1c 停止経路（項目 2・3・4 が省かれる）
2. **シンプル化の挑戦** — 新しい状態・プロセス・機構は導入しない（文言のみ）。却下した代替案は 2 つあり、**どちらも `/implement` Step 5 で ADR へ回収する候補である**: (i)「項目 2 と項目 3 を 1 つに畳む」——検証（カテゴリ A〜F）とレビュー（check スキル・エージェント）は別の失敗様態であり、同じ項目に混ぜると片方が沈黙する。(ii)「4b の結果をコミットメッセージ側へ寄せる」——git に残る利点はあるが、**issue が述べる失敗面は意思決定時に読む会話面であって永続性ではない**。報告面を git へ移しても、その場で「実施した回と素通りした回」を区別する手立ては増えない
3. **破壊不変条件 + 検知手段** — 壊れたら即アウトなのは不変条件 1・2（母集団が閉じなくなる＝実施の有無が再び区別できなくなる）。**検知手段は `/norm-review` の 2 クラスの読者だけであり、機械検出器は無い**。`docs/development-principles.md`「『検知手段が無い』と書きかけたら、検知可能になる設計へ変えられないか先に問う」に従って (a) 責務を検査可能な層へ移す・(b) 観測点を作る の 2 種を検討したが、**どちらも成立しない**——`governance:check` に検査器を足せば「規範文が書かれているか」は測れても、報告そのものは会話にしか存在せず git にもディスクにも残らない（これは issue #765 が述べる失敗様態そのものであり、本変更が規範で塞ぐと決めた対象である）。**受容する残余として記録する**

### 受容する残余

- **`/norm-review` 自身は項目 3 の母集団の外にある** — `AGENTS.md`「条件別チェック（トリガー → 参照先）」表 → `.claude/rules/safety-nets.md` の 2 段間接で到達し、4a が名指しする 6 スキルに含まれない。**本変更自身がその実例である**（規範を変える変更では `/norm-review` が走るのに、項目 3 はそれを報告させない）。母集団を広げると閉じた集合を失うため広げず、**名前を挙げて残余とする**
- **項目 3 の母集団は `AGENTS.md`「条件別チェック（トリガー → 参照先）」表の写しである**（不変条件 1 の後段）。表と 4a の括弧書きの乖離を検知する機構は無く、表へ足したスキルが報告母集団から沈黙して落ちうる。`/norm-review` の合格条件 6 で読者側からは当てるが、機構では塞がない
- **本変更の遵守を測る機械検出器は置かない**（上記 3 の結論）
- **`/norm-review` は上限 2 巡で打ち切った** — **2 巡目に入れた 3 件の塞ぎは検証されていない**（`/norm-review`「Step 4 — 次巡、または停止」）。3 巡目は回していない
- **2 巡目で成立したまま残した抜け道**（降格ではなく、予算内に収まらず受容したもの）:
  - *1c の「実装を始めない」が、Step 2 の実装途中で立った停止では字面上すでに偽である* — 既存文言の問題であり本変更が作ったものではない
  - *「実行不能」の限定（実行者・実行環境に由来するもの）が、ツールチェイン欠如のような偶発的失敗を三値のどこにも置けない* — それらは Step 3 の再実行・5 サイクル中止経路が受け持つが、出力節はそう名指ししていない
  - *「該当なし」を 1 行にまとめる書式と、値ごとに根拠を書く要求が、多件数のときの詰め込み基準を欠く*
- **母集団同期の保守義務が、義務を負う人の視界の外にある** — `SKILL.md:128` の「表へ check スキルを足すときは 4a の列挙も同じ変更で直す」を実行するのは `AGENTS.md` の表を編集する人だが、その人が `/implement`「出力」を読む必然性は無い。`AGENTS.md` 側へ書くと G10 の常時ロード面（残り 100 字）を食うため書かず、**記載位置の非対称ごと受容する**（`code-reviewer` M5）
- **項目 2 の値の粒度（カテゴリ）と実行可否の粒度（コマンド）が一致しないカテゴリが 1 つある** — カテゴリ C は `npm test` が全 OS 実行可能で smoke 2 本が Windows 専用のため、非 Windows で C を丸ごと「実行不能」と書くと `npm test` が沈黙する（`code-reviewer` L1）
