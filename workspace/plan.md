# plan: issue #743 — `←` の階層上昇は仕様どおり。選択位置の as-built 明文化と回帰テスト

前提は `workspace/research.md`（実測 2 件・トレーススモークの生データ込み）。**症状は再現せず、
受け入れ条件 1 は現行コードが既に満たしている**（トレース 7 打鍵すべてで親へ上昇・ユーザーの画面確認も一致）。
本計画は残る受け入れ条件 2・3 と、フォローアップの切り出しだけを扱う。

## 裁定（2026-07-28〜29・ユーザー選択）

- 根本原因の確定手段: 協働トレーススモーク → **実施済み・症状は不具合ではないと確定**
- 受け入れ条件 2（展開後の選択位置）: **row 0 を as-built で明文化**
- スコープ: **SPEC 明文化 + 回帰テストのみ**（コードの挙動は変えない）
- 調査中に判明した「results 窓が実件数フィットで 1 行まで縮む」件は、SPEC §4.5 が明示的に選んだ仕様
  （#646 PR2 決定7）であり、変更するなら仕様変更。**別 issue へ切り出す**（本 PR では触らない）

## 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `SPEC.md` §6.1 | フォルダ内容を置換する操作（左右カーソルキー**共通**）の選択を as-built で追記。**2 段で書く**——(1) 打鍵の時点で先頭の候補へ移り、直前の選択を引き継がない (2) 非同期に届く候補の到着では選択を戻さず件数範囲へ収めるだけ（0 件なら選択対象なし）。あわせて「直前まで居たフォルダの行が選ばれることは保証しない」を書く。**裸の「選択位置」という語は使わない**（§6.4 が同じ語で別概念を指すため） |
| `src-tauri/src/egui_shell/search_state.rs`（`mod tests`） | 純粋核テストを 5 本追加（下の「テスト方針」に一覧） |

コード（`launcher_controller.rs` / `view.rs` / `snotra-core`）の**挙動は 1 行も変えない**。
`SPEC.md` の §6.2 / §6.3 / §6.4 / §6.6 / §8.6、各 `CLAUDE.md`、`docs/` は変更しない
（根拠は下の「変更しないと判断した対象」）。

## 実装順序

### Phase 1 — SPEC §6.1 の as-built 明文化

- [ ] `SPEC.md` §6.1 に、候補を置換する操作の選択を **2 段**で追記する——(1) 打鍵時点で先頭の候補へ移る
      (2) 到着時はクランプのみで戻さない・**0 件なら指す行が無い**。射程は**フォルダ内容を置換する前進方向の
      操作に限る**（Escape の復帰は §6.4 の復元であり反例になる。「常に」「すべての」と書かない）
- [ ] 同じ規則を**左右カーソルキー共通**として書く（`←` 側だけに書かない——両者は同じ遷移を共有しており、
      片側だけ書くと `→` が仕様の外へ落ちる。独立導出と `spec-doc` の 2 体が独立に指摘）
- [ ] 「直前まで居たフォルダの行が選ばれることは保証しない」を書く（先頭行が何になるかは列挙順が決める。
      **列挙順そのものは書き写さない**——正本は `snotra-core/src/folder.rs` の `//!`）
- [ ] **裸の「選択位置」という語を使わず「先頭の候補」と書く**——§6.4 の「選択位置」は展開開始前に
      退避した別の値であり、隣接節で同じ語を使うと次の読者が「Escape でも先頭行へ」と読む。
      **語の衝突を避けることで閉じ、§6.4 への相互参照は増やさない**（相互参照は腐る。独立導出の提案）
- [ ] 追記文に**バッククォート付き識別子を持ち込まない**（`SPEC.md` は governance-check の語彙参照元であり、
      書いた識別子が他文書の語彙判定の入力になる。散文で書けば足りる）

### Phase 2 — 純粋核テストで固定（受け入れ条件 3）

- [ ] `left_twice_climbs_two_levels`: `C:\a\b\c` から 2 段上昇し、各段で現在ディレクトリが追随し token が進むこと
- [ ] `left_at_drive_root_is_noop`: ドライブルート `C:\` と UNC 共有ルート `\\srv\share` で親が `None`（§6.2 終端）
- [ ] `folder_navigation_resets_selection_to_first_row`: **非ゼロへ動かしてから**突入・遷移し、`enter_folder` と
      `navigate_folder` の**両方**で選択が先頭へ戻ること（`rust-search-state` 指摘: 0 から始めると何も実証しない）
- [ ] `arriving_rows_clamp_selection_without_resetting_it`: ナビ後に選択を動かし、行の差し替えで**戻らずクランプ
      されるだけ**であること（SPEC の 2 段目の裏付け＝「常に先頭行」が偽である側を固定する）。
      **数値は非退化のものを使う**——`clamp_selected` は `idx.min(len - 1)`（`search_state.rs:374-380`）ゆえ、
      既存様式の 3 件→1 件では `2.min(0) == 0` となり**無条件リセットと区別が付かず何も証明しない**。
      5 件・選択 3 → 4 件到着で **3 のまま**（`3.min(3)`）を主軸に置く
- [ ] `arriving_empty_rows_leaves_no_selectable_row`: 0 件到着では選択が 0 でありながら**指す行が無い**こと。
      `set_results` を空で呼ぶテストは既存に 1 件も無く（grep 0 件）、空フォルダは実在しうる境界である。
      **4 本目に同居させない**——「0 に戻さない」と名乗るテストに「0 になる」ケースを入れると名実が食い違う。
      2 つの 0（方針としてのリセット / 有効な index が無いための 0）は別物であり、doc コメントで区別する
- [ ] 5 本すべてで、`navigate_folder` を呼ぶ前に必ず `enter_folder` を通す。**`navigate_folder` は
      `folder == None` でも黙って通る**（`search_state.rs:250-258` は `if let Some(f)` の外で filter クリア・
      選択 0・世代 bump を行う）ため、突入を忘れたテストは何も検証しないまま緑になる
- [ ] 5 本の doc コメントに射程を 1 行書く: **固定するのは状態核の合成であって、キー割り当てと driver の分岐ではない**
      （`on_nav_keys` の `←` 分岐を消してもこの 5 本は緑のまま——ラウンド 2 で `if false && left` にして実測済み。
      false green を読者に許さない）

### Phase 3 — 記録と切り出し

- [ ] issue #743 へ調査結果をコメントする（トレース実測の要点・症状が不具合でないこと・誤読の機序）
- [ ] フォローアップ issue A（仕様変更）: results 窓の高さを「常に visible_rows 分を確保」へ変える提案。
      SPEC §4.5 と #646 PR2 決定7 を覆す変更であること、0 件時の扱い（現在は高さ 0 = 非表示が
      `present_results` の連言④）と作業領域下端クランプとの相互作用を本文に書く
- [ ] フォローアップ issue B（type:feat）: フォルダ展開中に現在フォルダが UI に出ない（§6 で任意扱い・
      #532 SU3 M2 Task 3 で見送り）。誤読の実例として本件を引く

### Phase 4 — 検証

- [ ] `cargo test -p snotra` が緑（`.rs` 編集では PostToolUse hook が自動実行し沈黙 = 合格だが、明示的に 1 度回す）
- [ ] `npm run governance:check` が緑（**`SPEC.md` 単独編集では PostToolUse は何も走らせない**——沈黙は合格を
      意味しない。手で回すことが唯一の検査）

## 不変条件

1. **コードの挙動を変えない。** 差分は `SPEC.md` とテストコードだけ。検知は `git diff --stat` で足りる
   （製品コードのファイルが出たら違反）
2. **SPEC は実装より強い主張をしない。** 「置換後は必ず先頭行」と書かないこと——**一次資料で確認済み**:
   `folder_load_pending` は `launcher_controller.rs:209,489`（起動系 2 箇所）でしか参照されず、
   `move_selection` は同 1030/1033 で無条件に走り、`set_results` は `clamp_selected` でクランプするだけ
   （`search_state.rs:192-196`）。よってロード窓中の `↑↓` は到着後も残り、選択は 0 でない。
   **破れると規範を守る読者を誤りへ導く**（`AGENTS.md`「全称表現は前提条件とセットで書く」）
3. **左右カーソルキーの対称を崩さない。** 選択位置の規則は両者が共有する遷移の性質であり、片側だけに
   書けば文書上の非対称が残る。破れの検知は「§6.1 を読んで `→` の選択位置が分かるか」の 1 問で足りる
4. **純粋核テストがカバーするのは状態核の合成だけである。** `←` が Folder 分岐へ入ること自体
   （`on_nav_keys` の分岐）は driver 側にあり、`AppHandle` を要するためユニットテストできない
   （`rust-search-state` が `launcher_controller.rs` に `mod tests` が 0 件であることまで確認）。
   **この残余は受容し、根拠をトレース実測として PR 本文と issue コメントに残す**。Phase 2 の doc コメントが
   「テストが緑だから ← の分岐も守られている」という誤読への歯止めになる
5. **一時計測の痕跡を残さない。** スモーク用トレース（`probe743:*`）と一時テスト（`snotra-core/tests/tmp_probe_743.rs`）は
   撤去済みであり、PR の差分に現れてはならない（`git status --short` が `workspace/` のみであることを確認済み）

## テスト方針

- 追加は `src-tauri/src/egui_shell/search_state.rs` の `#[cfg(test)] mod tests` 内 5 本のみ（Phase 2 の一覧）。
  実パスは実機トレースで観測した形（`C:\Toolbox\ghost-launcher`）を使ってよいが、階層の深さが要るテストは
  `C:\a\b\c` のような合成パスでよい
- **既存テストと重複しない**ことを確認済み: `parent_dir_drive_and_unc_roots` は自由関数だけを測り、
  `SearchState` インスタンスの `parent_dir()`（= `←` が実際に通る合成経路）を呼ぶテストは現状 0 件
  （`spec-doc` と `rust-search-state` が独立に grep で確認）
- 境界条件: (a) 2 段上昇の中間状態 (b) ドライブルート終端 (c) UNC 共有ルート終端 (d) 非ゼロ選択からの遷移
  (e) 到着時のクランプ（行が減らない場合／減る場合／**0 件**）。(f) folder filter のクリアは既存
  `navigate_folder_bumps_gen_and_clears_filter` が持つため追加しない
- 検証コマンド: `cargo test -p snotra`（カテゴリ A）/ `npm run governance:check`（カテゴリ F）
- **スモーク（カテゴリ C）・目視（カテゴリ D）は不要**: trace イベント名・hotkey・表示経路・フォント登録の
  いずれにも触れない。`←` の実挙動は実機協働トレースで測定済みであり、再スモークで新しい情報は出ない。
  ただし**「挙動を変えていない」の根拠はそのトレースであってテストではない**ことを PR 本文に明記する

## SPEC.md 更新要否

**要**。挙動変更に伴う更新ではなく、未記述だった as-built の明文化である（`.claude/rules/spec.md`
「as-built を記述する」）。§6.1 の項目内に追記するのみで**セクション番号は動かさない**（§6.2〜§6.6・§7 はそのまま）。
§8.6 の状態遷移図は確認済みで、選択位置に言及せず `FolderExpansionMode --> FolderExpansionMode: ArrowLeft [parent exists]`
を既に持つ（図・実装・本計画は整合するため変更しない）。

## 変更しないと判断した対象（影響するケースを列挙して裏付ける）

| 対象 | 判断根拠 |
|---|---|
| `launcher_controller.rs` の `←` 分岐 | 実機トレースで 2 回とも親へ上昇。仕様どおりでバグは否定済み |
| `view.rs` の `←` の読み | 非破壊 `key_pressed` で読むだけ。処置は controller 側（分担は同ファイルの doc が明記） |
| `snotra-core/src/folder.rs` のソート順 | as-built の主張は選択の index であって行の内容ではない。順序は既存 2 テストが固定済みで、足すのは gold-plating |
| `history.rs` の展開履歴記録 | 誤読の機序に関与するが、記録規則（`→` と Results からの `←` は記録・展開中の `←` は記録しない）は既決事項 |
| `SPEC.md` §6.2 / §6.3 / §6.4 / §6.6 / §8.6 | §6.2=終端維持 / §6.3=打鍵時のリセットは別機構 / §6.4=別概念（復元値）/ §6.6=列挙失敗行も同じクランプ規則に乗るため二重に書かない / §8.6=選択位置に言及しない |
| `results_view.rs` のスクロール | #714・PR #742 の射程。表示側は選択の写しであって別の真実ではない |
| `docs/adr/` | 否定の知識が生じていない（「行位置追従を採らず先頭行」は却下ではなく as-built の記述） |

## セルフレビュー

**ラウンド 3 まで回して打ち切った**（上限）。台帳は毎ラウンド 3 エントリ・計 9 件すべて実在で、
**独立レビュー不成立のエントリは 1 件も無い**。ラウンドごとの結果:

- **R1**: スカウトの要対処 0。Step 2b が「`row 0 固定` は全称として偽」を提示 → 私が一次資料で再照合して成立し
  **要対処へ昇格**。SPEC 文案を 2 段へ、テストを 2 本 → 4 本へ改訂
- **R2**: 要対処 0。4 本を実装して 46 件緑を実測、`←` 分岐を `if false && left` で殺しても緑のままを実測（射程の裏付け）。
  Step 2b の指摘 2 件（0 件到着の境界・「選択位置」の語を避ける）を反映。スカウトの軽微 1 件は**降格**
  （`VOCAB_DOCS = ["SPEC.md"]` を自分で開き、計画が書いていない主張への反証だったため）
- **R3**: **要対処 2 件**（テスト④の数値が退化していて何も証明しない／0 件ケースの名実不一致）+ Step 2b の新規指摘 1 件
  （`navigate_folder` は `folder == None` でも黙って通る）。**いずれも反映済み**（テスト 4 本 → 5 本）

**打ち切りの残余（これだけが未レビュー）**: 上の R3 の反映は最終ラウンドの後に行ったため、独立レビューを受けていない。
反映内容は 3 件とも「答えが決まっている計画の修正」であり、核心（`clamp_selected` の退化・`navigate_folder` の
無ガード）は私が一次資料で再照合済みである。**未確定欄に `- [ ]` は 1 つも残っていない。**

5b の 3 観点:

1. **境界条件**: (a) 2 段上昇の中間 (b) ドライブルート終端 (c) UNC 共有ルート終端 (d) 非ゼロ選択からの遷移
   (e) 到着時のクランプ（**非退化の数値**で・行が減らない/減る）(f) 0 件到着。いずれも検証ケースを持つ
2. **シンプル化**: 新しい状態・フラグ・スレッド・プロセスを 1 つも導入しない（コード変更ゼロ）。SPEC 側も
   §6.4 への相互参照を増やさず、**語の選択だけで**概念衝突を閉じた（相互参照は腐るため）
3. **破壊不変条件 + 検知手段**: 挙動を変えないため「戻ってこない」系のリスクは無い。守るべきは
   **「挙動を変えていない」こと自体**で、検知は `git diff --stat`（製品コードのファイルが出たら違反）・
   `cargo test -p snotra`・`npm run governance:check` の 3 つ。加えて **false green の検知**として、
   `←` 分岐を殺しても 5 本が緑である事実を実測済みで、テストの doc コメントに射程として残す

**範囲外だが記録**: 独立導出が「§6.3 の絞り込み記述は列挙失敗のエラー行表示中だけ偽」（フィルタ非適用）という
既存 SPEC の不正確さを見つけた。本 issue の射程外につき本 PR では触らない。

## 未確定（実装前に潰す）— ラウンド 3

- [x] 症状が実在するか — **測った**: 協働トレーススモークで 7 打鍵すべて親へ上昇（`research.md` 事実 5）。
      ユーザーの画面スクリーンショット 3 枚とも一致。**不具合ではない**と確定
- [x] 展開後の選択位置をどう仕様化するか — **裁定**: 「row 0 を as-built で明文化」（ユーザー選択・2026-07-28）。
      代替案「直前まで居たフォルダを選択」は非同期到着後の行探索状態が要り、filter・エラー行・未着窓と
      相互作用するため却下
- [x] 「row 0 固定」と無条件に書けるか — **測った（一次資料で再照合）**: 書けない。到着経路はクランプのみで
      ロード窓中の `↑↓` が残る（不変条件 2 に根拠を記載）。**SPEC 文案を 2 段へ変更した**
- [x] 現在フォルダの可視化を本 issue に含めるか — **裁定**: 含めない。type:feat の別 issue へ（ユーザー選択・2026-07-29）
- [x] results 窓の高さ（実件数フィット）を直すか — **裁定**: 本 PR では直さない。SPEC §4.5・#646 PR2 決定7 を
      覆す仕様変更のため独立 issue へ（ユーザー選択・2026-07-29）
- [x] `/symmetric-check` と `/state-check` を起動するか — **決めた**: 起動しない。`AGENTS.md`「条件別チェック」の
      トリガーはいずれも「対称ペアを**変更**」「UI モード・ガード条件を**追加/変更**」であり、本計画は
      挙動を変えない文書とテストのみだから。**ただし両者が守る中身は自分で実施した**——対称は Phase 1 で
      左右共通則として書くことで閉じ、状態側は §8.6 を読んで整合を確認済み（上の「SPEC.md 更新要否」）
