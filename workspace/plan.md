# plan — SPEC.md §7.2 タブ補完 (#408)

## 種別判定（AGENTS.md Step 0）

**doc 同期**（バグでも仕様変更でもない）。実装は既に 7 タブで稼働（`app.rs` TabId が真実源）。SPEC §7.2 が as-built に対し網羅欠落しているだけ。SPEC を実装に寄せる方向で、挙動変更を一切伴わない。

## 変更ファイル一覧

| ファイル | 変更 |
|---|---|
| `SPEC.md` §7.2（261-305 付近） | `[オープナー]` タブの後・`### 7.3` の前に、`[インスタントコマンド]` と `[バックアップ]` の 2 エントリを追加 |

他ファイルの変更なし（doc-only、コード・テスト・docs/ への波及なし）。

## 実装内容（逐語）

§7.2 の `[オープナー]` ブロック末尾（`- ツール追加/編集/削除/並び替え（順序 = 優先度）` の直後・`### 7.3` の前）に以下を挿入する。**順序は `TabId::ALL` に合わせ InstantCommand → Backup**:

```
`[インスタントコマンド]` タブ:

- プレフィックス設定・コマンド一覧の追加/編集/削除/複製（詳細は §19.8 参照）

`[バックアップ]` タブ:

- config.toml のエクスポート/インポート・設定フォルダを開く（詳細は §13.3 参照）
```

## 実装順序

単一フェーズ。SPEC.md に 1 回の Edit。

## 不変条件

1. **DRY**: フル複製しない。各エントリは 1 行の要約 + `（詳細は §N 参照）` のポインタに留める（§19.8/§13.3 が SSOT）。
2. **順序整合**: 7 タブの並びは `TabId::ALL`（General/Search/Index/Visual/Opener/InstantCommand/Backup）と一致させる。挿入位置は Opener と §7.3 の間。
3. **セクション番号不変**: §7.2 内バレット追加のみ。`### 7.x` 子番号・`## N.` 番号を動かさない。
4. **参照先の実在**: §19.8（line 920）・§13.3（line 539）は実在を確認済み。リンク切れを作らない。
5. **既存 5 タブの記述は不変**: 既存エントリのテキストには触れない。

## テスト方針

doc-only のため自動テストなし。検証は `docs/build-commands.md` カテゴリ外（Rust/TS 変更なし）。目視確認:
- §7.2 のタブ列挙が 7 タブになり `TabId::ALL` 順と一致するか
- 参照先 §19.8/§13.3 が実在し内容が対応するか
- 既存 5 タブの記述が無改変か（`git diff` で挿入のみ・削除 0 を確認）

## SPEC.md 更新要否

本タスク自体が SPEC.md の更新。挙動変更は伴わない（as-built 記述の網羅補完）。docs/architecture.md 等への波及なし。

## セルフレビュー

`/start-issue` Step 5b に基づく（doc-only のため該当観点のみ）:

1. **対称コードパス**: 該当なし（doc-only、コードパス変更なし）。
2. **影響範囲の網羅性**: §7.2 のタブ列挙は他に存在しない（§18.6/§19.8 の列挙は #404 で是正済み・本タスク対象外）。`app.rs` TabId を真実源として 7 タブを裏取り済み。
3. **境界条件**: 挿入位置（Opener と §7.3 の間）・順序（InstantCommand→Backup）を明示。空行体裁を既存ブロックに揃える。
4. **リソース管理**: 該当なし。
5. **既存パターンとの整合**: `[オープナー]` の `（詳細は §N 参照）` ポインタ方式を踏襲＝新規パターン導入なし。
6. **YAGNI 違反**: なし。フル複製を避け参照に留める＝issue の要求範囲ちょうど。
7. **シンプル化**: 2 バレット追加が最小。状態・インターフェース導入なし。
8. **破壊不変条件**: 該当なし（doc-only、Win32/フック/IPC 無関係）。

### check スキル判定

- `/plan-review`: 本来 always だが、**doc-only・2 バレット・コードパス/不変条件/スコープの検証対象が実質ゼロ**のため、多エージェント fan-out は不均衡（チーム憲章「やりすぎでは を歓迎」）。真実源（`app.rs` TabId）と参照先（§19.8/§13.3 実在）は本計画内で裏取り済み。`/plan-review` Step 2b も「局所的な計画では省略可」に該当。→ インラインのセルフレビューで代替。
- `/symmetric-check` `/cache-check` `/state-check` `/race-check`: いずれも非該当（doc-only）。
