# 実装計画: コードコメントを要点中心に簡潔化する（issue #1185）

調査は `workspace/research.md`、独立導出は `workspace/plan-review-comment-compaction.md`、
敵対的調査は `workspace/adversarial-1185.txt`。

要求判断はユーザー回答（2026-08-25）で確定——**ハイブリッド**／**issue 記載の 3 ファイル**。
ただし**「着地させる」の向きは既存 ADR が既に決めている**（下の A-2 の線引き）。

## 目的と受け入れ条件

コードコメントを**現在のコードを理解・変更するために必要な情報へ圧縮**し、証拠と歴史を正本へ分離する。

| # | 受け入れ条件 | 確かめ方 |
|---|---|---|
| A1 | `docs/comment-guidelines.md` に「短く保つ」基準が在り、それが**密度と配置の基準**として書かれている（行数・文字数の上限を導入していない＝非目標に触れない） | 目視。**行数を受け入れ条件にしない** |
| A2 | 「設計理由」と「証拠・履歴・実測」の置き場所が表で明文化され、**A-2 の 3 分類と矛盾しない** | 目視 |
| A3 | module rustdoc / item rustdoc / inline / ADR・docs の役割分担が「配置基準（3 層）」の拡張として整理されている | 目視 |
| A4 | 3 ファイルの列挙シンボルが圧縮され、**責務宣言・不変条件・契約が残っている** | Phase 4 の逆向き監査 |
| A5 | `index_tree.rs` の `//!` から**層違いの内容**（実装詳細）が item doc へ降りている | 目視。**行数目標にしない**（B-5） |
| A6 | 既存の正準形参照が壊れていない | `npm run governance:check` |
| A7 | `cargo doc` が緑 | カテゴリ A の `cargo doc` 行 |
| A8 | 圧縮で**版リテラル**が新たに残っていない | `grep -nE 'v[3-7]' snotra-core/src/search/build.rs` |

## 変更ファイルと対象シンボル

| ファイル | 対象 | 完了条件 |
|---|---|---|
| `docs/comment-guidelines.md` | 「配置基準（3 層）」拡張／「rustdoc の様式」末尾／「歴史メモの様式」／「定型ラベル」の実例／「TSDoc の様式」（死んだ節）／新設「短く保つ」節 | 1, 2, 3 |
| `.claude/rules/comments.md` | 「読む正本」箇条へ新設節のポインタ 1 行 | 6 |
| **`RETROSPECTIVE.md`** | `:23` の全形参照を新しい見出し名へ（D-1） | 3 |
| `snotra-core/src/indexer.rs` | `CachedMasks` / `LowerFileName` / `CachedLower` / `root_roles` / `LoadOrScanStats`（`:419-484`） | 4, 5 |
| `snotra-core/src/search/build.rs` | `DerivedStrings` / Wave 1・2 / `assemble` / **版リテラル**（`:4`, `:427-435`, `:501`, `:523-526`, `:90`） | 4, 5 |
| `snotra-core/src/index_tree.rs` | module `//!` / `NameArena` / 長文塊 | 4, 5 |

**触らない**: `PERFORMANCE.md`（A-2 により**新規の着地はほぼ発生しない**）・ADR・`docs/superpowers/`。
**`snotra-core/CLAUDE.md`**: `//!` から降ろした内容の受け皿が要るときのみ（B-5）。
**新規 ADR**: 立てない（A-2 により ADR への着地が不要になったため。C-2 は解消）。

### 節名を変えると偽になる散文（実測・2026-08-25／A-6 で訂正済み）

| 節 | 参照元 | 機構が捕まえるか |
|---|---|---|
| 第一原則: コメントは「なぜ」を書く | `.claude/rules/comments.md:15`・`.claude/agents/code-reviewer.md:28`（切り詰め形） | **捕まえる** |
| 同上（**全形**） | `snotra-core/src/indexer.rs:53`・`RETROSPECTIVE.md:23` | **捕まえない**——`HEADING_REF` のラベル文字クラスが `[^「」\n]` で、見出し名に `「なぜ」` が入れ子になっているため**一致が生成されない**（`lib.mjs:177` を読んで実測） |
| 名指しと正本の指名 | `.claude/rules/comments.md`・`snotra-core/src/index_tree.rs:1126` | **捕まえる** |
| rustdoc の様式 / 日本語の折返し / 言語（日英） | `.claude/rules/comments.md` ほか | **捕まえる** |
| 歴史メモの様式 | `PERFORMANCE.md` / **`docs/adr/ADR-measurement-record-provenance.md`** | PERFORMANCE.md は捕まえる。**ADR は捕まえない**（`docs/adr/` は走査元から除外） |

⇒ **既定は「節名を変えない」**（節の**追加**と節**内**の再構成に留める）。例外は下の D-1 の判断。

## A-2 の線引き（実測値をどう扱うか・**ハイブリッドの実行形**）

`PERFORMANCE.md`「この文書へ記録するときの規約」と `ADR-measurement-canon-in-code-doc` が既に決めている:

> **測定値の正本は `PERFORMANCE.md` に限らない。…寄せ先が無いときはコードの doc を正本にしてよい。**
> **この文書を「今も支えている値」と「歴史」に分けない。**…今も設計を支えている値は**コードの doc を正本にする**形で表す。

| 分類 | 例 | 採る手 |
|---|---|---|
| **着地済み**（`PERFORMANCE.md` の節を正準形で指している） | `indexer.rs:60/356/508/1060/1102/1718`・`build.rs:77/191/344/460`・`index_tree.rs:62/147/179` | **そのまま。外さない** |
| **着地先の無い判断根拠** | `index_tree.rs:822`（段数 2/4/8 段）・`:857`・`:884`・`:911`・`:477`・`:167-180`、`indexer.rs:79`・`:353-363` | **コードの doc を正本として残す**。`PERFORMANCE.md` へ新設すると出所が写しになり `AGENTS.md`「照合は SSOT に対して行う」に触れる |
| **判断を支えていない経緯値** | 「反復 N で〜した」型の変化量・却下案の詳細比較・レビュー時の正当化 | **`#NNN` へ委ねて外す。ここが本来の圧縮対象** |

⚠ **「実測値をコメントから追い出す」を機械的に適用してはならない。** 既に正しく置かれている
`build.rs:77`（値を残し経緯を委譲）の形まで壊す。

## 実装順序

### Phase 1 — 規約の改訂（コードより先）

- [x] 1a: 「配置基準（3 層）」へ**層の中の分担**の表を追加（`//!` / `///` / `//` / ADR / `docs/`）。
      節名は変えず節内の追加に留めた（完了条件 3）
- [x] 1b: 「歴史メモの様式」から「長くてよい」を外し、**判定基準は残して射程を歴史・却下案へ絞った**（A-4）
- [x] 1c: 新設「短く保つ」節——**層の問題として 3 つの問い**で書き、hard limit を置かない。
      測定値の置き場は `ADR-measurement-canon-in-code-doc` を**指すだけ**にして写しを作らない
- [x] 1d: 死んだ `## TSDoc の様式` を 4 項 → 2 項へ整理（B-4）
- [x] 1e: 「定型ラベル」の `回帰テスト:` の実例を `src-tauri/src/egui_shell/view.rs` へ（A-1）
- [x] 1f: 「**同ファイル**」を `snotra-core/src/index_tree.rs` へ（B-1）
- [x] 1g: `.claude/rules/comments.md` の「読む正本」へ「短く保つ」のポインタ 1 行（完了条件 6）
- [x] 1h: **D-1**——`## 第一原則: コメントは理由を書く` へ改名し、**同じ変更で**全形参照 2 本
      （`snotra-core/src/indexer.rs` / `RETROSPECTIVE.md`）を書き換えた。切り詰め形 2 本は触っていない
- [x] 1i: **D-1 の検算**——見出し参照の照合件数が **357 → 359** へ増えた（＝全形 2 本が新たに照合対象に入った）。
      **増えなければ死角は消えていない**という判定で、増えたので消えた
- [x] 1k: **（計画外・実装中に判明）**「第一原則」の「書く価値があるもの」が**新設節と正面から矛盾**していた——
      「過去の事故（実測値・再現条件・issue 番号つきの経緯）」「設計判断の理由と却下した代替案」が
      経緯と却下案の詳細をコメントに正当化していた。**現在の形の理由**として書く形へ直し、経緯は `#NNN`、
      却下案の詳細は ADR へ振った（知見を捨てるのではなく置き場を分ける・非目標に触れない）
- [x] 1j: `npm run governance:check` が緑（見出し参照 361 件）

### Phase 2 — シンボルごとの事前確認（圧縮の前に必ず打つ）

機構が見ないので手順で守る。**各シンボルについて 3 点**:

1. **外からの名指し**——その doc の**中身**に依存する散文が無いか（`.md` を grep）
2. **規約の模範例か**——`docs/comment-guidelines.md:30` が `IndexTree::file_key_into` の doc を
   **逐語で引用**している（B-2。照合する機構は無い）。⇒ **この doc は圧縮対象から外す**
3. **A-2 の 3 分類**を塊ごとに当てる（C-5。行数は冗長度の代理指標にすぎない）

⚠ **`grep` の否定文字クラスを日本語に使わない**——バイト単位に落ちて**静かに 0 件**を返す
（調査中に私と敵対枠が独立に踏んだ）。リテラル照合か ripgrep を使う。

- [ ] 2a: 8 シンボル＋長文塊に対して上の 3 点を実施し、結果を本節へ追記
- [ ] 2b: 圧縮対象から外すものを確定する（既定で `file_key_into` を含む）

### Phase 3 — 圧縮（ファイル単位でコミット）

| 残す | 外す（`#NNN` へ委譲） |
|---|---|
| 責務宣言（`//!`・`snotra-core/CLAUDE.md:10` が正本に指名） | 過去の事故の経緯 |
| 不変条件と破った場合の影響 | 却下案の詳細な比較 |
| 順序・同期・所有権の局所制約／非自明な失敗モード | レビュー時だけ必要だった正当化 |
| **着地済み・着地先の無い判断根拠の実測値**（A-2 の 1・2 分類） | **判断を支えていない経緯値**（A-2 の 3 分類） |

- [ ] 3a: `index_tree.rs`——`//!` は**層違いの内容を item doc へ降ろす**方向で縮める（B-5）
- [ ] 3b: `search/build.rs`——長文塊の圧縮に加え、**版リテラルを落として variant 名だけ残す**（A-3。
      `indexer.rs:36-42` が「版のリテラルを他所へ焼き込まないこと」と名指しで禁じている）
- [ ] 3c: `build.rs:29` の「構築 68 → 58 ms」を落として `PERFORMANCE.md` の節を指す形へ
      （`PERFORMANCE.md:2071` は「89 → 58 ms」。**同一コミット `78650d6e` 由来ゆえ履歴では
      どちらが正しいか決まらないが、写しを 1 つに畳むことは値の正誤と独立に正しい**。
      値の正誤は本 issue の射程外）
- [ ] 3d: `indexer.rs`——`LoadOrScanStats`（`:419-484` の 20/23 行 2 塊）を筆頭に
- [ ] 3e: 各 Phase 後に `cargo doc --workspace --no-deps --document-private-items` と
      `cargo test -p snotra-core` が緑（A-8 が言うとおり **hook は沈黙する**）

### Phase 4 — 逆向き監査（完了条件 5 の唯一の検証手段）

**適合検査ではなく、「差分が消した行の不変条件を名指し、再確立地点を探す」向きで見る。**

**手段は測って決めた**（C-3）: `**…**` 強調行は 3 ファイルで **625 行**あり雑音過多。
**不変条件語**（`不変|保証|契約|必ず|してはならない|禁じ`）を含むコメント行は **79 行**
（indexer 51 / index_tree 24 / build 4）で、1 行ずつ照合できる規模である。

- [ ] 4a: 圧縮前後で不変条件語を含むコメント行の集合を差分する
- [ ] 4b: 消えた各行について「同じ内容が圧縮後の版か正本のどちらかに在るか」を照合し、無ければ復元
- [ ] 4c: `npm run governance:check` と `npm test` が緑

## 不変条件と異常系

- **`docs/comment-guidelines.md` の節名を変えない**——**例外は D-1 の 1 件のみ**（承認済み）。
  その 1 件でも参照元 4 本の内訳（全形 2 本＝要手直し／切り詰め形 2 本＝前方一致で不要）を
  測ってから動かす
- **責務宣言は消えない**（`snotra-core/CLAUDE.md:10`）
- **`IndexTree::file_key_into` の doc は変えない**（規約が逐語引用・機構は見ない）
- **版リテラルを新たに書かない**（`indexer.rs:36-42` の禁止条項）
- 異常系: 圧縮は**行の再折返しを必ず伴う**ので `G-folded-code-spans` / `G-folded-heading-refs` が
  最も発火しやすい（A-7）。基線は今日緑（コードスパン 18107 件 / 折れうる位置 20 件）
- 異常系: intra-doc link 切れは **CI でのみ**赤くなる（A-8）。Phase 3e でローカルの `cargo doc` を打つ

## テスト方針と検証コマンド

挙動を変えないので挙動テストは増やさない。カテゴリ A（`cargo doc` を含む）とカテゴリ F
（`governance:check`）。**新しい機械検査は足さない**——issue の非目標（hard limit を導入しない）が
直接それを禁じる。（`ADR-comment-guideline-delivery-by-pointer` の却下 3・4 は**特定 2 案**への
却下であり別形の検知器一般を排除しない。また同 ADR の却下理由 1（`G-area-budget` が赤になる）は
**その検査が撤去済みゆえ現在成り立たない**〔B-3〕——退けるなら残る 3 理由で退ける。）

## SPEC.md・関連文書の更新要否

- `SPEC.md`: **不要**（`grep -nE "indexer|index_tree|search/build" SPEC.md` が 0 件・B-7）
- `snotra-core/CLAUDE.md`: `//!` から降ろした内容の受け皿が要るときのみ
- `PERFORMANCE.md`: **原則不要**（A-2）

## 未確定（実装前に潰す）

（なし——下の 5 件はすべて解消済み。経緯は各行に残す）

- [x] **正本候補どうしの数値食い違い①** — `git log -S` で辿ると `68` と `89` は**同一コミット
      `78650d6e` 由来**で、履歴では裁定できない。⇒ 値の正誤を決めずに**写しを畳む**（Phase 3c）
- [x] **「判断を支えている値」の分水嶺** — 私が新たに決める必要は無かった。
      `ADR-measurement-canon-in-code-doc` と `PERFORMANCE.md` の記録規約が既に 3 分類を持つ（A-2）
- [x] **規約が肥大しないか** — 行数を受け入れ条件から外し（非目標に触れる）、**死んだ TSDoc 節の
      整理**という既に名指し済みの縮小先を Phase 1d に置いた
- [x] **完了条件 5 のレビュー手段**（C-3）— 代表入力で測り、不変条件語 79 行を手段に決めた
- [x] **ADR への着地の可否**（C-2）— A-2 により ADR への着地が不要になり、問い自体が消えた

## 人間の裁定（決定済み）

- **D-1: 見出しから `「なぜ」` を外す。採用**（2026-08-26 承認）。
  見出し名の入れ子鉤括弧による**構造的な死角が消える**——`HEADING_REF` のラベル文字クラスが
  `[^「」\n]` ゆえ、全形で書かれた参照は**一致が生成されず**検査に載らない。

  **前方一致であることは実測で確かめた**（`G-heading-refs.mjs:61` の
  `anchors.some((a) => a.startsWith(normAnchor(label)))`、正規化は `**`・バッククォート・`「」`・
  空白の除去）。頭に「第一原則」を保つ限り、**切り詰め形の 2 本**
  （`.claude/rules/comments.md:15` / `.claude/agents/code-reviewer.md:28`）は**手当て不要で緑のまま**。

  **手で直すのは全形の 2 本だけ**——`snotra-core/src/indexer.rs:53` と `RETROSPECTIVE.md:23`。
  これらは新しい見出し名へ書き換えて初めて検査対象に入る（現状は照合すら生成されていない）。

  ⇒ **`RETROSPECTIVE.md` が変更ファイルに加わる。** 独立レビューの再実行は行わない——この変更は
  **A-6 (3) が提案し、その所見を出した当の独立導出が既に射程に入れていた**ものであり、
  同じ枠組みを当て直しても同じ結論を再導出するだけである（`/plan-review`「独立導出は 1 回で終える」）。

## セルフレビュー

- リスク: **高**（ガバナンス文書・rules の変更を含む）
- plan-review: **独立導出 1 体**（Step 2b・`--deep`）
- エージェント数: **2**（敵対的調査 1 + 独立導出 1）
- 要対処: 敵対枠 6 件採用／導出 8 件（A-1〜A-8）と軽微 7 件（B-1〜B-7）を反映。
  **A-2・A-6・A-1 は根拠を自分で再照合して成立を確認**（降格 0 件）
- 未検証: C-1（入れ子鉤括弧を持つ見出しの他文書での数え上げ）は本 issue の射程外として残す。
  C-4（`cargo doc` / `cargo test` の実行）は Phase 3e で実施

## plan-review 結果

- リスク: 高
- レビュー方式: 独立導出 1 体（Step 2b）
- エージェント数: 1

### 要対処

- **A-2 ハイブリッドの実行形** — 計画へ「A-2 の線引き」節として反映 — `PERFORMANCE.md:38` と `ADR-measurement-canon-in-code-doc:51-52` を自分で読んで確認
- **A-6 「第一原則」節の死角** — 参照元の表を訂正し D-1 として人間の裁定へ — `lib.mjs:177` の `HEADING_REF` と見出しの実体を照合
- **A-1 定型ラベルの実例が腐っている** — Phase 1e — `grep -Fc` で 3 ファイル 0 件、`src-tauri` に 3 件を確認
- A-3 版リテラル / A-4 自己矛盾 / A-5 セーフティネット / A-7 折返し / A-8 cargo doc — 各 Phase と不変条件へ反映
- B-1・B-2・B-4・B-5・B-7 — Phase 1f・Phase 2・Phase 1d・Phase 3a・SPEC 節へ反映

### 軽微

- B-3（`ADR-comment-guideline-delivery-by-pointer` の却下理由 1 が現在成り立たない）— 本 issue で統合案を再検討しないので影響なし。テスト方針の節に注記のみ

### 未検証

- C-1（他文書の入れ子鉤括弧見出し）— 射程外
- C-4（ビルド系）— Phase 3e で実施

### 判断

- 実装着手: **可**（D-1 決定済み・全体承認済み）

## 人間レビュー

- [x] 承認済み — 2026-08-26 / 問い: "**D-1: 「第一原則: コメントは「なぜ」を書く」の見出しから
  `「なぜ」` を外すか。**" および "`workspace/plan.md` へ注釈を追加いただくか、明示的にご承認ください。"
  / 回答: "D-1 の見出しから外そう。" および "計画承認"
