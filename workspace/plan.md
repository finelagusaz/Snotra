# plan: issue #518 — AGENTS.md「環境制約」節の解体

## 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `CLAUDE.md` | (1) 新節「サブエージェント委譲と worktree」を「チーム憲章」の後に追加し、委譲3項目 + worktree cleanup 1行を移す（太字=指示/後続=理由の形式維持、issue アンカー #439/#435/#489/#431 保持）。(2) :5 の「ワークフロー・事前チェック・環境制約」から「環境制約」を除去 |
| `AGENTS.md` | (1) 「環境制約」節（:85-91）を丸ごと削除（ポインタ項目は行き先なしで削除 — 「ドキュメント参照」節 :19 と重複のため）。(2) 条件別チェック表に引き金行「サブエージェントへ委譲する・worktree を使う → ルート CLAUDE.md「サブエージェント委譲と worktree」」を追加。(3) 冒頭の責務宣言行に変更なし（既に「Claude Code 固有の運用は CLAUDE.md」とあり、移動後に初めて真になる） |

## 命題対応表（移動前 → 移動後。脱落検査の真実源）

| # | 移動前（AGENTS.md 環境制約節） | 移動後 |
|---|---|---|
| 1 | worktree isolation 使用可・場所・gitignore 済み・clean:worktrees | CLAUDE.md 新節 |
| 2 | 並列委譲はファイル境界で衝突予測（直列化・隣接ファイル・リベースは本人）#439 #435 | CLAUDE.md 新節 |
| 3 | 委譲はコンテキスト非継承（パス明示・allowed-tools はインラインを拘束しない・検査対象を変更しながら検査しない）#489 | CLAUDE.md 新節 |
| 4 | 長時間委譲は中断前提で Phase 分割（Phase ごと検証 green 後コミット）#431 | CLAUDE.md 新節 |
| 5 | モジュール固有不変条件は各 CLAUDE.md 参照 | 削除（AGENTS.md:19「ドキュメント参照」+ CLAUDE.md:6 に同内容が現存 — 写しの削除であり命題は残る。注: :91 固有の注釈語「TDD ルール（Win32 依存モジュールをユニットテスト前提にしない等）」は :19 に無いが、実命題は snotra-core/CLAUDE.md:3,:61 で健在のため許容脱落とする） |

項目 3 は内部に独立命題が3つ（非継承 / allowed-tools / 検査対象変更禁止）あるため、移動時に3つとも保持する。**特に allowed-tools 命題は RETROSPECTIVE.md:6 が「圧縮で一度静かに落ちた」と記録する当の命題** — 実装後 grep で単独確認する。

worktree cleanup 行はコマンド名のみ残し `-- --force` 等のフラグ詳細は書かない（AGENTS.md:53 のコマンド二重メンテ回避原則。SSOT は docs/build-commands.md:68 に既記載）。

## 実装順序

1. CLAUDE.md へ新節追加 + :5 文言更新
2. AGENTS.md 節削除 + 表に引き金行追加
3. 対応表で全命題の着地を照合（plan-review Step 2b の独立再導出と突き合わせ）
4. コミット・PR

## 不変条件

- **命題の脱落ゼロ**: 上の対応表 5 行（+ 項目3の内部3命題）がすべて着地する。issue 番号アンカーも保持
- **参照の腐り防止**: 節名「環境制約」の参照は CLAUDE.md:5 のみ（grep 済み・序数参照なし）→ 同一コミットで更新。retrospective SKILL.md:65 は移動によって既存の腐りが**治る**方向
- **到達性**: 委譲の局面で AGENTS.md 条件別チェック表 → CLAUDE.md 新節に到達できる（引き金の地図の流儀維持）

## テスト方針

機構的検査なし（*.md は selectChecks 対象外・沈黙は合格ではない）。代替:
- plan-review Step 2b の独立再導出との差分照合
- 実装後に対応表の各行を移動先 grep で1件ずつ確認
- `/health-check` は次サイクル定期実行に委ねる（本 PR では対応表照合で足りる）

## SPEC.md 更新要否

不要（開発プロセス文書のみ。アプリ挙動・SPEC 記載事項に変更なし）。

## セルフレビュー

1. 対称ペア: 該当なし（文書移動）
2. 影響範囲: 節名・委譲・worktree で AGENTS.md/CLAUDE.md/skills/rules/docs/メモリを grep 済み（research 参照）
3. 境界条件: 項目3の複合命題を分解して対応表に明記
4. リソース管理: 該当なし
5. 既存パターン: CLAUDE.md の太字形式・条件別チェック表の2列形式を踏襲
6. YAGNI: `.claude/rules/delegation.md` 新設案は不採用 — rules はパス起点の自動配送であり「委譲」はパスに紐付かない局面のため、常時読込の CLAUDE.md が正しい置き場
7. シンプル化: 移動+1行追加+2文言更新のみ。書き換え・要約はしない（命題を変えると脱落検査が成立しなくなる）
8. 破壊不変条件: 「命題の脱落」が唯一のアウト。検知は対応表照合 + 独立再導出差分
