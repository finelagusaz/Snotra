# research — SPEC.md §7.2 タブ補完 (#408)

## issue の要約

SPEC.md §7.2「タブ構成と設定項目」は `[全般]`/`[検索]`/`[インデックス]`/`[ビジュアル]`/`[オープナー]` の **5 タブのみ**を詳述しており、実在する 2 タブ（`[インスタントコマンド]`・`[バックアップ]`）のサブセクションを欠く。§18.6/§19.8 のタブ列挙は #404 で実 7 タブに是正済みだが、§7.2 だけが網羅欠落のまま残っている。

**あるべき姿**: §7.2 にポインタ的小エントリを 2 つ追加し 7 タブで整合させる。フル複製はせず `→ §19.8` / `→ §13.3` の参照に留める（DRY）。doc-only。

## 関連コード（真実源の裏取り）

- **`snotra-settings/src/app.rs:52-70`** — 真実源。`enum TabId` の `ALL` は順に **General / Search / Index / Visual / Opener / InstantCommand / Backup**（7 タブ）。issue の主張通りで確認済み。
- **`SPEC.md:261-305`** — §7.2 本文。現状 5 タブ詳述。`[オープナー]` タブ（300-304）が既に `（詳細は §18 参照）` のポインタ方式を採用済み＝補完で踏襲すべき既存パターン。
- **`SPEC.md:539-546`** — §13.3「設定バックアップ」。エクスポート/インポート・「設定フォルダを開く」を詳述。バックアップタブ・エントリの参照先。
- **`SPEC.md:920-924`** — §19.8「設定画面」。プレフィックス設定（デフォルト `@`）・コマンド追加/編集/削除/複製を詳述。インスタントコマンドタブ・エントリの参照先。

## 既存パターン

§7.2 内の `[オープナー]` タブが既に「一覧（詳細は §18 参照）」というポインタ方式。新規 2 タブもこの体裁・粒度に揃える（一覧+操作を 1 行で要約し、詳細セクションへ転送）。

## 技術的制約

- doc-only。コード・テスト・IPC への影響なし。Win32・非同期の論点なし。
- 挙動変更なし＝SPEC は as-built を記述するだけ（実装は既に 7 タブで動作している）。
- セクション番号の整合: §7.2 内へのバレット追加のみで、子セクション番号（§7.3 以降）も `## N.` 番号も動かさない。

## 未解決の疑問

なし。issue が設計判断（DRY ポインタ・参照先 §番号・順序）まで明示しており、真実源も裏取り済み。
