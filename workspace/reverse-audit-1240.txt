reverse-audit-1240 — 削除された太字 bullet の逆向き監査（対応表を読む前に git grep で現在形を探し、その後で対応表と突き合わせた）
対象: git diff d2e9804..174b81c -- snotra-core/CLAUDE.md src-tauri/CLAUDE.md の削除行のうち太字（**…**）を含む bullet 39 本
書式: 元の文の先頭 / 見つかった (file:line) or 見つからない / 対応表と一致したか

== snotra-core/CLAUDE.md ==
01 `IndexInputs` には「変わったら索引を建て直さねば / 見つかった snotra-core/src/engine.rs:77-85（IndexInputs の ///・向きを持たない・show_icons を外した経緯・config_watcher のエッジまで同文） / 一致（写し）
02 config 変更と index 再構築のコヒーレンシ判断は `index_stale` / 見つかった snotra-core/src/engine.rs:7-13（//! 新設段落・4 メソッド・Mutex 内側が根拠・#347/#348-A・消費者と AtomicBool の役割） / 一致（単一・全文）
03 `complete_index_drain` は「ビルド開始時 snapshot == 現在 / 見つかった snotra-core/src/engine.rs:307-309（/// 「一致するときだけ stale を落とす」） / 一致（写し）
04 crate の外から見える名前は `config.rs` の re-export が決める / 見つかった snotra-core/src/config.rs:4-6 / 一致（写し）
05 新しいセクション・設定キーには serde の既定を付ける（#824） / 見つかった snotra-core/src/config/schema.rs:6-11（検知器 2 種・配列要素の例外・SPEC 13.1 まで） / 一致（写し + 差分）
06 新しい永続ファイルの保存先も `Config::config_dir()` から導く / 見つかった snotra-core/src/config/location.rs:3-11・:31-35 / 一致（写し + 差分）
07 env 上書き（`SNOTRA_CONFIG_DIR`）はそのまま使い `Snotra` を付け足さない / 契約本体は見つかった location.rs:44（config_dir_from の ///）・判定核が env を読まない代償と結線の検知器は location.rs:8-11 / **不一致**: 対応表は「Windows の `dirs` 同一性の実測は判定核の結線と同じ段落に含めた」と書くが、`dirs::config_dir()` と `dirs::data_dir()` が同一である旨・危険なのは `config_local_dir()` / `data_local_dir()` である旨は location.rs のどこにも無い（`grep -n "data_dir\|config_local_dir" snotra-core/src/config/location.rs` は 0 件。`config_local_dir` は location/tests.rs:72 の検知器 doc にだけ残る）。主張自体が消えた
08 旧キーの後方互換移行 / 見つかった snotra-core/src/config/migrate.rs:124-145（apply_migrations 本体の行末コメント: `[appearance].max_results`・`[search].top_n_history` > `[appearance].top_n_history`・`max_history_display` の 2 層）・//! L7-8 / 一致（写し）
09 migration を足すときは系統ごとの private fn として書く / 見つかった migrate.rs:7-8（//!）・6 本の private fn は migrate.rs:111/122/158/176/187/200 に実在 / 一致（写し。fn 名の列挙は消え、`mod` 本体が正本）
10 migration の呼び出し順は元と同一に固定し、依存は行末コメントが正本 / 見つかった migrate.rs:7-8（//!）・apply_migrations 本体冒頭コメント（diff 文脈行「呼び出し順は挙動不変のため元のまま固定する。順序依存を作ったら…行末へ書くこと」） / 一致（写し。「実測: 責務分割のレビューまで…」は経緯として捨てた、と表にあり整合）
11 `additional` → `scan` の追加が正規化より先であることは検知器が守る / 見つかった migrate.rs:59-63（apply_migrations の /// 新設段落・検知器名・2 種の変異・4 本は並びを通らない） / 一致（単一・全文）
12 `validate.rs` は検出だけを行い、補正はしない / 見つかった snotra-core/src/config/validate.rs:3-4 / 一致（写し）
13 `kana_lower_names` をアリーナへの逐次 push で組み直してはならない（#1056） / 見つかった snotra-core/src/search/build.rs:446-454（new_with_cached_masks の /// 「不変条件:」段落・kana_column_survives_chunked_parallel_merge・KANA_CHUNK の注意） / 一致（単一・全文）。注: CLAUDE.md 側にあった「`new_with_cached_masks` の `kana_for_cached` は」の名指しは「ここは」へ縮んだが、同関数の doc に付いているので所在は保たれる
14 `kana_lower_names` / `kana_char_masks` は `migemo_enabled` が true のときのみ構築 / 見つかった build.rs:11-15（//! 新設段落・同時に空か同長・kana_available 空ガード・is_empty 棄却・窓の panic 防止） / 一致（単一）。注: 「~2.1–2.7MB/50k・構築も約 2 倍速」の数値は落ちた（数値を散文に写さない方針と整合・PERFORMANCE.md が正本）
15 パスマッチング（スコア式・has_path_sep で pre-filter スキップ） / 式とフォールバック条件は snotra-core/src/search/scoring.rs:41-43（PATH_BASE の ///）・スキップは query_plan.rs:9-11（//!） / 一致（単一）。注: `3000 - min(byte_pos, 500)` の逐語は docs/adr/ADR-path-query-tail-top-k.md:54 にも残る
16 スコアリング・順位計算は `search/scoring.rs` へ置く（列挙） / 見つかった scoring.rs:1-7（//!）・TopK/EntryView/score_one_entry は scoring.rs:94/241/325 に pub(super) で実在 / 一致（索引）
17 クエリ計画の純粋導出は `search/query_plan.rs` へ置く（列挙） / 見つかった query_plan.rs:1-7（//!）・QueryPlan/prepare_query_plan は :28/:59 に pub(super) / 一致（索引）
18 `target_path` は索引に持たず、フォルダ木の接頭辞共有から組み立てる / 見つかった snotra-core/src/search/path_store.rs:1-9（正本は index_tree・adopt）・:20-27（組み立ての 2 系統・流用禁止）・:264-267（セグメント単位で比べてはならない・検知器名） / 一致（索引・写し）
19 索引の構築処理は `search/build.rs` へ置く（唯一の入口） / 見つかった build.rs:1-9（//!・版の番号を書かない）・:401-403（from_material の ///） / 一致（索引・写し）。注: from_material の /// は「『唯一の入口』と無条件に書いてはならない」と明記しており、消した bullet の「**索引を建てる唯一の入口**」の方が古かった。消したことで矛盾が解消した
20 記録側が潰したものを、cache-miss の枝がそのまま索引の表現に使う（bullet は残り、一部だけ削除） / 削られた「木と派生データは `indexer::IndexMaterial` が組のまま運ぶ…同じそこを通る」は snotra-core/src/indexer.rs:4-7（//!）・:99-101（extend_with_path_entries の ///・起動経路と背景再構築が同じここを通る・検知器 2 本） / 一致（写し）
21 常駐ヒープの内訳は `search/footprint.rs` が数える / 見つかった snotra-core/src/search/footprint.rs:1-17（構築前の Vec を走査しない・`..` を書かない・コンパイラ担保）・#[doc(hidden)] は footprint.rs:106 / 一致（索引・写し）
22 剪定容量 `top_n` は焼き込まず引数で受け取る（live-read） / 見つかった snotra-core/src/history.rs:3-4（//!）・:83-85（load の ///）・prepare_save_if_dirty/prepare_flush/prune は :169/:206/:346 で top_n を引数に取る / 一致（写し）
23 `HistoryStore` に `top_n` フィールドを再導入しないこと / 見つかった history.rs:5-6（//! 新設 1 文） / 一致（単一）
24 crate の外から見える名前は `indexer.rs` の re-export が決める / 見つかった indexer.rs:9-11 / 一致（写し）
25 `str_arena.rs` 線上表現は `Vec<Option<String>>` のまま・守るのは `*_wire_format_is_identical_to_*` / 見つかった snotra-core/src/str_arena.rs:13-17（「線上表現は変わっていない」節） / 一致（索引・写し）
26 `index_tree.rs` 辿る規則はここが唯一持つ / 見つかった snotra-core/src/index_tree.rs:14-18（「辿る規則は 1 つ」節・TreeNodes・walk_to_root / raw_path_into） / 一致（索引・写し）
27 `autostart.rs` 状態の正本は OS であり `Config` にフィールドを持たない / 見つかった snotra-core/src/autostart.rs:4-7（SPEC §7.7 の指しまで） / 一致（索引・写し）
28 `tests/path_query_cost.rs` …`normalized_keys` を保持するか導出するかの差を測る唯一の計器 / 見つかった snotra-core/tests/path_query_cost.rs:6-7（//!） / 一致（単一・1 文）
29 `measure_path_query_frame_cost_at_operating_point` は実起動の経路を再現する（#1067） / 見つかった path_query_cost.rs:9-14（//! 新設段落・据え置き側・一致の実測・旗の要点） / 一致（単一・全文。「7,500〜8,000 µs」は「数千 µs」へ丸め、正本は PERFORMANCE.md と表に明記）

== src-tauri/CLAUDE.md ==
30 `main.rs` …背景再スキャンの spawn と結果適用は #1001 で撤去済み / 見つかった src-tauri/src/main.rs:4-5（//!・ADR-rescan-explicit-only の指し） / 一致（写し）
31 インデックスビルドの開始/終了は `try_begin_index_build()` / `finish_index_build()` 経由 / 見つかった src-tauri/CLAUDE.md:115（「実装パターン」節の同文・.claude/rules/src-tauri.md:12 が指す）・src-tauri/src/state.rs:106-123（両メソッドの ///） / 一致（写し・同文書内）
32 `invalidate_icon_cache` はメモリ内と `icons.bin` を単一 lock 内で無効化 / 見つかった src-tauri/src/icon.rs:5-6（//!）・:190（単一 lock 内で原子的に）・:200・:493（実測 17/2000 回） / 一致（写し）
33 ビルド本体は `catch_unwind` で包む（panic 戦略依存） / 見つかった src-tauri/src/indexing.rs:5（//!）・:62-65（catch_unwind 直前のコメント: abort 戦略でも silent wedge にならない） / 一致（写し）
34 finish 後に `is_index_stale` を再チェック・unwind の panic 経路では再 kick しない / 見つかった indexing.rs:23-24（start_index_build の /// 新設段落）・本体 :75-87 のコメント / 一致（単一）
35 `LoadOutcome::ReadFailed` では適用せず早期 return / 見つかった src-tauri/src/config_watcher.rs:4-8（//! 反転: 本ファイルが正本）・:182-190（should_apply_config_change の ///・history.bin 損失・default scan 誤再構築・Config::load の保全に揃える） / 一致（写し）
36 `heap_trace.rs` 既定ビルドには入らない・欄の不在は「測っていない」 / 見つかった src-tauri/src/heap_trace.rs:3-4 / 一致（索引・写し）。注: 「欄の不在が『測っていない』であり 0 ではない」の逐語は //! L3-4 には無い（「`trace.rs` の出力も従来どおり」まで）。含意は保たれるが逐語では弱い
37 `startup.rs` 終端を `RegisterInitialHotkey` の arm だけに閉じない / 見つかった src-tauri/src/startup.rs:30・:38・:69（一度きり性・受容する残余） / 一致（索引・写し）
38 `monitor.rs` 基準モニターは必ず点から決める / 見つかった src-tauri/src/monitor.rs:77-86（point_monitor_work_area の ///・window_monitor_work_area が #835 で消えた旨まで） / 一致（索引・写し）
39 起動の入口は `launcher_controller/` の直下に置くこと / 見つかった src-tauri/src/egui_shell/launcher_controller/activation.rs:3-13（//!。ただし主張は「集める規範は要らなくなった」へ更新済みで、消した bullet の方が古い） / 一致（表も「`//!` の方が新しい」と明記）

== 太字でない削除行（対応表には載る）==
- `Config::icon_cache_cap()` は派生 → snotra-core/src/config/schema.rs:470-482（保守注意）/ 一致
- 早期 return の前にバウンドリトライ（3 回 × 150ms） → config_watcher.rs:211-212（定数）・:214-219（///）・//! に intra-doc link / 一致（数は定数へ）
- コヒーレンシは engine の `index_stale` ledger に一元化 → src-tauri/CLAUDE.md state.rs 行の横断 bullet に残存 / 一致
- `layout.rs` の撤去済み一覧 → 経緯として捨てた（layout.rs //! は現在形のみ）/ 一致

== 対応表との食い違い（要約）==
- #07: 「Windows の dirs 同一性の実測は…同じ段落に含めた」は偽。主張はリポジトリの生きた層から消えた（tests.rs:72 の検知器 doc に `config_local_dir` の名だけ残る）
- #36: 「欄の不在は 0 ではない」の逐語が `//!` に無い（含意のみ）。表は「写し」としているが厳密には一部欠落

== 消した bullet を指していた散文で、付け替えられていないもの（対応表の「付け替えた指し先」6 件の外）==
- docs/adr/ADR-path-query-forward-single-pass.md:15 「…唯一の経路である（`snotra-core/CLAUDE.md` の search.rs 節）」— この主張は #15 で query_plan.rs の //! へ移ったため、指し先は空を指す
- .claude/rules/snotra-core-search.md:17 「横断不変条件（並列 Vec レイアウト・ビットマスク一元化・has_path_sep 非互換）: `snotra-core/CLAUDE.md` の search.rs 節」— 「has_path_sep 非互換」は #15 で消え、search.rs 節に無い（並列 Vec レイアウトは残っている）
- src-tauri 側の「モジュール構成」を指す 10 件（activation.rs:348・search_flow.rs:44・layout.rs:394・mod.rs:21・view.rs:972/1214/1248・window_coordinator.rs:24/190・state.rs:101）は `###` 小節（#1032 条項・window_coordinator.rs の項・trace 規範）を指しており、それらは残っているので影響なし
