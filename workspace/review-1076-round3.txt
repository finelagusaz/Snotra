code-reviewer / #1076 ラウンド 3（f5fff5b — 偽にしにいく枠組み）
行番号・文番号はすべて f5fff5b 時点。作業ツリー clean・変更は加えていません。
=====================================================================

## 結論

**Medium 2 / Low 1 / ⚠️ Low 1。問い 3 は所見なし。**

- **f5fff5b が新しく持ち込んだ欠陥は 1 件だけ**です（Medium 1・文の重複）。
- **Medium 2 は「新しい文言が覆えていない範囲」**であって退行ではありません。当たる 2 例は
  どちらも既に read_config を通っており**現時点の欠陥はゼロ**です。字面を将来に適用したとき
  だけ誤ります。
- 問い 3（ラウンド 2 の修正が新しく偽にしたもの）は**壊せませんでした**。6 か所すべてを
  実装から再導出して真であることを確認しています。

以下、各問いについて「**何をして壊しにいったか**」→「**壊せたか**」→「**所見／壊せなかった理由**」
の順で書きます。

=====================================================================
# 問い 1 — 新しい弁別子は全 11 事例を矛盾なく分けるか

## 壊しにいった手順

src-tauri/CLAUDE.md:57 の条項を句点で分割し、**文 5・文 7・文 8 だけを判定規則として字面どおり
適用**しました（文 5 = 例外の定義、文 7 = 弁別子は走るスレッド・動機を判定に使うな、
文 8 = イベントループスレッドで走るのは何か）。

1. **11 事例へ機械的に当てる**（例外側 4・非例外側 7）
2. **母集団を 11 の外へ広げる**——「イベントループスレッドで走る config の読み」を
   read_config の呼び出し点から**全部**数え直し、文 8 の列挙で覆えるかを見る
3. 依頼された 3 つの下位項目（config_watcher の二重帰属／resolve_opener の可搬性／
   インラインと post の取り違え）を個別に崩しにいく

## 結果 (1) — 11 事例は**矛盾なく分かれます**（壊せませんでした）

| # | 位置 | 実行文脈 | 文 5・文 8 を字面どおり当てると | 期待 |
|---|---|---|---|---|
| 1-4 | launcher_controller.rs:503/736/750/766 | フレームの中 | 文 8 の「フレームの中」→ 非例外 | 非例外 OK |
| 5 | commands/instant.rs:37 | フレームの中（文 10 が名指し） | 非例外 | 非例外 OK |
| 6-7 | window_coordinator.rs:193/224 | show 経路 | 文 8 の「show / hide」→ 非例外 | 非例外 OK |
| 8 | commands/icon.rs:16-18 | spawn した icon worker | 文 5 が名指し → 例外 | 例外 OK |
| 9 | launcher_controller.rs:827,837 | spawn した folder worker | 文 5 が名指し → 例外 | 例外 OK |
| 10-11 | commands/launch.rs:105,156 | platform スレッド | 文 5 が名指し → 例外 | 例外 OK |
| 12 | config_watcher.rs:87 | watcher スレッド | 文 12/13 が射程外と明記 | 射程外 OK |

**ラウンド 2 で壊れた 3 つ（icon / folder / tray）は、判定を「走るスレッド」へ移したことで
今度は正しく例外側へ落ちます。** 動機（ユーザーが待つか）を判定に使うなと文 7 が明示的に
禁じているため、私がラウンド 2 で使った崩し方（「ユーザーはフォルダ一覧を待っている」）は
**もう通りません**。

## 結果 (2) — 母集団を 11 の外へ広げると**壊せました** → Medium 2

## 結果 (3) — 下位 3 項目

- **config_watcher.rs:87 の二重帰属**: **解けています。** 射程外の理由が手続き
  （update_config と同じ錠の内側で取ることに意味がある）へ移り、スレッド規則で例外側へ
  落ちることと**矛盾しません**——どちらも「engine lock のままでよい」へ収束し、かつ手続き側の
  理由は「**そこに置かねばならない**」という、許可より強い要求を足しています（スレッド規則だけ
  では出てこない情報なので、残すのが正しい）。**ただし文が 2 本残っています** → Medium 1
- **resolve_opener の可搬性**: **壊せませんでした。** 「launch_item_with_state が将来
  イベントループ側から呼ばれる」という将来を仮定して当てたところ、文 9（**どちらかは呼び出し元を
  辿って判定すること——同じ関数が両方から呼ばれるようになれば分類は変わる**）が捕まえます。
  文 5 の「platform スレッド（resolve_opener 系）」は関数の属性のように読めますが、文 9 が
  隣接して呼び出し点の属性へ引き戻しています。**現時点の呼び出し元は platform/tray.rs:67,69,76,441
  だけ**（実測）で、egui 側は別関数（resolve_tools）を使うため衝突していません
- **インラインと post の取り違え**: **壊せました** → Medium 2 の後半

=====================================================================
# 問い 2 — 古い散文の列挙は今度こそ閉じているか

## 壊しにいった手順

そちらが使った入口は「engine lock / engine ロック / engine.lock」です。**「engine」を落として
数え直しました**——「lock を取」「lock 回数」「1 lock」「lock 1 回」「ロックを取」「錠を取」
「錠越し」「ロック越し」を .rs 全体（src-tauri/src + snotra-core/src）と生きた .md
（SPEC.md / docs/architecture.md / 各 CLAUDE.md / PERFORMANCE.md）へ当て、さらに
「read_config / read_visual / read_metrics」を --include=*.md で全リポジトリへ当てました。

## 結果 — **壊せました（Low 3・⚠️ Low 4）。ただし厳密な偽は 1 件もありません。**

落ちていたのは「lock」とだけ書いてある散文で、**engine の錠と config.read() の区別が付いて
いない世代の言い回し**です。詳細は下の Low 3 / ⚠️ Low 4。

## 壊せなかった入口（何を見て真と判定したか）

- **PERFORMANCE.md:557-580**（#1032 の A/B 節）— 「…read_config を通るようにした」は
  **過去の行為**であり、移行の完了も範囲も主張していない。表は日付つきの実測値。**真**
- **PERFORMANCE.md:531**「read_visual の lock 取得 ≤ 24」— 当時の計測値。**真**
- **docs/architecture.md:91**「egui view は config を毎フレーム live-read する」— **真**
- **snotra-core/src/engine.rs:244-256**（config_handle の doc）— 「UI が毎フレーム行う
  live-read を外側の Mutex<Engine> の外へ出すための口」。**真**
- **src-tauri/src/state.rs:88-93** — 「その間に UI が同じ lock を取りに行っていたのが #1032 の
  主因**で**」= 過去形。**真**
- **src-tauri/src/egui_shell/visual.rs:11** — 「別々に lock を取っていた」= 過去形。**真**
- **commands/icon.rs:11 / commands/launch.rs:98-101** — 例外側で今も engine lock を保持する。**真**
- **commands/instant.rs:51**「clipboard は呼び出し側がエンジンロック外で読んで渡す」——
  呼び出し側への**契約**であり、engine lock が消えた今も破れていない。**真**
- **docs/superpowers/plans/ ・ specs/ ・ .superpowers/sdd/ ・ docs/adr/** — 凍結された歴史で
  あり腐るに任せる層（ADR-adr-frozen-history）。**射程外**として数えていません

=====================================================================
# 問い 3 — ラウンド 2 の修正が新しく偽にしたものはあるか

## 壊しにいった手順

書き換えた 6 か所を**実装から独立に再導出**しました（doc を読んでから実装を見るのではなく、
実装を先に読んで期待文を作り、doc と突き合わせる形）。

## 結果 — **所見なし（壊せませんでした）**

- **docs/architecture.md:228**（数え上げと逐語写しを外した形）——
  「クエリが空のときと indexing 中のときは行を同期でクリアするだけで engine の錠を取らない
  （枝は on_enter が持つ）」。launcher_controller.rs:1457-1464 の 3 分岐と一致し、
  **数え上げも条件式の写しも残っていません**。到達可能性の主張（全消し直後は armed／
  索引再構築中も同様）も :1385-1387 の on_input() が indexing によらず arm することで成立
- **activation_uses_frame_values_not_live_reads の doc**——「起動の入口がヘルパーを呼び、
  そのヘルパーが read_config を呼ぶ形は、**何段挟まっていても**緑のまま通る」。
  **shift_activate の直呼び（:662 → resolve_tools）を正しく覆っています**——「何段」は 1 段を
  含み、実例の名指しも「instant_prefix や resolve_tools を経る形」と関数名だけになったので、
  私がラウンド 2 で指摘した経路の数え上げは消えています
- **launcher_controller.rs:71**「action 抽出だけ UI スレッド・spec C 節。抽出が読む config は
  read_config から取る」——:503-513 と一致。**逆向きに強すぎません**（「UI スレッド」の主張は
  維持され、錠の帰属だけが訂正されている）
- **launcher_controller.rs:852 / :1381**「毎打鍵の config の読みの回数を減らす（当時は
  engine lock の回数だった・#1076 で読み口が移った）」——changed エッジが取得済み prefix を
  渡す構造（:1381-1383 → :858）は不変で、減るのが「読みの回数」なのも真
- **window_coordinator.rs:475**「read_metrics は読みをクロージャ内で取り切って返すため、
  ここで保持は残らない（#1036 までは engine lock だった）」——:52-53 が read_config を
  通ることと一致。「#1036 までは」は**時制として正しい**（#1036 が移した）
- **src-tauri/CLAUDE.md:57 の hotkey 先例の追加**——main.rs:459-464 に実在し、
  on_event_loop の中で read_config から読んでいる。**真**

=====================================================================
# 発見事項

## Medium 1 — f5fff5b が config_watcher の射程外の文を**2 本**にした（この差分が持ち込んだ唯一の欠陥）

**場所**: src-tauri/CLAUDE.md:57（#1032 条項の中・句点で数えて文 12 と文 13）

**実測**: grep -c は 1（同じ行に在るため）、**grep -o は 2** を返します。

    grep -c "config_watcher\` が適用の前に読む旧 config は射程外である" src-tauri/CLAUDE.md  → 1
    grep -o "config_watcher\` が適用の前に読む旧 config は射程外である" src-tauri/CLAUDE.md | wc -l → 2

- 文 12（新）: 「…**外れる理由はスレッドではなく手続きで**、あれは読みではなく適用の一部であり、
  update_config と同じ錠の内側で取ることに意味がある」
- 文 13（旧）: 「…あれは読みではなく適用手続きの一部で、update_config と同じ錠の内側で取ることに
  意味がある」

**根本原因**: Low 4 の修正で**書き直した文を挿入し、元の文を消していない**。壊れている不変条件は
「同じ事実を 2 か所へ書かない」（AGENTS.md「文書に事実の写しを増やす変更」）。**同一の条項の中の
写しなので、片方だけが将来直されると条項が自分と食い違います**（本サイクルで 3 度目の同型）。

**なぜ機構が捕まえないか**: リンク切れでも索引でもないので governance:check も cargo doc も緑です
（この差分は .md と doc コメントだけなので、当然どの検査も沈黙します）。

**修正例**: 文 13 を削除する。

**この所見を偽にする手順**: 上の grep -o を走らせて 1 が返れば私の誤りです。

## Medium 2 — 文 8 の列挙が「イベントループスレッドで走る読み」を覆い切っていない（2 つの実例）

**場所**: src-tauri/CLAUDE.md:57 の文 8——「**イベントループスレッドで走るのは、フレームの中・
show / hide・on_event_loop が<インラインへ倒れる>listener の本体である**」

判定規則は文 5＋文 8 の組で「例外 = イベントループスレッドを止めない場所」ですから、
**文 8 が『イベントループスレッドで走るもの』の外延を決めます**。その外延が 2 か所で足りません。

### (a) post された on_event_loop の本体が外延から落ちる——**しかもそれが文 11 の先例そのもの**

on_event_loop は**インラインでも post でもクロージャをイベントループスレッドで走らせます**。
一次証拠は snotra-egui-runtime/src/proof.rs:38-40 の doc です——「**フレームの外から
イベントループスレッドへ入る唯一の口**」「イベントループスレッドから呼ぶとその場で同期・
再入的に実行される（…）。**別スレッドからは PostMessageW で post して即座に戻る**」。
インラインか post かは**いつ走るか**の違いであって、**どこで走るか**の違いではありません。

ところが文 8 は「インラインへ倒れる listener の本体」だけを挙げます。hotkey の読み
（main.rs:459-464 の hotkey_toggle）は **platform スレッドから on_event_loop を呼ぶので post 側**
です（egui_shell/mod.rs:531-537 が「emit 元は platform スレッドであり、on_event_loop は
インラインに倒れず **post** になる」と明記）。ゆえに**文 8 を字面どおり当てると、hotkey の読みは
イベントループスレッドで走らないことになり例外側へ落ちます**——**文 11 が同じ読みを
「先例である」と名指しているのと矛盾します。**

### (b) tao の window-event リスナーが外延から落ちる

egui_shell/mod.rs:373-378 が main 窓へ登録する Moved リスナーは、
window_coordinator.rs:710-719 の position_results_below_main を呼び、そこは
**read_config で window_gap を読みます**。これはイベントループスレッドで走りますが、
文 8 の 3 項目のどれでもありません（フレームの中でもなく——登録箇所のコメントが
「**ネイティブ移動ループ中は egui フレームが回る保証が無い**ため」と書いています——
show / hide でもなく、tauri の listener でもない）。

**同じファイルの別条項が反例を明示しています**: src-tauri/CLAUDE.md:56 は window_gap について
「position_results_below_main が別に読む（**Moved リスナーとも共用しフレームに閉じないため**・
意図的な除外）」と書いています。

### 現時点の欠陥はゼロである

(a) も (b) も**既に read_config を通っています**。誤るのは「新しい読みをそこへ足す人が文 8 を
字面どおり当てたとき」だけで、退行ではありません。**ラウンド 2 の Medium 2/3 と同じ性格**
（分類は正しく、文言の射程だけが足りない）です。

**修正例（1 手で両方閉じる）**: 列挙をやめて性質を書く。

> イベントループスレッドで走るのは、フレームの中・show / hide・on_event_loop へ渡した
> クロージャの本体（**インラインか post かによらない**——正本は proof.rs の on_event_loop の
> doc）・tao の window-event リスナー（Moved → position_results_below_main が実例）である。

あるいは**列挙そのものを置かず**、EventLoopProof の構築点（proof.rs:23 が構造的に列挙している）
を正本として指す。後者を勧めます——同じ条項が「件数・列挙を書くと腐る」の規範と同じ面に居り、
構築点は型が保証しているためです。

**この所見を偽にする手順**: (a) は proof.rs:38-40 を読み、post されたクロージャが
**呼び出し元スレッドで**走ると読める記述を示せば私の誤りです。(b) は
window_coordinator.rs:715 の read_config が Moved 経路から到達しないこと（登録が
mod.rs:373-378 に無いこと）を示せば私の誤りです。

## Low 3 — 「1 フレーム 1 lock」の言い換えが 4 か所取り残されている（問い 2 の別入口が拾った）

**場所**:
- src-tauri/src/egui_shell/view.rs:560「テーマ値（色・font・Metrics・show_icons）は **1 フレーム 1 lock** で読み切る」
- src-tauri/src/egui_shell/view.rs:636「font_family のエッジ検出も同一 lock で読む（… **lock 1 回/フレーム**）」
- src-tauri/src/egui_shell/results_view.rs:537「テーマ値は **1 フレーム 1 lock**（#673 spec 決定 4）」
- src-tauri/src/egui_shell/launcher_controller.rs:786「都度読み——**lock 1 回/フレーム**の既存ヘルパー群と同型」

**根本原因**: 本サイクルの最初のコミットが window_coordinator.rs:186 で
「1 フレーム 1 lock の規律」→「**1 フレーム 1 読みの規律**」と言い換えたのに、**同じ言い回しの
兄弟が 4 か所残っています**。壊れている不変条件は「同じ概念の言い換えは対で当てる」
（対称修正）。

**厳密には偽ではありません**——read_visual / read_config は config.read() を 1 回だけ取るので、
「1 フレーム 1 lock」は今も字義どおり成立します。**ゆえに Low です。** ただし #1032/#1076 が
「どの錠か」を論点にした後では、engine の錠と読み取り錠のどちらを指すのか読者に分かりません
（launcher_controller.rs:786 の「既存ヘルパー群と同型」が最も曖昧で、その「既存ヘルパー群」は
#1036 より前は engine lock を取っていた面です）。

**修正例**: 4 か所とも「lock」→「読み」へ揃える（window_coordinator.rs:186 と同じ言い換え）。
**採らない判断も妥当です**——偽ではなく、射程は doc コメントの読みやすさに閉じています。

**この所見を偽にする手順**: window_coordinator.rs:186 が今も「1 フレーム 1 lock」であること
（＝言い換えが行われていないこと）を示せば、対称の欠けという枠組みごと消えます。

## ⚠️ Low 4 — main.rs:458 の「read_config が錠そのものを避ける」

**場所**: src-tauri/src/main.rs:456-459（hotkey_toggle の短絡の理由づけ）——
「この短絡はかつて『show 経路で engine ロックを取らない』ための対策でもあったが、
**read_config が錠そのものを避ける**ようになったので、いまは無駄な読みを省くだけの意味になった」

read_config が避けるのは **engine の錠**で、config.read() は取ります。ラウンド 1 の Low 4
（「錠を一切待たない」）と同型の言い過ぎです。**この PR は触っていない既存文**であり、
文の結論（短絡はいま無駄な読みを省くだけ）は真です。⚠️ を付けるのは、射程が
「錠そのもの」という語の読み方に依るためです。

**修正例**: 「read_config が **engine の錠を**避けるようになったので」。

**この所見を偽にする手順**: read_config が RwLock の read guard を取らない実装であることを
示せば私の誤りです（egui_shell/mod.rs:429 が s.config.read().unwrap() を呼んでいます）。

=====================================================================
# 実行しなかったこと

- **検査は 1 つも走らせていません。** この差分は .md と doc コメントだけを触り、私はソースを
  変更していないためです（カテゴリ A は hook が担当し、governance:check は f5fff5b の時点で
  passed の報告を受領済み）
- **ラウンド 1・2 で挙げた所見の再点検はしていません**（今回の枠組みの外）。ラウンド 2 の 7 件が
  当たっているかは問い 3 として検算し、6 か所とも真でした
