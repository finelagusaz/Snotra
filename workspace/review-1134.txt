# code-review — #1134（fix/error-row-icon-key・作業ツリー差分）

対象: `git diff`（引数なし）で得た未コミット差分。コード 3 ファイル
（snotra-core/src/ui_types.rs / src-tauri/src/egui_shell/icon_textures.rs /
src-tauri/src/egui_shell/results_view.rs）。workspace/plan.md の checkbox 更新は対象外。

## 総評

**Critical / High の「コード欠陥」は 0 件。** 実装は計画どおりで、挙動面の所見は無い。
以下の所見はすべて散文（doc コメント）と計画準拠の水準であり、うち 1 件だけを High に置いた
——計画が「書かない」と記録した文が実際には書かれているためで、修正は 1 文の削除で足りる。

### 自分で測ったこと（代理ではなく対象で）

- `cargo test -p snotra-core --lib icon_key` → 5 passed（icon_key_is_none_for_error_rows を含む）
- `cargo test -p snotra icon_` → 18 passed / 3 ignored
  （error_row_never_resolves_a_texture_from_a_previous_generation・
   wanted_icon_keys_never_loads_icons_for_error_rows を含む）
- icon_key の**製品コード呼び出し点**の再列挙（grep。LSP は本セッションでも未応答）:
  icon_textures.rs:93（wanted_icon_keys）/ :111（visible_icon_keys）/ :122（icon_for_row）の 3 点のみ。
  他のヒットはすべてテストと doc。icon_textures の HashMap を icon_for_row を経ずに引く箇所は無い
  （results_view.rs:484 が唯一の引き）。
- is_error: true の**生成点**の再列挙: snotra-core/src/folder.rs:214 の error_result だけ。
  icon は常に IconSource::FromPath、要素 1 の Vec。instant.rs の行は is_error: false を明示的に測っている（:1088）。
- 変異注入は**再実行していない**（同じ作業ツリーに他エージェントが同居しているため、対象を変更しながらの
  検査は避けた）。代わりに読み取りで裁定した——icon_key の is_error 枝を外すと、両テストの fixture は
  FromPath / Explicit とも Some を返し、(c)(d) と icon_key_is_none_for_error_rows の各 assert が決定論的に
  落ちる。チームの実測（3 本が赤）と矛盾しない。

---

## Phase 1: 実装検証

- **エラー行以外の挙動**: 変更は icon_key 先頭の `if self.is_error { return None; }` のみで、
  is_error == false の行は `match &self.icon` へ素通りする。FromPath / Skip / Explicit の返り値は変わらない。
  既存テスト 3 本（ui_types.rs:85 / :93 / :102）と instant.rs の 5 本がそのまま緑であることが接地。
  **判定: 挙動変化なし。**
- **wanted_icon_keys からの `if r.is_error { continue }` 撤去**: 直後の
  `let Some(key) = r.icon_key() else { continue };` が同じ行集合を落とす。**「撤去すると壊れるもの」は
  見つからなかった**——両者の間に副作用は無く（icon_key は &self の読みのみ）、is_error 行が wanted へ
  載る経路は icon_key が Some を返す場合に限られ、それは同じコミットが塞いでいる。
- セキュリティ / シークレット / システム境界の入力: 該当なし（純関数の分岐 1 本）。
- テストカバレッジ（実装者と独立に見た所見）は Phase 2 末尾の項へ。
- コメント第一原則（docs/comment-guidelines.md）に触れる所見は Medium 1 / Low 1〜3。
- **コードスパンの行またぎ**（docs/comment-guidelines.md「日本語の折返し」・どの機械検査も見ない型）:
  新規・改稿した全段落を目視。**0 件**——コードスパンはいずれも 1 行に収まっている。

---

## Phase 2: 計画判断の検証

### 2a. plan.md 不変条件との照合

不変条件照合: I1 — エラー行はアイコンキーを持たない。担い手は SearchResult::icon_key ただ 1 つ
実装箇所: snotra-core/src/ui_types.rs:58-67
判定: 守られている（検知器 2 本が緑・呼び出し点 3 つがすべてこの導出を通ることを grep で再確認）

不変条件照合: I2 — エラー行に placeholder を描かない（担い手は draw_result_row の None 枝）
実装箇所: src-tauri/src/egui_shell/results_view.rs:366-369
判定: 守られている（差分は当該行に触れていない。I1 と別の事実である旨も :366-367 のコメントが保つ）

不変条件照合: I3 — 通常行は従来どおりキーを持つ
実装箇所: snotra-core/src/ui_types.rs:62-66
判定: 守られている（既存 icon_key_* / wanted_icon_keys_* / instant.rs の各テストが緑）

### 2b. 対称コードパスチェック

対称チェック: wanted_icon_keys（要求）↔ visible_icon_keys（保持）↔ icon_for_row（引き）
判定: 同じ修正が届いている（3 者とも icon_key を通る導出であり、個別の is_error 条件を持たない）

対称チェック: icon_key の is_error 折り込み ↔ draw_result_row の `None if !result.is_error`
判定: 不要 — 理由: 前者は「キーを引くか」、後者は「placeholder を描くか」で、SPEC.md:87 が 2 つを
      並べて定めている（「抽出せず、placeholder も描かない」）。片方を他方へ畳むと SPEC の一方が
      担い手を失う。ケース列挙:
        ケース A: エラー行 + テクスチャ在り → icon_key=None → icon_for_row=None → None 枝 → 何も描かない [OK]
        ケース B: エラー行 + テクスチャ無し → 同上 → 何も描かない [OK]
        ケース C: 通常行 + テクスチャ無し → None 枝の !is_error が真 → placeholder [OK・従来どおり]

対称チェック: is_error 行の生成（folder::error_result）↔ 消費（icon 経路）
判定: 不要 — 理由: 生成点は 1 つで icon は常に FromPath。Explicit のエラー行は型としてのみ可能で、
      テストが両方を測っている。他に is_error を立てる生成点は grep で 0 件

### 2c. DRY / 関数カバレッジチェック

- icon_key を手書きで代替している箇所（行の path を直接キーに使う・match r.icon を別に書く）は 0 件。
- icon_textures を icon_for_row を通さずに .get() する箇所は 0 件。
- イベント定数（events.rs）に関わる変更は無い。
- **判定: DRY 違反なし。**

### 2d. リソースライフサイクルチェック

リソース: 世代交代フレームでのテクスチャ保持（results_view.rs:647-655 の retain_visible ほか 3 本）
破棄: 同 :649-654（icon_textures / icon_attempts / icon_pending を同じ visible 集合で刈る）
判定: ペア成立

本差分の副作用は「エラー行の世代で、そのキーのテクスチャが**追加で 1 件**落ちる」ことだけである
（従来はエラー行の path が可視集合に載り、そのテクスチャだけが残っていた）。復帰経路は既存の
wanted_icon_keys → worker で新設不要、かつ commands/icon.rs の load_icon_pngs が IconCache を先に引くため
SHGetFileInfoW の呼び直しにならない（計画 §5 の実測と一致）。**リークなし・4 本のクリーンアップは
同じ条件で揃っている。**

### 2e. SPEC.md 同期チェック

SPEC 同期: 列挙失敗行がアイコンを引かない（機構を要求側 1 点から導出へ移した）
SPEC 該当節: §3.4（SPEC.md:86-87）。§19.5（:1038・種別ごとのアイコン写像）も確認したがエラー行の
             記述を持たない。§6.6（:290-295）はエラー行の表示・キー操作のみ
判定: SPEC 対象外（更新不要）— §3.4 は「抽出せず placeholder も描かない」という**観測される挙動**を
      既に定めており、今回の差分はそれに**合わせる**方向（AGENTS.md「バグ（記述に合わせる）」）。
      機構の所在は SPEC の管轄ではない。diff に SPEC.md が含まれないことは正しい

### 2f. 「変更不要」判断の再評価

再評価: visible_icon_keys を is_error で絞らない（#1133 の意図的判断を維持）
ケース:
  ケース A: 通常行が抽出直後に世代交代 → キーが可視集合に残る → 積み直しの往復なし [OK]
  ケース B: エラー行の世代 → icon_key=None ゆえ可視集合に載らない → retain が落とす [OK・#1134 が求めた挙動]
  ケース C: エラー行と通常行が同じ世代に混在 → 現状は起こらない（error_result は要素 1 の Vec を
            set_results で丸ごと差し替える）。仮に混在しても落ちるのはエラー行のキーだけ [OK]
結論: 変更不要を確認（保持側に is_error を足す必要はない）

再評価: draw_result_row の `None if !result.is_error`（placeholder を描かない枝）を残す
ケース: 上の 2b のケース A / B / C に同じ
結論: 変更不要を確認（I2 の唯一の担い手であり、I1 とは別の事実）

再評価: results_view.rs のコード（コメントのみ変更）
ケース: icon_for_row の返り値の型も呼び出し形も不変 → 差分ゼロ
結論: 変更不要を確認

### テストが「測っていると主張するもの」を実際に測っているか

- icon_key_is_none_for_error_rows: **測っている。** FromPath の行は path が実在ディレクトリ、
  Explicit の行は表示文字列と別のキーを持つ——どちらも修正前なら Some を返す形。
- error_row_never_resolves_a_texture_from_a_previous_generation:
  - (a) 保持: 測っている（可視集合にエラー行の path が載らない）。
  - (b) 剪定: 測っている（前の世代のテクスチャが落ちる）。
  - (c) 引き: **独立に効いている唯一の枝**——剪定を通していない map に残ったテクスチャを引けてしまう
    修正前の形を、保持側の挙動に依存せず落とす。
  - (d) Explicit: 測っている。path は "x" なので path で引く実装なら偶然 None になるところ、
    stale に**キー**の側を入れてあるので、キー導出が生きていることを実際に区別している。
  - ⚠️ **(a)(b) だけなら「visible_icon_keys が常に空集合を返す」変異でも緑になる。**
    その母集団（正の側）を持っているのは visible_icon_keys_omits_rows_that_take_no_icon（:355）と
    icon_for_row_looks_up_by_key_not_by_path（:372）であり、**現状はカバーされている**。所見ではなく確認事項。
  - ⚠️ 「エラー行と通常行が同じ世代に混在したとき、通常行のキーだけが残る」は測っていない。
    現状その並びは製品に現れない（計画で裁定済み）ので**受容してよい**が、set_results にエラー行を運ぶ
    経路が増えたらここが死角になる。
- wanted_icon_keys_never_loads_icons_for_error_rows: 測っている（要求側に導出が届いていなければ赤）。
  ただしコメントの主張の強さについては Low 2 を参照。

---

## Phase 3: パフォーマンス検証

icon_key は性能に敏感な経路（icon_for_row は描画ループで全行ぶん・既定 200 行／フレーム）に居るが、
追加されたのは**フィールド 1 本の bool 分岐**で、その行の SearchResult は同じ命令列で既に読まれている。
アロケーションも lock() も増えていない。visible_icon_keys / wanted_icon_keys 側は世代交代フレームと
viewport 範囲に限られる（results_view.rs:647 / :675-681）。**所見なし。**

---

## 発見事項

過去パターン再発の疑い: **1 件**（Low 1。docs/comment-guidelines.md「他のコードの現在の状態を
主張するもの」＋ 別 crate の構造を doc に書く型）。

### High 1 — wanted_icon_keys の doc が、計画が「書かない」と記録した文を持っている

**場所**: src-tauri/src/egui_shell/icon_textures.rs:84

    /// どの行がキーを持つかは [`SearchResult::icon_key`] が決める。

**根本原因**: 計画 Phase 3-8（workspace/plan.md:75）が「**icon_key が担い手であることをここで名指ししない**」
と記録し、その代わりを「導出が 1 つであることはファイル冒頭のブロックコメントが持つ」と定めている。
実装はブロックコメント（:74-77）にそれを書いたうえで、ここにも 2 つ目の表現を置いた——
**同じ事実の 2 つ目の表現を残さない**という、この差分自身の方針（wanted_icon_keys から
`if r.is_error { continue }` を撤去した理由）と逆向きになっている。

**注記（機序の帰属）**: 計画が引用するユーザーの逐語は "重複排除と抽出要否の判定である旨を簡潔に
書いてほしい" までであり、「名指ししない」は**計画がその裁定を敷衍して記録した制約**である。
「ユーザーの逐語に反する」とは書かない。

**具体的な修正例**: 当該 1 文を削除し、責務の 1 文だけを残す。3 行上のブロックコメントが導出の
一意性を持つので、失われる情報は無い。

    /// ここの責務は**重複排除と抽出要否の判定**である（[`needs_extraction`] と in-flight の除外）。

### ⚠️ High 1 の対（同型・計画の文言が弱いので裁定を仰ぐ）

**場所**: src-tauri/src/egui_shell/icon_textures.rs:107

    /// 往復になる。エラー行を保持の対象から外すのは [`SearchResult::icon_key`] の仕事であり、この
    /// 関数は導出が返したキーをそのまま集める（#1133 / #1134）。

計画 Phase 3-9（plan.md:76）は「is_error で狭めない理由（通常行の往復回避）**だけを残す**」と書いており、
文字どおり読めばこの 2 文目も範囲外になる。ただし 3-9 はユーザー裁定を伴わないため、
**High 1 だけを直して :107 を残すと修正が半分になる**という理由でここに並べる。採否は lead の裁定で。
（残すなら、High 1 の「名指ししない」との非対称の理由を 1 行添えるのが筋が通る。）

### Medium 1 — icon_key の doc が経路の**数**を書いている（「3 者」）

**場所**: snotra-core/src/ui_types.rs:57

    /// 通って生き残る。ここで折り込むことで、要求・保持・引きの 3 者が同時にエラー行を扱わなくなる。

**根本原因**: docs/comment-guidelines.md「第一原則」の**書かないもの**——「コードが決めている構造の
事実——経路の**数**、分岐の**列挙**、**呼び出し元**」「**数は書かない**（「経路は 2 つ」「呼び出し元は
3 箇所」）」。「3 者」は icon_key の呼び出し元の数そのもので、4 つ目の消費者が増えた日にこの散文だけが
腐り、しかもどの機械検査も見ない。計画 Phase 3-12（plan.md:79）自身は**数を持たない言い回し**を
指定している（逐語:「エラー行はキーを持たないので、要求・保持・引きのいずれもエラー行を扱わない」）。

**具体的な修正例**: 計画の言い回しへ戻す。

    /// 生き残っていた。エラー行はキーを持たないので、要求・保持・引きのいずれもエラー行を扱わない。

### Low 1 — snotra-core の doc が src-tauri の**現在の挙動**を現在形で主張している

**場所**: snotra-core/src/ui_types.rs:55-57、および src-tauri/src/egui_shell/icon_textures.rs:276-277（テスト doc）

    /// 足りない——同じ `path` が前の世代で通常行として抽出されていれば、そのテクスチャは保持側を
    /// 通って生き残る。

**根本原因**: docs/comment-guidelines.md「『なぜ』に添える根拠のうち、**他のコードの現在の状態を
主張するもの**」（関係）。保持側（visible_icon_keys / retain_visible）は別 crate にあり、その方針が
変われば——visible_icon_keys の doc 自身が「剪定の方針は費用の都合で変わりうる」と書いている——
この動機文だけが黙って偽になる。テスト doc の「生き残り、引き側が返してしまう」も同型。

**具体的な修正例**: ガイドラインの指定どおり**過去の観測**へ移す。
「要求側だけを止めていた版では、前の世代で抽出したテクスチャが残って引かれた（#1134）」。

### Low 2 — 検知器の主張が双条件になっている（緑の含意が強すぎる）

**場所**: src-tauri/src/egui_shell/icon_textures.rs:248-249

    // #1134 でこの関数から `is_error` の条件を外したので、**この assert が緑であることは
    // 「`icon_key` の導出が要求側まで届いている」ことの検知器**でもある。

**根本原因**: 検知器を定義するのは「何が**赤**にするか」であって緑ではない。この assert は
「エラー行が要求に載らない」ことしか測らず、wanted_icon_keys に is_error 条件が戻されても緑のままである
——「緑 ⇒ 導出が届いている」は成り立たない。実装より強い主張（AGENTS.md「検証の作法」）。

**具体的な修正例**: 過去の実測として書く。
「icon_key の is_error 折り込みを外すとこの assert は赤になる（2026-08-19 の変異注入で実測）。」

### Low 3（⚠️）— 「両 variant」が IconSource の variant 数（3）と読み違えられる

**場所**: snotra-core/src/ui_types.rs:115

    // （#1134）。`icon` の値によらずキーを持たないことを両 variant で測る。

IconSource は 3 variant（FromPath / Skip / Explicit）で、測っているのは**キーを返しうる 2 つ**である
（Skip は元から None）。「両」は 2 つの意味で正しいが、直前が「icon の値によらず」なので全 variant を
測ったと読める。確信度は低い（読み手依存）。

**具体的な修正例**:「キーを返す 2 variant で測る（Skip は元から None）」。

---

## 指摘しなかったもの（lead の申し送りに対する応答）

1. visible_icon_keys が行の性質で絞らないこと（#1133 の意図）— **DRY / 重複として指摘していない**。
   2f で「変更不要を確認」として再評価した（⚠️ High 1 の対は doc の文言についてであり、絞りの方針ではない）。
2. results_view.rs の `None if !result.is_error` — 統合も撤去も提案していない。2b でケース列挙のうえ
   「別の事実」として確認した（SPEC.md:87 が根拠）。
3. wanted_icon_keys からの `if r.is_error { continue }` 撤去 — 「念のため残せ」は書いていない。
   **壊れる証拠を探した結果は 0 件**（Phase 1 に記載）。
4. SPEC.md 不変更 — 2e で「更新不要」に同意（§3.4 / §19.5 / §6.6 を実際に読んで判定）。
