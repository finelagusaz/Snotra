# code-review ラウンド 2 — issue #985 / fix/temp-dir-process-unique（修正差分の検算）

レビュー日: 2026-08-16（ラウンド 1 の所見へ当てた fix-forward が対象）
対象: `git diff`（未コミットのまま。`main...HEAD` は使っていない）
変更ファイル: snotra-core/src/{binfmt,config,folder,history,window_data}.rs + workspace/plan.md（6 files, +152 / -26）

**レビュー中に plan.md が動いた。** 最初に取った diff には「意図的な構造 4 点は 1 件も誤検出されなかった
（#872 の申し送りが効いた 2 例目）」という記述があり、私はこれを「訂正差分に新しい数え上げが入った」として
挙げるつもりでいた。読み直したところ `plan.md:169` の blockquote で**自ら撤回済み**になっていたため、
diff を取り直して現在の状態に対して判定している。以下はすべて**取り直し後の状態**に対する所見である。

## 総括

**Critical / High は無し。** 新規 Medium 1 件・Low 5 件（うち ⚠️ 4 件）。
ラウンド 1 の ⚠️ 3 件はすべて**一次証拠で決着**した（Medium 2 / Low 1 は ⚠️ 解除、Low 3 は機序を訂正のうえ決着）。

Medium 1 の解消は**私自身の道具で測り直した**（team-lead の測定を逐語追認していない）。

再検証（現在の作業ツリー・すべて本ラウンドで実行）:

- `cargo test -p snotra-core` → **586 passed / 0 failed / 11 ignored**（ラウンド 1 と同数）
- `cargo fmt --all -- --check` → exit 0
- `cargo clippy --workspace --all-targets -- -D warnings` → exit 0（再コンパイルなし＝この source 状態で検査済み）

---

## Medium 4（新規）— plan.md:246 の前提が Phase 3b で偽になっている（Medium 3 と同型の 4 か所目）

**場所**: `workspace/plan.md:246`（「レビュー後に入った差分（2026-08-16）」節の理由 1）

逐語:

    1. Step 2b の再実行条件は「issue の要件、または変更対象のファイル・シンボル集合が
       変わった場合」（`/plan-review`「Step 2b」）。**どちらも変わらない**——同じ
       `temp_dir_with_contents`、同じ新規検知器、変更ファイル 5 枚も不変。

**根本原因**: Phase 3b が**シンボル集合を 1 つ増やした**——`tests::dedup_load_does_not_rewrite_config_file` である。
`plan.md:41` が今回の修正でその行を表へ足しており、**plan.md 自身が同じファイルの中で矛盾を持っている**。
ファイル枚数（5 枚）は確かに不変だが、条件は「ファイル**または**シンボル集合」なので、
「どちらも変わらない」は偽である。

**これは Medium 3 が直した 3 か所と同じ形の 4 か所目**だが、性質が重い——他の 3 つは列挙の食い違いで、
これは**レビュー工程（Step 2b の再実行）を省く判断の根拠そのもの**だからである。
plan.md が書いた再実行条件を plan.md の記録に当てると、いまトリガーが立っている。

**ただし私は実質的な再実行が要るとは考えない。** Step 2b の射程は「5 ヘルパーの網羅性」であり、
Phase 3b はその母集団を動かさない（動かしたのは手書き 1 件をヘルパー経由へ寄せる向きで、
母集団はむしろ 1 件減った——下の Low 4 参照）。**要るのは結論の変更ではなく根拠の書き直し**である。

**修正例**: 246 行を次のように改める。

    1. Step 2b の再実行条件は「issue の要件、またはファイル・シンボル集合が変わった場合」。
       **issue の要件は変わらない。** シンボル集合は Phase 3b で 1 つ増えた
       （`dedup_load_does_not_rewrite_config_file`）が、Step 2b の射程は「5 ヘルパーの
       網羅性」であり Phase 3b はその母集団を動かさないため再実行しない。

---

## Low 4（⚠️・新規）— 数え上げの stale が 2 か所残っている（Medium 3 と同じ形の穴）

**場所**: `workspace/plan.md:193` / `:194` / `:225`

(a) `plan.md:193`「母集団（`research.md` の **11 hit**）が `.rs` 全数か」

実測で HEAD と作業ツリーを取り比べた:

    $ git grep -n "env::temp_dir" HEAD -- "*.rs" | wc -l   ->  11
    $ git grep -n "env::temp_dir"      -- "*.rs" | wc -l   ->  10

減った 1 件は `config.rs` の手書き `snotra-dedup-{pid}` で、**Phase 3b がヘルパー経由へ寄せたぶん**である。

(b) `plan.md:194`「`config` は 9 ではなく **12**」/ `plan.md:225`「`config` は 9 → **12**、`folder` は 11 → **20**」

検知器タグ `process_unique` を 5 モジュールへ足したので、各モジュールのタグ数は +1 されている。

**根本原因**: どちらも「投稿時の観測」として読めば嘘ではないが、`plan.md:157`（トリガー判定表・網羅性が要件）が
`research.md` を**母集団の典拠**として指しているため、現在形として読まれうる。
`AGENTS.md`「数え上げも同じ強さである」の射程内。

**なお `plan.md:234` は同じ罠を自分で回避している**——「**本数を書かない**——検知器を足した時点で
変わる数であり、危険の根拠は本数ではなく…構造のほうである」。同じ配慮を 193 / 194 / 225 へ及ぼせば揃う。

**修正例**: 193 へ「（Phase 3b で 1 件がヘルパー経由になり、現在の tree では 10）」を添える。
194 / 225 は「モジュール内で相異なる」という結論だけ残して数を落とす（225 は既に
「根拠の作り方をタグの数からヘルパーが 1 か所であることへ差し替えた」と書いているので、数は用済み）。

---

## Low 5（⚠️・新規）— 人間の承認は「触らない」に dedup サイトを含む版に対して与えられている

**場所**: `workspace/plan.md:200`（人間レビュー節）

承認の逐語は「承認。Phase 3 も入れて進めて」。この承認が与えられた時点の `plan.md:42` は、
`config.rs` の `snotra-dedup-{pid}` を**触らない**と明記していた。Phase 3b はその明示的な除外を覆したが、
人間レビュー節に再確認の記録が無い（`plan.md:45` の blockquote は経緯を説明するもので、承認の記録ではない）。

変更そのものはラウンド 1 で安全と検証済み（assert 不変・テスト green・tag 一意・remove-first は安全側・
`git show 3059b0fa` で存在理由の消滅も裏取り）。**これは欠陥ではなく記録の穴である。**
再確認を要するかは team-lead / ユーザーの判断であり、私からは記録の欠落を挙げるまでとする。

---

## Low 6（⚠️・新規・**ラウンド 1 の自分の所見の機序を訂正する**）

**ラウンド 1 で私が書いた「参照元も参照先も `#[cfg(test)]` の私的関数で rustdoc からは見えず」は、
2 つの理由を混ぜていた。ブロッカーは `#[cfg(test)]` だけであり、私的であることは関係ない。**
`plan.md:178` がその混ざった機序を写しているので、直すならそこの文言である。

実測:

    Cargo.toml:22                 broken_intra_doc_links = "deny"
    snotra-core/src/lib.rs:12     #![allow(rustdoc::private_intra_doc_links)]

`lib.rs:1-6` は private への link を**明示的に許可**した理由（doc の建て方が
`--document-private-items` 固定であること）と、「リンク切れの検査は弱まらない」ことを書いている。
つまり private なシンボルは**リンクしてよい**。落ちるのは `#[cfg(test)]` ゆえ非 test ビルドの
rustdoc に**存在しない**からである。

**さらに同 crate に同型の先例がある**（ラウンド 1 では見つけられていなかった）:

    snotra-core/src/history.rs:90-91
      /// （`Self::empty` を intra-doc link にしないのは `#[cfg(test)]` ゆえ非 test ビルドの
      /// rustdoc から見えず、`broken_intra_doc_links = "deny"` で CI が落ちるため）

参照先 `HistoryStore::empty` は `history.rs:109-110` の `#[cfg(test)] pub(crate) fn empty()` である
——**`pub(crate)` でもリンクできない**ことが、ブロッカーが可視性ではなく `#[cfg(test)]` である
ことの直接の証拠になっている。

この先例の形は「**リンクにしない理由を doc に書く**」である。5 か所へ同じ一文を足すと写しが 5 枚増えるので
**推奨まではしない**が、先例の存在は記録に値する——将来だれかが「親切に」リンク化して CI を落とす事故の
予防になるからである。判断は team-lead に委ねる。

**結論**: Low 3 の所見（リンク化不可・機械検査を持たない残余）は**正しい**。根拠は 1 つに絞られる。

---

## Low 7（nit・新規）— 変更ファイル表の新しい行がファイル別のまとまりから外れている

**場所**: `workspace/plan.md:41`

表の他の行はファイル別に固まっている（binfmt ×2 → config ×2 → folder ×3 → history ×2 → window_data ×2）が、
新しい `config.rs` の行は `window_data.rs` の後ろに追加されている。読みやすさだけの問題で、内容は正しい。

---

## Low 8（⚠️・nit・新規）— plan.md:221 の「0 件」は前提条件の書かれていない全称である

**場所**: `workspace/plan.md:221`（Step 1 の項目 7）

「ヘルパー名 5 種を逐語で持つファイルは対象 5 ファイル以外に **0 件**」。実測すると `snotra_test_` は
`workspace/adversarial-985.txt` / `workspace/plan-review-temp-dir-pid.md` / `workspace/plan.md` にも在る。
「`workspace/` の作業文書と `docs/superpowers/` の履歴を除く」という前提が書かれていない
（`AGENTS.md`「全称表現は前提条件とセットで書く」）。

**ラウンド 1 から在る記述であり、今回の訂正差分で新たに偽になったものではない。**
team-lead から「plan.md 全体の列挙・全称表現を洗う」と依頼された射程に入るので挙げる。

---

## ラウンド 1 の ⚠️ 3 件の決着

### Medium 2 ⚠️ → **解除**（記録の欠陥として正しく、記録は修正された）

team-lead の申告（実際の変異は書式文字列と引数の**両方**を外していた）を逐語追認せず、
**算術的な整合を自分で検算した**:

    記録された変異結果   : 585 passed; 1 failed; 11 ignored
    私が独立に測った現状 : 586 passed; 0 failed; 11 ignored

585 + 1 = 586 で一致し、ignored の 11 も一致する。捏造された数字がこの 2 か所で同時に噛み合う可能性は低い。
加えて「書式と引数の両方を外す」形は**コンパイルが通る唯一の変異形**であり、私のラウンド 1 の
コンパイルエラー論と矛盾しない（`-{}` だけ外す形は通らない、という私の主張はそのまま生きている）。
`plan.md:118-121` は実施形・逐語結果・対照の差の 3 点を持つ形になっており、記録として十分である。

**なお `plan.md:121` の「赤になったのは検知器 1 本だけで、他 585 本は緑のまま——つまり検知器が
無ければ pid の脱落は沈黙する」という推論は健全である。** 単一プロセスの実行では pid 無しでも衝突は
起きないので、検知器以外は原理的に落ちない。A/B の差が証拠になっている。

### Low 1 ⚠️ → **解除**（`/dry-check` の結論を私が独立に再導出して一致）

`git grep -n "create_dir_all"` を 4 crate（snotra-core / src-tauri / snotra-settings / snotra-egui-runtime）へ
当てて 16 hit を 1 行ずつ分類した:

- **ヘルパー本体 6**: binfmt:229 / config:3228 / folder:251 / history:433 / indexer:2438 / window_data:111
- **ヘルパーで得た dir 配下の下位ディレクトリ 4**: indexer:2521 / 2553 / 2681 / 2682（置換対象ではない）
- **製品コード 2**: binfmt:99 / config:1046
- **別 crate 3**: src-tauri/build.rs:7 / icon.rs:503 / icon.rs:564
- **コメント 1**: indexer:2432

**ヘルパーへ寄せられる手書き重複の残存は 0 件**で、team-lead の分類と一致する。
（team-lead の列挙に `src-tauri/build.rs:7` が無いが、別 crate かつ生成物の親ディレクトリ作成なので射程外。
結論は動かない。）

`env::temp_dir` 側も取り直し、`folder.rs:378` と `tests/search_frame_cost.rs:75` が
ディレクトリを作らない（`create_dir_all` を呼ばない）ことを確認した——ヘルパーへ置換できないという
team-lead の判定は正しい。

### Low 3 ⚠️ → **決着**（所見は正しい・機序は Low 6 で訂正）

---

## 見たが無かった（観点ごと・証拠つき）

### Medium 1 の解消 — 私自身の道具で測り直した

**(a) 句 grep**

    $ git grep -c "`indexer.rs` の `temp_dir` の doc を正本とする" -- snotra-core/src/
    snotra-core/src/binfmt.rs:1
    snotra-core/src/config.rs:1
    snotra-core/src/folder.rs:1
    snotra-core/src/history.rs:1
    snotra-core/src/window_data.rs:1

**5/5**。`grep -c` は**行**を数えるので、文途中で折られていれば 0 になる——1 が返ることが
「1 行に収まっている」の直接の証拠である。ラウンド 1 では 0 件だった。

**(b) `static Mutex` の汚染**

    $ git grep -n "static .*Mutex" -- binfmt.rs config.rs folder.rs window_data.rs
    (0 hit / exit 1)

    $ git grep -n "static .*Mutex" -- 上記 + history.rs
    snotra-core/src/history.rs:46: static HISTORY_WRITE_STATE: OnceLock<Mutex<...>> = OnceLock::new();

ラウンド 1 は 6 hit 中 5 hit がコメントだった。いまはコメント由来の hit が **0** で、残る 1 件は
実在するコードである。**汚染は解消**。

### doc の内容欠落

**意図した 1 か所以外に欠落なし。** 落ちたのは parenthetical「プロセス内の `static Mutex` では
射程が足りないこと・pid を落とすと何が起きるか」だけで、これは**私のラウンド 1 の推奨どおり**であり、
機序の全文は正本（`indexer.rs:2426-2433`）が持っている。保持を確認したもの:

- `folder.rs:246` の 2 段落目（prefix が名残であるという名指し）: 逐語で保持
- `folder.rs:377` のインラインコメント（不在を固定名に期待している旨）: 逐語で保持
- 5 モジュールの 1 行 doc: `plan.md:55` のテンプレートと**逐語一致**

### 行長 — 別の規約に触れないか

**触れない。修正は不要。** ただし**この長い行は私の推奨の帰結である**ことを明記しておく。

測った根拠:

- `rustfmt.toml` / `.rustfmt.toml` は**存在しない**（実測）。`cargo fmt --all -- --check` は exit 0
- `docs/comment-guidelines.md`「日本語の折返し」自身が「rustfmt はコメントを折り返さない」と述べ、
  1 段落 1 行を**要求**している。つまり長い行は規約の帰結であって違反ではない

参考値（この 5 ファイルのコメント行長の分布を測った）:

- 新しい共通 doc 行は **135 字**。既存コメント行は 100〜144 字が多数あり、その分布の中に収まる
- ただし **folder.rs:246（216 字）と folder.rs:377（213 字）は突出**しており、
  この 5 ファイルの既存最長（config.rs:656 の 144 字）の約 1.5 倍である
- 短くしたいなら、規約を守ったままできる形が 1 つある: `——` 以降を `///` の空行で区切って
  **別段落**にする（1 段落 1 行は保たれる）。**任意であり、私は求めない**

### 逆向きの監査 — ラウンド 2 の修正差分が消した行

**再確立されていない不変条件は無い。** 消えたものを 5 件列挙した:

1. doc の parenthetical（`static Mutex` への言及・pid を落とすと何が起きるか）
   → **意図的に落とした**（私のラウンド 1 の推奨）。機序の正本は `indexer.rs:2426-2433` にあり再確立不要。
2. `plan.md:42` の「触らない」項目（`config.rs` の dedup サイト）
   → `plan.md:45` の blockquote が経緯ごと再確立。**なぜ外したかまで書いてある**点が良い。
3. `plan.md:117` の曖昧な変異記述（「`format!` から `-{pid}` を一時的に外し」）
   → `plan.md:118-121` で実施形・逐語結果・対照の差の 3 点として再確立。
4. `plan.md:155` の「`/dry-check` は不要」
   → ✓ へ改め、誤りだったことと理由（`/dry-check` の射程は新設 5 本の圧縮可能性だけではない）を明示。
5. 「意図的な構造 4 点は誤検出 0 / #872 の 2 例目」の記述
   → `plan.md:169` の blockquote で**自ら撤回**（レビュー中に入った変更）。
   **これは私が挙げるつもりだった所見を先回りしている**——「誤検出 0 は構成上そうなるだけで、
   渡さなければどうだったかの証拠にならない」という区別は正しい。

### コード部分はラウンド 1 から変わっていない

`git diff -- snotra-core/` からコメント行（`///` と `// `）を除いた差分行を抽出し、
ラウンド 1 で私が取った diff の同じ部分と突き合わせた。**一致する**——内訳は
`format!` の書き換え 5 か所、検知器 5 本、dedup 置換 1 か所（2 行 → 1 行）、
folder の直書き固定名 1 か所。変わったのは doc コメントだけである。
（比較対象はラウンド 1 で私が取得した diff 出力であり、保存された成果物ではない。
抽出は `grep -vE "^[+-] *///" | grep -vE "^[+-] *// "` で機械的に行った。）

したがってラウンド 1 の「見たが無かった」——タグ衝突・モジュール間の名前空間・パス長・
bench の動的タグ・既存テストの前提・SPEC 同期・リソース対・パフォーマンス・セキュリティ——は
**そのまま維持される**。うち母集団だけは本ラウンドで取り直した（下記）。

### 母集団の再取得

`git grep -n "env::temp_dir" -- "*.rs"` → **10 hit**（ラウンド 1 と同じ）。
snotra-core 側の 8 件はすべて `std::process::id()` を含む
（binfmt:227 / config:3226 / folder:249 / folder:378 / history:431 / indexer:2436 /
window_data:109 / tests/search_frame_cost.rs:75）。残る 2 件は `src-tauri/src/icon.rs:501,562` で
どちらも pid 付き・別 crate。**行番号は doc の 1 行化で 2 ずつ繰り上がっている**が、集合は不変。

### 不変条件 2 の構造的検算

`plan.md:127`「成功テスト数が変わらないこと（新規検知器 5 本の増加を除く）」。
差分が足す `#[test]` はちょうど **5 本**、消すものは **0**（diff から機械的に抽出して確認）。
実測 586 passed はラウンド 1 と同数で整合する。

### Low 2（folder.rs の 3 つ目の写し）の行番号

`plan.md:177` は `folder.rs:378-381` と書いている。**現在の行番号として正しい**
（378 = `let dir = ...join(format!(`、379 = prefix リテラル、380 = `std::process::id()`、381 = `));`）。
ラウンド 1 の私の報告は 382-385 だったが、doc の 1 行化で 3 行繰り上がったためで、team-lead が更新済み。
コメントは 377 行にある。

### 「意図的な構造」4 点への誤検出

**本ラウンドでも 0 件。** ただし `plan.md:169` の但し書きのとおり、これは私が 4 点を
指摘対象から外して見ているからであって、「渡さなくても誤検出しなかった」の証拠ではない。

---

## 未検証（受容した残余・私が測っていないもの）

- **変異注入を自分で実行していない**（コード変更禁止の指示のため）。決着は算術整合と
  コンパイル可能性の論証によるもので、再現実験ではない。
- **並行 2 プロセスでの再現／非再現**は本ラウンドでも測っていない（ラウンド 1 と同じ）。
- **`research.md` 本体は差分に含まれないため詳細レビューの対象外**とした。Low 4 で挙げたのは
  plan.md がその数を引いている点であって、research.md の記述そのものの是非ではない。

---

## 成果物と変更

- コード・`workspace/plan.md` は**変更していない**。コミットもしていない。
- 本ラウンドで作った新規ファイルは `workspace/review-985-round2.txt` の 1 つだけ
  （ラウンド 1 の `workspace/review-985.txt` はそのまま残してある）。

