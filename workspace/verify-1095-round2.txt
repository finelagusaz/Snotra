検証・レビュー報告 ラウンド 2 — #1095 修正差分の検算
アンカー sha 17523504a32280d77d11a64e77fa227bd12503bd
担当: 検証委譲エージェント / 2026-08-21
（ラウンド 1 の報告は `workspace/verify-1095.txt`。上書きしていない）

================================================================================
0. アンカー確認
================================================================================

    git rev-parse HEAD → 4a0493cbf2b178c58aa208713258cd53fc5fbd5a（前回のアンカーのまま）
    → **`git checkout --detach 1752350` を要した。**
    git rev-parse HEAD → 17523504a32280d77d11a64e77fa227bd12503bd
    git status --porcelain → 空

途中、⚠️ 1 を決着させるため親（`46abb10`）へ一時 detach し、測定後アンカーへ戻した。
終了時 HEAD = `1752350`、`git status --porcelain` は空。


================================================================================
1. 依頼項目 5 — 変異注入の再実行要否を自分で確かめた
================================================================================

    $ git diff 4a0493c 1752350 -- scripts/governance/checks/G-heading-refs.mjs
      （出力なし）
    $ git rev-parse 4a0493c:scripts/governance/checks/G-heading-refs.mjs
      9e9a40ca097ea27e9e2068467ef3b0eba3b8442e
    $ git rev-parse 1752350:scripts/governance/checks/G-heading-refs.mjs
      9e9a40ca097ea27e9e2068467ef3b0eba3b8442e

→ **blob 同一。** 「種 3」の変異注入の再実行は不要と自分で確認した（再実行していない）。
   なお `lib.test.mjs` も 2 アンカー間で無変更なので、種 3 の fixture 側も動いていない。

**ただし本ラウンドで別の変異を 1 本入れた**（下記 §3）。書き換えた banner の主張の
**射程**を測るためで、依頼の手順外の追加である。復元済み・ハッシュ照合済み。


================================================================================
2. 依頼項目 1 — 書き換えた banner は今日の実測と合っているか
================================================================================

対象（`scripts/governance/checks/G-stale-identifiers.mjs:88-92`）:

    //   **開けたままにするのは、塞ぐと偽陽性が出るからである**——語彙源へその変換を当てると、
    //   `#[cfg(test)]` の中にしか綴りを持たないテストヘルパやテスト名を正しく引いている文書が
    //   finding になる（2026-08-21 実測。`#[serde(rename_all)]` が属性から導出する綴りも同じ側へ落ちる）。
    //   導入当時（#891）は全セルで 0 件だったので理由は「測って動かなかったから」だったが、**今日は動く**
    //   ——穴のせいで見逃された真の腐りは今日も 0 件のまま、塞ぐ側の代償だけが育った

主張を 5 つに割り、**主エージェントの裁定を逐語追認せず**、それぞれ独立に測った。

【主張 1】塞ぐと偽陽性が出る（finding になる）
  → **真**。§3 の変異で `exit 1 / 9 件`。ラウンド 1 と同一。

【主張 2】`#[cfg(test)]` の中にしか綴りを持たないテストヘルパやテスト名である
  → **真**。5 識別子の定義位置を行番号つきで確認（`fn` はすべて `#[cfg(test)]` より後）:
       method_body                    launcher_controller.rs:1509（cfg(test) は :1493）
       owners_of                      launcher_controller.rs:1719（同上）
       assert_read_once_in_this_file  view.rs:1400（cfg(test) は :1311）
       run_pass                       view.rs:1427（同上）
       input_text_sits_vertically_... view.rs:2089（同上）
  → **文書側の引用が正当であることも確かめた**（⚠️ 7 の宿題）。
     `docs/development-principles.md:182-194` は「存在形 / 否定形 / 一致形」の設計論で、
     `assert_read_once_in_this_file`・`owners_of`・`method_body` を**実在する手本として
     名指している**（`owners_of` は #1112 が帰属濾過へ移した当のヘルパ）。腐りではない。

【主張 3】`#[serde(rename_all)]` が属性から導出する綴りも同じ側へ落ちる
  → **真。主エージェントの機序の説明も正しい**（誤っている可能性を名指しで問われた点）。
     一次証拠 `snotra-core/src/config.rs`:
         :37  #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Default)]
         :38  #[serde(rename_all = "snake_case")]
         :39  pub enum AutoUpdateMode {
         :40      #[default]
         :41      Full,      // チェック + インストール（インストーラー版向け）
         :42      CheckOnly, // チェックのみ・通知する（ポータブル版向け）
         :43      Disabled,  // チェックしない
     → `check_only` は `CheckOnly` + `rename_all = "snake_case"` から導出される綴りであり、
       **production のソースには 1 文字も現れない**。`config.rs:42` という行指定も正確だった。
     → 引用側も正当: `SPEC.md:1158` は auto_update の 3 モードの表の 1 行、
       `docs/architecture.md:140` は「3モード: `full` / `check_only` / `disabled`」。

【主張 4】導入当時（**#891**）は全セルで 0 件だった
  → **偽。これが本ラウンドの唯一の要対処（M-1）。** 詳細は §4。

【主張 5】穴のせいで見逃された真の腐りは今日も 0 件のまま
  → **真。しかも私が測れる最大の射程で真である**（§3 の追加測定）。
     ラウンド 1 で私が測ったのは Rust 側 3 形のうち**形 1 だけ**（`#[cfg(test)]` の切り落とし）で、
     形 2・形 3 は語彙へ寄付したままだった。今回 3 形すべてを塞いで測り直したところ
     **finding は同じ 9 件**だった。banner が「3 つの形」と宣言している射程の全体で
     主張 5 が成立することを確認した——主エージェントの再現より広い範囲である。


================================================================================
3. 変異注入（追加 1 本・3 形すべてを塞ぐ）
================================================================================

目的: 主張 5 の射程を、宣言どおりの 3 形すべてで測る。

--- 母集団の確認（形 2 / 形 3 が実在するか） ---

    $ git ls-files "*/tests/*.rs"        → 13 件
        snotra-core/tests/{dir_stat_cost,memory_footprint,path_query_cost,search_frame_cost}.rs  … 形 2（4 件）
        snotra-core/src/search/tests/{basic,build,common,incremental,migemo,mod,path,performance,ranking}.rs … 形 3（9 件）

→ 形 2・形 3 は空集合ではない。ラウンド 1 の測定はここを語彙に残したままだった。

--- 注入した実バイト（Edit で書き、git diff で読み返した） ---

    @@ -149,7 +149,9 @@ export function currentVocabulary(snapshot) {
    -    parts.push(/\.(ps1|toml|yml)$/.test(f) ? src.replace(/#.*$/gm, " ") : stripRustComments(src));
    +    if (/\.rs$/.test(f) && f.includes("/tests/")) continue;
    +    const cut = f.endsWith(".rs") ? src.split(/^#\[cfg\(test\)\]/m)[0] : src;
    +    parts.push(/\.(ps1|toml|yml)$/.test(f) ? cut.replace(/#.*$/gm, " ") : stripRustComments(cut));

--- 変異が効いたことの証跡（exit code だけでは無効な変異と区別できない） ---

    currentVocabulary 長さ
        変異なし          1,609,968 字
        形 1 のみ（R1）   1,088,003 字（−32.4%）
        形 1+2+3（今回）    960,361 字（−40.3%）

--- 結果 ---

    $ node scripts/governance-check.mjs → **exit 1 / 9 件**（内訳は形 1 のみのときと完全に同一）

→ **形 2・形 3 を塞いでも finding は 1 件も増えない。** 127,642 字ぶんの語彙を落としても
   免罪が 1 件も剥がれなかった＝**今日それらのファイルだけが免罪している散文の識別子は 0 件**。

--- 巻き戻し（内容ハッシュ・mtime は使っていない） ---

    $ git restore scripts/governance/checks/G-stale-identifiers.mjs
    $ git rev-parse 1752350:scripts/governance/checks/G-stale-identifiers.mjs
      eaa647c00525bb94e371b0ffef45bd9ec15709e8   ← コミット済み blob
    $ git hash-object scripts/governance/checks/G-stale-identifiers.mjs
      eaa647c00525bb94e371b0ffef45bd9ec15709e8   ← 復元後
    $ git status --porcelain → 空


================================================================================
4. レビュー所見（修正差分と、その直近の文脈）
================================================================================

--- Critical / High ---
なし。ラウンド 1 の H-1 は解消（§6 の追跡表）。

--- Medium ---

[M-1] `scripts/governance/checks/G-stale-identifiers.mjs:91` —
      **修正が新しく持ち込んだ、誤った由来の帰属。** 所見は正しかったが、
      その所見に添えた**機序の説明が独立に誤っている**（ルート `CLAUDE.md`
      「レビューの委譲では、⚠️（確信の持てない所見）を返り値の列挙に含めさせ、
      前段の裁定は結論ではなく争点の形で渡す」の #1056 と同型）。

  書かれている文:
      //   導入当時（**#891**）は全セルで 0 件だったので理由は「測って動かなかったから」だったが、…

  **実測（道具を変えて 2 通りで測った）**:

      $ git log --oneline -S "測定の全セルで 0 件" -- scripts/
        1752350  （本コミット・置換）
        4f5f4d3  検査を 1 検査 1 ファイルへ分け…(#1093)     ← 移送
        066cb3f  docs: SPEC を as-built へ畳み、腐り検出器の射程を SPEC.md へ広げる (#885)  ← **導入**

      $ git log --oneline -G "全セルで 0 件" -- scripts/     # 変更を含む全コミット
        1752350 / 4a0493c / 4f5f4d3 / 066cb3f                ← **#891 は 1 度も現れない**

      $ git log --oneline -S "全セルで" -- docs/adr/
        066cb3f (#885)                                        ← ADR 側も同じ

      $ git log -1 --format="%h %ad %s" --date=short 066cb3f
        066cb3f 2026-08-03 … (#885)

  → **測定は #885（2026-08-03）が入れたもので、#891 は当該文へ 1 度も触れていない。**

  **さらに重要なのは、0 → 9 になった本当の理由が #891 ではないことである。**
  今日の 9 件を作っている 6 識別子は**すべて lowercase snake_case** である
  （`check_only` / `run_pass` / `owners_of` / `method_body` /
    `assert_read_once_in_this_file` / `input_text_sits_vertically_centered_for_both_body_and_hint`）。
  その述語 `STALE_LOWER_SNAKE_IDENT` が入ったのは:

      $ git log --oneline -S "STALE_LOWER_SNAKE_IDENT" -- scripts/
        4f5f4d3  （移送）
        25f48c6  fix(governance): G-stale-identifiers に lowercase snake_case を足す（照合 70 → 285 件）

  同ファイル :118-120 が自ら書いているとおり「**このリポジトリの主要言語の語彙はここに居る**
  ——Rust の関数名・テスト名・フィールド名はすべて lowercase snake_case であり、
  上の 2 述語はそのどれにも当たらなかった」。

  → #885 の測定時点では、述語は camelCase と SCREAMING_SNAKE しか見ていなかった。
     **Rust のテストコードが寄付する語は、そもそも 1 つも判定対象になりえなかった**。
     当時の「全セルで 0 件」は**穴について測った経験的事実ではなく、述語の構成から
     保証されていた 0** である。今日 9 件になったのは母集団が広がったから（#891）ではなく、
     **述語が Rust の綴りを見るようになったから**である。

  → **提案**: `#891` を `#885` に直し、変化の理由を「母集団が広がった」ではなく
     「述語が lowercase snake_case を見るようになった」に置き換える。
     `.claude/rules/governance-docs.md`「機構の実装の詳細（述語の種類・件数・分岐の列挙）を
     散文へ写さない」があるので、述語の一覧を書く必要はない——「当時の述語では Rust の綴りが
     判定対象にならなかった」という**帰結**だけで足りる。

  ※ 同ファイル :24 の「**自称スコープ**（#891 で広げた…）」は本差分の対象外の既存記述であり、
     ここでは問わない（#891 が射程を広げたこと自体は否定していない）。

--- Low ---

[L-1] コミット message の数え上げが閉じない。
      「**9 件**の所在をすべて裁定した結果、真の腐りは 0 件だった。**うち 5 件**は …
        テストヘルパ・テスト名 … で、**残る check_only** は …」
      5 + 1 = 6 であって 9 ではない。実際の内訳は
          finding 単位: テストヘルパ・テスト名 **7 件** + `check_only` **2 件** = 9
          識別子単位:   5 + 1 = **6**
      で、「9 件」は finding、「5 件」は識別子を数えており単位が混ざっている。
      **`workspace/plan.md` のフェーズ 3 は正しく書けている**（「6 識別子すべての所在を裁定し
      …残る 5 件は…」）ので、腐っているのは message の側だけ。
      squash でこの message が main の履歴になる（`AGENTS.md`「検証の作法（全タスク共通）」の
      「数え上げも同じ強さである」）。→ 「9 件（6 識別子）」等と単位を添えれば閉じる。

[L-2] `check_only` の免罪は**今日、糸 1 本で吊られている**——banner はそれを書いていない。
      全語彙源拡張子を走査した結果:
          $ (glob `*.{rs,ts,tsx,mjs,ps1,toml,yml}`) check_only
            src-tauri/src/egui_shell/notify.rs:326:  "check_only は install 不可"
      ヒットは **1 件だけ**で、しかも `#[cfg(test)]`（:275）の内側の**assert メッセージ文字列**である。
      つまり `SPEC.md:1158` と `docs/architecture.md:140` が今日緑なのは、
      **テストの失敗メッセージにたまたまその綴りが書いてあるから**にすぎない。
      誰かがそのメッセージを日本語だけに書き換えれば、変異を入れなくても
      `governance:check` は正当な引用 2 件に対して赤くなる。
      banner の新しい文は「塞ぐと落ちる」とは書いたが、「今日すでに 1 本の糸で吊られている」ことは
      書いていない。**欠陥ではなく、記録されていない残余**として挙げる
      （直すなら banner ではなく `notify.rs` 側の assert に一言添えるのが安い）。

--- 修正の周辺（新しい誤りが入っていないかの逆向き確認） ---

- `lib.mjs:573-574` の一意化は**正しく効いている**。
      * 意図であり、`lib.test.mjs` の種 3（`#[cfg(test)]` の内側のコメントも見る）がそれを固定する
      * ——入れるとあの it が落ちる。
  :111 と :191 の 2 つの `種 3` のうち :111 を括弧内の語で特定できる。
  括弧内の文字列は :111 の `it` タイトルの前方部分と逐語一致する。
- **新しい参照を 1 件も作っていない**ことを確認した。書き換えた 2 か所とも `「」` を含まず、
  `HEADING_REF` / `NEAR_REF` はどちらも `「」` を必須とする。実測でもサマリの
  見出し参照 324 / 近傍 19 / 折れうる位置 24 はすべて不変（§5）。
- banner の隣接する箇条書き「**テストコードにしか無い識別子も偽陽性になりうる**……（今日 0 件）」
  （:98-99）は本差分の対象外だが、**今日も真である**——これは `VOCAB_TEST_FILE`
  （`.test.mjs` / `.test.ts`）で**既に外している**側の残余であり、該当があれば
  無変異の `governance:check` が今日赤くなる。exit 0 なので 0 件は自己検証されている。
- `workspace/plan.md` の追記（「残る出現 4 件」に `workspace/` 除外を添える申し送り／
  フェーズ 2 の結果／フェーズ 3）に事実誤りは見つからなかった。フェーズ 2 の
  「324 → 307」「検知点は種 3 ただ 1 つ」は私の実測と一致する。

--- Phase 2 / Phase 3（コードレビューの残りのフェーズ） ---

2e. SPEC.md 同期: **対象外**。差分はコメントのみでガバナンス検査器の内部。
    `SPEC.md:1158` は本差分で 1 文字も変わっていない（`git show 1752350 --stat` に SPEC.md 無し）。
2b/2c/2d: 対称ペア・新規関数・リソースいずれも差分に無い。該当なし。
Phase 3（性能）: 実行経路の変更ゼロ。`governance:check` の照合件数も実行時間も不変。


================================================================================
5. 依頼項目 4 — カテゴリ A〜F（前回と同じ母集団で再実行）
================================================================================

A. Rust（*.rs）        → **該当なし**（差分は `.mjs` 2 + `.md` 2。`.rs` は 1 バイトも動いていない）
B. TypeScript          → **該当なし**（`vitest.config.ts` は差分に無い）
C. 窓/hotkey/slash     → トリガー**該当なし**。ただし `npm test` は `scripts/` を含むため**実行**:
       $ node .../vitest.mjs run
         Test Files  36 passed (36) / Tests  865 passed (865) / exit 0
   `test:powershell` / `smoke:startup` / `smoke:egui` は該当なし（⚠️ 5 は未解決のまま）
D. 表示の見た目        → **該当なし**（描画コードゼロ。#1106 の「広げる方向にも外れる」に従い
                          人間への視覚スモーク申し送りは出さない）
E. .githooks/**        → **該当なし**（差分に無い。`npm test` は C で実行済み・緑）
F. ガバナンス文書      → **実行**:
       $ node scripts/governance-check.mjs → exit 0
       governance:check — 全検査 passed（検査 21 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 /
       恒久規範 常時ロード 16972 字・rules 13080 字 / 見出し参照 324 件を md 48 件 + .rs 101 件 +
       スクリプトのコメント 111 件から照合 / workspace member 4 件の lints opt-in / clippy 禁止 8 件 /
       散文の識別子 404 件を 34 文書から照合 / 近傍の見出し参照 19 件 / 折れうる位置 24 件 /
       ADR 70 本の名前 / ADR の短縮引用 376 件）

   → この 1 行は**ラウンド 1 で `4a0493c` から取った文字列と 1 文字も違わない**。


================================================================================
6. 依頼項目 3 — ラウンド 1 の ⚠️ 7 件と所見の決着状況
================================================================================

⚠️ 1（サマリのバイト一致を真の byte-diff で測っていない）
   → **決着した。** 親 `46abb10`（= `4a0493c^`）へ detach して実測:
        governance:check — 全検査 passed（… 見出し参照 324 件 … 散文の識別子 404 件を 34 文書 …
        近傍の見出し参照 19 件 / 折れうる位置 24 件 / ADR 70 本の名前 / ADR の短縮引用 376 件）
        exit 0
     `46abb10`（変更前）・`4a0493c`（R1）・`1752350`（R2）の**3 点でサマリ行が完全一致**。
     ラウンド 1 で未照合と申告した項目（ADR 70 / 短縮引用 376 / clippy 禁止 8 等）も含め、
     代理ではなく対象そのもので確認できた。**「バイト単位で一致」は真。**

⚠️ 2（node_modules がメインリポジトリ側）→ **未解決**（同じ導入を使い続けている）。
   マニフェスト 3 種のバイト一致は再確認していないが、2 アンカー間で
   `package.json` / `package-lock.json` / `vitest.config.ts` は差分に無い。

⚠️ 3（複製ではなく稼働中のファイルへ変異）→ **未解決・本ラウンドで 1 回追加**。
   射程は前回と同じ: 被験体は検査器であってゲートではなく、種 3 のテストは一度も
   無効化していない。コミット・push なし、この worktree に CI は走っていない。

⚠️ 4（L-1 の正準形提案の採否）→ **部分決着**。主エージェントは (b)「指し先が一意でない」を
   括弧で閉じ、(c)「機械照合が付いていない」は**採らない判断**をした。これは正当な選択で、
   残余は「:111 の `it` が改題されるとこの散文が黙って腐る」1 点に縮んだ。再提案しない。

⚠️ 5（Pester を該当なしと判定したが実行していない）→ **未解決**（根拠は依然 grep 1 経路）。

⚠️ 6（`visible_rows` の教訓の喪失）→ **未解決**（本差分と無関係。裁定は plan.md にある）。

⚠️ 7（「9 件すべて偽陽性」を 1 件しか文脈裁定していない）
   → **決着した。** 6 識別子すべてを独立に裁定した（§2 の主張 2・3）。
      `check_only` は serde 導出＋SPEC/architecture の正当な引用、
      残り 5 件は `#[cfg(test)]` 内の実在ヘルパを `docs/development-principles.md` が
      設計論の手本として名指したもの。**真の腐りは 0 件**で、主エージェントの裁定と一致する。
      加えて 3 形すべてを塞いだ測定でも件数が増えないことを確認した（§3）。

ラウンド 1 の所見の解消状況:
   H-1（偽の実測が残っていた）      → **解消**。ただし置き換えた文に M-1 の新しい誤りが入った
   L-1（種 3 の指しが一意でない）   → **解消**（(b) を閉じ、(c) は採らない判断）
   L-2（「4 件」の除外条件）        → **申し送りとして解消**。`plan.md:84` に条項が入った
                                      （`4a0493c` の message 自体は履歴なので直せない）
   L-3（plan.md フェーズ 2 未チェック）→ **解消**。`- [x]` + 結果 2 行が入った
   L-4（visible_rows）              → 変更なし（裁定済み・⚠️ 6）
   L-5（近傍 19 の予測）            → 変更なし（実害なし）


================================================================================
7. コミット・push
================================================================================
行っていない。触ったのは変異注入 1 か所のみで、コミット済み blob と一致するまで復元済み。
終了時 HEAD = `17523504a32280d77d11a64e77fa227bd12503bd`、`git status --porcelain` は空。


================================================================================
8. ⚠️ 確信の持てない所見（本ラウンド）
================================================================================

⚠️ A. **M-1 の「#885 が導入した」は `-S` / `-G` の 2 通りで測ったが、`docs/adr/` 側の
   同文（`ADR-stale-identifier-detector-scope.md:108`）が #891 の作業で**再測定**された
   可能性までは否定していない。** 私が示せるのは「当該文字列を含む差分に #891 のコミットが
   1 度も現れない」ことまでである。リポジトリ全体で `--grep "#891"` を打ってもコミットは
   出てこなかったので、#891 は commit message に番号を持たない形で入ったか、issue 番号
   だけが存在する可能性がある。**「#891 ではない」は確度が高いが、「#885 である」は
   文字列の導入位置に基づく推定を含む。**

⚠️ B. **M-1 の後半（0 → 9 の真の理由は述語の追加である）は、機序としては強いが
   反実仮想を直接は測っていない。** 「#885 当時の述語（camelCase + SCREAMING_SNAKE）で
   今日の木を測れば 0 件になるか」を実際に走らせてはいない。根拠は
   「今日の 9 件を作る 6 識別子がすべて lowercase snake_case である」という観測と、
   同ファイル :118-120 の自己記述である。**測ろうと思えば測れる**（述語を 1 つ外して
   変異を打つ）ので、主エージェントが M-1 を採るなら、直す前にそこまで測るとよい。

⚠️ C. **§3 の「形 2・形 3 は今日 0 件しか免罪していない」は、私が書いた述語
   （`f.includes("/tests/")`）が 3 形を正しく表しているという前提に乗っている。**
   `git ls-files "*/tests/*.rs"` が返す 13 件はすべてこの述語に当たるが、
   banner が言う「`src/**/tests/*.rs` へ分けたテストファイル」に、`tests` という
   ディレクトリ名を使わない別の分け方（例: `*_test.rs`）が含まれるなら射程の外である。
   リポジトリ内にその形が無いことは確かめていない。

⚠️ D. **L-2（`check_only` が糸 1 本）を「記録されていない残余」と位置づけたが、
   記録すべきかどうかは判断しきれない。** `.claude/rules/governance-docs.md` の
   書く約束 (2)「必要なことだけ」に照らすと、この種の脆さを全部書くと banner が膨らむ。
   私は「安い側（`notify.rs` の assert に一言）」を挙げたが、**何も書かないのも
   妥当な選択**だと思う。

⚠️ E. **前ラウンドから引き継いだ ⚠️ 2 / 3 / 5 / 6 は本ラウンドでも解消していない**
   （§6 に個別に記載）。とくに ⚠️ 5（Pester 未実行）は 2 ラウンド連続で
   「該当なしと判定したので実行していない」であり、判定が誤っていた場合は
   2 ラウンドとも検証の穴になる。

⚠️ F. **私は「修正差分」だけでなく前回と同じ母集団で検査を回したが、
   ラウンド 1 で「該当なし」と判定した A / B / D / E を再導出はしていない**
   （差分の性質が同じなので前回の判定を引き継いだ）。
   `.rs` が 1 バイトも動いていないことは `git show --stat` で確認しているが、
   判定そのものを 0 から立て直してはいない。
