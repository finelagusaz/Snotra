検証・レビュー報告 ラウンド 3 — #1095 修正差分の検算
アンカー sha 39ce1a79ffe8770d0db7d527b169eec822a354d4
担当: 検証委譲エージェント / 2026-08-21
（ラウンド 1 = workspace/verify-1095.txt、ラウンド 2 = workspace/verify-1095-round2.txt。
  どちらも上書きしていない）

================================================================================
0. アンカー確認
================================================================================

    $ git rev-parse HEAD
      361c13bf249a71b3575b221a6159a2c380d49047   ← main 先端。**アンカーではない**
    → **`git checkout --detach 39ce1a7` を要した。**
    $ git rev-parse HEAD
      39ce1a79ffe8770d0db7d527b169eec822a354d4
    $ git status --porcelain → 空

終了時 HEAD = `39ce1a7`、`git status --porcelain` は空（§8）。

--- node_modules の導入（⚠️ 2 の決着） ---

この worktree に `node_modules` は無い。メインリポジトリ（`C:/workspace/Snotra`）のものを
使ったが、**代理ではなくアンカーの blob そのものと照合した**:

    $ git rev-parse 39ce1a7:package.json 39ce1a7:package-lock.json 39ce1a7:vitest.config.ts
      e454724a430350b76e71f25719cb71574ed46101
      749011d2ea124df942e0b4ceb3511f7ba56ba8c5
      94a30bb790274eb65e4acf4e9b69a5b993fe8f45
    $ (cd C:/workspace/Snotra) git hash-object package.json package-lock.json vitest.config.ts
      e454724a430350b76e71f25719cb71574ed46101
      749011d2ea124df942e0b4ceb3511f7ba56ba8c5
      94a30bb790274eb65e4acf4e9b69a5b993fe8f45   ← **3 種ともバイト一致**
    $ (cd C:/workspace/Snotra) git rev-parse HEAD → 39ce1a7（メイン側の作業ツリー自体がアンカー）
    $ node_modules/vitest の version → 4.1.10（`package.json` の `^4.1.8` を満たす）

→ **⚠️ 2 は決着した。** 「2 アンカー間で差分に無い」ではなく、アンカーの blob と作業ツリーの
   内容ハッシュで照合している。

--- 変異注入の場所（⚠️ 3 の決着） ---

本ラウンドの変異は **5 本すべて scratchpad へ `git archive` で展開した複製**へ当てた。
稼働中のファイルには 1 バイトも触っていない（`git status` は最初から最後まで空）。
複製は `git init` して実リポジトリと同じ `gitIgnoredPaths` 経路が働く状態にした
（していないと `.claude/settings.local.json` が gitignore 判定を受けられず、
  無変異でも 4 テストと G-references が落ちる——これは環境の作り方の artifact であり
  リポジトリの欠陥ではない）。


================================================================================
1. 差分の分解 — 「3 度書き換えられた hunk」は 2 コミットに分かれている
================================================================================

依頼は `git diff 1752350 39ce1a7` を対象と述べたが、その区間には**コミットが 2 本**ある。
どちらが何を入れたかを分けないと帰属を誤るので、まず分けた。

    $ git log --oneline -5 39ce1a7
      39ce1a7 fix(governance): 沈黙の主張へ測った母集団を添え、全称を実測の範囲へ収める
      3e58b85 fix(governance): 0 件の由来を実測へ正し、禁止が沈黙して破られることを禁止の隣へ置く
      1752350 fix(governance): 受容する残余の理由づけを実測へ合わせ、種 3 の指し先を一意にする
      4a0493c refactor(governance): 撤去済みの層が残した productionOnly を消し、…
      46abb10 chore: workspace 調査・計画 (issue #1095 …)

  - **3e58b85** = banner の 3 度目の書き換え（M-1 対処）**と** `lib.mjs` の沈黙警告の初稿
  - **39ce1a7** = `lib.mjs` の全称へ母集団を付け、「**検知点は種 3 ただ 1 つ**」を削っただけ

  → 依頼文の「主エージェント自身が全称の言い過ぎを見つけて直した（39ce1a7）」は
    `lib.mjs` の文だけに当たる。banner の 3 度目は `3e58b85` である。

    $ git diff --stat 4a0493c 39ce1a7 -- scripts/
      G-stale-identifiers.mjs | 9 +++++++--
      lib.mjs                 | 5 ++++-
      2 files changed, 11 insertions(+), 3 deletions(-)
    → **どちらもコメント／JSDoc のみ。実行される行は 1 行も動いていない。**


================================================================================
2. 依頼項目 5 — 変異注入の要否を自分で確かめた
================================================================================

    $ git diff 4a0493c 39ce1a7 -- scripts/governance/checks/G-heading-refs.mjs
      （出力なし・exit 0）

→ **バイト同一を自分で確認した。** 「種 3」の変異注入の再実行は不要。
   ただし本ラウンドでは**別の目的で** 5 本の変異を打っている（§3・§4・§5）。


================================================================================
3. 依頼項目 1 — banner の「受容する残余」の Rust の項を、今日の実測と照合した
================================================================================

対象（`scripts/governance/checks/G-stale-identifiers.mjs:88-94`）を 6 つの主張に割り、
主エージェントの裁定もラウンド 1・2 の結論も**逐語追認せず**、それぞれ独立に測った。

【主張 1】塞ぐと偽陽性が出る（finding になる）
  → **真。自分で再現した。** アンカーの複製の `currentVocabulary` へ
     `#[cfg(test)]` 以降の切り落としを当てて `node scripts/governance-check.mjs`:

        exit 1 / **9 件**（識別子は 6 種）
          docs/architecture.md:140                check_only
          SPEC.md:1158                            check_only
          docs/build-commands.md:104              input_text_sits_vertically_centered_for_both_body_and_hint
          docs/development-principles.md:159      run_pass
          docs/development-principles.md:184      assert_read_once_in_this_file
          docs/development-principles.md:186/188/194  owners_of（3 件）
          docs/development-principles.md:194      method_body

     → ラウンド 1・2 の 9 件と完全に一致。**finding 9 件 = 識別子 6 種**
        （ラウンド 2 の L-1 が指摘した単位の混線は、この内訳で閉じる）。

【主張 2】`#[cfg(test)]` の中にしか綴りを持たないテストヘルパ・テスト名である
  → **真。定義位置を自分で引いた**（`#[cfg(test)]` の行番号と定義行の前後関係）:
        launcher_controller.rs  #[cfg(test)] :1493  /  method_body :1509 ・ owners_of :1719
        view.rs                 #[cfg(test)] :1311  /  assert_read_once_in_this_file :1400 ・
                                                       run_pass :1427 ・
                                                       input_text_sits_vertically_… :2089
     すべて `#[cfg(test)]` より後。**5 件とも偽陽性で確定。**

【主張 3】`#[serde(rename_all)]` が属性から導出する綴りも同じ側へ落ちる
  → **真。** `snotra-core/src/config.rs:37-43` に
        #[serde(rename_all = "snake_case")] / pub enum AutoUpdateMode { Full, CheckOnly, Disabled }
     があり、`check_only` は導出綴り。`.rs` 全体での唯一の字面の出現は
     `src-tauri/src/egui_shell/notify.rs:326`（`#[cfg(test)]` は :275）の assert 文字列だけ
     ——ラウンド 2 の L-2「糸 1 本で吊られている」を独立に再現した（⚠️ D）。

【主張 4】「#885 当時の述語は lowercase snake_case を見ておらず（#989 で足した）」
  → **真。しかも banner が書いているより強い。** 道具を変えて 3 通りで測った:

     (a) 導入の帰属（⚠️ A の決着）
         $ git log --oneline -S "測定の全セルで 0 件" -- scripts/ docs/
           3e58b85 / 1752350 / 4f5f4d3 (#1093) / **066cb3f (#885)**
         **文字列の位置ではなく、066cb3f の diff で `+` 行として当該文が入っているのを読んだ**:
           $ git show 066cb3f -- scripts/governance-check.mjs | grep -n "全セルで" -B 12
             +//   1 件も無く（測定の全セルで 0 件）、測って動かなかったものを入れないだけの理由で開けてある
         → **#885 が導入したのは推定ではなく、追加行として観測できる。⚠️ A は決着。**
         なお `#891` はリポジトリ内に**コミットを 1 本も持たない**（`--grep "(#891)"` が空）
         ——issue 番号であって、当該文へ触れていないのは当然である。

     (b) #885 当時の述語（ラウンド 2 の記述を**訂正する**）
         $ git show 066cb3f:scripts/governance-check.mjs | grep -n "STALE_.*IDENT ="
           1427:const STALE_IDENT = /^([a-z][a-z0-9]*(?:[A-Z][a-z0-9]*)+)(\(\))?$/;
         名前パターンの grep だけを根拠にせず、判定本体も読んだ（:1479）:
           const im = raw.match(STALE_IDENT);   ← **述語はこれ 1 本だけ**
         → **#885 当時は camelCase の 1 述語しかなかった。** SCREAMING_SNAKE すら無い。
           ラウンド 2 の「述語は camelCase と SCREAMING_SNAKE しか見ていなかった」は
           **#989 の直前**（25f48c6^）の状態であって #885 当時ではない。
           **この訂正は banner を弱めず、強める**——当時の述語はさらに狭かった。

     (c) `#989` という番号の妥当性（**私は当初これを誤りと疑い、測って撤回した**）
         同ファイル :52 と :130 は同じ変更を **`#975`** と呼んでいる（ADR の見出し
         `## その後（#975・述語へ lowercase snake_case を足し、…）` も同じ）。
         一見「同一ファイルが同じ事象へ 2 つの番号」に見える。しかし:
           $ git log --oneline --grep "(#989)" → 25f48c6（PR 番号）
           $ git log --oneline --grep "(#975)" → 空（issue 番号。25f48c6 の本文は
             「issue #975 は開いたままにする」と明記）
           $ grep -rn "#989" .claude/ docs/
             .claude/rules/governance-docs.md:16  「#989 が 3 つ目の述語を足し（照合 70 → 285 件）」
             docs/adr/ADR-stale-identifier-detector-scope.md:349 / superpowers/…:100 も同じ用法
         → **`#989` はこのリポジトリで既に「述語を足した変更」の呼び名として定着している。**
           `#975` は動機となった（今も open の）issue。**誤りではない。所見に挙げない。**

【主張 5】「0 は述語の構成が保証していたのであり、述語が広がった時点で潜在的に動いていた」
  → **真。反実仮想を 2 通りで実測した**（ラウンド 2 の ⚠️ B の宿題）。

     --- 実測 1: #885 の木で述語だけを動かす 2×2（`066cb3f` を複製して測った）---

        | 述語                          | cfg(test) 切り落とし | finding |
        |-------------------------------|----------------------|--------:|
        | camelCase のみ（当時のまま）  | なし（当時のまま）   |   **0** |
        | camelCase のみ                | **あり**             |   **0** |
        | camelCase + lowercase snake   | なし                 |       5 |
        | camelCase + lowercase snake   | **あり**             |   **6** |

        差（6 − 5）= **`check_only`（SPEC.md:1113）ちょうど 1 件**。
        → **同じ木・同じ変換で、述語を広げた瞬間にだけ穴が鳴る。**
          「0 は述語の構成が保証していた」は機序の推論ではなく**実測**である。
        （当時の照合件数は `散文の識別子 1 件を 25 文書から照合`。
          「全セルで 0 件」は**候補が 1 個しか無い母集団**の上で測られていた）

     --- 実測 2: #989 の直後の木そのもの（`25f48c6` を複製して測った）---

        無変異     → exit 0（`散文の識別子 285 件を 33 文書から照合`。
                     25f48c6 の commit message の「70 → 285 件」と一致）
        変換を注入 → **exit 1 / 2 件**
                       docs/architecture.md:140  check_only
                       SPEC.md:1139              check_only

        → **「述語が広がった時点で潜在的に動いていた」は、その時点の木で実際に動く。**
          `潜在的に` という語に頼らずに真である。**⚠️ B は決着。**

【主張 6】穴のせいで見逃された真の腐りは今日も 0 件のまま
  → **真。** 9 件の指し先はすべて実在する（上の定義位置＋ serde 導出）。
     引用側も正当: `docs/development-principles.md` は設計論の手本として実在するヘルパを
     名指し、`SPEC.md:1158` / `docs/architecture.md:140` は auto_update の 3 モードの綴り。

→ **依頼項目 1 の結論: banner の Rust の項は、今日の実測と 6 主張すべてで一致する。
   要対処の誤りは無い。** ラウンド 1（High）・ラウンド 2（Medium）が指摘した誤りは
   どちらも解消され、**その置き換えが新しい事実誤りを持ち込んではいない**（§5 に別種の所見）。


================================================================================
4. 依頼項目 2 — `headingRefSourceDocs` の JSDoc の全称と、AGENTS.md の全称条項
================================================================================

現在の文（`scripts/governance/lib.mjs:573-577`）:

    * ——入れるとあの it が落ちる。**`governance:check` の全検査と `scripts/` のテストを通して、
    * 落ちるのはそれだけである**（2026-08-21 実測）——`governance:check` 自身は緑のまま、
    * `#[cfg(test)]` の内側の生きた正準形が黙って照合から外れる。

--- 4a. 宣言された母集団は、実際に測られた範囲と一致しているか → **一致している** ---

3e58b85 の初稿は母集団のない裸の全称（「落ちるのはそれだけである」「検知点は種 3 ただ 1 つ」）で、
39ce1a7 がそこへ母集団を足した。その母集団が実測と合っているかを、**主エージェントの
commit message ではなく自分の実行で**確かめた。

  複製へ禁止された変換を注入（`scanHeadingRefs` の `.rs` 読み取りを `#[cfg(test)]` で切る）:

    $ node scripts/governance-check.mjs
      全検査 passed / **見出し参照 307 件**（無変異は 324 件）/ **exit 0**
      → 「`governance:check` 自身は緑のまま」「黙って照合から外れる」は**真**。
         plan.md の「324 → 307」も再現した。

    $ npx vitest run scripts          # ＝ JSDoc が宣言した母集団
      **32 files / 523 tests** のうち **1 failed**:
        lib.test.mjs > 種 3: `#[cfg(test)]` の内側のコメントも見る
      → 宣言された母集団の中で落ちるのは**種 3 ただ 1 つ**。**主張は真。**

  → 39ce1a7 の commit message が書く「scripts/ 全体は 1 failed（種 3）/ 522 passed」は、
    私の実行と件数まで一致する。**母集団の宣言は、実際に測った範囲と一致している。**
    AGENTS.md「全称表現は前提条件とセットで書く」への適合という点では、
    **3e58b85 → 39ce1a7 の変更は正しい方向であり、正しく着地している。**

--- 4b. 宣言の外に検知点が無いと読者が誤読しないか → **誤読しても今日は真。ただし…** ---

「宣言の外」を私が数え上げた（推論ではなく一次証拠で）:

    ① PostToolUse（`.claude/hooks/post-edit.mjs` の `selectChecks`）
       `scripts/governance/lib.mjs` は `.rs` でも `.githooks/` でも `.claude/hooks/` でも
       `CHECK_DEFINITION` でもない → **返る検査は空配列**。編集しても何も走らない。
    ② `npm test`（vitest の include は `.claude/hooks/**` + `.githooks/**` + `scripts/**`）
       **宣言（`scripts/` のみ）より広い母集団で私が測り直した**:
         36 files / 865 tests → **1 failed（種 3）** / 864 passed
       → 他 2 根にも検知点は無い。**広げても主張は真。**
    ③ CI の `governance manifest delta`（`npm run governance:manifest --compare`）
       変換を注入した状態で base と比較 → 「**構造母集団に差分なし**」/ exit 0
       → **これは宣言の外にある 4 つ目の入口だが、鳴らない。**
    ④ CI の Pester（`ci.yml:222` `npm run test:powershell`）→ §6 の判定（結合なし）
    ⑤ cargo 系 → `.mjs` を見ない

  → **今日、宣言の外に検知点は無い。**「種 3 だけ」という読者の推論は結果として正しい。

--- 4c. それでも残る所見（Low・§5 の L-1 / L-2 へ） ---

  - 「`scripts/` のテスト」は**字面では `scripts/lib/*.Tests.ps1`（Pester 5 本）も含む**が、
    測られたのは vitest だけである（過大宣言）。→ L-1
  - 逆に `npm test`（＝ `docs/build-commands.md` と CI の SSOT コマンド）は 3 根 36 files を
    走らせるので、**宣言は実際に走らせるべきコマンドより狭い**。私の②が広い側でも真だと
    示したので、宣言を広げても嘘にならない。→ L-1

--- 4d. 別の重い所見: 「落ちるのはそれだけ」の**射程と、段落が宣言する意図の食い違い** ---

  **これは §5 の M-1 として本ラウンドの主所見にする。** 詳細は §5。


================================================================================
5. レビュー所見
================================================================================

--- Critical ---
なし。

--- High ---
なし。ラウンド 1 の H-1・ラウンド 2 の M-1 はどちらも解消（§7 の追跡表）。

--- Medium ---

[M-1] `scripts/governance/lib.mjs:572-573` —
      **「種 3 が*それ*を固定する」の「それ」は**意図**を指しているが、
        種 3 が実際に固定しているのは、その意図を破る**実装形の一方だけ**である。
        もう一方は `headingRefSourceDocs` 自身の中に在り、どの層も鳴らない。**

  **どの節が過大なのかを、指示語を束ねて特定した**（ここを取り違えると、直す先を誤る）:

      566: **Rust のテストコードを外さない。**              ← 宣言された意図（広い）
      571: この走査元へ **`#[cfg(test)]` 以降を落とす変換を入れてはならない**  ← 禁止（狭い・1 実装形）
      572: テストコードを外さないのは意図であり、種 3 が **それ** を固定する
             └─ 「それ」= **566 の意図**（「テストコードを外さないのは意図である」）← **過大なのはここ**
      573: ——入れるとあの it が落ちる                       ← 「入れる」= 571 の変換。真
      574: **`governance:check` の全検査と `scripts/` のテストを通して、落ちるのはそれだけである**
             └─ 「それ」= **573 の「あの it」**（種 3）。狭い。**§4a で実測して真**

  → **3 度書き換えられた 574 は真である。過大なのは、その 1 行上の 572 である。**

  **さらに 572 はこの差分より前から在る**（実測）:
      $ git show 4a0493c:scripts/governance/lib.mjs | sed -n '572,573p'
        * 意図であり、`lib.test.mjs` の種 3 がそれを固定する（入れるとあの it が落ちる）。
    → 3e58b85 / 39ce1a7 は 572 を 1 文字も変えていない。**この差分が作った誤りではない。**
      ただし両コミットが足した「（実測）落ちるのはそれだけである」が **572 の直後に置かれた**
      ことで、572 の過大な主張が**測定の裏書きを得たように読める**——見つけにくくなった。

  意図を破る**もう一方の実装形**——この JSDoc が付いている当の関数の述語で
  テストファイルを外す形——は、**種 3 を素通りする**。種 3 の helper は
  `scanHeadingRefs(snap, ["src/a.rs"])` と**母集団を直接渡す**（`lib.test.mjs:94`）ため、
  `headingRefSourceDocs` を一切通らないからである。

  実測（アンカーの複製へ 1 行だけ変異）:

      export function headingRefSourceDocs(snapshot) {
    -   return snapshot.files.filter((f) => f.endsWith(".rs"));
    +   return snapshot.files.filter((f) => f.endsWith(".rs") && !f.includes("/tests/"));
      }

      $ node scripts/governance-check.mjs
        全検査 passed / **見出し参照 318 件**（324 から 6 件減）
        / `.rs` **88 件**（101 から 13 件減） / **exit 0**
      $ npx vitest run          （3 根・36 files）
        **36 passed / 865 passed / exit 0**

  → **赤くなる層が 1 つも無い。** 黙って照合から落ちる 6 件は全部**生きた正準形**である:
        snotra-core/tests/memory_footprint.rs:282  `snotra-core/CLAUDE.md`「index.bin 書き込みの排他」
        snotra-core/tests/memory_footprint.rs:302  `PERFORMANCE.md`「採用: 背景再スキャンの比較を digest へ…」
        snotra-core/tests/path_query_cost.rs:3     `snotra-core/CLAUDE.md`「モジュール構成」
        snotra-core/tests/path_query_cost.rs:9     `PERFORMANCE.md`「索引の常駐の内訳」
        snotra-core/tests/path_query_cost.rs:264   `docs/development-principles.md`「検証の層と、層と層の隙間」
        snotra-core/tests/path_query_cost.rs:586   `PERFORMANCE.md`「パスクエリ全走査のコスト」

  **なぜこれを本ラウンドの主所見にするのか。** 「テストコードを外す実装形」の候補は、
  この差分の**すぐ隣**（`G-stale-identifiers.mjs:86-87`）で「`<crate>/tests/*.rs` の統合テスト・
  `src/**/tests/*.rs` へ分けたテストファイル——…残る 2 つには述語が要る」と**名指しで
  列挙されている**。読者はこの 1 行を書く動機と書き方を隣の banner から与えられ、
  572 を読んで「種 3 が止めてくれる」と信じ、書いても誰にも止められない。

  **報告のみ。依頼の「意図的に分けた構造 3」には触れていない**——私は `.rs` の腕の
  非対称を揃えよとは言っていない。**非対称を守る側の主張（572）が、実際の検知より
  広い**ことだけを報告する。
  なお §9 のとおり、**これは 574（3 度書き換えられた文）を書き直す理由にはならない。**

--- Low ---

[L-1] `scripts/governance/lib.mjs:574` — 宣言した母集団「`scripts/` のテスト」が、
      **測った集合と字面で一致していない**（両方向に）。
        過大: `scripts/lib/*.Tests.ps1`（5 本・Pester・CI の `ci.yml:222` が実行する）は
              `scripts/` 配下のテストだが、測られていない。
        過小: 実際に打つべき SSOT コマンドは `npm test`（`docs/build-commands.md` カテゴリ
              C/E・CI）で、母集団は `.claude/hooks/**` + `.githooks/**` + `scripts/**` の
              36 files / 865 tests。宣言はその部分集合（32 files / 523 tests）を指す。
      **私が広い側（`npm test` 全体）で測っても主張は真だった**（§4b ②）ので、
      **今日この文は嘘ではない**。腐り方は「読者が `npm test` を打って母集団の差に気づく」
      ——沈黙ではないので、severity は Low に留める。

[L-2] `3e58b85` / `39ce1a7` の commit message の「検証: vitest は 32 ファイル 523 テスト
      passed」は、**`npm test` ではなく `npx vitest run scripts` の数字**である
      （私の実測: `npx vitest run scripts` = 32/523、`npm test` = 36/865）。
      部分集合であることが message に書かれていない。ラウンド 2 の L-1（message の
      数え上げが閉じない）と同じ型で、**squash 後に main の履歴になるのは PR 本文**なので
      直接の実害は小さいが、**PR 本文へこの数字をそのまま写すと母集団が曖昧になる**。
      → PR 本文では `npm test`（36 files / 865 tests）の数字を書くか、
        「scripts/ のみ」と単位を添える。

[L-3] `G-stale-identifiers.mjs:92` の「上で偽陽性になる識別子は**すべて**その字種である」は
      **今日の 6 識別子についての全称**で、独自の日付を持たない（日付は 1 文前の
      「2026-08-21 実測」に付いている）。camelCase / SCREAMING_SNAKE の識別子が将来
      偽陽性側へ入れば黙って偽になる。ただし**この文が支えている結論**（#885 当時の 0 の由来）は
      歴史的事実で、腐っても結論は倒れない。**「上で」が直前の実測へ係ると読めば既にスコープ済み**
      でもある。→ **書き換えを勧めない**（§9 のメタ注意）。

[L-4] `workspace/verify-1095-round2.txt:188` —
      **この差分が追加している当のファイルに、私が反証した機序の主張が残っている。**
      `git diff --stat 1752350 39ce1a7 -- workspace/verify-1095-round2.txt` → **374 行の新規追加**
      ＝ **レビュー対象の差分の一部**である（文脈として読んだだけで済ませない）。

        :188  → #885 の測定時点では、述語は camelCase と SCREAMING_SNAKE しか見ていなかった。

      **偽。** #885（066cb3f）の判定本体は `raw.match(STALE_IDENT)` ただ 1 本で
      （`scripts/governance-check.mjs:1479` @066cb3f）、SCREAMING_SNAKE の述語は存在しない
      （§3 主張 4b）。その記述が当たるのは **#989 の直前**（25f48c6^）の状態で、
      25f48c6 の commit message の文言である。

      **なぜ Low で済み、なぜ挙げるのか。**
        - 済む理由: banner へ写されたのは「lowercase snake_case を見ておらず」だけで、
          **どちらの記述でも真**。伝播した誤りは無い。
        - 挙げる理由: **このファイルは既に「本文へ逐語で昇格される元」として実績がある**
          ——ラウンド 2 の副産物「検知点は種 3 ただ 1 つ」は 3e58b85 が `lib.mjs` へ
          そのまま写した。次に誰かがここを引くとき、機序の側が腐ったまま昇格しうる。
      **修正は不要**（コミット済みの検証記録であり、Step 4 で `workspace/` ごと消える）。
      **PR 本文や規範文書へこの 1 文を引かないこと**だけが申し送りである。

--- 3 度書き換えられた hunk の周辺（依頼項目 3・逆向きの監査） ---

差分が**消した行**の不変条件を名指しし、再確立地点を探す向きで見た。

  - 消えた「導入当時（#891）は全セルで 0 件だったので理由は『測って動かなかったから』」
    → 再確立地点: 新しい 3 文（#885 / 述語の構成 / 述語の拡張）。**すべて実測で真**（§3）。
      **却下の記録としての機能は保たれている**——「なぜ塞がないか」の答えが
      「測って動かなかったから」→「塞ぐと偽陽性が出るから」へ**強い方の理由**に替わった。
  - 消えた「**検知点は種 3 ただ 1 つ**」（39ce1a7 が削除）
    → これは母集団のない全称だったので削除は正しい。ただし**削除で失われた情報は無い**
      （後段が同じことを母集団つきで言う）。
  - 新しい参照を作っていないことを**実測で**確かめた（ラウンド 2 は 1752350 の追加行しか
    見ていなかったので、3e58b85 と 39ce1a7 の追加行を含めて測り直した）:
      $ (1752350 の複製) node scripts/governance-check.mjs > sum-1752350.txt
      $ (39ce1a7)        node scripts/governance-check.mjs > sum-39ce1a7.txt
      $ 両者を **バイト比較 → 完全一致（506 バイト）**
      → 見出し参照 324 / 近傍 19 / 折れうる位置 24 / スクリプトのコメント 111 / 散文の識別子 404
        がすべて不変。banner の新しい `「測定の全セルで 0 件」` は**正準形になっていない**
        （直前にバッククォートの対象が無い）——依頼の「意図的に分けた構造 4」を
        **推論ではなく件数で**確認した。
  - **文の整合**: `:87` の「`#[cfg(test)]` 以降を落とす変換を通しても落ちるのは 1 つ目だけで、
    残る 2 つには述語が要る」と、新しい `:89-94` は矛盾しない。
  - **`.claude/rules/governance-docs.md`「機構の実装の詳細（述語の種類…）を散文へ写さない」**
    との関係: 新しい文は「lowercase snake_case」という述語の字種を書いている。ただし
    **`#885 当時の` と過去形で限定した歴史記述**であり、将来の述語追加で腐らない。
    同 rules 自身が `:16` で「#989 が 3 つ目の述語を足し（照合 70 → 285 件）」と同型の
    歴史記述をしている。**違反ではないと判定する。**
  - **新しい事実誤りは 1 件も見つからなかった。** 型（帰属の誤り）としての再発は無い。

--- Phase 2 / Phase 3（code-reviewer 相当の残りのフェーズ）---

  2b 対称ペア / 2c 新規関数・型 / 2d リソース: **該当なし**（差分に実行行が無い）。
  2e SPEC.md 同期: **対象外**。`SPEC.md` は 4a0493c..39ce1a7 で 1 バイトも動いていない。
  Phase 3 性能: 実行経路の変更ゼロ。governance:check のサマリがバイト一致（上記）。


================================================================================
6. 依頼項目 4 — カテゴリ A〜F（拡張子ではなくコードパスの意味で判定・全件ゼロから再導出）
================================================================================

母集団の正本は `docs/build-commands.md`「変更後の検証チェックリスト」。
**ラウンド 1・2 の判定を引き継がず、差分から立て直した**（ラウンド 2 の ⚠️ F への対処）。

対象差分: `4a0493c..39ce1a7 -- scripts/` = `.mjs` 2 ファイルの**コメント／JSDoc のみ**、
          `1752350..39ce1a7` はそれに `workspace/plan.md` と `workspace/verify-1095-round2.txt` を足したもの。

┌───┬────────────────────────────────┬──────────┬────────────────────────────────────────────┐
│ A │ Rust（`*.rs`）                 │ 該当なし │ 差分に `.rs` は 0 件。意味でも、変更は      │
│   │ fmt / check / clippy / test /  │          │ ガバナンス検査器の散文だけで Rust のビルド │
│   │ doc                            │          │ にも実行にも触れない。`cargo doc` の       │
│   │                                │          │ intra-doc も対象外（`.mjs` に rustdoc なし）│
├───┼────────────────────────────────┼──────────┼────────────────────────────────────────────┤
│ B │ TypeScript（`vitest.config.ts`）│ 該当なし │ `vitest.config.ts` の blob はアンカーと    │
│   │                                │          │ 一致（§0）。差分に `.ts` は 0 件           │
├───┼────────────────────────────────┼──────────┼────────────────────────────────────────────┤
│ C │ 窓生成／表示順・hotkey・       │ トリガー │ 窓も hotkey も slash も触れていない。ただし│
│   │ slash（`npm test` /            │ 該当なし │ **`npm test` は `scripts/**` を含むので    │
│   │ `test:powershell` /            │ ＋一部   │ 上位互換として実行した**:                  │
│   │ `smoke:startup` / `smoke:egui`）│ 実行     │   `npm test` → **36 files / 865 tests      │
│   │                                │          │   passed / exit 0**（無変異・アンカーの複製）│
│   │                                │          │ `test:powershell` → **実行不能**（§6 注 1）│
│   │                                │          │ `smoke:startup` / `smoke:egui` → 該当なし  │
│   │                                │          │ （trace イベント名・hotkey・表示経路の変更 │
│   │                                │          │   がゼロ。実バイナリも未ビルド）           │
├───┼────────────────────────────────┼──────────┼────────────────────────────────────────────┤
│ D │ 表示の見た目（目視 /           │ 該当なし │ 描画コード 0 行。#1106 の「該当判定は広げる│
│   │ `smoke:manual` / `check:colors`│          │ 方向にも外れ、そちらは安全側ではない」に   │
│   │ / `check:input-metrics`）      │          │ 従い、**人間への目視申し送りを出さない**   │
├───┼────────────────────────────────┼──────────┼────────────────────────────────────────────┤
│ E │ git hook（`.githooks/**`）     │ 該当なし │ 差分に無い。なお C で走らせた `npm test` が│
│   │                                │          │ `.githooks/**` も含み緑（36 files の内訳）│
├───┼────────────────────────────────┼──────────┼────────────────────────────────────────────┤
│ F │ ガバナンス文書                 │ **該当・**│ 拡張子は `.mjs` だが、**変更した散文は     │
│   │ （`npm run governance:check`） │ **実行**  │ `headingRefCommentDocs` の走査元そのもの** │
│   │                                │          │ （サマリの「スクリプトのコメント 111 件」）│
│   │                                │          │ ＝ 意味で該当する。                        │
│   │                                │          │   `node scripts/governance-check.mjs`      │
│   │                                │          │   → **exit 0 / 全検査 passed**             │
│   │                                │          │   → 1752350 のサマリと**バイト単位で一致** │
│   │                                │          │ 併せて CI の 2 つ目の入口も回した:         │
│   │                                │          │   `governance-manifest.mjs --compare`      │
│   │                                │          │   → 「構造母集団に差分なし」/ exit 0       │
└───┴────────────────────────────────┴──────────┴────────────────────────────────────────────┘

注 1 — `npm run test:powershell` を **実行不能**と判定した根拠（⚠️ 5 の 2 ラウンドぶんの宿題）:
  **今回は grep 1 経路ではなく、走らせようとして止まる地点を読んだ。**
    `scripts/run-pester.ps1:37-40` は `Resolve-SnotraCargoExecutable` で本体 exe を解決し、
    無ければ **`throw`** する。実測: `C:/workspace/Snotra/target/` に `snotra.exe` は無い
    （`debug/` に在るのは `snotra-settings.exe` のみ）。さらに Pester 6.0.1 も
    `target/pester/` に未取得で PSGallery からの取得を要する。
    → **`cargo build -p snotra` 無しには構造的に起動しない。**
  **関連性は内容で決着させた**（実行できないことを理由に沈黙させない）:
    5 本の `.Tests.ps1` が `BeforeAll` で import するのは**兄弟の `.psm1` だけ**で、
    ガバナンスの JS 層（`lib.mjs` / `checks/`）への結合は 1 本も無い
    （`grep -l "governance\|headingRef\|lib.mjs" scripts/lib/*.Tests.ps1` → 0 件。
      `.psm1` 側の 1 ヒットは `SnotraStartupContract.psm1:18` のコメント内の
      正準形参照で、`.psm1` は `.rs` ではないため今回の変換の射程外）。
    → **`.mjs` のコメント変更が Pester を落とすことはありえない。**
       ただしこれは「実行して緑を見た」ではない。**⚠️ 5 は「実行不能・理由つき」へ
       降格したが、完全には閉じていない**（§8 ⚠️ i）。


================================================================================
7. 依頼項目 5 — 前 2 ラウンドの未解決 ⚠️ の決着
================================================================================

  ⚠️ A（#885 が導入した、は文字列位置に基づく推定を含む）
     → **決着。** 066cb3f の diff で当該行が `+` として入っているのを直接読んだ（§3 主張 4a）。
        推定ではない。加えて `#891` はリポジトリにコミットを 1 本も持たない issue 番号で、
        当該文へ触れていないのは当然と分かった。

  ⚠️ B（0 → 9 の真因が述語の追加である、は反実仮想を直接測っていない）
     → **決着。2 通りで実測**（§3 主張 5）。#885 の木で述語だけを動かす 2×2 と、
        #989 の直後の木そのもの（exit 1 / 2 件）。**`潜在的に` に頼らず真。**

  ⚠️ C（`f.includes("/tests/")` が 3 形を正しく表すか）
     → **決着。** `git ls-files "*.rs" | grep -E "_tests?\.rs$"` → **0 件**。
        `tests` ディレクトリを使わない分け方はリポジトリに存在しない。
        `*/tests/*.rs` の 13 件が形 2＋形 3 の全部である。
     → **ただし「3 形すべてを塞いでも finding は同じ 9 件」（plan.md フェーズ 4）は
        ラウンド 2 からの引き継ぎであって、私が測り直した数字ではない。**
        私が測ったのは**形 1 だけ**（§3 主張 1 の 9 件）。
        ⚠️ C の決着により「形 2・形 3 の母集団は 13 件で全部」は確定したので、
        除外を広げても finding は減りようがなく **9 件は下限**である。
        よって主張の向きは保たれるが、**上限の一致（＝ちょうど 9 件）は私の実測ではない。**

  ⚠️ 2（node_modules がメインリポジトリ側）
     → **決着。** マニフェスト 3 種をアンカーの blob と**内容ハッシュで**照合（§0）。

  ⚠️ 3（複製ではなく稼働中のファイルへ変異）
     → **決着（本ラウンドについて）。** 5 本すべて `git archive` の複製へ当てた。
        稼働中のファイルは 1 バイトも動いていない（`git status` 常時空）。

  ⚠️ 4（種 3 の指しの正準形化を採らない判断）
     → 再提案しない。**この判断は今も正当**——依頼の「意図的に分けた構造 4」と同じ理由で、
        正準形にすると `G-heading-refs` が恒久的に赤くなる。

  ⚠️ 5（Pester 未実行）
     → **降格。実行不能の理由を機構の側から示し、関連性を内容で決着させた**（§6 注 1）。
        「該当なしと判定したので実行していない」という 2 ラウンドぶんの循環は解いた。

  ⚠️ 6（`visible_rows` の教訓の喪失）→ 本差分と無関係。裁定は plan.md にある。変更なし。

  ⚠️ D（`check_only` の脆さを記録すべきか）→ **判断を返さない**（§8 ⚠️ ii）。
        なお `notify.rs:326` の 1 件依存は私も独立に再現した（§3 主張 3）。

  ⚠️ E / F（引き継ぎ・A/B/D/E を再導出していない）
     → **F は決着。** §6 で A〜F を差分からゼロで立て直した。

  ラウンド 1・2 の所見:
     H-1（偽の実測）      → **解消**（§3 主張 1〜3 で再現・確定）
     M-1（由来の誤帰属）  → **解消**（§3 主張 4。置き換えた文に新しい事実誤りは無い）
     L-1（message の単位）→ **申し送りで解消**（plan.md:84）。ただし L-2 として再発（§5）
     L-2（`check_only` の糸 1 本）→ 記録されていない残余のまま（⚠️ ii）
     L-3 / L-4 / L-5      → 変更なし（裁定済み・実害なし）
     ※ ラウンド 1 の L-4 と、本ラウンドの L-4（§5・round2 報告 :188 の機序の誤り）は別物。


================================================================================
8. ⚠️ 確信の持てない所見（本ラウンド）
================================================================================

⚠️ i. **M-1 の変異 `!f.includes("/tests/")` が「誰かが実際に書きうる形」だという
   前提に、私は実績を持たない。** 「6 件の生きた正準形が黙って落ちる」ことと
   「どの層も赤くならない」ことは実測だが、**その変異が起きる確率**は測れていない。
   隣の banner が形 2・形 3 を名指しで列挙していることを状況証拠としたが、
   これは動機の推測である。**M-1 を「機構を足す根拠」に使う場合は、
   AGENTS.md「ガバナンス機構自身の配置を変える」の引き金
   （壊れたとき緑が緑のまま推移するか）で改めて裁定すべきで、私はそこまで踏み込んでいない。**

⚠️ ii. **Pester を「実行不能・関連なし」と結論したが、実行して緑を見てはいない。**
   `throw` する地点を読んだのと結合の不在を測ったのが根拠で、
   **もし `run-pester.ps1` が exe 不在でも一部を走らせる経路を持つなら**私の判定は
   その部分について空振りである（`Invoke-Pester` は `throw` の後にしか無いので
   可能性は低いと見ているが、実行では確かめていない）。

⚠️ iii. **L-3（「すべてその字種である」の全称）を Low に留めた判断に自信がない。**
   AGENTS.md「全称表現は前提条件とセットで書く」を厳格に当てれば書き直し対象だが、
   **この hunk は既に 3 度書き換えられ、2 度とも修正が新しい誤りを生んでいる**。
   4 度目の書き換えの期待損失が、この Low の期待損失を上回ると私は判断した。
   **これは severity の判断ではなく運用の判断であり、主エージェントが覆してよい。**

⚠️ iv. **§4b の数え上げ（「宣言の外に検知点は無い」）は、私が思いついた入口の集合に
   閉じている。** `selectChecks` / `vitest.config.ts` の `include` / `ci.yml` の全 step /
   `governance-manifest.mjs` を読んだが、**「入口の母集団を誰が知っているか」に
   正本は無い**（AGENTS.md「列挙の完全性」）。`e2e.yml`（smoke）は読んだが `.mjs` を
   見ないことしか確認していない。

⚠️ v. **`066cb3f` の複製で得た「散文の識別子 1 件を 25 文書から照合」は、
   `git archive` 展開＋当時の `fs` 走査による再現値であって、当時の実行ログではない。**
   当時の CI 出力は持っていない。数字そのものより
   「候補が極端に少なかった」という向きの結論だけを使うのが安全である。

⚠️ vi. **`.claude/rules/governance-docs.md` の「述語の種類を散文へ写さない」に対する
   私の「歴史記述だから違反ではない」という判定は、規範の解釈である。**
   規範の書き手がどちらを意図したかは測っていない。ラウンド 2 は逆に
   「述語の一覧を書く必要はない」と助言しており、**そこは意見が割れている**。

⚠️ vii. **`3e58b85` / `39ce1a7` は squash でメッセージが消えるので、L-2 の実害は
   PR 本文次第である。** PR 本文を私は読んでいない（この worktree からは
   PR が存在するかも確かめていない）。

⚠️ viii. **「3 形すべてを塞いでも 9 件」はラウンド 2 の実測を引き継いでいる**（§7 ⚠️ C）。
   私が独立に測ったのは形 1 だけで、9 が**下限**であることまでしか自分では言えない。


================================================================================
9. 主エージェントへの申し送り（メタ）
================================================================================

**この hunk は 3 度書き換えられ、2 度とも「修正が周辺に新しい誤りを生む」型が出た。**
本ラウンドで**書き換えられた当の文には、事実誤りが 1 件も見つからなかった**（§3・§5）。

  - **M-1 が指しているのは、3 度書き換えられた文（`lib.mjs:574`）ではない。**
    574 は真である（32/523 と 36/865 の両方で実測）。過大なのは**その 1 行上の 572**
    （「種 3 が*意図*を固定する」）で、**4a0493c から 1 文字も変わっていない既存の節**である。
    → **574 を 4 度目に書き換える理由は無い。凍結してよい。**
    → 一方、**572 の指示語を狭める編集は、検知の側を 1 バイトも触らずに読者の推論を
      正しくする**（「種 3 が固定するのは*その変換*である」の向き）。**3 度書き換えられた
      574 とは別の節**なので、この差分の危険な hunk へ手を入れずに済む。
      **初稿で私は「散文を直しても差は縮まらない」と書いたが、これは誤りだった**
      ——指示語を 572 / 574 で束ね直して撤回する。採否は主エージェントの判断。
    → **`.rs` の腕の非対称そのものは触らない**（依頼の「意図的に分けた構造 3」）。
      M-1 は「揃えよ」ではなく「守る側の主張が実際の検知より広い」という報告である。
  - L-1 / L-2 は**文言ではなく PR 本文で閉じられる**（母集団の単位を書く）。
  - L-3 は ⚠️ iii のとおり、**触らない方を私は勧める。**
  - L-4 は**修正不要**。PR 本文・規範文書へ引かないことだけ。

→ 私の推奨: **`lib.mjs:574` と banner は凍結。散文を触るなら `lib.mjs:572` の 1 節だけ。
   L-1 / L-2 / L-4 は PR 本文で処理する。**


================================================================================
10. コミット・push・リポジトリの状態
================================================================================

コミット・push は行っていない。
リポジトリのファイルは 1 バイトも変更していない（変異はすべて scratchpad の複製）。

    $ git rev-parse HEAD → 39ce1a79ffe8770d0db7d527b169eec822a354d4
    $ git status --porcelain → 空

**注**: メインリポジトリ側（`C:/workspace/Snotra`）の `git status` には、この報告書
`workspace/verify-1095-round3.txt` が untracked として現れる。**これは依頼された成果物
であって、制約違反ではない。** 「変更したら戻す」の対象は検証のための一時的な実測であり、
それは 1 件も残っていない。

**この報告書が `governance:check` を赤くしないことも実測で確かめてある**（推論ではない）:
`workspace/verify-1095.txt` は**コミット済みで `productionOnly` を 17 件持つ**
（camelCase＝ `STALE_IDENT` に当たる綴りで、production のソースからは消えている）のに、
アンカーの `node scripts/governance-check.mjs` は **exit 0** である。
→ `workspace/**` はどの検査の母集団にも入っていない（`headingRefDocs` は
  `!f.startsWith("workspace/")` で明示除外し、`.txt` はどの走査元の拡張子でもない）。
  本報告書は**接頭辞と拡張子の二重で射程外**であり、`.md` にしていないので
  Write の拒否にも当たらない。
