検証・レビュー報告 — #1095 `productionOnly` 削除（sha 4a0493cbf2b178c58aa208713258cd53fc5fbd5a）
担当: 検証委譲エージェント / 2026-08-21

================================================================================
0. アンカー確認
================================================================================

セッション開始時の worktree HEAD:
    git rev-parse HEAD → 361c13bf249a71b3575b221a6159a2c380d49047
    （= main の先端。指定 sha ではない）

→ **`git checkout --detach 4a0493c...` を要した。** 実行後:
    git rev-parse HEAD → 4a0493cbf2b178c58aa208713258cd53fc5fbd5a
    git status --porcelain → 空（clean）

以降のすべての測定はこの detached HEAD 上で行った。
終了時も `git status --porcelain` は空（§3 の変異注入 2 件の復元後に再確認済み）。

**測定環境についての注記（重要）**: この worktree に `node_modules` が無い。
`package.json` / `package-lock.json` / `vitest.config.ts` が 4a0493c と 361c13b で
バイト一致することを確認した上で（`git diff 4a0493c 361c13b --stat -- ...` が空）、
メインリポジトリの導入を使って vitest を起動した:
    node C:/workspace/Snotra/node_modules/vitest/vitest.mjs run <target>
Node のモジュール解決は worktree から上方向に歩いて C:/workspace/Snotra/node_modules へ
到達するため、テスト側の import も同じ導入で解決される。
（`.claude/rules/safety-nets.md` が警告する「解決失敗と検知の exit code が同じ」形は、
  ベースラインが 32 files / 523 tests passed で緑だったことで排除されている）


================================================================================
1. 検証表（`docs/build-commands.md`「変更後の検証チェックリスト」カテゴリ A〜F 全件）
================================================================================

差分が触るファイル（`git show 4a0493c --stat`）:
    scripts/governance/checks/G-stale-identifiers.mjs | 11 +++-----
    scripts/governance/lib.mjs                        |  5 ++-
    scripts/governance/lib.test.mjs                   |  4 +-
    workspace/plan.md                                 | 12 +++---

--------------------------------------------------------------------------------
A. Rust ファイル（*.rs）を変更した場合                              → **該当なし**
--------------------------------------------------------------------------------
根拠: `.rs` は 1 ファイルも差分に無い。**拡張子ではなくコードパスの意味でも該当しない**——
削除された `productionOnly` は呼び出し元ゼロであり（親コミットで実測: `git grep -n
productionOnly 4a0493c^ -- scripts/` の 4 件は定義 1 + 散文 3 で、`productionOnly(` の
呼び出し行は 0 件）、`.rs` を読み出す経路の入力も出力も 1 バイトも動いていない。
`cargo` 側の成果物（コンパイル・lint・rustdoc）に到達する変更は存在しない。

--------------------------------------------------------------------------------
B. TypeScript ファイル（vitest.config.ts 等）を変更した場合          → **該当なし**
--------------------------------------------------------------------------------
根拠: 残る `.ts` は `vitest.config.ts` のみで、差分に含まれない
（4a0493c と 361c13b の間でもバイト一致を確認済み）。

--------------------------------------------------------------------------------
C. ウィンドウ生成／表示順・ホットキー・スラッシュコマンド            → トリガー該当なし
   ただし `npm test` は **実行**（下記）
--------------------------------------------------------------------------------
トリガー該当なしの根拠: 窓・ホットキー・スラッシュコマンドのいずれにも触れていない
（差分は `scripts/governance/` の 3 ファイルと `workspace/plan.md` だけ）。
`npm run test:powershell` / `smoke:startup` / `smoke:egui` は **該当なし**——実バイナリと
trace イベントを測る検査であり、ガバナンス検査器とは結合が無い。裏取りとして
`git grep -n governance -- "*.ps1" "*.psm1"` を実行したところ、ヒット 2 件は
`SnotraStartupContract.psm1:18` と `manual-smoke.ps1:72` の**規範への参照**だけで、
`governance-check.mjs` を呼ぶ PowerShell は存在しない（この根拠の弱さは ⚠️ 5）。

ただし C が挙げる `npm test`（= `vitest run`）は、差分の在る `scripts/` を母集団に含むため
**実行した**:

    $ node C:/workspace/Snotra/node_modules/vitest/vitest.mjs run
    Test Files  36 passed (36)
         Tests  865 passed (865)
    exit 0

    $ node C:/workspace/Snotra/node_modules/vitest/vitest.mjs run scripts/
    Test Files  32 passed (32)
         Tests  523 passed (523)
    exit 0

    → commit message が主張する「32 ファイル 523 テスト passed」と一致。

--------------------------------------------------------------------------------
D. UI のスタイル・レイアウト・テキスト表示に影響する変更            → **該当なし**
--------------------------------------------------------------------------------
根拠: 描画するコードは 1 行も無い。**該当判定を広げていないことを明記する**——
D 節自身が「該当判定は広げる方向にも外れ、そちらは安全側ではない。過大な申し送りは
本当に要る項目を埋もれさせる」（#1106）と定めており、本差分は表示経路に到達しない。
人間への視覚スモークの申し送りは**不要**。

--------------------------------------------------------------------------------
E. git hook（.githooks/**）を変更した場合                           → **該当なし**
--------------------------------------------------------------------------------
根拠: `.githooks/` 配下は差分に無い。
（なお E の必須コマンド `npm test` は C で実行済み・緑。`.githooks/githooks.test.mjs` は
  その 36 ファイルに含まれる）

--------------------------------------------------------------------------------
F. ガバナンス文書（*.md・.claude/rules/・.claude/skills/・workflow）  → **実行**
--------------------------------------------------------------------------------
該当の根拠: `workspace/plan.md` が `.md` であることに加え、より本質的に
`lib.mjs` / `G-stale-identifiers.mjs` のコメントは `headingRefCommentDocs`（#1138）の
母集団に**入っている**——書き換えた散文そのものが G-heading-refs / G-near-heading-refs /
G-folded-heading-refs の走査対象である。

    $ node scripts/governance-check.mjs        # = npm run governance:check（package.json で確認）
    governance:check — 全検査 passed（検査 21 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 /
    恒久規範 常時ロード 16972 字・rules 13080 字 / 見出し参照 324 件を md 48 件 + .rs 101 件 +
    スクリプトのコメント 111 件から照合 / workspace member 4 件の lints opt-in / clippy 禁止 8 件 /
    散文の識別子 404 件を 34 文書から照合 / 近傍の見出し参照 19 件 / 折れうる位置 24 件 /
    ADR 70 本の名前 / ADR の短縮引用 376 件）
    exit 0

commit message が名指す 4 つの数はすべて一致した:
    散文の識別子 404 件 / 34 文書   ✓
    見出し参照 324 件               ✓
    近傍の見出し参照 19 件          ✓
    折れうる位置 24 件              ✓

**「サマリ行が変更前とバイト単位で一致」の裏取り（代理ではなく機構で測った）**:
親ツリーを作って真の byte-diff を取ることはしなかった（⚠️ 1）。代わりに、
書き換え前後の散文が実際に何件の参照を供給するかを実測した
（合成入力へ `HEADING_REF` / `NEAR_REF` / `isRefTargetSpelling` を当てた）:

    isRefTargetSpelling('productionOnly') = false
    isRefTargetSpelling('lib.test.mjs')   = true
    旧 5 行: HEADING_REF 受理 0 件 / NEAR_REF 受理 0 件
             （NEAR_REF は `productionOnly`「G-stale-identifiers との対称性の完成」に
               正規表現としては**当たった**が、`isRefTargetSpelling` が対象綴りとして
               棄却するため計上されない）
    新 6 行: HEADING_REF 受理 0 件 / NEAR_REF 受理 0 件

→ 旧側も新側も供給 0 件。ゆえに 324 / 19 が動かないのは偶然ではなく機構の帰結である。
   `散文の識別子 404 件 / 34 文書` は母集団が `.claude/**` + `docs/**` + `STALE_EXTRA_DOCS` で
   `scripts/**` を含まないため、`scripts/` のコメント編集では原理的に動かない。


================================================================================
2. レビュー所見（`.claude/agents/code-reviewer.md` の 3 フェーズに沿う）
================================================================================

--- Critical ---
なし。

--- High ---

[H-1] `scripts/governance/checks/G-stale-identifiers.mjs:88` —
      **この差分が書き直した当の 3 行の中に、今日すでに偽の実測主張が残っている。**

  現在の本文（:87-89。**:87 を書き換え、:88-89 は同じ hunk で流し直された**——つまり
  「隣に在った」のではなく、このコミットが**再度書いた**行である）:

      //   `#[cfg(test)]` 以降を落とす変換を通しても落ちるのは 1 つ目だけで、残る 2 つには述語が要る。
      //   現時点でこの穴に落ちた finding は 1 件も無く（測定の全セルで 0 件）、測って動かなかったものを
      //   入れないだけの理由で開けてある

  **実測（§3 の変異注入 2 本目）**: 語彙源の側（`currentVocabulary`・同ファイル :149）へ
  `#[cfg(test)]` 以降を落とす変換——`productionOnly` と逐語で同じ `src.split(/^#\[cfg\(test\)\]/m)[0]`
  ——を当てると:

      currentVocabulary 長さ: 1,609,968 → 1,088,003 字（−32.4%。変異が実際に効いた証跡）
      node scripts/governance-check.mjs → **exit 1 / 9 件の不整合**

      docs/architecture.md:140          `check_only`
      docs/build-commands.md:104        `input_text_sits_vertically_centered_for_both_body_and_hint`
      docs/development-principles.md:159 `run_pass`
      docs/development-principles.md:184 `assert_read_once_in_this_file`
      docs/development-principles.md:186 `owners_of`
      docs/development-principles.md:188 `owners_of`
      docs/development-principles.md:194 `method_body`
      docs/development-principles.md:194 `owners_of`
      SPEC.md:1158                      `check_only`

  → **「現時点でこの穴に落ちた finding は 1 件も無く（測定の全セルで 0 件）」は今日 偽である。**
    9 件動く。

  **9 件が「過剰な変異の副産物」ではないことを裏取りした**（safety-nets.md
  「注入したことと、注入が正しい強さであることは別である」）。6 つの識別子はすべて
  `#[cfg(test)]` の内側にしか存在しない:
      method_body                       src-tauri/src/egui_shell/launcher_controller.rs:1509（cfg(test) は :1493）
      owners_of                         同 :1719
      assert_read_once_in_this_file     src-tauri/src/egui_shell/view.rs:1400（cfg(test) は :1311）
      run_pass                          同 :1427
      input_text_sits_vertically_...    同 :2089
      check_only                        src-tauri/src/egui_shell/notify.rs:326 の文字列リテラル
                                        （cfg(test) は :275）。実体は config の enum 値で、
                                        SPEC.md:1158 は auto_update の 3 モードの 1 つとして
                                        正当に引いている

  **何が壊れていて、何は壊れていないか（重要な区別）**:
    - 壊れている: **測定の主張**。「0 件」は 2026-08-21 現在 9 件である。
      #891（母集団を `docs/**` + SCREAMING_SNAKE へ拡大）と #993（型修飾形の追加）で
      照合面が大きく広がったのに、測定値だけが当時のまま持ち越されている。
    - 壊れていない: **結論**（穴を開けたままにする）。9 件はいずれも「文書がテスト専用の
      識別子・config 値を正当に引いている」形＝偽陽性であり、穴を塞ぐと 9 件の誤爆が出る。
      ゆえに決定はむしろ**今日のほうが強く支持される**。
    - 直すべきは理由づけの向きである: 「測って動かなかったから開けてある」→
      「塞ぐと文書が正当に引くテスト専用の名が誤爆する（2026-08-21 実測で 9 件）から開けてある」。

  **なぜ High か**: このコミットの表題は「禁止から**偽の前提**を落とす」であり、
  隣接する 2 か所の偽の前提を正確に落としている。その同じ hunk の中に、
  **同じ型の（現在形で書かれ、拡大した母集団の下で腐った）実測主張が 1 件残った**。
  `.claude/rules/governance-docs.md` の書く約束 (3)「古い情報を残さない——触った節の隣にある
  主張が今も真か見る」に、触った節そのものが当たっている。

  **`docs/adr/ADR-stale-identifier-detector-scope.md:108` の同文は直さなくてよい**——
  ADR 本文は決定日時点の記述として凍結する（`ADR-adr-frozen-history`）。腐っているのは
  「現時点で」と現在形で名乗っている banner の側だけである。

--- Medium ---
なし。

--- Low ---

[L-1] `scripts/governance/lib.mjs:573` — 禁止の相手方を**機械照合の付かない序数**で指している。

  該当の文（実測: `grep -n "種 3 がそれを固定する" scripts/governance/lib.mjs` → 573）:
      * 意図であり、`lib.test.mjs` の種 3 がそれを固定する（入れるとあの it が落ちる）。

  このコミットは「消すだけだと**相手方を探せない警告**が残り、次の保守者が警告ごと消す経路が
  開く」を理由に、識別子を消すのではなく書き直した。その物差しで測ると:

  (a) 相手方は**探せるようになった**（旧文には指しが無かった）。目的は達している。
  (b) ただし指しは**一意でない**（実測）:
          git grep -n "種 3" scripts/governance/lib.test.mjs
          111:  it("種 3: `#[cfg(test)]` の内側のコメントも見る（テストコードを母集団から外さない）", ...
          191:  it("種 3: ブロックコメント（`/* */`）の中も見る——継続行は行頭に `//` を持たない", ...
      同一ファイルに「種 3」で始まる `it` が 2 件。文脈（同じ文が `#[cfg(test)]` を名指す）で
      人間は :111 と読めるし、2 件は別の `describe` に居るので実害は小さい。
  (c) **機械照合が付いていない**。:111 の `it` が改題されれば、この指しは黙って腐る——
      書き直しが閉じようとした当の減衰の型である。しかも機械照合の付く選択肢は在り、
      **実測で着地する**: 合成スナップショット（`lib.test.mjs` は実ファイルの内容そのまま）へ

          `lib.test.mjs`「種 3: `#[cfg(test)]` の内側のコメントも見る」

      を蒔いて `scanHeadingRefs` を通すと **checked = 1 / findings = 0**（解決し、着地した）。
      `ANCHOR_SPECS` の第 4 スペック `/^\s*(?:describe|it)\(\s*"([^"]*)"/` が `it(...)` の
      タイトルをアンカーとして供給し、`isRefTargetSpelling` は `.mjs` を受理するため。
      （対照: 序数を外した「`#[cfg(test)]` の内側のコメントも見る」は前方一致 false。
        照合は前方一致ゆえ頭の「種 3: 」は落とせない）

  **commit message の理由づけ自体は成り立っている**——「「」を外せば HEADING_REF も NEAR_REF も
  当たらないので、正準形の誤設置と近傍形の誤爆を構造的に避けられる」は真である。
  私が実測で示したのは「**機械照合の付く選択肢が在り、それは着地する**」までであって、
  書かれた理由が偽だということではない。

  → 提案（採否は著者判断・⚠️ 4）: 正準形へ載せると序数は残っても「ずれても誰も気づかない」
     性質が消え、`it` の改題を CI が名指しで落とす。向きはリポジトリ自身の裁定と一致する
     （`docs/adr/ADR-canonical-heading-references.md:46`「テスト名を指す参照は実在の有用な
     指しであり、散文へ落とすと読み手が指し先を辿れなくなる」）。代償はタイトルが実質凍ること。

  ※ 依頼の「意図的に分けた構造 4」（banner の「受容する残余」を正準形へ格上げしない）とは
     **別の対象**である。あちらは `G-stale-identifiers.mjs` の行コメント箇条書きで、行頭が `/` ゆえ
     `ANCHOR_SPECS` の第 3 スペックに当たらずアンカーが存在しない。こちらは `it(...)` タイトルで、
     アンカーが**存在することを実測した**。
  ※ `.claude/rules/governance-docs.md`「序数で他を指してはならない」は**引かない**。あの規範が
     禁じるのは「序数で名づける実務」であり、`種 1..5` という採番は本差分より前から
     `lib.test.mjs` に在る。本差分がしたのは既存の名で指したことだけで、採番を作ってはいない。

[L-2] commit message の「残る `productionOnly` の出現 4 件」は、**除外条件が本文に無い**。
      実測:
          git grep -l productionOnly           → 6 ファイル
              docs/adr/ADR-stale-identifier-detector-scope.md
              docs/superpowers/plans/2026-08-15-governance-check-per-check-split.md
              workspace/adversarial-1095.txt / plan-review-production-only.md /
              plan.md / research.md
          git grep -c productionOnly -- . ":!workspace/"  → ADR 1 件 + superpowers 3 件 = 4 件
      「4 件」が真になるのは `workspace/` を除いたときだけで、その但し書きは
      `workspace/research.md` にしか無い。**squash でこの本文が main の commit message になる**のに、
      `workspace/` はマージ後の main に残らない（＝但し書きだけが消える）。将来「4 件」を検算しようと
      素の `git grep productionOnly` を打った読者は 6 ファイルを見る。
      `AGENTS.md`「検証の作法（全タスク共通）」の「数え上げも同じ強さである」に当たる形。
      → 「`workspace/` を除いて 4 件」と 1 語足すだけで閉じる。

[L-3] `workspace/plan.md:98` のフェーズ 2 のチェックボックスが `- [ ]` のまま。
      これは `gh pr create` を pre-bash hook が拒む条件（#749）である。
      **本報告 §3 の測定がこの項目そのものを満たす**——「`G-heading-refs.mjs` の
      `scanHeadingRefs` へ `#[cfg(test)]` 切り落としを注入し、「種 3」が赤くなることを
      確認して復元する」を実施し、赤・道連れ無し・内容ハッシュ一致まで測った。
      （チェックを入れる操作は差し控えた——本委譲の制約「変異注入以外でリポジトリの
        ファイルを変更しない」に当たるため。主エージェント側で入れてほしい）

[L-4] 削除した JSDoc が `visible_rows` の実測記録を道連れにした——ただし**裁定済み**。
      消えた 2 行は「母集団と読み手の両方に適用する——読み手側で落とさないと
      「テストだけが読む」フィールドが読まれている側へ落ちる（`visible_rows` で実測）」。
      これは #793 の G12（config 到達性検査）が両側へ変換を当てた理由の記録であり、
      G12 は #897 で撤去済み。`git grep -n 読み手 -- docs/ scripts/ .claude/` に
      `scripts/governance/` の後継は 0 件。ただし:
        - G12 撤去の着地先は `docs/development-principles.md`「config の値は到達性の検出器を持たない」
          に在り、失われた値を引用している生きた記述は無い。
        - `workspace/plan.md` の「軽微（3 件・全件採用）」が
          「`visible_rows` の実測記録は**意識的に落とす**——G12 ごと撤去済みで、保存義務は薄い」と
          裁定を記録している。
        - `AGENTS.md` の撤去トリガーが要求する「`PERFORMANCE.md` へ出所つきで着地」は
          性能数値に向いた条項で、本件は性能数値ではない。
      → **欠陥ではない。閉じる。** 記録として残す（残る不確かさは ⚠️ 6）。

[L-5] `workspace/plan.md:23` は「この文を書き直せば（近傍の見出し参照）19 が動くのが自然である」と
      予測したが、動かなかった。機序は `isRefTargetSpelling` が `productionOnly` を
      対象綴りとして棄却することで（§1-F の実測。NEAR_REF の正規表現自体は当たっている）、
      計画は「動いたときの問いは……」と両睨みで書いていたので実害は無い。
      将来の読者が「近傍検査が旧文を見ていた」と誤読しないよう機序を残す。

--- Phase 1: 実装検証 ---

- 差分は意図どおり。`productionOnly` の定義（JSDoc 2 行 + 本体 3 行 + 後続空行）が消え、
  それを名指していた 3 か所の散文が変換の名で言い直されている。消し忘れは無い
  （`git grep productionOnly` の残存は L-2 のとおり ADR 1 + superpowers 3 で、
  いずれも `staleIdentifierGuideDocs` の「歴史記録 2 種」除外に入る＝母集団外）。
- **狙った 2 件の偽の前提は正確に落ちている。** 旧文の「G-stale-identifiers との対称性の完成」
  （＝ #897 以降存在しない非対称）は消え、禁止は変換の名で言い直された。
  親コミットで `productionOnly(` の呼び出しが 0 件であることを自分で実測して裏を取った。
- **ただし同じ hunk に 3 件目の偽の前提が残った**——H-1。
- banner の「受容する残余」を保存した理由づけは検算した: `grep -n production
  scripts/governance/checks/G-stale-identifiers.mjs` → 3 件（:55 / :136 / :183）。
  commit message の「ヘッダが 3 度使う「production の」という語の唯一の訂正」は正確。
- 新しく入った「残る 2 つには述語が要る」は新規の未測定の断定ではない——
  `docs/adr/ADR-stale-identifier-detector-scope.md:108` に同一の語で既に在る。
- シークレット露出・入力バリデーション・システム境界: 該当なし。
- 与えられた「意図的に分けた構造」4 件は、いずれも重複として挙げていない。
  うち 1 件は裏取りした——`stripRustComments` は同ファイル :149 から現に呼ばれている。

--- Phase 2: 計画判断の検証 ---

2a. plan.md 不変条件との照合
    フェーズ 1 の 6 項目すべてが `- [x]` で、6 項目とも私が独立に再測して一致した
    （governance exit 0 / サマリ 4 数値 / 523 テスト / grep の全件振り分け）。違反なし。
    ただし受け入れ条件 5「測定結果と却下理由を保存し」は、**保存した測定結果が今日偽である**
    ことまでは問うていない（H-1）。計画の側の死角である。

2b. 対称コードパス
    対称チェック: 「母集団側で `#[cfg(test)]` を落とす」↔「読み手側で落とす」
    判定: **不要**——全ケース列挙: この対は G12 のものであり、G12 の 2 つの呼び出し点は
    #897 で**両方**消えている（`git log -S productionOnly -- scripts/` の b2ff79c）。
    片側だけが残る状態は存在しない。
    対称チェック: 「G-stale-identifiers の語彙源」↔「G-heading-refs の `.rs` の腕」
    判定: **不要**——非対称は意図（依頼の「意図的に分けた構造 3」・`lib.mjs` の
    `headingRefSourceDocs` の JSDoc が根拠）。§3 の変異注入はこの意図が機械で守られていることを
    実測しており、揃える提案は行わない。

2c. DRY / 関数カバレッジ: 削除のみで新規・変更関数は無い。該当なし。
2d. リソースライフサイクル: 生成／破棄を持つリソースは差分に無い。該当なし。
2e. SPEC.md 同期: **SPEC 対象外**。`grep -ci "governance\|G-heading-refs\|G-stale" SPEC.md` → 0。
    ガバナンス検査器の内部は SPEC.md に文書化された挙動ではない。SPEC.md の更新は不要で、
    差分に含まれないのは正しい。検査ファイルの追加・削除も無いため `registry.mjs` の
    `checks/` 走査にも影響しない（実測: 検査 21 件のまま）。
2f. 「変更不要」判断の再評価
    再評価: `scripts/governance/checks/G-stale-identifiers.test.mjs`
      ケース A: `productionOnly` を import している → 親コミットで `git grep` 0 件（未 export）→ OK
      ケース B: 語彙から 1 語消えて赤に転ぶ → governance:check exit 0 で実測 → OK
      結論: 変更不要を確認。
    再評価: `docs/adr/ADR-stale-identifier-detector-scope.md:108`
      ケース A: 生きた層が ADR のこの行を根拠に使っている → ADR 本文は凍結（`ADR-adr-frozen-history`）
      ケース B: G-stale-identifiers の母集団に入り赤になる → `docs/adr/` は歴史記録として除外 → OK
      結論: 変更不要を確認（H-1 の直しも ADR 側へは及ばない）。
    再評価: `stripRustComments`
      ケース A: 呼び出し元ゼロで同じ腐りが残る → :149 に生きた呼び出し → 変更不要
      結論: 変更不要を確認。

--- Phase 3: パフォーマンス ---
該当なし。削除された関数は呼び出し元ゼロで実行経路に無く、書き換えたのはコメントだけ。
`governance:check` の実行時間・照合件数（324 / 19 / 24 / 404）は変更前後で同一。


================================================================================
3. 変異注入の結果（フォールトインジェクション・2 本）
================================================================================

########## 変異 1（依頼の指定）— 読み手側 `G-heading-refs.mjs` ##########

目的: `.rs` の走査元がテストコードを外さないという禁止を守る機構が、削除で損なわれて
いないかを測る。

--- 手順 1-2: 注入した正規表現が実際に効くことを、注入の**前に**確かめた ---

    $ node <scratchpad>/splitprobe.mjs
    parts.length = 2 (2 なら切れている)
    parts[0] = "fn f() {}\n\n"
    head contains 「Git 運用」 ? false (false であるべき)
    orig contains 「Git 運用」 ? true (true であるべき)

→ `/^#\[cfg\(test\)\]/m` は種 3 の fixture を実際に切り、参照を含む後半を落とす。
   エスケープが落ちた無効な変異ではない。

--- 注入した実バイト（shell のエスケープ層を通さず Edit で書き、git diff で読み返した） ---

    @@ -38,7 +38,8 @@ export function scanHeadingRefs(snapshot, docs) {
       for (const doc of docs) {
    -    const text = snapshot.read(doc);
    +    let text = snapshot.read(doc);
    +    if (doc.endsWith(".rs") && text != null) text = text.split(/^#\[cfg\(test\)\]/m)[0];
         if (text == null) {

--- 手順 3: 赤くなったテストと、道連れの有無 ---

    $ node .../vitest.mjs run scripts/governance/lib.test.mjs      → exit 1
      × 種 3: `#[cfg(test)]` の内側のコメントも見る（テストコードを母集団から外さない）
        AssertionError: expected [] to have a length of 1 but got +0
        at scripts/governance/lib.test.mjs:117:15   （expect(f).toHaveLength(1)）
      Tests  1 failed | 68 passed (69)

    $ node .../vitest.mjs run scripts/                              → exit 1
      Test Files  1 failed | 31 passed (32)
           Tests  1 failed | 522 passed (523)

**赤くなったのは「種 3」ただ 1 件。道連れは 0 件**（scripts/ の 523 テスト中 522 が緑のまま）。
失敗は期待どおり `toHaveLength(1)` で received 0——「参照が母集団から落ちた」という
本来の回帰の姿そのものであり、変異が本来の回帰より強いわけではない。

--- 追加の実測（依頼の手順外・禁止の射程を測るため） ---

変異を入れたまま `node scripts/governance-check.mjs` を走らせた:

    見出し参照 **307** 件を md 48 件 + .rs 101 件 + スクリプトのコメント 111 件から照合
    ... exit **0**（全検査 passed）

→ **禁止された変換を入れても、稼働中の `governance:check` は緑のまま推移する。**
   静かに 324 → 307 へ縮む（`.rs` の `#[cfg(test)]` の内側に在る**生きた正準形 17 件**が
   照合されなくなる）。具体対象を 1 件名指しすると:
       snotra-settings/src/tabs/visual.rs:396
         `.claude/rules/safety-nets.md`「フォールトインジェクションでは、稼働中のガードを弱めない」
       （同ファイルの `#[cfg(test)]` は :351。396 > 351 ゆえ変異で落ちる）

   これは `.claude/rules/safety-nets.md` が #1143 で名指した「緑のまま保護が失効する」形と
   同型であり、**その失効を捕まえる検知点はリポジトリ全体で「種 3」ただ 1 つ**である。
   ＝ 禁止を「消さずに言い直す」という本コミットの判断は、実測で支持される。

--- 手順 4: 内容ハッシュによる巻き戻しの照合（mtime は使っていない） ---

    $ git restore scripts/governance/checks/G-heading-refs.mjs
    $ git rev-parse 4a0493c:scripts/governance/checks/G-heading-refs.mjs
      9e9a40ca097ea27e9e2068467ef3b0eba3b8442e     ← コミット済み blob
    $ git hash-object scripts/governance/checks/G-heading-refs.mjs
      9e9a40ca097ea27e9e2068467ef3b0eba3b8442e     ← 復元後の作業ツリー
    $ git status --porcelain → 空

→ 一致。自分で取った事前ハッシュではなく**コミット済み blob** を基準にしている。

--- 手順 5: 復元後の再測定 ---

    $ node .../vitest.mjs run scripts/       → 32 files / 523 tests passed, exit 0
    $ node scripts/governance-check.mjs      → 見出し参照 **324** 件に復帰, exit 0


########## 変異 2（追加）— 語彙源側 `G-stale-identifiers.mjs:149` ##########

目的: banner が**保存した**実測主張「現時点でこの穴に落ちた finding は 1 件も無く
（測定の全セルで 0 件）」が、今日も真かを測る。方向として脆い——変換は語彙を**狭める**
ので、finding は増える側にしか動かない。しかも当時の測定以降、#891 が母集団を
`docs/**` + SCREAMING_SNAKE へ、#993 が型修飾形へ広げている。

--- 注入した実バイト ---

    @@ -146,7 +146,8 @@ export function currentVocabulary(snapshot) {
    -    parts.push(/\.(ps1|toml|yml)$/.test(f) ? src.replace(/#.*$/gm, " ") : stripRustComments(src));
    +    const cut = f.endsWith(".rs") ? src.split(/^#\[cfg\(test\)\]/m)[0] : src;
    +    parts.push(/\.(ps1|toml|yml)$/.test(f) ? cut.replace(/#.*$/gm, " ") : stripRustComments(cut));

    （`src.split(/^#\[cfg\(test\)\]/m)[0]` は削除された `productionOnly` の本体と逐語で同じ）

--- 変異が効いたことの証跡（exit code だけでは無効な変異と区別できない） ---

    currentVocabulary 長さ  変異前 1,609,968 字 → 変異後 1,088,003 字（−32.4%）

--- 結果 ---

    $ node scripts/governance-check.mjs → **exit 1 / 9 件の不整合**（内訳は H-1）

→ **保存された主張は偽。0 件ではなく 9 件動く。** 詳細・裏取り・解釈は H-1。

--- 巻き戻し ---

    $ git restore scripts/governance/checks/G-stale-identifiers.mjs
    $ git rev-parse 4a0493c:scripts/governance/checks/G-stale-identifiers.mjs
      e7f0ac604ec4bf093b78d4d1f6e6fa811f6bdbee     ← コミット済み blob
    $ git hash-object scripts/governance/checks/G-stale-identifiers.mjs
      e7f0ac604ec4bf093b78d4d1f6e6fa811f6bdbee     ← 復元後
    $ git status --porcelain → 空

終了時の HEAD は 4a0493cbf2b178c58aa208713258cd53fc5fbd5a、`git status --porcelain` は空。


================================================================================
4. コミット・push
================================================================================
行っていない（禁止どおり）。リポジトリのファイルは変異注入 2 か所のみ触り、両方とも
コミット済み blob と一致するまで復元済み。


================================================================================
5. ⚠️ 確信の持てない所見（返り値の列挙に必ず含める指定）
================================================================================

⚠️ 1. **「サマリ行が変更前とバイト単位で一致」を真の byte-diff では測っていない。**
   親ツリーを作って `governance-check` を走らせる代わりに、(a) HEAD のサマリが commit
   message の名指す 4 数値と一致すること、(b) 書き換え前後の散文が受理される参照を
   0 件しか供給しないこと、(c) `散文の識別子` の母集団が `scripts/**` を含まないこと、
   の 3 点で押さえた。サマリ行には私が照合していない項目（ADR 70 本 / 短縮引用 376 件 /
   clippy 禁止 8 件 等）も在り、それらが親と一致することは**未測定**である。
   ただしそれらの母集団に `scripts/governance/*.mjs` は入らないので、動く筋は見えない。

⚠️ 2. **測定環境がこの worktree の導入ではない。** `node_modules` が無いため
   メインリポジトリの導入で vitest を起動した（§0）。マニフェスト 3 種のバイト一致は
   確認したが、**メインの `node_modules` が lockfile から乖離していないことは確認していない**。
   乖離があれば私のテスト結果はそれを継承する。

⚠️ 3. **変異を複製ではなく稼働中のファイルへ当てた（射程を明示する）。**
   `.claude/rules/safety-nets.md`「フォールトインジェクションでは、稼働中のガードを弱めない
   ——複製に変異を当てる」は複製を求めており、依頼文と `workspace/plan.md` フェーズ 2 は
   実ファイルへの注入を指定している。私は指定に従った。
   **射程**: 弱めていないものを名指すと——検査対象の**ガード（種 3 のテスト）は一度も
   無効化していない**（変異の被験体は検査器の側であり、ゲートの側ではない）。コミットも push も
   行っておらず、この worktree に対して CI は走っていない。`governance-check.mjs` は
   オンデマンドの報告器で、常時ゲートではない。複製規則の先例（#504）は `pre-commit` を
   実際に無害化して main 保護が消えた事例であり、本件はその形ではない。
   それでも **worktree の検査器が変異した状態だった窓が 2 回（各数分）ある**のは事実で、
   複製で回すほうが規則に忠実だった可能性は残る。

⚠️ 4. **L-1 の「正準形へ載せる」提案の採否は、私には決めきれない。**
   正準形にすると `it` のタイトルが実質凍る（改題すると CI が赤くなり、参照も直す必要がある）。
   `ADR-canonical-heading-references.md:46` はテスト名への参照を積極的に肯定しているが、
   commit message は逆向きの判断を明示している。私が実測で示したのは
   「機械照合の付く選択肢が在り、それは着地する」までであって、**採否そのものではない**。
   一方 (b) の「同一ファイルに `種 3` が 2 件」は測定であり、こちらは確信がある。

⚠️ 5. **`npm run test:powershell` / `smoke:startup` / `smoke:egui` を「該当なし」と判定したが、
   実行はしていない。** 判定の裏取りは `git grep -n governance -- "*.ps1" "*.psm1"` の
   ヒット 2 件がいずれも規範への参照であることの確認まで。Pester の中身を全件読んで
   「ガバナンス検査器を呼ぶものが 1 件も無い」ことを確かめたわけではない（不在の観測 1 つで
   確定させない、という `AGENTS.md`「検証の作法（全タスク共通）」の全称否定の条項に照らすと、
   私の根拠は grep 1 経路だけである）。**時間を理由に省いたのではなく、
   該当なしと判定した結果として実行していない。**

⚠️ 6. **L-4（`visible_rows` の実測記録の喪失）を「閉じる」と書いたが、
   「その教訓を将来別の検出器が必要とするか」は測っていない。**
   「変換は母集団と読み手の両方へ当てないと非対称に漏れる」という一般則は、
   今日リポジトリのどこにも残っていない（`git grep 読み手` の実測）。G12 が無い今は
   消費者ゼロだが、次に同型の検出器を書く人がこれを再導出する必要は生じうる。
   plan.md の裁定（「保存義務は薄い」）に私は同意するが、確信は中程度である。

⚠️ 7. **H-1 の「9 件はすべて偽陽性である」は、6 識別子の所在（すべて `#[cfg(test)]` の内側）
   までしか測っていない。** そこから「文書側の引用が正当である」を導くのは意味判断であり、
   9 件を 1 件ずつ文書の文脈に当てて確かめたのは `check_only`（SPEC.md:1158 = auto_update の
   3 モードの 1 つ）だけである。残り 8 件は「テストヘルパ・テスト名を `docs/development-principles.md`
   と `docs/build-commands.md` が例示として引いている」形に見えるが、逐一の裁定はしていない。
   **「9 件動く」という測定そのものには確信があり、H-1 の成立はそれだけで足りる**——
   偽陽性か真の腐りかは、直し方（理由づけの書き換え vs 文書の修正）を分けるだけである。
