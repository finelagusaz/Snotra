# #1140 検証・レビュー結果（対象 sha: 3ee79c22e6670389276d60bf492f65ecd0e8a995）

## カテゴリ A〜F 判定表

| カテゴリ | 該当/非該当 | 根拠 |
|---|---|---|
| A. Rust (.rs) | 非該当 | git diff --name-only で .rs 変更 0 件を実測。rustdoc / intra-doc link に触れる変更なし |
| B. TypeScript (vitest.config.ts 等) | 非該当 | 触れていない |
| C. smoke-egui / build-commands 前提 | 非該当 | trace イベント名・hotkey 登録・表示経路の変更なし |
| D. 目視項目 | 非該当 | UI 変更なし |
| E. npm test（vitest: dependents.test.mjs / post-edit.test.mjs 他） | 該当・実施済み | `npm test` 実行 → 33 ファイル / 793 件 全 pass |
| F. npm run governance:check | 該当・実施済み | 実行 → 全検査 passed（検査19件・対象文書35件・見出し参照271件 md47+.rs101+スクリプトのコメント105 から照合 等） |
| cargo doc | 非該当と判定（実行せず） | 根拠: .rs 変更 0 件（git diff --name-only で実測）。hook は元々 .rs にしか発火しないため、この PR では発火対象自体が存在しない |

F は実際に実行して確認した。cargo doc は「実行しても対象が無い」ことを .rs 変更 0 件から判定し、実行はしていない。

## レビュー所見

### Critical
なし。

### High

1. **`main()` が `dependentsReminder` を呼ぶ配線そのもののテストカバレッジが無い（実測で検証）**
   - 場所: `.claude/hooks/post-edit.mjs:474-476`（呼び出し箇所）／ `.claude/hooks/post-edit.test.mjs`（テスト側の欠落）
   - 根本原因: `dependentsReminder` 関数自体は spy を使った単体テストで手厚くカバーされているが、`main()` がその戻り値を `warnings` に積んで `systemMessage` へ反映する配線を検証する統合テストが無い。post-edit.test.mjs には「統合: post-edit.mjs をプロセスとして起動する」という describe ブロックが実在し、config-warn 等は実際に `runHook()` 経由で `systemMessage` に出ることを end-to-end で確認しているのに、reminder（#1140）だけはこの経路のテストを持たない。
   - 変異注入で実証: 複製リポジトリで `main()` 内の `const reminder = dependentsReminder(rel, root); if (reminder) warnings.push(reminder);` の2行を削除し、`.claude/hooks/post-edit.test.mjs` を対象に `npm test` を実行したところ、**96件全て pass のまま**——検知されなかった（検証範囲はこのテストファイル1本。`dependents.test.mjs` は hook を import せず、`governance:check` も hook の配線を読まないため、他の検証経路も原理的にこの欠落を捕まえない）。同じ複製で (a) 純追記フィルタ除去、(b) 逆引き索引を1本の腕に縮小、の2種は狙い通り赤くなったのと対照的（下記「変異注入の結果」参照）。
   - 修正例: 「統合: post-edit.mjs をプロセスとして起動する」ブロックへ、依存を持つ既知の節（例: 複製 fixture 上の md ファイル）を実際に spawn 経由で編集し、`systemMessage` に WARN 行が現れることを確認するテストを1件追加する。

### Medium

1. **`isRefTargetSpelling` が計画の主張どおりには共有されていない（DRY 未達）**
   - 場所: `scripts/governance/lib.mjs:171`（新規定義）／ `scripts/governance/checks/G-heading-refs.mjs:47`／ `scripts/governance/checks/G-near-heading-refs.mjs:67`（未移行の重複）
   - 根本原因: 本タスクの「意図的に分けた構造」注記2は「下位部品（refScanLines / HEADING_REF / isRefTargetSpelling / resolveRefTarget）は共有済み」と述べるが、この前提が事実と食い違う。実際に `isRefTargetSpelling` を呼んでいるのは新規の `dependents.mjs` だけであり、既存の `G-heading-refs.mjs` と `G-near-heading-refs.mjs` は同じロジック `!target.endsWith(".md") && !/^\/[a-z0-9-]+$/.test(target)` をそれぞれ独立にインライン実装したまま残っている（この重複自体は本 PR 以前から存在し、本 PR はそこへ3本目のコピーを増やさず関数化したが、既存2本を移行しなかった）。今後この綴り規則を変えるとき、3箇所中2箇所が追随しない可能性がある。
   - 修正例: `G-heading-refs.mjs:47` と `G-near-heading-refs.mjs:67` のインライン条件を `!isRefTargetSpelling(target)` に置き換え、`isRefTargetSpelling` を import する。

### Low / ⚠️（確信の持てない所見）

1. ⚠️ **`collectAnchors` の書き換えは出力順序を変えるが、2つの既知消費者（G-heading-refs / G-near-heading-refs）はどちらも `.some()` で判定するため挙動には影響しない**ことを確認した（`scripts/governance/checks/G-heading-refs.mjs:59` / `G-near-heading-refs.mjs:74`）。ただし将来 `collectAnchors` の返り値に順序依存の消費者が追加された場合に備え、doc コメントへ「呼び出し側は順序に依存してはならない」旨を明記してもよいかもしれない（必須ではない）。
2. ⚠️ **「3 本の腕」という腕数の数値をコメントに書く箇所が新たに2つ増えた**（`dependents.mjs` の `buildDependentIndex` doc・`lib.mjs` の `allHeadingRefDocs` doc）。`docs/comment-guidelines.md`「第一原則」が禁じる「コードが決めている構造の事実（数）の写し」に近い形だが、(a) この言い回し自体は本 PR 以前から `governance-check.mjs` に存在した確立済みパターンの踏襲であること、(b) 「母集団は G-heading-refs と同じ」という関係の主張自体は `allHeadingRefDocs(snapshot)` を実際に共有呼び出ししている構造的事実（要石）であることから、強い指摘とはしない。ただし腕が4本目に増えた場合、この2箇所の「3本」という文字列は追随せず腐る。
3. ⚠️ **`.md` 訂正7箇所の完全性を、plan.md が明記する2本の grep をそれぞれ正しい ripgrep 構文で再実行して検算した**（`rg -g "*.md" -n "PostToolUse hook 検査は走らない|md 全般|何も走らなかった"` と `rg -n "何も走らな|沈黙は合格ではない"`）。生きた層（CLAUDE.md / docs/build-commands.md / docs/hooks.md / .claude/rules/governance-docs.md）に訂正漏れは見つからなかった。両方の grep がヒットした箇所はすべて (i) reminder への言及を伴って修正済み、(ii) ADR内の凍結記述、(iii) G-hook-fires.test.mjs の合成 fixture（実物のテーブル行とは別物）、(iv) docs/superpowers/ 配下の凍結された過去計画、のいずれかであり、問題なし。
4. コードスパン（バッククォート区切りの識別子）を行またぎさせる折返しは、新規コメント（dependents.mjs / lib.mjs / post-edit.mjs / ADR 本文）を目視走査した範囲では確認されなかった。

## 変異注入の結果（複製: scratchpad/mutant-1140-repo、node_modules は fs.symlinkSync junction）

| # | 変異内容 | 対象 | 検証範囲 | 結果 | 検知テスト数 | 判定 |
|---|---|---|---|---|---|---|
| a | `changedSectionDependents` の `h.oldLen > 0` フィルタを `true` に置換（純追記も報告対象にする） | dependents.mjs | dependents.test.mjs | 赤（1/10 failed） | ちょうど「純粋な追記では出ない」テスト1件のみが失敗、他9件は無関係のまま pass | 適正——過剰検知なし、狙った不変条件のみ壊れた |
| b | `allHeadingRefDocs(snapshot)` を `headingRefDocs(snapshot)`（.md腕のみ）に縮小 | dependents.mjs | dependents.test.mjs | 赤（2/10 failed） | 「3本の腕すべてから集める」テストと、.rs/スクリプトも依存元に含む複合テストの2件が失敗 | 適正——縮小した腕に依存する既存テストのみが反応 |
| c | `main()` 内の `dependentsReminder` 呼び出し2行を削除 | post-edit.mjs | post-edit.test.mjs | **緑のまま（96/96 pass）** | 0件 | **検知できず——上記 High 所見1と同一事象** |

いずれの変異も内容ハッシュ（sha256sum）で baseline との一致を確認したうえで `git checkout --` により複製側のみ元に戻した（本体の検証対象ツリーには一切触れていない）。

(a)(b) は「変異が本来の回帰の姿と同じか」を確認済み——(a) は plan.md が明記する「純追記は既存参照を偽にしない」不変条件そのものの直接的な違反であり、(b) は「腕が増えたときに報告だけが取り残される」という dependents.mjs 自身の doc コメントが名指す退行そのものである。(c) は「レビュー観点として最重要」と依頼文が明示した「post-edit.mjs に静的 import が入っていないこと」の**姉妹契約**（呼び出しそのものが生きているか）であり、静的 import の欠如を守るテスト（`静的 import を足していない`）は存在するが、呼び出し自体の生存を守るテストが存在しない非対称である。

## 実測値

- **hook 所要時間**（複製リポジトリ、CLAUDE.md への実編集）:
  - reminder が出る場合（フック節の本文を書き換え）: 386ms（1サンプル）
  - reminder が出ない場合（純追記 / 依存を持たない編集）: 330〜351ms（3回平均 ~341ms）
  - 差は誤差範囲内——`dependentsReminder` は .md 編集であれば reminder の有無によらず必ず subprocess を起動し索引を構築するため、コストは「鳴るか鳴らないか」ではなく「.md を編集したかどうか」で決まる（この構造はコードから読み取れる。サンプル数の非対称はこの結論を左右しない）
  - plan.md の見積もり（node起動109〜130ms + 索引129ms ≒ 250ms前後）より実測は100ms程度高い。開発機の違い・複製ディレクトリのディスクキャッシュ差の可能性があり、実装の欠陥とまでは言えない（⚠️ 未確定）

- **報告が出る節の実例**（複製リポジトリ、CLAUDE.md「フック（.claude/settings.json）」節の本文1文を書き換え）:
  ```
  WARN: CLAUDE.md の「フック（.claude/settings.json）」の本文を変更しました。この節に依存する参照が 7 件あります
  （.claude/rules/governance-docs.md:23 / .claude/skills/implement/SKILL.md:86 / .claude/skills/merge-pr/SKILL.md:24 ほか 4 件）。
  指し先が変わったなら参照側も読み直してください（全件: `node scripts/governance/dependents.mjs CLAUDE.md`）。
  ```

## 結論

Critical なし。High 1件（reminder 配線の統合テスト欠落——修正推奨だが reminder は gate ではないため exit code には影響しない）。Medium 1件（isRefTargetSpelling 未移行によるDRY未達）。設計判断（意図的分離3点）・SPEC同期（対象外）・doc正本化・7箇所の訂正・静的import回避はいずれも一次証拠で確認済み、問題なし。
