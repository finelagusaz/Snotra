================================================================================
#1143 ラウンド 2 — 検証・逆向きの監査・修正差分レビュー
================================================================================

アンカー: **8dd4a311c730e819018613f5e7a88ec6480a5380**（実測・作業終了時に再測定）
  `git log --oneline -2`:
    8dd4a311 fix(governance): 被覆検査の母集団の下界を縛り、死角の宣言を補う (#1143)
    7261a4e9 fix(rules): paths を部分木ごと覆い、被覆をファイル側から検める (#1143)

  再アンカー時の障害と処理: ラウンド 1 の未追跡 `workspace/verify-1143.txt` が
  「checkout で上書きされる」として checkout を中断させた（新アンカーが同パスを追跡するため）。
  scratchpad へ退避し（SHA-256 134680001AFE）、メインチェックアウト側の複製も同ハッシュで
  実在することを確認したうえで削除して checkout した。**成果を失う経路は通っていない**
  （`git checkout-index -f` の類は使っていない）。

  ラウンド 2 で変更されたファイルの作業ツリー内容は `git show HEAD:` と SHA-256 で 4/4 一致。

【出力先】呼び出し側指定の `C:\workspace\Snotra\workspace\verify-1143.txt` への Write は
  worktree 隔離により harness が拒否した（ラウンド 1 と同じ）。worktree 側へ書き、
  PowerShell の Copy-Item で指定先へ複製している。2 か所とも実在し内容は同一（複製後に照合）。

================================================================================
0. 逆向きの監査 — この修正差分が消した・弱めた不変条件は無いか
================================================================================

**結論: 無い。判定ロジックは 1 行も変わっておらず、非コメント行の変更は純増（削除ゼロ）である。**

散文（「テストと散文しか足していないつもり」）を逐語追認せず、**自分の道具で測った**。
稼働中の `linesOfComments`（`lib.mjs`）でコメント行を落としてから比較している。

  scripts/ 配下の変更ファイル 3 件について、コメント除去後の SHA-256:

    G-rules-script-coverage.mjs      4a247ed01e8119fd → 4a247ed01e8119fd  **同一**
    lib.mjs                          3b4e7919107e8424 → 3b4e7919107e8424  **同一**
    G-rules-script-coverage.test.mjs 3daf39f747309789 → f91950c3fdd4adcb  変化あり

  変化のある 1 件を**行の多重集合**で比較した（集合比較だと「同じ行が 2 回 → 1 回」を
  取りこぼすため、そこも塞いだ）:

    G-rules-script-coverage.mjs      減った 0 種 / 増えた 0 種
    lib.mjs                          減った 0 種 / 増えた 0 種
    G-rules-script-coverage.test.mjs 減った **0 種** / 増えた 9 種（すべて新テスト `it(...)` の中身）

  ⇒ 3 ファイル合計で**消えた非コメント行は 0 行**。既存の 7 テストも判定関数も無傷であり、
    「テストと散文だけ」という申告は**実測で裏が取れた**。

================================================================================
1. 検証（新アンカーで回し直し）
================================================================================

--- C: `npm test` ---------------------------------------------------- 実行
  exit code: **0**（`$LASTEXITCODE` を直接読む。パイプ越しの `$?` は使わない）
  Test Files 35 passed (35) / Tests **828 passed (828)** / 14.96s
  ラウンド 1 の 827 から **+1**——新設された「母集団の下界」テストの分と一致する。

--- F: `npm run governance:check` ------------------------------------ 実行
  exit code: **0** / 全検査 passed（検査 20 件 / 対象文書 35 件 / rules 8 件 / skills 12 件 …）
  検査数はラウンド 1 と同じ 20。新テストは検査を増やさない（`*.test.mjs` は registry の対象外）。

--- A / B / D / E ---------------------------------------------------- 該当なし
  ラウンド 2 の差分は `//!` コメント・ADR 散文・テスト 1 本・`workspace/` の 2 ファイルのみ。
  `.rs` なし（A）、`.ts` なし（B）、描画に無影響（D）、`.githooks/` 不変（E）。

================================================================================
2. ラウンド 1 の各指摘の解消状態（**自分の道具で測った。報告を逐語追認していない**）
================================================================================

--- [High-1] SCRIPT_EXT を狭める変異が全層で沈黙する ------------- **解消（実測）**

  自分で複製へ変異を当てて測った（稼働中のファイルは無傷）:
    scratchpad へ `scripts/governance` を複製 → `SCRIPT_EXT` を `/\.(mjs)$/` へ置換
    → `npx vitest run checks/G-rules-script-coverage.test.mjs --reporter=verbose`

  結果: **exit 1 / Tests 1 failed | 7 passed (8)**
    × 母集団の下界: .ps1 / .psm1 も対象である（SCRIPT_EXT を狭める変異を捕まえる）
      → AssertionError: **scripts/lib/SnotraSmoke.psm1 が母集団から落ちている**
    他 7 本は pass（＝変異を狙い撃ちしており、常に赤いゲートにはなっていない）

  呼び出し側の報告「1 failed / 7 passed」と**独立に一致**。カスタム assert メッセージが
  落ちたファイル名を名指すので、失敗時に原因が読める形になっている点も良い。

--- [Medium-2] 「COVERAGE に載らない rule は見ない」の宣言漏れ ---- **解消**
  `G-rules-script-coverage.mjs:15-16` に死角として追加され、
  「**表に足すのは手作業である**」まで書かれている。指摘した内容を過不足なく満たす。
  （ただし**行内の狭さ**は別問題として残る——下の ⚠️-4 決着を参照）

--- [Medium-1] 「切り詰めない」×「広がったら赤」の積 -------------- **解消**
  `:19` に「（**未被覆は切り詰めず全件名指すので、その赤は長い**）」が入り、
  2 つの宣言が同じ文の中で結び付いた。読者が積を自分で導く必要がなくなっている。

--- [Low-1] ADR の被覆件数 32 → 84 ------------------------------- **解消（かつ新主張も検算した）**
  件数が削除され、構造的な記述へ置き換わった:
  「差分は `scripts/governance/` 配下の全ファイルと `scripts/run-codex.sh` である（件数は増減するので書かない）」

  **これは新しい全称主張なので、受け取らずに検算した**（`AGENTS.md`「新設した規約は既存の全事例に
  当てて検算してから書く」）。実測:
    失うもの                       = 0 件（ADR の主張どおり）
    実際の差分（新 \ 旧）          = 54 件
    ADR が主張する集合             = 54 件（scripts/governance/ 配下 53 + run-codex.sh 1）
    主張に無いが実際には増えた     = **0 件**
    主張にあるが実際は増えていない = **0 件**
  ⇒ **集合として完全一致。全称主張は正しい。**
  （参考: `scripts/governance/` 配下 53 件のうち非 .mjs/.ps1/.psm1 は 0 件）

--- [Low-2] lib.mjs の「19 検査」 -------------------------------- **解消**
  「各検査」へ置換。両義性（その 2 関数の消費者か／全検査か）を数を捨てることで解いており、
  ラウンド 1 で私が提案した方向と同じ。

--- [Low-3/4/5] 見送られた 3 件 ---------------------------------- **異議なし**（下に理由）

================================================================================
3. 見送り 2 件への回答（異議の有無）
================================================================================

**どちらも異議なし。** ただし理由を実測で裏付けておく。

- **`ctx` 未使用**: 異議なし。`G-rules-globs.mjs:8-11` と逐語同型で、JSDoc が「この検査は使わない」と
  明示している。registry の契約（`run(snapshot, ctx)`）に合わせた統一であり、片方だけ引数を削ると
  「なぜこの検査だけ形が違うのか」を次の読者が導出する羽目になる。**残すほうが正しい。**

- **`SCRIPT_EXT` の大文字小文字**: 異議なし。加えて、**ラウンド 2 の修正でこの見送りは
  より安全になった**ことを確認した——大小を区別しない向きの変更（`/i` を足す）は母集団を
  **広げる**側なので新テストは緑のまま通るが、それは無害な向きである。危険な向き
  （狭める）だけが新テストに捕まる。露出 0 件（`.PS1` 等のファイルは 0 件・再実測）と合わせて、
  見送りの判断は妥当。

================================================================================
4. ラウンド 1 の残り ⚠️ 3 件 — 決着
================================================================================

--- [⚠️-2] `rulePathPatterns` が `paths:` 以外の引用リストも読む --- **決着: 真だが露出 0**

  実測: `.claude/rules/` の **8 本すべて**の frontmatter を走査した結果、
  **キーは `paths:` の 1 つだけ**（8/8 とも 2 行目が `paths:` で、他のキーは 1 つも無い）。
  ⇒ このリポジトリの rule 形式では、「frontmatter の引用リストを読む」と
    「`paths:` の引用リストを読む」は**観測上区別できない**。関数名と実装の乖離は
    字義どおり真だが、**偽にする入力が今日 1 つも作れない**。
  **結論: 指摘として起票しない。** 2 つ目の frontmatter キーを導入する変更が来たときに
  初めて意味を持つので、そのとき考えれば足りる（先回りは「やりすぎでは」に当たる）。

--- [⚠️-3] 「harness 配送は未検証」の三重宣言 -------------------- **決着: 現状維持でよい**

  実測: 宣言の在り処は `lib.mjs`（`globToRegex` の doc）・
  `G-rules-script-coverage.mjs`（`//!`）・`ADR-rules-paths-subtree-coverage.md`（帰結）の 3 か所で、
  **ラウンド 2 で 4 つ目は増えていない**。
  3 つは読者が違う（関数の呼び出し側 / 検査の保守者 / 決定の経緯を読む人）ので写しではない。
  **結論: 何もしなくてよい。** ラウンド 1 で私が「やりすぎかもしれない」と留保した側が正しかった。

--- [⚠️-4] `governance-docs` の `inPopulation` が手作業依存 ------ **決着: 真。ラウンド 2 では塞がっていない**

  実測（複製 snapshot へ注入・稼働中のファイルは無傷）:
    現行                                                  → findings 0 件
    [仮定 A] governance-docs の paths が `.claude/hooks/**` を得た
             （＝射程が広がったのに `inPopulation` は `scripts/` のまま）  → **0 件（沈黙）**
    [仮定 B] 射程を広げたつもりで paths に `.claude/hooks/**` が無い
             （＝配送されない）                                            → **0 件（沈黙）**
    対照: safety-nets から `.claude/hooks/**` を落とすと                   → **6 件の赤**

  ＝ **同じ欠落を safety-nets は見て governance-docs は見ない**。非対称そのものは意図であり
  `COVERAGE` の doc が根拠を持つ（ラウンド 1 で渡された意図的分離 (3)）。問題は、
  ラウンド 2 で足された死角宣言が「**どの rule を見るか**」（表の行）を扱っており、
  「**行の中で母集団がどこまで及ぶか**」を扱っていない点である。
  **結論: Low として残す（下の Low-R2-2）。** 発火条件は仮定的（governance-docs の射程が
  実際に広がったとき）なので、今すぐ直す必要は無い。

================================================================================
5. 修正差分そのもののレビュー（7261a4e9..8dd4a311 が対象）
================================================================================

-------------------------------------------------------------------- Critical
なし。

------------------------------------------------------------------------ High

[High-R2-1] 新設した `//!` の指示が、**それが名指す 2 つの狭め方のうち片方にしか効かない**
  根拠: scripts/governance/checks/G-rules-script-coverage.mjs:19-24

  当該箇所はこう読める:
    「そのときは **`WALK_EXCLUDE_PATHS` へ 1 行足すか下の `inPopulation` を絞る**」（:20-21）
    「**ただし母集団を狭めるときは、下界を縛るテストを同じ変更で直すこと**」（:22）
    「それを捕まえているのは …「母集団の下界」**1 本だけである**」（:24）

  「母集団を狭める」は直前の文が挙げた **2 つの手段**を指す。ところが「母集団の下界」テストは
  **fixture snapshot（`snap(...)`）で組まれており `makeSnapshot` を通らない**ため、
  `WALK_EXCLUDE_PATHS` 側の狭め方には**原理的に反応しない**。

  実測（複製の `lib.mjs` へ `"scripts/governance"` を 1 行足しただけ・稼働中は無傷）:
    snapshot.files          474 → 421（−53）
    SCRIPT_EXT 母集団        92 → 39（`scripts/governance/` 配下 53 → **0**）
    現行 rules での findings  0 →  0（緑のまま）
    **#1093 の再発形（旧 paths）を仕込んだときの検出**: **106 件 → 0 件**
      うち `scripts/governance/` を名指す finding: **0 件**

  そして 3 層すべてが黙る:
    - 新テスト「母集団の下界」 … fixture 由来ゆえ**緑のまま**
    - `npm run governance:check` … 0 件 → 0 件で**緑のまま**
    - `governance manifest delta` … **0 件**（実測。4 列 checks/docs/rules/skills のどれも
      `scripts/` を含まない。`checks` 列は `registry.mjs` の `readdirSync` 由来で
      snapshot を経由しないため、走査の除外に影響されない: live 20 / narrowed 20）

  なぜ High か: **この検査の存在理由そのもの（#1093 の再発検知）が 106 → 0 で完全に失われるのに、
  どの層も赤くならない。** しかも `//!` の新しい一文は、その状況で保守者へ
  「下界のテストを直せ」と指示する——**指示に従っても何も起きない**（テストは元から緑）。
  ラウンド 1 の High-1 と同型の穴が、High-1 を塞ぐ差分の隣に残っている。
  タスクが言う「修正は指摘箇所へ注意が集中し、周辺に新しい誤りを生む」の実例である。

  ⚠️ 弱める材料も書いておく（裁定は呼び出し側へ）:
    - `WALK_EXCLUDE_PATHS` は `lib.mjs` の共有物で、そこへ `scripts/governance` を足す動機は
      現実には薄い（doc が想定するのは `ui/node_modules`）。`ui/node_modules` を足す形なら
      `scripts/` の母集団は減らないので、この穴は踏まれない。
    - つまり**実際に踏まれる確率は低い**。High としたのは確率ではなく
      「踏んだときに検査が丸ごと無力化し、かつ全層が沈黙する」帰結の大きさによる。
      Medium への格下げは合理的に議論できる。

  塞ぎ方の候補（実装は指示があってから・どれか 1 つで足りる）:
    (a) `//!` の一文を正確にする——「`inPopulation` を絞る場合は下界テストが捕まえる。
        **`WALK_EXCLUDE_PATHS` 側は捕まえない**」と非対称を明示する（最小・散文だけ）
    (b) `makeSnapshot(ROOT)` を使う実ツリーのテストを 1 本足し、
        `scripts/governance/` 配下の `.mjs` が母集団に**存在すること**を固定する
        （`G-module-linkage.test.mjs:152` 等に実ツリー snapshot の先例がある）
    (c) manifest へ 5 列目（`scripts/` 配下の SCRIPT_EXT ファイル）を足す——ただし
        `governance-manifest.mjs` 冒頭の「構造母集団だけ・散文は入れない」設計判断に触るので重い

---------------------------------------------------------------------- Medium
なし（ラウンド 1 の Medium 2 件はどちらも解消済み）。

------------------------------------------------------------------------- Low

[Low-R2-1] 件数を消して置いた全称主張が、**別の形で腐りうる**
  根拠: docs/adr/ADR-rules-paths-subtree-coverage.md:24-26
  「差分は `scripts/governance/` 配下の全ファイルと `scripts/run-codex.sh` である」は
  **今日は集合として完全に正しい**（上で 0 件 / 0 件を実測）。
  ただし `scripts/` の下に**新しいトップレベルのディレクトリ**（例 `scripts/tools/x.mjs`）が
  1 つできた瞬間に偽になる——差分にはそれも入るが、主張は `scripts/governance/` しか挙げていない。
  `MEMORY.md` の [universal-claim-fix-regenerates-itself]（「偽の全称を直した文がまた全称で偽になる」）
  がまさにこの形で、そこでの止め方は「数え上げをやめ『〜だけではない』の**下限主張**へ倒す」である。
  ＝ 件数 → 構造的全称 は改善だが、腐りの速度を落としただけで種類は同じ。
  **ADR は凍結層なので書き換えは求めない。** 記録として残す。

[Low-R2-2] 死角宣言が「どの rule を見るか」は覆い、「行の中の母集団の狭さ」を覆っていない
  根拠: scripts/governance/checks/G-rules-script-coverage.mjs:15-16（新設の死角）と :33（`inPopulation`）
  上の ⚠️-4 決着の実測（仮定 A / B ともに 0 件、safety-nets は同じ欠落で 6 件）が根拠。
  新しい死角宣言は「`COVERAGE` に**載っていない** rule は見ない」であり、
  「載っている rule でも `governance-docs` の母集団は `scripts/` に閉じる」は別の事実である。
  後者は `COVERAGE` の doc（:28-30）が射程の**理由**は書いているが、
  **それが死角でもある**ことは死角一覧の側に現れていない。1 行で足りる。

[Low-R2-3] ラウンド 1 のレビュー成果物が `workspace/` へコミットされた
  根拠: `git diff --stat 7261a4e9..8dd4a311` に `workspace/verify-1143.txt | 463 +++++`
  `workspace/` は `makeSnapshot` の `WALK_EXCLUDE_PATHS` が落とす作業バッファで、
  `headingRefDocs` も除外する——**どの governance 検査もこのファイルを一度も見ない**。
  squash マージで main の履歴に入るが、`/implement` が消す前提の場所でもある。
  意図的なら問題ないが、**撤去条件がファイル自身に書かれていない**
  （`AGENTS.md`「調査・測定のための一時的な足場」の規約）。
  なお**良い側の事実**: コミットされたのは**撤回済みの版**であり（`grep -c 撤回` = 3、
  382 行目に撤回見出し）、ラウンド 1 の誤った ⚠️-1 が真として記録に残る形は回避されている。

------------------------------------------ ⚠️（確信の持てない所見・切り捨てない）

[⚠️-R2-1] High-R2-1 の格付けに確信が無い。
  「検出力 106 → 0・全 3 層が沈黙」という帰結の大きさは実測で確実だが、
  **その変異が現実に起きる確率**は測っていない（測れない）。`//!` が想定する将来
  （`ui/node_modules`）を素直に実装するなら `scripts/` は減らないので踏まない。
  確率で見れば Low、帰結で見れば High。**High に置いたのは私の判断であって測定ではない。**

[⚠️-R2-2] 新テストの fixture が `scripts/lib/SnotraSmoke.psm1` と `scripts/smoke-egui.ps1` という
  **実在するパスを逐語で書いている**（G-rules-script-coverage.test.mjs:48）。
  fixture snapshot なので実在は不要であり、この 2 本が改名・削除されてもテストは緑のまま通る
  （＝壊れない）。害は無い。
  ⚠️ ただし逆に、読者が「実ツリーを見ている」と誤読しうる形でもある——実際 High-R2-1 は
  まさにその誤読が `//!` の一文として結晶したものだと私は見ている。
  fixture に架空名（`scripts/x.psm1` 等）を使えば誤読は消えるが、
  「実在する代表例を挙げる」ことの読みやすさは失う。**どちらが良いか決めきれない。**

[⚠️-R2-3] ラウンド 1 で私が犯した confabulation（⚠️-1）と同型の誤りが、
  **本ラウンドの報告に無いこと**を私は保証できない。
  今回とった予防策は「散文を追認せず自分の道具で測る」「測定結果を逐語で貼る」の 2 つで、
  実際 High-R2-1 と ADR の全称検算はその手順から出ている。
  ただしラウンド 1 の誤りも「周辺の事実がすべて真」だったために自己レビューを素通りした。
  **本レポートの主張のうち、実測ログを併記していない箇所（格付けの理由づけ・
  「なぜ High か」の因果の説明）は、同じ弱さを持つ。**

================================================================================
件数まとめ
================================================================================
アンカー: 8dd4a311c730e819018613f5e7a88ec6480a5380（実測）
逆向きの監査: 消えた非コメント行 **0 行 / 3 ファイル**。判定ロジックの変更なし（純増のみ）
検証: 実行 2（npm test exit 0 / **828 passed**、governance:check exit 0 / 20 検査）
      該当なし 4（A / B / D / E）
ラウンド 1 指摘の解消: High 1/1・Medium 2/2・Low 2/2 が**解消（すべて自分で再測）**
      見送り 2 件は**異議なし**（理由を実測で裏付け）
ラウンド 1 の ⚠️ 3 件: **3 件とも決着**（⚠️-2 露出 0 で起票せず / ⚠️-3 現状維持 /
      ⚠️-4 真・未解消 → Low-R2-2 へ）
ラウンド 2 の新規所見: Critical 0 / **High 1** / Medium 0 / Low 3 / ⚠️ 3
      High-R2-1 = `//!` の新しい指示が `WALK_EXCLUDE_PATHS` 側の狭め方に効かず、
      その狭め方は検出力 106 → 0 で全 3 層（新テスト・governance:check・manifest delta）が沈黙する
