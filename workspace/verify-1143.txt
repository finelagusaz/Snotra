================================================================================
#1143 ラウンド 4 — Medium 2 件の修正差分の確認（範囲限定）
================================================================================

アンカー: **81b81b175f416eb5f2ba5ade6ab4a61a209fdb32**（実測・作業終了時に再測定）
  81b81b17 fix(governance): canary の射程を scripts/ 部分木へ絞り、中間層の消滅を捕まえる (#1143)
  対象差分: 528e6c6e..81b81b17

Q4（必須の 2 コマンド・新アンカーで各 1 回）:
  `npm test`                 exit **0** / Test Files 35 passed (35) / Tests **829 passed (829)**
  `npm run governance:check` exit **0** / 全検査 passed（検査 20 件）

作業ツリー: `git status --porcelain` = 空（本レポート書き出し前）。
  変更対象 `G-rules-script-coverage.test.mjs` は `git show HEAD:` と SHA-256 一致。
  **稼働中のファイルは一度も変更していない**——変異はすべて scratchpad のミラー上。

【出力先】前 3 巡と同じく worktree 隔離のため指定先への直接 Write は harness が拒否する。
  worktree 側へ書き Copy-Item で `C:\workspace\Snotra\workspace\verify-1143.txt` へ複製。

================================================================================
0. 逆向きの監査（528e6c6e..81b81b17）
================================================================================

  scripts/ 配下の変更は **1 ファイルのみ**（`G-rules-script-coverage.test.mjs`）。

  **検査本体 `G-rules-script-coverage.mjs`: 全文一致（1 バイトも変わっていない）**
  **共有基盤 `lib.mjs`: 全文一致**
  ⇒ 判定ロジックは今回まったく触られていない。差分は canary の本体のみ。

  コメント除去後の行の多重集合（減った 4 種 / 増えた 8 種）——消えた 4 行はすべて
  旧 canary の 4 脚・前方一致の実装で、同じ役割の新実装に置き換わっている:
    - for (const prefix of ["scripts/governance/checks/", "scripts/lib/", ".claude/hooks/", ".githooks/"]) {
    - expect(pop.some((f) => f.startsWith(prefix)), ...)
    - files.filter(governanceDocs.inPopulation).some((f) => f.startsWith("scripts/governance/checks/")),
    - "governance-docs 側の母集団から scripts/governance/checks/ が消えている",
    + const dirOf = (f) => f.slice(0, f.lastIndexOf("/"));
    + for (const dir of ["scripts/governance/checks", "scripts/governance", "scripts/lib"]) {
    + pop.some((f) => dirOf(f) === dir),  ほか
  ⇒ **テストは 1 本も削られていない**（9 本のまま）。既存 8 本は無傷。

--- 測定手法 ---
  新 canary は位置依存（`../../../`）ゆえ単純な複製では測れない（ラウンド 3 と同じ）。
  `scripts/governance` `scripts/lib` `.claude/hooks` `.githooks` を同じ相対位置へ複製した
  **ミラー**を組み、baseline が実ツリーと同じ **9 passed** になることを確認してから変異を当てた。
  各変異は当てた直後に原本へ戻し、内容一致を確認している。

================================================================================
Q1. Medium-1 は解消したか — **解消（実測）**
================================================================================

  変異: `.githooks/githooks.test.mjs` を削除（＝別ディレクトリへの移設を模す）
  結果: **exit 0 / Tests 9 passed (9)**

  ラウンド 3 では同じ変異が `1 failed | 8 passed`（AssertionError: `.githooks/ が母集団から
  消えている`）だった。**もはや赤くならない。**

  併せて、脚を外したことで検出力が落ちていないことを確認した（後述 Q2 の回帰 2 件）。
  ラウンド 3 で私が実測した「`.claude/hooks/` と `.githooks/` の 2 脚は変異 A・B の
  どちらの検出にも寄与していない」が、除去後も検出力が変わらないことで**裏づけられた**。

================================================================================
Q2. Medium-2 は解消したか — **解消（実測）**
================================================================================

  **罠の回避**: ご指摘のとおり、`scripts/governance/` 直下の層を「消す」形はミラーでは作れない
  ——`lib.mjs` / `test-helpers.mjs` がその層に居り、テスト自身が import するためである
  （ファイルを消せば import が解決できず、変異の検知と区別が付かない）。
  また `WALK_EXCLUDE_PATHS` はディレクトリ単位の完全一致ゆえ、`scripts/governance` を足すと
  **配下の `checks/` ごと**消えてしまい、中間層だけを孤立させられない。

  そこで**母集団の述語側**へ変異を当てた——`inPopulation` から
  `/^scripts\/governance\/[^/]+\.mjs$/`（＝中間層 13 件）だけを除く。
  ファイルは 1 つも消えないので import は解決でき、`checks/` と `scripts/lib/` は無傷のまま、
  **中間層だけが母集団から落ちる**状態を作れる。これはラウンド 3 の変異 D と同一である。

  結果: **exit 1 / Tests 1 failed | 8 passed (9)**
    × canary: 実ツリーの母集団が走査側で縮んでいない
      AssertionError: **scripts/governance/ 直下が母集団から消えている**

  ラウンド 3 では**まったく同じ変異が exit 0 / 9 passed（完全な沈黙）**だった。
  失敗メッセージが落ちた層を名指ししており、診断も正しい方向を指す。

--- 回帰（既存の検出力が保たれているか） ---
  変異 A（`WALK_EXCLUDE_PATHS` へ `scripts/governance` を追加）
    → exit 1 / **1 failed | 8 passed**
      AssertionError: `scripts/governance/checks/ 直下が母集団から消えている`
  変異 B（`SCRIPT_EXT` を `/\.(mjs)$/` へ狭窄）
    → exit 1 / **2 failed | 7 passed**
      × 母集団の下界: .ps1 / .psm1 も対象である
      × canary: 実ツリーの母集団が走査側で縮んでいない
  ⇒ 前 2 巡で確立した検出力は**そのまま保たれている**。

================================================================================
Q3. この修正差分が新しく開けた穴は無いか — **無い（実測で決着）**
================================================================================

これはラウンド 3 で私が「未測定」と断った点である（⚠️-R3-1）。**今回測って決着させた。**

--- 問い: 外した 2 脚は、今日**この検査だけ**が縛っていたものか ---

  実ファイルには触れず、`snapshot.files` を絞った複製を **全 20 検査へ通した**
  （`buildChecks` で検査ごとに findings を集計）。対照（無変異）は 0 検査発火。

  シナリオ 1: `.claude/hooks/` が走査から消える（6 件除去）
    発火した検査 **3 件**:
      G-references        (9 件)  バッククォート参照のパスが実在しない: .claude/hooks/pre-bash.mjs
      **G-rules-globs**   (1 件)  paths glob が実在ファイルに 1 件もマッチしない: .claude/hooks/**
      G-stale-identifiers (22 件) 散文に、現行語彙に無い識別子が残っている: `judgeCommandShape` ほか

  シナリオ 2: `.githooks/` が走査から消える（6 件除去）
    発火した検査 **3 件**:
      G-hook-fires        (1 件)  代表パスが実在しない（死んだ行は判定を通り続ける）: .githooks/pre-commit
      G-references        (1 件)  バッククォート参照のパスが実在しない: .githooks/githooks.test.mjs
      **G-rules-globs**   (1 件)  paths glob が実在ファイルに 1 件もマッチしない: .githooks/**

  シナリオ 3: `.claude/hooks/` の `.mjs` だけ消える（6 件除去）  → 同じ 3 検査が発火
  シナリオ 4: `.githooks/` の `.mjs` だけ消える（1 件除去）
    発火した検査 **1 件**: G-references (1 件)

  ⇒ **4 シナリオすべてで、外した 2 脚が無くても別の検査が赤にする。**
    外した脚は**唯一の見張りではなかった**。ラウンド 3 の私の懸念は否定された。

--- とくに注目すべき事実 ---

  シナリオ 1〜3 では **`G-rules-globs` が発火する**。これは本 issue の対になる検査であり、
  「`.claude/hooks/**` という glob が実在ファイルに 1 件もマッチしない」を見る**順方向**である。
  ＝ この 2 検査は、`.claude/hooks/` / `.githooks/` については**互いを覆っている**。
  被覆形（逆方向）の脚を外しても、順方向が同じ消滅を捕まえる。
  脚を外した判断は、**この対称性の上で正しい**。

  シナリオ 4（＝ Q1 の「githooks.test.mjs を移設」と同じ形）だけは G-references 1 件のみで、
  それは「どこかの文書がそのパスをバッククォートで引いている」ことに依存する間接的な守りである。
  ただし**発火はする**ので死角ではない。しかも失敗の意味が正しい——
  「文書の参照が腐った」であり、直す先は文書である。旧 canary の
  「`WALK_EXCLUDE_PATHS` の狭窄」という**誤誘導するメッセージ**より明確に良い。

  なお manifest delta は 4 シナリオとも 0 件（沈黙）。これは既知の性質
  （4 列 checks/docs/rules/skills に `.claude/hooks/` も `.githooks/` も入らない）で、
  本差分が作ったものではない。

================================================================================
所見の一覧
================================================================================

-------------------------------------------------------------------- Critical
**0 件。**

------------------------------------------------------------------------ High
**0 件。**

---------------------------------------------------------------------- Medium
**0 件。** ラウンド 3 の Medium-1・Medium-2 はいずれも実測で解消を確認した
（Q1: 9 passed で偽陽性が消えた / Q2: 同一変異が沈黙 → 1 failed へ転じた）。
新たに開いた穴は Q3 の 4 シナリオ実測で**見つからなかった**。

------------------------------------------------------------------------- Low
**0 件。**

------------------------------------------ ⚠️（確信の持てない所見・切り捨てない）

[⚠️-R4-1] `startsWith` → `dirOf(f) === dir` の厳格化は、**`scripts/lib/` を階層化する
  再編で赤くなる**。実測: ミラーで `scripts/lib/*.ps*1` を `scripts/lib/smoke/` へ移すと
  exit 1 / 1 failed（`scripts/lib/ 直下が母集団から消えている`）。
  ⚠️ ただしこれを**欠陥とは考えていない**——(1) 厳格化そのものが Medium-2 を塞いだ当の変更であり、
  緩めれば中間層の沈黙が戻る、(2) メッセージが正確で、直す先（脚の一覧）が自明、
  (3) 対象がこの検査自身の射程内（`scripts/` 部分木）なので、Medium-1 で問題にした
  「他サブシステムを縛る」形ではない。**指摘には挙げず、受容される費用として記録する。**
  確信が持てないのは「記録に留める」か「テストのコメントに 1 行書くべきか」の判断のほうである。

[⚠️-R4-2] Q3 は「**今日の木で**別の検査が発火するか」を測った。
  G-references と G-stale-identifiers の発火は、**文書がそのパス／識別子を引いていること**に
  依存している（シナリオ 4 はとくにこれ 1 本）。将来その文書の記述が消えれば、
  発火する検査は減りうる。**「脚を外しても守りは残る」は今日の測定であって恒久の保証ではない。**
  G-rules-globs による守り（シナリオ 1〜3）は glob そのものを見るので、こちらは構造的で強い。

[⚠️-R4-3] 本ラウンドは指示どおり**この差分の 2 点にのみ**枠を当てた。
  前 3 巡が見た面（検証カテゴリ・ADR・散文の正確さ・`COVERAGE` の export・
  ⚠️-4 の機構案）は今回まったく見ていない。**「Low 0 件」「Medium 0 件」は
  その面を見て 0 だったという意味ではない。** とくに ⚠️-4（`governance-docs` の
  `inPopulation` が手作業依存／ラウンド 3 で「機構案が約 10 行で動く」と実測した件）は
  **今回の差分では触れられておらず、未決のまま残っている**——収束の判断材料として明示しておく。

================================================================================
まとめ
================================================================================
アンカー: 81b81b175f416eb5f2ba5ade6ab4a61a209fdb32（実測）
Q1 Medium-1: **解消**（githooks.test.mjs 削除 → exit 0 / 9 passed。前巡は 1 failed）
Q2 Medium-2: **解消**（中間層を母集団から外す変異 → exit 1 / 1 failed で層を名指し。前巡は 9 passed）
             回帰も維持（変異 A: 1 failed / 変異 B: 2 failed）
Q3 新しい穴: **無い**。外した 2 脚は唯一の見張りではなく、4 シナリオすべてで別検査が発火
             （とくに対の `G-rules-globs` が 3/4 で発火し、順方向が逆方向を覆っている）
Q4 検証: npm test exit **0**（829 passed）/ governance:check exit **0**（20 検査）
逆向きの監査: 検査本体と `lib.mjs` は**全文一致**。テストは 1 本も削られていない
所見: **Critical 0 / High 0 / Medium 0 / Low 0** / ⚠️ 3（いずれも指摘ではなく、
      受容される費用・測定の射程・未決事項の明示）
