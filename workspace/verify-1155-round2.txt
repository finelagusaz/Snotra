================================================================================
issue #1155 fix-forward の検証とレビュー（第 2 巡） — 対象 sha 0c949ff4
検証者: 隔離 worktree agent-a3d6dba6825a6087d
実施日: 2026-08-20

【出力先】worktree 隔離のため共有パスへは書けない。本ファイルの所在は
  C:\workspace\Snotra\.claude\worktrees\agent-a3d6dba6825a6087d\workspace\verify-1155-round2.txt
第 1 巡の verify-1155.txt は 0c949ff4 に追跡ファイルとして取り込まれており（blob
1aa77adb… が私の未追跡ファイルと一致することを確認）、回収済みであることを実測で確かめた。
================================================================================

--------------------------------------------------------------------------------
0. アンカーの確認
--------------------------------------------------------------------------------
切り替え前の HEAD: 702fa046（第 1 巡の対象）
`git checkout --detach 0c949ff4` は最初 **abort した** — 未追跡の workspace/verify-1155.txt が
0c949ff4 の追跡ファイルと衝突したため。**消す前に同一性を確かめた**（`git-rm-refusal-is-
protection-not-obstacle` の教訓: checkout 系で未コミットの成果を失う形を避ける）:
    git show 0c949ff4:workspace/verify-1155.txt | git hash-object --stdin → 1aa77adb8cae…
    git hash-object workspace/verify-1155.txt                            → 1aa77adb8cae…
    → **バイト単位で同一**。回収済みと確認できたので削除して切り替えた。

確認後の HEAD: 0c949ff45297add46da12684eefa6f38ce66f202  ✅
作業ツリーは検証終了時点で clean（`git status --porcelain` 無出力。本レポートは検証完了後に書いた）。

--------------------------------------------------------------------------------
1. 「散文のみ・コードの変更はゼロ」の検算（自分で測った）
--------------------------------------------------------------------------------
`git diff --stat 702fa046..0c949ff4`:
 .claude/rules/governance-docs.md             |   2 +-
 docs/adr/ADR-canonical-heading-references.md |   2 +-
 scripts/governance/lib.mjs                   |  14 +-      ← 12 挿入 / 2 削除
 workspace/plan.md                            |  16 +-
 workspace/verify-1155.txt                    | 470 +++++    ← 第 1 巡の成果物の取り込み

`lib.mjs` に 12/2 の変更があるので、申告を鵜呑みにせず**機械的に検算した**:
    ブロックコメントと行コメントを除去した実行コードを 702fa046 と突き合わせ
    → **同一: true**（長さ 12057 / 12057 バイトで一致）
差分は JSDoc ブロックの内側に閉じており（+/- 行がすべて ` * ` 始まり）、**実行コードの変更はゼロ**。✅
この結果により、第 1 巡で測った機構の挙動（変異 A・B の結果）はそのまま有効である。

--------------------------------------------------------------------------------
2. カテゴリ A〜F の再実行
--------------------------------------------------------------------------------
差分は `.mjs`（コメントのみ）と `.md` のみ。`.rs` / `.ts` / `.githooks/` はゼロ。
→ A（Rust）・B（TS）・C（表示経路）・D（UI 目視）・E（githooks）は**該当なし**。

  $ npm run governance:check
  governance:check — 全検査 passed（検査 21 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 /
  恒久規範 常時ロード 16948 字・**rules 12981 字** / 見出し参照 324 件を md 48 件 + .rs 101 件 +
  スクリプトのコメント 111 件から照合 / workspace member 4 件 / clippy 禁止 8 件 /
  散文の識別子 396 件を 34 文書から照合 / 近傍の見出し参照 19 件 / 折れうる位置 24 件 /
  ADR 69 本の名前 / ADR の短縮引用 363 件）
  **exit code: 0** ✅

  $ npm test
  Test Files 36 passed (36) / Tests **852 passed (852)** / Duration 12.24s
  **exit code: 0** ✅

計器の差分は 1 か所だけ: **rules の面積が 12995 → 12981 字**（-14）。M-1 の修正で
`.claude/rules/governance-docs.md` から綴りの列挙が消えた分と整合する。
他の計器（324 / 19 / 24 / 396 / 21 / 36 / 8 / 12 / 16948 / 4 / 8 / 69 / 363）は**全件不変**——
実行コードが変わっていないことの独立な裏付けになっている。

node_modules の junction は checkout をまたいで残っていた（未追跡ゆえ）。
vitest は正常起動しており、後述の exit 1 はモジュール解決の失敗ではない。

--------------------------------------------------------------------------------
3. H-1 の修正が成立しているかを、自分の道具で測る（依頼の中心）
--------------------------------------------------------------------------------
呼び出し側の指摘は正しい: 「`plans/` を対象にした正準形は 0 件」**も全称否定**であり、
grep のパターン依存を引き継ぐ。**そこで grep を使わずに測った**——
`AGENTS.md`「検証の作法」の「列挙も SSOT のツール自身に問う」に従い、**機構の解決器そのもの**を
走らせた（一時 script を書き、測定後に削除）。

  測り方: `allHeadingRefDocs(snapshot)` の全走査元を `refScanLines` + `HEADING_REF` で舐め、
          `isRefTargetSpelling` を通った参照ごとに `resolveRefTarget` を呼んで**解決先のパス**を得る。
          パターンマッチではなく、機構が実際に着地先として選ぶファイルを列挙している。
          → 綴りの形（`§` の有無・相対/絶対・ベア名の suffix 一致）を**すべて自動的に含む**。

  結果:
    走査元の文書数: 261   綴りゲートを通った参照: 324（`governance:check` の計器と一致）

  [A] docs/superpowers/ 配下へ解決される参照: **1 件**
        docs/architecture.md:229 -> docs/superpowers/specs/2026-08-10-search-worker-design.md
        「4.5 in-flight の失効 — 同期で行を差し替える経路はすべて `pending_seq` を進める」
      → **`plans/` 配下は 0 件**。呼び出し側の実測は**私の独立な方法でも再現した**。✅

  [A2] suffix 一致の経路が生きていることの実証（呼び出し側が名指しした懸念）:
        resolveRefTarget(snap, "AGENTS.md", "2026-05-14-deps-update-skill.md")
          -> docs/superpowers/plans/2026-05-14-deps-update-skill.md
      → **ベア名を書けば `plans/` 配下へ解決される機構は現に生きている。**
        今日それを行使している参照が 0 件なだけである。
        ゆえに doc が「受容する残余」として宣言した形は**正しく残余であり**、
        「今日は着地候補にならない」という限定つきの書き方も正確である。✅

  [B] 「新しい腕が無ければ着地しなくなる参照」を純計算で列挙: **2 件**
        scripts/governance/checks/G-rules-script-coverage.mjs:25 -> …test.mjs 「母集団の下界」
        scripts/governance-check.test.mjs:118 -> …evidence.test.mjs 「配線:」
      → **`.md` の参照は 1 件も腕に依存していない。** 変異注入を使わずに同じ結論へ到達した
        （後述の変異 A の実行結果とも一致）。

  [C] `.mjs` を対象にした参照（走査元にある全件）: **2 件**（上と同一）

  **第 2 の消費者も確かめた。** `collectAnchors` は解決先にしか呼ばれないが、`ANCHOR_SPECS` は
  `dependents.mjs` の `sectionsOf` も直接読み、そちらは `post-edit.mjs` の `dependentsReminder` が
  **拡張子 `.md` でありさえすれば発火する**（`if (!rel.endsWith(".md")) return ""` のみ・
  ディレクトリ除外なし）。つまり `plans/*.md` を編集すれば `sectionsOf` は走る。
  しかし `changedSectionDependents` は**依存を持つ節しか報告しない**ので、`plans/` を指す参照が
  0 件である以上、逆引き索引に該当エントリが無く出力は空になる。
  → **doc が挙げた 1 つの事実（当該ディレクトリを対象にした正準形が無い）が、2 つの消費者を
    どちらも無害にしている。** 機序の説明として過不足ない。✅

  判定: **H-1 の修正は成立している。** 数え上げ（「1 件も無い」）が消え、存在主張
  （「`plans/` の擬似コードには実在する」）＋限定つきの無害性（「今日それが着地候補になることは無い」）
  ＋機序＋受容する残余、という形になっており、私の独立な測定がすべて裏付けた。

--------------------------------------------------------------------------------
4. 新しく書いた散文の全称表現・数え上げの点検（依頼 3）
--------------------------------------------------------------------------------
■ 新規の指摘

[R2-1] Medium — **同じ偽の主張が `workspace/plan.md` に 2 か所そのまま残っている**
       （#977 の「取りこぼすのは写しを直す当のコミットが典型」の再現）

  workspace/plan.md:114（Phase 4 の実行記録・**未修正**）:
    - [x] `ANCHOR_SPECS` に … 腕を足した。**波及は `.mjs` に閉じる**——`.md` と `.rs` に
      この形の行は **0 件（実測）**。行頭アンカーなので …
  workspace/plan.md:145（Phase 4 の実行記録・**未修正**）:
    - [x] `dependents.mjs` への波及を確認 — 新しい腕が当たる行は `.md` / `.rs` に
      **0 件（実測）**ゆえ節境界の計算は不変

  一方、**同じファイルの** Phase 5（:149 以降）は
    「`.md` と `.rs` にこの形の行は 1 件も無い」は**偽**だった。… `.md` は
     `docs/superpowers/plans/` に実在する。
  と書いている。**ファイルが自分自身と矛盾している。**

  検出方法: `lib.mjs` から消した文言で全文を走査した（`この形の行|新しい腕が当たる行`）。
  ヒットは 3 か所で、うち `lib.mjs:418` は修正済みの新しい文、残り 2 か所が plan.md。
  （`workspace/verify-1155.txt:99` のヒットは私の第 1 巡レポートが**欠陥を引用している**もので、
    これは正しく残るべきもの。）

  重み: `workspace/` は作業バッファで機構の走査対象外、しかも :145 の**結論**
  （「節境界の計算は不変」）は上記 3 節で私が独立に確かめたとおり**結果としては正しい**
  （根拠が偽で結論が真）。ゆえに Critical/High ではない。
  それでも報告するのは 2 つの理由による:
    (1) plan.md は PR の証跡であり、squash で main の commit message に入りうる
        （`pr-body-is-outside-the-grep-population` の逆向き——**リポジトリの grep には入るが
          修正の母集団から落ちた**）
    (2) これは #1155 が AGENTS.md へ足したばかりの規範
        （「写しを直す当のコミットが取りこぼす」）が、**その規範を書いた差分自身で再現した**形である
  是正: :114 / :145 に取り消し線か「Phase 5 で偽と判明」の 1 行を足すだけでよい（本文は歴史として残す）。

[R2-2] ⚠️ — **`docs/superpowers/` へ解決される参照は 0 ではなく 1 件ある**（`specs/` 側）

  `docs/architecture.md:229` が `docs/superpowers/specs/2026-08-10-search-worker-design.md` を
  対象にした正準形を持つ（上記 [A]）。新しい doc の文言は「当該ディレクトリ（= `plans/`）」に
  限定されているので**偽ではない**。ただし「走査元から外れた歴史資料には誰も向いていない」という
  直観は**正しくない**——生きた層は現に `superpowers/` の中を 1 件指している。
  今日 `specs/` にテスト名の形の行は 0 件（実測）なので露出はゼロ。
  決着: `docs/superpowers/specs/` に `describe(` / `it(` 形の行が現れたら残余が生きる。
  doc の限定（`plans/`）は正確なので修正は要らないと考えるが、余白が 1 ディレクトリ分しか
  無いことは記録に値する。

[R2-3] ⚠️ — `.claude/rules/governance-docs.md` の新しい括弧書きが、正本より少し強い

  新しい文: 「（**対象として認める綴りの正本は … `isRefTargetSpelling` である**——ここへ写さない。
             **同 doc が、綴りごとに何が着地先になるかも持つ**）」
  実際の `isRefTargetSpelling` の doc（`lib.mjs:179-192`）が着地先に触れているのは **`.mjs` の 1 綴りだけ**
  （「着地先は `ANCHOR_SPECS` のテスト名の腕が供給する」）。`.md` と `/skill-name` の着地先は書いていない。
  「綴りごとに」は**全称の含みを持つ**ので、正本を読みに行った書き手が期待を裏切られる。
  害は小さい（M-1 の修正方向自体は正しく、列挙は確かに消えている）。
  決着: 「`.mjs` については着地先も同 doc が持つ」へ弱めるか、正本側に `.md` / skill の着地先を 1 行足す。
  どちらでもよいが、**前者のほうが下限主張なので腐りにくい**。

[R2-4] ⚠️ — W-4 の記述の細部（`describe` の節が閉じる位置）

  新しい文: 「`describe` の節は次の `it` で閉じる」
  実際の `sectionsOf` は「深さ 7 以下の**次のアンカー**」で閉じるので、閉じるのは次の `it` とは限らない
  （次の `describe`、あるいは JSDoc の太字リード行でも閉じる）。`.test.mjs` では次のアンカーが
  `it` であることが多いという経験則を書いた形。**受容する残余の宣言としては十分に機能する**ので
  指摘の重みは低い。決着: 「次のアンカー」に替えれば正確になる。

■ 検算して**問題なし**と結論したもの

  (a) `lib.mjs:415` 「**行頭に錨を張る**ので、`split("\n")` のように行の途中に現れる `it(` には
      当たらない」 — **W-1 の指摘どおりに直っている。**「他の腕と違い引用符の中だけを見る」という
      誤った理由づけが消え、正しい理由（行頭錨）だけが残った。✅ **W-1 は解消。**

  (b) `lib.mjs:418` 「`docs/superpowers/plans/` の擬似コードにはこの形の行が実在する
      （**`.rs` には無い**）」 — 全称否定だが、**2 つの方法で測って真**:
        ① `grep -rnE '^[[:space:]]*(describe|it)\([[:space:]]*"' --include=*.rs .` → 0 件
        ② `git grep -nE '…' -- '*.rs'`（追跡ファイルのみ・除外句なし）→ 0 件
      根拠は**この 2 つの実測だけ**である——「Rust では構文上ありえない」とは書かない
      （`it` という名の関数呼び出しが文頭に来る形も、マクロが生成する形も、構文としては書ける）。
      **文の主部は存在主張（「実在する」）で書かれており、これは下限主張＝腐りにくい形である。**
      括弧内の全称否定 1 つだけが残っているが、測って真なので指摘しない。✅

  (c) `docs/adr/ADR-canonical-heading-references.md:43` 「導入時に赤くなった**既存の腐りは**
      #1155 が直す 1 件で … （もう 1 件は下に書くとおり、この差分が自分で持ち込んだ例示である）」
      — **M-2 の指摘どおりに直っている。**「既存の」という限定が入り、2 行下の記述と
      `workspace/plan.md:115` の実行記録の両方と整合した。✅ **M-2 は解消。**

  (d) `.claude/rules/governance-docs.md` から**綴りの列挙が消えている**（検算: 当該行に
      `.md` / `.mjs` / `/skill-name` の 3 つ組が無いことを目視と grep で確認）。
      同じ差分が `G-folded-heading-refs.mjs` へ足した「綴りをここへ列挙しない」との自己矛盾は解消。
      ✅ **M-1 は解消**（残る細部は R2-3）。

  (e) **H-2 の新しい受け入れ条件が、書かれたとおりに成立している。**
        $ git grep -nE '\b(crateSources|skillDocs)\b' -- 'scripts/' '.claude/' '*.md' ':!docs/' ':!workspace/'
        → **0 件** ✅
      U4 の裁定（`docs/design/2026-08-20-…` は引用原文ゆえ触らない）を維持したうえで、
      条件の側を実態へ合わせた形。**「機構が生きた層と判定すること」と「書き換えてよいこと」は
      別である**という非対称を受容する残余として明記しており、これは私が第 1 巡で
      「反対側の重み」として挙げたものを正面から採った書き方である。裁定として妥当と考える。
      ただし条件文が「`scripts/` と `.claude/` とルート `*.md` について」と**列挙**になったので、
      将来 `docs/` 直下に生きた層の文書が増えたら射程から漏れる。下限主張ではないが、
      受け入れ条件はその PR 限りのものなので指摘しない（記録のみ）。

  (f) W-5（`AGENTS.md` への規範追加の合意）— 呼び出し側が「人間の承認済み・記録は
      `workspace/plan.md`「人間レビュー」に逐語」と回答。**私はその節を読んでいないが、
      承認の有無は私が判定すべき事項ではない**（`agent-plan-approval-is-not-user-consent` の
      裏返しで、承認の所在を主張するのはユーザー側の権能）。**⚠️ を解除する。**

■ ⚠️ の決着（依頼 4）

  [W-1] **解消**（上記 (a)）。修正差分が理由づけを正しい側へ置き換えた。

  [W-3] **決着した。** 代表入力を腕の正規表現へ直接当てて実測（機構を変えずに測れる形）:
          MATCH "double"   <- '  it("double", () => {})'
          no match         <- "  it('single', () => {})"        ← シングルクォート
          no match         <- '  it.each([1])("x")'             ← `.each`
          no match         <- '  describe.skip("y")'            ← `.skip`
          no match         <- '  const a = s.split("\n")'       ← 行中（行頭錨が効いている）
          no match         <- '// it("in comment")'             ← コメント行
          no match         <- '  it ("space before paren")'     ← 括弧前の空白
        → 当たるのは `describe(` / `it(` の直後にダブルクォートが来る行頭形のみ。
          **`it.skip` / `it.each` / `describe.each` / シングルクォートはすべて着地先にならない。**
          向きは **fail-loud**（そのテスト名を対象にした正準形は「着地しない」で赤くなる）ので
          沈黙しない。**露出は今日 0**（リポジトリは prettier 既定で全件ダブルクォート）。
          → **欠陥ではない。宣言する死角として doc に 1 行あってもよいが、必須ではない。⚠️ を解除する。**

  [W-2] **未対応のまま残っている（Medium へ格上げして持ち越す）。**
        `scripts/governance/lib.mjs:181` の「`G-heading-refs` / `G-near-heading-refs` /
        `G-folded-heading-refs` / `dependents.mjs` の **4 者**が同じ述語を読む」は今巡も未修正。
        再測すると今日も真（非テストの消費者はちょうど 4 つ）。
        しかし M-1 で「綴りを列挙するな」を貫いた**同じファイルの 11 行下**に、
        今度は**消費者の数え上げ**が残っている。`.claude/rules/governance-docs.md` の
        「機構の実装の詳細（**述語の種類・件数**・分岐の列挙）を散文へ写さない」に当たる形である。
        M-1 を採るなら整合上こちらも採るのが筋だと考える（採否は呼び出し側の判断）。
        直し方の手本は**同じファイルの 5 行上**にある——`HEADING_REF` の doc が
        「**消費者は 1 つではない**」という下限主張で同じことを言っている（:176）。
        決着の手順: `4 者` を消して下限主張へ倒すか、そのまま受容すると宣言する。

--------------------------------------------------------------------------------
5. 変異注入の再実行（自分の枠組みを修正差分にも当てる）
--------------------------------------------------------------------------------
`AGENTS.md`「条件別チェック」の「レビュー指摘へ修正（fix-forward）を当てた」に従い、
**第 1 巡で指摘を出した枠組み（変異 A）を修正差分にも再実行した**。
「解消した」の判定を報告の受け取りではなく**自分の道具で**行うため。

ベースライン: `git rev-parse 0c949ff4:scripts/governance/lib.mjs`
              = f5a1fc0a003fb8902fc5332203cb25251db9a482
              `git hash-object` も同値（作業ツリーは 0c949ff4 の内容）

変異 A（`ANCHOR_SPECS` からテスト名の腕を削除）:
  $ npm run governance:check
  governance:check — 2 件の不整合:
    scripts/governance/checks/G-rules-script-coverage.mjs:25  見出し参照が着地しない:
      `G-rules-script-coverage.test.mjs`「母集団の下界」
    scripts/governance-check.test.mjs:118  見出し参照が着地しない:
      `governance/evidence.test.mjs`「配線:」
  **exit code: 1** ✅ 第 1 巡（702fa046）と**逐語で同一の 2 件**。
  → 散文だけの修正で機構が劣化していないことを実測で確認。
  → 上記 3 節 [B] の純計算（腕に依存する参照 2 件）とも**独立に一致**。

巻き戻し（内容ハッシュで照合）:
  $ git checkout -- scripts/governance/lib.mjs
  $ git hash-object scripts/governance/lib.mjs
    f5a1fc0a003fb8902fc5332203cb25251db9a482  ← ベースラインと一致 ✅
  $ git status --porcelain → 無出力

変異 B（`isRefTargetSpelling` から `.mjs` を外す）は**再実行していない**。理由は 2 つ:
  (1) `lib.mjs` の実行コードが 702fa046 と**バイト単位で同一**であることを機械的に確かめてあり
      （2 節）、変異 B の対象行も結果（324 → 322・`npm test` の 1 件失敗）も変わりようがない
  (2) 3 節 [C] の実測（`.mjs` を対象にした参照はちょうど 2 件）が、322 への減少幅を
      変異を当てずに独立に裏付けている
必要なら再実行できる。**「測っていない」と明記しておく**（測ったふりをしない）。

注入は隔離 worktree（複製）のみ。`C:/workspace/Snotra` の稼働中のガードには触れていない。
一時 script（解決器プローブ）は scratchpad へ書き、リポジトリ側の複製は測定後に削除した
（`git status --porcelain` の無出力で確認済み）。

--------------------------------------------------------------------------------
6. まとめ
--------------------------------------------------------------------------------
- 検証: `npm run governance:check` **exit 0** / `npm test` **exit 0**（852 passed）。
  A〜E は該当なし。計器の差分は rules 面積 12995 → 12981 字の 1 か所のみで、M-1 の修正と整合。
- 「散文のみ」は**機械的に検算した**（コメント除去後の `lib.mjs` が 702fa046 と同一）。
- **H-1 の修正は成立している。** grep を使わず機構の解決器そのものを走らせて独立に測り、
  `plans/` へ解決される参照が 0 件であること、suffix 一致の経路は生きているが未行使であること、
  第 2 の消費者（`sectionsOf`）も同じ 1 つの事実で無害化されていることを確認した。
- **M-1・M-2・W-1 は解消。W-3 は決着（欠陥ではない）。W-5 は解除**（承認の所在は呼び出し側の権能）。
  **H-2 は裁定を採り、新しい受け入れ条件が書かれたとおり成立していることを実測。**
- 新規: **Medium 1（R2-1）/ ⚠️ 3（R2-2・R2-3・R2-4）**、持ち越し **Medium 1（W-2）**。
  Critical・High はゼロ。
- **R2-1 と W-2 は同じ形をしている**——どちらも「今回の差分が確立した規範を、同じ差分の
  別の場所が破っている」。#1155 は規範の内容としては正しく、**射程の適用が 1 手ぶん届いていない**。
================================================================================
