================================================================================
issue #1155 — 第 5 巡（最終） — 対象 sha b0e67251
検証者: 隔離 worktree agent-a3d6dba6825a6087d / 実施日: 2026-08-20

出力先: C:\workspace\Snotra\.claude\worktrees\agent-a3d6dba6825a6087d\workspace\verify-1155-round5.txt
前巡の verify-1155-round4.txt は b0e67251 に blob 4c5a2284… として取り込まれており、
私の未追跡ファイルとバイト一致することを確認してから削除・切り替えた。
================================================================================

■■ 結論 ■■
  (i)  直した負例は**狙った変異で落ちる**。落ちたのは**新しい負例 1 本だけ**であることまで確認。✅
  (ii) 命題の残存は生きた層に **1 件**（相手の 3 パターンが取りこぼす言い換え）。
       ただし **#1152 由来で #1155 の射程外** → 次の issue へ送る。
  **#1155 として閉じてよい。** 残る所見は「対応不要」2 群と「次の issue」2 件に分かれる。

--------------------------------------------------------------------------------
0. アンカーと実行
--------------------------------------------------------------------------------
HEAD: b0e67251690d6524b9f7f49c005695f9e6a07a18 ✅

  $ npm run governance:check  →  **exit 0**
    （検査 21 件 / 見出し参照 324 件 / 近傍 19 件 / 折れうる位置 24 件 /
      ADR の短縮引用 363 件 / rules 12976 字 — **前巡から全件不変**。今回の差分が
      `.mjs` のコメントと `workspace/` だけであることと整合する）
  $ npm test                  →  **exit 0**（36 files / **853 passed**）

--------------------------------------------------------------------------------
1. 【依頼 (i)・最優先】直した負例は変異で落ちるか → **落ちる。しかも正しい 1 本が落ちる**
--------------------------------------------------------------------------------

【変異】4 巡目と同一（`scripts/governance/lib.mjs:194-195`）:
    -  target.endsWith(".md") || target.endsWith(".mjs") || /^\/[a-z0-9-]+$/.test(target);
    +  target.endsWith(".md") || target.includes(".")     || /^\/[a-z0-9-]+$/.test(target);

【結果】`npm test` → **exit 1**

    ❯ scripts/governance/checks/G-folded-heading-refs.test.mjs (22 tests | 1 failed)
        × ドットを持っても対象綴りでなければ見ない（`.ps1` / `.ts` は宣言された死角）

    FAIL scripts/governance/checks/G-folded-heading-refs.test.mjs
      > 負例 — 折れていないものを赤にしない
      > **ドットを持っても対象綴りでなければ見ない（`.ps1` / `.ts` は宣言された死角）**
    AssertionError: expected [ Array(1) ] to deeply equal []
      + [ { "file": "docs/x.md", "line": 1,
      +     "message": "正準形の参照が物理改行で折れている（ラベルが次行から始まっている）:
      +                 `scripts/manual-smoke.ps1` — G-heading-refs の母集団から落ちる。…" } ]
      ❯ scripts/governance/checks/G-folded-heading-refs.test.mjs:105:61

      Test Files  1 failed | 35 passed (36)
      Tests       **1 failed** | 852 passed (853)

【帰属の確認（依頼された切り分け）】
  - 落ちたテストは **`ドットを持っても対象綴りでなければ見ない（.ps1 / .ts は宣言された死角）`**
    ＝**今回直した負例そのもの**である。
  - 落ちたのは **1 本だけ**（852 passed）。**別のテストが落ちて「落ちた」と読んだ形ではない。**
  - assertion の差分が**理由まで示している**——`scripts/manual-smoke.ps1` が対象綴りとして
    受理された結果、折れ検知が finding を 1 件返した。前巡の空振り（走査行数 0）とは違い、
    **入力が判定へ到達したうえで判定が変わった**ことが出力から読める。
  - 前巡の「述語は動くが入力が到達しない」との対照:
      前巡（`file="a.mjs"`）: 走査行数 **0** → findings [] → **緑のまま**
      今回（`file` 既定 `.md`）: 走査行数 3 → findings **1 件** → **赤**
    → **是正案 (A) は狙いどおり効いている。RA-2 は解決。**

【付随の観察（欠陥ではない・記録のみ）】
  失敗したのは 1 行目の `.ps1` の expect（:105）で、2 行目の `.ts`（:106）は
  1 行目が throw した時点で実行されない。ゆえに **`.ts` 側は今回独立には測られていない**。
  ただし述語が `.ts` だけを受理して `.ps1` を弾く変異なら :105 が通って :106 が落ちるので、
  **どちらの綴りが緩んでも赤になる**。固定点としては両方が効いている。

【前向きの約束も真になった】
  4 巡目に「偽」と指摘したテストコメントの
  「**死角を埋める日には同じ差分でこの負例も動く**」は、今回**真**である——
  死角を埋める（`.ps1` を対象綴りへ足す）変更は上の変異に含まれ、現にこの負例が落ちた。

--------------------------------------------------------------------------------
2. 【依頼 (ii)】「両方とも沈黙する」の命題の残存
--------------------------------------------------------------------------------
相手の 3 パターン（「両方とも沈黙」「どちらも沈黙」「どの層も赤」）が**取りこぼす言い換え**を
探すため、パターンではなく**命題の語彙空間**を 2 回に分けて掃いた:

  掃き 1: (両方|双方|いずれ|どちら|2 つとも|どの層|どの検査|どこ|誰)
          × (沈黙|黙|鳴ら|赤く?(な|し)|緑|素通|気づか|出ない|捕ま)
  掃き 2: (狭窄|狭めて|縮んで|縮めて|落として) × (沈黙|黙|鳴ら|赤|緑|気づか|捕ま|通る|素通)
          および (exit 0|緑のまま|全緑) × (沈黙|推移|通る|まま)
  射程: `scripts/` `.claude/` ルート `*.md`（`docs/superpowers/`・`workspace/` を除く）

【結果 — 生きた層に 1 件】

  ★ `.claude/skills/health-check/SKILL.md:78`
      **「母集団が黙って縮んでも誰も赤くしない」は受容した残余であり、発見事項ではない**
      ——同 ADR の「受容する残余」が正本。

    - **相手の 3 パターンはどれも当たらない**（実測: `git grep -nE '(両方とも沈黙|どちらも沈黙|どの層も赤)'
      -- .claude/skills/health-check/SKILL.md` は**空**）。言い換えは「誰も赤くしない」である。
    - **生きた層である**——凍結 ADR ではなく、`/health-check` を走らせる運用者へ配送されるスキル。
    - 私の実測と食い違う: 母集団が縮む 2 経路のどちらにも赤くする層が在る
      （`WALK_EXCLUDE_PATHS` 経由 → `governance:check` **exit 1 / 11 件**、
        `SCRIPT_EXT` 経由 → `npm test` **2 本**失敗）。
    - **出所: `git log -S` → 74ae45fc（#1152）。#1155 の差分ではない。**
    - → **#1155 の射程外**（4 巡目で双方が採った「射程を広げ続けると閉じられない」の裁定と同じ）。
      **次の issue へ送る**（下の N-1）。

【それ以外のヒットはすべて別命題】
  `ADR-facade-evidence-static-imports.md:27`（`ctx.record` の配線）・`G-clippy-disallowed`
  `G-workspace-lints`（clippy / cargo の exit 0）・`G-module-linkage`（`mod` 忘れ）・
  `SnotraWindowColors.psm1`（閾値の緩み）等は、**どれも「2 つの狭窄」とは無関係の命題**である。
  `lib.mjs:305`「許可集合への所属で判定する側は縮んでも赤くならない」は条件付き（「〜する側は」）で
  全称ではなく、原理の記述として正しい。

【#1155 が導入した分の残存】
  `git log -S` で出所を確かめたところ、**#1155 が導入した命題の言い換えは生きた層にゼロ**である。
  `G-rules-script-coverage.mjs:50` は否定形へ書き換わり（「捕まる理由も、どの層が鳴るかも形ごとに
  違う。上の 2 つがその実例である」）、`:63` と `G-adr-citations.mjs:52` は
  **訂正の記録として**旧文言を引用している（引用と主張は別物なので残ってよい）。
  → **相手の「生きた層の残存はゼロ」は、#1155 が導入した分については正しい。**
    唯一の取りこぼしは #1152 由来の 1 件であり、それは相手の分類（「#1143/#1152 由来＝射程外」）に
    そのまま乗る——**パターンが当たらなかったので分類にすら到達しなかった**、という違いだけである。

--------------------------------------------------------------------------------
3. 連鎖は止まったか → **止まった**
--------------------------------------------------------------------------------
b0e67251 が追加した行を全部読み、全称・数え上げ・前向きの約束を抽出して実測と突き合わせた:

  - 「`file` を既定（`.md`）のままにすること。…**変異を当てても緑のままだった**（実測）」
    → 私の 4 巡目の測定と**逐語で一致**。機序（`refScanLines` が `linesOfComments` を選ぶ）も正しい ✅
  - 「今日これを縛るのは `npm test` の**側**である（上に挙げた検知器がその経路を持つ）」
    → 単一テストの名指しをやめた。実測（2 本落ちる・どちらも `npm test`）と整合 ✅（R4-4 解決）
  - 「**そこを走査する検査が鳴る**——どれが鳴るかは落とした先に依るのでここで検査名を挙げない」
    → 列挙をやめた。私の是正案そのもの ✅（R4-3 解決）
  - 「捕まる理由も、どの層が鳴るかも形ごとに違う。上の 2 つがその実例である
     （片方は `governance:check` で沈黙し、片方は赤くなるが原因を名指さない）」
    → 撤回済みの「両方とも沈黙」が消え、実測どおりの非対称になった ✅（R4-2 解決）
  - **新しい全称否定・数え上げ・未測定の前向きの約束は 1 件も入っていない。**

  → **4 巡続いた「直した文が別の形で偽になる」連鎖は、この巡で初めて再生しなかった。**
    機序としては、今回の 2 つの修正がどちらも**列挙・名指しをやめる方向**（下限主張・正本への委譲）
    だったことが効いている。増やす方向の修正は次の反復で腐りうるが、減らす方向は腐り先を持たない。

  ⚠️ 1 点だけ、`workspace/plan.md:190` の実行記録
    「生きた層の残存は**ゼロ**を確認」は、§2 の 1 件を含めると厳密には過大である。
    ただし `workspace/` は畳まれる作業バッファであり、**この巡の後は誰も見ない**（相手の申告）。
    **対応不要**と裁定する。

--------------------------------------------------------------------------------
4. 残る所見の振り分け
--------------------------------------------------------------------------------

════════════════════════════════════════════════════════════════════════════════
A. 対応不要と裁定できるもの（#1155 で閉じてよい）
════════════════════════════════════════════════════════════════════════════════
 A-1  負例の `.ts` 行が第 1 の expect の throw で短絡し、独立には測られていない。
      → **不要。** どちらの綴りが緩んでも赤になる（片方だけ受理する変異なら :106 が落ちる）。
 A-2  `docs/superpowers/plans/` の 95 行が新しい腕でアンカーになる（1 巡目 H-1 の残余）。
      → **不要。** `ANCHOR_SPECS` の doc が**受容する残余**として宣言済みで、
        「今日それが着地候補にならない」ことは私が解決器で独立に実測した（`plans/` へ解決される
        参照 0 件）。数え上げも入っていない。
 A-3  `docs/design/2026-08-20-…-derivations.md` の幽霊識別子（1 巡目 H-2）。
      → **不要。** U4 の裁定（引用原文ゆえ触らない）を維持し、受け入れ条件の側を実態へ直した。
        「機構が生きた層と判定すること」と「書き換えてよいこと」は別、という非対称も明記済み。
 A-4  `docs/architecture.md:229` が `docs/superpowers/specs/` を指す 1 件（2 巡目 R2-2）。
      → **不要。** doc の限定は `plans/` であり偽ではない。私自身がそう書いた。
 A-5  `workspace/plan.md:190` の「生きた層の残存はゼロ」（上記 §3 の ⚠️）。
      → **不要。** 作業バッファであり畳まれる。

════════════════════════════════════════════════════════════════════════════════
B. 次の issue へ送るべきもの（#1155 の射程を越える）
════════════════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────────────
N-1  「母集団が黙って縮んでも誰も赤くしない」が生きた層に残り、実測と食い違う
────────────────────────────────────────────────────────────────────────────────
 **所在**
   - `.claude/skills/health-check/SKILL.md:78`（**生きた層**・`/health-check` の運用者へ配送）
   - `docs/adr/ADR-governance-anchor-layer-discarded.md:35`（**凍結 ADR**・上記が正本として指す先）

 **出所**: `git log -S "母集団が黙って縮んでも誰も赤くしない"` → **74ae45fc（#1152）**

 **なぜ偽か（#1155 の 3・4 巡目で実測）**
   母集団（`judgingScripts`）が縮む道は 2 つあり、**どちらにも赤くする層が在る**:
     - `WALK_EXCLUDE_PATHS` へ 1 語足す → `npm run governance:check` **exit 1 / 11 件**
       （見出し参照 1・バッククォート参照の実在 5・散文の識別子 5）
     - `SCRIPT_EXT` を `/\.(mjs)$/` へ狭める → `npm test` **2 本失敗**
       （「母集団の下界」と「canary: 実ツリーの母集団」）
   失われたのは**検出**ではなく**帰属**（原因を「母集団が縮んだ」と名指すこと）である。

 **害**
   SKILL.md は運用者へ「**発見事項ではない**」と指示している。母集団の縮みで実際に赤が出たとき、
   運用者はそれを**受容済み残余として棄却する**よう導かれる。`/health-check` は「報告のみ・
   修正しない」スキルなので、**誤った棄却は記録にも残らない**。

 **直し方（1 段落）**
   #1155 が `judgingScripts` の doc と `G-adr-citations` で採ったのと同じ形へ倒す——
   「赤くなること自体は在る。無いのは**帰属**である」。ADR は凍結ゆえ本文を触らず、
   SKILL.md 側に日付つきの読み替えを置く（`ADR-governance-meta-demotion` へ #1155 が
   足した追記と同じ手）。

 **なぜ #1155 で閉じないか**
   #1152 由来であり、#1155 の射程（撤去した層の**語彙**の後始末）は `scripts/` の doc に閉じている。
   4 巡目に双方が「射程を広げ続けると #1155 が閉じられない」で合意した R4-5 と同じクラスである。

────────────────────────────────────────────────────────────────────────────────
N-2  `judgingScripts` の 2 経路は「別々の検知器」で縛られていない（実測）
────────────────────────────────────────────────────────────────────────────────
 **所在**: `scripts/governance/checks/G-rules-script-coverage.mjs:23-31`
   - `:23`「母集団を狭める道は 2 つあり、**縛っている検知器が別々である**」
   - `:24`「（…**放っておけばどの層も赤にしない**・2026-08-19 実測）」
   - `:25-26` が 2 経路へ検知器を 1 本ずつ割り当てている

 **出所**: `git log -S "放っておけばどの層も赤にしない"` → **fce42f13（#1143）**

 **なぜ偽か（#1155 の 4 巡目で実測）**
   `SCRIPT_EXT` を狭めると `npm test` は **2 本**落ちる:
     × 母集団の下界: .ps1 / .psm1 も対象である（SCRIPT_EXT を狭める変異を捕まえる）
     × **canary: 実ツリーの母集団が走査側で縮んでいない（WALK_EXCLUDE_PATHS の狭窄を捕まえる）**
   後者は `:26` が**もう一方の経路に割り当てている**カナリアである。ゆえに
   「検知器が別々」は成り立たず、`:24` の「どの層も赤にしない」も偽（他検査 11 件・npm test 2 本）。

 **害**
   `:25-26` の対応表を信じて片方の検知器を消す/緩める改修が、
   「もう一方は別の検知器が守っている」という誤った安心の上で通りうる。

 **直し方（1 段落）**: 「別々である」をやめ、**片方の狭め方が両方を落とす**ことを書く。
   あるいは対応表を「どちらの狭め方も `npm test` が縛る（どのテストが落ちるかは狭め方による）」へ畳む。

 **粒度**: N-1 と同じファイル群・同じ性質（撤去後に残った「誰も見ていない」の過大主張）なので、
   **1 つの issue にまとめてよい**。表題案:
   「撤去後の doc に残る『誰も赤くしない』を実測に合わせる（#1152 / #1143 由来・#1155 で実測済み）」

--------------------------------------------------------------------------------
5. 注入と巻き戻し
--------------------------------------------------------------------------------
  対象: `scripts/governance/lib.mjs`（`isRefTargetSpelling` を `includes(".")` へ）
  結果: `npm test` **exit 1 / 1 failed**（新しい負例のみ）
  巻き戻し: `git checkout --` 後の `git hash-object` = **939e8b3394bb847b821b1b9e414a49fdbba3cbb5**
            （`git rev-parse b0e67251:scripts/governance/lib.mjs` と一致）✅
  最終状態: `git status --porcelain` は本レポート以外に何も出さない。
  注入は隔離 worktree（複製）のみ。稼働中のガードには触れていない。

--------------------------------------------------------------------------------
6. まとめ
--------------------------------------------------------------------------------
- `npm run governance:check` **exit 0** / `npm test` **exit 0**（853 passed）。計器は前巡から全件不変。
- **(i) RA-2 は解決した。** 変異で **exit 1**、落ちたのは**新しい負例 1 本だけ**で、
  assertion の差分が理由（`.ps1` が受理されて折れ検知が発火）まで示している。
  前巡の空振り（走査行数 0）との対照も取った。
- **(ii) 命題の残存は生きた層に 1 件**（`health-check/SKILL.md:78` の「誰も赤くしない」）。
  相手の 3 パターンは当たらない。ただし **#1152 由来で #1155 の射程外** → N-1 として次の issue へ。
  **#1155 が導入した分の残存はゼロ**であり、相手の確認はその範囲で正しい。
- **4 巡続いた連鎖は、この巡で初めて再生しなかった。** 今回の修正がどちらも
  「列挙・名指しをやめる」方向だったことが効いている——**減らす方向の修正は腐り先を持たない**。
- **#1155 として閉じてよい。** 対応不要 5 件、次の issue へ 2 件（1 つの issue にまとめられる）。
================================================================================
