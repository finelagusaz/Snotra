================================================================================
issue #1155 実装の検証とレビュー — 対象 sha 702fa046
検証者: 隔離 worktree agent-a3d6dba6825a6087d
実施日: 2026-08-20

【出力先について】呼び出し側が指定した C:\workspace\Snotra\workspace\verify-1155.txt へは
書けなかった（worktree 隔離により共有チェックアウトへの Write が拒否された）。
本ファイルは worktree 側の同名パス
  C:\workspace\Snotra\.claude\worktrees\agent-a3d6dba6825a6087d\workspace\verify-1155.txt
に置いてある。回収が必要なら worktree が消える前にコピーされたい。
================================================================================

--------------------------------------------------------------------------------
0. アンカーの確認
--------------------------------------------------------------------------------
起動時の HEAD:  585c09913079f907a63a5569a85b3b819783fc8b (#1156 / main)
→ アンカーと不一致。`git checkout --detach 702fa046` で合わせた。
確認後の HEAD:  702fa0460ced70148975f8e20f145258a76a1692  ✅

コミット列（実測）:
  702fa046 fix(governance): 撤去した錨の層の語彙を、消えた保証を消えたものとして書き直す (#1155)
  01c6eede feat(governance): 正準形の対象綴りに .mjs を足し、撤去済みファイルへの参照を赤にする (#1155)
  179a085d chore: workspace 調査・計画 (issue #1155)   ← 比較の基点
  585c0991 (main)

作業ツリーは検証終了時点で clean（`git status --porcelain` 無出力）。

--------------------------------------------------------------------------------
1. カテゴリ A〜F の検証
--------------------------------------------------------------------------------
`git diff --stat 179a085d..702fa046`（自分で実測）:

 .claude/rules/governance-docs.md                        |  2 +-
 AGENTS.md                                               |  1 +
 docs/adr/ADR-canonical-heading-references.md            |  6 +++
 docs/adr/ADR-governance-meta-demotion.md                |  9 ++++
 scripts/governance/checks/G-adr-citations.mjs           | 18 +++----
 scripts/governance/checks/G-check-skill-enumeration.mjs | 10 ++--
 scripts/governance/checks/G-folded-heading-refs.mjs     |  3 +-
 scripts/governance/checks/G-folded-heading-refs.test.mjs| 10 +++-
 scripts/governance/checks/G-module-index.mjs            | 25 +++++----
 scripts/governance/checks/G-module-linkage.mjs          |  2 +-
 scripts/governance/checks/G-rules-script-coverage.mjs   | 26 +++++----
 scripts/governance/lib.mjs                              | 46 ++++++++++++----
 workspace/plan.md                                       | 61 +++++++++++-------
 13 files changed, 140 insertions(+), 79 deletions(-)

→ `.rs` は 0 ファイル。`.mjs`（ガバナンススクリプト）と `.md`（規範文書・ADR）のみ。
  変更が触れるコードパスも Rust を含まない（`scripts/governance/` は node のみで、
  `lib.mjs` の依存は Node 標準モジュールに限られる — 同ファイルの `//!` 契約）。

| カテゴリ | 判定 | 根拠・コマンド・exit code |
|---|---|---|
| A. Rust (`*.rs`) | 該当なし | 差分に `.rs` が 0 件。Rust のコードパスを 1 行も通らない |
| B. TypeScript | 該当なし | 差分に `.ts` が 0 件（残る `.ts` は `vitest.config.ts` のみで未変更） |
| C. 窓生成/表示順/hotkey/スラッシュコマンド | 該当なし（ただし `npm test` は実行） | 表示経路・hotkey・trace 名に触れていない。`npm test` はカテゴリ C の必須行だが、本差分では「変更した `scripts/*.mjs` と `*.test.mjs` を検証する層」として呼び出し側の指示どおり実行した |
| D. UI 表示 | 該当なし | 描画を 1 ピクセルも変えない（`docs/build-commands.md` カテゴリ D の「変えたのが表示の見た目かどうかで決める」に従う。#1106 の誤認を繰り返さない） |
| E. git hook (`.githooks/**`) | 該当なし | `.githooks/` に差分なし |
| F. ガバナンス文書 | 実行 | `npm run governance:check` → exit 0 |
| （追加）`npm test` | 実行 | `npm test`（vitest run） → exit 0 |

実行の詳細:

  $ npm run governance:check
  governance:check — 全検査 passed（検査 21 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 /
  恒久規範 常時ロード 16948 字・rules 12995 字 / 見出し参照 324 件を md 48 件 + .rs 101 件 +
  スクリプトのコメント 111 件から照合 / workspace member 4 件の lints opt-in / clippy 禁止 8 件 /
  散文の識別子 396 件を 34 文書から照合 / 近傍の見出し参照 19 件 / 折れうる位置 24 件 /
  ADR 69 本の名前 / ADR の短縮引用 363 件）
  exit code: 0   （`npm run governance:check > /dev/null 2>&1; echo $?` で単独取得。
                  パイプ越しに `$?` を読まない・safety-nets.md の注意に従う）

  $ npm test
  Test Files  36 passed (36)
  Tests  852 passed (852)
  Duration 12.27s
  exit code: 0   （同上の形で単独取得）

測定環境:
  worktree に `node_modules` が無かったため junction を張った。
    New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/workspace/Snotra/node_modules
  → `node_modules/vitest` の存在を確認（True）。以後 vitest は正常起動しており、
    本報告に現れる exit 1 はいずれもモジュール解決の失敗ではない
    （変異 B の `npm test` は 851 passed / 1 failed で、失敗したのは変異対象の
     `G-folded-heading-refs.test.mjs` の 1 ケース＝検知である。vitest 本体は解決できていた）。

--------------------------------------------------------------------------------
2. レビュー
--------------------------------------------------------------------------------

■ Critical — 0 件

■ High — 2 件

[H-1] scripts/governance/lib.mjs:416 — 新しく書いた散文の全称否定が偽である
      （実装より強い主張・AGENTS.md「検証の作法（全タスク共通）」の全称否定条項に直撃）

  該当行（`ANCHOR_SPECS` の doc・#1155 で追加）:
      * 波及は `.mjs` に閉じる——`.md` と `.rs` にこの形の行は 1 件も無い（実測）。

  反証（一行のコマンド）:
      $ grep -rnE '^[[:space:]]*(describe|it)\([[:space:]]*"' --include=*.md .
      → 6 ファイル / 95 行。`.rs` は 0 件（そちらの半分は真）。

      docs/superpowers/plans/2026-07-09-hook-responsibility-layers.md       19 行
      docs/superpowers/plans/2026-07-26-skill-workflow-boundary.md           2 行
      docs/superpowers/plans/2026-07-27-plan-checklist-gate.md              16 行
      docs/superpowers/plans/2026-08-14-governance-check-pruning.md         32 行
      docs/superpowers/plans/2026-08-15-governance-check-per-check-split.md 13 行
      docs/superpowers/plans/2026-08-19-governance-domains-phase1.md        13 行

  散文だけの誤りではなく、機能的にも偽である。実測（makeSnapshot + collectAnchors を直接呼んだ）:
    - この 6 ファイルは WALK_EXCLUDE_PATHS（workspace / .claude/worktrees / .superpowers /
      node_modules / target / dist）のいずれにも当たらず、snapshot.files に入っている
      （`in snapshot: true` を実測）。
    - collectAnchors はフェンスをマスクしない（dependents.mjs:73 が明示）。
    - ゆえに 95 行すべてが「新しい腕だけが生む本物のアンカー」になる
      （既存 3 腕との重複は 0 件。「この腕を外すと消えるアンカー」を数えて 95 と実測）。
    - 例: docs/superpowers/plans/2026-08-14-governance-check-pruning.md のアンカーは 63 件へ増え、
      「走査の母集団が黙って縮んでも発火する（WALK_EXCLUDE_PATHS へ 1 行足した形）」のような
      フェンス内のテスト名が着地先として採用されている。
    - 参照先のアンカー解決は走査元の除外に従わない（headingRefDocs の doc が
      「除外が絞るのは走査元だけである——参照先のアンカー解決は snapshot.files 全体に対して
      行われる」と明記）。docs/superpowers/ を走査「元」から外していることは、
      着地「先」としての 95 件を否定しない。

  害の向き: fail-open（沈黙側）。docs/superpowers/plans/*.md の消えた見出しを指す参照が、
  フェンス内のテスト名に偶然一致して緑になりうる。今日の実害は 0（そのような参照は現存しない）だが、
  #1155 自身が「緑のまま推移する形」を塞ぐ変更である以上、同じ形の穴を doc で否定して残すのは筋が悪い。
  加えて sectionsOf（dependents.mjs）でも同じ 95 行が深さ 7 の節境界になり、
  当該 6 文書の依存 reminder の粒度が黙って変わる。

  なお同 doc の「`.mjs` には ATX 見出しが無く、`//! - **…**` は行頭が `//!` なので太字リードにも
  当たらない（実測）」は真であり、腕が必須であること自体は変異 A で独立に裏付いた（後述）。
  問題は「波及範囲の全称否定」だけである。

  決着させる手順: 上の grep 1 本。書き換えの向きは、数え上げ（「1 件も無い」）をやめて
  「`.rs` には当たらない。`.md` では docs/superpowers/plans/ のコードフェンス内に当たるが、
  走査元から外れた文書なので着地先が増えるだけである」のような下限主張へ倒すこと
  （memory の universal-claim-fix-regenerates-itself — 直した文がまた全称で偽になる形を避ける）。


[H-2] docs/design/2026-08-20-governance-meta-demotion-derivations.md:68,128 —
      幽霊識別子の掃除が、機構が生きた層と判定している文書 1 枚を取りこぼしている

  #1155 自身が置いた受け入れ条件（workspace/plan.md:14）:
      2. 幽霊識別子がゼロである — crateSources / skillDocs は現行コードに実体が無い
         （実体は crateSourceFiles / skillFiles）。
         `git grep -nE '\b(crateSources|skillDocs)\b'` の出力が空になる。

  実測（702fa046 の木で、そのとおりのコマンドを実行）:
      $ git grep -nE '\b(crateSources|skillDocs)\b' | grep -v '^workspace/'
      docs/design/2026-08-20-governance-meta-demotion-derivations.md:68:
        (d) 言える。今日の重複（crateSources=moduleIndexSources）はすべて意図的で、畳んではならない
      docs/design/2026-08-20-governance-meta-demotion-derivations.md:128:
        （ruleDocs / skillDocs ドメイン）と重複しており、失われない
      docs/superpowers/plans/2026-08-19-governance-domains-phase2.md:25,26,28,60,61,72
      docs/superpowers/plans/2026-08-20-governance-domains-phase3.md:35,63

  docs/superpowers/ は #589 で非規範化された歴史資料であり、機構も走査元から外している
  （実測: headingRefDocs に含まれない）。射程外として妥当。
  docs/design/ は違う。実測:
      in headingRefDocs (live scan source): true
      in governanceDocs:                    true
  すなわち機構自身が生きた層として扱っている文書に crateSources と skillDocs が残っている。

  反対側の重み（採否は呼び出し側に委ねる）:
    - 当該文書は冒頭で「ここに置くのは、その結論を出した 2 つの導出の原文である」と自称し、
      :68 / :128 はどちらもその引用ブロックの内側にある。#1155 が AGENTS.md へ足した振り分け
      （「撤去を描写している / 撤去されたものが在る前提で書いている」）では前者に寄せる余地がある。
    - しかし :68 は「今日の重複（crateSources=moduleIndexSources）はすべて意図的で」と現在形で
      書かれており、crateSources は現行コードに実体が無い。docs/adr/ と違って凍結宣言
      （ADR-adr-frozen-history に相当するもの）を持たず、機構の母集団からも外れていない。
    - #1155 が AGENTS.md へ足した規範自身が「生きた層から手順として委譲された凍結文書は
      射程の外である」を死角として名指しているのに、その死角に自分が落ちた形になっている。

  最小の是正案（提案・実装はしない）: 当該 2 行に日付つきの読み替え注記を足すか、
  docs/design/ を凍結層として宣言するかのどちらか。前者は ADR-governance-meta-demotion へ
  #1155 が採った手（本文を書き換えず追記で読み替えを与える）と同型。


■ Medium — 2 件

[M-1] .claude/rules/governance-docs.md:9 — 綴りの集合を散文へ列挙しており、
      同じ差分の別の場所がそれを禁じている

  追加された文:
      他を指すときは正準形 `` `<対象>`「<見出し>」 `` で書く（対象として認める綴りの正本は
      scripts/governance/lib.mjs の isRefTargetSpelling。今日は `.md` / `.mjs` / `/skill-name` で、
      `.mjs` の着地先はテスト名である）。

  同じ差分が scripts/governance/checks/G-folded-heading-refs.mjs:39-40 へ足した禁止:
      // プレースホルダは `isRefTargetSpelling` に当たらないので、この検査自身の finding にならない
      // （綴りをここへ列挙しない——#1155 で `.mjs` が加わったとき、列挙した側だけが古びる）。

  さらに同じ rules ファイルの 2 つ下の箇条書きが
      「機構の実装の詳細（述語の種類・件数・分岐の列挙）を散文へ写さない。」（#984 の実例つき）
  と書いている。同一ファイル内で、規範の 2 行上が規範を破っている形。

  反対側の重み: (a) SSOT（isRefTargetSpelling）を名指している、(b)「今日は」でヘッジしている、
  (c) rules は書き手へ配送される規範なので、使える綴りを 1 度は示す実用上の必要がある。
  ゆえに Critical/High ではなく Medium。決着は「この列挙が次に綴りが増えたとき腐るか」で、
  答えは #984 の先例どおり腐る（並行 PR が足せば squash の瞬間に偽になる）。

[M-2] docs/adr/ADR-canonical-heading-references.md:37 — 「1 件だけ」が同じ ADR の 2 行下と食い違う

      - 効果は今日の検知数ではない。導入時に赤くなったのは #1155 が直す 1 件だけで、…
      （2 行下）
      - #925 の却下理由「検出器の説明が検出器を赤にする」は、対象綴りの側でも起きた——
        導入と同じ差分で、isRefTargetSpelling の doc に書いた例示のプレースホルダ自身が赤になった。

  workspace/plan.md:115 にも「同じ Red で lib.mjs:185 も鳴った」と実行記録が残っている。
  すなわち導入時に赤くなったのは 2 件（うち 1 件は同じ差分が自ら作って自ら消した）。
  意味は 2 行の合成で伝わるが、「1 件だけ」という数え上げは単独で読むと偽である
  （AGENTS.md「検証の作法」の「数え上げも同じ強さである」）。


■ 指摘なし（問い合わせのうち、結論が「問題なし」だったもの）

  (a) ANCHOR_SPECS の新しい腕の過剰一致 — 問題なし（`.mjs` について）
      scripts/governance/lib.mjs:422:
          { re: /^\s*(?:describe|it)\(\s*"([^"]*)"/, depth: () => 7, label: (m) => m[1] },
      - `.rs`: 0 件（実測）
      - 非テストの `.mjs`: 0 件（実測。当たる 834 行はすべて *.test.mjs）
      - `.mjs` のコメント行: `//` `//!` `/**` `*` はいずれも `^\s*` の直後が describe|it に
        ならないので構造的に当たらない
      - テンプレートリテラル内の偽アンカー: ヒューリスティック走査（バッククォート数のパリティ）で
        5 件出たがすべて誤検出で、実体は本物の `it(` 呼び出しだった。今日の偽アンカーは 0 件
      → 過剰一致は `.md` の 95 行のみで、それは H-1 として別立てにした。

  (b) resolveRefTarget の skill 形（`/name`）の扱い — 変わっていない
      scripts/governance/lib.mjs:439-451:
          export function resolveRefTarget(snapshot, doc, target) {
            if (/^\/[a-z0-9-]+$/.test(target)) {        // ← skill 形はここで必ず早期 return
              const p = `.claude/skills/${target.slice(1)}/SKILL.md`;
              return snapshot.files.includes(p) ? p : null;
            }
            if (!isRefTargetSpelling(target)) return null;   // ← 変更点（旧: !target.endsWith(".md")）
            …
      skill 形の分岐は綴りのゲートより前にあり、しかも両者の正規表現は逐語で同一である。
      ゆえに `/name` が第 2 行へ到達する経路が存在しない。挙動差は `.mjs` が return null を
      免れる 1 点のみ。✅
      （呼び出し側が「意図的に別」と申し送った 2 つの正規表現は、そのとおり別のままである。
        述語は「対象として認めるか」、分岐は「SKILL.md のパスを構築する」。畳んでいない。✅）

  (c) 測定値の保持 — 2026-08-20 の実測値・実測日ともに全件残っている
      ADR-measurement-canon-in-code-doc の裁定（寄せ先が無ければコードの doc が正本／
      数値を落として害の説明だけ残す案は却下）に照らして `git show 179a085d:<path>` と突き合わせた。

      scripts/governance/checks/G-module-index.mjs
        BEFORE :28-29  「（2026-08-20 実測: `exts` を狭めて 30 件を落としても 3 つとも緑）」
        AFTER  :31-32  「（2026-08-20 実測: `exts` を狭めて 30 件を落としても、
                          錨・部分集合テスト・#701 のカナリアの 3 つとも緑だった）」
        → 実測日・「30 件」・「3 つとも緑」すべて保持。むしろ 3 つの名前を明示して情報が増えている。
        併存する 2026-08-09 実測（#1008）も無傷。✅

      scripts/governance/checks/G-rules-script-coverage.mjs
        BEFORE の A/B（SCRIPT_EXT → 錨なし exit 0 で沈黙 / 錨あり exit 1・finding 2 件とも錨。
        WALK_EXCLUDE_PATHS → 錨なしでも赤い）が exit code と件数まで逐語で保持され、
        さらに「この測定は錨の層が現存した最後の日のものである」という再現不能性の但し書きが
        足されている。2026-08-19 実測（「106 件 → 0 件」）も無傷。✅
        → ADR-measurement-canon-in-code-doc の要求（撤去は出所が永久に失われる瞬間である）を
          正しく満たしている。失われた値は 0。

  (d) ADR が宣言した死角「`.ps1` / `.psm1` / `.rs` は対象に置いた正準形が 0 件」— 実測で真
      $ grep -rnE '`[^`]*\.(ps1|psm1|rs)`[[:space:]]*(§[^「]*)?「' \
          --include=*.md --include=*.mjs --include=*.rs --include=*.ps1 --include=*.psm1 .
      → 0 件（node_modules / .claude/worktrees / workspace/ を除く）。✅

  (e) crateSourceFiles の doc が言う「守り手は #1152 で撤去された」— 一次証拠で確認
      $ git show --diff-filter=D --name-only 74ae45fc | grep domains
        scripts/governance/domains.mjs
        scripts/governance/domains.test.mjs
      → 「moduleIndexSources は本関数の結果の部分集合」を縛っていたテストは実在し、
        74ae45fc（#1152）で削除されている。「畳んではならないという制約だけが残り、
        畳まれていないことの検知は無い」は正しい。✅

  (f) ADR-governance-meta-demotion の追記が主張する件数 — 実測で真
      scripts/governance-check.mjs の metaFindings へ積む箇所はちょうど 2 つ:
        :175  metaFindings.push(...checkNormativeAreaInstrument(snapshot));   ← 面積計器の入力ガード
        :204  evidenceView({...}, metaFindings)                               ← evidence の供給断検知
      「ゲートへ戻す」の実体として引用された
        :209  if (metaAuditEnabled()) findings.push(...metaFindings);
      も逐語で一致。META_CHECK_IDS は scripts/ に 1 件も残っていない
      （残るのは docs/adr/ADR-governance-meta-demotion.md の本文 3 か所で、追記はそれを
        「本文を書き換えず読み替えを与える」形で扱っており凍結規約と整合）。✅

  (g) isRefTargetSpelling の doc が言う「4 者が同じ述語を読む」— 実測で真（ただし数え上げ）
      非テストの消費者は正確に 4 つ:
        scripts/governance/checks/G-heading-refs.mjs:49
        scripts/governance/checks/G-near-heading-refs.mjs:68
        scripts/governance/checks/G-folded-heading-refs.mjs:76,83
        scripts/governance/dependents.mjs:43
      今日は真。ただしこれも「4 者」という数え上げなので、5 人目が来れば黙って偽になる。
      → ⚠️ W-2 として再掲。

  (h) 申し送られた「意図的に分けた構造」— 4 件とも畳まれていない（誤検出 0）
      - resolveRefTarget:440 の分岐 と isRefTargetSpelling の述語 → 別のまま。✅
        （実際 `.mjs` を足しても前者は 1 文字も変わっていない — 「片方だけが変わる将来」が
          この差分で現実に起きたことになる）
      - crateSourceFiles と moduleIndexSources → 別のまま。✅
        MODULE_INDEX_CRATES の doc が正本であることも維持され、「2 本目の導出は意図である」
        の文言が残っている
      - skillTreeDocs と skillFiles → 別のまま。✅ 畳んではならない理由（差は
        .claude/skills/health-check/references/mechanized-checks.md の 1 件）も残っている
      - headingRefDocs / headingRefSourceDocs / headingRefCommentDocs → 3 本のまま。✅
        runAll の 0 件検知が母集団ごとに 1 本ずつ要る、という理由も 3 か所とも残っている
      注記: この 4 件は呼び出し側から根拠の所在つきで先に渡された。#872 / #985 の先例どおり、
      渡された項目の誤検出 0 は「申し送りが効いた証拠」ではなく定義から出る数である
      （memory の excluded-findings-are-not-zero-findings）。ここに書いたのは
      「畳まれていないことを実際に確認した」という肯定的な観測であって、判別力の主張ではない。

■ ⚠️（確信の持てない所見 — 何を測れば決着するかつき）

  [W-1] scripts/governance/lib.mjs:416 の
        「他の腕と違い引用符の中だけを見るので、`split("\n")` のような行中の `it(` には
        当たらない（行頭アンカー）」
        — 結論（当たらない）は正しいが、理由づけが弱い。
        当たらない本当の理由は `^\s*` の行頭錨であって「引用符の中だけを見る」ことではない
        （太字リードの腕も `**…**` の中だけをラベルにしており、「他の腕と違い」は成り立たない）。
        括弧内に「行頭アンカー」と正しい理由も併記されているので実害はない。
        決着: `/^\s*(?:describe|it)\(/` から `^` を外して governance:check を回せば、
        行中一致が何件増えるかで「行頭錨が効かせている分」が測れる。
        今回は変異 2 件の予算内に収めるため測っていない。

  [W-2] isRefTargetSpelling の doc の「4 者が同じ述語を読む」（lib.mjs:181 付近）
        — 今日は真（上記 (g) で実測）だが、.claude/rules/governance-docs.md の
        「機構の実装の詳細（述語の種類・件数・分岐の列挙）を散文へ写さない」に当たる形である。
        M-1 と同じクラスだが、こちらは #1155 が書き足した文（旧版は「対象は `<path>.md` か
        `/skill-name`」だけだった）。M-1 と別立てにしたのは、こちらが「消費者の数」で、
        綴りより増えにくいため。
        決着: 5 人目の消費者を足す PR が来たときに、この行が同じ差分で直るかを見る。
        今すぐ決着させるなら、数を消して「消費者は 1 つではない」の下限主張へ倒す
        （同じファイルの HEADING_REF の doc が既にその書き方を採っている — :176）。

  [W-3] `it('…')`（シングルクォート）は腕に当たらないので、単一引用符で書かれたテスト名は
        着地先にならない。今日のリポジトリは全件ダブルクォート（prettier 既定）なので露出 0。
        向きは fail-loud（着地しない＝赤）なので沈黙しない。宣言する死角として doc に
        1 行あってもよいが、無いことを欠陥とまでは言えない。
        決着: *.test.mjs に `it('…')` を 1 件書いて、それを対象にした正準形が赤くなるかを見る。

  [W-4] describe と it が同じ `depth: () => 7` を持つため、sectionsOf では describe の中の it が
        入れ子にならず、兄弟として扱われる。
        `.mjs` を指す参照は今日 2 件しかなく、どちらも it のラベルを指しているので実害 0。
        ただし describe のラベルを指す参照が現れると、その節は「次の it の手前」で切れ、
        describe ブロック全体を覆わない。#1140 が ATX 見出しについて直した欠陥
        （「`## 節` が最初の箇条書きの手前で終わる」）と同型である。
        決着: describe を指す正準形を 1 件書き、その describe の中ほどの行を編集して
        依存 reminder が鳴るかを見る（鳴らなければ同型の欠陥が `.mjs` 側に再現している）。

  [W-5] AGENTS.md に規範の行を 1 行足している（条件別チェック表の「機構・層・ファイル群を
        撤去する」）。ルート CLAUDE.md「最重要ルール」2 は
        「セーフティネットの変更は合意してから — 母集団は … 規範文書（ルート CLAUDE.md /
        AGENTS.md）を含む。Claude が単独で判断しない」と定めている。
        issue #1155 がこの追加を明示的に含んでいれば合意済みだが、私は issue 本文を見ていないので
        判定できない。呼び出し側で確認されたい。
        決着: `gh issue view 1155` に当該規範追加が含まれるか。

--------------------------------------------------------------------------------
3. 変異注入（フォールトインジェクション）
--------------------------------------------------------------------------------
注入は隔離 worktree（= 複製）に対してのみ行った。C:/workspace/Snotra の稼働中のガードは
一切触っていない（.claude/rules/safety-nets.md「稼働中のガードを弱めない——複製に変異を当てる」）。

ベースライン（変異前の内容ハッシュ）:
  $ git rev-parse 702fa046:scripts/governance/lib.mjs
    0e4c24cc5aea047da76931e4e27f80da90d67ca7
  $ git hash-object scripts/governance/lib.mjs
    0e4c24cc5aea047da76931e4e27f80da90d67ca7   （一致 — 作業ツリーは 702fa046 の内容）

────────────────────────────────────────────────────────────────────────────────
変異 A: ANCHOR_SPECS からテスト名の腕を削除する
────────────────────────────────────────────────────────────────────────────────
  変異内容: scripts/governance/lib.mjs:422 の
            `{ re: /^\s*(?:describe|it)\(\s*"([^"]*)"/, depth: () => 7, label: (m) => m[1] },`
            の 1 行を削除（他は無改変）。

  結果: 赤くなった。exit 1。✅ 予測どおり。

  $ npm run governance:check
  governance:check — 2 件の不整合:
    scripts/governance/checks/G-rules-script-coverage.mjs:25  見出し参照が着地しない:
      `G-rules-script-coverage.test.mjs`「母集団の下界」
      （scripts/governance/checks/G-rules-script-coverage.test.mjs に該当する見出し・リード文が無い）
    scripts/governance-check.test.mjs:118  見出し参照が着地しない:
      `governance/evidence.test.mjs`「配線:」
      （scripts/governance/evidence.test.mjs に該当する見出し・リード文が無い）
  exit code: 1

  解釈: 腕は現に 2 件の実在する参照を支えている。何も守っていないわけではない。
  この 2 件は #1155 の ADR 追記が「拡張前の写しで独立導出が実測・2 件」と書いたものと
  逐語で一致する（独立に再現できた）。腕を持たずに対象綴りだけ広げれば、この 2 件が
  恒久的に赤くなるという ADR の主張は正しい。

  巻き戻し:
    $ git checkout -- scripts/governance/lib.mjs
    $ git hash-object scripts/governance/lib.mjs
      0e4c24cc5aea047da76931e4e27f80da90d67ca7   ← ベースラインと一致 ✅
    （mtime ではなく内容ハッシュで照合した）

────────────────────────────────────────────────────────────────────────────────
変異 B: isRefTargetSpelling から `.mjs` を外す
────────────────────────────────────────────────────────────────────────────────
  変異内容: scripts/governance/lib.mjs:194-195
      -  target.endsWith(".md") || target.endsWith(".mjs") || /^\/[a-z0-9-]+$/.test(target);
      +  target.endsWith(".md") || /^\/[a-z0-9-]+$/.test(target);

  結果: 緑のまま。exit 0。✅ 予測どおり。

  $ npm run governance:check
  governance:check — 全検査 passed（… 見出し参照 322 件を md 48 件 + .rs 101 件 +
  スクリプトのコメント 111 件から照合 … 近傍の見出し参照 17 件 / 折れうる位置 21 件 …）
  exit code: 0

  照合件数の変化（実測・要求どおり確認）:
      見出し参照        324 → 322  （-2）✅ 要求された数値と一致
      近傍の見出し参照   19 → 17   （-2）← 呼び出し側が指定していない列も動いた（実測を報告）
      折れうる位置       24 → 21   （-3）← 同上
      それ以外の列（検査 21 件・対象文書 36 件・rules 8 件・skills 12 件・面積 16948/12995 字・
      member 4 件・clippy 8 件・散文の識別子 396 件・ADR 69 本・ADR 短縮引用 363 件）は不変。

  解釈: これが #1155 の存在理由の再現である。対象綴りから `.mjs` を外すと照合そのものが
  生成されず（checked にも数えられず）、撤去済みファイルを指す参照が緑のまま推移する。
  「照合していない」と「差分ゼロ」が区別できない形が、324 → 322 という減った数字にしか
  現れないことが、この変異で目に見える形になっている。
  併せて動いた 17 / 21 は、同じ述語を G-near-heading-refs と G-folded-heading-refs も
  読んでいるためで、上記 (g) の「4 者が同じ述語を読む」の実地の裏付けにもなっている。

  追加の観測（別の層は赤くなるか）:
  $ npm test
    Test Files  1 failed | 35 passed (36)
    Tests       1 failed | 851 passed (852)
    FAIL scripts/governance/checks/G-folded-heading-refs.test.mjs
      > 形 A — 対象綴りで行が終わり、ラベルが次行から始まる
      > `.mjs` を対象にした参照の折れも見る（#1155 で対象綴りに入った）
    exit code: 1
  → #1155 が「意図を反転させた不変条件」として置いたテストが、変異を検知する側として
    実際に機能している。vitest 本体は正常起動しており（851 件が通っている）、
    これはモジュール解決の失敗ではなく検知である。
    すなわち `.mjs` の対象綴りは governance:check（沈黙）と npm test（赤）の 2 層で
    扱いが分かれ、縛っているのは npm test 側である。

  巻き戻し:
    $ git checkout -- scripts/governance/lib.mjs
    $ git hash-object scripts/governance/lib.mjs
      0e4c24cc5aea047da76931e4e27f80da90d67ca7   ← ベースラインと一致 ✅
    $ git status --porcelain
      （無出力 — 作業ツリー clean）

  注入の強さについて: 変異 A・B とも「実際に起きうる回帰の姿」と同型である。
  A は「腕を足し忘れる／後の PR が消す」、B は「対象綴りを狭める（= #1155 以前の状態）」であり、
  どちらも本来の回帰より強くない（.claude/rules/safety-nets.md「注入したことと、
  注入が正しい強さであることは別である」）。

--------------------------------------------------------------------------------
4. まとめ
--------------------------------------------------------------------------------
- 検証: カテゴリ F（npm run governance:check）exit 0、npm test exit 0。
  カテゴリ A〜E は該当なし（`.rs` 0 ファイルを diffstat で自分で確認済み）。
- 機構（01c6eede）は効いている。変異 A で 2 件の実在参照が赤に落ち、
  変異 B で照合件数が 324 → 322 へ戻り、npm test の新テストが検知した。
- 測定値の保持は完全（失われた値 0）。ADR の裁定に反する書き換えは無い。
- 意図的に分けた構造 4 件はすべて維持（畳まれていない）。
- Critical 0 / High 2 / Medium 2 / ⚠️ 5。High 2 件はどちらも
  #1155 が塞ごうとした形と同じクラスの残余である:
    H-1 は「実装より強い主張が沈黙で推移する」形を新しい doc の中に作ってしまったもの、
    H-2 は「幽霊識別子ゼロ」という自分の受け入れ条件を、機構が生きた層と判定している
    文書 1 枚について満たしていないもの。
  どちらも一行のコマンドで決着する（本文に記載）。
================================================================================
