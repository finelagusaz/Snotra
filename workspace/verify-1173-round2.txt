# #1173 検証・レビュー報告 ラウンド 2（アンカー 82c3a0d40f343677f1062ba5c1789dbfd5a6171d）

実施: 2026-08-26 / worktree C:/workspace/Snotra/.claude/worktrees/agent-a3cb7149d125e8bdd
実装の修正は 47673db31fe95110ca0930209cb60d19fa875622、その上の 82c3a0d4 はラウンド 1 の報告を回収しただけ。

HEAD 整合: `git rev-parse HEAD` は当初 fa9d597e。`workspace/verify-1173.txt`（ラウンド 1 の未追跡コピー）が
checkout を阻んだため、**削除前に 3 つのハッシュが一致することを確認してから**外した——
82c3a0d4 のコミット版 / worktree コピー / 元リポジトリのコピーがいずれも
`db4f6bf6c9341d6b56242d5a32cb3d3688b4285a10049b4ea4e65e2acd4066eb` で同一。
（`git rm` の local modifications 停止は保護であるという教訓の適用。成果を落としていない。）
その後 `git checkout --detach 82c3a0d4` で一致。作業ツリーは清浄。

## 1. カテゴリ A〜F（全件宣言・新アンカーで再実行）

A. Rust（`*.rs`）— **実行**
  cargo fmt --all -- --check                                    exit 0
  cargo check --workspace                                       exit 0
  cargo clippy --workspace --all-targets -- -D warnings         exit 0
  cargo test -p snotra -q                                       exit 0（303 passed / 0 failed / 4 ignored）
  cargo doc --workspace --no-deps --document-private-items      exit 0（警告 0・新しい 4 列表と ⚠️ 絵文字も無害）

B. TypeScript — **該当なし**（`.ts` の変更 0 件）
C. ウィンドウ生成／表示順・ホットキー — **該当なし**（rustdoc と SPEC 散文のみ。挙動 0 行）
D. UI のスタイル・レイアウト・テキスト表示 — **該当なし**（描画を 1 ピクセルも変えない）
E. git hook — **該当なし**（`.githooks/**` の変更 0 件）

F. ガバナンス文書 — **実行**
  node scripts/governance-check.mjs（= npm run governance:check）  exit 0
  「全検査 passed（… コードスパン 18211 件 …）」※ ラウンド 1 は 18209 件。新規スパン 2 件ぶん増えている

実行不能だったカテゴリ: なし。

## 2. 前巡の指摘は解消したか（指摘を見つけた道具＝生ログとの逐一突き合わせで自分で測った）

### ✅ H1 解消 — 表の量化が実測と一致するようになった

新しい表（クランプ有効の行）を `measurement-1173-treatment.txt` と 1 セルずつ突き合わせた:

| 主張 | 実測 | 判定 |
|---|---|---|
| 移動中に落ち着く値 = **1020**（作業領域の下端ちょうど） | 全 5 反復の k=120〜200 が 1020、確定前も 5/5 で 1020 | ✅ |
| 一時的な逸脱 = **5 反復中 4 回** | 反復 1 のみ 越えた=False、反復 2〜5 が 越えた=True | ✅ |
| 逸脱の大きさ = **1132〜1133** | 反復 2:1132 / 3:1132 / 4:**1133** / 5:1132 → 範囲は 1132〜1133 | ✅ |
| 外に留まれるか = 留まれない（`Enter` の前に戻る） | 確定前 5/5 で 1020 | ✅ |

本文へ足した「**『移動中は外へ出られない』ではない**…外に出た状態が数十回の打鍵にわたって観測される」も
実測どおり——反復 2 は k=40 が 1020、k=60/80/100 が 1132、k=120 で 1020 へ復帰する。
標本粒度が 20 打鍵なので、外に出ていた区間は **40〜80 打鍵**（下限 k=60→100、上限 k=41→119）と挟める。
rustdoc の「数十回の打鍵」はこの範囲に収まる安全な書き方である。

### ✅ M1 解消 — SPEC.md の同じ量化が消えた

削除: 「単一モニターでは『外へ出せない』に留まるが」
追加: 「移動中に作業領域の外へ出ても内側へ引き戻される」
実測（一時的に 112〜113px 外へ出る）と一致する。「移動先が…確定できない」は据え置きで、これは 5/5 で正しい。
「復帰が毎フレーム働き」→「復帰が働き続け」への弱化も適切。

### ✅ M2 解消 — 条件つきの実測値が rustdoc へ着地した

書かれた条件をすべて生ログで検算した。**7 項目すべて一致**:
  2026-08-26 → treatment 22:29:06 / control 22:35:13 ✅
  release → 「バイナリ: target/release/snotra.exe（release）」 ✅
  単一モニター 1920x1080 → rcMonitor: 0,0,1920,1080 ✅
  125% → 窓 DPI: 120（120/96 = 1.25） ✅
  作業領域の下端 1020 物理 px → rcWork: 0,0,1920,1020 ✅
  5 反復 → 反復 1〜5 ✅
  `↓` 200 回 → 系列が k=20〜200 の 10 標本 ✅
これで `workspace/` の生ログが撤去条件どおり消えても、doc 単独で検算に足る条件が残る。

### ✅ L1 解消 — SPEC.md §8.2 の「窓」が 0 件になった

`sed -n '460,486p' SPEC.md | grep -c 窓` → **0**。隣接行の「ウィンドウ」に揃った。
rustdoc 側の「窓」は据え置きだが、これは当該ファイルの既存表記と一致しており正しい
（`docs/comment-guidelines.md`「言語（日英）」が「窓」を定着語と認め、守るべきは同一ファイル内の不混在）。

### ✅ W2 解消 — 「毎フレーム発火する」が適切に弱まった

「回ったフレームでは必ず発火する（**ループ中に何枚回るかは測っていない**）」。
`view.rs:1279` の `if !ui.input(|i| i.pointer.any_down())` から見て正確。
**ただし同じ段落に新しい機序の断定が入ったため、段落全体としては弱まっていない**（→ R2-1）。

### ✅ W1 記録 — ⚠️ として rustdoc に着地した（射程の書き方も正しい）

「⚠️ 対照側は確定後に 1134 → 1106 と 28 px 上がる（5/5）。**理由は特定していない**——クランプ以外にも
確定後の位置を動かす経路があることを意味するが、外側で落ち着く事実（1106 > 1020）は変わらないので
上の分離は保たれる。」——結論の保全のしかたは妥当。ただし数値に 1 件のずれがある（→ R2-2）。

## 3. 修正差分が持ち込んだ新しい所見

### [Medium] R2-1 — 未測定の機序の断定が新しく入り、同じ段落の但し書きと張り合う

追加された本文: 「引き戻しは**モーダルループの `SetWindowPos` との競り合いであり**」

- **測っていない。** この調査は窓矩形を 20 打鍵ごとに刻んだだけで、モーダルループ側の位置更新と
  クランプ側の `set_position` の**順序も頻度も観測していない**。計器が無い。
- **4 行上の但し書きと張り合う。** 同じ段落が「ループ中に何枚回るかは測っていない」と認めている。
  枚数が未知のまま「競り合い」（両者が同程度の頻度で撃ち合う像）を断定することはできない。
- **系列だけでは 2 つの説明を分離できない。** 反復 2 の逸脱は 40〜80 打鍵（上記の挟み込み）続く。
  `src-tauri/CLAUDE.md`「イベント駆動 wake の不変条件（#532 SU5）」は「runtime はイベント駆動
  （`RedrawRequested` 待ち）で**通常フレームは勝手に回らない**」と定める。**(a) 競り合い**でも
  **(b) フレームが疎であることによる単なる遅延**でも同じ系列が出るので、この観測はどちらも支持しない。
  なおログに時刻は無く、実時間は測っていない（治具は 20 打鍵ごとに 60ms スリープするが、それは実測値ではない）。
- これは #1056 型である——**所見（逸脱がある）は正しいのに、添えた機序が独立に誤りうる**。
  H1 で消したのは「未測定の量化」だったが、それが「未測定の機序」として再生している。

**この所見を偽にする手順**: モーダル移動ループ中の trace で、逸脱区間のフレーム数と
`set_position` の発火回数を数える。逸脱している間にフレームが多数回っていた（＝発火しているのに
押し戻せていない）ことが示されれば (a) が支持され、本所見は偽。

### [Low] R2-2 — 対照側の値が反復 1 だけ実測とずれる（量化の潰れが小規模に再発）

doc:「クランプ無効 | 落ち着く値 **1134**」「⚠️ 確定後に **1134 → 1106 と 28 px** 上がる（**5/5**）」

`measurement-1173-control.txt` の実測:
  反復 1: 確定前 B=**1133** → 確定後 B=1106（＝**27 px**）、移動中の落ち着き先も 1133
  反復 2〜5: 確定前 B=1134 → 確定後 B=1106（＝28 px）

つまり「1134 / 28 px」が当たるのは **4/5** で、反復 1 は 1133 / 27 px。「上がる」こと自体は 5/5 で真だが、
具体値と (5/5) を並置すると読者は全体が 5/5 だと読む。
ラウンド 1 の報告は「27〜28px」「1133/1134 → 1106」と**幅で**書いており、修正でその幅が単一値へ潰れた。
**H1 と同型（幅のある観測を単一値で書く）であり、規模だけが小さい。**

**偽にする手順**: `grep -n "確定前\|確定後" workspace/measurement-1173-control.txt` の 27〜28 行
（反復 1）を読む。B=1134 → 1106 なら本所見は偽。

### [Low] R2-3 — 対照アームの再現情報が今も doc に無い（前巡 W1 の副次所見が未対処）

再測手順は今も「**クランプ行を落とした対照が要る**——単独の系列では頭打ちがクランプかカーソル限界かを
分離できない」とだけ書き、**どの行をどう落とすかを名指ししていない**（`sed -n '796,802p'` で確認）。

実際の対照は `view.rs:1279` の `if !ui.input(|i| i.pointer.any_down()) {` を
`if false && !ui.input(|i| i.pointer.any_down()) {` にした 1 行パッチ——**この情報は会話にしか無く、
リポジトリのどこにも無い**（`git grep` で裏が取れない）。`measure-1173.ps1` の `.NOTES` により
`workspace/` の生ログは次サイクルで消えるので、そのとき対照アームは doc 単独では再現できない。
M2 で treatment 側の条件を着地させた対処と**同じ扱いを対照側にも与えるべき**である。

**偽にする手順**: `grep -n "any_down" src-tauri/src/egui_shell/window_coordinator.rs` で再測手順の段落に
当該行の名指しがあれば偽。→ 実測: 794 行付近の代替判定の話にしか現れず、再測手順には無い。

## 4. 検知器の死角は新しい行にも当てはまるか（1 行で確認・変異の再注入はしていない）

前巡で測った 2 つの死角を、今回足した行に当てた。

- **`.rs` の rustdoc は識別子照合の射程外** → **当てはまる。**
  今回の追加行が持つコードスパンは `` `Enter` `` / `` `SetWindowPos` `` / `` `WorkArea::clamp` `` の 3 つ
  （`git show 47673db3 -- …window_coordinator.rs | grep "^+" | grep -o '`[A-Za-z_][A-Za-z0-9_:]*`'`）。
  前巡の実測（同じ変異が `.rs` では緑・`.md` では赤）どおり、いずれも実在を検算されない。
  とくに **`SetWindowPos` は今回新規に持ち込まれた識別子**で、誰も検算していない。
- **序数引用「却下 N」は照合されない** → **当てはまるが、今回新しくは足していない。**
  `grep -n "却下 [0-9]"` → 724 行（既存・`ADR-show-path-derives-drawn-height` 却下 2）と
  794 行（前巡の差分・`ADR-main-window-clamp-on-pointer-release` 却下 1）の 2 件。今回の差分は増やしていない。

## 5. 確信の持てない所見（⚠️）

### ⚠️ W1（更新）— 渡された対照パッチの情報で**半分だけ**検算できた

いただいた情報（`if false && !ui.input(…)` の 1 行パッチ）で、**「対照パッチが不完全でクランプが残っていた」
可能性は排除できる**——`false &&` は短絡評価で `clamp_main_into_work_area` の呼び出しを無条件に殺すため。
ゆえに 28 px を動かしているのは**クランプ以外の経路**であり、rustdoc の ⚠️ の書き方
（「クランプ以外にも確定後の位置を動かす経路がある」）は**正しい**。

**しかし経路の同定は依然できていない。** 1106 は作業領域の下端（1020）でもモニタ下端（1080）でもなく、
窓高 64 は不変の純平行移動である。静的に読んだ限りでは `clamp_main_into_work_area` が唯一の
`main.set_position` 経路に見えるので、残る候補は tao/OS 側かクランプ以外の位置適用になるが、**特定できていない**。
doc が「理由は特定していない」と書いていることは**正直で適切**。

**なお、この検算は会話で渡された証言に依存しており、リポジトリの artifact では裏が取れない**（→ R2-3）。
**偽にする手順**: 対照パッチを再現して `cargo build --release -p snotra` し、`Enter` 前後で
`set_position` を trace して発火の有無を見る。

### ⚠️ W2（更新）— 弱化は正しいが、段落全体の強さは下がっていない

「回ったフレームでは必ず発火する（ループ中に何枚回るかは測っていない）」は分岐条件どおりで正しい。
ただし R2-1 のとおり、同じ段落に**より強い未測定の断定**（競り合い）が新しく入ったため、
読者が受け取る確信度は前巡より上がっている可能性がある。**弱化と強化が同じ差分に同居している。**
**偽にする手順**: R2-1 と同じ計器。

### ⚠️ W4（新規・確信は薄い）— 「クランプ無効 | 一時的な逸脱 | 無し」の読まれ方

control 反復 1 は 654 → 753 → 823 → 936 → 1048 → 1119 → 1133 と**単調に接近**し、k=140 でようやく
頭打ちになる（反復 2〜5 は最初から 1134 で一定）。「逸脱 無し」は「落ち着く値から一時的に外れることが無い」
という意味では正しいが、**「クランプ無効なら即座に最下点へ着く」と読まれると誤る**——反復 1 は 200 打鍵の
7 割を使って到達している。害は小さく、表の他の列と読み合わせれば誤解しにくい。
**偽にする手順**: control 反復 1 の系列を読む。k=20 の時点で 1133 に達していれば本所見は偽。

### ⚠️ W3（据え置き）— 多モニターの射程宣言は今も適切

rustdoc「多モニターでの封鎖そのものは未測定である——測ったのは機序であり、封鎖は上の作業例からの
演繹にとどまる」、SPEC「隣のモニターへ渡れないと**見込まれる**」。SPEC 側から「単一モニターでは…」の
断定が消えたことで、**両者の射程がむしろ揃った**。測定環境は単一モニターなのでこれ以上強く書けない。
**所見ではなく、反証を探して見つからなかったことの記録である。**

## 6. Critical / High の扱い

**Critical / High は 0 件**（前巡の H1 は解消を実測で確認）。修正は一切当てていない（報告のみ）。
作業ツリーはアンカーのまま清浄。呼び出し側が判断すべきは **R2-1（Medium）**、次いで R2-2 / R2-3（Low）。
R2-1 は「文を消して所見だけ残す」（機序を書かない）か「計器で測る」かの二択であり、
**doc を直すより先に、どちらの説明が正しいかを決める必要がある**点に注意。
