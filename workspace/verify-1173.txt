# #1173 検証・レビュー報告（アンカー fa9d597e651721a79e7755a2594e7a01ed1658b9）

実施: 2026-08-26 / worktree C:/workspace/Snotra/.claude/worktrees/agent-a3cb7149d125e8bdd
HEAD 整合: `git rev-parse HEAD` は当初 73703a36 → `git checkout --detach fa9d597e` でアンカーへ合わせた（実測）
注記: Write ツールが worktree 外（C:/workspace/Snotra/workspace/）への直接書き込みを拒否したため、
worktree 内へ書いてから元リポジトリへ複製している。

## 0. 差分の実体（git show fa9d597e --stat）

    SPEC.md                                        |  1 +
    src-tauri/src/egui_shell/window_coordinator.rs | 25 +++++++++++++++++++++++++
    workspace/plan.md                              |  8 ++++----

製品コードのロジック変更は 0 行（rustdoc と SPEC 散文と plan.md のチェックのみ）。ブリーフの記述と一致する。

## 1. カテゴリ A〜F（全件宣言・ベースライン＝変異なし）

A. Rust（`*.rs`）— **実行**
  cargo fmt --all -- --check                                    exit 0
  cargo check --workspace                                       exit 0
  cargo clippy --workspace --all-targets -- -D warnings         exit 0
  cargo test -p snotra -q                                       exit 0（303 passed / 0 failed / 4 ignored）
  cargo doc --workspace --no-deps --document-private-items      exit 0（intra-doc link 切れなし）
  ※ snotra-core / snotra-egui-runtime / snotra-settings は差分に含まれないため -p テストは非必須
    （カテゴリ A の但し書きどおり、CI の rust-check が workspace 全 crate を担保する）

B. TypeScript — **該当なし**（`.ts` の変更 0 件）

C. ウィンドウ生成／表示順・ホットキー・スラッシュコマンド — **該当なし**
  window_coordinator.rs を触っているが変更は rustdoc のみで、生成・表示順・hotkey の挙動は 1 行も
  変わらない。カテゴリ D の但し書き（#1106・「表示経路を触った」を理由に該当と誤認するな／過大な
  申し送りは本当に要る項目を埋もれさせる）と同じ判定を C にも当てた。

D. UI のスタイル・レイアウト・テキスト表示 — **該当なし**（描画を 1 ピクセルも変えない）

E. git hook（`.githooks/**`）— **該当なし**（変更 0 件）

F. ガバナンス文書 — **実行**
  node scripts/governance-check.mjs（= npm run governance:check の実体）  exit 0
  「全検査 passed（検査 23 件 / 対象文書 36 件 / … / ADR の短縮引用 429 件）」
  ※ worktree に node_modules が無いが governance-check.mjs は依存を要さず実行できた
    （ERR_MODULE_NOT_FOUND は出ていない。npm test / vitest は今回の該当カテゴリではない）

実行不能だったカテゴリ: なし。

## 2. レビュー所見

### [High] H1 — rustdoc の表の 1 列目が treatment の生ログと食い違う

対象: window_coordinator.rs の新規表
    | クランプ有効 | 作業領域の下端**ちょうど** | 留まれない（`Enter` の**前に**引き戻される） |

`workspace/measurement-1173-treatment.txt` の生の観測:
    反復 1: 移動中の最大 bottom: 1020（rcWork.bottom=1020・越えた=False）
    反復 2: 移動中の最大 bottom: 1132（越えた=True）
    反復 3: 移動中の最大 bottom: 1132（越えた=True）
    反復 4: 移動中の最大 bottom: 1133（越えた=True）
    反復 5: 移動中の最大 bottom: 1132（越えた=True）

5 反復中 4 反復で、クランプ有効のまま bottom が作業領域の下端を **112〜113px 越えている**。
しかもログ自身の判定行が「越えた=True」と書いている。系列を見ると一時的な逸脱である——
反復 2 は k=20/40 が 1020、k=60/80/100 が 1132、k=120 以降 1020 へ復帰する。
つまり「移動中に到達する bottom」は**下端ちょうどではない**。下端ちょうどなのは**定常値**である。

**害**: rustdoc は同じ段落で再測手順を配っている。手順どおり測り直した読者は「越えた=True」を
受け取り、doc と矛盾する／クランプが退行した、と読む。実際には仕様どおりである。

**支持されている主張（撤回すべきでない部分）**:
- 「留まれない（`Enter` の前に引き戻される）」— 確定前は 5/5 反復で B=1020。正しい。
- 「移動先が作業領域の外になる位置には確定できない」— 確定前・確定後とも 5/5 で 1020。正しい。
- 実験の結論そのもの（止めているのはカーソル限界ではなくクランプである）— treatment の定常値
  1020 に対し control は 1133/1134 で頭打ちなので、**対照の差としては成立している**。
  誤っているのは「到達する bottom」という量化であって、実験の設計でも結論でもない。

**この所見を偽にする手順**: `workspace/measurement-1173-treatment.txt` の反復 2〜5 の
「移動中の最大 bottom:」行を読む。すべて 1020 以下・越えた=False であれば本所見は偽。

### [Medium] M1 — SPEC.md の同じ量化が、同じ理由で実測より強い

SPEC.md:478 の「移動中もバー矩形は作業領域の内側へ引き戻される」「単一モニターでは
『外へ出せない』に留まる」は、H1 と同じ観測（一時的に 112px 外へ出る）と食い違う。
実測が支持するのは「一時的には出るが、確定できない」である。
「移動先が作業領域の外になる位置には確定できない」の方は正しいので、前半だけの問題。

**偽にする手順**: H1 と同じ。

### [Medium] M2 — 実測値が rustdoc に 1 つも無く、足場の撤去で出所が消える

rustdoc の新規 25 行に**数値は 1 つも現れない**（1020 も 1133/1134 も反復数 5 も無い。
`git show fa9d597e:src-tauri/src/egui_shell/window_coordinator.rs` の当該範囲を grep して確認）。
あるのは日付（実測 2026-08-26）と定性的な語（「下端ちょうど」「カーソルが画面下端で止まる位置」）だけ。

`docs/comment-guidelines.md`「歴史メモの様式」は「**実測値には条件を添える**（日付・エントリ数・
試行回数など）。条件のない数値は再検証できない」と定める。ここは数値そのものが無いので、
「下端ちょうど」を後から検算する術が doc 側に無い。

かつ `workspace/measure-1173.ps1` の `.NOTES` が撤去条件を持つ:
  「#1173 を閉じる PR が main へマージされた後、次に `workspace/` を書き換えるサイクルで削除する。
   撤去対象は…`measurement-1173-treatment.txt` / `measurement-1173-control.txt`…」
撤去後、1020 / 1133 / 1134 という一次証拠はリポジトリから消える。`AGENTS.md` の
「調査・測定のための一時的な足場」行の**撤去時**条項（撤去は出所が永久に失われる瞬間であり、
着地し損ねた値は写し同士が支え合うだけになる・#1032）に正面から当たる。

なお `docs/adr/ADR-measurement-canon-in-code-doc.md` の方針（寄せ先の無い値はコードの doc が正本）
自体は正しく適用されている——**正本に指名した面に値が無い**ことが問題である。

**呼び出し側ブリーフの前提が事実と異なる**: 「測定値（1020 / 1134）は rustdoc にだけ書き、
SPEC には書いていません」と渡されたが、rustdoc にも書かれていない。

**偽にする手順**: `git show fa9d597e:src-tauri/src/egui_shell/window_coordinator.rs | sed -n '766,795p'`
に数字が現れれば偽。

### [Medium] M3 — 検知器の死角（変異注入で実測）

この差分を守るのは `governance:check` だという前提を測った。ベースラインは exit 0（緑）なので、
以下の赤／緑は**その変異だけに帰属する**（対照は同一ツリー・同一コマンド）。

| # | 変異 | 対象 | 結果 |
|---|---|---|---|
| M1 | `ADR-main-window-clamp-on-pointer-release` → 存在しない slug | 新規 rustdoc 784 行 | **exit 1・赤**。`…window_coordinator.rs:784 ADR の短縮引用が実在しない` と**行番号まで名指し** |
| M2 | `WorkArea::clamp` → `WorkArea::clampZZZ`（コードスパン） | 新規 rustdoc 779 行 | **exit 0・緑**（`governance:check`）。`cargo doc` も exit 0・警告 0 |
| M4 | 同じ識別子変異を `.md` 側へ | SPEC.md 478 行 | **exit 1・赤**。`散文に、現行語彙に無い識別子が残っている: WorkAreaZZZ::clampZZZ` |
| M5 | 「却下 1」→「却下 9」（ADR に 却下 9 は無い。実在は 1〜5） | 新規 rustdoc 784 行 | **exit 0・緑** |
| 到達性 | 存在しない ADR slug を SPEC の新規行へ注入 | SPEC.md 478 行 | **exit 1・赤**（新規 SPEC 行が母集団内であることの確認） |

**指示された変異のうち 1 件は実行不能だった（宣言）**:
`ADR-adr-frozen-history` の変異は **実行不能**。この slug は fa9d597e のどのファイルにも現れない——
出現は**コミットメッセージ本文のみ**である（`git show fa9d597e -- src-tauri/…/window_coordinator.rs
| grep -n "ADR-"` の結果、ファイル側の ADR 引用は `ADR-main-window-clamp-on-pointer-release` の
2 件〔既存 1・新規 1〕だけ）。呼び出し側ブリーフは rustdoc 内にあるものとして指示していたが、実体と食い違う。

副次所見: コミットメッセージは `governance:check` の走査母集団（リポジトリのファイル）の**外**にある
ため、この `ADR-adr-frozen-history` の引用は**どの検知器にも守られていない**。squash マージで main の
commit message になる面が grep の母集団から落ちるのは #1056 と同型の穴である。
（**偽にする手順**: `git grep -l "ADR-adr-frozen-history" fa9d597e` がファイルを 1 つでも返せば偽。）

読み取れること:
- **ADR の短縮引用（slug）だけは `.rs` の rustdoc でも守られる。** 新しい行に届いている。
- **識別子の実在照合は `.md`（34 文書）にしか掛からず、`.rs` の rustdoc は射程外である**
  （M2 緑 / M4 赤の非対称が証拠）。この差分の新規 rustdoc 25 行に現れる識別子
  （`WorkArea::clamp` / `GetGUIThreadInfo` / `GUI_INMOVESIZE` / `any_down()` / `Send-SnotraKey`
  / `GetWindowRect` / `SNOTRA_CONFIG_DIR`）は、**どの機構も検算していない**。
  plain backtick は intra-doc link ではないので `cargo doc` も原理的に見ない。
- **ADR 内の序数引用（「却下 N」）は照合されない。** `.claude/rules/governance-docs.md` は
  「**序数で他を指してはならない**…番号は構造を凍らせ、ずれても誰も気づかない」と定めており、
  新しい行はその形になっている。正準形 `` `<対象>`「<見出し>」 `` で書けば G-heading-refs が
  見る余地があった（先例: `docs/build-commands.md`:73 が `` `docs/adr/ADR-….md`「却下 6」 `` の形）。
  ただし現行ツリーには序数引用の散文形が多数あり（`docs/development-principles.md`:68、
  `docs/check-skill-skeleton-design.md` 等）、この差分だけの逸脱ではない。

**変異の巻き戻しは内容ハッシュで照合した**（mtime ではない）:
  window_coordinator.rs  c85cf782b6a997d26e014680ec8b68b290ff8e9821f8f1c559ed5dcfbafe1a17（変異前＝復元後で一致）
  SPEC.md                e6dcfd2758f271e5fe3189b48f00ec2428d97c727acda8d4743958e58e894c57（同上）
  復元後の `git status --short` は追跡ファイルの変更 0 件、`governance:check` は exit 0。

**偽にする手順**: 各変異を再注入して exit code を見る。M2 / M5 が赤になれば当該項は偽。

### [Low] L1 — SPEC.md §8.2 の中で「窓」がこの 1 行にだけ現れる

新規行は「キーボードによる**窓**移動」。§8.2 の範囲（SPEC.md 460〜486 行）で「窓」を使うのは
**この新規行だけ**であり（`sed -n '460,486p' SPEC.md | grep -n 窓` が 1 件のみ）、隣接する
476 / 477 / 479 / 480 / 481 / 485 行はすべて「ウィンドウ」を使う。

ただし断定は弱めておく: `docs/comment-guidelines.md`「言語（日英）」の射程条項が
「『窓』は `src-tauri/CLAUDE.md` や `snotra-egui-runtime/CLAUDE.md` が使うので**このリポジトリの語**
であり、カタカナへ寄せ直さない」「定着語について守るのは**同一ファイル内で語を混在させないこと**
だけ」と明記しており、SPEC.md は既に両方を使っている（「ウィンドウ」60 件・§4.5/§4.7/§8.5 等で
「窓」も多数）。ゆえに**訳語規則違反ではなく、§8.2 内の局所的な不統一**として報告する。
rustdoc 側の「窓」は当該ファイルの既存表記（「これから出す窓をどこへ置くか」等）と一致しており問題ない。

**偽にする手順**: `sed -n '460,486p' SPEC.md | grep -c 窓` が 2 以上（＝新規行以外にも既存の「窓」がある）
なら本所見は偽。

### [Info] I1 — issue 本文の誤りを差分が正しく訂正している（積極的所見）

issue #1173 本文は「tao の wndproc サブクラス化は同 ADR が**却下 2** で退けている」と書くが、
`docs/adr/ADR-main-window-clamp-on-pointer-release.md` の実際は
  却下 1 — `WM_MOVING` をフックして移動矩形そのものを拘束する（理由の 1 つが「wndproc は tao の所有でサブクラス化が要る」）
  却下 2 — 毎フレーム無条件にクランプする
であり、rustdoc の「却下 1」が正しい。issue の記述を写さずに ADR を読み直した形跡である。

また 却下 1 は理由を 2 つ持ち、rustdoc が退けているのは 1 つ目（サブクラス化）だけだが、
2 つ目（「得られるのは『ドラッグ中も出せない』だけ」）は**却下 1 の提案の価値**についての理由で
あり、今回の候補は目的が逆（拘束を**外す**ための「移動中である」判定）なので当たらない。
「当たらない**見込み**・**実呼び出しは未検証**」と二重に弱めてあり、射程宣言として適切。

### [Info] I2 — SPEC §8.2 の既存記述との矛盾は無い

477 行「ドラッグしている間は拘束しない」と 478 行「キーボード移動は拘束される」は、
入力経路が違うものの対比であって矛盾しない。478 行の「上と同じ理由で」は 477 行が述べる
封鎖の機序（移動元モニターとの重なりが優勢な区間で引き戻される）を指しており、正しく着地している。
476 行の一般則（ポインタ非押下フレームでは戻す）から見れば 478 行はその帰結であり、as-built として整合。

### [Info] I3 — 意図的に分けた構造は保たれている（DRY 違反として挙げない）

SPEC＝語彙層（利用者から見える挙動）／ rustdoc＝機序・対照・再測手順、の分担は
`AGENTS.md`「ドキュメント参照」「3層分担」どおり。ADR ではなく生きた層へ書いた判断も
`ADR-adr-frozen-history`（ADR 本文は決定日時点で凍結）と整合する。

## 3. 確信の持てない所見（⚠️）

### ⚠️ W1 — control 側の「確定後」が「確定前」より一貫して 27〜28px 上へ動く（機序不明）

`measurement-1173-control.txt` の 5 反復すべてで:
  反復 1: 確定前 T=1069 B=1133 → 確定後 T=1042 B=1106
  反復 2〜5: 確定前 T=1070 B=1134 → 確定後 T=1042 B=1106
クランプ行を落としたはずの対照でも、`Enter` の後に何かが窓を 28px 引き上げている。
落ち着く先は作業領域の下端（1020）でもモニタ下端（1080）でもない 1106 で、窓高（64）は不変。
treatment 側では確定前後とも 1020 で動きが無いため、**外に出ているときにだけ**起きる。

doc はこれに一言も触れていない。「クランプ行を落とした対照」が**クランプだけを落とした清潔な分離**
であることを弱めうる（別の位置調整経路が残っていた可能性）。ただし doc の主張
「クランプ無効 → 外に留まる」は 1106 > 1020 なので**偽にはならない**。

**偽にする手順**: 対照ビルドのローカルパッチ差分を読む。——ただし**そのパッチはリポジトリに
記録されていない**（未コミットのローカル改変で、どの行をどう落としたかが残っていない）ので、
現時点では検算できない。これ自体が副次的な所見である: 対照アームは再現に必要な情報を欠く。

### ⚠️ W2 — 「毎フレーム発火する」は分岐条件としては正しいが、フレームの頻度は測っていない

rustdoc「OS のモーダル移動ループ中もフレームは回り、**ポインタが押されていないのでクランプが
毎フレーム発火する**」。前半（フレームが回る）は実測が支持する（クランプが現に効いている）。
後半も `view.rs:1279` の `if !ui.input(|i| i.pointer.any_down())` から見て「走ったフレームでは必ず
発火する」という意味では正しい。

ただし `src-tauri/CLAUDE.md`「イベント駆動 wake の不変条件（#532 SU5）」は
「runtime はイベント駆動（`RedrawRequested` 待ち）で**通常フレームは勝手に回らない**」と定めており、
モーダル移動ループ中に**フレームが何枚走るか**はこの調査では測っていない。
H1 で見た平坦区間（反復 2 の k=60/80/100 が 3 標本連続で 1132）は「フレームが疎である」と整合する。
読者が「毎フレーム」を「連続的に拘束されている」と読むと、H1 の逸脱と衝突する。

**偽にする手順**: モーダル移動ループ中のフレーム数を計器（trace）で数える。1 打鍵あたり 1 枚以上
走っていることが示されれば、この懸念は消える。**未実施**。

### ⚠️ W3 — 多モニターの射程宣言は適切に保たれている（反証を探したが見つからなかった）

rustdoc「**多モニターでの封鎖そのものは未測定である**——測ったのは機序であり、封鎖は上の作業例
からの演繹にとどまる」、SPEC「隣のモニターへ渡れないと**見込まれる**」。
どちらも全称にも断定にもなっておらず、`AGENTS.md`「検証の作法」の全称表現の条項に照らして適切。
測定環境は単一モニター（rcMonitor 0,0,1920,1080）なので、これ以上強く書けない。
**この項は所見ではなく、疑って裏が取れなかったことの記録である。**

## 4. Critical / High の扱い

修正は当てていない（報告のみ）。作業ツリーはアンカーのまま（ハッシュ照合済み・`git status` 清浄）。
呼び出し側が当てるべきは H1（と、同じ量化の写しである M1）である。
