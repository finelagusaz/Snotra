# verify-1179-round4.txt — issue #1179 / 4 巡目（最終巡）

アンカー: `dc88f9170df631f1455e0930a2043276661c1a84`（最終確認時も HEAD = このsha）
前回アンカー: `3bff387b`（差分は 1 コミット `dc88f917`）
worktree: `C:/workspace/Snotra/.claude/worktrees/agent-a9f0ef41cb3d195ef`

**最終整合**: 全変異の巻き戻し後、`git status --porcelain` は `?? workspace/verify-1179-round4.txt`
のみ、`git diff --stat HEAD` は空、5 ファイルの SHA256 全一致、`test:powershell` 再実行で 131 passed。

---

## 0. アンカー合わせ — 途中で**私自身が誤った観測をした**ので先に記録する

段取りどおり「未追跡コピーとコミット済み版を照合してから消す」を行ったが、**1 度目の照合が
偽の不一致を出した**:

```
git show dc88f917:workspace/verify-1179-round3.txt | Set-Content <tmp> -Encoding utf8
  MINE      = BCD8041100DDE12F5455FDDDDDFE6ABCFA28EDC3763E7092AB40D80B2900C93D
  COMMITTED = 35EC7612382E2B469FBBB8078ADD953FDE91A2F606988D3530A59594F90C7BAA
  MATCH     = False
```

**これは内容の差ではなく、私の観測経路の差である**（`Set-Content -Encoding utf8` が BOM を付け
CRLF へ直し末尾へ改行を足す）。git 自身の内容ハッシュで測り直すと:

```
MY_WORKING_BLOB = 331505b588b8c52fcc56ec399a609d6ad8d2288e
COMMITTED_BLOB  = 331505b588b8c52fcc56ec399a609d6ad8d2288e   ← 一致
MAIN_COPY_BLOB  = 331505b588b8c52fcc56ec399a609d6ad8d2288e   ← 一致
```

round3 はコミット済みの版と**バイト単位で同一**である。**壊れた出力から推論せず測り直した**ので
削除に進めた——そのまま信じていれば「委譲元が私の報告を改変した」と誤って報告していた
（`MEMORY.md`「read-methods-observe-different-targets」の逐語的な再現）。

### 巻き戻し照合用ベースライン（SHA256・`dc88f917` 時点／最終確認でも全一致）

```
291A0154D3BA1D5D85CEE5B80E261C6D07DA6B8406DB1920DE5054E221A3FC2F  scripts/bench-startup.ps1
9CB1C46B0C06E4CF9A572DE24C07BB736BDE516DBA988E9252D8ABAFEBD69D57  scripts/smoke-startup.ps1   ← 2 巡目から不変
4F5511690AE5F3EA790C6770401AFEF11AACF10CD32B80F0C6662C3F7236E38A  scripts/lib/SnotraSmoke.psm1 ← 1 巡目から不変
108E689A7D68FB5EF02F78B439005A6C888B0E1CACF9800C770F08381CD4DBDE  scripts/lib/SnotraSmoke.Tests.ps1
2F224781E99EB55D10975F9F99A84DCA5427D8F2F0D17C38413239B376DD016B  docs/build-commands.md      ← 3 巡目から不変
```

---

## 1. (d) カテゴリ A〜F 再判定（差分の証拠つき）

| カテゴリ | 判定 | 差分の証拠 |
|---|---|---|
| **A. Rust** | **該当なし** | `git diff --stat 3bff387b..dc88f917 -- '*.rs' 'Cargo.lock' '*/Cargo.toml' 'snotra-core' 'snotra-egui-runtime' 'src-tauri' 'snotra-settings'` → **空** |
| **B. TypeScript** | **該当なし** | `.ts` に差分なし（`git diff --stat` の全体が bench / Tests / plan / round3 の 4 件） |
| **C.** | **文面上は非該当／一部実行** | Phase 7 は `SnotraSmoke.Tests.ps1`（検査そのもの）と `bench-startup.ps1` を変更 → `test:powershell` と bench は必須で**実行した**。**`smoke:startup` は省略**——同じ `git diff --stat` に `'scripts/smoke-startup.ps1'` と `'.github'` を当てて**空**、`smoke-startup.ps1` の SHA256 も 2 巡目と同一、`.rs` も不変。**本体不変を差分で示せたので省いた**（なお変異注入で smoke を 10 回以上起動しており、正版が緑であることは各アームの前後で実測している） |
| **D. UI の見た目** | **該当なし** | 描画に関わるファイルに差分なし |
| **E. `.githooks/`** | **該当なし** | 差分なし |
| **F. ガバナンス文書** | **該当・実行** | `workspace/plan.md`（`.md`）を変更。`docs/build-commands.md` は今回**不変**（3 巡目から SHA256 同一） |

**「実行不能」は 0 件。**

### ベースライン（変異注入の前）

| コマンド | exit | 証拠 |
|---|---|---|
| `npm run governance:check` | **0** | 全検査 passed |
| `npm test`（vitest） | **0** | `Tests 901 passed (901)` |
| `npm run test:powershell` | **0** | `Tests Passed: 131, Failed: 0` |
| bench（既定・cwd = メイン） | **0** | §3 |

述語が見る 3 点を素で評価しても baseline は健全:
```
bench-startup.ps1 : param=1 [[string]$ExePath = '',] / assign=1 / calls=1
smoke-startup.ps1 : param=1 [[string]$ExePath = '']  / assign=1 / calls=1
```

---

## 2. (a) 変異 I と J の再現

| 足 | 3 巡目 | 4 巡目 | 読み |
|---|---|---|---|
| **I**（`param()` の既定を直書き絶対パスへ） | **exit 0 / 131 passed**（素通り） | **exit 1 / 130 passed 1 failed** | **入口は閉じた** |
| **J**（導出後に `$ExePath` を上書き） | exit 0 / 131 passed | **exit 0 / 131 passed** | **宣言どおり** |

I の文言:
`Expected $null or empty, because 既定を空にして導出へ委ねること（パスを直書きすると導出が走らず
#1179 が復活する）, but got 'C:/workspace/Snotra/target/debug/snotra.exe'.`

**J が緑なのは失敗ではない、と読めるか → 読める。** 新しい宣言は
「**射程の外（受容する残余）**: `$ExePath` が導出の**後で**上書きされる形には届かない。ここは
『どう書かれているか』を見る述語であって、実行時の最終値を追わない」と**名指しで**書いており、
私が測った J の挙動（Pester 131 緑・`本体: C:\workspace\Snotra\target\debug\snotra.exe`）は
この文と過不足なく一致する。**宣言が実態と一致していることの実測**である。

（`AGENTS.md`「射程の宣言と逸脱の明記を対で書く」の要求が、測って裏づいた形。1 巡目・2 巡目の
`-Profile` / `-ProfileDir` の残余も同じ扱いで、いずれも実測で裏が取れている。）

---

## 3. ⚠️-E（`GetFullPath` の版揃え）の検算 — Medium-1 は保たれている

両枝を 2 引数版へ揃えたので、Medium-1 を捕まえたのと同じ道具で測り直した。**今回は
「メイン側が動かない」だけでなく「worktree 側が動く」も見た**——前者だけでは「そもそも何も
書かなかった」と区別が付かないためである。

```
cwd = C:\workspace\Snotra（メイン作業コピー）で、worktree の bench を絶対パス・引数なしで起動

              BEFORE                AFTER
メイン側      20:24:42              20:24:42     ← **動いていない**
worktree 側   20:14:58 / 20:14:59   20:36:24 / 20:36:25   ← **動いた（正しい宛先）**

Exe:      …\agent-a9f0ef41cb3d195ef\target\release\snotra.exe
Profile:  …\agent-a9f0ef41cb3d195ef\target\bench-startup\profile
起動計器 passed（1 runs）。 exit 0
```

**Medium-1 の修正は保たれている。**

なお委譲元の「`LastWriteTime`（19:53:07 → 20:24:42）で実測」という記述は、私の 2 巡目の litter
（19:53:07）が 20:24:42 へ**更新された**ことを意味しており、文面だけでは「出荷版がメイン側へ
書いた」とも読めた。**自分の run を前後で挟んで測り直した結果、出荷版はメイン側へ 1 バイトも
書いていない**（20:24:42 は委譲元自身の測定中のいずれかの run が残したもの）。曖昧さは
文面ではなく測定で解いた。

---

## 4. (b) 5 つ目の迂回 — **2 件見つかった。どちらも fail-open**（High-4）

入口・導出・使用点の 3 点を**すべて無傷のまま** #1179 を復活させる形である。

### 変異 P: `param()` の既定を行継続で折り、次行へ直書きする

```powershell
  [string]$ExePath =
    'C:/workspace/Snotra/target/debug/snotra.exe'
```

```
述語が見る param 行 = [  [string]$ExePath =]    ← '=' の右が空なので BeNullOrEmpty が通る
ハーネスは構文として妥当（ParserError なし）
本体: C:\workspace\Snotra\target\debug\snotra.exe   ← メインのコピー = #1179
npm run test:powershell : exit 0 / 131 passed        ← 4 つの Should が全部通る
```

**しかも cwd = worktree（`npm run` と同じ最も有利な条件）で症状が出る。**
入口の `Should` の `-Because` は「param() の ExePath 宣言を**1 行で**書くこと」と要求しているが、
**述語はその要求を強制していない**——折った残りは別の行になり、正規表現に 1 件も当たらないので
`Count -Be 1` も素通りする。**宣言している要求と、実際に効いている述語がずれている。**

### 変異 Q: 代入構文を使わずに `$repoRoot` を差し替える

```powershell
$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot '..')).Path   ← 無傷
Set-Variable -Name repoRoot -Value 'C:/workspace/Snotra'          ← ^\s*\$repoRoot\s*= に当たらない
```

```
assign 件数 = 1（Count -Be 1 が通る）／使用点も無傷
本体: C:\workspace\Snotra\target\debug\snotra.exe
npm run test:powershell : exit 0 / 131 passed
```

M-add（`$repoRoot = …` を 2 行目に足す形・3 巡とも赤）と**意味は同じで構文だけが違う**。

### この 2 件の位置づけ

**P も Q も「宣言済みの残余（J = 導出後の上書き）」とは別のクラスである**——J は「述語が見ない
場所」だが、P と Q は**述語が見ているつもりの場所を、別の構文で通り抜けている**。
つまり検知器の**弁別が、覆っているはずの足で崩れている**。

ただし率直に言えば、これは**ソースの形を見る述語の原理的な天井**である。同じ意味を書ける構文が
複数ある以上、述語を 1 つ足すたびに「その形以外」が残る——`MEMORY.md`
「universal-claim-fix-regenerates-itself」の述語版であり、**Should を足し続けても収束しない**。
**私の推奨は「述語をもう 1 段足す」ではなく、宣言を『覆うのは各点の代表的な書き方であって、
同義の別構文は射程外』へ弱めることである**（`AGENTS.md`「数え上げをやめ『〜だけではない』の
下限主張へ倒すと止まる」）。P・Q は**その下限主張の裏づけとして**記録に残す価値がある。

---

## 5. (c) 新しい `param()` 既定の `Should` の誤検出面 — **2 件（Low-1）**

いずれも**先にハーネスを実行して意味不変を確かめてから**掛けた。

| プローブ | ハーネス | Pester | 判定 |
|---|---|---|---|
| **c1** 二重引用符 `= ""` | 正常（worktree の本体） | **exit 0** | 誤検出なし（対処済み） |
| **c2** 型名の大文字 `[String]` | 正常 | **exit 0** | 誤検出なし（`-match` は既定で大小無視） |
| **c3** 空白を増やす `[string]   $ExePath   =   ''` | 正常 | **exit 0** | 誤検出なし |
| **c4** 行末コメント `= ''   # 空なら導出へ委ねる（#1179）` | 正常 | **exit 1** | **誤検出** |
| **c5** 既定に `$null`（`[string]` へは `''` に落ちる） | 正常 | **exit 1** | **誤検出**（様式は珍しい） |
| **c6** smoke で末尾カンマ `= '',` | **ParserError**（`Missing expression after ','`） | exit 0 | 誤検出ではない（下記） |

- **c4 が実質的な 1 件。** 3 巡目の N（関数名に触れたコメント行）を消すために
  `-notmatch '^\s*#'` で**行頭コメントだけ**を除いたが、**行末コメントはその網の外**にあり、
  そこから値の切り出し（`-split '='` → `Trim` → `TrimEnd(',')` → `Trim("'", '"')`）が崩れる。
  ファイル自身の様式は説明を上の行へ置いているので踏みにくいが、**説明を行末へ寄せるのは
  自然な編集**である。fail-closed なので優先度は低い。
- **c6 について**: 委譲元が挙げた「bench にだけ在る末尾のカンマ」は**正しく扱えている**
  （baseline で bench の `= '',` が緑・`TrimEnd(',')` が効いている）。smoke に同じ形を持ち込むと
  PowerShell の構文エラーになる（param が 1 つしかないため）ので、**そもそも書けない**。
  ただしこのとき **Pester は exit 0 を返す**——2 巡目 Low-3 と同じ「ソース述語は構文を見ない」
  性質の再現であり、既に却下で合意済みの論点なので所見にはしない。

---

## 6. 所見

### Critical — 0 件

### High-4: 入口の `Should` が宣言している要求（「1 行で書くこと」）を述語が強制しておらず、fail-open の迂回が 2 形ある

- 根拠は変異 P と Q（§4）。**どちらも 4 つの `Should` を全部通し、vitest 901 も通り、
  smoke は exit 0 で緑のまま `本体:` がメイン作業コピーを名指す。**
- **P は cwd = worktree（`npm run` と同じ条件）でも症状が出る**ので、D / G より踏まれやすい。
- 閉じる条件への当てはめ（判断は委譲元に委ねる）:
  - **(i) 出荷する挙動を変えるか → 変えない。** 出荷される `bench-startup.ps1` /
    `smoke-startup.ps1` / `SnotraSmoke.psm1` は正しく、実測でも worktree の本体を測っている。
  - **(ii) 測定済みの足で検知器の弁別が壊れているか → 部分的に当たる。**
    P は I（測定済み・赤）の、Q は M-add（測定済み・赤）の**同義の別構文**であり、
    「宣言済みの残余と同じクラス」（= J）ではない。ただし**新しい死角**でもある。
- **私の推奨は「`Should` をもう 1 段足す」ではない。** 足すたびに別構文が残る構造であり
  （`universal-claim-fix-regenerates-itself` の述語版）、**宣言を
  「覆うのは各点の代表的な書き方であって、同義の別構文は射程外」という下限主張へ弱める**のが
  収束する形だと考える。P・Q はその宣言の裏づけとして記録に残せばよい。
  実装を変えないので**ブロックしない扱いで妥当**だと私は考える。
- どうしても 1 手だけ打つなら、**入口の `-Because` から「1 行で」の要求を外す**のが最小
  （強制していない要求を宣言しない）。これは実装を変えず、宣言と実態のずれだけを消す。

### Medium — 0 件

**何を測った上での 0 件か**: Phase 7 の 2 ファイル（`bench-startup.ps1` の `GetFullPath` 統一 4 行、
`SnotraSmoke.Tests.ps1` の `It` 全文と宣言コメント）を実読し、bench の既定枝を
cwd = メインから端から端まで実行して**メイン側・worktree 側の LastWriteTime を両方**測り、
検知器を 4 足の再現（I / J / P / Q）と 6 件の誤検出プローブ（c1〜c6）に掛けた。
この範囲で Medium は出なかった。

### Low-1: 行末コメントで誤検出する（`$null` 既定も同様）

実測は §5。**fail-closed** で、ファイル自身の様式（説明は上の行）なら踏まない。直すなら
値の切り出しの前に行末コメントを落とす（`-replace '#.*$', ''`）が最小だが、**それ自体が
また別の構文（文字列内の `#`）で崩れる**ので、High-4 と同じ理由から**対処しない判断も妥当**である。

### ⚠️（確信の持てないもの）

**⚠️-G: `$calls.Count | Should -BeGreaterThan 0` は、いま何も守っていない可能性がある**
`foreach ($call in $calls) { … }` は `$calls` が空なら**0 回まわって黙る**ので、
`-BeGreaterThan 0` は「呼び出しを全部消す」形を捕まえる唯一の受け皿として**必要**である
——ここまでは正しい。ただし呼び出しを全部消せば `$ExePath` は param 既定の `''` のまま
`Test-Path -LiteralPath ''` へ落ち、**ハーネスが即座に赤くなる**（2 巡目の変異 B で実測済み）。
つまりこの `Should` が守るのは「ハーネスを 1 度も回さずにコミットする」経路だけである。
無害だが、`-Because`（「本体の導出は Resolve-SnotraCargoExecutable へ委ねること」）が
示唆するほどの働きはしていない。**意図的に置いた保険である可能性があるので ⚠️ に留める。**

**⚠️-H: 検知器の宣言が長くなり続けている（27 行のコメントに対し述語は 8 行）**
1 巡目 0 行 → 2 巡目 13 行 → 3 巡目 20 行 → 4 巡目 27 行と、**巡ごとに宣言だけが伸びている**。
内容はいずれも私が測った実測に紐付いており腐ってはいないが、`AGENTS.md`
「保証を宣言する文の隣に、保証されていない一覧を置かない」の観点では、
**宣言が長いほど読者が『ここに書いてあることは全部守られている』と読む面積が広がる**。
所見ではなく観察として置く。確信は持てない。

**⚠️-I（持ち越しの決着）**: ⚠️-D（`$calls.Count` の必要性）は「件数の縛りをやめて全件へ課す」で
**解消**（c4 以外の誤検出が消えたことを実測）。⚠️-E（`GetFullPath` の版割れ）は**解消を実測**（§3）。
⚠️-A / ⚠️-B / ⚠️-1 / ⚠️-2 / ⚠️-3 / Low-2 / Low-3 は前巡までに決着済み。

### Phase 7 が壊した既存の保証 — **0 件**

**何を測った上での 0 件か**: (1) 3 巡目までに赤かった足のうち I を再現して赤を確認、
(2) 宣言済みの残余 J が宣言どおり緑であることを確認、(3) Medium-1 の維持を
**メイン側と worktree 側の両方の LastWriteTime** で確認、(4) 3 巡目の誤検出 3 形（K / L / N）の
うち L・N に相当する形（c1〜c3、および `-RepositoryRoot[:\s]` のコロン記法対応）が
消えていることを確認、(5) `governance:check` / `npm test` / `test:powershell` が
ベースラインで緑、(6) `.rs` / `.github` / `smoke-startup.ps1` / `docs/build-commands.md` が
差分ゼロ（SHA256 で確認）。

---

## 7. パフォーマンス

`.rs` 不変（実測）。検知器の実行費用は 61ms + 11ms（3 巡目の 38 + 14 から、入口の 2 `Should` と
コメント除去のフィルタ分）。`test:powershell` 全体に対して無視できる。
`GetFullPath` の版揃えは起動あたり 1 回の文字列演算。**懸念なし。**

## 8. 最終巡としての総括

4 巡で所見の在り処は「製品 → 検知器 → 検知器の宣言 → 述語の構文的な天井」と移った。
**出荷される 3 ファイル（`bench-startup.ps1` / `smoke-startup.ps1` / `SnotraSmoke.psm1`）に対する
所見は 2 巡目の Medium-1 が最後で、以降は 0 件である**（`psm1` は 1 巡目から、
`smoke-startup.ps1` は 2 巡目から、`docs/build-commands.md` は 3 巡目からバイト単位で不変）。
残る High-4 は検知器の宣言と述語のずれであって、**出荷する挙動は正しい**——受け入れ条件 1〜6 は
4 巡すべてで実測により満たされている。
