# verify-1188-round2.txt — fix-forward 差分の再検証

対象 sha: 57d60306521502631c6ec92e9b01521906eb5251
前アンカー: c4cb443314bccd1f509c8e417693db59f8755532
worktree: C:/workspace/Snotra/.claude/worktrees/agent-ac35f0e259482f596
検証日: 2026-08-26（ラウンド 2）

## 0. アンカー確認（実測）

- `git checkout --detach 57d60306` → HEAD = 57d60306521502631c6ec92e9b01521906eb5251（一致）
- `git status --porcelain` = 空（開始時・全変異の復元後とも）
- node_modules の junction は生きている（`node_modules/vitest` 実在）
- **実装は不変**: `git diff c4cb4433 57d60306 -- scripts/governance/lib.mjs` に
  `HEADING_REF = new RegExp(...)` 行の差分は無い（実測）
- 差分の規模: 7 ファイル / +340 -5

## 1. 検証（カテゴリ A〜F）

| カテゴリ | 判定 | コマンド | exit |
|---|---|---|---|
| A. Rust | **該当なし** | `.rs` を 1 枚も触っていない（`git diff --stat c4cb4433 57d60306`） | — |
| B. TypeScript | **該当なし** | `.ts` 未変更 | — |
| C. 窓生成/hotkey/slash | **該当なし** | 該当経路を触らない | — |
| C. `npm test` | **実行**（`scripts/` 変更のため） | `npm test` | 0（38 files / **906 tests**・前回 905 から fixture 1 件増） |
| D. 目視 GUI | **該当なし** | 描画を変えない | — |
| E. git hook | **該当なし** | `.githooks/**` 未変更 | — |
| F. ガバナンス文書 | **実行** | `npm run governance:check` | 0 |

`governance:check` の evidence 比較（c4cb4433 → 57d60306）:
- 見出し参照 **365 → 365**（不動）／近傍 **19 → 19**／折れうる位置 **20 → 20**
- コードスパン **18152 → 18159**（+7 ＝新設 doc と fixture が足したぶん・想定内）
→ **回帰なし。実行不能なカテゴリは無い。**

---

## 2. 新規の所見（重大度つき）

### [Critical（依頼の定義による）／実質は High] R2-2 — 「唯一の観測性の後退」が偽。死角 4 も同じ後退である

**対象は 3 か所ある**（`git grep -n 観測性の後退 -- . ':!workspace/'` で数え上げ。
「唯一」より「観測性の後退」のほうが取りこぼしにくい鍵である）:
- `scripts/governance/lib.mjs:194` — 死角 5: 「…**この変更が作った唯一の観測性の後退である**」
- `docs/adr/ADR-nested-quote-heading-ref-labels.md:69` — 「**観測性の後退を 1 つ作った（唯一）。**」
- `scripts/governance/checks/G-heading-refs.test.mjs:103` — 「**この変更が作った唯一の観測性の後退**（正本は `lib.mjs` の doc の死角 5）。」

**3 枚目（fixture のコメント）を落とさないこと。** #977 / #1056 の「写しを直す当のコミットが
取りこぼす」形がここに揃っている。**数え上げの母集団に PR 本文も含める**——squash で main の
commit message になるのにリポジトリの grep には入らない。現行の commit message
（`fix(governance): HEADING_REF を…`）にも「最後の 1 つは唯一の挙動変化」がある。

同様に **R2-1 の「15 件」は 2 か所**である（`git grep -n "15 件"` で数え上げ・他のヒットは無関係）:
`scripts/governance/lib.mjs:195` と `docs/adr/ADR-nested-quote-heading-ref-labels.md:74`。

**実測**（同一入力に OLD 実装と NEW 実装を当て、「OLD が赤 → NEW が沈黙」を後退と定義）:

| 形 | OLD | NEW | 後退 |
|---|---|---|---|
| 死角 5: 外側の綴りが**無効** + 内側が腐っている | checked=1 **findings=1** | checked=0 findings=0 | **あり** |
| 死角 4: 外側の綴りが**有効で着地する** + 内側が腐っている | checked=1 **findings=1** | checked=1 **findings=0** | **あり** |
| 対照: 入れ子なしで内側だけ | checked=1 findings=1 | checked=1 findings=1 | なし |

OLD/NEW とも `finding` の文言は `着地しない: \`docs/inner.md\`「消えた見出し」` で同一。
違いは **NEW がそれを出さないこと**だけである。

**つまり観測性の後退は 2 つある。** 死角 4 と死角 5 の違いは `checked` が 1 に留まるか 0 になるかであって、
**内側の腐りが見えなくなる点は同じ**である。「唯一」は成り立たない。

**この反証は、このリポジトリ自身の fixture が既に持っている。** 変異 1（旧正規表現へ巻き戻し）で
fixture 4「他の参照のラベルの内側に置かれた正準形参照は独立には照合されない」が
`expected { findings: [1件], checked: 1 } to deeply equal { findings: [], checked: 1 }` で落ちる——
**OLD では赤が 1 件出て、NEW では出ない**ことを逐語で示している（ラウンド 1・2 の両方で観測）。

**重大度**: 依頼の規則「doc が実装より強い主張をしていたら Critical」に当たる。「唯一」は
**被害の有界性を主張する安全側の断定**であり、実装は 2 件を許している。ADR の受容の論拠
（「唯一なので受容する」）もこの数の上に乗っている。
一方で**実質的な緩和がある**——読者は死角 4 の側も別 bullet で警告されており、
「内側は独立に照合されない」は書かれている。偽なのは**メタな数え上げ**である。
最終的な Critical / High の裁定は主エージェントへ委ねる。

**推奨する直し方**: 「唯一」を落とし、下限の主張へ倒す。例:
> 観測性の後退はこれだけではない——死角 4 も、外側が着地すれば内側の腐りを隠す。
> 死角 5 が重いのは `checked` すら 0 になり evidence 行から読めない点である。

### [High] R2-1 — 「無効綴りの頭 15 件」が再現しない（13 件）。数え方が死角 5 と一致せず、書いた当のコミットで腐っている

`lib.mjs` 死角 5 と ADR「観測性の後退を 1 つ作った（唯一）」が露出の根拠に据える数である。

**私の再測定**（本物の照合経路 = `makeSnapshot` + `allHeadingRefDocs` + `refScanLines` +
出荷される `HEADING_REF`。同じスクリプトが**有効綴りの頭を 365 件**と出し `governance:check` の
evidence と一致するので母集団の作り方は正しい）:
```
docs=266
有効綴りの頭（= checked に載る）  = 365 件   ← governance:check と一致
無効綴りの頭                     =  13 件   ← ADR/doc は「15 件」と主張
そのうち内側に有効綴りの参照を持つ行 = 0 件   ← doc の「今日 0 件」は**真**
```

**15 が出る数え方を網羅的に探した**（6 変種）:
| 変種 | 57d60306 | c4cb4433 |
|---|---:|---:|
| A 照合母集団 × `HEADING_REF`（＝死角 5 の定義） | **13** | **13** |
| B 照合母集団 × `REF_HEAD`+`「`（閉じを問わない頭） | **16** | **15** ← |
| C 全追跡ファイル全行 × `HEADING_REF` | 49 | — |
| D 全追跡ファイル全行 × 頭のみ | 64 | — |
| E D から workspace/ 除外 | 49 | — |
| F C から workspace/ 除外 | 43 | — |

→ **15 は変種 B を fix-forward の「前」の木で測った値である。** 二重に問題がある。

1. **定義が死角 5 と一致しない。** 変種 B は**ラベルが閉じない頭まで数える**。死角 5 は
   「`HEADING_REF` が外側として**一致した**のに `isRefTargetSpelling` で弾かれる」形であり、
   ラベルが閉じない頭は `HEADING_REF` に一致しないので**内側を飲み込むことが原理的にできない**。
   露出の指標として誤っている。正しい数は **13**（両 rev で不動）。
2. **書いた当のコミットで既に腐っている。** 変種 B は c4cb4433 で 15、57d60306 で **16**。
   増えた 1 件は `scripts/governance/lib.mjs:187` ——**この fix-forward 自身が足した doc 行**
   （「同検査の `OPEN_UNCLOSED` は入れ子の `「` で切れる」）である。
   同じ JSDoc が 2 段落上で警告している当の罠にはまっている:
   > **ここへ書く例が検出器を赤にしうる**——このファイルのコメントは照合母集団に入る（#1155 と同じ罠）

**重大度**: 依頼の Critical 規則には当たらない（15 > 13 は露出を**過大**に見せる向きで、
無い安全を約束していない）。しかし前回 R-5 と同型の失敗が、今度は**恒久文書**
（`docs/adr/` と `lib.mjs` の JSDoc）へ着地した点で前回より重い。**High**。

**推奨する直し方**（`AGENTS.md`「検証の作法（全タスク共通）」の「数ではなく正本を指す」）:
数を消して形で書く。残すなら「13 件（`HEADING_REF` が一致した外側のうち `isRefTargetSpelling` に
外れるもの・2026-08-26 実測）」と**定義つき**にする。

### [Medium] R2-3 — 死角 2 の新しい文が、また全称で偽になっている（向きは安全側）

新しい文: 「**入れ子かつ物理改行で折れた形**。**`G-folded-heading-refs` も拾わない**——同検査の
`OPEN_UNCLOSED` は入れ子の `「` で切れる（実測。入れ子を含まない同じ折れ方は拾う）」

**実測**（折れには 2 形あり、`G-folded` の扱いが分かれる）:
| 形 | 入れ子 | G-heading | G-folded |
|---|---|---|---|
| 形 B（`「` まで開いて次行へ流れる） | あり | checked=0 findings=0 | checked=0 findings=0（沈黙） |
| 形 B | なし | checked=0 findings=0 | checked=1 findings=1（赤） |
| **形 A（行末が対象綴り・次行が `「`）** | **あり** | — | **checked=1 findings=1（赤）** |
| 形 A | なし | — | checked=1 findings=1（赤） |

→ 「入れ子かつ物理改行で折れた形」は**形 A も含む**が、形 A は入れ子でも `G-folded` が**拾う**。
「`G-folded-heading-refs` も拾わない」は形 A について偽である。

**向きは前回と逆で安全側**（拾わないと言って実は拾う）ので実害は小さい。しかし
**同じ bullet で 2 回続けて偽の全称になっている**——MEMORY の
`universal-claim-fix-regenerates-itself`（偽の全称を直した文がまた全称で偽になる・#1091 で 3 連鎖）
の 2 連鎖目である。**Medium**。

**推奨**: 形を名指しして自足させる。例:
> ラベルが同一行で閉じずに次行へ流れる形（`G-folded` の形 B）は、入れ子があると同検査も拾わない
> ——`OPEN_UNCLOSED` が入れ子の `「` で切れるため。行末が対象綴りで終わる形（形 A）は入れ子でも拾う。

**なお機序の記述は正しく、私のラウンド 1 の言い方を訂正している。** 私は「入れ子の `」` で切れる」と
書いたが、`[^「」\n]*` が最初に当たる除外文字は `「` の側である。**fix-forward の記述が正しい。**

**⚠️ 付随（Low・情報）**: 「**どちらの検査の宣言にも無かった穴であり、ここが唯一の記載である**」は
`git grep -n OPEN_UNCLOSED -- . ':!workspace/'` で検算した——ヒットは `G-folded-heading-refs.mjs`
の実装 2 行と `lib.mjs:187` のみ。**散文の記載としては唯一で、主張は成り立つ。**

---

## 3. 前回 ⚠️（R-3〜R-6）の決着状況

| 前回の所見 | 状態 | 根拠（実測） |
|---|---|---|
| **R-3**（死角 4 の不在断定が外側非閉の角で偽） | **決着** | 新文「外側が**有効な対象綴り**なら外側 1 件へ統合され、内側は独立に照合されない。外側が閉じなければ内側が独立に照合される（安全側）」を実測と突合 → 外側有効+閉じる: checked=1 findings=0 ✓／外側非閉: checked=1 findings=1（内側が赤）✓。**両方向とも一致** |
| **R-4**（ADR の `G-near` 残余が偽陰性の向きだけ） | **未決着**（意図的な見送りと見られる・採否リストに無い） | ADR の当該段落は無変更。実測は不変: 入れ子ラベルの近傍形は checked=1 findings=0（「不可視」ではなく別物として一致）／`「「概要」の話」` + `## 概要` で **誤った正準形「`docs/c.md`「概要」と書け」を提案する finding が出る**。`G-near` は本件で 1 バイトも変わっていないので**回帰ではない**。残余宣言の射程の話として残る |
| **R-5**（probe NOTES が出荷時点で 1 件ずれ） | **決着** | NOTES が「深さ 2 以上 **1 件**」へ修正され、**超集合スキャンと照合母集団の差**（`refScanLines` が `.mjs` から供給するのはコメント行だけ）が明記された。probe を 57d60306 で再実行 → 深さ 2 以上 3 件（workspace/ 2 + fixture 1）＝除くと **1 件** ✓／行内で閉じない 14 → 除くと 13 ✓／閉じ不足 1 → 除くと 0 ✓。**3 項すべて一致** |
| **R-6**（perf の数値が再現しない） | **決着** | ADR が「むしろ速い」→「**速くなるとは書かない**……向きは弱く支持される程度」へ弱まった。`research.md` に私の再測定値（OLD 65.6〜74.3 / NEW 64.2〜71.9・6 組中 5 組で NEW ≤ OLD）と「93.6 は OLD 未 warmup」の仮説が**未検証と明記して**残された。表の太字も外れている |
| **R-1**（無効綴りの外側が内側を飲み込む） | **採用・宣言済み**。ただし添えた数（15）と「唯一」が新たな所見（R2-1 / R2-2） | 下記 |
| **R-2**（死角 2 の括弧書きが全称で偽） | **採用**。ただし直した文がまた全称で偽（R2-3） | 上記 |

---

## 4. 変異注入（worktree の中だけ・本体は無変更）

baseline SHA-256:
- `scripts/governance/lib.mjs` = `0095F2F561C305B8B6D8E4BD59E70426B3A26299B690FC6824CF3A618766AFCA`
- `scripts/governance/checks/G-heading-refs.mjs` = `91D18D99F9061BD4D2D6E0E40C932AA86E3D5AF7D615EBF6A4271B91D7647FCE`

baseline `npx vitest run scripts/governance/checks/G-heading-refs.test.mjs` = **13 passed / exit 0**

### 変異 1 — 旧正規表現へ巻き戻し（`git show 'c4cb4433^:...'` から逐語取得）

**4 failed / 9 passed（exit 1）**——前回の 3 件に fixture 5 が加わった。

| # | fixture | 巻き戻し時 | 判定 |
|---|---|---|---|
| 1 | 全形の参照が照合され着地する | `checked` 1 → 0 | **落ちる** |
| 2 | 入れ子を含む見出しを改名すると finding | `findings` 1 → 0 | **落ちる** |
| 3 | 深さ 2 以上の入れ子は一致を生成しない | 変化なし（両実装とも checked=0） | **生き残る** |
| 4 | ラベル内側の正準形参照は独立には照合されない | `findings` 0 → 1 | **落ちる** |
| 5 | **外側の綴りが無効だと内側ごと沈黙する**（新設） | `{findings:[], checked:0}` → **`{findings:[1件], checked:1}`** | **落ちる** |
| — | 既存 8 件 | 変化なし | **生き残る**（非入れ子の挙動は完全に不変） |

### 変異 2 — 深さ 2 まで受け入れる形へ広げる

**1 failed / 12 passed。** 落ちたのは fixture 3 のみ:
`expected { findings: [1件], checked: 1 } to deeply equal { findings: [], checked: 0 }`
（`checked` と `findings` の**両方**が動く）

### 変異 3 — 死角 5 を塞ぐ修正（`G-heading-refs.mjs`: 弾いた外側のラベルを内側として再走査する）

**1 failed / 12 passed。** 落ちたのは fixture 5 のみ:
`expected { findings: [1件], checked: 1 } to deeply equal { findings: [], checked: 0 }`

### 依頼への直接の回答 — fixture 5 は**両方向**に効く（fixture 3 は片方向）

| fixture（`checked: 0` を主張する死角固定） | 狭める向き（変異 1・巻き戻し） | 広げる向き | 判定 |
|---|---|---|---|
| **3**（深さ 2 以上） | **生き残る**（両実装で checked=0） | 変異 2 で落ちる | **片方向**（広げる側のみ） |
| **5**（無効綴りの外側） | **落ちる** | 変異 3 で落ちる | **両方向** |

fixture 5 が両方向に効くのは、**この死角が「旧実装では見えていた形」だから**である——
巻き戻すと観測が復活するので差が出る。fixture 3 の深さ 2 は旧実装でも見えなかったので、
巻き戻しでは差が出ようがない。**fixture 3 が片方向なのは fixture の欠陥ではなく、
固定している死角の性質である**（前回と同じ結論）。

なお変異 1 と変異 3 は fixture 5 に**同じ観測値**（`{findings:[1件], checked:1}`）を与えるので、
この fixture は「線が動いたこと」は捕まえるが「どちら向きに動いたか」は区別しない。**⚠️ 情報**。

### 生きたツリーは今回も旧新を区別しない

変異 1 の下で `npm run governance:check` = **exit 0**、evidence 行が 1 文字も変わらない
（365 / 19 / 20 / コードスパン 18159）。**fix-forward が doc と fixture を足した後も、
この変更を守っているのは fixture だけである**（前回と同じ結論を新しい木で再確認）。

### 復元と照合

- `git checkout 57d60306 -- scripts/governance/lib.mjs scripts/governance/checks/G-heading-refs.mjs`
- lib.mjs = `0095F2F5…AFCA`（baseline と一致）／G-heading-refs.mjs = `91D18D99…7FCE`（一致）
- `git status --porcelain` = 空
- `npm test` = 38 files / **906 tests passed** / exit 0

本体 `C:/workspace/Snotra` へ書いたのは本ファイル `workspace/verify-1188-round2.txt` のみ。
前回の `workspace/verify-1188.txt` は上書きしていない（コミット済みの内容のまま）。
