# verify-1188.txt — issue #1188 実装の検証・レビュー・変異注入

対象 sha: c4cb443314bccd1f509c8e417693db59f8755532
worktree: C:/workspace/Snotra/.claude/worktrees/agent-ac35f0e259482f596
検証日: 2026-08-26

## 0. アンカー確認（実測）

- worktree の初期 HEAD = 9916cff2453e722f729ab46bf045343c45d04438（**アンカーと不一致**）
- `git checkout --detach c4cb4433` → HEAD = c4cb443314bccd1f509c8e417693db59f8755532（一致）
- `git status --porcelain` = 空（開始時・変異復元後の両方で確認）
- node_modules を junction で張った。`node_modules/vitest` の実在 = True

---

## 1. 検証（カテゴリ A〜F）

| カテゴリ | 判定 | コマンド | exit |
|---|---|---|---|
| A. Rust | **実行** | `cargo fmt --all -- --check` | 0 |
| A. Rust | **実行** | `cargo check --workspace` | 0 |
| A. Rust | **実行** | `cargo clippy --workspace --all-targets -- -D warnings` | 0 |
| A. Rust | **実行** | `cargo test -p snotra-core -q` | 0 |
| A. Rust | **実行** | `cargo doc --workspace --no-deps --document-private-items` | 0 |
| B. TypeScript | **該当なし** | `.ts` を 1 枚も触っていない（`git show --stat c4cb4433`） | — |
| C. 窓生成/表示順/hotkey/slash | **該当なし** | 該当経路を触らない | — |
| C. `npm test` | **実行**（`scripts/` 変更のため必須） | `npm test` | 0（38 files / 905 tests passed） |
| D. 目視 GUI | **該当なし** | 描画を 1 ピクセルも変えない（`build.rs` の変更は rustdoc 2 行の削除のみ）。`docs/build-commands.md` カテゴリ D の #1106 の注意（該当判定を広げる方向にも外れる）に照らして「該当」へ広げない | — |
| E. git hook | **該当なし** | `.githooks/**` を触っていない | — |
| F. ガバナンス文書 | **実行** | `npm run governance:check` | 0 |

**実行不能だったカテゴリは無い。**

`governance:check` の evidence 行（逐語）:
```
governance:check — 全検査 passed（検査 23 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 /
恒久規範 常時ロード 18661 字・rules 13560 字 / 見出し参照 365 件を md 48 件 + .rs 102 件 +
スクリプトのコメント 116 件から照合 / workspace member 4 件の lints opt-in / clippy 禁止 8 件 /
散文の識別子 420 件を 34 文書から照合 / 近傍の見出し参照 19 件 / 折れうる位置 20 件 /
コードスパン 18152 件 / reminder 表 10 行 / ADR 74 本の名前 / ADR の短縮引用 424 件）
```
→ コミットメッセージの「checked 365 / findings 0」「G-near 19 / G-folded 20 も不動」を独立に再現。

---

## 2. 肯定的に検算できたこと（すべて自分で実測）

1. **歴史データの主張を逐語で再現。**
   `node workspace/measure-heading-ref-nesting.mjs <repo> 'b93b0fb8^' <libUrl>` →
   `OLD checked=357 findings=0  NEW checked=359 findings=0` /
   `OLD にだけ在る = 0 件` / `NEW にだけ在る = 2 件`:
   - `RETROSPECTIVE.md ¦ 23 ¦ docs/comment-guidelines.md ¦ 第一原則: コメントは「なぜ」を書く ¦ true`
   - `snotra-core/src/indexer.rs ¦ 53 ¦ docs/comment-guidelines.md ¦ 第一原則: コメントは「なぜ」を書く ¦ true`
   → 「18 日間沈黙していた 2 件が赤を 1 件も出さずに照合済みへ上がる」が正しい。
2. **生きたツリー（c4cb4433）で同スクリプト** → `OLD checked=365 findings=0 NEW checked=365 findings=0`、
   集合差は両向き 0 件。「不動」「純粋に加算的」が正しい。
3. **`build.rs` から消した 2 行は消して正しい。** 実ツリーの `PERFORMANCE.md` に当てた実測:
   - 全形 `` `PERFORMANCE.md`「潰し済みかどうかを型で区別する（理由は「壊れるから」ではなく「無駄だから」）」 ``
     → NEW で **matches=1 / lands=true**（旧実装では一致すら生成されなかった形）
   - 切り詰め形も引き続き lands=true（前方一致）
   - `scanHeadingRefs(snap, ["snotra-core/src/search/build.rs"])` → checked=5 findings=0
   → 「切り詰めて書いてある理由」は本件で本当に偽になったので、**読者が失う情報は無い**。
4. **「生きた層の該当は build.rs の 1 件だけ」を独立に検算。**
   `git grep -n 鉤括弧 -- . ':!workspace/' ':!docs/adr/ADR-nested-quote-heading-ref-labels.md'` の残りは
   新設 fixture のコメント 2 行と `lib.mjs` の新 JSDoc 1 行のみ。
5. **破滅的バックトラックは無い。** 病的入力 4 種で線形域に留まる（下記 L-3 に数値）。
6. **SPEC.md 同期は不要。** ガバナンス検査の正規表現は `SPEC.md` の記述対象外（プロダクト仕様ではない）。
   `git show --stat` に `SPEC.md` は無い。判定は妥当。
7. **意図的に分けた構造 3 件（申し送り済み）を DRY 違反として指摘しない。** `REF_HEAD` / `HEADING_REF` の分離、
   `NEAR_REF` の手書き、`OPEN_UNCLOSED` の据え置きはいずれも根拠を確認したうえで是とする。

---

## 3. レビュー所見（重大度つき）

### [High] R-1 — 未宣言の沈黙経路: 対象綴りとして無効な「外側」が、内側の正当な参照を丸ごと飲み込む

**実測**（入力 1 行）:
```
`HEADING_REF`「外側の語 `docs/inner.md`「消えた見出し」の話」
```
| | 一致 | scanHeadingRefs |
|---|---|---|
| OLD | `target=docs/inner.md label=消えた見出し`（**内側**が拾われる） | checked=1 → 着地しないので**赤** |
| NEW | `target=HEADING_REF label=外側の語 `docs/inner.md`「消えた見出し」の話` | **checked=0 / findings=0（完全な沈黙）** |

機序: NEW は外側 1 件へ統合するが、`isRefTargetSpelling("HEADING_REF") === false`（実測）ゆえ
`G-heading-refs.mjs:49` の `continue` に落ち、**`checked` が加算される前に捨てられる**。
内側は既に消費されているので二度と見られない。

**なぜ重いか**: これは本 issue が消しに来た当の形——「照合していない」と「差分ゼロ」が区別できない——
の再生産である。しかも `checked` が 1 ではなく **0** なので、evidence 行からも読めない。

**宣言との食い違い**: JSDoc の死角 4 は「他の参照のラベルの内側に置かれた正準形参照——**外側 1 件へ統合され**、
独立には照合されない」と書く。この場合は統合先が **0 件**であり、宣言は機序を取り違えている。
`checked` が「見た」の証跡である以上、1 と 0 の差は装飾ではない。
fixture 4 は外側が**有効な綴り**の場合しか固定していない（`docs/c.md`）。

**露出**: 今日 0 件。`measure-heading-ref-nesting.mjs` を c4cb4433 に当てて `OLD にだけ在る = 0 件` を
独立に再現済み。ゆえに**潜在**であって実害は無い。形としては起こりうる
（`` `識別子`「規範の一文 `AGENTS.md`「見出し」…」 `` のような引用は本リポジトリの散文で自然）。

**対処の候補**（採否は主エージェント）: (a) 死角の宣言へ「外側の綴りが対象綴りでないとき、内側は
`checked` にも現れずに消える」を 1 行足す、(b) fixture を 1 件足して固定する、
(c) `isRefTargetSpelling` に外れた外側のラベルを再走査する（面積が増えるので推さない）。

### [High／依頼の定義では Critical] R-2 — JSDoc 死角 2 の括弧書きが実装より強い

対象: `lib.mjs` の
`- 入れ子かつ物理改行で折れた形（**折れ一般は `G-folded-heading-refs` が別に赤にする**）`

**実測**（4 通り）:
| 形 | 入れ子 | G-heading | G-folded |
|---|---|---|---|
| 形 B（`「` まで開いて次行へ流れる） | あり | checked=0 findings=0 | **checked=0 findings=0（沈黙）** |
| 形 B | なし（対照） | checked=0 findings=0 | checked=1 findings=1（赤） |
| 形 A（行末が対象綴り・次行が `「`） | あり | — | checked=1 findings=1（赤） |
| 形 A | なし（対照） | — | checked=1 findings=1（赤） |

機序: `G-folded-heading-refs.mjs:54` の `OPEN_UNCLOSED = REF_HEAD + 「[^「」\n]*$` は
ラベルの文字クラスが入れ子の `」` で切れるため、**形 B かつ入れ子**に一致しない。

**指摘の中身**: `OPEN_UNCLOSED` を広げないこと自体は申し送りどおり意図的なので**修正は求めない**。
問題は `lib.mjs` 側の括弧書きが「折れ**一般**は別が赤にする」という**安心を読者へ与える**ことである。
実際に赤になるのは「形 A（入れ子の有無を問わず）」と「形 B（入れ子なし）」であって、
**形 B かつ入れ子はどの検査も見ない**。

さらに: この形は `G-folded-heading-refs.mjs` 自身の**宣言する死角 3 つ**
（バッククォート跨ぎ／近傍形の折れ／`refScanLines` が次行を供給しない形）にも入っていない。
**2 ファイルのどちらの宣言にも無い形**である。

依頼は「doc が実装より強い主張をしていたら Critical」と定めているのでその定義では Critical。
ただし当の bullet 自身は「入れ子かつ折れ」を死角と**正しく名指している**ので、
私の判定は High とし、Critical/High の裁定は主エージェントへ委ねる。

**最小の直し方**: 括弧書きを「折れ一般」から「**形 A は入れ子でも `G-folded` が赤にする。
形 B かつ入れ子はどの検査も見ない**」へ具体化する（数え上げではなく形の名指しなので腐らない）。

### [Medium ⚠️] R-3 — JSDoc 死角 4 の不在断定が、外側が閉じない角で偽

**実測**: `` `docs/c.md`「外側 `docs/inner.md`「内側」だ ``（末尾の `」` 無し）
→ `checked=1 / findings=1`。finding は **内側**（`` `docs/inner.md`「内側」 ``）に対して出る。
つまり「独立には照合されない」がこの角では偽で、**独立に照合されている**。

弁護は成り立つ（外側が閉じないなら「他の参照」は存在しないので前提が成り立たない）。
向きも安全側（照合が増える）で実害は無い。ただし `AGENTS.md`「検証の作法（全タスク共通）」が
不在断定へ射程を要求しているので **⚠️** として挙げる。R-1 と合わせると、死角 4 の一文は
**両方向に射程が甘い**（外側が有効綴りのときしか「統合」は起きず、外側が閉じないときは統合が起きない）。

### [Low ⚠️] R-4 — ADR「受容する残余」の `G-near` 宣言が片側だけ

ADR は「近傍形（助詞などを挟んだ形）で入れ子の見出しを指す参照は、引き続き同検査から**不可視**である
（今日 0 件・実測）」とだけ書く。実測は 2 点で宣言とずれる。

(a) `` `docs/c.md` の「外「内」の続き」 `` → `G-near` は **checked=1 / findings=0**。
    「不可視」ではなく、**ラベル `内` という別物として一致が生成されている**（gap が `「外` まで伸びる）。
    結果（指摘が出ない）は宣言どおりだが、`checked` は「見た」を意味しないので語が機序を誤らせる。

(b) より重い向き: `` `docs/c.md` の「「概要」の話」 `` で `docs/c.md` に `## 概要` が在ると、
    `G-near` は **finding を出して「`docs/c.md`「概要」と書け」と誤った正準形を提案する**
    （外側のラベルを落とした形）。実測の finding 文言:
    `見出し参照が正準形でない（G-heading-refs の視界外）: `docs/c.md`【 の「】「概要」— `docs/c.md`「概要」と書く`

`G-near` は本件で 1 バイトも変わっていないので**回帰ではない**。指摘は ADR の残余宣言が
偽陽性の向きを持たないことについて。今日 0 件（`governance:check` の近傍 19 件 / findings 0）。

### [Low ⚠️] R-5 — `workspace/probe-heading-ref-blind-spots.mjs` の NOTES が出荷時点で 1 件ずれている

NOTES: 「2026-08-26 の実測では、workspace/ を除くと **深さ 2 以上 0 件**・閉じ不足 0 件・行内で閉じない 13 件」

同スクリプトを rev=c4cb4433 に当てた私の再実行:
- 深さ 2 以上 = **3 件**（`workspace/research.md` 2 件 + **`scripts/governance/checks/G-heading-refs.test.mjs:88` 1 件**）
  → workspace/ を除くと **1 件**（0 ではない）
- 行内で閉じない = 14 件（うち `workspace/plan.md` 1 件）→ 除くと 13 件 ✓ 一致
- その他（閉じ不足）= 1 件（すべて workspace/）→ 除くと 0 件 ✓ 一致

ずれの正体は**このコミット自身が足した深さ 2 の fixture** である。`AGENTS.md`「検証の作法（全タスク共通）」の
「数え上げは偽になる時点が確定している」の典型で、腐らせたのは**その数え上げを書いた当のコミット**。

**重要な区別（混同しないこと）**: `lib.mjs` の JSDoc が言う「宣言する死角（今日いずれも 0 件）」は**真である**。
`refScanLines` は `.mjs` でコメント行だけを供給し（実測: 全 118 行のうち **13 行**のみ・
88 行目はコード行なので供給されない）、fixture は照合母集団に入らない。
probe が「母集団を絞らない」超集合スキャンだからこの差が出る。**JSDoc は正しく、NOTES だけがずれている。**

`workspace/` は作業バッファなので影響は小さい。

### [Low ⚠️] R-6 — perf の数値が再現しない（向きは支持される）

commit message と `workspace/research.md` の表: 「入れ子 1 件を含む通常行 × 20 万回: 93.6 ms → **66.5 ms**」
→「通常行では NEW のほうが速い」。

私の再測定（**両側 warmup 済み**・OLD 先／NEW 先の 2 順序 × 3 ラウンド、実在の形の行で 20 万回）:
```
round0: OLD=68.5/69.4ms  NEW=68.5/69.3ms
round1: OLD=68.3/74.3ms  NEW=64.3/71.9ms
round2: OLD=66.9/65.6ms  NEW=64.6/64.2ms
```
→ **差はノイズの範囲**で、29% の改善は再現しない。向き（NEW ≤ OLD）は 6 組中 5 組で成立するので
「遅くなっていない」は支持される。**「むしろ速い」の程度が過大**で、93.6 ms は OLD 側を
warmup せずに測った値である可能性が高い（`ADR` 本文は数値を持たず「むしろ速い」だけなので、
腐るのは commit message と `research.md`）。

破滅的バックトラックが無いことは別途確認（5 回ぶんの合計）:
```
閉じない（あ×20000）      : OLD=0.47ms  NEW=1.13ms
「x」×5000                : OLD=0.31ms  NEW=0.49ms
「あ「い」」×5000          : OLD=0.43ms  NEW=0.29ms
深さ2 未閉（「「あ×20000）: OLD=1.55ms  NEW=0.27ms   ← NEW が 5.7× 速い
```
いずれも線形域。**指数的な爆発は 1 件も観測しない。**

### 宣言する死角 4 つ × 実装の突き合わせ（依頼の主眼・まとめ）

| # | 宣言 | 実装の挙動（実測） | 判定 |
|---|---|---|---|
| 1 | 深さ 2 以上の入れ子 | 深さ 2 / 深さ 3 とも `checked=0 findings=0`。対照の深さ 1 は `checked=1 findings=1` | **一致** |
| 2 | 入れ子かつ折れ（括弧書き「折れ一般は G-folded が赤にする」） | 形 B かつ入れ子は **G-heading も G-folded も沈黙**。形 A は入れ子でも赤 | **括弧書きが実装より強い → R-2** |
| 3 | ラベルが閉じない形 | `checked=0`。入れ子なしなら G-folded が拾う（`checked=1` 赤）／入れ子ありなら G-folded も沈黙 | **一致**（括弧書きが無いので過大主張なし） |
| 4 | 他の参照のラベルの内側の正準形参照 | 外側が有効綴りで閉じる → `checked=1 findings=0`（宣言どおり）／外側が閉じない → 内側が独立に赤（R-3）／外側が無効綴り → **`checked=0` で完全沈黙**（R-1） | **2 方向に射程が甘い** |

---

## 4. 変異注入（この worktree の中だけ・本体は無変更）

baseline SHA-256 `scripts/governance/lib.mjs` = `24524054EC6CF87A547060639D8CD00336FC9C01BB7717A38D5292B04FE257F1`
baseline `npx vitest run scripts/governance/checks/G-heading-refs.test.mjs` = **12 passed / exit 0**

### 変異 1 — 旧正規表現へ巻き戻し

巻き戻す文字列は手打ちせず `git show 'c4cb4433^:scripts/governance/lib.mjs'` から逐語で取った:
```js
export const HEADING_REF = new RegExp(`${REF_HEAD}「([^「」\\n]+)」`, "g");
```

結果: **3 failed / 9 passed（exit 1）**

**落ちた fixture（3 件・すべて #1188 で新設）**
| fixture | 期待 → 実測 |
|---|---|
| 見出し名が鉤括弧を入れ子に含んでも全形の参照が照合され着地する | `{findings:[], checked:1}` → `{findings:[], checked:0}` |
| 入れ子を含む見出しを改名すると finding（赤） | `length 1` → `length 0` |
| 他の参照のラベルの内側に置かれた正準形参照は独立には照合されない | `{findings:[], checked:1}` → `{findings:[1件], checked:1}` |

**生き残った fixture（9 件）**
- 既存 8 件が 1 件も落ちない → **非入れ子の挙動は完全に不変**（変更が純粋に加算的であることの直接の証拠）
- **新設 fixture のうち「深さ 2 以上の入れ子は一致を生成しない（宣言する死角）」だけが生き残る。**
  旧実装でも新実装でも `checked=0` だからである。**これは「何も守らない fixture」ではない**——変異 2 が証明する。

**追加の測定（重要）**: 変異 1 の下でも `npm run governance:check` は **exit 0**、
evidence 行は 1 文字も変わらない（365 / 19 / 20）。
→ **今日この変更を守っているのは fixture だけであり、生きたツリーは旧新を区別しない。**
ADR「この変更自身の劣化を見張るのは `G-heading-refs.test.mjs` の fixture である」を実測で裏づけた。

### 変異 2 — 深さ 2 まで受け入れる形へ広げる（死角の劣化）

```js
export const HEADING_REF = new RegExp(`${REF_HEAD}「((?:[^「」\\n]|「(?:[^「」\\n]|「[^「」\\n]*」)*」)+)」`, "g");
```
結果: **1 failed / 11 passed（exit 1）**。落ちたのは死角固定の fixture だけ:
```
expected { findings: [ { …(3) } ], checked: 1 } to deeply equal { findings: [], checked: +0 }
```
→ **`checked` と `findings` の両方が動いた。** 「指し先を着地しない見出しにしてある」設計が効いており、
`checked: 0` の主張は「見なかった」を一意に表せている。**fixture 3 は死角の固定として有効である。**

### 復元と照合

- `git checkout c4cb4433 -- scripts/governance/lib.mjs`
- SHA-256 = `24524054EC6CF87A547060639D8CD00336FC9C01BB7717A38D5292B04FE257F1`（**baseline と一致**）
- `git status --porcelain` = 空
- `npm test` = 38 files / **905 tests passed** / exit 0

本体 `C:/workspace/Snotra` へ書いたのは本ファイル `workspace/verify-1188.txt` のみ。
他の本体ファイルには一切触れていない。変異はすべて worktree 内。

---

## 5. 副次的な観察（情報）

- `workspace/probe-heading-ref-blind-spots.mjs` は **NUL バイトを 1 個含む**（実測）ため git が binary と
  判定し、`git show --stat` に `Bin 1954 -> 2543 bytes` と出る。中身は UTF-8 の正常な ESM で
  引数 3 つ（repo / rev / libUrl）を取り、正しく実行できる（provenance に問題は無い）。
  NUL は 25 行目の binary 判定 `text.includes("<NUL>")` を逐語で書いたもの。
  `"\u0000"` と書けば今後この足場の差分が読めるようになる（**Low・情報のみ**）。
- `workspace/plan.md` の未チェック `- [ ]` は 0 件（チェック済み 11 件）。`gh pr create` の hook は通る。
