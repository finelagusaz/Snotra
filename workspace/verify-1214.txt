issue #1214 検証・レビュー結果
================================

## 0. アンカー確認

git rev-parse HEAD（開始時）: 444805b79f5b65ea2008bf8dff211a7509fd7605（worktree はセッション開始時点の main 先頭で切られていた）
→ アンカー b8cbe328 と不一致のため `git checkout --detach b8cbe328680ad1e9cdda3a942c46682b51f84917` を実行
git rev-parse HEAD（検証時）: b8cbe328680ad1e9cdda3a942c46682b51f84917
アンカーと一致: 一致

以降の全測定はこの sha で実施した。

## 1. カテゴリ A〜F の検証（`docs/build-commands.md`「変更後の検証チェックリスト」SSOT）

変更ファイル: scripts/governance/checks/G-module-index.mjs / 同 .test.mjs /
docs/adr/ADR-module-index-reverse-scope.md（新規）/ AGENTS.md / docs/hooks.md /
.claude/skills/implement/SKILL.md / workspace/plan.md / workspace/research.md /
workspace/adversarial-1214.txt / workspace/injection-1214.txt / workspace/plan-review-module-index-scope.md

`.rs` は 1 行も変更していない。判定は拡張子ではなく変更が触れるコードパスの意味で行った。

| カテゴリ | 判定 | 根拠・実行結果 |
|---|---|---|
| A. Rust ファイル（*.rs） | 該当なし | 変更ファイルに `.rs` は 0 件（`git show --stat b8cbe328` で確認）。カテゴリ A のコマンド（fmt/check/clippy/test/doc）が対象とする成果物（rustc の型・lint・rustdoc）に触れる変更が無い |
| B. TypeScript（vitest.config.ts 等） | 該当なし | `vitest.config.ts` は変更していない。変更した `.mjs`（G-module-index.mjs / .test.mjs）はカテゴリ B の対象外だが、後述の「一般（npm test）」で実測済み |
| C. ウィンドウ生成／表示順・ホットキー・スラッシュコマンド | 該当なし | 変更はガバナンス検査（ドキュメント照合ロジック）のみで、Tauri window / hotkey / slash command の経路に触れていない |
| D. UI スタイル・レイアウト・テキスト表示 | 該当なし | UI の描画・レイアウトに影響する変更なし（`cargo run -p snotra` を目視する対象コードを変更していない） |
| E. git hook（.githooks/**） | 該当なし | `.githooks/**` を変更していない |
| F. ガバナンス文書（*.md・.claude/rules・.claude/skills・workflow） | 実行（governance:check, exit 0） | `npm run governance:check` を実行。結果: `全検査 passed（検査 23 件 / 対象文書 36 件 / rules 8 件 / skills 12 件 …）`。AGENTS.md・docs/hooks.md・.claude/skills/implement/SKILL.md・docs/adr/ADR-module-index-reverse-scope.md（新規）・workspace/*.md すべて対象文書に含まれる |
| 一般（scripts/governance/*.mjs の実装+テスト・PostToolUse は割り当て対象外）| 実行（npm test, exit 0） | `npm test`（vitest run）→ `Test Files 38 passed (38)` / `Tests 908 passed (908)`。G-module-index.test.mjs（新規 2 件含む）・edit-findings.test.mjs を含む全スイートが緑。`.mjs` は `post-edit.mjs` の `selectChecks` に割り当てが無く（CHECK_DEFINITION は .claude/settings.json / package.json / vitest.config.ts / Cargo.toml のみ）、PostToolUse hook は発火しない＝沈黙は「実施しなかった」と同義（AGENTS.md/CLAUDE.md の規約どおり）なので、手動で `npm test` を実行して埋めた |
| `scripts/manual-smoke.ps1`（目視項目） | 実行不能（人間へ引き渡し） | `Read-Host` で判定を採るためエージェントには実行できない。今回の変更は UI 描画に触れないため該当性自体が低いが、判定は人間へ委ねる |

worktree の node_modules: junction は張っていない（本体リポジトリ内側の worktree のため親を遡って解決。`npm test` が module 解決エラーなく完走したことで確認済み）。

## 2. レビュー所見（実装の正しさ / 計画判断の妥当性 / パフォーマンス）

### 実装の正しさ

- なし（Critical/High 無し）。`section.includes` への変更は素直で、`sectionOf` の失敗経路（見出し無し・複数見出し・非 ATX 見出し・フェンス不整合・`ending: "heading"` で終端見出し無し）はすべて `sec.body == null` を返し、`checkModuleIndex` は `findings.push(...sec.findings); continue;` で forward/reverse 両方をスキップする——例外を投げず、沈黙もしない（findings が必ず non-empty で返る）。既存の実装パターンを踏襲しており壊れていない。
- `moduleIndexCrateOf`（edit-findings.mjs）・`attributesTo` は本コミットで変更されていないが、`G-module-index.mjs` の finding メッセージが `実ファイル ${f} ...`（`f` は `production` の要素＝`moduleIndexSources` が返す相対パス）という逐語を保っているため、帰属は壊れていない。**実測で確認済み**（後述「3. 変異注入」）。
- 空の節（`section === ""`）で「全件が赤になる」というコメントの主張は `"".includes(x)` が常に `false` を返すことから成立する（production が非空であれば）。既存テスト「緑: 本文が空の節は『節が無い』ではない」がこの分岐を間接的に固定している。

### 計画判断の妥当性

- ADR（`ADR-module-index-reverse-scope.md`）内の数表は内部で算術的に整合している（自分で検算）:
  - 却下1の内訳表: 各行 `行頭が所有 + 集約行に載る + それ以外` が `実ファイル` 列と一致（59=31+0+28 / 12=12+0+0 / 45=37+7+1 / 15=6+9+0）。合計 131 も一致。
  - 却下3のフォールトインジェクション表: 各行 `赤+緑` が「索引行」列と一致（34=15+19=23+11 等）。`現行` 列の縦計 15+8+12+3=38、`案A` 列の縦計 23+9+12+6=50。差 12 は本文の「12 件多く捕まえ」と一致。
  - `workspace/injection-1214.txt` の個別変異ログ（style.rs 赤・autostart.rs 沈黙・mod忘れ→G-module-linkage 赤2件・tabs/集約行→赤8件・raster/renderer 相互参照→片方削除で沈黙・両方削除で赤2件）が ADR の表と 1 対 1 で一致する。
  - `workspace/adversarial-1214.txt` は計画段階で research.md の「32 件」という数字の誤り（表の内訳との不一致）を独立に検出しており、最終 ADR はこの誤りを含まない別の正しい数字（却下1の「それ以外 29 件」）を使っている——**計画段階の指摘が実際に反映されている**。
  - **⚠️ 未再現**: ADR の「86 本へ 1 本ずつ削る変異を全数へ注入」という全数フォールトインジェクション（却下3の表そのもの）は、コストの観点から自分では再実行していない。個別の抜き取り（`workspace/injection-1214.txt` の 6〜7 件）と現在の実木での `governance:check` 0 件（＝表の「現行ツリーへ当てたときに新たに赤くなるファイルは 0 件」の直接確認）は再現・確認したが、「案 A が現行より 12 件多く捕まえる」という差分の全数は算術的整合性と部分サンプルの一致からの間接検証にとどまる。
- 「意図的に閉じなかった死角」（同じ節内の別言及に救われる形）は ADR が却下理由（案 B の費用・テストモジュール 22 件の性格変更・basename 天井が別に存在）を明記しており、閉じない判断は妥当と判断した。反証は試みたが、却下理由を覆す根拠は見つからなかった。

### 規範3枚（AGENTS.md / docs/hooks.md / implement SKILL.md）

- 3枚とも、性質の記述を短く書き換え、詳細（射程・受容する残余）は `ADR-module-index-reverse-scope` を指すだけになっている——写しの増殖ではなく参照への一本化。「索引の reminder が鳴らなかったことを『`mod` も足りている』と読まない。索引そのものが足りている証拠にもならない」の直後の説明も、機序の詳細（判定述語の逐語）を書きつつ末尾で ADR を指しており、`.claude/rules/governance-docs.md`「機構の実装の詳細を散文へ写さない」の精神に沿っている。
- 全称表現・数え上げの機械チェック（`だけが|すべて|唯一|全て|全部|常に|必ず` で grep）を diff 全体に対して実施。ヒットした箇所はいずれも (a) 本コミットで変更されていない既存文（#1155 撤去節等）か、(b) 条件付き・下限主張として妥当（例: 「`mod` 忘れは今も `governance:check` だけが赤にする」は G-module-linkage が唯一の検知点であることを指しており、偽にする入力は無い＝正しい全称）。

### パフォーマンス

- 影響なし。`text.includes` → `section.includes` は探索対象文字列が短くなる方向の変更（`section` は `text` の部分文字列）であり、悪化しない。ループ構造・呼び出し回数に変更なし。

### ⚠️ その他の弱い所見

- ⚠️ `workspace/plan.md`（コミットに含まれる）の古い履歴的記述（旧メッセージ文言 `索引（本文のバッククォート）` を含む箇所が2箇所）は、計画の「実施前→実施後」の対比を書いた過去形の記録であり、governance:check（G-references 等）は 0 件で通っている。実害は無いが、将来この plan.md を「現状の説明」として誤読するリスクはゼロではない（ただし plan.md は本質的に作業記録であり、AGENTS.md の運用上问题とされる「写し」には当たらないと判断した）。

## 3. 変異注入

複製は使わず、worktree 内の実ファイルへ変異を当てて `npx vitest run` → 元に戻す、をハッシュで確認する形で実施した（本体リポジトリ C:/workspace/Snotra の作業ツリーは触れていない）。

### 変異1（今回の修正の巻き戻し）: 逆方向を `section` から `text` へ戻す

- ベースラインハッシュ（変異前）: `66a26fac306f5e48d89fae52bb7d7137142f2f88545f5183308cb5705dddfe9c` (scripts/governance/checks/G-module-index.mjs, sha256)
- 変異内容: `if (!section.includes(...) && !section.includes(...))` → `if (!text.includes(...) && !text.includes(...))`（105行目）
- 期待: 新設テスト「赤（逆方向）: 節の外の言及では緑にならない（照合先は `text` 全体ではなく「モジュール構成」節）」が赤になる
- 実際: `npx vitest run scripts/governance/checks/G-module-index.test.mjs` → `Tests 1 failed | 7 passed (8)`。失敗したテストは期待どおり当該1件のみ
- 赤の理由: `AssertionError: expected false to be true`（`f.some((x) => x.message.includes("search.rs"))` が false）——**検査の assert であり、実行時エラーやモジュール解決の失敗ではない**（出力で確認済み）
- 巻き戻し確認: 変異を revert 後、sha256 が `66a26fac306f5e48d89fae52bb7d7137142f2f88545f5183308cb5705dddfe9c` に一致（内容ハッシュで完全一致を確認）

### 変異2（帰属の沈黙経路の検知器）: finding メッセージから `実ファイル ${f} ` の逐語パスを外す

- ベースラインハッシュ: 変異1と同じ（`66a26fac...`、変異1を戻した直後に確認済み）
- 変異内容: `` `実ファイル ${f} が索引（「モジュール構成」節のバッククォート）に見当たらない` `` → `` `basename ${base} が索引（「モジュール構成」節のバッククォート）に見当たらない` ``（109行目。`f` の逐語パスを message から除去）
- 期待: `edit-findings.test.mjs` が赤になる（帰属フィルタ `attributesTo` が `message.indexOf(rel)` に依存するため）
- 実際: `npx vitest run scripts/governance/edit-findings.test.mjs` → `Tests 4 failed | 25 passed (29)`。失敗した4件:
  1. 「赤: 索引に載っていない `.rs` を編集すると、その1件だけが出る」— `expected [] to have a length of 1 but got +0`
  2. 「**帰属**: 索引漏れが2件あっても、編集した当のファイルの分だけが出る」— 同上
  3. 「**接頭辞で誤爆しない**: `a.rs` の編集が...」— 同上
  4. 「件数・対象・全件を見る再現コマンドを含む」— `expected '' to contain 'snotra-core/src/orphan.rs'`
- 赤の理由: 全件 `AssertionError`（`toHaveLength` / `toContain` の不一致）——**検査の assert であり、実行時エラーではない**。`attributesTo` が message 中に `rel`（フルパス）を見つけられず 0 件へ静かに倒れる、というドキュメント記載どおりの沈黙経路が実際に再現された
- 巻き戻し確認: 変異を revert 後、sha256 が `66a26fac306f5e48d89fae52bb7d7137142f2f88545f5183308cb5705dddfe9c` に一致

### 事後確認

- `git status --porcelain`: 追跡ファイルの変更は 0 件（`workspace/verify-1214.txt` の untracked のみ）
- `npm test` 再実行: `Test Files 38 passed (38)` / `Tests 908 passed (908)`（両変異とも完全に戻っていることを再確認）

## 4. 結論

- カテゴリ A〜F: 実行 2 件（F=governance:check, 一般=npm test）／該当なし 5 件（A/B/C/D/E）／実行不能 1 件（manual-smoke.ps1、人間へ引き渡し）
- レビュー所見: Critical 0 / High 0 / Medium 0 / Low 0 / ⚠️ 2（ADR全数フォールトインジェクションの非再現・plan.md の履歴的文言の残存、いずれも実害なしと判断）
- 変異注入2件: 両方とも期待どおり赤（assert 起因）。巻き戻しをハッシュで確認済み
